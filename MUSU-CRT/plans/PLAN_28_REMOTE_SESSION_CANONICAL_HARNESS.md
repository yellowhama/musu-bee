# PLAN 28 - Remote Session Canonical Harness

## 목표

`Slice 3 - remote session controller`를 `MUSU-CRT` 내부 canonical harness에서 self-contained proof로 올린다.

## 작업

1. remote session fixture 추가
2. canonical harness에 remote state panel 추가
3. attach / close 상태를 summary와 smoke script에 반영
4. 상태 문서를 `Slice 3 active` 기준으로 갱신

## 완료 기준

- remote session fixture 존재
- canonical harness에서 remote state 렌더
- smoke script가 remote checks를 통과
