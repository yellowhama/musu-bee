# musu-connects Audit 2026-04-02

## Findings

1. High: `PairingService`의 token 검증은 길이만 본다.
   - 파일: [pairing.rs](/home/hugh51/musu-functions/musu-connects/crates/musu-connects-core/src/application/pairing.rs)
   - 현재는 baseline으로는 괜찮지만, 실제 인증으로 쓰면 위험하다.

2. Medium: `ImportedRouteRegistry::merge`의 dedupe 기준이 `import_id` 하나뿐이다.
   - 파일: [registries.rs](/home/hugh51/musu-functions/musu-connects/crates/musu-connects-core/src/domain/registries.rs)
   - origin peer 변경, alias collision, stale import 처리 정책이 더 필요하다.

3. Medium: `InMemoryDiscoveredPeerRegistry`는 stale peer eviction이 없다.
   - 파일: [discovery.rs](/home/hugh51/musu-functions/musu-connects/crates/musu-connects-core/src/application/discovery.rs)
   - provider에서 사라진 peer가 계속 남을 수 있다.

4. Low: `quinn` dependency는 추가됐지만 실제 endpoint open/dial 코드는 아직 없다.
   - 파일: [musu-connects-core/Cargo.toml](/home/hugh51/musu-functions/musu-connects/crates/musu-connects-core/Cargo.toml)
   - 현 단계에선 의도된 상태다.

## No Findings

- 현재 baseline 구조 자체를 막는 컴파일/테스트 레벨 문제는 보이지 않는다.
