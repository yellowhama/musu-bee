## process snapshot
hugh51   80498 74149 33 04:00 ?        00:00:00 /bin/bash -c set -euo pipefail TS=$(date +%Y%m%dT%H%M%S%z) EVID=/home/hugh51/musu-functions/artifacts/mus1688-investigate-${TS} mkdir -p "$EVID" {   echo "created_at=$(date -Is)"   echo "run_id=${PAPERCLIP_RUN_ID:-}"   echo "task_id=${PAPERCLIP_TASK_ID:-}" } > "$EVID/metadata.txt"  (   echo '## process snapshot'   ps -ef | rg 'next (dev|start|build)|node.*next' || true ) > "$EVID/process_snapshot.txt" 2>&1  (   cd /home/hugh51/musu-functions/musu-bee   echo '## env + versions'   node -v   npm -v   npx --yes next --version ) > "$EVID/env_snapshot.txt" 2>&1  # single contract run (   cd /home/hugh51/musu-functions/musu-bee   set +e   rm -rf .next   npm run build > "$EVID/build_single.log" 2>&1   B=$?   npm run typecheck > "$EVID/typecheck_single.log" 2>&1   T=$?   npx --yes tsx --test src/app/brand-tokens.test.ts src/app/landing-exp/page.module.test.ts src/app/landing-exp/page.contract.test.ts > "$EVID/tests_single.log" 2>&1   S=$?   set -e   printf 'build_single_exit=%s\ntypecheck_single_exit=%s\ntests_single_exit=%s\n' "$B" "$T" "$S" > "$EVID/contract_single.exit" )  # repeated cold-build loop (   cd /home/hugh51/musu-functions/musu-bee   set +e   : > "$EVID/build_loop_summary.txt"   for i in 1 2 3 4 5; do     rm -rf .next     npm run build > "$EVID/build_loop_${i}.log" 2>&1     code=$?     echo "loop_${i}_exit=${code}" >> "$EVID/build_loop_summary.txt"     if [ "$code" -ne 0 ]; then       echo "--- loop_${i} tail ---" >> "$EVID/build_loop_summary.txt"       tail -n 40 "$EVID/build_loop_${i}.log" >> "$EVID/build_loop_summary.txt"     fi   done   set -e )  # runtime smoke only if build single passed (   cd /home/hugh51/musu-functions/musu-bee   set +e   B=$(grep '^build_single_exit=' "$EVID/contract_single.exit" | cut -d= -f2)   if [ "$B" = "0" ]; then     NODE_ENV=production ./node_modules/.bin/next start --hostname 127.0.0.1 --port 3222 > "$EVID/runtime_start.log" 2>&1 &     PID=$!     ok=0     for _ in $(seq 1 40); do       if curl -sSf http://127.0.0.1:3222/landing-exp >/dev/null 2>&1; then ok=1; break; fi       sleep 1     done     {       echo "landing-exp $(curl -s -o /dev/null -w '%{http_code}' http://127.0.0.1:3222/landing-exp)"       echo "landing $(curl -s -o /dev/null -w '%{http_code}' http://127.0.0.1:3222/landing)"       echo "pricing $(curl -s -o /dev/null -w '%{http_code}' http://127.0.0.1:3222/pricing)"       echo "install $(curl -s -o /dev/null -w '%{http_code}' http://127.0.0.1:3222/install)"       echo "faq $(curl -s -o /dev/null -w '%{http_code}' http://127.0.0.1:3222/faq)"       echo "pro $(curl -s -o /dev/null -w '%{http_code}' http://127.0.0.1:3222/pro)"     } > "$EVID/runtime_smoke.log"     kill "$PID" >/dev/null 2>&1 || true     wait "$PID" >/dev/null 2>&1 || true     echo "runtime_smoke_exit=0" > "$EVID/runtime_smoke.exit"   else     echo "runtime_smoke_exit=skipped_build_failed" > "$EVID/runtime_smoke.exit"   fi   set -e )  # token checks (qa/review style) (   cd /home/hugh51/musu-functions   {     echo '# brand hex in globals+landing-exp only'     rg -n '#2D1D19|#FFD166|#FDFCF0' musu-bee/src/app/globals.css musu-bee/src/app/landing-exp || true     echo '# forbidden brand hex outside globals (tests excluded)'     rg -n '#2D1D19|#FFD166|#FDFCF0' musu-bee/src/app -g '!**/*.test.ts' -g '!**/*.test.tsx' -g '!**/*.spec.ts' -g '!**/*.spec.tsx' -g '!globals.css' || true   } ) > "$EVID/token_checks.txt" 2>&1  printf '%s\n' "$EVID"
hugh51   80504 80498  0 04:00 ?        00:00:00 /bin/bash -c set -euo pipefail TS=$(date +%Y%m%dT%H%M%S%z) EVID=/home/hugh51/musu-functions/artifacts/mus1688-investigate-${TS} mkdir -p "$EVID" {   echo "created_at=$(date -Is)"   echo "run_id=${PAPERCLIP_RUN_ID:-}"   echo "task_id=${PAPERCLIP_TASK_ID:-}" } > "$EVID/metadata.txt"  (   echo '## process snapshot'   ps -ef | rg 'next (dev|start|build)|node.*next' || true ) > "$EVID/process_snapshot.txt" 2>&1  (   cd /home/hugh51/musu-functions/musu-bee   echo '## env + versions'   node -v   npm -v   npx --yes next --version ) > "$EVID/env_snapshot.txt" 2>&1  # single contract run (   cd /home/hugh51/musu-functions/musu-bee   set +e   rm -rf .next   npm run build > "$EVID/build_single.log" 2>&1   B=$?   npm run typecheck > "$EVID/typecheck_single.log" 2>&1   T=$?   npx --yes tsx --test src/app/brand-tokens.test.ts src/app/landing-exp/page.module.test.ts src/app/landing-exp/page.contract.test.ts > "$EVID/tests_single.log" 2>&1   S=$?   set -e   printf 'build_single_exit=%s\ntypecheck_single_exit=%s\ntests_single_exit=%s\n' "$B" "$T" "$S" > "$EVID/contract_single.exit" )  # repeated cold-build loop (   cd /home/hugh51/musu-functions/musu-bee   set +e   : > "$EVID/build_loop_summary.txt"   for i in 1 2 3 4 5; do     rm -rf .next     npm run build > "$EVID/build_loop_${i}.log" 2>&1     code=$?     echo "loop_${i}_exit=${code}" >> "$EVID/build_loop_summary.txt"     if [ "$code" -ne 0 ]; then       echo "--- loop_${i} tail ---" >> "$EVID/build_loop_summary.txt"       tail -n 40 "$EVID/build_loop_${i}.log" >> "$EVID/build_loop_summary.txt"     fi   done   set -e )  # runtime smoke only if build single passed (   cd /home/hugh51/musu-functions/musu-bee   set +e   B=$(grep '^build_single_exit=' "$EVID/contract_single.exit" | cut -d= -f2)   if [ "$B" = "0" ]; then     NODE_ENV=production ./node_modules/.bin/next start --hostname 127.0.0.1 --port 3222 > "$EVID/runtime_start.log" 2>&1 &     PID=$!     ok=0     for _ in $(seq 1 40); do       if curl -sSf http://127.0.0.1:3222/landing-exp >/dev/null 2>&1; then ok=1; break; fi       sleep 1     done     {       echo "landing-exp $(curl -s -o /dev/null -w '%{http_code}' http://127.0.0.1:3222/landing-exp)"       echo "landing $(curl -s -o /dev/null -w '%{http_code}' http://127.0.0.1:3222/landing)"       echo "pricing $(curl -s -o /dev/null -w '%{http_code}' http://127.0.0.1:3222/pricing)"       echo "install $(curl -s -o /dev/null -w '%{http_code}' http://127.0.0.1:3222/install)"       echo "faq $(curl -s -o /dev/null -w '%{http_code}' http://127.0.0.1:3222/faq)"       echo "pro $(curl -s -o /dev/null -w '%{http_code}' http://127.0.0.1:3222/pro)"     } > "$EVID/runtime_smoke.log"     kill "$PID" >/dev/null 2>&1 || true     wait "$PID" >/dev/null 2>&1 || true     echo "runtime_smoke_exit=0" > "$EVID/runtime_smoke.exit"   else     echo "runtime_smoke_exit=skipped_build_failed" > "$EVID/runtime_smoke.exit"   fi   set -e )  # token checks (qa/review style) (   cd /home/hugh51/musu-functions   {     echo '# brand hex in globals+landing-exp only'     rg -n '#2D1D19|#FFD166|#FDFCF0' musu-bee/src/app/globals.css musu-bee/src/app/landing-exp || true     echo '# forbidden brand hex outside globals (tests excluded)'     rg -n '#2D1D19|#FFD166|#FDFCF0' musu-bee/src/app -g '!**/*.test.ts' -g '!**/*.test.tsx' -g '!**/*.spec.ts' -g '!**/*.spec.tsx' -g '!globals.css' || true   } ) > "$EVID/token_checks.txt" 2>&1  printf '%s\n' "$EVID"
hugh51   80506 80504  0 04:00 ?        00:00:00 rg next (dev|start|build)|node.*next
