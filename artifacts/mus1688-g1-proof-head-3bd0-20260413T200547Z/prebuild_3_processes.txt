401 sh -c NODE_ENV=production next build
402 node /home/hugh51/musu-functions/musu-bee/node_modules/.bin/../.pnpm/next@15.5.14_@playwright+test@1.59.1_react-dom@19.2.4_react@19.2.4__react@19.2.4/node_modules/next/dist/bin/next build
76372 /bin/bash -c set -euo pipefail ART="/home/hugh51/musu-functions/artifacts/mus1688-g1-proof-head-3bd0-$(date -u +%Y%m%dT%H%M%SZ)" mkdir -p "$ART" cd /home/hugh51/musu-functions/musu-bee  echo "$ART" | tee "$ART/artifact_dir.txt" date -u +"%Y-%m-%dT%H:%M:%SZ" | tee "$ART/started_at_utc.txt" git rev-parse HEAD | tee "$ART/head_sha.txt" git show --stat --name-status --oneline -1 > "$ART/head_commit_show.txt" git status --short > "$ART/git_status_short.txt"  echo "== targeted tests ==" | tee "$ART/step_targeted_tests.txt" set +e npx --yes tsx --test src/app/brand-tokens.test.ts src/app/landing-exp/page.module.test.ts src/app/landing-exp/page.contract.test.ts > "$ART/targeted_tests.log" 2>&1 TEST_EXIT=$? set -e printf "%s\n" "$TEST_EXIT" > "$ART/targeted_tests.exit"  echo "== typecheck ==" | tee "$ART/step_typecheck.txt" set +e npm run typecheck > "$ART/typecheck.log" 2>&1 TYPE_EXIT=$? set -e printf "%s\n" "$TYPE_EXIT" > "$ART/typecheck.exit"  echo "== cold build replay x5 ==" | tee "$ART/step_build_replay.txt" for i in 1 2 3 4 5; do   rm -rf .next   pgrep -af "next build|next dev|next start -p 3311" > "$ART/prebuild_${i}_processes.txt" || true   set +e   NODE_ENV=production npm run build > "$ART/build_${i}.log" 2>&1   BUILD_EXIT=$?   set -e   printf "%s\n" "$BUILD_EXIT" > "$ART/build_${i}.exit"   echo "build_${i} exit=${BUILD_EXIT}" | tee -a "$ART/build_replay_summary.txt" done  echo "== runtime smoke /landing-exp ==" | tee "$ART/step_runtime_smoke.txt" PORT=3311 node ./node_modules/next/dist/bin/next start -p "$PORT" > "$ART/runtime_start.log" 2>&1 & SERVER_PID=$! printf "%s\n" "$SERVER_PID" > "$ART/runtime_start.pid" for i in $(seq 1 30); do   if curl -fsS "http://127.0.0.1:${PORT}/landing-exp" -o "$ART/runtime_landing_exp.html"; then     break   fi   sleep 1 done if [ ! -s "$ART/runtime_landing_exp.html" ]; then   echo "runtime_smoke_failed" > "$ART/runtime_smoke.status"   kill "$SERVER_PID" >/dev/null 2>&1 || true   wait "$SERVER_PID" 2>/dev/null || true   exit 1 fi curl -sS -o /dev/null -w "%{http_code}\n" "http://127.0.0.1:${PORT}/landing-exp" > "$ART/runtime_landing_exp.status_code" rg -n "MUSU Early Access|Try MUSU Early Access|control\.musu" "$ART/runtime_landing_exp.html" > "$ART/runtime_landing_exp.content_markers.log" || true kill "$SERVER_PID" >/dev/null 2>&1 || true wait "$SERVER_PID" 2>/dev/null || true echo "runtime_smoke_ok" > "$ART/runtime_smoke.status"  echo "== token evidence ==" | tee "$ART/step_token_evidence.txt" rg -n "#2D1D19|#FFD166|#FDFCF0|#F8F6F1|#2d1d19|#ffd166|#fdfcf0|#f8f6f1" src/app/landing-exp/page.module.css > "$ART/landing_exp_raw_hex_scan.log" 2>&1 || true rg -n "var\(--musu-color-brand-(ink|accent|canvas|stroke)\)|var\(--musu-color-brand-(ink|accent|canvas)-rgb\)" src/app/landing-exp/page.module.css > "$ART/landing_exp_token_scan.log" 2>&1 || true  git show -- src/app/brand-tokens.test.ts src/app/landing-exp/page.tsx src/app/landing-exp/page.module.css src/app/landing-exp/page.module.test.ts src/app/landing-exp/page.contract.test.ts > "$ART/mus1688_scope.diff"  date -u +"%Y-%m-%dT%H:%M:%SZ" | tee "$ART/finished_at_utc.txt" printf "ARTIFACT_DIR=%s\n" "$ART"
89507 /bin/bash -c set -euo pipefail ART="/home/hugh51/musu-functions/artifacts/mus1688-g1-proof-head-3bd0-rerun-$(date -u +%Y%m%dT%H%M%SZ)" mkdir -p "$ART" cd /home/hugh51/musu-functions/musu-bee  echo "$ART" | tee "$ART/artifact_dir.txt" date -u +"%Y-%m-%dT%H:%M:%SZ" | tee "$ART/started_at_utc.txt" git rev-parse HEAD | tee "$ART/head_sha.txt" git show --stat --name-status --oneline -1 > "$ART/head_commit_show.txt" git status --short > "$ART/git_status_short.txt"  echo "== targeted tests ==" | tee "$ART/step_targeted_tests.txt" set +e npx --yes tsx --test src/app/brand-tokens.test.ts src/app/landing-exp/page.module.test.ts src/app/landing-exp/page.contract.test.ts > "$ART/targeted_tests.log" 2>&1 TEST_EXIT=$? set -e printf "%s\n" "$TEST_EXIT" > "$ART/targeted_tests.exit"  echo "== typecheck ==" | tee "$ART/step_typecheck.txt" set +e npm run typecheck > "$ART/typecheck.log" 2>&1 TYPE_EXIT=$? set -e printf "%s\n" "$TYPE_EXIT" > "$ART/typecheck.exit"  echo "== cold build replay x5 ==" | tee "$ART/step_build_replay.txt" for i in 1 2 3 4 5; do   rm -rf .next   pgrep -af "next build|next dev|next start -p 3311" > "$ART/prebuild_${i}_processes.txt" || true   set +e   NODE_ENV=production npm run build > "$ART/build_${i}.log" 2>&1   BUILD_EXIT=$?   set -e   printf "%s\n" "$BUILD_EXIT" > "$ART/build_${i}.exit"   echo "build_${i} exit=${BUILD_EXIT}" | tee -a "$ART/build_replay_summary.txt" done  echo "== runtime smoke /landing-exp ==" | tee "$ART/step_runtime_smoke.txt" PORT=3311 node ./node_modules/next/dist/bin/next start -p "$PORT" > "$ART/runtime_start.log" 2>&1 & SERVER_PID=$! printf "%s\n" "$SERVER_PID" > "$ART/runtime_start.pid" for i in $(seq 1 30); do   if curl -fsS "http://127.0.0.1:${PORT}/landing-exp" -o "$ART/runtime_landing_exp.html"; then     break   fi   sleep 1 done if [ ! -s "$ART/runtime_landing_exp.html" ]; then   echo "runtime_smoke_failed" > "$ART/runtime_smoke.status"   kill "$SERVER_PID" >/dev/null 2>&1 || true   wait "$SERVER_PID" 2>/dev/null || true   exit 1 fi curl -sS -o /dev/null -w "%{http_code}\n" "http://127.0.0.1:${PORT}/landing-exp" > "$ART/runtime_landing_exp.status_code" rg -n "MUSU Early Access|Try MUSU Early Access|control\.musu" "$ART/runtime_landing_exp.html" > "$ART/runtime_landing_exp.content_markers.log" || true kill "$SERVER_PID" >/dev/null 2>&1 || true wait "$SERVER_PID" 2>/dev/null || true echo "runtime_smoke_ok" > "$ART/runtime_smoke.status"  echo "== token evidence ==" | tee "$ART/step_token_evidence.txt" rg -n "#2D1D19|#FFD166|#FDFCF0|#F8F6F1|#2d1d19|#ffd166|#fdfcf0|#f8f6f1" src/app/landing-exp/page.module.css > "$ART/landing_exp_raw_hex_scan.log" 2>&1 || true rg -n "var\(--musu-color-brand-(ink|accent|canvas|stroke)\)|var\(--musu-color-brand-(ink|accent|canvas)-rgb\)" src/app/landing-exp/page.module.css > "$ART/landing_exp_token_scan.log" 2>&1 || true  git show -- src/app/brand-tokens.test.ts src/app/landing-exp/page.tsx src/app/landing-exp/page.module.css src/app/landing-exp/page.module.test.ts src/app/landing-exp/page.contract.test.ts > "$ART/mus1688_scope.diff"  date -u +"%Y-%m-%dT%H:%M:%SZ" | tee "$ART/finished_at_utc.txt" printf "ARTIFACT_DIR=%s\n" "$ART"
