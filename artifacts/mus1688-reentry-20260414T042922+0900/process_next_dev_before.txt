31294 /bin/bash -c set -euo pipefail ROOT=/home/hugh51/musu-functions APP=$ROOT/musu-bee RUN=cf4679e3-f59b-43a9-aaee-c552c4dd4668 TS=$(date +%Y%m%dT%H%M%S%z) ART=$ROOT/artifacts/mus1688-reentry-${TS} mkdir -p "$ART" {   echo "run_id=$RUN"   echo "timestamp=$TS"   echo "pwd=$APP"   echo "node=$(node -v)"   echo "npm=$(npm -v)" } > "$ART/meta.txt"  pgrep -af "next dev" > "$ART/process_next_dev_before.txt" || true  cd "$APP" STATUS_FILE="$ART/build_replay_status.tsv" echo -e "iteration\texit_code\tseconds\tlog" > "$STATUS_FILE"  for i in 1 2 3 4 5; do   rm -rf .next   LOG="$ART/build_run_${i}.log"   START=$(date +%s)   set +e   npm run build > "$LOG" 2>&1   EC=$?   set -e   END=$(date +%s)   DUR=$((END-START))   echo -e "$i\t$EC\t$DUR\t$(basename "$LOG")" | tee -a "$STATUS_FILE"   if [ "$EC" -ne 0 ]; then     echo "build_failed_at_iteration=$i" > "$ART/failure_marker.txt"     break   fi done  pgrep -af "next dev" > "$ART/process_next_dev_after.txt" || true  cat "$STATUS_FILE" echo "ARTIFACT_DIR=$ART"
