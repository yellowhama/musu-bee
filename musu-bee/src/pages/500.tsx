export default function Custom500() {
  return (
    <div style={{ display: "flex", height: "100vh", alignItems: "center", justifyContent: "center", background: "#0d0d0d", color: "#6b7280", fontFamily: "sans-serif" }}>
      <span>500 — Internal server error.</span>
    </div>
  );
}
