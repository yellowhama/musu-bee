# Next Steps After Current External Gate Snapshot

## Current State

Current clean HEAD `0ba26d6d27a23a213240962517079d5fd817c7e8` has live public
metadata passing and current local packaged evidence passing on `HUGH_SECOND`.
Public release remains blocked by second-PC, hosted P2P, support mailbox, and
Store evidence.

## Next Actions

1. Install or run this current MUSU build on a second Windows machine.
2. Rerun the second-PC release kit and import successful route, desktop-open CPU,
   full runtime matrix, and process attribution evidence.
3. Log in the packaged runtime against production `https://musu.pro`.
4. Provision production KV / Upstash storage for owner-scoped relay leases and
   payload proof records.
5. Implement the distinct release `quic_relay_tunnel` payload endpoint and keep
   the preview store-forward queue marked non-release-grade.
6. Capture hosted route evidence with `route_kind=relay`, actual relay transport
   proof, and payload delivery proof.
7. Record operator-verified `musu@musu.pro` delivery evidence.
8. Record Partner Center product reservation, app submission, Microsoft
   certification, and restricted capability approval evidence.

## Non-Goals

- Do not use the localhost developer dashboard to satisfy packaged runtime
  evidence.
- Do not move execution from MUSU Desktop into MUSU.PRO.
- Do not count preview store-forward queue delivery as release tunnel delivery.
- Do not mark hosted P2P ready until relay route transport proof and payload
  delivery proof counts are both greater than zero.
