# V28_NEXT_STEPS_2026_05_26

## 1. Wire Up SSE Payloads for AI Commands
- Currently the `musu-rs` bridge receives the `/api/tasks/forward` command and triggers `task_runner`.
- **Next Step**: Map explicit AI tool payloads back to the `overlayWidgets` array in `useFleetStore`. When an agent "executes a command", the output should immediately manifest as a Holographic Component on the infinite canvas.

## 2. P2P Delegation / Group Chat implementation
- The `AgentPage` currently supports 1:1 interactions.
- We need to implement `@mentions` to summon other agents into the space (e.g. `CEO` delegating to `Dev`).

## 3. Persistent Agent Context
- Hook up `musu-rs` SQLite/Memory module to track agent-specific context per `/dashboard/agent/[id]`. The CEO agent must remember what it discussed with the user across sessions.
