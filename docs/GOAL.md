# GOAL — musu autonomous /loop source of truth

**Wiki ID**: wiki/485
**Created**: 2026-05-19
**Last reshape**: 2026-05-20 (V24-Rust-big-bang → R-fast/R-cleanup phased → 2026-05-20 evening pivot: single-machine first, R9 deferred per operator "이 기계에서 무수 다른기능-멀티기기 말고 다 되는지")
**Updates**: append-only revision history at bottom
**Owner**: autonomous /loop orchestrator (Claude Opus 4.7 1M context). Per user 2026-05-20: "니가 결정, 나는 고객임, 니가 사장임" — orchestrator is 사장 (decides closure), user is customer (rejection right + AS duty owed back).
**User**: emptyermind@gmail.com

This is the single source of truth for the autonomous /loop. Future Claude sessions read this first. Goals are **explicit + dated + measurable**. Section A is current cycle goal. Sections B–D are reference state (branch / gates / deferred).

**Current operating pointer (2026-06-01)**: the historical V24/V26 planning chain remains useful context, but the live beta-readiness state is now `1.15.0-rc.1`. Future sessions should start with `docs/RELEASE_1_15_0_RC1_CURRENT_STATUS_AUDIT_2026_05_31.md` (wiki/522), `docs/RELEASE_1_15_0_RC1_RUNTIME_HARDENING_RELAY_ROADMAP_2026_05_31.md` (wiki/523), `docs/MUSU_RUNTIME_STABILIZATION_EXECUTION_PLAN_2026_05_31.md` (wiki/525), `docs/MSIX_DESKTOP_ENTRYPOINT_AUDIT_2026_05_31.md` (wiki/526), `docs/RELEASE_1_15_0_RC1_QUAL_AUDIT_NEXT_STEPS_2026_06_01.md` (wiki/527), `docs/BETA_RELEASE_CHECKLIST_1_15_0_RC1.md`, and this file's revision history before resuming roadmap work.

---

## §A — Current cycle goal: V24-Rust-cleanup (panel-reshaped 2026-05-20)

### A.0 The decision (post-panel YELLOW reshape)

**Initial draft (REJECTED by Phase -1 panel YELLOW, 4 HIGH)**: "Big-bang Rust rewrite — Python 25k 통째로 Rust로. 4-6주. 동안 musu = down."

**Panel-reshaped (user accepted all 4 HIGH 2026-05-20)**: **Two-phase Rust cleanup release**.

- **R-fast (~2 weeks → JTBD ships)**: R0 + R1 (bridge module) + R2 (core module) + R7 (musu-bee wire) + R8 (4060Ti E2E) + R9 (5070Ti cross-machine). Rust auth + bridge live on both machines; land-os + vibecode-town companies registered; one cross-machine task routes. Python still alive at port 8071 serving control/indexer/writer behind Rust auth facade.
- **R-cleanup (~2-4 weeks parallel to operator dogfood)**: R3 (control module) → R4 (indexer module) → R5 (writer module) → R6 (installer rewrite) → R10 (Python deletion + final closure). Each step removes one Python service from facade.
- **End state identical to original big-bang**: Python fully deleted, all musu Rust. But JTBD ships day ~14, not day 28-42.

**WHY** (panel HIGH-1 + HIGH-2 acceptance):
1. User instruction "백엔드로 파이썬 쓰지 말라고" still load-bearing. Python ban applies to NEW code; existing Python tolerated behind Rust facade ONLY during R-cleanup window.
2. HIGH-1 Christensen: original big-bang inverted JTBD priority. R1+R2 subset is already incremental (7 endpoints, fresh schema, 10-20 tools) — language change doesn't make it big-bang.
3. HIGH-2 Taleb: 4-6 weeks single window = fragile (4060Ti hardware, Windows reinstall, rmcp/tantivy yank, GitHub outage, operator emergency all reset clock).
4. Thesis lock from SSOT_1PAGE_2026-04-09 holds: 4-layer (CoS / machines / companies / agents) on 4060Ti + 5070Ti.

**STACK** (panel MED-1 reshape): **single binary crate `musu-rs`** with internal modules (`bridge`, `core`, `control`, `indexer`, `writer`). NOT 5 separate crates. tokio + axum + sqlx (or rusqlite) base; per-module deps audited at R0 per MED-2. Saves ~28 subagent-phase invocations.

**VERSION** (panel HIGH-3 + LOW-1): **1.14.0** at V24 close, NOT 2.0.0. V24 is engineering cleanup with no Create dimension (no user-facing capability that exists *because of* Rust). 2.0.0 would oversell engineering work as strategic milestone.

**ACCEPTANCE METRIC** (panel HIGH-4, ungameable Goodhart firewall): §9.12 in wiki/490 — operator-attested text reply OR operator git commit in land-os/vibecode-town within 7 days of R10 confirming ≥3 named real tasks completed via musu. Orchestrator MAY NOT mark V24 §9.11 (GOAL.md §A complete) until §9.12 exists in wiki/500 closure HTML. Structurally ungameable: orchestrator cannot fake operator terminal history or git authorship signature.

**Reshape lock semantics** (panel MED-3 resolved by user 2026-05-20): "reshape 금지" covers ORCHESTRATOR-initiated mid-execution reshape proposals during R1-R10 only. Phase -1 panel HIGH findings DO trigger plan reshape (standard agent-team interpretation per MODE_Agent_Team.md). This reshape (panel-driven) is explicitly allowed; future orchestrator-initiated reshapes during execution are NOT.

### A.1 Day-2 acceptance (operator-defined + panel-strengthened)

**Acceptance bar = D (cross-machine) + §9.12 (operator-attested)**:
1. ✅ Installer succeeds end-to-end on 4060Ti from clean shell (R-fast end, ~day 14)
2. ✅ Installer succeeds on 5070Ti (R-fast end)
3. ✅ `F:\Aisaak\Projects\land-os` registered as musu company "land-os 개발팀"
4. ✅ `F:\Aisaak\Projects\vibecode-town` registered as musu company
5. ✅ `/api/nodes/add` mesh: both `nodes.toml` mention each other
6. ✅ Cross-machine task: one task delegated 4060Ti↔5070Ti; audit_log on receiving machine shows arrival; result returns
7. ✅ audit_log has ≥ 5 rows where `actor_ip != 'testclient'` (gameable per HIGH-4 — backstopped by §9.12)
8. ✅ V24 final closure HTML wiki/500 + qual eval committed
9. ✅ **§9.12 ungameable closure metric**: operator-attested text reply in V24 closure thread OR operator git commit in land-os/vibecode-town within 7 days of R10 confirming ≥3 named real tasks completed via musu. Reproducible from operator's own terminal history or git log. **Orchestrator MAY NOT mark V24 GOAL.md complete until §9.12 written by operator.** Structurally ungameable.

Closure decision shape:
- Items 1-8: orchestrator (사장) declares done per acceptance evidence.
- Item 9 (§9.12): operator-authored, not orchestrator-asserted. This is the Goodhart firewall.
- Until §9.12 exists, V24 stays "code shipped, acceptance pending" — not closed.

### A.1.1 Pivot 2026-05-20 evening: single-machine completeness before R9

**Trigger**: R8 PASS on 4060Ti (wiki/498c, commit `0d58f08`). Operator follow-up: "이 기계에서 무수 다른기능-멀티기기 말고 다 되는지. 자동 업데이트 기능 깉은 편의성 기능 있는지". Probe found musu-rs surface is bridge + DB + auth + audit + dedup + company CRUD only; every other Python endpoint (`/api/system/update`, `/api/agents`, `/api/instructions`, `/api/briefing`, `/api/sse`, `/api/wiki/*`, `/api/workflows`, `/api/templates`, `/api/system/backup`, `/api/system/restart`) returns 502 because Python facade target is gone after `~/.musu` delete.

**Reorder** (sequence change, NOT scope change — same R-cleanup sub-WS set):
- **OLD**: R3 (control/MCP) → R4 (indexer) → R5 (writer) → R6 (installer) → R9 (cross-machine) → R10 (Python delete)
- **NEW**: **R5 (writer) → R6 (installer + auto-update + supervisor) → R3 (control/MCP) → R4 (indexer)** → R9 → R10

**Why R5 first**: writer-stub currently inserts `route_executions` row + tries facade POST to dead Python = `python_unreachable`. This is the load-bearing endpoint for any agent task; without native writer, every `/api/companies/{id}/run` is a stub. Highest single-machine impact.

**Why R6 second**: no supervisor + no auto-update + no service registration = operator must keep a bash window open to run musu. R6 brings systemd/Windows-service equivalent + `git pull` auto-update path. This is what the operator literally asked about ("자동 업데이트 기능 깉은 편의성 기능 있는지").

**Why R9 deferred**: cross-machine is multi-machine by definition; operator explicitly carved it out for this cycle's focus. R9 task still in §A.2 table, status moved to "deferred (post single-machine completeness)".

**R-cleanup sequencing budget**: R5 (~1500 LOC) → R6 (~600 LOC) → R3 (~1500 LOC) → R4 (~1500 LOC). Solo-operator review bottleneck per panel H5: strict sequence, no parallel. Each sub-WS = Phase 0 Researcher → Phase 1 Planner → Phase 1.5 Critic (MED risk) → Phase 3 Builder → Phase 5 Auditor → Phase 7 Scribe HTML closure → Const VII per-push.

**§9.12 unchanged**: still locked to V24 closure (wiki/500) post-R10. Single-machine reorder does not affect Goodhart firewall.

### A.2 Sub-WS (panel-reshaped, R-fast → R-cleanup phases)

Detail per-WS plans live in wiki/491..500 reservations. Phase -1 strategic gate on master plan (wiki/490) ran 2026-05-20: verdict YELLOW, 4 HIGH accepted, plan reshaped per §A.0 above. Per-sub-WS Phase 1.5 Critic still required for HIGH/MED sub-WS.

**Workspace** (panel MED-1): single binary crate `musu-rs/` with internal modules. NOT 5 separate crates.

```
musu-rs/Cargo.toml      (single workspace)
musu-rs/src/main.rs     (subcommand dispatch)
musu-rs/src/bridge/     (R1)
musu-rs/src/core/       (R2)
musu-rs/src/control/    (R3)
musu-rs/src/indexer/    (R4)
musu-rs/src/writer/     (R5)
```

**R-fast (~2 weeks → JTBD ships at R9 end)** — strict sequence, no parallel:

| Sub-WS | Wiki | Module | Scope | Risk |
|---|---|---|---|---|
| V24-R0 | wiki/490 | — | This reshape commit + workspace bootstrap + Phase 0 audit of musu-supervisor/ + musu-port/ (MED-2) + dependency-audit doc | — |
| V24-R1 | wiki/491 | `bridge` | tokio+axum, 7 endpoints, bearer auth, **+ Python-facade reverse-proxy** for control/indexer/writer/wiki paths → localhost:8071 | HIGH |
| V24-R2 | wiki/492 | `core` | SQLite schema v1 (fresh, no migration) + companies.yaml loader | HIGH |
| V24-R7 | wiki/497 | — | musu-bee TS wire-up (BRIDGE_URL + endpoint paths) | LOW |
| V24-R8 | wiki/498 | — | 4060Ti E2E: install + register land-os + vibecode-town; Python still on port 8071 behind facade | MED |
| V24-R9 | wiki/499 | — | 5070Ti install + cross-machine task. **JTBD ships at end of R9** (~day 14). Operator starts dogfooding. | MED |

**R-cleanup (~2-4 weeks parallel to operator dogfood)** — strict sequence, each removes one Python service from facade. **Sequence reordered 2026-05-20 evening per §A.1.1 pivot**: R5 first (writer = highest single-machine impact), then R6 (installer + auto-update = operator-asked ergonomic gap), then R3/R4. R9 deferred until single-machine complete.

| Sub-WS | Wiki | Module | Scope | Risk | Order |
|---|---|---|---|---|---|
| V24-R5 | wiki/495 | `writer` | Agent task execution + SSE; replace writer-stub Python forward in `/api/companies/{id}/run` with native execution. Integrate `musu-supervisor-isolation-{linux,windows,macos}` crates. | MED | 1st |
| V24-R6 | wiki/496 | — | Installer rewrite (single-binary, no facade, no Python) + **auto-update path** (`git pull` + rebuild + service restart) + **service supervisor** (systemd unit + Windows Service + auto-restart) | MED | 2nd |
| V24-R3 | wiki/493 | `control` | MCP server stdio JSON-RPC (~10-20 tools subset); remove /api/control/* from facade | MED | 3rd |
| V24-R4 | wiki/494 | `indexer` | Per-company indexing (tantivy or FTS5); remove /api/indexer/* from facade | MED | 4th |
| V24-R9 | wiki/499 | — | 5070Ti install + cross-machine task. **Deferred** post single-machine completeness (R5+R6+R3+R4 done) per operator carve-out 2026-05-20 evening. | MED | 5th (deferred) |
| V24-R10 | wiki/500 | — | MANUAL GATE: Python `rm -rf` + closure HTML + **CHANGELOG 1.14.0** + §9.12 wait | — | 6th |

**Total Rust LOC estimate**: ~10,200 (vs 25,885 Python). Subagent-phase count: 7 chains (R7 compressed), saving ~28 invocations vs 5-crate split.

### A.3 Hard constraints (cycle-level invariants)

1. **No new SaaS dep** — [[feedback-self-contained-product]]. Rust crates from crates.io only; no fly/AWS/Vercel/etc.
2. **No Python** — [[feedback-no-python]]. New code Rust only. Shell scripts (install.sh) OK. Existing 25k Python = deprecate target, no new endpoints/handlers/migrations added to it.
3. **No K8s/Argo/CRD/Operator pattern** — [[feedback-no-yagni-architecture]]. Single binary + tokio async. No Kubernetes runtime at musu scale (4060Ti + 5070Ti, 4 companies max).
4. **No new schema migrations on existing Python codebase** — schema v37 is the last Python-side schema. Rust port designs fresh schema (semver reset for SQLite migrations to v1).
5. **Reshape lock ON** — orchestrator does not propose scope reshapes during V24 execution without explicit user request.
6. **Scribe HTML closure** — [[feedback-scribe-html-only]]. Per-sub-WS closure markdown; final wiki/500 HTML.
7. **Critic per sub-WS** — V24-R1 + V24-R2 + V24-R3 + V24-R5 + V24-R8 + V24-R9 (≥MED risk) require `system-architect` Critic per [[feedback-plan-stage-auditor]]. LOW-risk skips.
8. **Dual audit for V24-R1 + V24-R6** — auth-touching endpoints + installer (one-way blast radius via service registration).

### A.4 Operator gates (where /loop pauses)

Per [[feedback-autonomous-loop]]:

| Gate | Status | Action |
|---|---|---|
| **Phase -1 RED verdict** | not yet evaluated | If RED, HALT V24 until user decision |
| **Const VII main-merge** | PENDING (#436) | Orthogonal to V24; /loop continues on v23/phase4-rust or new v24/main-rust branch |
| **Const III schema apply** | will fire on R2 | New SQLite schema apply on operator machine = manual gate |
| **Production deploy** | N/A (single-operator) | — |
| **Irreversible destructive op** | wiki/500 Python deletion | Manual gate before `rm -rf musu-bridge/ musu-core/ musu-control/ musu-indexer/ musu-writer/` |

### A.5 What does NOT change

- musu thesis (SSOT_1PAGE 2026-04-09, 4-layer CoS/machines/companies/agents) — locked, not under review
- musu-bee (TS/React frontend) — keep, only adjust endpoints
- musu-relay-gateway Rust crate (V23.1 T1.8) — keep, integrate
- musu-supervisor-isolation-{linux,windows,macos} Rust crates (V21.D) — keep, integrate
- Operator hardware: 4060Ti + 5070Ti (multi-machine reality, locked since 5b8b103 2026-04-07)
- `INSTALL.md` / `QUICKSTART.md` / `ONBOARDING.md` — keep as templates, rewrite content for Rust bridge in V24-R6

### A.6 Supersedes

- **V23.6-minimal (prior §A)** — superseded. W2 direnv + W4 LESSONS_LEARNED were operator hygiene tasks; V24 makes operator hygiene moot until the install path actually runs. Re-evaluate post-V24 if still relevant.
- **V24-Python plan (deleted 2026-05-20)** — was extending Python violation. Replaced with V24-Rust-big-bang.

---

## §A-prior (ARCHIVED) — V23.6-minimal (superseded 2026-05-20 by V24-Rust-big-bang)

### A.0 v2 amend (2026-05-19): scope cut from 5 sub-WS to 2

Per `business-panel-experts` adversarial critique of wiki/471 v1 + W5 RED verdict from V23.6 Phase −1 strategic gate, V23.6 scope is cut to **operator hygiene only**:

**Ship**: W2 direnv pattern + `.envrc.example` (~40 LOC, doc + operator hygiene) + W4 LESSONS_LEARNED.md seed (~30 LOC, doc). Total ~70 LOC.

**Paused** (all V23.7+ pending entry conditions): W1 mitmproxy wiretap, W3 uv migration, W5 litellm (RED verdict).

**Entry conditions for V23.6 master plan Phase −1 spawn** (ALL three required):
1. Operator clears #436 (V23.3 + V23.4 + V23.5 → main) OR explicitly confirms #436 batching policy (deliberate vs accidental)
2. **~~≥1 V23.5 product-outcome telemetry signal exists~~ → DATA RETURNED 2026-05-19 v3: ZERO.** Ground-truth: `audit.db` 601 rows = 100% pytest `testclient`, `/api/wiki/*` + briefing paths = 0 hits ever; `telemetry.db` install_attempt + nat_pierce + agent_spawn = 0 rows ever. **Revised condition: operator decides what to do with the zero-usage finding** — either (a) accept "V23.5 was a capability investment, no runtime expected yet, V23.6 proceeds anyway", (b) "stop adding surface until V23.5 features are actually exercised once" (panel-aligned default), or (c) "build the runtime that would consume V23.5 surface first, before V23.6 hygiene work" (deepest change).
3. Operator reviews ≥3 V23.5 sub-WS Builder diffs end-to-end (still pending — no signal exists that this has happened)

Rationale: panel H5 (parallelism doesn't help solo operator) + M5 (#436 4-cycle backlog) + H2 (V23.5 product outcome) — H2 now confirmed empirically zero, not unknown. The remaining question is not "is V23.5 used" (answer: no) but "given that V23.5 is not used, what does V23.6 do."

### A.1 In-scope (v2)

**V23.6-minimal**: 2 sub-WS only (W2 + W4). All other candidates from wiki/484 deferred to V23.7+ pending entry conditions in §A.0.

| Wave | Sub-WS | Source | LOC | Risk | Why this wave |
|---|---|---|---|---|---|
| 1 | **V23.6-W2** direnv pattern + `.envrc.example` | wiki/484 #2 (5-step #1) | ~40 + doc | LOW | operator hygiene; no musu code change; `.env.example` is starting point |
| 1 | **V23.6-W4** LESSONS_LEARNED.md seed | wiki/484 #5 ("lessons.md") | ~30 + 5 seeds | LOW | 2AM-debugging running journal; distinct from MEMORY.md (per-type) + qual eval (post-mortem) |
| 2 | **V23.6 final closure (HTML wiki/486) + qual eval (wiki/487)** | per [[feedback-scribe-html-only]] | — | — | Const VII per-push automated; main-merge stays operator-pending. |

**Total**: ~70 LOC + closure. Estimated 1-2 /loop iterations.

**Phase chain for V23.6-minimal (compressed)**:
1. Operator clears 3 entry conditions in §A.0 (this is the only gate; no autonomous bypass)
2. Cut `v23/phase5` branch off `main` (or continue on `v23/phase4`)
3. Phase −1 strategic gate: SKIP — scope is operator hygiene only, no thesis extension, no new SaaS dep, no new code surface
4. Phase 1 Planner: minimal wiki/485 (V23.6-minimal master plan, ~1 page)
5. Phase 1.5 Critic: SKIP per `[[feedback-plan-stage-auditor]]` (LOC <500, no thesis extension)
6. Phase 3 Builder × 2 (W2 + W4 sequential — solo operator review-friendly per panel H5)
7. Phase 7 Scribe: wiki/486 HTML closure + wiki/487 qual eval
8. Const VII push gate per commit

**Paused** (V23.7+ pending entry conditions in §A.0):
- V23.6-W1 mitmproxy wiretap (wiki/484 #1) — was MED risk, deferred
- V23.6-W3 uv migration musu-bridge (wiki/484 #4) — was LOW risk, deferred
- V23.6-W5 litellm in front of `cos_briefing_agent` (wiki/484 #3) — Phase −1 RED verdict locked 2026-05-19

### A.2 Out-of-scope (firewalled to V23.7+)

| Item | Source | Why deferred |
|---|---|---|
| **inspect-ai eval suite** (wiki/484 #6) | 5-step #5 EVALS | HIGH risk; needs Phase −1 mini-gate validating [[feedback-no-yagni-architecture]] ("pytest + Playwright 부족 입증 필수") |
| **AutoAgent meta-agent harness** (wiki/484 #7) | Karpathy auto-research + Kevin Goo AutoAgent | HIGH+ research-tier. Prerequisites: inspect-ai shipped + 60d behavior metric + invariant lock + LLM budget cap. Stress points in wiki/484 §3.7. |
| 10 V23.6 scope firewall items (wiki/469) | V23.5 W-8 closure | Wiki page editing UI, RAG auto-retrieval, agent session memory persistence, multi-tenant scope expansion, H-4b QUIC, T2-D-visual React Flow, C-3 Y-path promotion, Paperclip/OpenClaw observer, wiki page editing API, server-side render mode. All require new Phase −1 gate. |
| C-3 LLM synthesis promotion to default | wiki/479 + wiki/488 reserved | V23.7 gate: ≥40% click rate / 60d. V23.6-W5 litellm satisfies 2/4 preconditions; V23.7 picks up other 2 (caching policy + budget UI). |

### A.3 Success criteria

V23.6 cycle is DONE when:
1. ✅ wiki/485 V23.6 master plan exists with Phase −1 verdict + Critic findings resolved
2. ✅ 5 sub-WS shipped on branch (v23/phase5 or v23/phase4 continuation), each commit reference closure doc
3. ✅ All tests green per sub-WS: tsc + pytest + Playwright + jest baseline maintained (~217 V23.5 baseline + V23.6 additions)
4. ✅ wiki/486 final closure (HTML) + wiki/487 qual eval (MD) committed
5. ✅ CHANGELOG 1.14.0 entry finalized
6. ✅ Const VII per-push gate satisfied (each commit pushed)
7. ✅ This GOAL.md §A updated with V23.6 completion + V23.7 cycle goal set (or paused for operator decision)

### A.4 Hard constraints (cycle-level invariants)

1. **No new SaaS dependency** — [[feedback-self-contained-product]]. litellm (in-process), uv (in-process), direnv (operator hygiene), mitmproxy (dev mode only), LESSONS_LEARNED (doc) — all compliant.
2. **No YAGNI** — [[feedback-no-yagni-architecture]]. Each candidate's leverage proxy in wiki/484 §3 must hold or stop.
3. **Schema stable** — no new migrations (Const III not triggered). schema v37 is V23.6 baseline.
4. **Performance** — no Const VI experiment. LLM costs are opt-in user-paid (V23.5 C-3 hard constraints inherited).
5. **Branch hygiene** — operator main-merge optional acceleration. autonomous /loop continues on v23/phase4 if main-merge stays pending past 1 V23.6 wave.
6. **Scribe HTML** — [[feedback-scribe-html-only]]. Closure docs HTML; other phase docs Markdown.
7. **Plan-as-spec Auditor calibration** — [[feedback-plan-stage-auditor]]. Only fire if V23.6 master plan ≥500 LOC (current estimate ~330 → likely skip).
8. **Critic gate per non-trivial sub-WS** — V23.6-W5 litellm (MED risk) requires Critic. W1-W4 LOW risk → compressed Critic+Builder.

---

## §B — Branch + commit state (snapshot 2026-05-20, V24-Rust-cleanup start, panel-reshaped)

- **Active branch**: `v23/phase4` HEAD `b6896af` (GOAL.md V24-Rust-big-bang reshape commit) — to be branched into `v24/rust-cleanup` at V24-R0 start (renamed from `v24/rust-big-bang` per panel reshape)
- **Pushed to**: `origin/v23/phase4`
- **Cumulative LOC (Python, to be deleted at R10)**: 25,885 LOC across musu-bridge + musu-core + musu-control + musu-indexer + musu-writer (3,312 .py files verified)
- **Existing Rust workspaces (audit pending R0 per panel MED-2)**:
  - `musu-supervisor/` workspace (V21.D crates `musu-supervisor-core`, `musu-supervisor-isolation-{linux,windows,macos}`) — R5 integration target IF audit passes
  - `musu-port/` workspace — R9 NAT/punch-through fallback IF audit passes
- **CHANGELOG**: at 1.13.0 (V23.5 closed). V24-R0 will set **1.14.0-dev** (NOT 2.0.0 — panel HIGH-3 + LOW-1: V24 is engineering cleanup, no Create dimension).
- **VERSION**: 1.13.0-dev → 1.14.0-dev on V24-R0 commit

---

## §C — Operator gates (V24-Rust-big-bang scope)

Per [[feedback-autonomous-loop]]:

| Gate | Current status | Action |
|---|---|---|
| **Phase -1 strategic gate** | not yet evaluated | Next /loop iteration must spawn `business-panel-experts` debate on Rust big-bang thesis before V24-R0 plan body writes |
| **Const VII main-merge** | orthogonal to V24 | #436 still pending V23 backlog → main; /loop continues on `v24/rust-big-bang` branch independently |
| **Const III schema apply** | will fire on V24-R2 | New SQLite schema on operator machine = manual gate |
| **Const III Python schema rollback** | will fire on V24-R10 closure | Final Python deletion = manual gate (irreversible) |
| Production deploy | N/A (single-operator) | — |
| Irreversible destructive op | wiki/500 Python deletion | Manual gate before `rm -rf musu-bridge/ musu-core/ musu-control/ musu-indexer/ musu-writer/` |

**Phase -1 gate is next action.** /loop continues to plan + Phase -1 panel; only blocks if RED verdict.

---

## §D — Reference state (key wiki IDs)

| Wiki | Title | Status |
|---|---|---|
| wiki/431 | V23.4 Phase 4 master plan | shipped (Phase 4 closed) |
| wiki/447 | V23.4 Phase 4 final closure (HTML) | complete |
| wiki/448 | V23.4 Phase 4 qual eval | complete |
| wiki/459 v4 | V23.5 master plan | shipped (V23.5 closed) |
| wiki/460 | V23.5 implementation plan | shipped + extended with wiki/484 §9 candidates |
| wiki/469 | V23.5 W-8 closure (V23.6 firewall 10 items) | complete |
| wiki/470 | V23.5 final closure (HTML) | complete |
| wiki/471 | V23.5 qual eval | complete |
| wiki/479 | V23.7 promotion criterion (≥40% click rate / 60d) | doc-only, awaits 60d data |
| wiki/483 | H-4b QUIC mesh propagation | reserved (deferred V23.6/V23.7) |
| **wiki/484** | **V23.6 planning input — agentic 5-step + AutoAgent (7 candidates)** | complete; PRIMARY V23.6 input |
| **wiki/485** | **V23.6 master plan** | **TO BE WRITTEN** |
| wiki/486 | V23.6 final closure (HTML) | reserved |
| wiki/487 | V23.6 qual eval | reserved |
| wiki/488 | V23.7 Y-path promotion gate | reserved (V23.7 strategic gate) |

---

## §E — Loop heartbeat decision tree

Per `~/.claude/MODE_Agent_Team.md` /loop decision tree, evaluate in order:

1. **block-on-user?** → operator gate active (e.g., #436) OR Phase −1 RED verdict awaiting decision → emit gate prompt, halt
2. **regression?** → prior deploy invariants broken (V23.5 tests fail, /health degraded) → spawn `root-cause-analyst`, drop other work
3. **audit-fix open?** → HIGH finding from last audit unresolved → Builder
4. **post-build, no audit?** → commits since last audit → Auditor
5. **post-plan, critic done, no build?** → Builder (sub-WS V23.6-W1..W5)
6. **plan drafted, critic unrun?** → Critic (system-architect)
7. **new sub-task in cleared master?** → Researcher + Explore parallel, then Plan
8. **NEW master plan / thesis extension?** → Phase −1 strategic gate FIRST

### Current state evaluation (2026-05-20 — V24-Rust-big-bang start)

- Step 1: NO operator gate active (Phase -1 not yet run; not RED). #436 orthogonal.
- Step 8: **NEW master plan** → Phase -1 strategic gate FIRES. Next action: `EnterPlanMode` for V24 master plan (wiki/490) body draft + `business-panel-experts` debate on Rust big-bang thesis.

**Next /loop iteration goal**: enter plan mode → draft wiki/490 body (sub-WS table, sequence, acceptance, Const gates predicted) → spawn Phase -1 panel (Christensen + Taleb + Kim&Mauborgne + Drucker, debate mode) → record verdict in wiki/490 §0 → if GREEN/YELLOW proceed to V24-R0 commit, if RED HALT.

---

## §F — Revision history

| Date | Change | Reason |
|---|---|---|
| 2026-05-19 v1 | Initial GOAL.md created, V23.6 cycle defined | User request "야 골설정 제대로 해서 /loop으로 만들어"; covers scope 1+2 (V23.6 master plan entry + sub-WS impl); dynamic cadence; project-root location |
| 2026-05-19 v2 | §A scope cut: 5 sub-WS → 2 (V23.6-minimal: W2+W4 only). §A.0 added with 3 entry conditions. §E decision tree current-state recalibrated. | Post-`business-panel-experts` adversarial critique of wiki/471 V23.5 qual eval v1 (4-expert unanimous AMEND verdict: 5 HIGH + 6 MED). W5 RED verdict from V23.6 Phase −1 strategic gate absorbed. Scope cut directly addresses panel H5 (parallelism vs solo-operator review bandwidth) + M5 (#436 4-cycle backlog) + H2 (V23.5 product-outcome unmeasured). User approved Option 1 ("amend + W5 결정 같이"). |
| 2026-05-20 v3 | §A FULL RESHAPE: V23.6-minimal → V24-Rust-big-bang. §A archived as §A-prior. §B branch state pivoted to v24/rust-big-bang. §C operator gates rescoped to Rust install + Python deletion. §E next action = Phase -1 panel. | User explosion 2026-05-20 on discovering musu-bridge 25,885 LOC Python = 6 weeks of [[feedback-no-python]] violation. User locked Rust backend + α big-bang approach (4-6 weeks down time). Day-2 acceptance defined: land-os + vibecode-town companies cross-machine on 4060Ti↔5070Ti. Reshape lock ON per user "reshape 금지". |
| 2026-05-20 v4 | §A PANEL-RESHAPE: V24-Rust-big-bang → V24-Rust-cleanup (R-fast + R-cleanup phasing). Version 2.0.0 → 1.14.0. Added §9.12 ungameable operator-attested closure metric. Single-binary `musu-rs` workspace (not 5 crates). Reshape lock semantics resolved (panel HIGH OK; orchestrator drift not OK). Branch renamed v24/rust-big-bang → v24/rust-cleanup. | Phase -1 `business-panel-experts` debate returned YELLOW with 4 HIGH + 3 MED + 1 LOW. User accepted all 4 HIGH reshapes via AskUserQuestion 2026-05-20. HIGH-1 Christensen (JTBD priority inversion), HIGH-2 Taleb (4-6wk fragility window), HIGH-3 Kim&Mauborgne (no ERRC, 2.0.0 oversells), HIGH-4 Drucker (acceptance metric Goodhart-gameable by orchestrator). MED-3 governance contradiction resolved: reshape lock covers orchestrator drift, not panel HIGH. |
| 2026-05-27 v5 | Windows distribution pivot recorded: direct-download bootstrap remains the current operator path, but the intended Windows product direction is now Store/MSIX-ready packaged runtime. R6 output must not assume Task Scheduler + self-copy + self-update are the only Windows model. | Store/MSIX audit found the current Windows path is coherent for operator bootstrap but not for long-term product distribution. Product spec now records a dual-mode Windows runtime split (`direct-download` vs `store-msix`) and future Windows packaging work must honor package-managed install/update constraints. |
| 2026-05-27 v6 | Windows packaged path clarified into **three contracts**: direct-download bootstrap, local sideload / manual bridge, and Store-reviewed restricted-capability auto-start. Repo-local proof now closes the local/manual path; the Store auto-start path remains blocked only on Partner Center verification and Microsoft review. | Post-install and packaged-startup investigation showed local sideload cannot be truthfully sold as auto-start-ready. Product/docs/scripts were reshaped so local sideload is explicit manual bridge, while Store auto-start is a separate reviewed artifact and submission bundle. |
| 2026-05-29 v7 | `1.15.0-rc.1` beta-readiness state recorded. Single-machine Windows local loop is beta-ready via `musu up` + dashboard doctor + Claude task smoke; Store auto-start and full multi-machine remain separate tracks. | Live smoke verified dashboard `3001`, bridge `11041`, task output `MUSU_SMOKE_OK`. Current qualitative evaluation, code audit, and roadmap live in wiki/518. |
| 2026-05-29 v8 | Partner Center enrollment approval cleared by operator report; Store path moved from account-verification blocker to product-name/package-submission blocker. Existing 2026-05-27 `1.13.0.0` submission bundle must be regenerated for `1.15.0-rc.1` before upload. | User reported approval and supplied a cross-product launch note. Relevant Store/channel/promotion guidance was filtered into `STORE_LAUNCH_AND_PROMOTION_PLAN_2026_05_29.md`; unrelated product names were not adopted. |
| 2026-05-29 v9 | `yellowhama/musu-system` assessed for future MUSU integration. Decision: high value, but integrate as adjacent MCP/CLI/tooling layer, not Rust-core merge or first Store bundle. | Local clone + `go test ./...` passed for `core`, `crawl-ai`, `marketer`, and `nurikun`. Report: `docs/MUSU_SYSTEM_INTEGRATION_ASSESSMENT_2026_05_29.md`. |
| 2026-05-29 v10 | Single-machine smoke was converted into `scripts\windows\smoke-single-machine-beta.ps1` and passed live; multi-device runbook/script prepared as wiki/519. Release-grade desktop assessment clarified: runtime beta is credible, but Tauri GUI shell and Store package approval remain open gates. | Script smoke passed on dashboard `3000`, bridge `11041`, task `2d9e93b1-fb2f-4cd4-ab40-1147fea89a6d`, output `MUSU_SCRIPT_SMOKE_OK`, CLI route `MUSU_SCRIPT_CLI_OK`. Multi-device script: `scripts\windows\smoke-multidevice-beta.ps1`. |
| 2026-05-29 v11 | Current-version MSIX artifacts regenerated and verified for `1.15.0.0`; Store-reviewed submission bundle prepared. | local-sideload workflow passed packaged startup smoke with release `musu-startup.exe`; Store-reviewed manifest verified `ImmediateRegistration=true` + restricted startup capability. Bundle: `.local-build\msix\submission-bundles\store-reviewed-20260529-033609`. |
| 2026-05-29 v12 | Desktop release readiness audit added. Tauri metadata upgraded from scaffold values to `1.15.0-rc.1`, but public desktop release readiness remains false. | `scripts\windows\audit-desktop-release-readiness.ps1` reports runtime package ready=true, desktop shell ready=false, multi-device verified=false. Blocking: missing `frontendDist=../out`, build command does not emit static export, second-PC test pending. Report: `docs/DESKTOP_RELEASE_READINESS_AUDIT_2026_05_29.md` (wiki/520). |
| 2026-05-29 v13 | Desktop shell and Store metadata basics are now locally ready; the repo-local desktop release audit still fails on real second-PC multi-device evidence. | `audit-desktop-release-readiness.ps1` reports runtime package ready=true, desktop shell ready=true, Store metadata checks pass, multi-device verified=false. `/privacy` and `/support` build and verify locally; live `musu.pro` still lacks expected new content. `write-release-go-no-go.ps1` now aggregates local/external blockers. |
| 2026-05-29 v14 | Release infrastructure CI/deploy workflows repaired for the current Rust/Next repo shape. | `test.yml` no longer references deleted Python dirs; `e2e-musu-bee.yml` no longer references deleted `musu-port`; deploy and web checks use Node 22+ for `node:sqlite`; GitHub JavaScript actions are forced onto Node 24 runtime; Linux Rust CI installs Wayland/PipeWire/GBM native dependencies; legacy likely-required check names are preserved; `npm run test:e2e:ci` verifies `/privacy` and `/support` Store metadata content. Public release still waits on green remote Actions, live `musu.pro` deployment, support mailbox, second-PC evidence, and Microsoft review. |
| 2026-05-29 v15 | CI/deploy and live Store public metadata gates are now green. | Remote `Tests` passed on `ad5f752`; latest relevant `E2E Tests — musu-bee` and `Deploy musu-bee to Vercel` passed on `0919a83`; `verify-store-public-metadata.ps1 -BaseUrl https://musu.pro -Json` passed. `write-release-go-no-go.ps1` still reports public release No-Go only because real second-PC evidence and `musu@musu.pro` operator delivery verification are missing, plus Partner Center/Microsoft external gates. |
| 2026-05-29 v16 | Support mailbox gate is now evidence-backed instead of an assumption-only switch. | Added `verify-support-mailbox-evidence.ps1` and `record-support-mailbox-verification.ps1`; go/no-go now auto-detects valid support mailbox `*.evidence.json`. Temporary scanner tests passed and were deleted. Real `musu@musu.pro` evidence is still missing, so release remains No-Go with second-PC evidence still missing. |
| 2026-05-29 v17 | `yellowhama/musu-system` was rechecked under the latest integration request. Conclusion unchanged: integrate later as adjacent MCP/CLI/tooling, not as Rust-core merge or first Store bundle. | `musu-system` HEAD remains `d4e58e0`; split repo HEADs unchanged; `go test ./...` and `go vet ./...` passed in all four modules. Spot audit: `nurikun` keeps delivery ops out of MCP, but `watch` send-failure persistence should be fixed/wrapped before dashboard integration. |
| 2026-05-29 v18 | Fresh assistant-side single-machine smoke reran successfully and final operator gates were consolidated. | On commit `f9ae873`, `smoke-single-machine-beta.ps1` passed with dashboard task `b4b05b93-34d2-4946-b4cd-fdd5c5c6632d`, output `MUSU_RELEASE_SMOKE_OK_20260529_0652`, and CLI route `MUSU_CLI_ROUTE_OK_20260529_0652`. Remaining release blockers are documented in `docs/RELEASE_FINAL_OPERATOR_GATES_2026_05_29.md`. |
| 2026-05-29 v19 | Single-machine smoke is now evidence-backed in release audit. | `smoke-single-machine-beta.ps1` writes `musu.single_machine_smoke_evidence.v1`; added verifier/recorder scripts; committed evidence under `docs\evidence\single-machine\1.15.0-rc.1`. `audit-desktop-release-readiness.ps1` now reports `single_machine_verified=true`. Remaining No-Go blockers are second-PC multi-device evidence and `musu@musu.pro` delivery evidence. |
| 2026-05-29 v20 | `musu-system` integration report was refreshed against current GitHub/local state. | Fresh 07:17 KST recheck: `musu-system` HEAD `d4e58e0`, latest remote CI green, `go test ./...` and `go vet ./...` passed in `core`, `crawl-ai`, `marketer`, and `nurikun`. Decision unchanged: integrate later through MCP/CLI/bridge adapters; do not merge into `musu-rs` or first Store bundle. Added caveat that MCP registration needs explicit wiki/model path contracts. |
| 2026-05-29 v21 | Remaining manual release gates now have a single operator handoff packet generator, verifier, and completion runner. | Added `scripts\windows\prepare-final-operator-gate-packet.ps1`, `scripts\windows\verify-final-operator-gate-packet.ps1`, and `scripts\windows\complete-final-operator-gates.ps1`; they package/verify a fresh multi-device kit and then record returned multi-device + support evidence into final go/no-go. Latest verified packet is now tracked in v25. It does not close the gates; it reduces operator execution friction for the remaining external checks. |
| 2026-05-29 v22 | `musu-system` integration report was corrected against the current monorepo HEAD, not stale split-repo notes. | Fresh 07:52 KST recheck: HEAD `d4e58e0`, split repo HEADs unchanged, `go test ./...` and `go vet ./...` passed again in all four modules; current HEAD already has MCP schemas and marketer/nurikun DB parent creation. Remaining integration precondition is adapter-managed cwd/wiki/model/env context plus fixing/wrapping `nurikun watch` send-failure persistence before dashboard use. |
| 2026-05-29 v23 | Store release approval is now evidence-backed in final go/no-go instead of only listed as a manual external gate. | Added `scripts\windows\verify-store-release-evidence.ps1` and `scripts\windows\record-store-release-verification.ps1`; `write-release-go-no-go.ps1` now reports `store_release_verified` and blocks public desktop release until Partner Center submission, Microsoft certification, and restricted startup capability approval evidence is recorded. |
| 2026-05-29 v24 | `musu-system` was rechecked again after the latest integration request. | Fresh 08:14 KST recheck: GitHub visibility/HEADs/tags unchanged, latest `musu-system` branch CI green, and local `go test ./...` plus `go vet ./...` passed again in `core`, `crawl-ai`, `marketer`, and `nurikun`. Conclusion unchanged: integrate later as optional MCP/CLI/bridge ecosystem tooling; do not merge into `musu-rs` or first Store package. |
| 2026-05-29 v25 | Final operator gate packet was regenerated and hardened for the Store release evidence gate. | Packet alias `.local-build\final-operator-gates\musu-final-operator-gates-1.15.0-rc.1-latest.zip` verified with `ok=true`, `fail_count=0`, `kit_count=1` at this stage. The packet README/verifier began enforcing Store release approval evidence instructions; later MSIX-install hardening is tracked in v28. |
| 2026-05-29 v26 | Final completion runner now supports Store release evidence in the same command as multi-device/support evidence. | Added Store approval parameters to `complete-final-operator-gates.ps1`; packet README and verifier now require Store params in the final command. Smoke wrote Store evidence only to `.local-build\store-release-complete-smoke`, preserving the real No-Go state until actual Microsoft approval evidence is recorded. |
| 2026-05-29 v27 | Final release handoff status now has a one-screen evidence-non-recording script. | Added `scripts\windows\show-final-release-handoff-status.ps1`; final operator packets include it and packet verification requires the README status command. The script summarizes go/no-go flags, packet verification, evidence roots, and remaining operator commands without creating release evidence. Later v28 adds the clean/current MSIX install evidence gate to the same handoff path. |
| 2026-05-29 v28 | Second-PC MSIX install proof is now evidence-gated separately from multi-device routing. | Added `capture-msix-install-evidence.ps1`, `verify-msix-install-evidence.ps1`, and `record-msix-install-evidence.ps1`; `write-release-go-no-go.ps1` now reports `msix_install_verified` and blocks public release until clean/current Windows MSIX install evidence is recorded. Final packet/completion runner now carry `-MsixInstallEvidencePath`. |
| 2026-05-29 v29 | Release manifest writes are now resilient to concurrent local status checks. | A parallel `show-final-release-handoff-status.ps1` plus `write-release-go-no-go.ps1` run exposed a transient lock on `release-candidate-manifest.json`; `write-release-candidate-manifest.ps1` now writes manifest and checksum files through temp files with retry before replacement. |
| 2026-05-29 v30 | `musu-system` was rechecked again against current remote state after the latest integration request. | 09:19 KST recheck: monorepo HEAD remains `d4e58e0`, split repo HEADs/tags unchanged, latest monorepo CI/GHCR runs green, and local `go test ./...` plus `go vet ./...` passed in `core`, `crawl-ai`, `marketer`, and `nurikun` from `.local-build\external\musu-system`. Decision unchanged: integrate as optional MCP/CLI/bridge tooling, not Rust-core merge or first Store package. |
| 2026-05-29 v31 | Store release evidence now records Partner Center product-name reservation explicitly. | `record-store-release-verification.ps1` writes `product_name_reserved` and `product_name_reserved_at`; `verify-store-release-evidence.ps1` requires them before `store_release_verified=true`. Final handoff docs/status now state the Store gate as product-name reservation, app submission, Microsoft certification, and restricted capability approval. |
| 2026-05-29 v32 | Final Store evidence runner now requires the Partner Center product-name reservation timestamp. | `complete-final-operator-gates.ps1`, handoff status, and the final operator packet now expose `-StoreProductNameReservedAt`, and packet verification checks that the final command includes it. This prevents the final runner from relying on the recorder's fallback of using submission time as reservation time. |
| 2026-05-29 v33 | Go/no-go manual external gate list now includes support mailbox delivery verification. | `write-release-go-no-go.ps1` already blocked on missing `musu@musu.pro` evidence, but `manual_external_gates` omitted that operator action. It now lists `musu@musu.pro inbox delivery verification`, matching the blocker and final handoff operator steps. |
| 2026-05-29 v34 | Support mailbox release readiness can no longer be assumed by CLI flag. | Removed `-AssumeSupportMailboxVerified` from `write-release-go-no-go.ps1`; support readiness now requires recorded evidence from `record-support-mailbox-verification.ps1` / `verify-support-mailbox-evidence.ps1`. |
| 2026-05-29 v35 | Skipping public Store metadata verification no longer permits release readiness. | `write-release-go-no-go.ps1 -SkipPublicMetadata` now adds a `store-public-metadata` blocker instead of only a warning, keeping the option diagnostic-only for offline checks. |
| 2026-05-29 v36 | Final completion command now fails closed when blockers remain. | `show-final-release-handoff-status.ps1`, final operator packet README, and `RELEASE_FINAL_OPERATOR_GATES_2026_05_29.md` now include `-FailOnNotReady` for `complete-final-operator-gates.ps1`; packet verification fails if the final command omits it. |
| 2026-05-29 v37 | Dirty git state is now a release blocker. | `write-release-go-no-go.ps1` promotes dirty worktree status from warning to blocker, so public readiness cannot become true until changes are committed and the release manifest records `manifest_git.dirty=false`. |
| 2026-05-29 v38 | Final operator packet verification now catches stale dirty-git rules. | `verify-final-operator-gate-packet.ps1` checks the bundled `write-release-go-no-go.ps1` for the dirty-git blocker and checks the bundled verifier for the same assertion, so old packets with warning-only dirty-git handling fail verification. |
| 2026-05-29 v39 | Final operator packet generation is now clean-git only and source-attributed. | `prepare-final-operator-gate-packet.ps1` refuses dirty worktrees and writes `packet-build-metadata.json` with source branch, commit, and clean git state; packet verification requires valid clean source metadata. |
| 2026-05-29 v40 | Multi-device evidence is now schema/version/timestamp gated. | `smoke-multidevice-beta.ps1` writes `musu.multidevice_smoke_evidence.v1` with version, operator machine, and timestamps; `verify-multidevice-evidence.ps1` validates schema, expected version, `started_at`/`completed_at`, evidence age, and operator metadata. Release audit passes the repo `VERSION`, and final packet verification rejects stale multi-device verifiers missing these checks. |
| 2026-05-29 v41 | Manual support/Store evidence now fails closed on weak operator input. | Support mailbox evidence must match the current version, include an explicit `musu-...` token, use a sender distinct from `musu@musu.pro`, and pass timestamp order/future checks. Store evidence recording now requires explicit `-ProductNameReservedAt` and the verifier checks recording order/future timestamps. Final packet verification rejects stale support/Store evidence scripts without those guards. |
| 2026-05-29 v42 | MSIX install and multi-device evidence gates now reject underspecified operator evidence. | `verify-msix-install-evidence.ps1` defaults to the repo version, requires operator machine/user, non-future timestamps, installed/artifact version match, and required passing capture checks. `verify-multidevice-evidence.ps1` defaults to the repo version, requires operator user, and requires `remote_addr` to be `host:port`. Packet verification now rejects stale MSIX/multi-device verifiers missing these checks. |
| 2026-05-29 v43 | Single-machine smoke evidence is now tied to the current code commit. | `verify-single-machine-evidence.ps1` requires expected version/current commit and allows only documentation/evidence-only deltas; `smoke-single-machine-beta.ps1` uses timeout-safe process execution. Superseded evidence: `20260529-114932-HUGH_SECOND` on commit `a3ace02`, dashboard output `MUSU_RELEASE_SMOKE_OK_20260529_1149`, CLI route `MUSU_CLI_ROUTE_OK_20260529_1149`. |
| 2026-05-29 v44 | `musu-system` integration assessment was refreshed again against current remote state. | 11:55 KST recheck: public `musu-system` HEAD remains `d4e58e0`, split repo HEADs and active tags unchanged, and local `go test ./...` plus `go vet ./...` passed in `core`, `crawl-ai`, `marketer`, and `nurikun`. Decision remains optional MCP/CLI/bridge integration, not Rust-core merge or first Store package bundling. |
| 2026-05-29 v45 | A MUSU-specific Microsoft Store release run card was added from current source refresh. | `docs/MICROSOFT_STORE_RELEASE_RUN_CARD_2026_05_29.md` rejects other-product HiveLink/Vibe PM copy, keeps MUSU desktop as the Store product, preserves MSIX-first release strategy, and lists the exact remaining evidence sequence for second-PC MSIX install, multi-device, support mailbox, and Partner Center/Microsoft approval. |
| 2026-05-29 v46 | The final operator packet now includes and verifies the MUSU Store run card, and single-machine smoke was rerun against that code commit. | `prepare-final-operator-gate-packet.ps1` copies `MICROSOFT_STORE_RELEASE_RUN_CARD_2026_05_29.md`; `verify-final-operator-gate-packet.ps1` requires it. Current evidence: `20260529-121456-HUGH_SECOND` on commit `745d852`, dashboard output `MUSU_RELEASE_SMOKE_OK_20260529_1214`, CLI route `MUSU_CLI_ROUTE_OK_20260529_1214`. |
| 2026-05-29 v47 | The second-PC kit now writes a handoff JSON with candidate RemoteAddr values. | Added `collect-second-pc-handoff.ps1`; the multi-device kit and final packet include it, the packet verifier checks for it, and the docs now instruct the operator to return `.local-build\second-pc-handoff\*.handoff.json` plus MSIX/multi-device evidence. |
| 2026-05-29 v48 | `musu-system` was rechecked again for future integration value. | 12:41 KST recheck: canonical monorepo HEAD remains `d4e58e0`, split repo HEADs/tags unchanged, latest monorepo CI/GHCR successful, and local `go test ./...` plus `go vet ./...` passed in `core`, `crawl-ai`, `marketer`, and `nurikun`. Decision unchanged: high value, but adapter/MCP/CLI integration first; no Rust-core merge and no first Store package bundle. First candidate is `crawl-ai` knowledge/wiki ingestion. |
| 2026-05-29 v49 | PowerShell command capture was hardened and current single-machine evidence was refreshed. | `smoke-single-machine-beta.ps1` and `collect-second-pc-handoff.ps1` now avoid redirected-pipe `ReadToEnd()` hangs by running commands in a timeout-bounded job and reading stdout/stderr/exit code from temp files. Clean-commit smoke evidence: `20260529-125448-HUGH_SECOND` on commit `cc336b7`, dashboard output `MUSU_RELEASE_SMOKE_OK_20260529_1255`, CLI route `MUSU_CLI_ROUTE_OK_20260529_1255`. |
| 2026-05-29 v50 | Latest final handoff and remote CI were reverified after push. | Pushed HEAD `0057a48` passed GitHub Actions `Tests` run `26617175374`. Final operator packet `20260529-130100` verifies with `ok=true`, `fail_count=0`, `kit_count=1`, and clean source metadata for `0057a48`. `show-final-release-handoff-status.ps1 -Json` reports packet verified=true and `ready_for_public_desktop_release=false`; only the four external gates remain: second-PC MSIX install, real multi-device, support mailbox, and Store/Microsoft approval evidence. |
| 2026-05-29 v51 | Release support mailbox was corrected to the actual mailbox and centralized. | Replaced the incorrect hardcoded `support@musu.pro` with `musu@musu.pro`, added root `SUPPORT_EMAIL`, added `scripts\windows\release-config.ps1`, and added `musu-bee/src/lib/contact.ts`. Public `/privacy` and `/support`, Store metadata E2E, release gate scripts, final packet generation, and final packet verification now use the configured support mailbox. |
| 2026-05-29 v52 | Current single-machine evidence was refreshed after the support mailbox code change. | Current smoke evidence: `20260529-133756-HUGH_SECOND` on commit `ac98f6c`, dashboard output `MUSU_RELEASE_SMOKE_OK_20260529_1338`, CLI route `MUSU_CLI_ROUTE_OK_20260529_1338`, dashboard task `c52d5d28-4eb2-43c2-8bbc-aeeaac5428b5`, bridge `http://127.0.0.1:4652`. Live public metadata verifies with `musu@musu.pro`; public release still waits on second-PC MSIX install, real multi-device, `musu@musu.pro` inbox evidence, and Store/Microsoft approval evidence. |
| 2026-05-29 v53 | Added a packet-aware operator handoff card for the remaining external gates. | `show-operator-handoff-card.ps1` reads the latest final operator packet and prints the current support verification id, subject, second-PC kit name, second-PC commands, primary recording commands, and return-file list. The final packet generator/verifier now includes and requires `RELEASE_OPERATOR_HANDOFF_CARD_2026_05_29.md` and the handoff script, reducing stale packet-id risk for the user-run second-PC/support/Store gates. |
| 2026-05-29 v54 | Current single-machine evidence was refreshed after adding the operator handoff script. | Current smoke evidence: `20260529-135225-HUGH_SECOND` on commit `1597864`, dashboard output `MUSU_RELEASE_SMOKE_OK_20260529_1352`, CLI route `MUSU_CLI_ROUTE_OK_20260529_1352`, dashboard task `c43a853b-1dff-4017-9755-235fca427b5d`, bridge `http://127.0.0.1:4652`. |
| 2026-05-29 v55 | Final qualitative audit, product spec update, code audit, and next-step roadmap were consolidated. | Added wiki/521: `RELEASE_1_15_0_RC1_FINAL_QUAL_AUDIT_NEXT_STEPS_2026_05_29.md`. Current verdict: local single-machine beta and Store/operator-gate infrastructure are strong, no internal release-blocking issue found in scoped audit, but public desktop release remains No-Go until second-PC MSIX install, real multi-device, `musu@musu.pro` inbox, and Partner Center/Microsoft Store evidence are recorded. Final operator packet generator/verifier now include wiki/521. |
| 2026-05-29 v56 | Current single-machine evidence was refreshed after wiki/521 packet-script changes. | Current smoke evidence: `20260529-151324-HUGH_SECOND` on commit `5209a82`, dashboard output `MUSU_RELEASE_SMOKE_OK_20260529_1524`, CLI route `MUSU_CLI_ROUTE_OK_20260529_1524`, dashboard task `d1571cf6-fbf6-415b-adf5-1e88ca6a3266`, bridge `http://127.0.0.1:4652`. |
| 2026-05-29 v57 | Bridge doctor false-negative was fixed and single-machine evidence was refreshed again. | `musu doctor` bridge `/health` timeout changed from 3s to 10s after direct bridge health and dashboard doctor were ok but CLI doctor timed out on Windows loopback. Current smoke evidence: `20260529-163756-HUGH_SECOND` on commit `d1a8e28`, dashboard output `MUSU_RELEASE_SMOKE_OK_20260529_1640`, CLI route `MUSU_CLI_ROUTE_OK_20260529_1640`, dashboard task `4456cc33-bd26-4ae9-94dd-20245163296c`, bridge `http://127.0.0.1:8723`. |
| 2026-05-29 v58 | Second-PC returned handoff JSON now has a primary-side command card. | Added `scripts\windows\show-second-pc-return-card.ps1`; final packet and operator handoff card now include/verify it. It reads returned `.local-build\second-pc-handoff\*.handoff.json`, picks a `suggested_remote_addrs` candidate, and prints exact primary PC commands for MSIX install evidence recording, multi-device smoke, and multi-device evidence recording. |
| 2026-05-29 v59 | Smoke harness process capture was fixed and current single-machine evidence refreshed. | User noticed many Node processes; audit showed Codex plus the intentionally running Next dev server, while stale bridge processes came from failed harness attempts. `smoke-single-machine-beta.ps1` now uses readiness retries and `Start-Process` temp-file capture instead of PowerShell job/pipe capture that can hang when `musu up` spawns bridge. Current smoke evidence: `20260529-185958-HUGH_SECOND` on commit `242d75f`, dashboard output `MUSU_RELEASE_SMOKE_OK_20260529_185935`, CLI route `MUSU_CLI_ROUTE_OK_20260529_185935`, dashboard task `3cab5be8-1abf-40c0-91ad-3f5d2da33bcb`, bridge `http://127.0.0.1:9218`. |
| 2026-05-30 v60 | Remaining external actions now have a repeatable operator action pack generator/verifier. | Added `prepare-operator-action-pack.ps1` and `verify-operator-action-pack.ps1`. The pack is clean-git/source-attributed, verifies the final operator packet first, bundles second-PC transfer, Partner Center submission copy, and support-mailbox template, checks nested zips/checksums, and rejects private `.pfx` material. It reduces handoff error but does not close the four external evidence gates. |
| 2026-05-30 v61 | Current single-machine evidence was refreshed after operator action pack release infra. | Current smoke evidence: `20260530-053645-HUGH_SECOND` on commit `2b91d0a`, dashboard output `MUSU_RELEASE_SMOKE_OK_20260530_053611`, CLI route `MUSU_CLI_ROUTE_OK_20260530_053611`, dashboard task `d0f7f581-3c3e-49b2-a551-1f8881100aa8`, bridge `http://127.0.0.1:11971`. A first attempt hit dashboard status while Next was still compiling `/app`; after `musu up --json` reported bridge/dashboard `ok`, the rerun passed. |
| 2026-05-30 v62 | Final handoff status now reports operator action pack verification too. | `show-final-release-handoff-status.ps1` has `ActionPackPath` / `SkipActionPackVerification` inputs, reports `action_pack.exists` and `action_pack.verified`, and adds an operator step when the latest action pack is missing or invalid. Final packet generation now includes the action pack scripts and README commands; packet verification fails stale packets whose README/status script omit action-pack verification. |
| 2026-05-30 v63 | Current single-machine evidence was refreshed after handoff status action-pack verification. | Current smoke evidence: `20260530-060739-HUGH_SECOND` on commit `d7e745f`, dashboard output `MUSU_RELEASE_SMOKE_OK_20260530_060700`, CLI route `MUSU_CLI_ROUTE_OK_20260530_060700`, dashboard task `146d0165-5f4a-428e-9d74-f3ea0b05dbad`, bridge `http://127.0.0.1:12305`. |
| 2026-05-30 v64 | Added a one-command second-PC release check wrapper. | `run-second-pc-release-check.ps1` runs sideload readiness, install/verify, MSIX evidence capture, and second-PC handoff collection from the extracted kit. Multi-device kit generation includes it, packet verification requires it inside the kit, and operator/action-pack docs now prefer the wrapper before manual fallback. |
| 2026-05-30 v65 | Current single-machine evidence was refreshed after the second-PC wrapper. | Current smoke evidence: `20260530-085123-HUGH_SECOND` on commit `213b69e`, dashboard output `MUSU_RELEASE_SMOKE_OK_20260530_085059`, CLI route `MUSU_CLI_ROUTE_OK_20260530_085059`, dashboard task `14c9e9be-73b7-494f-9194-fd87d8f4b252`, bridge `http://127.0.0.1:12427`. |
| 2026-05-30 v66 | Beta release checklist current evidence pointer was corrected. | `BETA_RELEASE_CHECKLIST_1_15_0_RC1.md` now points its current machine-readable single-machine evidence section to `20260530-085123-HUGH_SECOND`; the older `20260529-185958-HUGH_SECOND` evidence remains historical harness evidence only. |
| 2026-05-30 v67 | `musu-system` integration decision was revalidated against current external state. | GitHub repo state, HEAD/main `d4e58e0`, active tags, and latest CI remain unchanged/green; local `go test ./...` plus `go vet ./...` passed again in `core`, `crawl-ai`, `marketer`, and `nurikun`. Decision unchanged: optional MCP/CLI/bridge integration later, no Rust-core merge and no first Store bundle. |
| 2026-05-30 v68 | Store submission bundle verification was added. | Added `verify-store-submission-bundle.ps1`, made `prepare-store-submission-bundle.ps1` write `SHA256SUMS.txt`, made desktop readiness audit verify the latest Store bundle, and made operator action pack generation refuse an invalid Store submission bundle. Latest verified bundle: `.local-build\msix\submission-bundles\store-reviewed-20260530-093140`. |
| 2026-05-30 v69 | Current single-machine evidence was refreshed after Store submission bundle verifier changes. | Current smoke evidence: `20260530-094254-HUGH_SECOND` on commit `45209e8`, dashboard output `MUSU_RELEASE_SMOKE_OK_20260530_094227`, CLI route `MUSU_CLI_ROUTE_OK_20260530_094227`, dashboard task `948fd44f-41b3-4bc8-9aa2-e05ea002d838`, bridge `http://127.0.0.1:12715`. |
| 2026-05-30 v70 | Second-PC return handoff now has a single archive path. | `run-second-pc-release-check.ps1` creates `.local-build\second-pc-return\*.zip` by default, and `show-second-pc-return-card.ps1 -ReturnZipPath ...` extracts it to locate the handoff/MSIX evidence and print primary-side commands. Packet/action-pack docs and verifiers require the return archive instructions. This reduces operator file-return risk; public release still waits on real second-PC, support inbox, and Store/Microsoft evidence. |
| 2026-05-30 v71 | Current single-machine evidence was refreshed after second-PC return archive flow. | Current smoke evidence: `20260530-100818-HUGH_SECOND` on commit `7294d65`, dashboard output `MUSU_RELEASE_SMOKE_OK_20260530_1008`, CLI route `MUSU_CLI_ROUTE_OK_20260530_1008`, dashboard task `695f1e1d-1d1b-46bd-8783-3eebb216842a`, bridge `http://127.0.0.1:2217`. First attempt failed before dashboard warm-up while Next compiled `/app`; rerun passed after dashboard warm-up. |
| 2026-05-30 v72 | Second-PC return ZIP importer was added. | `import-second-pc-return.ps1` imports `.local-build\second-pc-return\*.zip`, copies returned JSON to canonical `.local-build` roots, verifies MSIX install evidence, can record the MSIX install gate with `-RecordMsixInstall`, and emits primary-side multi-device commands. Final packet/action pack/status docs now prefer the importer over manual raw-file handling. |
| 2026-05-30 v73 | Current single-machine evidence was refreshed after the second-PC return importer commit. | Current smoke evidence: `20260530-224123-HUGH_SECOND` on commit `2eca040`, dashboard output `MUSU_RELEASE_SMOKE_OK_20260530_2240`, CLI route `MUSU_CLI_ROUTE_OK_20260530_2240`, dashboard task `dbb4d9dc-1c94-439c-89ae-98b43285f6b3`, bridge `http://127.0.0.1:3718`. |
| 2026-05-30 v74 | Current single-machine evidence was refreshed after documenting the second-PC return preview fallback. | Current smoke evidence: `20260530-225842-HUGH_SECOND` on commit `7c7d537`, dashboard output `MUSU_RELEASE_SMOKE_OK_20260530_2258`, CLI route `MUSU_CLI_ROUTE_OK_20260530_2258`, dashboard task `69d71cea-dec0-47af-a0bb-fdde166c080e`, bridge `http://127.0.0.1:3718`. |
| 2026-05-31 v75 | Current status audit was consolidated as wiki/522. | `docs/RELEASE_1_15_0_RC1_CURRENT_STATUS_AUDIT_2026_05_31.md` is now the current qualitative report, product spec lock, code audit, and next-step roadmap. Final operator packet generation and verification now include wiki/522 so handoff packets do not point only at the older wiki/521 report. Public desktop release remains No-Go until real second-PC MSIX install, real multi-device route, `musu@musu.pro` inbox delivery, and Partner Center/Microsoft Store approval evidence are recorded. |
| 2026-05-31 v76 | Windows/Tailscale mDNS health hang was mitigated. | During smoke revalidation, a logged-in default `~/.musu` with a failing Tailscale mDNS interface caused bridge `/health` to time out after the initial `musu up` probe. mDNS LAN auto-discovery is now opt-in via `MUSU_ENABLE_MDNS=1`; Store-candidate smoke/release runs use cloud/manual peer registration and second-PC handoff until mDNS has separate regression evidence. |
| 2026-05-31 v77 | Current single-machine evidence was refreshed after mDNS opt-in hardening. | Current smoke evidence: `20260531-154114-HUGH_SECOND` on commit `dbd90e3`, dashboard output `MUSU_RELEASE_SMOKE_OK_20260531_1540`, CLI route `MUSU_CLI_ROUTE_OK_20260531_1540`, dashboard task `9e00f24e-1bbc-4cbf-b6aa-7057fdfeb63c`, bridge `http://127.0.0.1:1407`. |
| 2026-05-31 v78 | Runtime hardening and relay-control became P0 release gates. | Second-PC MSIX install evidence was imported and recorded at `docs\evidence\msix-install\1.15.0-rc.1\20260531-165211-HUGH-MAIN.evidence.json`, closing the package-install proof but not the route proof. Operator-observed idle CPU busy-loop behavior on more than one PC changed the release decision from "externally blocked only" to "internally and externally blocked." Added wiki/523 runtime hardening/relay roadmap, `measure-musu-idle-cpu.ps1`, made clipboard polling opt-in via `MUSU_ENABLE_CLIPBOARD_SYNC=1`, and added `runtime_idle_cpu_verified` to go/no-go; idle CPU evidence must include a running MUSU runtime process and count Node.js/WebView2 in the CPU budget. |
| 2026-05-31 v79 | Runtime idle budget and P2P control-plane spec were tightened. | `measure-musu-idle-cpu.ps1` now separates MUSU, Node.js, and WebView2 roles, fails if no MUSU runtime process is sampled, and reports role counts/max CPU. The operator idle CPU command now includes `-IncludeNode -IncludeWebView2`. Frontend health/process/device/fleet polling was slowed and paused while hidden. The bridge `musu.pro` registration loop now defaults to `MUSU_CLOUD_HEARTBEAT_INTERVAL_SEC=300` with a 60s floor, backoff, and jitter. Added wiki/524 `MUSU_PRO_P2P_CONTROL_PLANE_SPEC_2026_05_31.md` to lock registry/rendezvous/path-selection/relay route evidence. |
| 2026-05-31 v80 | The three P0 blockers now have an execution plan and stricter route evidence gate. | Added wiki/525 `MUSU_RUNTIME_STABILIZATION_EXECUTION_PLAN_2026_05_31.md` to answer how idle CPU, `musu.pro` P2P assistance, and desktop hardening will be handled. `smoke-multidevice-beta.ps1` now emits `musu.route_evidence.v1`; `verify-multidevice-evidence.ps1` rejects passing evidence without route kind, handshake timing, peer identity verification, hardened encryption, payload transit truth, and success result. Rust cloud DTOs/client methods now cover rendezvous sessions, candidate endpoints, and route evidence. Legacy manual HTTP bearer routing is recorded honestly as unverified and cannot satisfy the final multi-device release gate. |
| 2026-05-31 v81 | Current single-machine evidence was refreshed after route-evidence gate hardening. | Current smoke evidence: `20260531-192015-HUGH_SECOND` on commit `5211ff2`, dashboard output `MUSU_RELEASE_SMOKE_OK`, CLI route `MUSU_CLI_ROUTE_OK`, dashboard task `986733bf-c10c-4434-bd8e-d9b0512ce193`, bridge `http://127.0.0.1:9818`. A first attempt failed because the dashboard dev server was not running; after starting Next on `127.0.0.1:3000`, `musu up --json` reported dashboard `ok` and the rerun passed. |
| 2026-05-31 v82 | Runtime CPU measurement was hardened against false WebView2/Node attribution. | `measure-musu-idle-cpu.ps1` now uses native Windows parent-process lookup instead of WMI/CIM, records helper process scope, and includes Node/WebView2 only when they are MUSU descendants or repo-related helpers by default; `-IncludeUnrelatedHelpers` remains available for whole-machine diagnostics. `write-release-go-no-go.ps1` now rejects runtime CPU evidence that omits Node/WebView2 budget flags, lacks a valid helper scope, times out process metadata, or cannot prove ownership in the default helper scope. Dashboard, NodePanel, and agents-surface polling moved from fixed intervals to non-overlapping 30s visible / 120s hidden recursive timeouts. A primary debug-runtime 60s diagnostic sample passed at `.local-build\runtime-idle-cpu\musu-idle-cpu-20260531-194854.json`, but the public release gate still needs packaged desktop/WebView2 samples on two PCs. |
| 2026-05-31 v83 | Current single-machine evidence was refreshed after idle CPU ownership hardening. | Current smoke evidence: `20260531-195832-HUGH_SECOND` on commit `3b1b1b0`, dashboard output `MUSU_RELEASE_SMOKE_OK`, CLI route `MUSU_CLI_ROUTE_OK`, dashboard task `d568c5f1-d15d-4cbf-8172-c4a308deaf95`, bridge `http://127.0.0.1:9818`. A first attempt failed while the dashboard dev server was still compiling; after `musu up --json` reported dashboard `ok`, the rerun passed and evidence was recorded. |
| 2026-05-31 v84 | Process ownership audit became a release gate. | Added `scripts\windows\audit-musu-process-ownership.ps1` and wired `process_ownership_verified` into go/no-go, final handoff status, and final operator packet verification. Local audit passed on `HUGH_SECOND` with one MUSU runtime, zero MUSU-owned Node helpers, zero MUSU-owned WebView2 helpers, zero repo-related orphan helpers, bridge registry PID alive, and `/health` HTTP 200; the 13 machine-wide WebView2 processes observed on the operator machine were not MUSU-owned. |
| 2026-05-31 v85 | Current single-machine evidence was refreshed after the process ownership release gate. | Current smoke evidence: `20260531-202354-HUGH_SECOND` on commit `9223404`, dashboard output `MUSU_RELEASE_SMOKE_OK`, CLI route `MUSU_CLI_ROUTE_OK`, dashboard task `89e6ab33-24de-4326-97f2-ebc2f6588209`, bridge `http://127.0.0.1:9818`. This keeps `single_machine_verified=true` after the process ownership gate code commit. |
| 2026-05-31 v86 | Startup single-instance audit became a release gate. | Added `scripts\windows\audit-musu-startup-single-instance.ps1` and wired `startup_single_instance_verified` into go/no-go, final handoff status, and final operator packet verification. Local evidence `20260531-203635-HUGH_SECOND` passed with three repeated `musu up --json` calls reusing bridge PID 31208, `after_musu_runtime=1`, `repeated_spawn_count=0`, and nested process ownership passing. |
| 2026-05-31 v87 | Current single-machine evidence was refreshed after the startup single-instance release gate. | Current smoke evidence: `20260531-205509-HUGH_SECOND` on commit `df49459`, dashboard output `MUSU_RELEASE_SMOKE_OK`, CLI route `MUSU_CLI_ROUTE_OK`, dashboard task `5b75f3cc-b140-43e9-8882-9e8822075128`, bridge `http://127.0.0.1:9818`. A first attempt failed while Next warmed `/app`; direct `musu up --json` then reported dashboard `ok` and the rerun passed. |
| 2026-05-31 v88 | Runtime idle CPU evidence became a resource-budget and desktop-open gate. | `measure-musu-idle-cpu.ps1` now records scenario, clean git state, process count budget, owned WebView2 budget, working set/private memory, and memory totals by role. `write-release-go-no-go.ps1` now requires `desktop-open` plus `-RequireOwnedWebView2` for final runtime evidence. Current bridge-only diagnostic `20260531-211448-HUGH_SECOND.bridge-only` passed with max one-core CPU `0.03%` and working set `27.7MB`; desktop-open attempt `20260531-211608-HUGH_SECOND.desktop-open-attempt` failed because owned WebView2 count was `0`. |
| 2026-05-31 v89 | Current single-machine evidence was refreshed after the runtime resource-budget gate fix. | Current smoke evidence: `20260531-212651-HUGH_SECOND` on commit `a89f6dd`, dashboard output `MUSU_RELEASE_SMOKE_OK`, CLI route `MUSU_CLI_ROUTE_OK`, dashboard task `0cdd096c-626d-46ee-95e4-c347805016d8`, bridge `http://127.0.0.1:9818`. A first attempt failed while Next warmed `/app`; direct `musu up --json` then reported dashboard `ok` and the rerun passed. |
| 2026-05-31 v90 | MSIX desktop entrypoint mismatch became a release blocker. | Added `scripts\windows\audit-msix-desktop-entrypoint.ps1` and wiki/526. Evidence `20260531-214327-HUGH_SECOND.store-msix-runtime-only` proves the current local-sideload/Store MSIX artifacts and installed package launch `musu.exe`, do not contain `musu-desktop.exe`, and still describe a CLI/bridge runtime package. `write-release-go-no-go.ps1` now reports `msix_desktop_entrypoint_verified=false`, `audit-desktop-release-readiness.ps1` reports `runtime_package_ready=false`, and `verify-store-submission-bundle.ps1` rejects the current Store bundle until the Tauri desktop shell is packaged as the MSIX application executable. |
| 2026-05-31 v91 | Idle polling and writer admission wakeups were reduced. | Added `musu-bee/src/lib/useLowDutyPolling.ts` and moved major dashboard/panel/inbox/fleet/company/machine polling to no-overlap, abort-on-unmount, hidden-tab pause, and capped failure backoff. Fleet/company/machine pages now use 30s safety-net polling plus SSE wakeups instead of 5s fixed refreshes. `musu-rs/src/writer/runner.rs` now uses `tokio::sync::Notify` for admission wakeups instead of waking queued tasks every 50ms. |
| 2026-05-31 v92 | Store MSIX artifact now packages the desktop entrypoint. | `build-msix.ps1` now stages `musu-desktop.exe` as the MSIX application executable, keeps `musu.exe` as the CLI alias, keeps `musu-startup.exe` as the startup task, and writes a desktop-shell description. Artifact evidence `20260531-224328-HUGH_SECOND.store-msix-desktop-artifact` passes, and regenerated Store bundle `.local-build\msix\submission-bundles\store-reviewed-20260531-224352` verifies with `ok=true`, `fail_count=0`. Installed evidence still fails until the fixed package is installed; source-fresh release build on HUGH_SECOND hit rustc OOM/pagefile pressure even with `CARGO_BUILD_JOBS=1`. |
| 2026-05-31 v93 | MSIX install evidence split and desktop process ownership were hardened. | Fixed local-sideload MSIX install now passes desktop-entrypoint audit (`20260531-232229-HUGH_SECOND.local-sideload-installed`), while Store-reviewed restricted-capability local install intentionally fails contract-mismatch audit (`20260531-232229-HUGH_SECOND.store-reviewed-contract-mismatch`). `install-msix.ps1` refuses Store-reviewed restricted-capability sideload by default; `write-release-go-no-go.ps1` now requires Store-reviewed artifact proof plus local-sideload installed proof for `msix_desktop_entrypoint_verified`. Process/CPU audits include `musu-desktop` as a root so packaged WebView2 children are attributed; local process ownership passed with one runtime, one desktop shell, six owned WebView2 helpers, and zero owned Node helpers. Clean primary desktop-open CPU diagnostic passed at `.local-build\runtime-idle-cpu\musu-idle-cpu-20260531-235500.json` with max one-core CPU `musu=0.03`, `webview2=0.03`, and working set `363.45MB`; second-PC desktop CPU evidence is still pending. `musu up` now terminates live-but-unhealthy registered bridge PIDs before restart only if the PID resolves to a MUSU runtime executable; `cargo check -j 1` passes, while local rustc builds still hit OOM/pagefile pressure. |
| 2026-06-01 v94 | Brand/public assets and current release audit were refreshed. | The app favicon/mark is the Tauri/MSIX icon under `musu-bee/src-tauri/icons/`; `musu-bee/public/images/favicon-header.png` now tracks that mark for web headers. `musu-bee/.gitignore` no longer drops required public runtime assets, and `/agents/*.png` plus the favicon are now tracked for clean checkout/CI builds. `MusuLogo` no longer points at missing `/images/logos/*` lockups; it renders the tracked mark plus a token-colored MUSU wordmark. Added wiki/527 qualitative audit and updated wiki/524 with 2026-06-01 implementation status. `npm run typecheck` passed. A current single-machine smoke attempt on commit `5e8d195` failed because dashboard task-status polling timed out after the local Next dev server became unreachable and PowerShell log-tail commands hit OOM/CLR errors; no new release evidence was recorded. Public release remains No-Go until current smoke, two-machine desktop CPU, real route evidence, `musu@musu.pro` inbox delivery, and Store evidence are recorded. |
| 2026-06-01 v95 | Single-machine smoke and mDNS/Tailscale IPv6 hardening were tightened. | `smoke-single-machine-beta.ps1` now generates unique dashboard/CLI expected strings per run to avoid duplicate-task 409s, retries dashboard task-status polling inside the task deadline, and records poll error count/last error in evidence. The supplied `mdns_sd::service_daemon` errors (`Tailscale` IPv6 link-local, `os error 10065`, then `closed channel`) are now treated as a real idle/log-spam risk: mDNS remains opt-in via `MUSU_ENABLE_MDNS=1`, and IPv6 mDNS is separately opt-in via `MUSU_MDNS_ENABLE_IPV6=1`. Default mDNS daemon setup disables IPv6 interfaces, so `musu discover` no longer sends to Tailscale IPv6 by default. `cargo check -j 1` and `cargo build --bin musu -j 1` passed locally; `musu discover --timeout 2` completed without the Tailscale IPv6 send errors. |
| 2026-06-01 v96 | Current single-machine evidence was refreshed after smoke and mDNS IPv6 hardening. | Current smoke evidence: `20260601-012801-HUGH_SECOND` on commit `d4820173dab1f19abf0ac287abbd073330f6eb1b`, dashboard output `MUSU_RELEASE_SMOKE_OK_20260601_012735`, CLI route `MUSU_CLI_ROUTE_OK_20260601_012735`, dashboard task `fe857b79-47af-47d8-abf0-80bcbb63d883`, bridge `http://127.0.0.1:10474`, `dashboard_task_poll_error_count=0`. Public release remains No-Go until two-machine desktop-open CPU evidence, hardened multi-device route evidence, `musu@musu.pro` inbox delivery evidence, and Store submission/release evidence are recorded. |
| 2026-06-01 v97 | Runtime CPU evidence freshness was hardened and primary desktop-open evidence was recorded. | `write-release-go-no-go.ps1` now rejects runtime idle CPU evidence whose `git_commit` is older than current HEAD unless the delta is docs/evidence-only, and final packet verification checks for that guard. This invalidated old `b246db7` CPU samples as intended. Initial primary evidence `20260601-014656-HUGH_SECOND.desktop-open` passed on commit `2987f15` but was later superseded by a current HEAD sample after script/logo changes. Runtime CPU gate still needs second-PC desktop-open evidence. |
| 2026-06-01 v98 | Current single-machine evidence was refreshed after runtime CPU gate hardening. | Current smoke evidence: `20260601-015424-HUGH_SECOND` on commit `e4509f6628fb2e54ef0c127c412722a3e52cf80f`, dashboard output `MUSU_RELEASE_SMOKE_OK_20260601_015402`, CLI route `MUSU_CLI_ROUTE_OK_20260601_015402`, dashboard task `bc27c955-bd1e-41e1-8ff6-c60a38328955`, bridge `http://127.0.0.1:5076`, `dashboard_task_poll_error_count=0`. |
| 2026-06-01 v99 | Second-PC return flow now carries runtime CPU evidence. | `run-second-pc-release-check.ps1` now captures the release-grade `desktop-open` runtime CPU sample by default after MSIX install and handoff: it opens MUSU Desktop, runs `measure-musu-idle-cpu.ps1` for 60s with owned WebView2/Node/WebView2 budgets, records `.local-build\runtime-idle-cpu\*.evidence.json`, and includes it in the second-PC return zip. `prepare-multidevice-test-kit.ps1`, final packet/action-pack verifiers, and the second-PC runbook now require/explain the CPU script. `import-second-pc-return.ps1` now resolves returned evidence by JSON schema and copies runtime CPU evidence to `.local-build\runtime-idle-cpu\` without confusing it with MSIX install evidence. |
| 2026-06-01 v100 | Favicon mark was promoted into real public logo assets. | The strong app/favicon mark remains the source of truth at `musu-bee\src-tauri\icons\icon.png` and `musu-bee\public\images\favicon-header.png`. Added `scripts\windows\generate-brand-logo-assets.ps1`, unignored `musu-bee\public\images\logos\`, and generated `musu-mark-512.png` plus header/display/hero transparent PNG lockups for `on-light`, `on-dark`, and `on-yellow`. These are now usable for Store, README, landing page, and launch handoff materials; screenshots/demo media remain separate release assets. |
| 2026-06-01 v101 | Single-machine evidence was refreshed after second-PC CPU-return and logo assets. | At that point, smoke evidence `20260601-022021-HUGH_SECOND` on commit `47f261475abf7a88deb7632a27c242a97b603771` passed with dashboard output `MUSU_RELEASE_SMOKE_OK_20260601_021958`, CLI route `MUSU_CLI_ROUTE_OK_20260601_021958`, dashboard task `04c4cae9-4b5c-4db3-bfb5-92e5429d8768`, bridge `http://127.0.0.1:1923`, and `dashboard_task_poll_error_count=0`. First rerun failed because the Next dev server was not running; after starting `next dev` on `127.0.0.1:3000`, `musu up --json` reported dashboard `ok` and the rerun passed. |
| 2026-06-01 v102 | Primary runtime idle CPU evidence was refreshed after the code/script commit. | Superseded primary packaged desktop-open evidence `docs\evidence\runtime-idle-cpu\1.15.0-rc.1\20260601-022649-HUGH_SECOND.desktop-open.evidence.json` on commit `ee8b0862accca12f3cfa93a2129605e7eae0596a` passed with packaged `musu-desktop.exe`, owned WebView2 `6`, owned Node `0`, max one-core CPU `musu=0`, `webview2=0.18`, total working set `340.21MB`, and `git_dirty=false`. It was later superseded by v105 after route/relay diagnostics. |
| 2026-06-01 v103 | Route/relay diagnostic CLI surfaces were added. | `musu route --explain [--json]` emits `musu.route_explain.v1` without executing a task, showing selected candidate/source, route-kind classification (`lan`, `tailscale`, `direct_quic`, or `local`), current `http_bearer` transport, and why the route cannot satisfy release evidence yet. `musu relay status [--json]` emits `musu.relay_status.v1`, showing login/cache/client readiness while explicitly reporting `bridge_path_selection_wired=false`, `relay_transport_wired=false`, and `release_route_evidence_ready=false`. `cargo check --manifest-path .\musu-rs\Cargo.toml -j 1` passed. Targeted `cli_commands` unit tests passed for both `--lib` and `--bin musu`; an earlier broad filtered `cargo test cli_commands` hit unrelated Windows elevation error 740 when Cargo tried to execute the filtered `r6_auto_update` integration test binary. |
| 2026-06-01 v104 | Current single-machine evidence was refreshed after route/relay diagnostics. | Current smoke evidence: `docs\evidence\single-machine\1.15.0-rc.1\20260601-030639-HUGH_SECOND.evidence.json` on commit `12f8cabaefb5722eb45941012d47848d95c9f895`, dashboard output `MUSU_RELEASE_SMOKE_OK_20260601_030619`, CLI route `MUSU_CLI_ROUTE_OK_20260601_030619`, dashboard task `42449837-4936-4e79-82a6-d7bbeede6108`, bridge `http://127.0.0.1:14130`, and `dashboard_task_poll_error_count=0`. First attempt failed while Next warmed `/app`; after `/app` and `/api/doctor` returned 200, rerun passed. |
| 2026-06-01 v105 | Primary runtime idle CPU evidence was refreshed after route/relay diagnostics. | Current primary packaged desktop-open evidence: `docs\evidence\runtime-idle-cpu\1.15.0-rc.1\20260601-030838-HUGH_SECOND.desktop-open.evidence.json` on commit `e0eebac44184e285ba120df5b4e0e9b209e83692`, with `git_dirty=false`, packaged `musu-desktop.exe`, owned WebView2 `6`, owned Node `0`, max one-core CPU `musu=0`, `webview2=0.21`, total working set `371.96MB`, total private memory `191.95MB`, and no resource-budget violations. Go/no-go still blocks release until second-PC desktop-open CPU evidence and hardened multi-device route evidence arrive. |
| 2026-06-01 v106 | Final go/no-go after route diagnostics remains an intentional No-Go. | `write-release-go-no-go.ps1 -Json` from clean HEAD `0c09f0c` reported `ready_for_public_desktop_release=false`, `local_artifacts_ready=true`, `single_machine_verified=true`, `msix_install_verified=true`, `msix_desktop_entrypoint_verified=true`, `process_ownership_verified=true`, `startup_single_instance_verified=true`, `public_metadata_ok=true`, `manifest_dirty=false`, and runtime CPU valid machine count `1`. Remaining blockers are exactly: real second-PC multi-device evidence, runtime idle CPU evidence on at least two machines, `musu@musu.pro` delivery verification, and Partner Center/Store release evidence. |
| 2026-06-01 v107 | Bridge direct path selection was wired into the shared router. | `musu-rs/src/bridge/router.rs` now classifies endpoints as `local`, `lan`, `tailscale`, or `direct_quic`, sorts candidates by LAN -> Tailscale -> direct public endpoint, preserves circuit-breaker filtering, and uses the shared selector for explicit target, GPU, and OS-hint routing. `musu route --explain --json` and `musu relay status --json` now report `bridge_path_selection_wired=true` while still reporting `rendezvous_session_wired=false`, `relay_transport_wired=false`, and `release_route_evidence_ready=false`. Validation passed: `cargo check --manifest-path .\musu-rs\Cargo.toml -j 1`, targeted `router` and `cli_commands` unit tests, `cargo build --manifest-path .\musu-rs\Cargo.toml --bin musu -j 1`, `git diff --check`, and both diagnostic CLI JSON commands. |
| 2026-06-01 v108 | Current single-machine evidence was refreshed after bridge path selection. | Current smoke evidence: `docs\evidence\single-machine\1.15.0-rc.1\20260601-033633-HUGH_SECOND.evidence.json` on commit `efff90cc9df3419f0cadc0398e5dd63d10f235de`, dashboard output `MUSU_RELEASE_SMOKE_OK_20260601_033608`, CLI route `MUSU_CLI_ROUTE_OK_20260601_033608`, dashboard task `759429bc-001e-4a05-9a68-88a4c44ad3c1`, bridge `http://127.0.0.1:13899`, and `dashboard_task_poll_error_count=0`. |
| 2026-06-01 v109 | Primary runtime idle CPU evidence was refreshed after bridge path selection. | Current primary packaged desktop-open evidence: `docs\evidence\runtime-idle-cpu\1.15.0-rc.1\20260601-033734-HUGH_SECOND.desktop-open.evidence.json` on commit `1163396a26cc81ce045f6de44f35fe0029074522`, with `git_dirty=false`, packaged `musu-desktop.exe`, owned WebView2 `6`, owned Node `0`, max one-core CPU `musu=0`, `webview2=0.16`, total working set `376.6MB`, total private memory `193.27MB`, and no resource-budget violations. Release still needs second-PC desktop-open CPU evidence. |
| 2026-06-01 v110 | CLI route attempts now write actual route evidence files. | `musu route --route-evidence-path <path>` writes `musu.route_evidence.v1` from the real CLI route attempt with route kind, candidate address, submit/handshake timing, total attempt timing, success/failure result, and the current legacy transport gap (`peer_identity_verified=false`, `encryption=none_http_bearer`). `smoke-multidevice-beta.ps1` now imports that CLI-written evidence instead of synthesizing route evidence from script-side inference. Validation passed: `cargo check --manifest-path .\musu-rs\Cargo.toml -j 1`, `cargo test --manifest-path .\musu-rs\Cargo.toml --lib cli_commands -- --nocapture`, `cargo test --manifest-path .\musu-rs\Cargo.toml --bin musu cli_commands -- --nocapture`, `cargo test --manifest-path .\musu-rs\Cargo.toml --lib router -- --nocapture`, PowerShell parser validation for `smoke-multidevice-beta.ps1`, and `git diff --check`. |
| 2026-06-01 v111 | Current single-machine evidence was refreshed after the CLI route evidence writer. | Current smoke evidence: `docs\evidence\single-machine\1.15.0-rc.1\20260601-040308-HUGH_SECOND.evidence.json` on commit `e8ac5a88c1dd9437c965f2d2e2f2c3b331596c2a`, dashboard output `MUSU_RELEASE_SMOKE_OK_20260601_040245`, CLI route `MUSU_CLI_ROUTE_OK_20260601_040245`, dashboard task `612c8aff-616e-4227-89dc-7023a77d4830`, bridge `http://127.0.0.1:13800`, and `dashboard_task_poll_error_count=0`. |
| 2026-06-01 v112 | Primary runtime idle CPU evidence was refreshed after the CLI route evidence writer. | Current primary packaged desktop-open evidence: `docs\evidence\runtime-idle-cpu\1.15.0-rc.1\20260601-040413-HUGH_SECOND.desktop-open.evidence.json` on commit `b330de871364877f990cd0cc4d5a6e6f8666a53f`, with `git_dirty=false`, packaged `musu-desktop.exe`, owned WebView2 `6`, owned Node `0`, max one-core CPU `musu=0`, `webview2=0.21`, total working set `369.66MB`, total private memory `188.25MB`, and no resource-budget violations. Release still needs second-PC desktop-open CPU evidence. |
| 2026-06-01 v113 | Runtime route evidence and mDNS/Tailscale hardening were extended. | `musu-rs/src/bridge/route_evidence.rs` now owns the shared `musu.route_evidence.v1` builder/writer used by CLI and runtime paths. `/api/tasks/delegate`, `/api/companies/{id}/run`, and workflow remote steps write local `~/.musu/route-evidence/<task_id>.route-evidence.json` files with real forwarding timing, retry result, and failure class. mDNS now default-disables Tailscale interfaces unless `MUSU_MDNS_ENABLE_TAILSCALE=1`, in addition to the existing `MUSU_ENABLE_MDNS` and `MUSU_MDNS_ENABLE_IPV6` gates. Added wiki/528 current audit. Validation passed: `cargo check --manifest-path .\musu-rs\Cargo.toml -j 1`, `cargo test --manifest-path .\musu-rs\Cargo.toml -j 1 --lib cli_commands -- --nocapture`, targeted `rustfmt --check`, and `git diff --check`; filtered `route_evidence` test execution was stopped after excessive MSVC test-binary compile time on this machine. |
| 2026-06-01 v114 | Route evidence now has a first `musu.pro` receiver stub and runtime submit path. | Added authenticated `POST /api/v1/p2p/route-evidence` in `musu-bee`, validating `musu.route_evidence.v1` and returning `release_grade` plus `blockers` while reporting `stored=false` until durable storage lands. Runtime bridge forwarding now converts local evidence into the cloud DTO and starts a background best-effort submit after local write when `~/.musu/token` exists, so cloud latency does not delay the user task. `MUSU_CLOUD_BASE_URL` now controls cloud client base URL for login/registry/evidence tests. This does not close the release gate: current transport still records `peer_identity_verified=false` and `encryption=none_http_bearer`; rendezvous, identity proof, QUIC/TLS, durable evidence storage, two-machine CPU evidence, `musu@musu.pro` inbox evidence, and Store evidence remain open. Validation passed: Next route tests, `npm run typecheck`, `cargo check --manifest-path .\musu-rs\Cargo.toml -j 1`, `cargo test --manifest-path .\musu-rs\Cargo.toml -j 1 --lib route_evidence -- --nocapture`, targeted rustfmt on changed Rust files, and `cargo fmt --manifest-path .\musu-rs\Cargo.toml --check`. |
| 2026-06-01 v115 | Route evidence is now stored and queryable on the control plane. | `POST /api/v1/p2p/route-evidence` now stores valid authenticated evidence and returns `stored=true` plus `evidence_id`; `GET /api/v1/p2p/route-evidence` returns stored records with filters for source, target, route kind, result, and release-grade status. Hosted storage uses Vercel KV/Upstash Redis as a capped list; local/dev uses `MUSU_ROUTE_EVIDENCE_STORE_PATH`; production fails closed without KV unless an explicit persistent file path is set. This improves auditability but still does not close public release because rendezvous, peer identity, QUIC/TLS proof, account-scoped evidence ownership/export, two-machine CPU evidence, `musu@musu.pro` inbox evidence, and Store evidence remain open. Validation passed: `npx tsx --test src/app/api/v1/p2p/route-evidence/route.test.ts`, `npm run typecheck`, `git diff --check`, and indexer sync `1063 files / 1997 symbols`. |
| 2026-06-01 v116 | The first `musu.pro` rendezvous session API is implemented. | Added authenticated short-lived rendezvous endpoints in `musu-bee`: `POST /api/v1/p2p/rendezvous`, `GET /api/v1/p2p/rendezvous/:id`, `POST /:id/candidates`, `POST /:id/approve`, and `POST /:id/close`. Sessions store source/target candidate sets, relay capability, public key, capabilities, approval state, and close state using Vercel KV or explicit local/dev file fallback. This moves the control plane from route-evidence-only to candidate exchange, but public release is still blocked because bridge runtime route attempts do not yet create/use rendezvous sessions and transport identity/encryption proof is still missing. Validation passed: rendezvous + route-evidence API tests, `npm run typecheck`, `git diff --check`, and indexer sync `1072 files / 2034 symbols`. |
| 2026-06-01 v117 | Bridge runtime route attempts now use `musu.pro` rendezvous sessions. | Added `musu-rs/src/bridge/rendezvous.rs` and wired remote forwarding through the short-lived control-plane lifecycle: source creates/refreshes a rendezvous session, publishes its advertised bridge endpoint, attaches `rendezvous_session_id` to the forwarded task, target publishes local candidates on receipt, source closes the session after terminal success/failure, and route evidence records the session id when present. Added `rendezvous_target_node_id` so target candidate publish can match sessions even when source selected a manual addr fallback, and added `MUSU_P2P_RENDEZVOUS_CLIENT_TIMEOUT_MS` with bounded fail-open behavior to the selected peer path. `musu relay status --json` and `musu route --explain --json` now report `rendezvous_session_wired=true`. Public release remains blocked because transport is still legacy HTTP bearer, peer identity/QUIC-TLS proof and relay fallback are absent, and real second-PC two-sided rendezvous evidence is missing. Validation passed: `cargo check --manifest-path .\musu-rs\Cargo.toml -j 1`, targeted `rendezvous`, `route_evidence`, and `cli_commands` tests for lib/bin, `cargo build --manifest-path .\musu-rs\Cargo.toml --bin musu -j 1`, both diagnostic CLI JSON commands, `git diff --check`, `cargo fmt --check`, and indexer sync `1074 files / 2050 symbols`. |
| 2026-06-01 v118 | Rendezvous target candidates can affect actual forwarding. | `musu-rs/src/bridge/router.rs` now exposes `select_best_remote_candidate` for already-authorized concrete endpoints, and `musu-rs/src/bridge/rendezvous.rs` maps refreshed session target candidates into `ResolvedPeer`s, ignores relay/failed candidates until relay transport exists, and chooses LAN -> Tailscale -> direct public endpoint. `forward_to_peer_with_retry` now forwards to that route peer when present, falls back once to the original selected peer if the rendezvous candidate is stale/failing, and returns the actual route peer in success/failure reports, so circuit breaker updates and route evidence use the actual candidate address. `musu-bee/src/lib/p2pRendezvousStore.ts` now caches recent node candidate sets from `/candidates` updates and seeds new rendezvous sessions from that cache when available, so target candidates can affect a later route attempt instead of relying only on same-session timing. Public release remains blocked by legacy HTTP bearer transport, missing peer identity/QUIC-TLS proof, missing relay fallback, and missing real second-PC target-candidate evidence. Validation passed: `cargo check --manifest-path .\musu-rs\Cargo.toml -j 1`, targeted `rendezvous`, `router`, `route_evidence`, and `cli_commands` Rust tests for lib/bin, `cargo build --manifest-path .\musu-rs\Cargo.toml --bin musu -j 1`, `npx tsx --test src/app/api/v1/p2p/rendezvous/route.test.ts`, `npx tsx --test src/app/api/v1/p2p/route-evidence/route.test.ts`, `npm run typecheck`, both diagnostic CLI JSON commands, `cargo fmt --check`, `git diff --check`, and indexer sync `1074 files / 2064 symbols`. |
| 2026-06-01 v119 | Rendezvous and route evidence now carry advertised peer identity material. | `musu-rs/src/install/tls.rs` can compute the SHA-256 fingerprint of the local TLS certificate. Logged-in bridge startup registers that fingerprint as `cert_fingerprint`/`peer_public_key`, and `musu-rs/src/bridge/rendezvous.rs` publishes it as candidate `public_key` when `~/.musu/tls/cert.pem` or `MUSU_TLS_CERT` exists. Refreshed target candidates carry the advertised fingerprint into `ResolvedPeer.meta`, and `bridge::route_evidence` records `peer_identity_method=advertised_tls_cert_fingerprint_unverified` plus `peer_public_key` when present. `musu.pro` route-evidence validation now rejects release-grade claims where `peer_identity_verified=true` but identity method/key material is missing. This is still not release-grade identity: current route attempts remain `peer_identity_verified=false`, `encryption=none_http_bearer`, and release is blocked until the transport proves remote key ownership over QUIC/TLS on a real second-PC route. Validation passed: `cargo check --manifest-path .\musu-rs\Cargo.toml -j 1`, targeted Rust `tls`, `rendezvous`, `route_evidence`, and `cli_commands` tests for lib/bin, `cargo build --manifest-path .\musu-rs\Cargo.toml --bin musu -j 1`, route-evidence and rendezvous API tests, `npm run typecheck`, both diagnostic CLI JSON commands, `cargo fmt --check`, `git diff --check`, and indexer sync `1075 files / 2071 symbols`. |
| 2026-06-01 v120 | HTTPS bridge forwarding now verifies advertised peer certificate fingerprints. | Candidate endpoints and registry metadata now preserve `scheme`, TLS-enabled bridge advertisements use `https://`, and `forward_to_peer_attempt` uses a fingerprint-pinned rustls client for HTTPS peers with advertised `sha256:<hex>` identity material. Route evidence now records `peer_identity_verified=true`, `peer_identity_method=tls_cert_fingerprint_pin`, and `encryption=https_tls_fingerprint_pin` only after the actual HTTPS POST succeeds with a matching server certificate fingerprint. `musu.pro` still keeps this non-release-grade with `transport_not_release_grade_quic_tls`; public release remains blocked until final QUIC/TLS route proof, real second-PC route evidence, relay fallback, two-machine CPU evidence, `musu@musu.pro` inbox evidence, and Store evidence are recorded. Validation passed: `cargo check --manifest-path .\musu-rs\Cargo.toml -j 1`, targeted Rust `forward`, `route_evidence`, `rendezvous`, `services`, `discovery`, and `cli_commands` tests for lib/bin, `cargo build --manifest-path .\musu-rs\Cargo.toml --bin musu -j 1`, route-evidence and rendezvous API tests, and `npm run typecheck`. |
| 2026-06-01 v121 | Route and relay diagnostics now expose HTTPS fingerprint pinning without claiming release readiness. | `musu relay status --json` now reports `https_fingerprint_pinning_wired=true` and `release_grade_transport_required=quic_tls_1_3`. `musu route --explain --json` reports the same fields and now distinguishes advertised fingerprint availability from verified identity and release-grade transport. Candidate diagnostics can show `transport_scheme`, `peer_identity_method`, `peer_public_key_present`, and `https_fingerprint_pin_available` while keeping advertised-only candidates unverified. This makes the next operator step explicit: verify HTTPS fingerprint pinning on a real second-PC route, then replace bridge HTTP/TLS with QUIC/TLS route proof. Validation passed: `cargo check --manifest-path .\musu-rs\Cargo.toml -j 1`, targeted `cli_commands` tests for lib/bin, `cargo build --manifest-path .\musu-rs\Cargo.toml --bin musu -j 1`, `musu relay status --json`, `musu route --explain --json "MUSU_ROUTE_EXPLAIN_CHECK"`, and `cargo fmt --check`. |
| 2026-06-01 v122 | Route evidence storage is now token-owner scoped. | `musu-bee/src/lib/p2pControlAuth.ts` now derives an `owner_key` as a SHA-256 hash of the accepted Bearer token, and `musu-bee/src/lib/routeEvidenceStore.ts` stores/query-filters route evidence by that owner key. `POST /api/v1/p2p/route-evidence` returns `owner_scoped=true`; `GET` only returns records for the caller's token-derived owner key and omits the stored key from response records. This closes the shared-bucket leakage gap in the current stub, while real account-id mapping, UI/export, retention policy, second-PC proof, QUIC/TLS route proof, relay fallback, two-machine CPU evidence, support inbox evidence, and Store evidence remain open. Validation passed: `npx tsx --test src/app/api/v1/p2p/route-evidence/route.test.ts`, `npm run typecheck`, `git diff --check`, and indexer sync `1078 files / 2107 symbols`. |
| 2026-06-01 v123 | Relay fallback now has a fail-closed control-plane lease boundary. | Added `musu-bee/src/app/api/v1/p2p/relay/lease/route.ts` and `musu-bee/src/lib/p2pRelayLeaseStore.ts`. `POST /api/v1/p2p/relay/lease` validates fallback requests, defaults to `lease_issued=false`, and only issues owner-scoped leases when direct routes failed and relay env policy is explicitly enabled: `MUSU_P2P_RELAY_ENABLED=1`, `MUSU_P2P_RELAY_TRANSPORT_WIRED=1`, `MUSU_P2P_RELAY_URL`, and `MUSU_P2P_RELAY_ENTITLEMENT=connect|pro|enterprise`. `GET` returns only the caller's token-owner scoped leases and omits `owner_key`. Rust cloud DTOs now include relay lease request/response plus `request_relay_lease`, and `musu relay status --json` reports `relay_control_plane_lease_wired=true`, `relay_lease_endpoint=/api/v1/p2p/relay/lease`, `relay_default_data_path=false`, and still `relay_transport_wired=false`. This prevents relay from becoming a silent default data path, but actual relay/tunnel transport, real account entitlement mapping, QUIC/TLS route proof, real second-PC proof, two-machine CPU evidence, support inbox evidence, and Store evidence remain open. Validation passed: relay lease API tests, combined P2P API tests, `npm run typecheck`, `cargo test --manifest-path .\musu-rs\Cargo.toml -j 1 --lib cloud::tests -- --nocapture`, `cargo check --manifest-path .\musu-rs\Cargo.toml -j 1`, `cargo build --manifest-path .\musu-rs\Cargo.toml --bin musu -j 1`, `cargo fmt --check`, `musu relay status --json`, and indexer sync `1082 files / 2141 symbols`. |
| 2026-06-01 v124 | Current primary desktop-open runtime CPU evidence was refreshed after the relay lease control-plane commit. | Opened the installed `Yellowhama.MUSU_1.15.0.0_x64__ygcjq669as2b6` Start app and ran `measure-musu-idle-cpu.ps1` for the release gate scenario: `-SampleSeconds 60 -Scenario desktop-open -RequireOwnedWebView2 -MaxOneCorePercent 5 -MaxOwnedProcessCount 16 -MaxOwnedWebView2ProcessCount 8 -MaxTotalWorkingSetMb 1024 -IncludeNode -IncludeWebView2 -FailOnHot`. Evidence `docs\evidence\runtime-idle-cpu\1.15.0-rc.1\20260601-082822-HUGH_SECOND.desktop-open.evidence.json` passed with `git_commit=cdefc1226481d554d2d151e6a08af4dc81572247`, `git_dirty=false`, 60.015s sample, one `musu-desktop` process, six owned WebView2 helpers, zero owned Node helpers, max one-core CPU `musu=0` and `webview2=0.16`, total working set `339.85MB`, total private memory `184.58MB`, and no resource budget violations. Post-commit go/no-go accepted this evidence as one valid machine (`valid_machine_count=1`, `HUGH_SECOND`) with `manifest_dirty=false`; release still needs second-PC desktop-open evidence, fresh single-machine smoke after the latest code commit, hardened route proof, support inbox evidence, and Store evidence. Indexer sync recorded `1084 files / 2141 symbols`. |
| 2026-06-01 v125 | Current single-machine smoke evidence was refreshed after the latest evidence/docs commit. | Started Next dev on `127.0.0.1:3000`, warmed `/app` after the first smoke attempt found dashboard readiness `warn`, then reran `smoke-single-machine-beta.ps1` against `musu-rs\target\debug\musu.exe`. Recorded evidence `docs\evidence\single-machine\1.15.0-rc.1\20260601-084028-HUGH_SECOND.evidence.json` with verification and summary. It passed on commit `a1ee33fa0c8e3b68e85dc4b48077134ec5dd99ac` with dashboard output `MUSU_RELEASE_SMOKE_OK_20260601_084005`, CLI output `MUSU_CLI_ROUTE_OK_20260601_084005`, dashboard task `5ac5baa6-471f-4633-9a57-9e3a87a20c7a`, bridge `http://127.0.0.1:13167`, and `cli_route_checked=true`. Go/no-go now reports `single_machine_verified=true`; release remains blocked by real second-PC multi-device evidence, two-machine desktop-open CPU evidence, `musu@musu.pro` delivery evidence, Store evidence, and dirty state until this evidence is committed. |
| 2026-06-01 v126 | The repeated Tailscale IPv6 mDNS errors were classified against current source defaults. | The operator re-supplied logs where `mdns_sd::service_daemon` repeatedly attempted `[ff02::fb%9]:5353` on the Windows `Tailscale` adapter and failed with `os error 10065`, then `sending on a closed channel`. Current source already treats this as an idle CPU/log-spam risk: bridge mDNS is opt-in through `MUSU_ENABLE_MDNS=1`, IPv6 mDNS through `MUSU_MDNS_ENABLE_IPV6=1`, and Tailscale mDNS interfaces through `MUSU_MDNS_ENABLE_TAILSCALE=1`. The release docs now state that default Store-candidate runs should not hit Tailscale IPv6 mDNS; if an installed desktop still emits this with defaults, assume stale installed bits or an explicit mDNS/IPv6/Tailscale opt-in environment until proven otherwise. |
| 2026-06-01 v127 | Runtime idle CPU gate reporting now exposes the machine count at top level. | `write-release-go-no-go.ps1` now emits `runtime_idle_cpu_min_machine_count`, `runtime_idle_cpu_valid_machine_count`, `runtime_idle_cpu_valid_machines`, and `runtime_idle_cpu_candidate_count` next to `runtime_idle_cpu_verified`, and its text output prints `runtime_idle_cpu_valid_machines: valid/min [machines]`. `show-final-release-handoff-status.ps1` carries the same fields under `gates`. This prevents operator/agent summaries from missing the real state: current HEAD reports `runtime_idle_cpu_verified=false`, `valid_machine_count=1`, `min_machine_count=2`, `valid_machines=[HUGH_SECOND]`, and `candidate_count=24`. Validation passed: PowerShell parser checks for both scripts, go/no-go JSON smoke, handoff-status JSON smoke, and `git diff --check`. |
| 2026-06-01 v128 | Current single-machine smoke evidence was refreshed after runtime CPU gate reporting code changed. | Started Next dev on `127.0.0.1:3000`, warmed `/app` and `/api/doctor`, then reran `smoke-single-machine-beta.ps1` against `musu-rs\target\debug\musu.exe`. Recorded evidence `docs\evidence\single-machine\1.15.0-rc.1\20260601-090548-HUGH_SECOND.evidence.json` with verification and summary. It passed on commit `da4999081073018ab3b1b72a26645140ad2e68f7` with dashboard output `MUSU_RELEASE_SMOKE_OK_20260601_090528`, CLI output `MUSU_CLI_ROUTE_OK_20260601_090528`, dashboard task `38d1eb2c-1905-493b-b536-459866c25c78`, bridge `http://127.0.0.1:5089`, evidence SHA-256 `52f34b6bc377404e118ec429bdc2c3d9c781d622870debee0d2d62a67e6eaae7`, and verification SHA-256 `70f8665ddcc015305b539bf8832bcf57b4d4507c1f07d7284a1d0bfd59f7f22a`. Primary desktop-open CPU evidence must be refreshed next because the reporting code commit made the previous CPU evidence stale. |
| 2026-06-01 v129 | mDNS default filtering now excludes common VPN/virtual adapters, not only Tailscale/IPv6. | The repeated `ff02::fb%9` Tailscale log remains a real stale/opt-in mDNS failure class. `musu-rs/src/peer/mdns.rs` now also filters common VPN/virtual adapters by default (`Tailscale`, `NordLynx`, WireGuard/wg, ZeroTier, vEthernet/WSL/Hyper-V, Docker, VMware, VirtualBox, tun/tap/utun/VPN names) and exposes `MUSU_MDNS_ENABLE_VIRTUAL_INTERFACES=1` only for explicit widening. Validation passed: `cargo test --lib -j 1 peer::mdns::tests::`, `cargo build --bin musu -j 1`, and `RUST_LOG=debug musu discover --timeout 2`; the discover run disabled 9 virtual/VPN interfaces and sent only on the physical `이더넷 2` LAN adapter, with no Tailscale/NordLynx/vEthernet/`ff02::fb`/`10065`/`closed channel` output. |
| 2026-06-01 v130 | Current smoke and primary desktop-open CPU evidence were refreshed after mDNS virtual-interface filtering. | Current smoke evidence `docs\evidence\single-machine\1.15.0-rc.1\20260601-093958-HUGH_SECOND.evidence.json` passed from commit `4ad4b5591bba3c03fffe7eb2d054a4e191b67bea` with dashboard output `MUSU_RELEASE_SMOKE_OK_20260601_093933`, CLI output `MUSU_CLI_ROUTE_OK_20260601_093933`, dashboard task `61f5d95d-ec59-418d-a583-a47336cdf126`, bridge `http://127.0.0.1:9189`, evidence SHA-256 `8ad3d4031f1b9592f6d6cabdd51a0ace8a9e78a08a527fde5716ecca2529d953`, and verification SHA-256 `b4695c68131c47627bbc08dc1d45284db5a0ae1ba07954a4627e8a413ac7279c`. Current primary packaged `desktop-open` CPU evidence `docs\evidence\runtime-idle-cpu\1.15.0-rc.1\20260601-094127-HUGH_SECOND.desktop-open.evidence.json` passed from clean commit `931e771650cda3026630cf0b2394c83211490dc6` with 60.029s sample, one `musu-desktop`, six owned WebView2 helpers, owned Node `0`, max one-core CPU `musu=0`, `webview2=0.08`, total working set `340.49MB`, private memory `181.8MB`, and no resource-budget violations. Release still needs second-PC desktop-open CPU evidence, hardened second-PC route evidence, `musu@musu.pro` inbox delivery evidence, and Store evidence. |
| 2026-06-01 v131 | Runtime CPU scenario matrix diagnostics were added after the latest mDNS log report. | Added wiki/529 `RUNTIME_CPU_SCENARIO_MATRIX_AND_MDNS_LOG_AUDIT_2026_06_01.md` and `scripts\windows\measure-musu-runtime-cpu-scenarios.ps1`. The matrix writes schema `musu.runtime_cpu_scenario_matrix.v1`, measures `runtime-started`, `dashboard-open`, `desktop-open`, and `post-route`, and delegates per-process CPU attribution to `measure-musu-idle-cpu.ps1` with Node/WebView2 budgets. `measure-musu-idle-cpu.ps1` now accepts `runtime-started`, `dashboard-open`, and `startup-open` scenario labels. The wrapper uses timeout-bounded `Start-Process` temp-file command capture for `musu up --json` to avoid the known bridge stdout pipe-handle hang. Parser checks passed for both scripts; a 3s local `runtime-started` smoke wrote `.local-build\runtime-cpu-scenarios\20260601-100515-HUGH_SECOND\20260601-100515-HUGH_SECOND.runtime-cpu-scenario-matrix.json` with `ok=true`, one MUSU process, owned Node `0`, owned WebView2 `0`, max one-core CPU `0`, and working set `27.72MB`. This is diagnostic-only and does not close the two-machine 60s `desktop-open` release CPU gate. |
| 2026-06-01 v132 | Code and docs were re-indexed after the runtime CPU scenario matrix update. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1102 files and 2147 symbols after wiki/529, the scenario matrix script, the idle sampler scenario labels, and the CoS memory note were added. Search terms include `musu.runtime_cpu_scenario_matrix.v1`, `measure-musu-runtime-cpu-scenarios.ps1`, `runtime-started`, `dashboard-open`, `post-route`, `20260601-100515-HUGH_SECOND.runtime-cpu-scenario-matrix.json`, and `bridge stdout pipe-handle hang`. |
| 2026-06-01 v133 | Current single-machine and primary CPU evidence were refreshed after the runtime CPU scenario matrix commit. | Current single-machine smoke evidence `docs\evidence\single-machine\1.15.0-rc.1\20260601-101859-HUGH_SECOND.evidence.json` passed from commit `9d39ab2f7a02aca75beaaeb5d35198d850bbad98` with dashboard output `MUSU_RELEASE_SMOKE_OK_20260601_101833`, CLI output `MUSU_CLI_ROUTE_OK_20260601_101833`, dashboard task `fae480fc-b2e4-4c4f-b5f9-7c452c6e0ec5`, bridge `http://127.0.0.1:1126`, evidence SHA-256 `eb1074d2f7a77b1b3dca94a2ffa437b38a17335288b5951ed52f3a866644a487`, and verification SHA-256 `33d920ccc0d208dba0d33d0b21e9b1e419eca2babbc3af6a4c433f694d0ea4cb`. Current primary packaged `desktop-open` CPU evidence `docs\evidence\runtime-idle-cpu\1.15.0-rc.1\20260601-101541-HUGH_SECOND.desktop-open.evidence.json` passed from clean commit `9d39ab2f7a02aca75beaaeb5d35198d850bbad98` with 60.032s sample, one `musu-desktop`, six owned WebView2 helpers, owned Node `0`, max one-core CPU `musu=0`, `webview2=0.13`, working set `342.44MB`, private memory `184.04MB`, and evidence SHA-256 `84a3458aabb7f49f7a3853588f44ee09e496c0f9d307a2dab75d8a2e030d59c0`. Go/no-go after these files reports `single_machine_verified=true`, `runtime_idle_cpu_valid_machine_count=1/2` with `HUGH_SECOND`, and public release still No-Go. |
| 2026-06-01 v134 | Code and docs were re-indexed after current evidence refresh. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1107 files and 2147 symbols after current single-machine smoke evidence, current primary desktop-open CPU evidence, wiki/529, and the new CoS evidence note. Search terms include `20260601-101859-HUGH_SECOND.evidence.json`, `20260601-101541-HUGH_SECOND.desktop-open.evidence.json`, `fae480fc-b2e4-4c4f-b5f9-7c452c6e0ec5`, `MUSU_RELEASE_SMOKE_OK_20260601_101833`, `webview2=0.13`, `342.44MB`, and `runtime CPU 1/2`. |
| 2026-06-01 v135 | Second-PC return flow now carries the CPU scenario matrix without weakening the release CPU gate. | `run-second-pc-release-check.ps1` now captures `musu.runtime_cpu_scenario_matrix.v1` by default, zips matrix files into `.local-build\second-pc-return\*.zip`, and exposes `-SkipRuntimeCpuScenarioMatrix` plus `-FailOnRuntimeCpuScenarioMatrix`. `import-second-pc-return.ps1` imports the matrix to `.local-build\runtime-cpu-scenarios\` and selects release CPU evidence only from `.local-build\runtime-idle-cpu\` with `scenario=desktop-open` and `require_owned_webview2=true`, so diagnostic matrix evidence cannot shadow the release gate sample. The multi-device kit, final operator packet, operator action-pack verifier, and readiness audit now include `measure-musu-runtime-cpu-scenarios.ps1`. Validation passed: parser checks for touched scripts, generated kit `musu-multidevice-1.15.0-rc.1-20260601-103622.zip` includes the matrix script, and a synthetic return zip imported both `runtime_idle_cpu_evidence_path` and `runtime_cpu_scenario_matrix_path`. |
| 2026-06-01 v136 | Code and docs were re-indexed after second-PC CPU matrix return flow wiring. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1108 files and 2147 symbols after second-PC wrapper/importer/kit/final-packet/action-pack wiring for CPU scenario matrices. Search terms include `SkipRuntimeCpuScenarioMatrix`, `runtime_cpu_scenario_matrix_path`, `musu.runtime_cpu_scenario_matrix.v1`, `Resolve-LatestRuntimeIdleReleaseEvidence`, `musu-multidevice-1.15.0-rc.1-20260601-103622.zip`, and `second-PC CPU matrix return flow`. |
| 2026-06-01 v137 | Current single-machine and primary CPU evidence were refreshed after the second-PC CPU matrix return-flow commit. | Current single-machine smoke evidence `docs\evidence\single-machine\1.15.0-rc.1\20260601-105019-HUGH_SECOND.evidence.json` passed from commit `04d1ab13f1960d9f7adb5fb2d389ccd39c63923d` with dashboard output `MUSU_RELEASE_SMOKE_OK_20260601_104959`, CLI output `MUSU_CLI_ROUTE_OK_20260601_104959`, dashboard task `e2f4b35b-7b79-4621-abbc-658413665d0b`, bridge `http://127.0.0.1:4980`, evidence SHA-256 `0b14f1e429b81a3b765b8ad6695a4bd02ce3685094ff127eda94af07a8ec66b8`, and verification SHA-256 `3a1fbfe7a38949b088004dbf9a2f1ea6a0cacdbe8a69e95ebadd74002429ee08`. Current primary packaged `desktop-open` CPU evidence `docs\evidence\runtime-idle-cpu\1.15.0-rc.1\20260601-104726-HUGH_SECOND.desktop-open.evidence.json` passed from clean commit `04d1ab13f1960d9f7adb5fb2d389ccd39c63923d` with 60.022s sample, one `musu-desktop`, six owned WebView2 helpers, owned Node `0`, max one-core CPU `musu=0`, `webview2=0.18`, working set `341.1MB`, private memory `185.71MB`, and evidence SHA-256 `fa62a7b93b0475e8b59c89fbe8bdb9c907e6dfd1f94aa2c7ad34e4bde633346c`. |
| 2026-06-01 v138 | Code and docs were re-indexed after the current evidence refresh. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1113 files and 2147 symbols after current single-machine smoke evidence, current primary desktop-open CPU evidence, BETA checklist pointer updates, WIKI/WIKI_INDEX state refresh, and the new CoS evidence note. Search terms include `20260601-105019-HUGH_SECOND.evidence.json`, `20260601-104726-HUGH_SECOND.desktop-open.evidence.json`, `e2f4b35b-7b79-4621-abbc-658413665d0b`, `MUSU_RELEASE_SMOKE_OK_20260601_104959`, `webview2=0.18`, `341.1MB`, and `04d1ab13f1960d9f7adb5fb2d389ccd39c63923d`. |
| 2026-06-01 v139 | Runtime direct-route failures now request a fail-closed relay lease. | `musu-rs/src/bridge/handlers/forward.rs` now calls `request_relay_lease_after_direct_failure` after all direct route attempts fail and before closing the rendezvous session. The request is skipped without a rendezvous session or account token, records attempted route kinds and `direct_path_failed=true`, and does not replace the original direct-route failure. `musu relay status --json` now reports `relay_runtime_fallback_lease_request_wired=true` while `relay_transport_wired=false` and `relay_default_data_path=false`. This advances the `musu.pro` P2P control-plane objective without making relay the default payload path. Validation passed: targeted rendezvous relay-request test, relay lease serialization test, `cargo fmt --check`, `cargo build --bin musu -j 1`, and relay status JSON. |
| 2026-06-01 v140 | Code and docs were re-indexed after runtime relay fallback wiring. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1115 files and 2157 symbols after wiki/530, runtime relay fallback docs, `relay_runtime_fallback_lease_request_wired`, and the CoS memory note were added. Search terms include `request_relay_lease_after_direct_failure`, `relay_runtime_fallback_lease_request_wired`, `direct_path_failed=true`, `RUNTIME_RELAY_FALLBACK_NEXT_STEPS_2026_06_01.md`, and `2026-06-01_1120_kst_runtime_relay_fallback_lease_request.md`. |
| 2026-06-01 v141 | Current single-machine and primary CPU evidence were refreshed after the runtime relay fallback commit. | Current single-machine smoke evidence `docs\evidence\single-machine\1.15.0-rc.1\20260601-113438-HUGH_SECOND.evidence.json` passed from commit `52698c4406de4747b4e1ce1834cfbad1cb0c75c1` with dashboard output `MUSU_RELEASE_SMOKE_OK_20260601_113412`, CLI route checked, dashboard task `7527eda1-b7f6-4a0d-8447-b17e76e756fe`, bridge `http://127.0.0.1:1473`, evidence SHA-256 `4ddb66e16f4714f0ea40e4d6dc3c436e713bc6434be713433f9d26e67768f2e5`, and verification SHA-256 `3561358fc9fd767237a5a738a829ea384040e90de39d490407b65a50c68e83cf`. Current primary packaged `desktop-open` CPU evidence `docs\evidence\runtime-idle-cpu\1.15.0-rc.1\20260601-113022-HUGH_SECOND.desktop-open.evidence.json` passed from clean commit `52698c4406de4747b4e1ce1834cfbad1cb0c75c1` with 60.029s sample, one `musu-desktop`, six owned WebView2 helpers, owned Node `0`, max one-core CPU `musu=0`, `webview2=0.16`, working set `343.16MB`, private memory `182.96MB`, and evidence SHA-256 `15b663f4a0059a6d4620fc17c8bfaa5a7111e7eab806ff6a3173de72b3ed7f72`. |
| 2026-06-01 v142 | Code and docs were re-indexed after the post-runtime-relay evidence refresh. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1120 files and 2157 symbols after the current CPU/smoke evidence, wiki/522/wiki/524/wiki/528/wiki/530 updates, BETA checklist pointer refresh, and CoS evidence note. Search terms include `20260601-113438-HUGH_SECOND.evidence.json`, `20260601-113022-HUGH_SECOND.desktop-open.evidence.json`, `7527eda1-b7f6-4a0d-8447-b17e76e756fe`, `MUSU_RELEASE_SMOKE_OK_20260601_113412`, `webview2=0.16`, `343.16MB`, and `52698c4406de4747b4e1ce1834cfbad1cb0c75c1`. |
| 2026-06-01 v143 | Relay fallback evaluation is now persisted in route evidence. | Added `relay_fallback` to Rust local/cloud route evidence, bridge forwarding failure reports, and the Next `/api/v1/p2p/route-evidence` contract. After terminal direct-route failure, bridge forwarding still requests a fail-closed relay lease only when a rendezvous session/account token exists, but failed route evidence now records whether the fallback was skipped/requested/denied/issued/failed/timed out, attempted route kinds, requested capability, policy/blockers, optional lease id, and relay failure class. This remains audit/control-plane evidence only: `relay_transport_wired=false`, `relay_default_data_path=false`, and relay payload transport is still not wired. Validation passed: `cargo fmt --check`, `cargo test --lib route_evidence`, targeted relay lease fallback test, `cargo build --bin musu -j 1`, route-evidence API tests including relay fallback persistence, `npm run typecheck`, and `musu relay status --json`. Public release remains No-Go pending second-PC desktop-open CPU evidence, real multi-device route proof, `musu@musu.pro` inbox delivery evidence, Store evidence, QUIC/TLS proof, and relay/tunnel transport. |
| 2026-06-01 v144 | Code and docs were re-indexed after relay fallback route-evidence persistence. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1121 files and 2163 symbols after Rust/Next route-evidence contract changes, wiki/522/wiki/524/wiki/528/wiki/530 updates, BETA checklist update, WIKI/WIKI_INDEX refresh, and CoS memory note `2026-06-01_1203_kst_relay_fallback_route_evidence.md`. Search terms include `relay_fallback`, `lease_requested`, `lease_issued`, `skipped_no_session`, `skipped_no_token`, `relay_lease_denied`, `stores relay fallback evidence after failed direct route`, and `RouteRelayFallbackEvidence`. |
| 2026-06-01 v145 | Current single-machine and primary desktop-open CPU evidence were refreshed after relay fallback route-evidence persistence. | Single-machine smoke evidence `docs\evidence\single-machine\1.15.0-rc.1\20260601-121339-HUGH_SECOND.evidence.json` verifies against current code because the only later commits are docs/evidence refreshes. It passed with dashboard output `MUSU_RELEASE_SMOKE_OK_20260601_121339`, CLI route checked, dashboard task `34375312-8b16-4015-ad1a-7bd5f2ccb19c`, bridge `http://127.0.0.1:9157`, and evidence SHA-256 `f05c5a8babf474a3a13069b033c9093079c22c72f70026de8316c607ccf2714d`. Primary packaged `desktop-open` CPU evidence `docs\evidence\runtime-idle-cpu\1.15.0-rc.1\20260601-121701-HUGH_SECOND.desktop-open.evidence.json` passed from clean git state with 60.023s sample, two MUSU processes (`musu` runtime plus `musu-desktop`), six owned WebView2 helpers, owned Node `0`, max one-core CPU `musu=0`, `webview2=0.03`, total working set `378.16MB`, private memory `192.07MB`, and evidence SHA-256 `21280f882fc05ed9b7284a268847ab150585c4b0d24d90dce91a0a03568d0fa2`. Go/no-go after these commits reports `single_machine_verified=true`, `runtime_idle_cpu_valid_machine_count=1/2` with `HUGH_SECOND`, and public release still No-Go. |
| 2026-06-01 v146 | Code and docs were re-indexed after current evidence refresh. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1124 files and 2163 symbols after current single-machine evidence, current primary desktop-open CPU evidence, wiki/522/WIKI/WIKI_INDEX/BETA checklist updates, and CoS memory note `2026-06-01_1218_kst_post_relay_fallback_current_evidence.md`. Search terms include `20260601-121339-HUGH_SECOND.evidence.json`, `20260601-121701-HUGH_SECOND.desktop-open.evidence.json`, `MUSU_RELEASE_SMOKE_OK_20260601_121339`, `34375312-8b16-4015-ad1a-7bd5f2ccb19c`, `webview2=0.03`, `378.16MB`, and `runtime CPU 1/2`. |
| 2026-06-01 v147 | Multi-device release verifier now requires QUIC/TLS route evidence, not merely non-legacy encryption. | `scripts\windows\verify-multidevice-evidence.ps1` now requires `peer_identity_method`, `peer_public_key`, and `encryption=quic_tls_1_3` for passing route evidence. `verify-final-operator-gate-packet.ps1` now rejects packets whose bundled multi-device verifier lacks those checks, and `prepare-final-operator-gate-packet.ps1` describes HTTPS fingerprint-pinned bridge evidence as diagnostic-only. This closes the gap where HTTPS fingerprint-pinned bridge evidence could be treated as hardened enough by the multi-device script even though the control-plane route-evidence API correctly marks it non-release-grade. Synthetic verifier validation passed: a `quic_tls_1_3` evidence fixture exited `0`, while an otherwise identical `https_tls_fingerprint_pin` fixture exited `1` with `route encryption release-grade`. |
| 2026-06-01 v148 | Code and docs were re-indexed after multi-device verifier hardening. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1125 files and 2163 symbols after the QUIC/TLS verifier gate, final packet stale-verifier guard, BETA checklist/current status/WIKI/WIKI_INDEX updates, and CoS memory note `2026-06-01_1228_kst_multidevice_verifier_quic_tls_gate.md`. Search terms include `route encryption release-grade`, `quic_tls_1_3`, `peer_identity_method`, `peer_public_key`, `https_tls_fingerprint_pin`, and `multi-device verifier QUIC/TLS gate`. |
| 2026-06-01 v149 | Current single-machine and primary desktop-open CPU evidence were refreshed after the QUIC/TLS verifier gate. | Single-machine smoke evidence `docs\evidence\single-machine\1.15.0-rc.1\20260601-124055-HUGH_SECOND.evidence.json` passed on commit `10548e70a85c38d864bf28e2a2d347d80135e9c9` with dashboard output `MUSU_RELEASE_SMOKE_OK_20260601_124032`, CLI route checked, dashboard task `e1c03dce-0d13-4482-ab9d-7988b10a9d2b`, and bridge `http://127.0.0.1:6483`. Primary packaged `desktop-open` CPU evidence `docs\evidence\runtime-idle-cpu\1.15.0-rc.1\20260601-124454-HUGH_SECOND.desktop-open.evidence.json` passed from clean commit `4d9435c3d4d97f7d3429b5eaf2b7154612e5eedb` with `git_dirty=false`, 60.018s sample, two MUSU processes, six owned WebView2 helpers, owned Node `0`, max one-core CPU `musu=0`, `webview2=0.10`, total working set `387.75MB`, and private memory `183.16MB`. This restores current local evidence after the verifier script code change; public release remains No-Go pending second-PC CPU, real QUIC/TLS route proof, support inbox, and Store evidence. |
| 2026-06-01 v150 | Code and docs were re-indexed after the post-QUIC/TLS-verifier evidence refresh. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1130 files and 2163 symbols after current single-machine evidence, current primary desktop-open CPU evidence, BETA checklist/current status/WIKI/WIKI_INDEX updates, and CoS memory note `2026-06-01_1248_kst_post_quic_tls_verifier_current_evidence.md`. Search terms include `20260601-124055-HUGH_SECOND.evidence.json`, `20260601-124454-HUGH_SECOND.desktop-open.evidence.json`, `MUSU_RELEASE_SMOKE_OK_20260601_124032`, `e1c03dce-0d13-4482-ab9d-7988b10a9d2b`, `webview2=0.10`, `387.75MB`, and `runtime CPU 1/2`. |
| 2026-06-01 v151 | Route evidence now requires a local transport verifier, not only a QUIC/TLS-looking encryption string. | Added `transport_verified_by` to Rust local/cloud route evidence and the `musu.pro` route-evidence API. The API and `verify-multidevice-evidence.ps1` now require `transport_verified_by=musu_quic_tls_transport` for release-grade evidence. Bridge HTTPS fingerprint-pinned forwarding writes `musu_bridge_forward_fingerprint_pinned_client` through an explicit local `RouteTransportProof`, which proves the interim HTTPS pin but remains non-release-grade. `bridge::route_evidence` now treats registry/rendezvous peer metadata as advertised material only: even metadata that claims `peer_identity_verified=true`, `encryption=quic_tls_1_3`, and `transport_verified_by=musu_quic_tls_transport` is downgraded unless the runtime transport code passes local proof. Validation passed: Rust `route_evidence` and `forward` tests, route-evidence API tests, PowerShell parser checks, synthetic multi-device verifier pass/fail fixture, `cargo fmt --check`, and `npm run typecheck`. |
| 2026-06-01 v152 | Code and docs were re-indexed after route transport-proof hardening and the repeated Tailscale IPv6 mDNS log classification. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1131 files and 2165 symbols after `transport_verified_by` was added to route evidence, the Windows multi-device/final-packet verifiers were tightened, WIKI/WIKI_INDEX/current-status docs were updated, and the operator-supplied `ff02::fb%9` Tailscale `os error 10065` mDNS pattern was classified as stale-binary or explicit mDNS opt-in unless reproduced on a current build with all mDNS opt-in variables unset. Search terms include `transport_verified_by`, `RouteTransportProof`, `musu_quic_tls_transport`, `missing_release_grade_transport_proof`, `route transport proof`, `musu_bridge_forward_fingerprint_pinned_client`, `ff02::fb%9`, `os error 10065`, and `MUSU_MDNS_ENABLE_TAILSCALE`. |
| 2026-06-01 v153 | Current rebuilt binary was checked against the re-supplied Tailscale IPv6 mDNS log pattern. | `cargo build --manifest-path .\musu-rs\Cargo.toml --bin musu -j 1` passed, then `RUST_LOG=debug musu discover --timeout 2` on `HUGH_SECOND` matched no `Failed to send|ff02::fb|10065|closed channel`, logged `mDNS Tailscale interfaces disabled by default`, disabled 9 virtual/VPN interfaces, and sent outbound multicast only on physical `이더넷 2`. `RUNTIME_ROUTE_EVIDENCE_MDNS_AUDIT_2026_06_01.md` and CoS memory `2026-06-01_1312_kst_route_evidence_transport_verifier.md` were updated with that evidence, then `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1131 files and 2165 symbols again. |
| 2026-06-01 v154 | Current single-machine evidence was refreshed after local transport-proof hardening. | Single-machine smoke evidence `docs\evidence\single-machine\1.15.0-rc.1\20260601-134022-HUGH_SECOND.evidence.json` passed from commit `87e5e5c24a6a062d8708d494dea3093cc516d92e` with dashboard output `MUSU_RELEASE_SMOKE_OK_20260601_134000`, CLI route checked, dashboard task `1f487f6b-7448-4745-b628-bfb1753cea1f`, bridge `http://127.0.0.1:4750`, evidence SHA-256 `1fa3ccc6383dcc68c9f7430e34855e843066723b7817c2d1c2ea0a2e340305bc`, and verification SHA-256 `33eaf6e626a98d7e30f33b816c84956dbd8a447334af411d4f48db343e985237`. |
| 2026-06-01 v155 | Current primary desktop-open CPU evidence was refreshed after committing the new single-machine evidence. | Primary packaged `desktop-open` CPU evidence `docs\evidence\runtime-idle-cpu\1.15.0-rc.1\20260601-134219-HUGH_SECOND.desktop-open.evidence.json` passed from clean commit `c316b8ba357235a9d1ea1c1ff437d78194d5b82e` with `git_dirty=false`, 60.022s sample, two MUSU processes, six owned WebView2 helpers, owned Node `0`, max one-core CPU `musu=0`, `webview2=0.18`, total working set `372.88MB`, and private memory `189.97MB`. This restores current local evidence after the route-evidence transport-proof code change; public release remains No-Go pending second-PC CPU, real QUIC/TLS route proof, support inbox, and Store evidence. |
| 2026-06-01 v156 | Code and docs were re-indexed after the post-transport-proof evidence refresh. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1136 files and 2165 symbols after the new single-machine evidence, primary desktop-open CPU evidence, BETA checklist/current status/WIKI/WIKI_INDEX updates, and CoS memory note `2026-06-01_1343_kst_post_transport_proof_current_evidence.md`. Search terms include `20260601-134022-HUGH_SECOND.evidence.json`, `20260601-134219-HUGH_SECOND.desktop-open.evidence.json`, `MUSU_RELEASE_SMOKE_OK_20260601_134000`, `1f487f6b-7448-4745-b628-bfb1753cea1f`, `webview2=0.18`, `372.88MB`, and `runtime CPU 1/2`. |
| 2026-06-01 v157 | Runtime CPU scenario matrix became a separate go/no-go attribution gate. | Added `verify-runtime-cpu-scenario-matrix.ps1` and wired it into `write-release-go-no-go.ps1`, second-PC wrapper verification, readiness audit, multi-device kit, final operator packet, and action-pack verification. Public release now requires clean/current 60s `musu.runtime_cpu_scenario_matrix.v1` evidence on two machines for `runtime-started`, `dashboard-open`, `desktop-open`, and `post-route`, with a successful post-route probe. This does not replace the existing two-machine `desktop-open` runtime idle CPU gate; it closes the gap where a busy-loop could hide in another runtime state. |
| 2026-06-01 v158 | Code and docs were re-indexed after promoting the CPU scenario matrix gate. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1138 files and 2165 symbols after the matrix verifier, go/no-go gate, second-PC wrapper, final packet/action-pack checks, wiki/529 update, and CoS memory note `2026-06-01_1415_kst_runtime_cpu_scenario_matrix_gate.md`. Search terms include `verify-runtime-cpu-scenario-matrix.ps1`, `runtime_cpu_scenario_matrix_verified`, `RunRuntimeCpuRouteProbe`, `RequirePostRouteProbe`, `musu.runtime_cpu_scenario_matrix.v1`, `runtime-started`, `dashboard-open`, `desktop-open`, and `post-route`. |
| 2026-06-01 v159 | Current single-machine and primary desktop-open CPU evidence were refreshed after the matrix gate commit. | Single-machine smoke `docs\evidence\single-machine\1.15.0-rc.1\20260601-141622-HUGH_SECOND.evidence.json` passed with dashboard output `MUSU_RELEASE_SMOKE_OK_20260601_141601`, CLI route checked, dashboard task `ab8b05fd-3a66-47c4-9f88-d151e1056b29`, bridge `http://127.0.0.1:7328`, evidence SHA-256 `8c31a691b50d34dfde6cc974380f5f43642f7d85c5c93d46593bce019817c73e`, and verification SHA-256 `96da6ccdd908501b32fa3ce0b5e85188d43cfd963763de34d3385c59e5d46e83`. Primary packaged `desktop-open` CPU evidence `docs\evidence\runtime-idle-cpu\1.15.0-rc.1\20260601-141730-HUGH_SECOND.desktop-open.evidence.json` passed from clean commit `668362afe47b7e78985dd12e22a5fb9cbde3bb0b` with 60.027s sample, two MUSU processes, six owned WebView2 helpers, owned Node `0`, max one-core CPU `musu=0`, `webview2=0.16`, working set `374.19MB`, and private memory `183.35MB`. |
| 2026-06-01 v160 | Code and docs were re-indexed after the post-matrix-gate evidence refresh. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1143 files and 2165 symbols after current single-machine evidence, current primary desktop-open CPU evidence, WIKI/WIKI_INDEX/BETA updates, and CoS memory note `2026-06-01_1420_kst_post_matrix_gate_current_evidence.md`. Search terms include `20260601-141622-HUGH_SECOND.evidence.json`, `20260601-141730-HUGH_SECOND.desktop-open.evidence.json`, `MUSU_RELEASE_SMOKE_OK_20260601_141601`, `ab8b05fd-3a66-47c4-9f88-d151e1056b29`, `webview2=0.16`, `374.19MB`, and `runtime_cpu_scenario_matrix_verified`. |
| 2026-06-01 v161 | Dashboard-open CPU matrix state can no longer be a no-op. | `measure-musu-runtime-cpu-scenarios.ps1` now stores the dashboard URL discovered from `musu up --json` and launches it for `dashboard-open` when no `-DashboardUrl` argument is supplied; if `dashboard-open` is run out of order, it performs its own bounded `musu up --json` discovery first. `verify-runtime-cpu-scenario-matrix.ps1` rejects dashboard-open entries whose preparation did not launch a dashboard URL, and `post-route` now requires the exact per-run route token. This makes the 4-state CPU matrix closer to the operator-reported states: runtime started, dashboard opened, desktop opened, and post-route. |
| 2026-06-01 v162 | Dashboard-open matrix harness smoke was checked after tightening. | Dirty-tree diagnostic matrix `.local-build\runtime-cpu-scenarios\20260601-143309-HUGH_SECOND\20260601-143309-HUGH_SECOND.runtime-cpu-scenario-matrix.json` ran `dashboard-open` alone with 3.018s sample. It performed bounded `musu up --json` discovery, launched `http://127.0.0.1:3000/app`, recorded `dashboard_url_source=musu_up_dashboard_open`, sampled one MUSU process, owned Node `0`, owned WebView2 `0`, and max one-core CPU `0`. This confirms harness behavior only; it is not release evidence because git was dirty and sample duration was 3s. |
| 2026-06-01 v163 | Code and docs were re-indexed after dashboard-open matrix gate tightening. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1144 files and 2165 symbols after the no-op dashboard-open guard, out-of-order dashboard URL discovery, exact per-run post-route token check, wiki/529 update, and CoS memory note `2026-06-01_1430_kst_dashboard_open_matrix_gate.md`. Search terms include `musu_up_dashboard_open`, `dashboard_url_source`, `expected_token`, `dashboard opened`, `20260601-143309-HUGH_SECOND`, and `MUSU_CPU_SCENARIO_ROUTE_OK`. |
| 2026-06-01 v164 | Current single-machine and primary desktop-open CPU evidence were refreshed after dashboard-open matrix gate tightening. | Single-machine smoke `docs\evidence\single-machine\1.15.0-rc.1\20260601-144154-HUGH_SECOND.evidence.json` passed with dashboard output `MUSU_RELEASE_SMOKE_OK_20260601_144129`, CLI route checked, dashboard task `a55012e1-adab-48e6-950f-e6953cd1566b`, bridge `http://127.0.0.1:2502`, evidence SHA-256 `2dc3311862fc4ea4f76b92df0592d1db8ddd9b2641a81b7552b138ec9e846c1f`, and verification SHA-256 `06766b45a72efcb291ede7bef7e22901a19b9d01d06f0821a42ea608548b5977`. Primary packaged `desktop-open` CPU evidence `docs\evidence\runtime-idle-cpu\1.15.0-rc.1\20260601-144311-HUGH_SECOND.desktop-open.evidence.json` passed from clean commit `aeb6c10d366b9759773bea5491604f79a594d81a` with 60.029s sample, one `musu-desktop`, six owned WebView2 helpers, owned Node `0`, max one-core CPU `musu=0`, `webview2=0.18`, working set `341.41MB`, and private memory `183.8MB`. |
| 2026-06-01 v165 | Code and docs were re-indexed after the current evidence refresh. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1149 files and 2165 symbols after current single-machine evidence, current primary desktop-open CPU evidence, BETA/WIKI/WIKI_INDEX updates, and CoS memory note `2026-06-01_1445_kst_post_dashboard_open_gate_current_evidence.md`. Search terms include `20260601-144154-HUGH_SECOND.evidence.json`, `20260601-144311-HUGH_SECOND.desktop-open.evidence.json`, `MUSU_RELEASE_SMOKE_OK_20260601_144129`, `a55012e1-adab-48e6-950f-e6953cd1566b`, `webview2=0.18`, `341.41MB`, and `musu_up_dashboard_open`. |
| 2026-06-01 v166 | Manual CPU matrix scenario arguments were hardened after a command-binding audit. | A dirty-tree harness check showed that `powershell -File ... -Scenario runtime-started dashboard-open ...` binds only `runtime-started`; the extra scenario tokens are not recoverable as unbound script arguments. `measure-musu-runtime-cpu-scenarios.ps1` now normalizes comma-separated scenario values, and operator packet/runbook/status commands now use `-Scenario runtime-started,dashboard-open,desktop-open,post-route`. Dirty-tree comma smoke `20260601-145441-HUGH_SECOND` confirmed `requested_scenarios=[runtime-started,dashboard-open]`. |
| 2026-06-01 v167 | Code and docs were re-indexed after the CPU matrix scenario argument fix. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1150 files and 2165 symbols after comma-separated CPU matrix command hardening, operator packet/kit/status/runbook updates, wiki/529 update, and CoS memory note `2026-06-01_1455_kst_cpu_matrix_scenario_argument_fix.md`. Search terms include `runtime-started,dashboard-open,desktop-open,post-route`, `20260601-145441-HUGH_SECOND`, `requested_scenarios`, `comma-separated scenarios`, and `CPU matrix scenario argument fix`. |
| 2026-06-01 v168 | The public site scroll, logo, and favicon-accent branding were fixed for production deployment. | `musu-bee/src/app/globals.css` no longer locks `html, body` with `overflow:hidden`; public pages now scroll via `overflow-y:auto`. `musu-bee/src/app/layout.tsx`, `musu-bee/src/app/page.tsx`, and `musu-bee/src/components/PublicSiteShell.tsx` now use `/images/favicon-header.png` as the browser/site logo, and the sampled favicon teal `#24C8DB` is exposed as `--musu-color-brand-emerald` plus used as a restrained public-site accent. `npm run typecheck` and `npm run build` passed. Production-mode Playwright checks against `next start -p 3001` confirmed `/`, `/landing`, `/pricing`, and `/install` scroll on desktop and mobile, render the favicon logo, and load `#24C8DB`. Deployment target is the existing Vercel workflow: push to `main` triggers `Deploy musu-bee to Vercel` with production `musu.pro`. |
| 2026-06-01 v169 | Code and docs were re-indexed after the public site scroll/logo/accent fix. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1154 files and 2165 symbols after the public site scroll fix, favicon logo wiring, `#24C8DB` accent token, GOAL/WIKI updates, and CoS memory note `2026-06-01_1520_kst_public_site_scroll_logo_accent.md`. Search terms include `overflow-y:auto`, `favicon-header.png`, `--musu-color-brand-emerald`, `#24C8DB`, `PublicSiteShell`, and `Deploy musu-bee to Vercel`. |
| 2026-06-01 v170 | Runtime CPU attribution now counts repo-related Node.js by command line, and dashboard-open matrix proof no longer accepts non-reachable dashboard URL fallbacks. | `measure-musu-idle-cpu.ps1` reads `Win32_Process` command-line metadata for matched executable names, so the local Next `node.exe` on `3001` is counted as `repo_related_node`. `measure-musu-runtime-cpu-scenarios.ps1` now uses only `dashboard.reachable_url` from `musu up --json` unless an explicit `-DashboardUrl` is supplied. This fixes the operator-reported "many Node.js processes" measurement blind spot and removes the earlier weak dashboard-open matrix proof. Validation: 3s diagnostic measured Node `1`, and the full matrix verifier passed after the clean run. |
| 2026-06-01 v171 | Current primary 4-state runtime CPU scenario matrix evidence now passes with Node attributed. | Evidence `docs\evidence\runtime-cpu-scenarios\1.15.0-rc.1\20260601-154503-HUGH_SECOND.runtime-cpu-scenario-matrix.json` verifies with `ok=true`, `fail_count=0`, clean source commit `68d183e0d285b3578b75e5c243a64855e64683bd`, and required scenarios `runtime-started`, `dashboard-open`, `desktop-open`, and `post-route`. It launched `http://127.0.0.1:3001/app`, counted repo Node `1`, and stayed under budget: max one-core CPU `runtime-started webview2=0.05`, `dashboard-open webview2=0.05`, `desktop-open node=0.03/webview2=0.05`, `post-route webview2=0.16`. The gate remains `1/2` until a second PC returns matching clean/current matrix evidence. |
| 2026-06-01 v172 | Current single-machine smoke and primary `desktop-open` CPU evidence were refreshed after the CPU attribution fix. | Single-machine smoke `docs\evidence\single-machine\1.15.0-rc.1\20260601-155630-HUGH_SECOND.evidence.json` passed on source commit `f4c2e0fd6565a81a21d5537e146a0f098ff763bd` with dashboard output `MUSU_RELEASE_SMOKE_OK_20260601_155610`, CLI route checked, dashboard task `e6757818-7dc2-432d-b9fb-19143cded009`, and bridge `http://127.0.0.1:4747`. Primary `desktop-open` CPU evidence `docs\evidence\runtime-idle-cpu\1.15.0-rc.1\20260601-160102-HUGH_SECOND.desktop-open.evidence.json` passed from clean source commit `34827ff4ecfdfe655e543b2635474308bb1ca53c` with MUSU `2`, repo Node `1`, owned WebView2 `6`, max one-core CPU `musu=0`, `node=0`, `webview2=0.08`, working set `504.02MB`, and private memory `330.43MB`. Go/no-go reports single-machine verified, runtime idle CPU `1/2`, runtime CPU scenario matrix `1/2`, public metadata ok, MSIX install ok, MSIX desktop entrypoint ok, and public release still No-Go. |
| 2026-06-01 v173 | Code and docs were re-indexed after the CPU attribution/current evidence refresh. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1160 files and 2165 symbols after Node command-line attribution, reachable-dashboard matrix tightening, current primary 4-state CPU matrix evidence, current single-machine smoke, current primary `desktop-open` CPU evidence, wiki/522/wiki/529/BETA/WIKI/WIKI_INDEX/qual-audit updates, and CoS memory note `2026-06-01_1605_kst_cpu_matrix_node_attribution_current_evidence.md`. Search terms include `repo_related_node`, `reachable_url`, `20260601-154503-HUGH_SECOND.runtime-cpu-scenario-matrix.json`, `20260601-155630-HUGH_SECOND.evidence.json`, `20260601-160102-HUGH_SECOND.desktop-open.evidence.json`, `MUSU_CPU_SCENARIO_ROUTE_OK_20260601_154503`, `webview2=0.08`, and `runtime CPU scenario matrix 1/2`. |
| 2026-06-01 v174 | Relay lease audit CLI was added and live `musu.pro` production auth was audited. | `musu relay leases --json` now queries `GET /api/v1/p2p/relay/lease` with optional `--session-id`, `--source-node-id`, `--target-node-id`, and bounded `--limit`, emits schema `musu.relay_leases.v1`, includes `owner_scope_verified`, and keeps `relay_transport_wired=false` plus `relay_default_data_path=false`. `musu relay status --json` now reports the cloud base URL through the shared cloud config helper. Live `https://musu.pro` diagnostics show `logged_in=true` and relay control-plane wiring, but `musu relay leases --json` fails with `p2p_control_auth_not_configured`; production P2P control auth must validate the runtime account/device token or a scoped control token before relay lease evidence is release-grade. The public site deploy question is also closed: Vercel production workflow run `26738950440` deployed `04929fd`, and live Playwright checks passed for scroll, favicon logo/browser icon, and `#24C8DB` accent on `/`, `/landing`, `/pricing`, and `/install` across desktop/mobile. |
| 2026-06-01 v175 | Code and docs were re-indexed after the relay lease audit CLI and live auth blocker docs. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1161 files and 2170 symbols after `musu.relay_leases.v1`, `owner_scope_verified`, `query_relay_leases`, live `p2p_control_auth_not_configured` documentation, public `musu.pro` deployment QA documentation, wiki/524/wiki/530/BETA/current-status/qual-audit updates, and CoS memory note `2026-06-01_1636_kst_relay_lease_audit_cli_live_auth_blocker.md`. Search terms include `musu relay leases --json`, `owner_scope_verified`, `p2p_control_auth_not_configured`, `26738950440`, `04929fd`, `relay_default_data_path=false`, and `relay_transport_wired=false`. |
| 2026-06-01 v176 | P2P control auth now supports SHA-256 runtime-token allowlisting. | `musu-bee/src/lib/p2pControlAuth.ts` accepts `MUSU_P2P_CONTROL_TOKEN_SHA256S` / `MUSU_P2P_CONTROL_TOKEN_SHA256` in addition to raw static control tokens, using timing-safe comparison and continuing to derive owner scope from the presented Bearer token hash. `scripts\windows\show-p2p-control-token-hash.ps1` computes the Vercel env value from `~\.musu\token` without printing the raw account token. Validation passed: 25 targeted P2P API/auth tests covering rendezvous, route evidence, relay lease, and hash allowlist; `npm run typecheck`; and the token-hash helper with a fake token. Remaining open work: deploy this `musu-bee/**` change, configure production `MUSU_P2P_CONTROL_TOKEN_SHA256S`, and re-run live `musu relay leases --json` until it returns an owner-scoped response instead of `p2p_control_auth_not_configured`. |
| 2026-06-01 v177 | Code and docs were re-indexed after P2P auth hash-allowlist wiring. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1164 files and 2177 symbols after `p2pControlAuth.test.ts`, `MUSU_P2P_CONTROL_TOKEN_SHA256S`, `show-p2p-control-token-hash.ps1`, wiki/522/wiki/524/wiki/530/BETA/CONFIG/WIKI/WIKI_INDEX updates, and CoS memory note `2026-06-01_1710_kst_p2p_control_auth_hash_allowlist.md`. Search terms include `sha256_bearer_token_allowlist`, `accepted_auth_modes`, `MUSU_P2P_CONTROL_TOKEN_SHA256`, `MUSU_P2P_CONTROL_TOKEN_SHA256S`, `raw_token_printed=false`, and `show-p2p-control-token-hash.ps1`. |
| 2026-06-01 v178 | P2P auth hash-allowlist code deployed to `musu.pro`; production env remains missing. | GitHub Actions for commit `b1c4378` passed: `Tests` run `26742743243`, `E2E Tests — musu-bee` run `26742743299`, and Vercel production deploy run `26742743319`. Live `musu relay leases --json` now returns `p2p_control_auth_not_configured` with `accepted_auth_modes=[]`, proving the new auth code is deployed but no raw token or SHA-256 allowlist is configured in production. Next required action is to set `MUSU_P2P_CONTROL_TOKEN_SHA256S` in the Vercel production environment using the value from `scripts\windows\show-p2p-control-token-hash.ps1 -Json`, then re-run `musu relay leases --json`. |
| 2026-06-01 v179 | Code and docs were re-indexed after recording the live P2P auth deploy/env check. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1165 files and 2177 symbols after GOAL/WIKI/WIKI_INDEX/wiki/522/wiki/530 updates and CoS memory note `2026-06-01_1718_kst_p2p_auth_deployed_env_missing.md`. Search terms include `b1c4378`, `26742743319`, `accepted_auth_modes=[]`, `p2p_control_auth_not_configured`, and `production env missing`. |
| 2026-06-01 v180 | Public site scroll/logo/accent was hardened with a focused regression test. | The public homepage and `PublicSiteShell` now share `musu-public-scroll-root`, which keeps public pages document-scrollable across desktop/mobile. Header/footer branding now uses `MusuLogo`, whose header size is tuned to the favicon-quality mark at `/images/favicon-header.png`. The homepage adds explicit `data-brand-accent=emerald` points and uses the favicon teal `#24C8DB` for the waitlist CTA and device dots. Added `playwright.public-site.config.ts` plus `e2e/public-site-scroll-brand.spec.ts` to assert `/` scrolls on desktop/mobile, has no horizontal overflow, renders the favicon mark, and exposes `--musu-color-brand-emerald=#24C8DB`. Validation passed: `npm run typecheck`, `npx playwright test --config=playwright.public-site.config.ts`, and `npm run build`. Deployment target remains the existing GitHub/Vercel production workflow on push to `main`; live `musu.pro` must be rechecked after this commit deploys. |
| 2026-06-01 v181 | Code and docs were re-indexed after the public site follow-up hardening. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1168 files and 2177 symbols after `MusuLogo` homepage/shell wiring, `musu-public-scroll-root`, missing brand RGB tokens, `public-site-scroll-brand.spec.ts`, `playwright.public-site.config.ts`, BETA/WIKI/WIKI_INDEX updates, and CoS memory note `2026-06-01_1722_kst_public_site_scroll_logo_followup.md`. Search terms include `public-site-scroll-brand`, `musu-public-scroll-root`, `MusuLogo`, `favicon-header.png`, `#24C8DB`, and `musu-color-brand-emerald`. |
| 2026-06-01 v182 | Public site follow-up commit deployed and live `musu.pro` QA passed. | Commit `674f501` (`Harden public site scroll and logo`) was pushed to `main`. GitHub Actions passed for `Tests` run `26743680160`, `E2E Tests — musu-bee` run `26743680172`, and `Deploy musu-bee to Vercel` run `26743680165`. Live Playwright checks against `https://musu.pro` with cachebuster `qa=674f501` passed on `/`, `/landing`, `/pricing`, and `/install` across desktop `1280x720` and mobile `390x844`: each route had `canScroll=true`, no horizontal overflow, `bodyOverflowY=auto`, `htmlOverflowY=auto`, `.musu-public-scroll-root=true`, favicon logo source containing `favicon-header.png`, and `--musu-color-brand-emerald=#24C8DB`. |
| 2026-06-01 v183 | Code and docs were re-indexed after recording live public-site deployment QA. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1168 files and 2177 symbols after GOAL/WIKI/WIKI_INDEX/BETA and CoS memory were updated with commit `674f501`, run IDs `26743680160`, `26743680172`, `26743680165`, and live `musu.pro` QA results. Search terms include `qa=674f501`, `canScroll=true`, `bodyOverflowY=auto`, `htmlOverflowY=auto`, `.musu-public-scroll-root=true`, and `favicon-header.png`. |
| 2026-06-01 v184 | Frontend polling hot-loop candidates were moved to the shared cancellable low-duty poller. | Removed direct `setInterval(` usage from `musu-bee/src` by migrating workflow run status, remote screen device refresh, agents surface refresh, and onboarding research polling to `useLowDutyPolling`. This adds AbortController cancellation on unmount/route change, hidden-tab throttling, in-flight request suppression, failure backoff, and terminal workflow stop behavior. Added `musu-bee/src/app/runtime-polling-contract.test.ts` so those surfaces cannot silently regress to direct intervals. Validation passed: `rg -n "setInterval\\(" musu-bee\\src -S` returned no matches, `npx tsx --test src/app/runtime-polling-contract.test.ts` passed 4/4, `npm run typecheck` passed, and `npm run build` passed. This is a busy-loop root-cause reduction; release CPU gates still require fresh 60s evidence on primary and second PC. |
| 2026-06-01 v185 | Code and docs were re-indexed after frontend polling hot-loop hardening. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1170 files and 2176 symbols after `runtime-polling-contract.test.ts`, `RunPanel`, `app/screen`, `useAgentsSurface`, `useOnboardingFlow`, BETA/WIKI/WIKI_INDEX/runtime CPU audit/runtime hardening roadmap updates, and CoS memory note `2026-06-01_1738_kst_frontend_polling_hot_loop_hardening.md`. Search terms include `no setInterval`, `useLowDutyPolling`, `frontend polling hot-loop`, `AbortSignal`, `runtime-polling-contract.test.ts`, and `setInterval\\(`. |
| 2026-06-01 v186 | Tauri desktop `Start Runtime` no longer waits on inherited stdout pipes indefinitely. | `musu-bee/src-tauri/src/lib.rs` now runs `musu up --json` through a timeout-bounded helper with temp-file stdout/stderr capture, `stdin=null`, a 45s deadline, and 200ms wait sleeps. This mirrors the safer Windows harness pattern and prevents the desktop shell from staying busy forever if a long-lived bridge child inherits output handles. Added a unit test proving command output capture without `.output()` pipes. Diagnostic process audit on `HUGH_SECOND` saw 16 machine-wide Node.js processes, but all were non-MUSU Codex/MCP/npx helpers; MUSU-owned Node `0`, repo-related orphan helpers `0`. The audit was not release evidence because no MUSU runtime was running and the bridge registry pointed at a dead PID. Validation passed: `cargo test --manifest-path .\musu-bee\src-tauri\Cargo.toml -j 1` passed 3/3 Tauri shell tests, and `rg -n "\.output\(\)|wait_with_output" musu-bee\src-tauri\src\lib.rs -S` found no direct output-pipe wait in the desktop shell. |
| 2026-06-01 v187 | Code and docs were re-indexed after Tauri desktop start-runtime hardening. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1171 files and 2184 symbols after `START_RUNTIME_TIMEOUT`, `run_command_with_timeout`, `timed_command_captures_stdout_without_output_pipes`, BETA/WIKI/WIKI_INDEX/runtime CPU audit/runtime hardening roadmap updates, and CoS memory note `2026-06-01_1751_kst_tauri_start_runtime_timeout_hardening.md`. Search terms include `musu up timed out`, `desktop Start Runtime busy`, `no .output()`, `machine-wide Node.js`, `MUSU-owned Node 0`, and `dead bridge PID`. |
| 2026-06-01 v188 | Tauri desktop status now cleans stale bridge registry entries before probing health. | `musu-bee/src-tauri/src/lib.rs` reads `~/.musu/services/bridge.json`, checks the recorded PID on Windows through `OpenProcess(PROCESS_QUERY_LIMITED_INFORMATION)`, removes the registry file when the PID is dead, and returns `bridge_url=null` with a stale-registry detail instead of probing a dead port. This turns the 17:53 KST audit failure class into desktop failure handling: a stale `bridge.json` no longer makes the shell look at an obsolete bridge endpoint before `Start Runtime`. Added tests for stale cleanup and live registry URL preservation. Validation passed: `cargo test --manifest-path .\musu-bee\src-tauri\Cargo.toml -j 1` passed 5/5 Tauri shell tests. |
| 2026-06-01 v189 | Code and docs were re-indexed after stale bridge registry cleanup. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1172 files and 2191 symbols after `bridge_registry_status`, `PROCESS_QUERY_LIMITED_INFORMATION`, stale registry cleanup tests, Cargo dependency updates, BETA/WIKI/WIKI_INDEX/runtime CPU audit/runtime hardening roadmap updates, and CoS memory note `2026-06-01_1807_kst_tauri_stale_bridge_registry_cleanup.md`. Search terms include `stale_bridge_registry_is_removed_before_status_probe`, `live_bridge_registry_returns_loopback_url`, `bridge_url=null`, `dead bridge PID`, and `stale bridge registry removed`. |
| 2026-06-01 v190 | `musu doctor --json` now reports the desktop background feature profile. | Added a `background` section to doctor output covering mDNS, IPv6 mDNS, Tailscale mDNS, VPN/virtual mDNS interfaces, clipboard polling, cloud registration, effective cloud heartbeat interval/floor, file watcher roots/writable state, and autonomous planner status. Doctor marks the background profile `ok` when optional hot-loop-prone features are off, and `warn` when opt-ins such as mDNS, clipboard sync, file sync, or planner are enabled so idle CPU evidence can explicitly include them. Validation passed: targeted doctor background unit tests 3/3, `cargo build --manifest-path .\musu-rs\Cargo.toml --bin musu -j 1`, `git diff --check`, and live `musu doctor --json` on `HUGH_SECOND` reported `background.status=ok`, mDNS/clipboard/file sync/planner off, `cloud_heartbeat_interval_sec=300`, and `cloud_heartbeat_floor_sec=60`. |
| 2026-06-01 v191 | Code and docs were re-indexed after doctor background profile wiring. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1173 files and 2200 symbols after `DoctorBackground`, `check_background_features`, doctor background tests, BETA/WIKI/WIKI_INDEX/runtime CPU audit/runtime hardening roadmap updates, and CoS memory note `2026-06-01_1835_kst_doctor_background_profile.md`. Search terms include `background.status=ok`, `cloud_heartbeat_interval_sec`, `MUSU_ENABLE_CLIPBOARD_SYNC`, `MUSU_MDNS_ENABLE_TAILSCALE`, `file_serve_root_count`, and `planner`. |
| 2026-06-01 v192 | Optional autonomous planner loop now has enforced idle-budget bounds. | `musu-rs/src/brain/planner.rs` floors `MUSU_PLANNER_INTERVAL_SEC` at 60s, clamps `MUSU_PLANNER_COMMAND_TIMEOUT_SEC` to 5s..120s, and runs the crawler through timeout-bounded `tokio::process::Command` with `stdin=null`, piped output, and `kill_on_drop(true)`. `musu doctor --json` now reports `planner_interval_sec`, planner interval floor, planner command timeout, floor, and ceiling. Live bad-env verification with `MUSU_ENABLE_PLANNER=1`, `MUSU_PLANNER_INTERVAL_SEC=0`, and `MUSU_PLANNER_COMMAND_TIMEOUT_SEC=9999` returned `planner_interval_sec=60`, `planner_command_timeout_sec=120`, and `background.status=warn`. Validation passed: planner tests 2/2, doctor background tests 4/4, `cargo build --manifest-path .\musu-rs\Cargo.toml --bin musu -j 1`, and `git diff --check`. |
| 2026-06-01 v193 | Code and docs were re-indexed after planner loop budget hardening. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1174 files and 2207 symbols after `PLANNER_MIN_INTERVAL_SEC`, planner timeout clamping, doctor planner budget fields, BETA/WIKI/WIKI_INDEX/runtime CPU audit/runtime hardening roadmap/current status updates, and CoS memory note `2026-06-01_1856_kst_planner_loop_budget_hardening.md`. Search terms include `MUSU_PLANNER_COMMAND_TIMEOUT_SEC`, `planner_command_timeout_sec`, `planner_interval_sec`, `planner crawler timed out`, `planner_command_timeout_is_bounded`, and `doctor_background_floors_planner_loop_budget`. |
| 2026-06-01 v194 | Live `musu.pro` scroll/logo/accent deployment was rechecked and cloud heartbeat hardware probes were bounded. | `https://musu.pro`, `/privacy`, and `/support` returned HTTP 200. Live browser QA on desktop `1280x720` and mobile `390x844` confirmed homepage scroll movement, no horizontal overflow, favicon-header logo source, `data-brand-accent=emerald`, and `--musu-color-brand-emerald=#24C8DB`, so no manual web deploy remains for that fix. `musu-rs/src/peer/hardware.rs` now routes Windows PowerShell/WMIC, macOS `sysctl`, and `nvidia-smi` probes through a 5s `command_stdout_with_timeout()` helper with `stdin=null`, stderr discarded, and fallback metadata on failure/timeout. Validation passed: hardware tests 2/2, `cargo build --manifest-path .\musu-rs\Cargo.toml --bin musu -j 1`, `cargo fmt --check`, and `git diff --check`. |
| 2026-06-01 v195 | Code and docs were re-indexed after hardware probe timeout hardening. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1175 files and 2212 symbols after live `musu.pro` deploy recheck, `HARDWARE_PROBE_TIMEOUT`, `command_stdout_with_timeout`, hardware probe timeout tests, BETA/WIKI/WIKI_INDEX/runtime CPU audit/runtime hardening roadmap/current status updates, and CoS memory note `2026-06-01_1919_kst_hardware_probe_timeout_hardening.md`. Search terms include `hardware-probe-ok`, `nvidia-smi`, `Win32_PhysicalMemory`, `machdep.cpu.brand_string`, `live-brand-scroll-desktop`, and `#24C8DB`. |
| 2026-06-01 v196 | Post-push go/no-go was refreshed for commit `4f099bf`. | `Tests` run `26749151136` passed for `4f099bf`. Local go/no-go summary on clean manifest commit `4f099bf701664c255c37f3a875fb094a7d9b9811` reports `ready_for_public_desktop_release=false`, public metadata ok, MSIX install ok, support false, Store false, multi-device false, and stale evidence reset: `single_machine_verified=false`, runtime idle CPU `0/2`, runtime CPU scenario matrix `0/2`. The next evidence step is not just second-PC capture; primary single-machine smoke, primary desktop-open CPU, and primary 4-state CPU matrix must also be refreshed after this new source commit before final handoff. |
| 2026-06-01 v197 | Code and docs were re-indexed after recording the post-push No-Go state. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1175 files and 2212 symbols after GOAL/WIKI/WIKI_INDEX/wiki/522/wiki/529/CoS memory updates recorded `4f099bf`, `26749151136`, `single_machine_verified=false`, runtime idle CPU `0/2`, and runtime CPU scenario matrix `0/2`. |
| 2026-06-01 v198 | Primary smoke and CPU evidence were refreshed after the hardware-probe commit. | Started `next start -p 3001`, repaired the stale bridge registry by running `musu up --json`, and refreshed primary evidence on `HUGH_SECOND`. Single-machine evidence passes at `docs\evidence\single-machine\1.15.0-rc.1\20260601-194130-HUGH_SECOND.evidence.json` with dashboard task `60cb73e5-ea3c-42c8-bcd6-41f09e618a16`, bridge `http://127.0.0.1:4752`, and CLI route checked. Clean `desktop-open` CPU evidence passes at `docs\evidence\runtime-idle-cpu\1.15.0-rc.1\20260601-194410-HUGH_SECOND.desktop-open.evidence.json` from commit `42cf892e41517f6c6d3a1066c6b0b4609a59f907`: 60.056s, `git_dirty=false`, `musu=2`, repo Node `1`, owned WebView2 `6`, max one-core CPU `musu=0`, `node=0.03`, `webview2=0.08`, working set `506.72MB`, no budget violations. Clean 4-state CPU matrix passes at `docs\evidence\runtime-cpu-scenarios\1.15.0-rc.1\20260601-194528-HUGH_SECOND.runtime-cpu-scenario-matrix.json`; all four required scenarios passed 60s samples and post-route token `MUSU_CPU_SCENARIO_ROUTE_OK_20260601_194528`. Go/no-go now reports single-machine true, runtime idle CPU `1/2` with `HUGH_SECOND`, runtime CPU matrix `1/2`, public metadata ok, MSIX install ok, support false, Store false, and public release still No-Go. |
| 2026-06-01 v199 | Code, docs, and refreshed evidence were re-indexed. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1181 files and 2212 symbols after primary smoke evidence, primary desktop-open CPU evidence, primary 4-state CPU matrix evidence, BETA/wiki/522/wiki/529/WIKI/WIKI_INDEX updates, and CoS memory note `2026-06-01_1950_kst_primary_smoke_cpu_evidence_refresh.md`. Search terms include `20260601-194130-HUGH_SECOND`, `20260601-194410-HUGH_SECOND.desktop-open`, `20260601-194528-HUGH_SECOND.runtime-cpu-scenario-matrix`, `60cb73e5-ea3c-42c8-bcd6-41f09e618a16`, and `MUSU_CPU_SCENARIO_ROUTE_OK_20260601_194528`. |
| 2026-06-01 v200 | Frontend polling hardening was extended to dashboard and node panel refresh loops. | `DashboardClient.tsx` and `NodePanel.tsx` moved from hand-written visibility/timer refresh loops to shared `useLowDutyPolling`. Dashboard agents/tasks/watchdog/runs refresh and node panel nodes/registry/discovery refresh now get AbortController cancellation, in-flight suppression, hidden-tab throttling, and failure backoff. `runtime-polling-contract.test.ts` now covers six surfaces. Validation passed: 6/6 polling contract tests, `npm run typecheck`, `npm run build`, `git diff --check`, and no direct `setInterval(` matches in `musu-bee/src`. Because this is a source change, previous CPU evidence is no longer release-current until primary and second-PC CPU samples are refreshed after the commit. |
| 2026-06-01 v201 | Code and docs were re-indexed after dashboard/node polling hardening. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1182 files and 2212 symbols after `DashboardClient.tsx`, `NodePanel.tsx`, polling contract tests, BETA/current-status/runtime CPU audit/runtime hardening roadmap/WIKI updates, and CoS memory note `2026-06-01_2010_kst_dashboard_node_polling_hardening.md`. Search terms include `dashboard main refresh`, `node panel registry/discovery refresh`, `useLowDutyPolling`, `runtime-polling-contract.test.ts`, `no setInterval`, and `frontend busy-loop risk`. |
| 2026-06-01 v202 | Post-polling-hardening go/no-go was refreshed for commit `a1082dec`. | Clean go/no-go on `a1082deca7cf33e16556b588181e56a531e519c3` reports `ready_for_public_desktop_release=false`, `manifest_dirty=false`, public metadata true, MSIX install true, MSIX desktop entrypoint true, process ownership true, startup single-instance true, but `single_machine_verified=false`, runtime idle CPU `0/2`, runtime CPU scenario matrix `0/2`, multi-device false, support false, and Store false. The drop from `1/2` to `0/2` is expected source freshness after the frontend polling source commit; next evidence step is to refresh primary smoke, primary `desktop-open` CPU, primary 4-state matrix, then import second-PC CPU/matrix/route evidence. |
| 2026-06-01 v203 | Code and docs were re-indexed after recording the post-polling No-Go state. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1182 files and 2213 symbols after GOAL v202, current-status, runtime CPU audit, WIKI/WIKI_INDEX, and the CoS memory note were updated with commit `a1082dec`, `manifest_dirty=false`, `single_machine_verified=false`, runtime idle CPU `0/2`, and runtime CPU scenario matrix `0/2`. |
| 2026-06-01 v204 | Public-site common-shell accent coverage and current primary evidence were refreshed after the polling hardening source change. | `PublicSiteShell.tsx` now marks the shared emerald `Open App` CTA with `data-brand-accent="emerald"`, and `public-site-scroll-brand.spec.ts` now checks `/`, `/landing`, `/pricing`, and `/install` on desktop/mobile; local Playwright passed 8/8 and `npm run typecheck` passed. Current single-machine smoke passes at `docs\evidence\single-machine\1.15.0-rc.1\20260601-203715-HUGH_SECOND.evidence.json` with dashboard task `4345c783-ddc2-4f7c-9328-6a721e7b3f1c`. Current primary desktop-open CPU passes at `docs\evidence\runtime-idle-cpu\1.15.0-rc.1\20260601-203537-HUGH_SECOND.desktop-open.evidence.json` with `musu=0`, `node=0.03`, `webview2=0.05` max one-core CPU and no hot processes. Current primary 4-state matrix passes at `docs\evidence\runtime-cpu-scenarios\1.15.0-rc.1\20260601-203835-HUGH_SECOND.runtime-cpu-scenario-matrix.json` with route token `MUSU_CPU_SCENARIO_ROUTE_OK_20260601_203835`; `desktop-open` peaks at `node=0.18`, `webview2=0.18`. The first desktop-open sample also showed stale packaged desktop shells can accumulate under repeated manual launches, so desktop window reactivation/single-instance hardening remains open. |
| 2026-06-01 v205 | Code, docs, and refreshed primary evidence were re-indexed. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1191 files and 2213 symbols after the public-site common-shell accent follow-up, latest single-machine/CPU/matrix evidence, BETA/current-status/runtime CPU audit/runtime hardening roadmap/qual audit/WIKI/WIKI_INDEX updates, and CoS memory note `2026-06-01_2043_kst_public_site_primary_evidence_refresh.md`. Search terms include `20260601-203715-HUGH_SECOND`, `20260601-203537-HUGH_SECOND.desktop-open`, `20260601-203835-HUGH_SECOND.runtime-cpu-scenario-matrix`, `MUSU_CPU_SCENARIO_ROUTE_OK_20260601_203835`, `data-brand-accent="emerald"`, and `desktop shell accumulation`. |
| 2026-06-01 v206 | Clean go/no-go was rerun after the post-polling primary evidence documentation commit. | On commit `1ab5a824be1291ac56667bc331474a7d63864d29`, go/no-go reports `ready=false`, `single_machine=true`, runtime idle CPU `1/2` with `HUGH_SECOND`, runtime CPU scenario matrix `1/2` with `HUGH_SECOND`, public metadata true, MSIX install true, MSIX desktop entrypoint true, process ownership true, startup single-instance true, multi-device false, support false, Store false, and `manifest_dirty=false`. |
| 2026-06-01 v207 | `musu.pro` deploy workflow was hardened and live public-site QA passed. | Vercel production run `26753317276` for commit `96303af352c489429bbd50d4a54f09fce1bb65b1` was stuck and canceled. Commit `659503846c30fcfb1d1c8836c94b27d3464b7d96` added workflow job/step timeouts and switched deploy to `vercel deploy --prebuilt --yes`; replacement Vercel run `26753908889` passed and `Tests` run `26753908911` passed. Live Playwright QA against `https://musu.pro` with `qa=65950384` passed on `/`, `/landing`, `/pricing`, and `/install` for desktop/mobile: scroll works, no horizontal overflow, favicon-header logo renders, `.musu-public-scroll-root` exists, and the emerald accent is `#24C8DB` / `36,200,219`. |
| 2026-06-01 v208 | Final primary evidence was refreshed after deploy workflow hardening. | Single-machine smoke passes at `docs\evidence\single-machine\1.15.0-rc.1\20260601-211031-HUGH_SECOND.evidence.json` with dashboard task `6a339c65-4d1a-46f3-894a-5a50cc69cd0e`, bridge `http://127.0.0.1:4752`, and output `MUSU_RELEASE_SMOKE_OK_20260601_211010`. Primary `desktop-open` CPU evidence passes at `docs\evidence\runtime-idle-cpu\1.15.0-rc.1\20260601-211132-HUGH_SECOND.desktop-open.evidence.json` from clean commit `a0184e89851d7ac99e1162a301f9219104a4df04` with MUSU `2`, repo Node `1`, owned WebView2 `6`, max one-core CPU `musu=0`, `node=0`, `webview2=0.23`, working set `506.71MB`, and no hot processes. Primary 4-state matrix passes at `docs\evidence\runtime-cpu-scenarios\1.15.0-rc.1\20260601-211252-HUGH_SECOND.runtime-cpu-scenario-matrix.json` with route token `MUSU_CPU_SCENARIO_ROUTE_OK_20260601_211252`. Public release remains No-Go until second-PC CPU/matrix, release-grade route, production P2P env/live verification, `musu@musu.pro`, Store evidence, and relay/tunnel transport are complete. |
| 2026-06-01 v209 | Code and docs were re-indexed after deploy workflow/final evidence documentation. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1197 files and 2213 symbols after `musu.pro` deploy workflow hardening documentation, live `qa=65950384` browser QA, final primary single-machine smoke, final primary desktop-open CPU evidence, final primary 4-state matrix evidence, BETA/current-status/runtime CPU audit/runtime hardening roadmap/qual audit/WIKI/WIKI_INDEX updates, and CoS memory note `2026-06-01_2122_kst_deploy_workflow_final_evidence_refresh.md`. Search terms include `26753317276`, `26753908889`, `26753908911`, `20260601-211031-HUGH_SECOND`, `20260601-211132-HUGH_SECOND.desktop-open`, `20260601-211252-HUGH_SECOND.runtime-cpu-scenario-matrix`, `MUSU_CPU_SCENARIO_ROUTE_OK_20260601_211252`, and `qa=65950384`. |
| 2026-06-01 v210 | Hosted P2P control-plane live evidence became a go/no-go gate. | Added `scripts\windows\record-p2p-control-plane-evidence.ps1` and `scripts\windows\verify-p2p-control-plane-evidence.ps1`; `write-release-go-no-go.ps1` now reports `p2p_control_plane_verified` and adds a `p2p-control-plane` blocker when live owner-scoped relay lease queries fail. Final packet verification and desktop release readiness script coverage now include the new scripts. Current live evidence `docs\evidence\p2p-control-plane\1.15.0-rc.1\20260601-214149-musu.pro.evidence.json` records `musu relay status --json` and `musu relay leases --json`: relay status is logged in and wired, but relay lease query still reports `ok=false`, `owner_scope_verified=false`, `owner_scoped=false`, and `p2p_control_auth_not_configured` with no accepted auth modes. The next action is production `MUSU_P2P_CONTROL_TOKEN_SHA256S` configuration and re-recording passing evidence. |
| 2026-06-01 v211 | Code and docs were re-indexed after the P2P control-plane live gate. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1203 files and 2213 symbols after the P2P recorder/verifier scripts, failing live `musu.pro` evidence, go/no-go blocker wiring, BETA/current-status/runtime hardening/qual audit/WIKI/WIKI_INDEX updates, and CoS memory note `2026-06-01_2141_kst_p2p_control_plane_live_gate.md`. Search terms include `musu.p2p_control_plane_live_evidence.v1`, `p2p_control_plane_verified`, `p2p-control-plane`, `20260601-214149-musu.pro`, `owner_scope_verified=false`, and `MUSU_P2P_CONTROL_TOKEN_SHA256S`. |
| 2026-06-01 v212 | Post-P2P-gate clean go/no-go was recorded for `a6e41609`. | After commit `a6e41609d1c9ceaaf13ce73119f25e62471bfb5b`, clean go/no-go reports `ready=false`, `manifest_dirty=false`, `single_machine=false`, runtime idle CPU `0/2`, runtime CPU scenario matrix `0/2`, `p2p_control_plane_verified=false`, relay leases `ok=false`, and `owner_scope_verified=false`. Blockers are `single-machine`, `multi-device`, `runtime-idle-cpu`, `runtime-cpu-scenario-matrix`, `support-mailbox`, `store-release`, and `p2p-control-plane`. The evidence reset is expected because the P2P gate commit changed release scripts; primary smoke/CPU/matrix must be refreshed again on the final commit before second-PC evidence can close the runtime gates. |
| 2026-06-01 v213 | Code and docs were re-indexed after the post-P2P-gate No-Go documentation. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1203 files and 2213 symbols after GOAL v212, current-status/BETA/qual-audit/runtime-hardening/WIKI/WIKI_INDEX updates, and CoS memory updates recorded the clean `a6e41609` No-Go state. Search terms include `a6e41609`, `single_machine=false`, `runtime idle CPU 0/2`, `runtime CPU scenario matrix 0/2`, `p2p_control_plane_verified=false`, and `owner_scope_verified=false`. |
| 2026-06-01 v214 | Primary smoke, desktop idle CPU, and 4-state CPU matrix were refreshed after the P2P gate commits. | Single-machine evidence passes at `docs\evidence\single-machine\1.15.0-rc.1\20260601-221225-HUGH_SECOND.evidence.json` with dashboard task `927874c7-ce4d-4eb1-a84d-1bd7517ff844`, bridge `http://127.0.0.1:4752`, and CLI route checked. Production-dashboard `desktop-open` CPU evidence passes at `docs\evidence\runtime-idle-cpu\1.15.0-rc.1\20260601-221918-HUGH_SECOND.desktop-open.evidence.json` from clean commit `bcfdbf3222166aca05bf3196184311180375c5f9`: MUSU `2`, repo Node `1`, owned WebView2 `6`, max one-core CPU `musu=0`, `node=0`, `webview2=0.13`, working set `469.28MB`, and no hot processes. Primary 4-state matrix passes at `docs\evidence\runtime-cpu-scenarios\1.15.0-rc.1\20260601-222043-HUGH_SECOND.runtime-cpu-scenario-matrix.json` from clean commit `278372551f12f54816db9a0f47e677e2ac0125da`, with route token `MUSU_CPU_SCENARIO_ROUTE_OK_20260601_222043`. Clean go/no-go on `5b8650f084a0df9cf5cabde77af31dd11b366c0a` now reports `single_machine=true`, runtime idle CPU `1/2`, runtime CPU scenario matrix `1/2`, `manifest_dirty=false`, `p2p_control_plane_verified=false`, and `ready=false`. |
| 2026-06-01 v215 | Code, docs, and refreshed primary evidence were re-indexed. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1209 files and 2213 symbols after the latest single-machine, desktop-open CPU, runtime CPU matrix evidence, current-status/BETA/qual-audit/runtime-hardening/WIKI/WIKI_INDEX updates, and CoS memory note `2026-06-01_2226_kst_primary_evidence_refresh_after_p2p_gate.md`. Search terms include `20260601-221225-HUGH_SECOND`, `20260601-221918-HUGH_SECOND.desktop-open`, `20260601-222043-HUGH_SECOND.runtime-cpu-scenario-matrix`, `MUSU_CPU_SCENARIO_ROUTE_OK_20260601_222043`, `webview2=0.13`, and `runtime CPU scenario matrix 1/2`. |
| 2026-06-01 v216 | Public site scroll hardening and manual production redeploy support were added. | Live `https://musu.pro` was checked before this change and already allowed actual scroll movement on desktop/mobile with the favicon-header logo and `--musu-color-brand-emerald=#24C8DB`. The remaining deployment gap was operational: GitHub/Vercel deploys had no `workflow_dispatch`, and local Vercel CLI access could not see the `musu.pro` project/domain under the default scope. `.github/workflows/deploy-musu-bee.yml` now supports manual dispatch, and `.musu-public-scroll-root` now explicitly opts into viewport scrolling/touch panning via `height:auto`, `max-height:none`, `touch-action:pan-y`, `-webkit-overflow-scrolling:touch`, plus `html/body:has(.musu-public-scroll-root)` overflow rules. Validation passed: `npm run typecheck`, `npx playwright test --config=playwright.public-site.config.ts` (8/8), `npm run build`, and `git diff --check`. Public desktop release remains No-Go because this does not close second-PC runtime, release-grade route, production P2P auth, support inbox, Store, or relay/tunnel gates. |
| 2026-06-01 v217 | Code and docs were re-indexed after public-site manual deploy hardening. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1210 files and 2213 symbols after `workflow_dispatch`, `.musu-public-scroll-root` touch/viewport scroll hardening, current-status/WIKI/WIKI_INDEX updates, and CoS memory note `2026-06-01_2248_kst_public_site_manual_deploy_scroll_hardening.md`. Search terms include `workflow_dispatch`, `musu-public-scroll-root`, `touch-action:pan-y`, `-webkit-overflow-scrolling`, `favicon-header.png`, `#24C8DB`, and `manual production redeploy`. |
| 2026-06-01 v218 | Public site manual-deploy hardening was deployed to `musu.pro` and live QA passed. | Commit `b08ed746` was pushed to `main`; GitHub `Tests` run `26759256487`, `E2E Tests - musu-bee` run `26759256574`, and Vercel production deploy run `26759256616` all passed. Live Playwright QA against `https://musu.pro` with `qa=b08ed746` passed on `/`, `/landing`, `/pricing`, and `/install` across desktop `1280x720` and mobile `390x844`: actual scroll movement after `window.scrollTo`, no horizontal overflow, `.musu-public-scroll-root=true`, favicon-header logo source, `data-brand-accent=emerald`, and `--musu-color-brand-emerald=#24C8DB`. |
| 2026-06-01 v219 | Final go/no-go after the public-site deploy remains No-Go and resets primary runtime evidence freshness. | `write-release-go-no-go.ps1 -Json` on commit `bce29066` reports `ready_for_public_desktop_release=false`, `manifest_dirty=false`, `public_metadata_ok=true`, `msix_install_verified=true`, `msix_desktop_entrypoint_verified=true`, but `single_machine_verified=false`, runtime idle CPU `0/2`, runtime CPU scenario matrix `0/2`, `p2p_control_plane_verified=false`, support false, and Store false. The drop from the previous `single_machine=true` / runtime `1/2` is expected because commit `b08ed746` changed public-site CSS and deploy workflow source after the last primary evidence. Next release-gate work is to refresh primary smoke, primary desktop-open CPU, and primary 4-state matrix on the new source, then continue second-PC runtime/route evidence and live P2P auth. |
| 2026-06-01 v220 | Primary smoke, desktop-open CPU, and 4-state CPU matrix were refreshed after the final public-site/docs source change. | Single-machine evidence `docs\evidence\single-machine\1.15.0-rc.1\20260601-231612-HUGH_SECOND.evidence.json` passed with dashboard output `MUSU_RELEASE_SMOKE_OK_20260601_231552`, dashboard task `900260dc-d0d1-4ac6-9503-0001b4a2f451`, bridge `http://127.0.0.1:4752`, and CLI route checked. Desktop-open CPU evidence `docs\evidence\runtime-idle-cpu\1.15.0-rc.1\20260601-231939-HUGH_SECOND.desktop-open.evidence.json` passed from clean commit `afd1ceab2db2b234c6c4d9f50a5a165830bfae65` with MUSU `2`, repo Node `1`, owned WebView2 `6`, max one-core CPU `musu=0`, `node=0`, `webview2=0.1`, working set `510.13MB`, and no hot processes. Runtime CPU matrix `docs\evidence\runtime-cpu-scenarios\1.15.0-rc.1\20260601-233638-HUGH_SECOND.runtime-cpu-scenario-matrix.json` passed from clean commit `5434b4caf0c5e0f5a8dd345e775cb45259e4ebbd` with all four scenarios, dashboard URL launched, and route token `MUSU_CPU_SCENARIO_ROUTE_OK_20260601_233638`. |
| 2026-06-01 v221 | Code and docs were re-indexed after wiki/531 and current evidence documentation. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1217 files and 2213 symbols after current primary evidence, wiki/531 qualitative/code audit, current-status/runtime-hardening/runtime-CPU audit updates, WIKI/WIKI_INDEX/BETA updates, and CoS memory note `2026-06-01_2345_kst_current_evidence_code_audit_roadmap.md`. Search terms include `20260601-231612-HUGH_SECOND`, `20260601-231939-HUGH_SECOND.desktop-open`, `20260601-233638-HUGH_SECOND.runtime-cpu-scenario-matrix`, `MUSU_CPU_SCENARIO_ROUTE_OK_20260601_233638`, `wiki/531`, and `machine-wide Node.js`. |
| 2026-06-02 v222 | Packaged desktop repeated activation was reproduced and source hardening was added. | Current installed package still has the desktop shell duplicate-launch bug: repeated Start-menu activation of `Yellowhama.MUSU_ygcjq669as2b6!MUSU` expanded one `musu-desktop.exe` shell to three PIDs (`5744`, `27512`, `31496`). Source now uses `tauri-plugin-single-instance = 2.4.2` and focuses/unminimizes/shows the existing `main` window on repeat activation. `cargo test --manifest-path .\musu-bee\src-tauri\Cargo.toml -j 1` passed 5/5. Duplicate desktop shells from the repro were stopped. This source change makes previous packaged desktop evidence stale until a fresh MSIX is built/installed and repeated-activation/process-ownership/CPU evidence is rerun. |
| 2026-06-02 v223 | Public site source was updated for favicon-mark logo, explicit scroll rules, and emerald accent before `musu.pro` deploy. | `MusuLogo` now renders the favicon mark without the wordmark, `.musu-public-scroll-root` has explicit width/auto height/`100svh`/`100dvh`/`overflow-y:auto`/touch scrolling rules, and the homepage `Open App` CTA uses the emerald `#24C8DB` point color. Local validation passed `npm run typecheck`, `npx playwright test --config=playwright.public-site.config.ts` 8/8, `npm run build`, `cargo test --manifest-path .\musu-bee\src-tauri\Cargo.toml -j 1` 5/5, and `git diff --check`. The next operational step is push, Vercel production deploy to `musu.pro`, and live QA on `/`, `/landing`, `/pricing`, and `/install`. |
| 2026-06-02 v224 | Code and docs were re-indexed after desktop single-instance and public-site follow-up. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1218 files and 2213 symbols after Tauri single-instance source, favicon-mark logo/scroll/accent site source, current-status/runtime-hardening/BETA/WIKI/WIKI_INDEX updates, and CoS memory note `2026-06-02_0017_kst_desktop_single_instance_public_site_followup.md`. Search terms include `tauri-plugin-single-instance`, `desktop repeated activation`, `favicon mark only`, `scrollbar-gutter: stable`, `100svh`, `#24C8DB`, and `musu.pro deploy verified`. |
| 2026-06-02 v225 | Public-site follow-up deployed to `musu.pro` and production QA passed. | Commit `0ed3673a27b058ad1fc5d050434bf8435cb21e5d` passed Vercel production deploy run `26764307713`, GitHub `Tests` run `26764309477`, and `E2E Tests - musu-bee` run `26764310368`. Production Playwright QA against `https://musu.pro` passed 8/8 on `/`, `/landing`, `/pricing`, and `/install`, verifying desktop/mobile scroll, no horizontal overflow, favicon-mark logo, and `#24C8DB` accent. The desktop single-instance fix remains unproven for users until a fresh MSIX is built and installed. |
| 2026-06-02 v226 | Code and docs were re-indexed after live `musu.pro` deploy verification. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1218 files and 2213 symbols after Vercel/GitHub run IDs, production Playwright QA results, WIKI/WIKI_INDEX/current-status/runtime-hardening/BETA updates, and CoS memory update. Search terms include `0ed3673a`, `26764307713`, `26764309477`, `26764310368`, `production Playwright QA 8/8`, and `musu.pro deploy verified`. |
| 2026-06-02 v227 | Packaged desktop repeated activation became a release gate and current installed package failed it. | Added `scripts\windows\audit-musu-desktop-single-instance.ps1` with schema `musu.desktop_single_instance_audit.v1`, wired `desktop_single_instance_verified` into go/no-go, final handoff status, final operator packet generation/verification, and desktop release readiness script coverage. Current installed package `Yellowhama.MUSU_1.15.0.0_x64__ygcjq669as2b6` fails: three Start-menu/AppsFolder activations expanded one `musu-desktop.exe` to four (`new_desktop_shell=3`), evidence `.local-build\desktop-single-instance\musu-desktop-single-instance-20260602-005439-HUGH_SECOND.json`, `ok=false`; cleanup stopped the extras. Next step is fresh MSIX build/install from current source, then rerun desktop single-instance, process ownership, smoke, CPU, and matrix evidence. |
| 2026-06-02 v228 | Code and docs were re-indexed after the desktop single-instance gate. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1221 files and 2213 symbols after wiki/532, the desktop single-instance audit script, go/no-go/handoff/packet wiring, current-status/runtime-hardening/BETA/WIKI/WIKI_INDEX updates, and CoS memory note `2026-06-02_0055_kst_desktop_single_instance_gate.md`. Search terms include `musu.desktop_single_instance_audit.v1`, `desktop_single_instance_verified`, `desktop-single-instance`, `audit-musu-desktop-single-instance.ps1`, `shell:AppsFolder`, and `20260602-005439-HUGH_SECOND`. |
| 2026-06-02 v229 | Fresh release MSIX primary evidence was recorded after the desktop single-instance source fix. | Release MSIX build/install succeeded with `CARGO_BUILD_JOBS=1`; installed package `Yellowhama.MUSU_1.15.0.0_x64__ygcjq669as2b6` now passes packaged desktop repeated activation at `docs\evidence\desktop-single-instance\1.15.0-rc.1\20260602-014803-HUGH_SECOND.evidence.json` with three activations and one desktop shell. Current primary single-machine smoke `20260602-015347-HUGH_SECOND`, desktop-open CPU `20260602-015358-HUGH_SECOND.desktop-open`, four-state CPU matrix `20260602-015510-HUGH_SECOND`, and process ownership `20260602-020031-HUGH_SECOND` all pass on commit `2e97d135538f063252577c49762f8018bc366843`. This closes the primary installed desktop single-instance blocker, but public release remains No-Go because second-PC CPU/matrix, release-grade multi-device route, live `musu.pro` P2P control-plane auth, `musu@musu.pro`, and Store evidence are still missing. |
| 2026-06-02 v230 | Code and docs were re-indexed after fresh MSIX primary evidence documentation. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1230 files and 2213 symbols after wiki/533, fresh MSIX evidence files, current-status/runtime-hardening/BETA/WIKI/WIKI_INDEX updates, and CoS memory note `2026-06-02_0205_kst_fresh_msix_primary_evidence.md`. Search terms include `wiki/533`, `fresh MSIX primary evidence`, `20260602-014803-HUGH_SECOND`, `20260602-015347-HUGH_SECOND`, `20260602-015358-HUGH_SECOND.desktop-open`, `20260602-015510-HUGH_SECOND.runtime-cpu-scenario-matrix`, `20260602-020031-HUGH_SECOND`, and `MUSU_CPU_SCENARIO_ROUTE_OK_20260602_015510`. |
| 2026-06-02 v231 | Direct `musu up --json` PowerShell pipe hang was fixed in source and live `musu.pro` scroll/logo/accent was rechecked. | `spawn_bridge_process()` now clears Windows standard-handle inheritance before launching the bridge and uses `DETACHED_PROCESS | CREATE_NEW_PROCESS_GROUP | CREATE_NO_WINDOW`; bridge stdout/stderr remain redirected to `~/.musu/logs/bridge.log`. Validation passed `cargo check --manifest-path .\musu-rs\Cargo.toml --bin musu -j 1`, `cargo build --manifest-path .\musu-rs\Cargo.toml --bin musu -j 1`, `cargo fmt --manifest-path .\musu-rs\Cargo.toml --check`, `git diff --check`, and the fresh debug command `.\musu-rs\target\debug\musu.exe up --json --timeout-sec 10 | ConvertFrom-Json` returned `ok=true`, `bridge_started=true`, bridge PID `37284`, bridge status `ok`, URL `http://127.0.0.1:5692`, without hanging. PID `37284` was stopped after verification. Live `https://musu.pro` QA passed 8/8 on `/`, `/landing`, `/pricing`, and `/install` for desktop/mobile scroll, no horizontal overflow, favicon-header logo, `.musu-public-scroll-root`, and `#24C8DB` accent. Public release remains No-Go pending packaged CLI pipe proof, second-PC CPU/matrix, release-grade route, live P2P auth, `musu@musu.pro`, and Store evidence. |
| 2026-06-02 v232 | Code and docs were re-indexed after wiki/534 CLI pipe/site deploy audit. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1232 files and 2214 symbols after `clear_standard_handle_inheritance`, `DETACHED_PROCESS`, wiki/534, product/network boundary notes, current-status/runtime-hardening/BETA/WIKI/WIKI_INDEX updates, and CoS memory note `2026-06-02_0242_kst_cli_pipe_site_deploy_audit.md`. Search terms include `wiki/534`, `ConvertFrom-Json`, `stdout pipe`, `bridge_pid 37284`, `http://127.0.0.1:5692`, `musu.pro live QA 8/8`, and `packaged CLI pipe proof`. |
| 2026-06-02 v233 | Clean go/no-go was rerun after the CLI pipe hardening commit. | The clean post-commit run reports `ready_for_public_desktop_release=false`, `manifest_dirty=false`, `local_artifacts_ready=true`, `public_metadata_ok=true`, `msix_install_verified=true`, `msix_desktop_entrypoint_verified=true`, `desktop_single_instance_verified=true`, `startup_single_instance_verified=true`, `process_ownership_verified=true`, but `single_machine_verified=false`, runtime idle CPU `0/2`, runtime CPU scenario matrix `0/2`, `p2p_control_plane_verified=false`, `support_mailbox_verified=false`, and `store_release_verified=false`. The drop from the prior fresh MSIX primary evidence is expected because the Rust CLI source changed after that package/evidence. Next step is fresh MSIX build/install with this fix, packaged CLI pipe proof, then primary smoke/CPU/matrix evidence refresh. |
| 2026-06-02 v234 | Code and docs were re-indexed after recording the clean post-commit No-Go state. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1232 files and 2214 symbols after GOAL v233, wiki/534, WIKI/WIKI_INDEX/BETA/current-status/runtime-hardening updates, and CoS memory note `2026-06-02_0242_kst_cli_pipe_site_deploy_audit.md` were updated with `manifest_dirty=false`, `single_machine_verified=false`, runtime idle CPU `0/2`, and runtime CPU scenario matrix `0/2`. |
| 2026-06-02 v235 | Fresh MSIX packaged CLI/runtime evidence was recorded after the pipe fix. | Fresh local-sideload package `Yellowhama.MUSU_1.15.0.0_x64__ygcjq669as2b6` passes explicit WindowsApps CLI pipe evidence at `docs\evidence\cli-pipe\1.15.0-rc.1\20260602-032728-HUGH_SECOND.packaged-cli-pipe.evidence.json` with `returned_without_hang=true`, duration `7544ms`, and bridge status `ok`. Current primary evidence also passes: single-machine `20260602-033029-HUGH_SECOND`, desktop single-instance `20260602-033145-HUGH_SECOND`, startup single-instance `20260602-033225-HUGH_SECOND`, process ownership `20260602-033257-HUGH_SECOND`, desktop-open CPU `20260602-033412-HUGH_SECOND.desktop-open`, and matrix `20260602-033636-HUGH_SECOND.runtime-cpu-scenario-matrix`. CPU max one-core is `musu=0`, `node=0`, `webview2=0.23`; machine-wide Node is `19` but MUSU-owned Node is `0`. |
| 2026-06-02 v236 | Live `musu.pro` P2P control-plane was rechecked and remains env-blocked. | Evidence `docs\evidence\p2p-control-plane\1.15.0-rc.1\20260602-034756-musu.pro.evidence.json` records logged-in/wired relay status, but relay leases still fail with `p2p_control_auth_not_configured`, `accepted_auth_modes=[]`, `owner_scope_verified=false`, and `relay_default_data_path=false`. The public site scroll/logo/emerald deploy is already live; the next hosted action is production `MUSU_P2P_CONTROL_TOKEN_SHA256S` or equivalent scoped auth plus Vercel production reload/redeploy. Added wiki/535 and CoS memory `2026-06-02_0348_kst_packaged_cli_runtime_evidence.md`. |
| 2026-06-02 v237 | Code, docs, and evidence were re-indexed after wiki/535. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1247 files and 2214 symbols after packaged CLI pipe evidence, primary runtime evidence, live P2P control-plane recheck evidence, wiki/535, WIKI/WIKI_INDEX/BETA/current-status/runtime-hardening updates, and CoS memory `2026-06-02_0348_kst_packaged_cli_runtime_evidence.md`. Search terms include `packaged-cli-pipe`, `returned_without_hang=true`, `20260602-033412-HUGH_SECOND.desktop-open`, `MUSU_CPU_SCENARIO_ROUTE_OK_20260602_033636`, `p2p_control_auth_not_configured`, and `owned Node 0`. |
| 2026-06-02 v238 | Hosted P2P auth was deployed to `musu.pro`; blocker shifted to relay lease KV storage. | Commit `3be37e54a30bbd0bee95e9b2e22ce27d0450846c` synced `MUSU_P2P_CONTROL_TOKEN_SHA256S` through the deploy workflow; manual production run `26776054030` succeeded and `Tests` run `26775836294` passed. Fresh live evidence `docs\evidence\p2p-control-plane\1.15.0-rc.1\20260602-041225-musu.pro.evidence.json` now fails with `relay_lease_query_failed` / `p2p_relay_lease_kv_not_configured`, not `p2p_control_auth_not_configured`. The next hosted action is provisioning Vercel KV/Upstash and setting `KV_REST_API_URL` plus `KV_REST_API_TOKEN`; current GitHub repo secrets lack those names and repo variables are empty. |
| 2026-06-02 v239 | Code, docs, and evidence were re-indexed after the hosted P2P auth/KV blocker update. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1255 files and 2214 symbols after the workflow env-sync patch, live P2P evidence `20260602-040849-musu.pro` and `20260602-041225-musu.pro`, `P2P_CONTROL_PLANE_MUSU_PRO_NEXT_ACTIONS_2026_06_02.md`, config/docs updates, and CoS memory `2026-06-02_0418_kst_p2p_auth_deploy_kv_blocker.md`. Search terms include `p2p_relay_lease_kv_not_configured`, `relay_lease_query_failed`, `KV_REST_API_URL`, `KV_REST_API_TOKEN`, `26776054030`, and `1255 files`. |
| 2026-06-02 v240 | Final remote verification passed for the P2P env-sync workflow. | Commit `9a3ec52df102d36075f245bdab526dc57fb99e08` passed `Tests` run `26776909221` and `Deploy musu-bee to Vercel` run `26776909275`. The deploy synced `MUSU_P2P_CONTROL_TOKEN_SHA256S`, skipped missing `KV_REST_API_URL`, `KV_REST_API_TOKEN`, and relay env values by name, built production, and aliased `https://musu.pro`. This validates the workflow patch and confirms the remaining hosted blocker is KV/Upstash provisioning, not auth or website deployment. |
| 2026-06-02 v241 | Hosted P2P env status became a repeatable local preflight. | Added `scripts\windows\show-musu-pro-p2p-env-status.ps1` with schema `musu.p2p_control_plane_env_status.v1`. It checks GitHub secret/variable names and latest live P2P evidence without printing secret values. Current JSON reports `MUSU_P2P_CONTROL_TOKEN_SHA256S` present, `KV_REST_API_URL` and `KV_REST_API_TOKEN` missing, evidence `20260602-041225-musu.pro`, and blockers `missing_kv_rest_api_token`, `missing_kv_rest_api_url`, and `live_evidence_p2p_relay_lease_kv_not_configured`. |
| 2026-06-02 v242 | Code and docs were re-indexed after the hosted P2P env preflight script. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1256 files and 2214 symbols after adding `show-musu-pro-p2p-env-status.ps1` and updating the P2P next-action docs, beta checklist, WIKI, WIKI_INDEX, GOAL, and CoS memory. Validation passed `git diff --check`, script JSON output, and `-FailOnProblem` failure behavior. |
| 2026-06-02 v243 | Latest HEAD was manually redeployed to `musu.pro` and live site QA passed. | `Deploy musu-bee to Vercel` workflow_dispatch run `26777905910` succeeded for commit `00694a2e766da8e0a79dd6dd7bb82fdadb6c39d1`. Live browser QA against `https://musu.pro` passed on `/`, `/landing`, `/pricing`, and `/install` across desktop `1280x720` and mobile `390x844`: actual scroll movement, no horizontal overflow, visible logo source contains `favicon-header.png`, `--musu-color-brand-emerald=#24C8DB`, and emerald accent color `36, 200, 219`. |
| 2026-06-02 v244 | Release evidence freshness gates were narrowed to docs/status/tooling-only deltas. | `write-release-go-no-go.ps1`, `verify-single-machine-evidence.ps1`, and `verify-runtime-cpu-scenario-matrix.ps1` now allow only `docs/*`, `.github/workflows/deploy-musu-bee.yml`, `scripts/windows/show-musu-pro-p2p-env-status.ps1`, and the exact release-gate verifier scripts as non-runtime-affecting deltas after a recorded evidence commit. Direct single-machine and CPU matrix verifier checks pass again for `20260602-033029-HUGH_SECOND` and `20260602-033636-HUGH_SECOND`; full go/no-go now reports `single_machine_verified=true`, runtime idle CPU `1/2 [HUGH_SECOND]`, runtime CPU scenario matrix `1/2 [HUGH_SECOND]`, and public release still No-Go pending second-PC/multi-device/P2P KV/support/Store evidence. |
| 2026-06-02 v245 | Code and docs were re-indexed after redeploy/freshness-gate documentation. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1257 files and 2214 symbols after the Vercel run `26777905910` note, live `musu.pro` QA results, freshness-gate script updates, BETA/WIKI/WIKI_INDEX/GOAL updates, and CoS memory `2026-06-02_0458_kst_musu_pro_redeploy_freshness_gate.md`. |
| 2026-06-02 v246 | Post-push freshness gate was corrected to include exact release-gate tooling paths. | After commit `da525769`, post-commit go/no-go briefly fell back to `single_machine=false`, runtime idle CPU `0/2`, and matrix `0/2` because the release-gate scripts themselves were not in the non-runtime-affecting allowlist. The allowlist now includes only the exact gate tooling paths, not arbitrary `scripts/*`; go/no-go from dirty state returns `single_machine_verified=true`, runtime idle CPU `1/2 [HUGH_SECOND]`, runtime CPU scenario matrix `1/2 [HUGH_SECOND]`, and the expected dirty-git blocker. `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1257 files and 2214 symbols. |
| 2026-06-02 v247 | The returned second-PC zip was imported and classified as incomplete for runtime release gates. | `import-second-pc-return.ps1 -ReturnZipPath F:\Aisaak\Projects\localsend\second-pc-return\20260531-165240-HUGH-MAIN.second-pc-return.zip -Json` returned `ok=true` for remote `HUGH-MAIN` at `192.168.1.192:8949`, imported MSIX install evidence to `.local-build\msix-install\20260531-165207-HUGH-MAIN.evidence.json`, and imported handoff to `.local-build\second-pc-handoff\20260531-165230-HUGH-MAIN.handoff.json`. The archive contains no release-grade runtime idle CPU evidence, no runtime CPU scenario matrix, and no release-check JSON, so go/no-go correctly remains `runtime idle CPU 1/2 [HUGH_SECOND]`, `runtime CPU matrix 1/2 [HUGH_SECOND]`, and `multi_device_verified=false`. |
| 2026-06-02 v248 | Fresh current-HEAD second-PC handoff artifacts were regenerated and verified. | `prepare-multidevice-test-kit.ps1` generated `.local-build\multi-device-test-kit\musu-multidevice-1.15.0-rc.1-20260602-052353.zip`; `prepare-final-operator-gate-packet.ps1 -Json` generated `.local-build\final-operator-gates\musu-final-operator-gates-1.15.0-rc.1-20260602-052411.zip`, and `verify-final-operator-gate-packet.ps1` passed with `ok=true`, `fail_count=0`, `kit_count=1`. `prepare-operator-action-pack.ps1` then generated `.local-build\operator-action-pack\MUSU-1.15.0-rc.1-operator-action-pack-20260602-052442.zip`; `verify-operator-action-pack.ps1` passed with `ok=true`, `fail_count=0`. The second-PC transfer zip to run next is `.local-build\operator-action-pack\MUSU-1.15.0-rc.1-operator-action-pack-20260602-052442\second-pc\MUSU-second-PC-transfer-1.15.0-rc.1-20260602-052442.zip`. |
| 2026-06-02 v249 | Code and docs were re-indexed after second-PC return classification. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1258 files and 2214 symbols after recording the incomplete `HUGH-MAIN` return zip classification, fresh second-PC kit/final-packet/action-pack paths, and CoS memory `2026-06-02_0524_kst_second_pc_return_classification.md`. |
| 2026-06-02 v250 | Node/WebView2 process attribution was made operator-visible and added to second-PC handoff flow. | Added `scripts\windows\show-musu-process-attribution.ps1` with schema `musu.process_attribution_summary.v1`, wired it into `run-second-pc-release-check.ps1`, `import-second-pc-return.ps1`, the multi-device kit, final operator packet, operator action-pack verifier, and desktop readiness audit. Current local diagnostic output on `HUGH_SECOND` reports machine-wide `node.exe=16`, MUSU-owned Node `0`, unowned Node `16`, machine-wide WebView2 `12`, MUSU-owned WebView2 `6`, unowned WebView2 `6`, repo-related orphan helpers `0`, and process ownership checks passing. The release policy is now explicit: raw machine-wide Node count is diagnostic; release blockers are MUSU-owned helpers, repo-owned orphan helpers, hot CPU/resource samples, and two-machine runtime evidence. The release freshness allowlist now includes only the exact operator packet/import/action-pack/process-attribution scripts touched here so this non-runtime tooling change does not invalidate current primary runtime evidence while still rejecting arbitrary `scripts/*` changes. |
| 2026-06-02 v251 | Code and docs were re-indexed after process attribution wiring. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1261 files and 2214 symbols after adding wiki/536 process attribution audit, CoS memory `2026-06-02_0540_kst_process_attribution_node_count.md`, the `musu.process_attribution_summary.v1` script, second-PC return/import wiring, packet/action-pack verification updates, and release evidence freshness allowlist updates. Search terms include `process-attribution-summary`, `machine-wide node.exe=16`, `owned_node=0`, `owned_webview2=6`, `show-musu-process-attribution.ps1`, and `wiki/536`. |
| 2026-06-02 v252 | Hosted P2P KV env configuration became an operator-safe script. | Added `scripts\windows\configure-musu-pro-p2p-env.ps1` with schema `musu.configure_musu_pro_p2p_env.v1`; it sets `KV_REST_API_URL` as a GitHub variable by default, `KV_REST_API_TOKEN` as a GitHub secret, optional relay policy env names, and can trigger `deploy-musu-bee.yml` without printing values. Dry-run validation with redacted KV inputs returned `ok=true`, `settings_requested=[KV_REST_API_URL variable, KV_REST_API_TOKEN secret]`, no missing required values, and no errors. Final packet generation/verification and desktop readiness script coverage now include the configurator. This does not provision KV by itself; it turns the remaining `musu.pro` P2P blocker into a safe runbook once the operator has Upstash/Vercel KV values. |
| 2026-06-02 v253 | Code and docs were re-indexed after hosted P2P KV env configurator wiring. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1263 files and 2214 symbols after adding `configure-musu-pro-p2p-env.ps1`, P2P next-action/CONFIG/BETA/WIKI/WIKI_INDEX updates, and CoS memory `2026-06-02_0605_kst_p2p_kv_env_configurator.md`. Search terms include `musu.configure_musu_pro_p2p_env.v1`, `KV_REST_API_URL variable`, `KV_REST_API_TOKEN secret`, `Deploy`, `DryRun`, and `p2p_relay_lease_kv_not_configured`. |
| 2026-06-02 v254 | mDNS discovery now exits on disconnected browse receivers instead of spinning for the full window. | `musu-rs/src/peer/mdns.rs` now distinguishes `flume::RecvTimeoutError::Timeout` from `Disconnected` when browsing `_musu._tcp.local.`. A timeout keeps the bounded discovery window alive; a disconnected receiver logs and breaks immediately. This preserves the default-off mDNS posture while hardening explicit `MUSU_ENABLE_MDNS=1` and `musu discover` paths against the operator-observed Windows/Tailscale `closed channel` failure class becoming a short busy-loop. Added a unit test for the receive-error classifier. Validation passed: `cargo test --manifest-path .\musu-rs\Cargo.toml --lib -j 1 peer::mdns::tests::` passed 3/3. The earlier non-`--lib` filtered run also passed the mDNS tests but failed when Cargo attempted the unrelated `r6_auto_update` integration harness, which requires elevation on this Windows host. |
| 2026-06-02 v255 | Code and docs were re-indexed after mDNS disconnected-receiver hardening. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1264 files and 2217 symbols after the mDNS receive-loop patch, BETA/WIKI/WIKI_INDEX updates, and CoS memory `2026-06-02_0633_kst_mdns_disconnected_receiver_hardening.md`. Search terms include `MdnsRecvTimeoutKind`, `flume::RecvTimeoutError`, `mDNS browse receiver disconnected`, `sending on a closed channel`, `MUSU_ENABLE_MDNS=1`, and `peer::mdns::tests::`. |
