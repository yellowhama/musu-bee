# GOAL — musu autonomous /loop source of truth

**Wiki ID**: wiki/485
**Created**: 2026-05-19
**Last reshape**: 2026-05-20 (V24-Rust-big-bang → R-fast/R-cleanup phased → 2026-05-20 evening pivot: single-machine first, R9 deferred per operator "이 기계에서 무수 다른기능-멀티기기 말고 다 되는지")
**Updates**: append-only revision history at bottom
**Owner**: autonomous /loop orchestrator (Claude Opus 4.7 1M context). Per user 2026-05-20: "니가 결정, 나는 고객임, 니가 사장임" — orchestrator is 사장 (decides closure), user is customer (rejection right + AS duty owed back).
**User**: emptyermind@gmail.com

This is the single source of truth for the autonomous /loop. Future Claude sessions read this first. Goals are **explicit + dated + measurable**. Section A is current cycle goal. Sections B–D are reference state (branch / gates / deferred).

**Current operating pointer (2026-06-01)**: the historical V24/V26 planning chain remains useful context, but the live beta-readiness state is now `1.15.0-rc.1`. Future sessions should start with `docs/RELEASE_1_15_0_RC1_CURRENT_STATUS_AUDIT_2026_05_31.md` (wiki/522), `docs/RELEASE_1_15_0_RC1_RUNTIME_HARDENING_RELAY_ROADMAP_2026_05_31.md` (wiki/523), `docs/MUSU_RUNTIME_STABILIZATION_EXECUTION_PLAN_2026_05_31.md` (wiki/525), `docs/MSIX_DESKTOP_ENTRYPOINT_AUDIT_2026_05_31.md` (wiki/526), `docs/RELEASE_1_15_0_RC1_QUAL_AUDIT_NEXT_STEPS_2026_06_01.md` (wiki/527), `docs/BETA_RELEASE_CHECKLIST_1_15_0_RC1.md`, and this file's revision history before resuming roadmap work.

---

## §A — Current cycle goal: V24-Rust-cleanup (panel-reshaped 2026-05-20)

### A.0 The decision (post-panel YELLOW reshape)

**Initial draft (REJECTED by Phase -1 panel YELLOW, 4 HIGH)**: "Big-bang Rust rewrite — Python 25k 통째로 Rust로. 4-6주. 동안 musu = down."

**Panel-reshaped (user accepted all 4 HIGH 2026-05-20)**: **Two-phase Rust cleanup release**.

- **R-fast (~2 weeks → JTBD ships)**: R0 + R1 (bridge module) + R2 (core module) + R7 (musu-bee wire) + R8 (4060Ti E2E) + R9 (5070Ti cross-machine). Rust auth + bridge live on both machines; land-os + vibecode-town companies registered; one cross-machine task routes. Python still alive at port 8071 serving control/indexer/writer behind Rust auth facade.
- **R-cleanup (~2-4 weeks parallel to operator dogfood)**: R3 (control module) → R4 (indexer module) → R5 (writer module) → R6 (installer rewrite) → R10 (Python deletion + final closure). Each step removes one Python service from facade.
- **End state identical to original big-bang**: Python fully deleted, all musu Rust. But JTBD ships day ~14, not day 28-42.

**WHY** (panel HIGH-1 + HIGH-2 acceptance):
1. User instruction "백엔드로 파이썬 쓰지 말라고" still load-bearing. Python ban applies to NEW code; existing Python tolerated behind Rust facade ONLY during R-cleanup window.
2. HIGH-1 Christensen: original big-bang inverted JTBD priority. R1+R2 subset is already incremental (7 endpoints, fresh schema, 10-20 tools) — language change doesn't make it big-bang.
3. HIGH-2 Taleb: 4-6 weeks single window = fragile (4060Ti hardware, Windows reinstall, rmcp/tantivy yank, GitHub outage, operator emergency all reset clock).
4. Thesis lock from SSOT_1PAGE_2026-04-09 holds: 4-layer (CoS / machines / companies / agents) on 4060Ti + 5070Ti.

**STACK** (panel MED-1 reshape): **single binary crate `musu-rs`** with internal modules (`bridge`, `core`, `control`, `indexer`, `writer`). NOT 5 separate crates. tokio + axum + sqlx (or rusqlite) base; per-module deps audited at R0 per MED-2. Saves ~28 subagent-phase invocations.

**VERSION** (panel HIGH-3 + LOW-1): **1.14.0** at V24 close, NOT 2.0.0. V24 is engineering cleanup with no Create dimension (no user-facing capability that exists *because of* Rust). 2.0.0 would oversell engineering work as strategic milestone.

**ACCEPTANCE METRIC** (panel HIGH-4, ungameable Goodhart firewall): §9.12 in wiki/490 — operator-attested text reply OR operator git commit in land-os/vibecode-town within 7 days of R10 confirming ≥3 named real tasks completed via musu. Orchestrator MAY NOT mark V24 §9.11 (GOAL.md §A complete) until §9.12 exists in wiki/500 closure HTML. Structurally ungameable: orchestrator cannot fake operator terminal history or git authorship signature.

**Reshape lock semantics** (panel MED-3 resolved by user 2026-05-20): "reshape 금지" covers ORCHESTRATOR-initiated mid-execution reshape proposals during R1-R10 only. Phase -1 panel HIGH findings DO trigger plan reshape (standard agent-team interpretation per MODE_Agent_Team.md). This reshape (panel-driven) is explicitly allowed; future orchestrator-initiated reshapes during execution are NOT.

### A.1 Day-2 acceptance (operator-defined + panel-strengthened)

**Acceptance bar = D (cross-machine) + §9.12 (operator-attested)**:
1. ✅ Installer succeeds end-to-end on 4060Ti from clean shell (R-fast end, ~day 14)
2. ✅ Installer succeeds on 5070Ti (R-fast end)
3. ✅ `F:\Aisaak\Projects\land-os` registered as musu company "land-os 개발팀"
4. ✅ `F:\Aisaak\Projects\vibecode-town` registered as musu company
5. ✅ `/api/nodes/add` mesh: both `nodes.toml` mention each other
6. ✅ Cross-machine task: one task delegated 4060Ti↔5070Ti; audit_log on receiving machine shows arrival; result returns
7. ✅ audit_log has ≥ 5 rows where `actor_ip != 'testclient'` (gameable per HIGH-4 — backstopped by §9.12)
8. ✅ V24 final closure HTML wiki/500 + qual eval committed
9. ✅ **§9.12 ungameable closure metric**: operator-attested text reply in V24 closure thread OR operator git commit in land-os/vibecode-town within 7 days of R10 confirming ≥3 named real tasks completed via musu. Reproducible from operator's own terminal history or git log. **Orchestrator MAY NOT mark V24 GOAL.md complete until §9.12 written by operator.** Structurally ungameable.

Closure decision shape:
- Items 1-8: orchestrator (사장) declares done per acceptance evidence.
- Item 9 (§9.12): operator-authored, not orchestrator-asserted. This is the Goodhart firewall.
- Until §9.12 exists, V24 stays "code shipped, acceptance pending" — not closed.

### A.1.1 Pivot 2026-05-20 evening: single-machine completeness before R9

**Trigger**: R8 PASS on 4060Ti (wiki/498c, commit `0d58f08`). Operator follow-up: "이 기계에서 무수 다른기능-멀티기기 말고 다 되는지. 자동 업데이트 기능 깉은 편의성 기능 있는지". Probe found musu-rs surface is bridge + DB + auth + audit + dedup + company CRUD only; every other Python endpoint (`/api/system/update`, `/api/agents`, `/api/instructions`, `/api/briefing`, `/api/sse`, `/api/wiki/*`, `/api/workflows`, `/api/templates`, `/api/system/backup`, `/api/system/restart`) returns 502 because Python facade target is gone after `~/.musu` delete.

**Reorder** (sequence change, NOT scope change — same R-cleanup sub-WS set):
- **OLD**: R3 (control/MCP) → R4 (indexer) → R5 (writer) → R6 (installer) → R9 (cross-machine) → R10 (Python delete)
- **NEW**: **R5 (writer) → R6 (installer + auto-update + supervisor) → R3 (control/MCP) → R4 (indexer)** → R9 → R10

**Why R5 first**: writer-stub currently inserts `route_executions` row + tries facade POST to dead Python = `python_unreachable`. This is the load-bearing endpoint for any agent task; without native writer, every `/api/companies/{id}/run` is a stub. Highest single-machine impact.

**Why R6 second**: no supervisor + no auto-update + no service registration = operator must keep a bash window open to run musu. R6 brings systemd/Windows-service equivalent + `git pull` auto-update path. This is what the operator literally asked about ("자동 업데이트 기능 깉은 편의성 기능 있는지").

**Why R9 deferred**: cross-machine is multi-machine by definition; operator explicitly carved it out for this cycle's focus. R9 task still in §A.2 table, status moved to "deferred (post single-machine completeness)".

**R-cleanup sequencing budget**: R5 (~1500 LOC) → R6 (~600 LOC) → R3 (~1500 LOC) → R4 (~1500 LOC). Solo-operator review bottleneck per panel H5: strict sequence, no parallel. Each sub-WS = Phase 0 Researcher → Phase 1 Planner → Phase 1.5 Critic (MED risk) → Phase 3 Builder → Phase 5 Auditor → Phase 7 Scribe HTML closure → Const VII per-push.

**§9.12 unchanged**: still locked to V24 closure (wiki/500) post-R10. Single-machine reorder does not affect Goodhart firewall.

### A.2 Sub-WS (panel-reshaped, R-fast → R-cleanup phases)

Detail per-WS plans live in wiki/491..500 reservations. Phase -1 strategic gate on master plan (wiki/490) ran 2026-05-20: verdict YELLOW, 4 HIGH accepted, plan reshaped per §A.0 above. Per-sub-WS Phase 1.5 Critic still required for HIGH/MED sub-WS.

**Workspace** (panel MED-1): single binary crate `musu-rs/` with internal modules. NOT 5 separate crates.

```
musu-rs/Cargo.toml      (single workspace)
musu-rs/src/main.rs     (subcommand dispatch)
musu-rs/src/bridge/     (R1)
musu-rs/src/core/       (R2)
musu-rs/src/control/    (R3)
musu-rs/src/indexer/    (R4)
musu-rs/src/writer/     (R5)
```

**R-fast (~2 weeks → JTBD ships at R9 end)** — strict sequence, no parallel:

| Sub-WS | Wiki | Module | Scope | Risk |
|---|---|---|---|---|
| V24-R0 | wiki/490 | — | This reshape commit + workspace bootstrap + Phase 0 audit of musu-supervisor/ + musu-port/ (MED-2) + dependency-audit doc | — |
| V24-R1 | wiki/491 | `bridge` | tokio+axum, 7 endpoints, bearer auth, **+ Python-facade reverse-proxy** for control/indexer/writer/wiki paths → localhost:8071 | HIGH |
| V24-R2 | wiki/492 | `core` | SQLite schema v1 (fresh, no migration) + companies.yaml loader | HIGH |
| V24-R7 | wiki/497 | — | musu-bee TS wire-up (BRIDGE_URL + endpoint paths) | LOW |
| V24-R8 | wiki/498 | — | 4060Ti E2E: install + register land-os + vibecode-town; Python still on port 8071 behind facade | MED |
| V24-R9 | wiki/499 | — | 5070Ti install + cross-machine task. **JTBD ships at end of R9** (~day 14). Operator starts dogfooding. | MED |

**R-cleanup (~2-4 weeks parallel to operator dogfood)** — strict sequence, each removes one Python service from facade. **Sequence reordered 2026-05-20 evening per §A.1.1 pivot**: R5 first (writer = highest single-machine impact), then R6 (installer + auto-update = operator-asked ergonomic gap), then R3/R4. R9 deferred until single-machine complete.

| Sub-WS | Wiki | Module | Scope | Risk | Order |
|---|---|---|---|---|---|
| V24-R5 | wiki/495 | `writer` | Agent task execution + SSE; replace writer-stub Python forward in `/api/companies/{id}/run` with native execution. Integrate `musu-supervisor-isolation-{linux,windows,macos}` crates. | MED | 1st |
| V24-R6 | wiki/496 | — | Installer rewrite (single-binary, no facade, no Python) + **auto-update path** (`git pull` + rebuild + service restart) + **service supervisor** (systemd unit + Windows Service + auto-restart) | MED | 2nd |
| V24-R3 | wiki/493 | `control` | MCP server stdio JSON-RPC (~10-20 tools subset); remove /api/control/* from facade | MED | 3rd |
| V24-R4 | wiki/494 | `indexer` | Per-company indexing (tantivy or FTS5); remove /api/indexer/* from facade | MED | 4th |
| V24-R9 | wiki/499 | — | 5070Ti install + cross-machine task. **Deferred** post single-machine completeness (R5+R6+R3+R4 done) per operator carve-out 2026-05-20 evening. | MED | 5th (deferred) |
| V24-R10 | wiki/500 | — | MANUAL GATE: Python `rm -rf` + closure HTML + **CHANGELOG 1.14.0** + §9.12 wait | — | 6th |

**Total Rust LOC estimate**: ~10,200 (vs 25,885 Python). Subagent-phase count: 7 chains (R7 compressed), saving ~28 invocations vs 5-crate split.

### A.3 Hard constraints (cycle-level invariants)

1. **No new SaaS dep** — [[feedback-self-contained-product]]. Rust crates from crates.io only; no fly/AWS/Vercel/etc.
2. **No Python** — [[feedback-no-python]]. New code Rust only. Shell scripts (install.sh) OK. Existing 25k Python = deprecate target, no new endpoints/handlers/migrations added to it.
3. **No K8s/Argo/CRD/Operator pattern** — [[feedback-no-yagni-architecture]]. Single binary + tokio async. No Kubernetes runtime at musu scale (4060Ti + 5070Ti, 4 companies max).
4. **No new schema migrations on existing Python codebase** — schema v37 is the last Python-side schema. Rust port designs fresh schema (semver reset for SQLite migrations to v1).
5. **Reshape lock ON** — orchestrator does not propose scope reshapes during V24 execution without explicit user request.
6. **Scribe HTML closure** — [[feedback-scribe-html-only]]. Per-sub-WS closure markdown; final wiki/500 HTML.
7. **Critic per sub-WS** — V24-R1 + V24-R2 + V24-R3 + V24-R5 + V24-R8 + V24-R9 (≥MED risk) require `system-architect` Critic per [[feedback-plan-stage-auditor]]. LOW-risk skips.
8. **Dual audit for V24-R1 + V24-R6** — auth-touching endpoints + installer (one-way blast radius via service registration).

### A.4 Operator gates (where /loop pauses)

Per [[feedback-autonomous-loop]]:

| Gate | Status | Action |
|---|---|---|
| **Phase -1 RED verdict** | not yet evaluated | If RED, HALT V24 until user decision |
| **Const VII main-merge** | PENDING (#436) | Orthogonal to V24; /loop continues on v23/phase4-rust or new v24/main-rust branch |
| **Const III schema apply** | will fire on R2 | New SQLite schema apply on operator machine = manual gate |
| **Production deploy** | N/A (single-operator) | — |
| **Irreversible destructive op** | wiki/500 Python deletion | Manual gate before `rm -rf musu-bridge/ musu-core/ musu-control/ musu-indexer/ musu-writer/` |

### A.5 What does NOT change

- musu thesis (SSOT_1PAGE 2026-04-09, 4-layer CoS/machines/companies/agents) — locked, not under review
- musu-bee (TS/React frontend) — keep, only adjust endpoints
- musu-relay-gateway Rust crate (V23.1 T1.8) — keep, integrate
- musu-supervisor-isolation-{linux,windows,macos} Rust crates (V21.D) — keep, integrate
- Operator hardware: 4060Ti + 5070Ti (multi-machine reality, locked since 5b8b103 2026-04-07)
- `INSTALL.md` / `QUICKSTART.md` / `ONBOARDING.md` — keep as templates, rewrite content for Rust bridge in V24-R6

### A.6 Supersedes

- **V23.6-minimal (prior §A)** — superseded. W2 direnv + W4 LESSONS_LEARNED were operator hygiene tasks; V24 makes operator hygiene moot until the install path actually runs. Re-evaluate post-V24 if still relevant.
- **V24-Python plan (deleted 2026-05-20)** — was extending Python violation. Replaced with V24-Rust-big-bang.

---

## §A-prior (ARCHIVED) — V23.6-minimal (superseded 2026-05-20 by V24-Rust-big-bang)

### A.0 v2 amend (2026-05-19): scope cut from 5 sub-WS to 2

Per `business-panel-experts` adversarial critique of wiki/471 v1 + W5 RED verdict from V23.6 Phase −1 strategic gate, V23.6 scope is cut to **operator hygiene only**:

**Ship**: W2 direnv pattern + `.envrc.example` (~40 LOC, doc + operator hygiene) + W4 LESSONS_LEARNED.md seed (~30 LOC, doc). Total ~70 LOC.

**Paused** (all V23.7+ pending entry conditions): W1 mitmproxy wiretap, W3 uv migration, W5 litellm (RED verdict).

**Entry conditions for V23.6 master plan Phase −1 spawn** (ALL three required):
1. Operator clears #436 (V23.3 + V23.4 + V23.5 → main) OR explicitly confirms #436 batching policy (deliberate vs accidental)
2. **~~≥1 V23.5 product-outcome telemetry signal exists~~ → DATA RETURNED 2026-05-19 v3: ZERO.** Ground-truth: `audit.db` 601 rows = 100% pytest `testclient`, `/api/wiki/*` + briefing paths = 0 hits ever; `telemetry.db` install_attempt + nat_pierce + agent_spawn = 0 rows ever. **Revised condition: operator decides what to do with the zero-usage finding** — either (a) accept "V23.5 was a capability investment, no runtime expected yet, V23.6 proceeds anyway", (b) "stop adding surface until V23.5 features are actually exercised once" (panel-aligned default), or (c) "build the runtime that would consume V23.5 surface first, before V23.6 hygiene work" (deepest change).
3. Operator reviews ≥3 V23.5 sub-WS Builder diffs end-to-end (still pending — no signal exists that this has happened)

Rationale: panel H5 (parallelism doesn't help solo operator) + M5 (#436 4-cycle backlog) + H2 (V23.5 product outcome) — H2 now confirmed empirically zero, not unknown. The remaining question is not "is V23.5 used" (answer: no) but "given that V23.5 is not used, what does V23.6 do."

### A.1 In-scope (v2)

**V23.6-minimal**: 2 sub-WS only (W2 + W4). All other candidates from wiki/484 deferred to V23.7+ pending entry conditions in §A.0.

| Wave | Sub-WS | Source | LOC | Risk | Why this wave |
|---|---|---|---|---|---|
| 1 | **V23.6-W2** direnv pattern + `.envrc.example` | wiki/484 #2 (5-step #1) | ~40 + doc | LOW | operator hygiene; no musu code change; `.env.example` is starting point |
| 1 | **V23.6-W4** LESSONS_LEARNED.md seed | wiki/484 #5 ("lessons.md") | ~30 + 5 seeds | LOW | 2AM-debugging running journal; distinct from MEMORY.md (per-type) + qual eval (post-mortem) |
| 2 | **V23.6 final closure (HTML wiki/486) + qual eval (wiki/487)** | per [[feedback-scribe-html-only]] | — | — | Const VII per-push automated; main-merge stays operator-pending. |

**Total**: ~70 LOC + closure. Estimated 1-2 /loop iterations.

**Phase chain for V23.6-minimal (compressed)**:
1. Operator clears 3 entry conditions in §A.0 (this is the only gate; no autonomous bypass)
2. Cut `v23/phase5` branch off `main` (or continue on `v23/phase4`)
3. Phase −1 strategic gate: SKIP — scope is operator hygiene only, no thesis extension, no new SaaS dep, no new code surface
4. Phase 1 Planner: minimal wiki/485 (V23.6-minimal master plan, ~1 page)
5. Phase 1.5 Critic: SKIP per `[[feedback-plan-stage-auditor]]` (LOC <500, no thesis extension)
6. Phase 3 Builder × 2 (W2 + W4 sequential — solo operator review-friendly per panel H5)
7. Phase 7 Scribe: wiki/486 HTML closure + wiki/487 qual eval
8. Const VII push gate per commit

**Paused** (V23.7+ pending entry conditions in §A.0):
- V23.6-W1 mitmproxy wiretap (wiki/484 #1) — was MED risk, deferred
- V23.6-W3 uv migration musu-bridge (wiki/484 #4) — was LOW risk, deferred
- V23.6-W5 litellm in front of `cos_briefing_agent` (wiki/484 #3) — Phase −1 RED verdict locked 2026-05-19

### A.2 Out-of-scope (firewalled to V23.7+)

| Item | Source | Why deferred |
|---|---|---|
| **inspect-ai eval suite** (wiki/484 #6) | 5-step #5 EVALS | HIGH risk; needs Phase −1 mini-gate validating [[feedback-no-yagni-architecture]] ("pytest + Playwright 부족 입증 필수") |
| **AutoAgent meta-agent harness** (wiki/484 #7) | Karpathy auto-research + Kevin Goo AutoAgent | HIGH+ research-tier. Prerequisites: inspect-ai shipped + 60d behavior metric + invariant lock + LLM budget cap. Stress points in wiki/484 §3.7. |
| 10 V23.6 scope firewall items (wiki/469) | V23.5 W-8 closure | Wiki page editing UI, RAG auto-retrieval, agent session memory persistence, multi-tenant scope expansion, H-4b QUIC, T2-D-visual React Flow, C-3 Y-path promotion, Paperclip/OpenClaw observer, wiki page editing API, server-side render mode. All require new Phase −1 gate. |
| C-3 LLM synthesis promotion to default | wiki/479 + wiki/488 reserved | V23.7 gate: ≥40% click rate / 60d. V23.6-W5 litellm satisfies 2/4 preconditions; V23.7 picks up other 2 (caching policy + budget UI). |

### A.3 Success criteria

V23.6 cycle is DONE when:
1. ✅ wiki/485 V23.6 master plan exists with Phase −1 verdict + Critic findings resolved
2. ✅ 5 sub-WS shipped on branch (v23/phase5 or v23/phase4 continuation), each commit reference closure doc
3. ✅ All tests green per sub-WS: tsc + pytest + Playwright + jest baseline maintained (~217 V23.5 baseline + V23.6 additions)
4. ✅ wiki/486 final closure (HTML) + wiki/487 qual eval (MD) committed
5. ✅ CHANGELOG 1.14.0 entry finalized
6. ✅ Const VII per-push gate satisfied (each commit pushed)
7. ✅ This GOAL.md §A updated with V23.6 completion + V23.7 cycle goal set (or paused for operator decision)

### A.4 Hard constraints (cycle-level invariants)

1. **No new SaaS dependency** — [[feedback-self-contained-product]]. litellm (in-process), uv (in-process), direnv (operator hygiene), mitmproxy (dev mode only), LESSONS_LEARNED (doc) — all compliant.
2. **No YAGNI** — [[feedback-no-yagni-architecture]]. Each candidate's leverage proxy in wiki/484 §3 must hold or stop.
3. **Schema stable** — no new migrations (Const III not triggered). schema v37 is V23.6 baseline.
4. **Performance** — no Const VI experiment. LLM costs are opt-in user-paid (V23.5 C-3 hard constraints inherited).
5. **Branch hygiene** — operator main-merge optional acceleration. autonomous /loop continues on v23/phase4 if main-merge stays pending past 1 V23.6 wave.
6. **Scribe HTML** — [[feedback-scribe-html-only]]. Closure docs HTML; other phase docs Markdown.
7. **Plan-as-spec Auditor calibration** — [[feedback-plan-stage-auditor]]. Only fire if V23.6 master plan ≥500 LOC (current estimate ~330 → likely skip).
8. **Critic gate per non-trivial sub-WS** — V23.6-W5 litellm (MED risk) requires Critic. W1-W4 LOW risk → compressed Critic+Builder.

---

## §B — Branch + commit state (snapshot 2026-05-20, V24-Rust-cleanup start, panel-reshaped)

- **Active branch**: `v23/phase4` HEAD `b6896af` (GOAL.md V24-Rust-big-bang reshape commit) — to be branched into `v24/rust-cleanup` at V24-R0 start (renamed from `v24/rust-big-bang` per panel reshape)
- **Pushed to**: `origin/v23/phase4`
- **Cumulative LOC (Python, to be deleted at R10)**: 25,885 LOC across musu-bridge + musu-core + musu-control + musu-indexer + musu-writer (3,312 .py files verified)
- **Existing Rust workspaces (audit pending R0 per panel MED-2)**:
  - `musu-supervisor/` workspace (V21.D crates `musu-supervisor-core`, `musu-supervisor-isolation-{linux,windows,macos}`) — R5 integration target IF audit passes
  - `musu-port/` workspace — R9 NAT/punch-through fallback IF audit passes
- **CHANGELOG**: at 1.13.0 (V23.5 closed). V24-R0 will set **1.14.0-dev** (NOT 2.0.0 — panel HIGH-3 + LOW-1: V24 is engineering cleanup, no Create dimension).
- **VERSION**: 1.13.0-dev → 1.14.0-dev on V24-R0 commit

---

## §C — Operator gates (V24-Rust-big-bang scope)

Per [[feedback-autonomous-loop]]:

| Gate | Current status | Action |
|---|---|---|
| **Phase -1 strategic gate** | not yet evaluated | Next /loop iteration must spawn `business-panel-experts` debate on Rust big-bang thesis before V24-R0 plan body writes |
| **Const VII main-merge** | orthogonal to V24 | #436 still pending V23 backlog → main; /loop continues on `v24/rust-big-bang` branch independently |
| **Const III schema apply** | will fire on V24-R2 | New SQLite schema on operator machine = manual gate |
| **Const III Python schema rollback** | will fire on V24-R10 closure | Final Python deletion = manual gate (irreversible) |
| Production deploy | N/A (single-operator) | — |
| Irreversible destructive op | wiki/500 Python deletion | Manual gate before `rm -rf musu-bridge/ musu-core/ musu-control/ musu-indexer/ musu-writer/` |

**Phase -1 gate is next action.** /loop continues to plan + Phase -1 panel; only blocks if RED verdict.

---

## §D — Reference state (key wiki IDs)

| Wiki | Title | Status |
|---|---|---|
| wiki/431 | V23.4 Phase 4 master plan | shipped (Phase 4 closed) |
| wiki/447 | V23.4 Phase 4 final closure (HTML) | complete |
| wiki/448 | V23.4 Phase 4 qual eval | complete |
| wiki/459 v4 | V23.5 master plan | shipped (V23.5 closed) |
| wiki/460 | V23.5 implementation plan | shipped + extended with wiki/484 §9 candidates |
| wiki/469 | V23.5 W-8 closure (V23.6 firewall 10 items) | complete |
| wiki/470 | V23.5 final closure (HTML) | complete |
| wiki/471 | V23.5 qual eval | complete |
| wiki/479 | V23.7 promotion criterion (≥40% click rate / 60d) | doc-only, awaits 60d data |
| wiki/483 | H-4b QUIC mesh propagation | reserved (deferred V23.6/V23.7) |
| **wiki/484** | **V23.6 planning input — agentic 5-step + AutoAgent (7 candidates)** | complete; PRIMARY V23.6 input |
| **wiki/485** | **V23.6 master plan** | **TO BE WRITTEN** |
| wiki/486 | V23.6 final closure (HTML) | reserved |
| wiki/487 | V23.6 qual eval | reserved |
| wiki/488 | V23.7 Y-path promotion gate | reserved (V23.7 strategic gate) |

---

## §E — Loop heartbeat decision tree

Per `~/.claude/MODE_Agent_Team.md` /loop decision tree, evaluate in order:

1. **block-on-user?** → operator gate active (e.g., #436) OR Phase −1 RED verdict awaiting decision → emit gate prompt, halt
2. **regression?** → prior deploy invariants broken (V23.5 tests fail, /health degraded) → spawn `root-cause-analyst`, drop other work
3. **audit-fix open?** → HIGH finding from last audit unresolved → Builder
4. **post-build, no audit?** → commits since last audit → Auditor
5. **post-plan, critic done, no build?** → Builder (sub-WS V23.6-W1..W5)
6. **plan drafted, critic unrun?** → Critic (system-architect)
7. **new sub-task in cleared master?** → Researcher + Explore parallel, then Plan
8. **NEW master plan / thesis extension?** → Phase −1 strategic gate FIRST

### Current state evaluation (2026-05-20 — V24-Rust-big-bang start)

- Step 1: NO operator gate active (Phase -1 not yet run; not RED). #436 orthogonal.
- Step 8: **NEW master plan** → Phase -1 strategic gate FIRES. Next action: `EnterPlanMode` for V24 master plan (wiki/490) body draft + `business-panel-experts` debate on Rust big-bang thesis.

**Next /loop iteration goal**: enter plan mode → draft wiki/490 body (sub-WS table, sequence, acceptance, Const gates predicted) → spawn Phase -1 panel (Christensen + Taleb + Kim&Mauborgne + Drucker, debate mode) → record verdict in wiki/490 §0 → if GREEN/YELLOW proceed to V24-R0 commit, if RED HALT.

---

## §F — Revision history

| Date | Change | Reason |
|---|---|---|
| 2026-05-19 v1 | Initial GOAL.md created, V23.6 cycle defined | User request "야 골설정 제대로 해서 /loop으로 만들어"; covers scope 1+2 (V23.6 master plan entry + sub-WS impl); dynamic cadence; project-root location |
| 2026-05-19 v2 | §A scope cut: 5 sub-WS → 2 (V23.6-minimal: W2+W4 only). §A.0 added with 3 entry conditions. §E decision tree current-state recalibrated. | Post-`business-panel-experts` adversarial critique of wiki/471 V23.5 qual eval v1 (4-expert unanimous AMEND verdict: 5 HIGH + 6 MED). W5 RED verdict from V23.6 Phase −1 strategic gate absorbed. Scope cut directly addresses panel H5 (parallelism vs solo-operator review bandwidth) + M5 (#436 4-cycle backlog) + H2 (V23.5 product-outcome unmeasured). User approved Option 1 ("amend + W5 결정 같이"). |
| 2026-05-20 v3 | §A FULL RESHAPE: V23.6-minimal → V24-Rust-big-bang. §A archived as §A-prior. §B branch state pivoted to v24/rust-big-bang. §C operator gates rescoped to Rust install + Python deletion. §E next action = Phase -1 panel. | User explosion 2026-05-20 on discovering musu-bridge 25,885 LOC Python = 6 weeks of [[feedback-no-python]] violation. User locked Rust backend + α big-bang approach (4-6 weeks down time). Day-2 acceptance defined: land-os + vibecode-town companies cross-machine on 4060Ti↔5070Ti. Reshape lock ON per user "reshape 금지". |
| 2026-05-20 v4 | §A PANEL-RESHAPE: V24-Rust-big-bang → V24-Rust-cleanup (R-fast + R-cleanup phasing). Version 2.0.0 → 1.14.0. Added §9.12 ungameable operator-attested closure metric. Single-binary `musu-rs` workspace (not 5 crates). Reshape lock semantics resolved (panel HIGH OK; orchestrator drift not OK). Branch renamed v24/rust-big-bang → v24/rust-cleanup. | Phase -1 `business-panel-experts` debate returned YELLOW with 4 HIGH + 3 MED + 1 LOW. User accepted all 4 HIGH reshapes via AskUserQuestion 2026-05-20. HIGH-1 Christensen (JTBD priority inversion), HIGH-2 Taleb (4-6wk fragility window), HIGH-3 Kim&Mauborgne (no ERRC, 2.0.0 oversells), HIGH-4 Drucker (acceptance metric Goodhart-gameable by orchestrator). MED-3 governance contradiction resolved: reshape lock covers orchestrator drift, not panel HIGH. |
| 2026-05-27 v5 | Windows distribution pivot recorded: direct-download bootstrap remains the current operator path, but the intended Windows product direction is now Store/MSIX-ready packaged runtime. R6 output must not assume Task Scheduler + self-copy + self-update are the only Windows model. | Store/MSIX audit found the current Windows path is coherent for operator bootstrap but not for long-term product distribution. Product spec now records a dual-mode Windows runtime split (`direct-download` vs `store-msix`) and future Windows packaging work must honor package-managed install/update constraints. |
| 2026-05-27 v6 | Windows packaged path clarified into **three contracts**: direct-download bootstrap, local sideload / manual bridge, and Store-reviewed restricted-capability auto-start. Repo-local proof now closes the local/manual path; the Store auto-start path remains blocked only on Partner Center verification and Microsoft review. | Post-install and packaged-startup investigation showed local sideload cannot be truthfully sold as auto-start-ready. Product/docs/scripts were reshaped so local sideload is explicit manual bridge, while Store auto-start is a separate reviewed artifact and submission bundle. |
| 2026-05-29 v7 | `1.15.0-rc.1` beta-readiness state recorded. Single-machine Windows local loop is beta-ready via `musu up` + dashboard doctor + Claude task smoke; Store auto-start and full multi-machine remain separate tracks. | Live smoke verified dashboard `3001`, bridge `11041`, task output `MUSU_SMOKE_OK`. Current qualitative evaluation, code audit, and roadmap live in wiki/518. |
| 2026-05-29 v8 | Partner Center enrollment approval cleared by operator report; Store path moved from account-verification blocker to product-name/package-submission blocker. Existing 2026-05-27 `1.13.0.0` submission bundle must be regenerated for `1.15.0-rc.1` before upload. | User reported approval and supplied a cross-product launch note. Relevant Store/channel/promotion guidance was filtered into `STORE_LAUNCH_AND_PROMOTION_PLAN_2026_05_29.md`; unrelated product names were not adopted. |
| 2026-05-29 v9 | `yellowhama/musu-system` assessed for future MUSU integration. Decision: high value, but integrate as adjacent MCP/CLI/tooling layer, not Rust-core merge or first Store bundle. | Local clone + `go test ./...` passed for `core`, `crawl-ai`, `marketer`, and `nurikun`. Report: `docs/MUSU_SYSTEM_INTEGRATION_ASSESSMENT_2026_05_29.md`. |
| 2026-05-29 v10 | Single-machine smoke was converted into `scripts\windows\smoke-single-machine-beta.ps1` and passed live; multi-device runbook/script prepared as wiki/519. Release-grade desktop assessment clarified: runtime beta is credible, but Tauri GUI shell and Store package approval remain open gates. | Script smoke passed on dashboard `3000`, bridge `11041`, task `2d9e93b1-fb2f-4cd4-ab40-1147fea89a6d`, output `MUSU_SCRIPT_SMOKE_OK`, CLI route `MUSU_SCRIPT_CLI_OK`. Multi-device script: `scripts\windows\smoke-multidevice-beta.ps1`. |
| 2026-05-29 v11 | Current-version MSIX artifacts regenerated and verified for `1.15.0.0`; Store-reviewed submission bundle prepared. | local-sideload workflow passed packaged startup smoke with release `musu-startup.exe`; Store-reviewed manifest verified `ImmediateRegistration=true` + restricted startup capability. Bundle: `.local-build\msix\submission-bundles\store-reviewed-20260529-033609`. |
| 2026-05-29 v12 | Desktop release readiness audit added. Tauri metadata upgraded from scaffold values to `1.15.0-rc.1`, but public desktop release readiness remains false. | `scripts\windows\audit-desktop-release-readiness.ps1` reports runtime package ready=true, desktop shell ready=false, multi-device verified=false. Blocking: missing `frontendDist=../out`, build command does not emit static export, second-PC test pending. Report: `docs/DESKTOP_RELEASE_READINESS_AUDIT_2026_05_29.md` (wiki/520). |
| 2026-05-29 v13 | Desktop shell and Store metadata basics are now locally ready; the repo-local desktop release audit still fails on real second-PC multi-device evidence. | `audit-desktop-release-readiness.ps1` reports runtime package ready=true, desktop shell ready=true, Store metadata checks pass, multi-device verified=false. `/privacy` and `/support` build and verify locally; live `musu.pro` still lacks expected new content. `write-release-go-no-go.ps1` now aggregates local/external blockers. |
| 2026-05-29 v14 | Release infrastructure CI/deploy workflows repaired for the current Rust/Next repo shape. | `test.yml` no longer references deleted Python dirs; `e2e-musu-bee.yml` no longer references deleted `musu-port`; deploy and web checks use Node 22+ for `node:sqlite`; GitHub JavaScript actions are forced onto Node 24 runtime; Linux Rust CI installs Wayland/PipeWire/GBM native dependencies; legacy likely-required check names are preserved; `npm run test:e2e:ci` verifies `/privacy` and `/support` Store metadata content. Public release still waits on green remote Actions, live `musu.pro` deployment, support mailbox, second-PC evidence, and Microsoft review. |
| 2026-05-29 v15 | CI/deploy and live Store public metadata gates are now green. | Remote `Tests` passed on `ad5f752`; latest relevant `E2E Tests — musu-bee` and `Deploy musu-bee to Vercel` passed on `0919a83`; `verify-store-public-metadata.ps1 -BaseUrl https://musu.pro -Json` passed. `write-release-go-no-go.ps1` still reports public release No-Go only because real second-PC evidence and `musu@musu.pro` operator delivery verification are missing, plus Partner Center/Microsoft external gates. |
| 2026-05-29 v16 | Support mailbox gate is now evidence-backed instead of an assumption-only switch. | Added `verify-support-mailbox-evidence.ps1` and `record-support-mailbox-verification.ps1`; go/no-go now auto-detects valid support mailbox `*.evidence.json`. Temporary scanner tests passed and were deleted. Real `musu@musu.pro` evidence is still missing, so release remains No-Go with second-PC evidence still missing. |
| 2026-05-29 v17 | `yellowhama/musu-system` was rechecked under the latest integration request. Conclusion unchanged: integrate later as adjacent MCP/CLI/tooling, not as Rust-core merge or first Store bundle. | `musu-system` HEAD remains `d4e58e0`; split repo HEADs unchanged; `go test ./...` and `go vet ./...` passed in all four modules. Spot audit: `nurikun` keeps delivery ops out of MCP, but `watch` send-failure persistence should be fixed/wrapped before dashboard integration. |
| 2026-05-29 v18 | Fresh assistant-side single-machine smoke reran successfully and final operator gates were consolidated. | On commit `f9ae873`, `smoke-single-machine-beta.ps1` passed with dashboard task `b4b05b93-34d2-4946-b4cd-fdd5c5c6632d`, output `MUSU_RELEASE_SMOKE_OK_20260529_0652`, and CLI route `MUSU_CLI_ROUTE_OK_20260529_0652`. Remaining release blockers are documented in `docs/RELEASE_FINAL_OPERATOR_GATES_2026_05_29.md`. |
| 2026-05-29 v19 | Single-machine smoke is now evidence-backed in release audit. | `smoke-single-machine-beta.ps1` writes `musu.single_machine_smoke_evidence.v1`; added verifier/recorder scripts; committed evidence under `docs\evidence\single-machine\1.15.0-rc.1`. `audit-desktop-release-readiness.ps1` now reports `single_machine_verified=true`. Remaining No-Go blockers are second-PC multi-device evidence and `musu@musu.pro` delivery evidence. |
| 2026-05-29 v20 | `musu-system` integration report was refreshed against current GitHub/local state. | Fresh 07:17 KST recheck: `musu-system` HEAD `d4e58e0`, latest remote CI green, `go test ./...` and `go vet ./...` passed in `core`, `crawl-ai`, `marketer`, and `nurikun`. Decision unchanged: integrate later through MCP/CLI/bridge adapters; do not merge into `musu-rs` or first Store bundle. Added caveat that MCP registration needs explicit wiki/model path contracts. |
| 2026-05-29 v21 | Remaining manual release gates now have a single operator handoff packet generator, verifier, and completion runner. | Added `scripts\windows\prepare-final-operator-gate-packet.ps1`, `scripts\windows\verify-final-operator-gate-packet.ps1`, and `scripts\windows\complete-final-operator-gates.ps1`; they package/verify a fresh multi-device kit and then record returned multi-device + support evidence into final go/no-go. Latest verified packet is now tracked in v25. It does not close the gates; it reduces operator execution friction for the remaining external checks. |
| 2026-05-29 v22 | `musu-system` integration report was corrected against the current monorepo HEAD, not stale split-repo notes. | Fresh 07:52 KST recheck: HEAD `d4e58e0`, split repo HEADs unchanged, `go test ./...` and `go vet ./...` passed again in all four modules; current HEAD already has MCP schemas and marketer/nurikun DB parent creation. Remaining integration precondition is adapter-managed cwd/wiki/model/env context plus fixing/wrapping `nurikun watch` send-failure persistence before dashboard use. |
| 2026-05-29 v23 | Store release approval is now evidence-backed in final go/no-go instead of only listed as a manual external gate. | Added `scripts\windows\verify-store-release-evidence.ps1` and `scripts\windows\record-store-release-verification.ps1`; `write-release-go-no-go.ps1` now reports `store_release_verified` and blocks public desktop release until Partner Center submission, Microsoft certification, and restricted startup capability approval evidence is recorded. |
| 2026-05-29 v24 | `musu-system` was rechecked again after the latest integration request. | Fresh 08:14 KST recheck: GitHub visibility/HEADs/tags unchanged, latest `musu-system` branch CI green, and local `go test ./...` plus `go vet ./...` passed again in `core`, `crawl-ai`, `marketer`, and `nurikun`. Conclusion unchanged: integrate later as optional MCP/CLI/bridge ecosystem tooling; do not merge into `musu-rs` or first Store package. |
| 2026-05-29 v25 | Final operator gate packet was regenerated and hardened for the Store release evidence gate. | Packet alias `.local-build\final-operator-gates\musu-final-operator-gates-1.15.0-rc.1-latest.zip` verified with `ok=true`, `fail_count=0`, `kit_count=1` at this stage. The packet README/verifier began enforcing Store release approval evidence instructions; later MSIX-install hardening is tracked in v28. |
| 2026-05-29 v26 | Final completion runner now supports Store release evidence in the same command as multi-device/support evidence. | Added Store approval parameters to `complete-final-operator-gates.ps1`; packet README and verifier now require Store params in the final command. Smoke wrote Store evidence only to `.local-build\store-release-complete-smoke`, preserving the real No-Go state until actual Microsoft approval evidence is recorded. |
| 2026-05-29 v27 | Final release handoff status now has a one-screen evidence-non-recording script. | Added `scripts\windows\show-final-release-handoff-status.ps1`; final operator packets include it and packet verification requires the README status command. The script summarizes go/no-go flags, packet verification, evidence roots, and remaining operator commands without creating release evidence. Later v28 adds the clean/current MSIX install evidence gate to the same handoff path. |
| 2026-05-29 v28 | Second-PC MSIX install proof is now evidence-gated separately from multi-device routing. | Added `capture-msix-install-evidence.ps1`, `verify-msix-install-evidence.ps1`, and `record-msix-install-evidence.ps1`; `write-release-go-no-go.ps1` now reports `msix_install_verified` and blocks public release until clean/current Windows MSIX install evidence is recorded. Final packet/completion runner now carry `-MsixInstallEvidencePath`. |
| 2026-05-29 v29 | Release manifest writes are now resilient to concurrent local status checks. | A parallel `show-final-release-handoff-status.ps1` plus `write-release-go-no-go.ps1` run exposed a transient lock on `release-candidate-manifest.json`; `write-release-candidate-manifest.ps1` now writes manifest and checksum files through temp files with retry before replacement. |
| 2026-05-29 v30 | `musu-system` was rechecked again against current remote state after the latest integration request. | 09:19 KST recheck: monorepo HEAD remains `d4e58e0`, split repo HEADs/tags unchanged, latest monorepo CI/GHCR runs green, and local `go test ./...` plus `go vet ./...` passed in `core`, `crawl-ai`, `marketer`, and `nurikun` from `.local-build\external\musu-system`. Decision unchanged: integrate as optional MCP/CLI/bridge tooling, not Rust-core merge or first Store package. |
| 2026-05-29 v31 | Store release evidence now records Partner Center product-name reservation explicitly. | `record-store-release-verification.ps1` writes `product_name_reserved` and `product_name_reserved_at`; `verify-store-release-evidence.ps1` requires them before `store_release_verified=true`. Final handoff docs/status now state the Store gate as product-name reservation, app submission, Microsoft certification, and restricted capability approval. |
| 2026-05-29 v32 | Final Store evidence runner now requires the Partner Center product-name reservation timestamp. | `complete-final-operator-gates.ps1`, handoff status, and the final operator packet now expose `-StoreProductNameReservedAt`, and packet verification checks that the final command includes it. This prevents the final runner from relying on the recorder's fallback of using submission time as reservation time. |
| 2026-05-29 v33 | Go/no-go manual external gate list now includes support mailbox delivery verification. | `write-release-go-no-go.ps1` already blocked on missing `musu@musu.pro` evidence, but `manual_external_gates` omitted that operator action. It now lists `musu@musu.pro inbox delivery verification`, matching the blocker and final handoff operator steps. |
| 2026-05-29 v34 | Support mailbox release readiness can no longer be assumed by CLI flag. | Removed `-AssumeSupportMailboxVerified` from `write-release-go-no-go.ps1`; support readiness now requires recorded evidence from `record-support-mailbox-verification.ps1` / `verify-support-mailbox-evidence.ps1`. |
| 2026-05-29 v35 | Skipping public Store metadata verification no longer permits release readiness. | `write-release-go-no-go.ps1 -SkipPublicMetadata` now adds a `store-public-metadata` blocker instead of only a warning, keeping the option diagnostic-only for offline checks. |
| 2026-05-29 v36 | Final completion command now fails closed when blockers remain. | `show-final-release-handoff-status.ps1`, final operator packet README, and `RELEASE_FINAL_OPERATOR_GATES_2026_05_29.md` now include `-FailOnNotReady` for `complete-final-operator-gates.ps1`; packet verification fails if the final command omits it. |
| 2026-05-29 v37 | Dirty git state is now a release blocker. | `write-release-go-no-go.ps1` promotes dirty worktree status from warning to blocker, so public readiness cannot become true until changes are committed and the release manifest records `manifest_git.dirty=false`. |
| 2026-05-29 v38 | Final operator packet verification now catches stale dirty-git rules. | `verify-final-operator-gate-packet.ps1` checks the bundled `write-release-go-no-go.ps1` for the dirty-git blocker and checks the bundled verifier for the same assertion, so old packets with warning-only dirty-git handling fail verification. |
| 2026-05-29 v39 | Final operator packet generation is now clean-git only and source-attributed. | `prepare-final-operator-gate-packet.ps1` refuses dirty worktrees and writes `packet-build-metadata.json` with source branch, commit, and clean git state; packet verification requires valid clean source metadata. |
| 2026-05-29 v40 | Multi-device evidence is now schema/version/timestamp gated. | `smoke-multidevice-beta.ps1` writes `musu.multidevice_smoke_evidence.v1` with version, operator machine, and timestamps; `verify-multidevice-evidence.ps1` validates schema, expected version, `started_at`/`completed_at`, evidence age, and operator metadata. Release audit passes the repo `VERSION`, and final packet verification rejects stale multi-device verifiers missing these checks. |
| 2026-05-29 v41 | Manual support/Store evidence now fails closed on weak operator input. | Support mailbox evidence must match the current version, include an explicit `musu-...` token, use a sender distinct from `musu@musu.pro`, and pass timestamp order/future checks. Store evidence recording now requires explicit `-ProductNameReservedAt` and the verifier checks recording order/future timestamps. Final packet verification rejects stale support/Store evidence scripts without those guards. |
| 2026-05-29 v42 | MSIX install and multi-device evidence gates now reject underspecified operator evidence. | `verify-msix-install-evidence.ps1` defaults to the repo version, requires operator machine/user, non-future timestamps, installed/artifact version match, and required passing capture checks. `verify-multidevice-evidence.ps1` defaults to the repo version, requires operator user, and requires `remote_addr` to be `host:port`. Packet verification now rejects stale MSIX/multi-device verifiers missing these checks. |
| 2026-05-29 v43 | Single-machine smoke evidence is now tied to the current code commit. | `verify-single-machine-evidence.ps1` requires expected version/current commit and allows only documentation/evidence-only deltas; `smoke-single-machine-beta.ps1` uses timeout-safe process execution. Superseded evidence: `20260529-114932-HUGH_SECOND` on commit `a3ace02`, dashboard output `MUSU_RELEASE_SMOKE_OK_20260529_1149`, CLI route `MUSU_CLI_ROUTE_OK_20260529_1149`. |
| 2026-05-29 v44 | `musu-system` integration assessment was refreshed again against current remote state. | 11:55 KST recheck: public `musu-system` HEAD remains `d4e58e0`, split repo HEADs and active tags unchanged, and local `go test ./...` plus `go vet ./...` passed in `core`, `crawl-ai`, `marketer`, and `nurikun`. Decision remains optional MCP/CLI/bridge integration, not Rust-core merge or first Store package bundling. |
| 2026-05-29 v45 | A MUSU-specific Microsoft Store release run card was added from current source refresh. | `docs/MICROSOFT_STORE_RELEASE_RUN_CARD_2026_05_29.md` rejects other-product HiveLink/Vibe PM copy, keeps MUSU desktop as the Store product, preserves MSIX-first release strategy, and lists the exact remaining evidence sequence for second-PC MSIX install, multi-device, support mailbox, and Partner Center/Microsoft approval. |
| 2026-05-29 v46 | The final operator packet now includes and verifies the MUSU Store run card, and single-machine smoke was rerun against that code commit. | `prepare-final-operator-gate-packet.ps1` copies `MICROSOFT_STORE_RELEASE_RUN_CARD_2026_05_29.md`; `verify-final-operator-gate-packet.ps1` requires it. Current evidence: `20260529-121456-HUGH_SECOND` on commit `745d852`, dashboard output `MUSU_RELEASE_SMOKE_OK_20260529_1214`, CLI route `MUSU_CLI_ROUTE_OK_20260529_1214`. |
| 2026-05-29 v47 | The second-PC kit now writes a handoff JSON with candidate RemoteAddr values. | Added `collect-second-pc-handoff.ps1`; the multi-device kit and final packet include it, the packet verifier checks for it, and the docs now instruct the operator to return `.local-build\second-pc-handoff\*.handoff.json` plus MSIX/multi-device evidence. |
| 2026-05-29 v48 | `musu-system` was rechecked again for future integration value. | 12:41 KST recheck: canonical monorepo HEAD remains `d4e58e0`, split repo HEADs/tags unchanged, latest monorepo CI/GHCR successful, and local `go test ./...` plus `go vet ./...` passed in `core`, `crawl-ai`, `marketer`, and `nurikun`. Decision unchanged: high value, but adapter/MCP/CLI integration first; no Rust-core merge and no first Store package bundle. First candidate is `crawl-ai` knowledge/wiki ingestion. |
| 2026-05-29 v49 | PowerShell command capture was hardened and current single-machine evidence was refreshed. | `smoke-single-machine-beta.ps1` and `collect-second-pc-handoff.ps1` now avoid redirected-pipe `ReadToEnd()` hangs by running commands in a timeout-bounded job and reading stdout/stderr/exit code from temp files. Clean-commit smoke evidence: `20260529-125448-HUGH_SECOND` on commit `cc336b7`, dashboard output `MUSU_RELEASE_SMOKE_OK_20260529_1255`, CLI route `MUSU_CLI_ROUTE_OK_20260529_1255`. |
| 2026-05-29 v50 | Latest final handoff and remote CI were reverified after push. | Pushed HEAD `0057a48` passed GitHub Actions `Tests` run `26617175374`. Final operator packet `20260529-130100` verifies with `ok=true`, `fail_count=0`, `kit_count=1`, and clean source metadata for `0057a48`. `show-final-release-handoff-status.ps1 -Json` reports packet verified=true and `ready_for_public_desktop_release=false`; only the four external gates remain: second-PC MSIX install, real multi-device, support mailbox, and Store/Microsoft approval evidence. |
| 2026-05-29 v51 | Release support mailbox was corrected to the actual mailbox and centralized. | Replaced the incorrect hardcoded `support@musu.pro` with `musu@musu.pro`, added root `SUPPORT_EMAIL`, added `scripts\windows\release-config.ps1`, and added `musu-bee/src/lib/contact.ts`. Public `/privacy` and `/support`, Store metadata E2E, release gate scripts, final packet generation, and final packet verification now use the configured support mailbox. |
| 2026-05-29 v52 | Current single-machine evidence was refreshed after the support mailbox code change. | Current smoke evidence: `20260529-133756-HUGH_SECOND` on commit `ac98f6c`, dashboard output `MUSU_RELEASE_SMOKE_OK_20260529_1338`, CLI route `MUSU_CLI_ROUTE_OK_20260529_1338`, dashboard task `c52d5d28-4eb2-43c2-8bbc-aeeaac5428b5`, bridge `http://127.0.0.1:4652`. Live public metadata verifies with `musu@musu.pro`; public release still waits on second-PC MSIX install, real multi-device, `musu@musu.pro` inbox evidence, and Store/Microsoft approval evidence. |
| 2026-05-29 v53 | Added a packet-aware operator handoff card for the remaining external gates. | `show-operator-handoff-card.ps1` reads the latest final operator packet and prints the current support verification id, subject, second-PC kit name, second-PC commands, primary recording commands, and return-file list. The final packet generator/verifier now includes and requires `RELEASE_OPERATOR_HANDOFF_CARD_2026_05_29.md` and the handoff script, reducing stale packet-id risk for the user-run second-PC/support/Store gates. |
| 2026-05-29 v54 | Current single-machine evidence was refreshed after adding the operator handoff script. | Current smoke evidence: `20260529-135225-HUGH_SECOND` on commit `1597864`, dashboard output `MUSU_RELEASE_SMOKE_OK_20260529_1352`, CLI route `MUSU_CLI_ROUTE_OK_20260529_1352`, dashboard task `c43a853b-1dff-4017-9755-235fca427b5d`, bridge `http://127.0.0.1:4652`. |
| 2026-05-29 v55 | Final qualitative audit, product spec update, code audit, and next-step roadmap were consolidated. | Added wiki/521: `RELEASE_1_15_0_RC1_FINAL_QUAL_AUDIT_NEXT_STEPS_2026_05_29.md`. Current verdict: local single-machine beta and Store/operator-gate infrastructure are strong, no internal release-blocking issue found in scoped audit, but public desktop release remains No-Go until second-PC MSIX install, real multi-device, `musu@musu.pro` inbox, and Partner Center/Microsoft Store evidence are recorded. Final operator packet generator/verifier now include wiki/521. |
| 2026-05-29 v56 | Current single-machine evidence was refreshed after wiki/521 packet-script changes. | Current smoke evidence: `20260529-151324-HUGH_SECOND` on commit `5209a82`, dashboard output `MUSU_RELEASE_SMOKE_OK_20260529_1524`, CLI route `MUSU_CLI_ROUTE_OK_20260529_1524`, dashboard task `d1571cf6-fbf6-415b-adf5-1e88ca6a3266`, bridge `http://127.0.0.1:4652`. |
| 2026-05-29 v57 | Bridge doctor false-negative was fixed and single-machine evidence was refreshed again. | `musu doctor` bridge `/health` timeout changed from 3s to 10s after direct bridge health and dashboard doctor were ok but CLI doctor timed out on Windows loopback. Current smoke evidence: `20260529-163756-HUGH_SECOND` on commit `d1a8e28`, dashboard output `MUSU_RELEASE_SMOKE_OK_20260529_1640`, CLI route `MUSU_CLI_ROUTE_OK_20260529_1640`, dashboard task `4456cc33-bd26-4ae9-94dd-20245163296c`, bridge `http://127.0.0.1:8723`. |
| 2026-05-29 v58 | Second-PC returned handoff JSON now has a primary-side command card. | Added `scripts\windows\show-second-pc-return-card.ps1`; final packet and operator handoff card now include/verify it. It reads returned `.local-build\second-pc-handoff\*.handoff.json`, picks a `suggested_remote_addrs` candidate, and prints exact primary PC commands for MSIX install evidence recording, multi-device smoke, and multi-device evidence recording. |
| 2026-05-29 v59 | Smoke harness process capture was fixed and current single-machine evidence refreshed. | User noticed many Node processes; audit showed Codex plus the intentionally running Next dev server, while stale bridge processes came from failed harness attempts. `smoke-single-machine-beta.ps1` now uses readiness retries and `Start-Process` temp-file capture instead of PowerShell job/pipe capture that can hang when `musu up` spawns bridge. Current smoke evidence: `20260529-185958-HUGH_SECOND` on commit `242d75f`, dashboard output `MUSU_RELEASE_SMOKE_OK_20260529_185935`, CLI route `MUSU_CLI_ROUTE_OK_20260529_185935`, dashboard task `3cab5be8-1abf-40c0-91ad-3f5d2da33bcb`, bridge `http://127.0.0.1:9218`. |
| 2026-05-30 v60 | Remaining external actions now have a repeatable operator action pack generator/verifier. | Added `prepare-operator-action-pack.ps1` and `verify-operator-action-pack.ps1`. The pack is clean-git/source-attributed, verifies the final operator packet first, bundles second-PC transfer, Partner Center submission copy, and support-mailbox template, checks nested zips/checksums, and rejects private `.pfx` material. It reduces handoff error but does not close the four external evidence gates. |
| 2026-05-30 v61 | Current single-machine evidence was refreshed after operator action pack release infra. | Current smoke evidence: `20260530-053645-HUGH_SECOND` on commit `2b91d0a`, dashboard output `MUSU_RELEASE_SMOKE_OK_20260530_053611`, CLI route `MUSU_CLI_ROUTE_OK_20260530_053611`, dashboard task `d0f7f581-3c3e-49b2-a551-1f8881100aa8`, bridge `http://127.0.0.1:11971`. A first attempt hit dashboard status while Next was still compiling `/app`; after `musu up --json` reported bridge/dashboard `ok`, the rerun passed. |
| 2026-05-30 v62 | Final handoff status now reports operator action pack verification too. | `show-final-release-handoff-status.ps1` has `ActionPackPath` / `SkipActionPackVerification` inputs, reports `action_pack.exists` and `action_pack.verified`, and adds an operator step when the latest action pack is missing or invalid. Final packet generation now includes the action pack scripts and README commands; packet verification fails stale packets whose README/status script omit action-pack verification. |
| 2026-05-30 v63 | Current single-machine evidence was refreshed after handoff status action-pack verification. | Current smoke evidence: `20260530-060739-HUGH_SECOND` on commit `d7e745f`, dashboard output `MUSU_RELEASE_SMOKE_OK_20260530_060700`, CLI route `MUSU_CLI_ROUTE_OK_20260530_060700`, dashboard task `146d0165-5f4a-428e-9d74-f3ea0b05dbad`, bridge `http://127.0.0.1:12305`. |
| 2026-05-30 v64 | Added a one-command second-PC release check wrapper. | `run-second-pc-release-check.ps1` runs sideload readiness, install/verify, MSIX evidence capture, and second-PC handoff collection from the extracted kit. Multi-device kit generation includes it, packet verification requires it inside the kit, and operator/action-pack docs now prefer the wrapper before manual fallback. |
| 2026-05-30 v65 | Current single-machine evidence was refreshed after the second-PC wrapper. | Current smoke evidence: `20260530-085123-HUGH_SECOND` on commit `213b69e`, dashboard output `MUSU_RELEASE_SMOKE_OK_20260530_085059`, CLI route `MUSU_CLI_ROUTE_OK_20260530_085059`, dashboard task `14c9e9be-73b7-494f-9194-fd87d8f4b252`, bridge `http://127.0.0.1:12427`. |
| 2026-05-30 v66 | Beta release checklist current evidence pointer was corrected. | `BETA_RELEASE_CHECKLIST_1_15_0_RC1.md` now points its current machine-readable single-machine evidence section to `20260530-085123-HUGH_SECOND`; the older `20260529-185958-HUGH_SECOND` evidence remains historical harness evidence only. |
| 2026-05-30 v67 | `musu-system` integration decision was revalidated against current external state. | GitHub repo state, HEAD/main `d4e58e0`, active tags, and latest CI remain unchanged/green; local `go test ./...` plus `go vet ./...` passed again in `core`, `crawl-ai`, `marketer`, and `nurikun`. Decision unchanged: optional MCP/CLI/bridge integration later, no Rust-core merge and no first Store bundle. |
| 2026-05-30 v68 | Store submission bundle verification was added. | Added `verify-store-submission-bundle.ps1`, made `prepare-store-submission-bundle.ps1` write `SHA256SUMS.txt`, made desktop readiness audit verify the latest Store bundle, and made operator action pack generation refuse an invalid Store submission bundle. Latest verified bundle: `.local-build\msix\submission-bundles\store-reviewed-20260530-093140`. |
| 2026-05-30 v69 | Current single-machine evidence was refreshed after Store submission bundle verifier changes. | Current smoke evidence: `20260530-094254-HUGH_SECOND` on commit `45209e8`, dashboard output `MUSU_RELEASE_SMOKE_OK_20260530_094227`, CLI route `MUSU_CLI_ROUTE_OK_20260530_094227`, dashboard task `948fd44f-41b3-4bc8-9aa2-e05ea002d838`, bridge `http://127.0.0.1:12715`. |
| 2026-05-30 v70 | Second-PC return handoff now has a single archive path. | `run-second-pc-release-check.ps1` creates `.local-build\second-pc-return\*.zip` by default, and `show-second-pc-return-card.ps1 -ReturnZipPath ...` extracts it to locate the handoff/MSIX evidence and print primary-side commands. Packet/action-pack docs and verifiers require the return archive instructions. This reduces operator file-return risk; public release still waits on real second-PC, support inbox, and Store/Microsoft evidence. |
| 2026-05-30 v71 | Current single-machine evidence was refreshed after second-PC return archive flow. | Current smoke evidence: `20260530-100818-HUGH_SECOND` on commit `7294d65`, dashboard output `MUSU_RELEASE_SMOKE_OK_20260530_1008`, CLI route `MUSU_CLI_ROUTE_OK_20260530_1008`, dashboard task `695f1e1d-1d1b-46bd-8783-3eebb216842a`, bridge `http://127.0.0.1:2217`. First attempt failed before dashboard warm-up while Next compiled `/app`; rerun passed after dashboard warm-up. |
| 2026-05-30 v72 | Second-PC return ZIP importer was added. | `import-second-pc-return.ps1` imports `.local-build\second-pc-return\*.zip`, copies returned JSON to canonical `.local-build` roots, verifies MSIX install evidence, can record the MSIX install gate with `-RecordMsixInstall`, and emits primary-side multi-device commands. Final packet/action pack/status docs now prefer the importer over manual raw-file handling. |
| 2026-05-30 v73 | Current single-machine evidence was refreshed after the second-PC return importer commit. | Current smoke evidence: `20260530-224123-HUGH_SECOND` on commit `2eca040`, dashboard output `MUSU_RELEASE_SMOKE_OK_20260530_2240`, CLI route `MUSU_CLI_ROUTE_OK_20260530_2240`, dashboard task `dbb4d9dc-1c94-439c-89ae-98b43285f6b3`, bridge `http://127.0.0.1:3718`. |
| 2026-05-30 v74 | Current single-machine evidence was refreshed after documenting the second-PC return preview fallback. | Current smoke evidence: `20260530-225842-HUGH_SECOND` on commit `7c7d537`, dashboard output `MUSU_RELEASE_SMOKE_OK_20260530_2258`, CLI route `MUSU_CLI_ROUTE_OK_20260530_2258`, dashboard task `69d71cea-dec0-47af-a0bb-fdde166c080e`, bridge `http://127.0.0.1:3718`. |
| 2026-05-31 v75 | Current status audit was consolidated as wiki/522. | `docs/RELEASE_1_15_0_RC1_CURRENT_STATUS_AUDIT_2026_05_31.md` is now the current qualitative report, product spec lock, code audit, and next-step roadmap. Final operator packet generation and verification now include wiki/522 so handoff packets do not point only at the older wiki/521 report. Public desktop release remains No-Go until real second-PC MSIX install, real multi-device route, `musu@musu.pro` inbox delivery, and Partner Center/Microsoft Store approval evidence are recorded. |
| 2026-05-31 v76 | Windows/Tailscale mDNS health hang was mitigated. | During smoke revalidation, a logged-in default `~/.musu` with a failing Tailscale mDNS interface caused bridge `/health` to time out after the initial `musu up` probe. mDNS LAN auto-discovery is now opt-in via `MUSU_ENABLE_MDNS=1`; Store-candidate smoke/release runs use cloud/manual peer registration and second-PC handoff until mDNS has separate regression evidence. |
| 2026-05-31 v77 | Current single-machine evidence was refreshed after mDNS opt-in hardening. | Current smoke evidence: `20260531-154114-HUGH_SECOND` on commit `dbd90e3`, dashboard output `MUSU_RELEASE_SMOKE_OK_20260531_1540`, CLI route `MUSU_CLI_ROUTE_OK_20260531_1540`, dashboard task `9e00f24e-1bbc-4cbf-b6aa-7057fdfeb63c`, bridge `http://127.0.0.1:1407`. |
| 2026-05-31 v78 | Runtime hardening and relay-control became P0 release gates. | Second-PC MSIX install evidence was imported and recorded at `docs\evidence\msix-install\1.15.0-rc.1\20260531-165211-HUGH-MAIN.evidence.json`, closing the package-install proof but not the route proof. Operator-observed idle CPU busy-loop behavior on more than one PC changed the release decision from "externally blocked only" to "internally and externally blocked." Added wiki/523 runtime hardening/relay roadmap, `measure-musu-idle-cpu.ps1`, made clipboard polling opt-in via `MUSU_ENABLE_CLIPBOARD_SYNC=1`, and added `runtime_idle_cpu_verified` to go/no-go; idle CPU evidence must include a running MUSU runtime process and count Node.js/WebView2 in the CPU budget. |
| 2026-05-31 v79 | Runtime idle budget and P2P control-plane spec were tightened. | `measure-musu-idle-cpu.ps1` now separates MUSU, Node.js, and WebView2 roles, fails if no MUSU runtime process is sampled, and reports role counts/max CPU. The operator idle CPU command now includes `-IncludeNode -IncludeWebView2`. Frontend health/process/device/fleet polling was slowed and paused while hidden. The bridge `musu.pro` registration loop now defaults to `MUSU_CLOUD_HEARTBEAT_INTERVAL_SEC=300` with a 60s floor, backoff, and jitter. Added wiki/524 `MUSU_PRO_P2P_CONTROL_PLANE_SPEC_2026_05_31.md` to lock registry/rendezvous/path-selection/relay route evidence. |
| 2026-05-31 v80 | The three P0 blockers now have an execution plan and stricter route evidence gate. | Added wiki/525 `MUSU_RUNTIME_STABILIZATION_EXECUTION_PLAN_2026_05_31.md` to answer how idle CPU, `musu.pro` P2P assistance, and desktop hardening will be handled. `smoke-multidevice-beta.ps1` now emits `musu.route_evidence.v1`; `verify-multidevice-evidence.ps1` rejects passing evidence without route kind, handshake timing, peer identity verification, hardened encryption, payload transit truth, and success result. Rust cloud DTOs/client methods now cover rendezvous sessions, candidate endpoints, and route evidence. Legacy manual HTTP bearer routing is recorded honestly as unverified and cannot satisfy the final multi-device release gate. |
| 2026-05-31 v81 | Current single-machine evidence was refreshed after route-evidence gate hardening. | Current smoke evidence: `20260531-192015-HUGH_SECOND` on commit `5211ff2`, dashboard output `MUSU_RELEASE_SMOKE_OK`, CLI route `MUSU_CLI_ROUTE_OK`, dashboard task `986733bf-c10c-4434-bd8e-d9b0512ce193`, bridge `http://127.0.0.1:9818`. A first attempt failed because the dashboard dev server was not running; after starting Next on `127.0.0.1:3000`, `musu up --json` reported dashboard `ok` and the rerun passed. |
| 2026-05-31 v82 | Runtime CPU measurement was hardened against false WebView2/Node attribution. | `measure-musu-idle-cpu.ps1` now uses native Windows parent-process lookup instead of WMI/CIM, records helper process scope, and includes Node/WebView2 only when they are MUSU descendants or repo-related helpers by default; `-IncludeUnrelatedHelpers` remains available for whole-machine diagnostics. `write-release-go-no-go.ps1` now rejects runtime CPU evidence that omits Node/WebView2 budget flags, lacks a valid helper scope, times out process metadata, or cannot prove ownership in the default helper scope. Dashboard, NodePanel, and agents-surface polling moved from fixed intervals to non-overlapping 30s visible / 120s hidden recursive timeouts. A primary debug-runtime 60s diagnostic sample passed at `.local-build\runtime-idle-cpu\musu-idle-cpu-20260531-194854.json`, but the public release gate still needs packaged desktop/WebView2 samples on two PCs. |
| 2026-05-31 v83 | Current single-machine evidence was refreshed after idle CPU ownership hardening. | Current smoke evidence: `20260531-195832-HUGH_SECOND` on commit `3b1b1b0`, dashboard output `MUSU_RELEASE_SMOKE_OK`, CLI route `MUSU_CLI_ROUTE_OK`, dashboard task `d568c5f1-d15d-4cbf-8172-c4a308deaf95`, bridge `http://127.0.0.1:9818`. A first attempt failed while the dashboard dev server was still compiling; after `musu up --json` reported dashboard `ok`, the rerun passed and evidence was recorded. |
| 2026-05-31 v84 | Process ownership audit became a release gate. | Added `scripts\windows\audit-musu-process-ownership.ps1` and wired `process_ownership_verified` into go/no-go, final handoff status, and final operator packet verification. Local audit passed on `HUGH_SECOND` with one MUSU runtime, zero MUSU-owned Node helpers, zero MUSU-owned WebView2 helpers, zero repo-related orphan helpers, bridge registry PID alive, and `/health` HTTP 200; the 13 machine-wide WebView2 processes observed on the operator machine were not MUSU-owned. |
| 2026-05-31 v85 | Current single-machine evidence was refreshed after the process ownership release gate. | Current smoke evidence: `20260531-202354-HUGH_SECOND` on commit `9223404`, dashboard output `MUSU_RELEASE_SMOKE_OK`, CLI route `MUSU_CLI_ROUTE_OK`, dashboard task `89e6ab33-24de-4326-97f2-ebc2f6588209`, bridge `http://127.0.0.1:9818`. This keeps `single_machine_verified=true` after the process ownership gate code commit. |
| 2026-05-31 v86 | Startup single-instance audit became a release gate. | Added `scripts\windows\audit-musu-startup-single-instance.ps1` and wired `startup_single_instance_verified` into go/no-go, final handoff status, and final operator packet verification. Local evidence `20260531-203635-HUGH_SECOND` passed with three repeated `musu up --json` calls reusing bridge PID 31208, `after_musu_runtime=1`, `repeated_spawn_count=0`, and nested process ownership passing. |
| 2026-05-31 v87 | Current single-machine evidence was refreshed after the startup single-instance release gate. | Current smoke evidence: `20260531-205509-HUGH_SECOND` on commit `df49459`, dashboard output `MUSU_RELEASE_SMOKE_OK`, CLI route `MUSU_CLI_ROUTE_OK`, dashboard task `5b75f3cc-b140-43e9-8882-9e8822075128`, bridge `http://127.0.0.1:9818`. A first attempt failed while Next warmed `/app`; direct `musu up --json` then reported dashboard `ok` and the rerun passed. |
| 2026-05-31 v88 | Runtime idle CPU evidence became a resource-budget and desktop-open gate. | `measure-musu-idle-cpu.ps1` now records scenario, clean git state, process count budget, owned WebView2 budget, working set/private memory, and memory totals by role. `write-release-go-no-go.ps1` now requires `desktop-open` plus `-RequireOwnedWebView2` for final runtime evidence. Current bridge-only diagnostic `20260531-211448-HUGH_SECOND.bridge-only` passed with max one-core CPU `0.03%` and working set `27.7MB`; desktop-open attempt `20260531-211608-HUGH_SECOND.desktop-open-attempt` failed because owned WebView2 count was `0`. |
| 2026-05-31 v89 | Current single-machine evidence was refreshed after the runtime resource-budget gate fix. | Current smoke evidence: `20260531-212651-HUGH_SECOND` on commit `a89f6dd`, dashboard output `MUSU_RELEASE_SMOKE_OK`, CLI route `MUSU_CLI_ROUTE_OK`, dashboard task `0cdd096c-626d-46ee-95e4-c347805016d8`, bridge `http://127.0.0.1:9818`. A first attempt failed while Next warmed `/app`; direct `musu up --json` then reported dashboard `ok` and the rerun passed. |
| 2026-05-31 v90 | MSIX desktop entrypoint mismatch became a release blocker. | Added `scripts\windows\audit-msix-desktop-entrypoint.ps1` and wiki/526. Evidence `20260531-214327-HUGH_SECOND.store-msix-runtime-only` proves the current local-sideload/Store MSIX artifacts and installed package launch `musu.exe`, do not contain `musu-desktop.exe`, and still describe a CLI/bridge runtime package. `write-release-go-no-go.ps1` now reports `msix_desktop_entrypoint_verified=false`, `audit-desktop-release-readiness.ps1` reports `runtime_package_ready=false`, and `verify-store-submission-bundle.ps1` rejects the current Store bundle until the Tauri desktop shell is packaged as the MSIX application executable. |
| 2026-05-31 v91 | Idle polling and writer admission wakeups were reduced. | Added `musu-bee/src/lib/useLowDutyPolling.ts` and moved major dashboard/panel/inbox/fleet/company/machine polling to no-overlap, abort-on-unmount, hidden-tab pause, and capped failure backoff. Fleet/company/machine pages now use 30s safety-net polling plus SSE wakeups instead of 5s fixed refreshes. `musu-rs/src/writer/runner.rs` now uses `tokio::sync::Notify` for admission wakeups instead of waking queued tasks every 50ms. |
| 2026-05-31 v92 | Store MSIX artifact now packages the desktop entrypoint. | `build-msix.ps1` now stages `musu-desktop.exe` as the MSIX application executable, keeps `musu.exe` as the CLI alias, keeps `musu-startup.exe` as the startup task, and writes a desktop-shell description. Artifact evidence `20260531-224328-HUGH_SECOND.store-msix-desktop-artifact` passes, and regenerated Store bundle `.local-build\msix\submission-bundles\store-reviewed-20260531-224352` verifies with `ok=true`, `fail_count=0`. Installed evidence still fails until the fixed package is installed; source-fresh release build on HUGH_SECOND hit rustc OOM/pagefile pressure even with `CARGO_BUILD_JOBS=1`. |
| 2026-05-31 v93 | MSIX install evidence split and desktop process ownership were hardened. | Fixed local-sideload MSIX install now passes desktop-entrypoint audit (`20260531-232229-HUGH_SECOND.local-sideload-installed`), while Store-reviewed restricted-capability local install intentionally fails contract-mismatch audit (`20260531-232229-HUGH_SECOND.store-reviewed-contract-mismatch`). `install-msix.ps1` refuses Store-reviewed restricted-capability sideload by default; `write-release-go-no-go.ps1` now requires Store-reviewed artifact proof plus local-sideload installed proof for `msix_desktop_entrypoint_verified`. Process/CPU audits include `musu-desktop` as a root so packaged WebView2 children are attributed; local process ownership passed with one runtime, one desktop shell, six owned WebView2 helpers, and zero owned Node helpers. Clean primary desktop-open CPU diagnostic passed at `.local-build\runtime-idle-cpu\musu-idle-cpu-20260531-235500.json` with max one-core CPU `musu=0.03`, `webview2=0.03`, and working set `363.45MB`; second-PC desktop CPU evidence is still pending. `musu up` now terminates live-but-unhealthy registered bridge PIDs before restart only if the PID resolves to a MUSU runtime executable; `cargo check -j 1` passes, while local rustc builds still hit OOM/pagefile pressure. |
| 2026-06-01 v94 | Brand/public assets and current release audit were refreshed. | The app favicon/mark is the Tauri/MSIX icon under `musu-bee/src-tauri/icons/`; `musu-bee/public/images/favicon-header.png` now tracks that mark for web headers. `musu-bee/.gitignore` no longer drops required public runtime assets, and `/agents/*.png` plus the favicon are now tracked for clean checkout/CI builds. `MusuLogo` no longer points at missing `/images/logos/*` lockups; it renders the tracked mark plus a token-colored MUSU wordmark. Added wiki/527 qualitative audit and updated wiki/524 with 2026-06-01 implementation status. `npm run typecheck` passed. A current single-machine smoke attempt on commit `5e8d195` failed because dashboard task-status polling timed out after the local Next dev server became unreachable and PowerShell log-tail commands hit OOM/CLR errors; no new release evidence was recorded. Public release remains No-Go until current smoke, two-machine desktop CPU, real route evidence, `musu@musu.pro` inbox delivery, and Store evidence are recorded. |
| 2026-06-01 v95 | Single-machine smoke and mDNS/Tailscale IPv6 hardening were tightened. | `smoke-single-machine-beta.ps1` now generates unique dashboard/CLI expected strings per run to avoid duplicate-task 409s, retries dashboard task-status polling inside the task deadline, and records poll error count/last error in evidence. The supplied `mdns_sd::service_daemon` errors (`Tailscale` IPv6 link-local, `os error 10065`, then `closed channel`) are now treated as a real idle/log-spam risk: mDNS remains opt-in via `MUSU_ENABLE_MDNS=1`, and IPv6 mDNS is separately opt-in via `MUSU_MDNS_ENABLE_IPV6=1`. Default mDNS daemon setup disables IPv6 interfaces, so `musu discover` no longer sends to Tailscale IPv6 by default. `cargo check -j 1` and `cargo build --bin musu -j 1` passed locally; `musu discover --timeout 2` completed without the Tailscale IPv6 send errors. |
| 2026-06-01 v96 | Current single-machine evidence was refreshed after smoke and mDNS IPv6 hardening. | Current smoke evidence: `20260601-012801-HUGH_SECOND` on commit `d4820173dab1f19abf0ac287abbd073330f6eb1b`, dashboard output `MUSU_RELEASE_SMOKE_OK_20260601_012735`, CLI route `MUSU_CLI_ROUTE_OK_20260601_012735`, dashboard task `fe857b79-47af-47d8-abf0-80bcbb63d883`, bridge `http://127.0.0.1:10474`, `dashboard_task_poll_error_count=0`. Public release remains No-Go until two-machine desktop-open CPU evidence, hardened multi-device route evidence, `musu@musu.pro` inbox delivery evidence, and Store submission/release evidence are recorded. |
| 2026-06-01 v97 | Runtime CPU evidence freshness was hardened and primary desktop-open evidence was recorded. | `write-release-go-no-go.ps1` now rejects runtime idle CPU evidence whose `git_commit` is older than current HEAD unless the delta is docs/evidence-only, and final packet verification checks for that guard. This invalidated old `b246db7` CPU samples as intended. Initial primary evidence `20260601-014656-HUGH_SECOND.desktop-open` passed on commit `2987f15` but was later superseded by a current HEAD sample after script/logo changes. Runtime CPU gate still needs second-PC desktop-open evidence. |
| 2026-06-01 v98 | Current single-machine evidence was refreshed after runtime CPU gate hardening. | Current smoke evidence: `20260601-015424-HUGH_SECOND` on commit `e4509f6628fb2e54ef0c127c412722a3e52cf80f`, dashboard output `MUSU_RELEASE_SMOKE_OK_20260601_015402`, CLI route `MUSU_CLI_ROUTE_OK_20260601_015402`, dashboard task `bc27c955-bd1e-41e1-8ff6-c60a38328955`, bridge `http://127.0.0.1:5076`, `dashboard_task_poll_error_count=0`. |
| 2026-06-01 v99 | Second-PC return flow now carries runtime CPU evidence. | `run-second-pc-release-check.ps1` now captures the release-grade `desktop-open` runtime CPU sample by default after MSIX install and handoff: it opens MUSU Desktop, runs `measure-musu-idle-cpu.ps1` for 60s with owned WebView2/Node/WebView2 budgets, records `.local-build\runtime-idle-cpu\*.evidence.json`, and includes it in the second-PC return zip. `prepare-multidevice-test-kit.ps1`, final packet/action-pack verifiers, and the second-PC runbook now require/explain the CPU script. `import-second-pc-return.ps1` now resolves returned evidence by JSON schema and copies runtime CPU evidence to `.local-build\runtime-idle-cpu\` without confusing it with MSIX install evidence. |
| 2026-06-01 v100 | Favicon mark was promoted into real public logo assets. | The strong app/favicon mark remains the source of truth at `musu-bee\src-tauri\icons\icon.png` and `musu-bee\public\images\favicon-header.png`. Added `scripts\windows\generate-brand-logo-assets.ps1`, unignored `musu-bee\public\images\logos\`, and generated `musu-mark-512.png` plus header/display/hero transparent PNG lockups for `on-light`, `on-dark`, and `on-yellow`. These are now usable for Store, README, landing page, and launch handoff materials; screenshots/demo media remain separate release assets. |
| 2026-06-01 v101 | Single-machine evidence was refreshed after second-PC CPU-return and logo assets. | At that point, smoke evidence `20260601-022021-HUGH_SECOND` on commit `47f261475abf7a88deb7632a27c242a97b603771` passed with dashboard output `MUSU_RELEASE_SMOKE_OK_20260601_021958`, CLI route `MUSU_CLI_ROUTE_OK_20260601_021958`, dashboard task `04c4cae9-4b5c-4db3-bfb5-92e5429d8768`, bridge `http://127.0.0.1:1923`, and `dashboard_task_poll_error_count=0`. First rerun failed because the Next dev server was not running; after starting `next dev` on `127.0.0.1:3000`, `musu up --json` reported dashboard `ok` and the rerun passed. |
| 2026-06-01 v102 | Primary runtime idle CPU evidence was refreshed after the code/script commit. | Superseded primary packaged desktop-open evidence `docs\evidence\runtime-idle-cpu\1.15.0-rc.1\20260601-022649-HUGH_SECOND.desktop-open.evidence.json` on commit `ee8b0862accca12f3cfa93a2129605e7eae0596a` passed with packaged `musu-desktop.exe`, owned WebView2 `6`, owned Node `0`, max one-core CPU `musu=0`, `webview2=0.18`, total working set `340.21MB`, and `git_dirty=false`. It was later superseded by v105 after route/relay diagnostics. |
| 2026-06-01 v103 | Route/relay diagnostic CLI surfaces were added. | `musu route --explain [--json]` emits `musu.route_explain.v1` without executing a task, showing selected candidate/source, route-kind classification (`lan`, `tailscale`, `direct_quic`, or `local`), current `http_bearer` transport, and why the route cannot satisfy release evidence yet. `musu relay status [--json]` emits `musu.relay_status.v1`, showing login/cache/client readiness while explicitly reporting `bridge_path_selection_wired=false`, `relay_transport_wired=false`, and `release_route_evidence_ready=false`. `cargo check --manifest-path .\musu-rs\Cargo.toml -j 1` passed. Targeted `cli_commands` unit tests passed for both `--lib` and `--bin musu`; an earlier broad filtered `cargo test cli_commands` hit unrelated Windows elevation error 740 when Cargo tried to execute the filtered `r6_auto_update` integration test binary. |
| 2026-06-01 v104 | Current single-machine evidence was refreshed after route/relay diagnostics. | Current smoke evidence: `docs\evidence\single-machine\1.15.0-rc.1\20260601-030639-HUGH_SECOND.evidence.json` on commit `12f8cabaefb5722eb45941012d47848d95c9f895`, dashboard output `MUSU_RELEASE_SMOKE_OK_20260601_030619`, CLI route `MUSU_CLI_ROUTE_OK_20260601_030619`, dashboard task `42449837-4936-4e79-82a6-d7bbeede6108`, bridge `http://127.0.0.1:14130`, and `dashboard_task_poll_error_count=0`. First attempt failed while Next warmed `/app`; after `/app` and `/api/doctor` returned 200, rerun passed. |
| 2026-06-01 v105 | Primary runtime idle CPU evidence was refreshed after route/relay diagnostics. | Current primary packaged desktop-open evidence: `docs\evidence\runtime-idle-cpu\1.15.0-rc.1\20260601-030838-HUGH_SECOND.desktop-open.evidence.json` on commit `e0eebac44184e285ba120df5b4e0e9b209e83692`, with `git_dirty=false`, packaged `musu-desktop.exe`, owned WebView2 `6`, owned Node `0`, max one-core CPU `musu=0`, `webview2=0.21`, total working set `371.96MB`, total private memory `191.95MB`, and no resource-budget violations. Go/no-go still blocks release until second-PC desktop-open CPU evidence and hardened multi-device route evidence arrive. |
| 2026-06-01 v106 | Final go/no-go after route diagnostics remains an intentional No-Go. | `write-release-go-no-go.ps1 -Json` from clean HEAD `0c09f0c` reported `ready_for_public_desktop_release=false`, `local_artifacts_ready=true`, `single_machine_verified=true`, `msix_install_verified=true`, `msix_desktop_entrypoint_verified=true`, `process_ownership_verified=true`, `startup_single_instance_verified=true`, `public_metadata_ok=true`, `manifest_dirty=false`, and runtime CPU valid machine count `1`. Remaining blockers are exactly: real second-PC multi-device evidence, runtime idle CPU evidence on at least two machines, `musu@musu.pro` delivery verification, and Partner Center/Store release evidence. |
| 2026-06-01 v107 | Bridge direct path selection was wired into the shared router. | `musu-rs/src/bridge/router.rs` now classifies endpoints as `local`, `lan`, `tailscale`, or `direct_quic`, sorts candidates by LAN -> Tailscale -> direct public endpoint, preserves circuit-breaker filtering, and uses the shared selector for explicit target, GPU, and OS-hint routing. `musu route --explain --json` and `musu relay status --json` now report `bridge_path_selection_wired=true` while still reporting `rendezvous_session_wired=false`, `relay_transport_wired=false`, and `release_route_evidence_ready=false`. Validation passed: `cargo check --manifest-path .\musu-rs\Cargo.toml -j 1`, targeted `router` and `cli_commands` unit tests, `cargo build --manifest-path .\musu-rs\Cargo.toml --bin musu -j 1`, `git diff --check`, and both diagnostic CLI JSON commands. |
| 2026-06-01 v108 | Current single-machine evidence was refreshed after bridge path selection. | Current smoke evidence: `docs\evidence\single-machine\1.15.0-rc.1\20260601-033633-HUGH_SECOND.evidence.json` on commit `efff90cc9df3419f0cadc0398e5dd63d10f235de`, dashboard output `MUSU_RELEASE_SMOKE_OK_20260601_033608`, CLI route `MUSU_CLI_ROUTE_OK_20260601_033608`, dashboard task `759429bc-001e-4a05-9a68-88a4c44ad3c1`, bridge `http://127.0.0.1:13899`, and `dashboard_task_poll_error_count=0`. |
| 2026-06-01 v109 | Primary runtime idle CPU evidence was refreshed after bridge path selection. | Current primary packaged desktop-open evidence: `docs\evidence\runtime-idle-cpu\1.15.0-rc.1\20260601-033734-HUGH_SECOND.desktop-open.evidence.json` on commit `1163396a26cc81ce045f6de44f35fe0029074522`, with `git_dirty=false`, packaged `musu-desktop.exe`, owned WebView2 `6`, owned Node `0`, max one-core CPU `musu=0`, `webview2=0.16`, total working set `376.6MB`, total private memory `193.27MB`, and no resource-budget violations. Release still needs second-PC desktop-open CPU evidence. |
| 2026-06-01 v110 | CLI route attempts now write actual route evidence files. | `musu route --route-evidence-path <path>` writes `musu.route_evidence.v1` from the real CLI route attempt with route kind, candidate address, submit/handshake timing, total attempt timing, success/failure result, and the current legacy transport gap (`peer_identity_verified=false`, `encryption=none_http_bearer`). `smoke-multidevice-beta.ps1` now imports that CLI-written evidence instead of synthesizing route evidence from script-side inference. Validation passed: `cargo check --manifest-path .\musu-rs\Cargo.toml -j 1`, `cargo test --manifest-path .\musu-rs\Cargo.toml --lib cli_commands -- --nocapture`, `cargo test --manifest-path .\musu-rs\Cargo.toml --bin musu cli_commands -- --nocapture`, `cargo test --manifest-path .\musu-rs\Cargo.toml --lib router -- --nocapture`, PowerShell parser validation for `smoke-multidevice-beta.ps1`, and `git diff --check`. |
| 2026-06-01 v111 | Current single-machine evidence was refreshed after the CLI route evidence writer. | Current smoke evidence: `docs\evidence\single-machine\1.15.0-rc.1\20260601-040308-HUGH_SECOND.evidence.json` on commit `e8ac5a88c1dd9437c965f2d2e2f2c3b331596c2a`, dashboard output `MUSU_RELEASE_SMOKE_OK_20260601_040245`, CLI route `MUSU_CLI_ROUTE_OK_20260601_040245`, dashboard task `612c8aff-616e-4227-89dc-7023a77d4830`, bridge `http://127.0.0.1:13800`, and `dashboard_task_poll_error_count=0`. |
| 2026-06-01 v112 | Primary runtime idle CPU evidence was refreshed after the CLI route evidence writer. | Current primary packaged desktop-open evidence: `docs\evidence\runtime-idle-cpu\1.15.0-rc.1\20260601-040413-HUGH_SECOND.desktop-open.evidence.json` on commit `b330de871364877f990cd0cc4d5a6e6f8666a53f`, with `git_dirty=false`, packaged `musu-desktop.exe`, owned WebView2 `6`, owned Node `0`, max one-core CPU `musu=0`, `webview2=0.21`, total working set `369.66MB`, total private memory `188.25MB`, and no resource-budget violations. Release still needs second-PC desktop-open CPU evidence. |
| 2026-06-01 v113 | Runtime route evidence and mDNS/Tailscale hardening were extended. | `musu-rs/src/bridge/route_evidence.rs` now owns the shared `musu.route_evidence.v1` builder/writer used by CLI and runtime paths. `/api/tasks/delegate`, `/api/companies/{id}/run`, and workflow remote steps write local `~/.musu/route-evidence/<task_id>.route-evidence.json` files with real forwarding timing, retry result, and failure class. mDNS now default-disables Tailscale interfaces unless `MUSU_MDNS_ENABLE_TAILSCALE=1`, in addition to the existing `MUSU_ENABLE_MDNS` and `MUSU_MDNS_ENABLE_IPV6` gates. Added wiki/528 current audit. Validation passed: `cargo check --manifest-path .\musu-rs\Cargo.toml -j 1`, `cargo test --manifest-path .\musu-rs\Cargo.toml -j 1 --lib cli_commands -- --nocapture`, targeted `rustfmt --check`, and `git diff --check`; filtered `route_evidence` test execution was stopped after excessive MSVC test-binary compile time on this machine. |
| 2026-06-01 v114 | Route evidence now has a first `musu.pro` receiver stub and runtime submit path. | Added authenticated `POST /api/v1/p2p/route-evidence` in `musu-bee`, validating `musu.route_evidence.v1` and returning `release_grade` plus `blockers` while reporting `stored=false` until durable storage lands. Runtime bridge forwarding now converts local evidence into the cloud DTO and starts a background best-effort submit after local write when `~/.musu/token` exists, so cloud latency does not delay the user task. `MUSU_CLOUD_BASE_URL` now controls cloud client base URL for login/registry/evidence tests. This does not close the release gate: current transport still records `peer_identity_verified=false` and `encryption=none_http_bearer`; rendezvous, identity proof, QUIC/TLS, durable evidence storage, two-machine CPU evidence, `musu@musu.pro` inbox evidence, and Store evidence remain open. Validation passed: Next route tests, `npm run typecheck`, `cargo check --manifest-path .\musu-rs\Cargo.toml -j 1`, `cargo test --manifest-path .\musu-rs\Cargo.toml -j 1 --lib route_evidence -- --nocapture`, targeted rustfmt on changed Rust files, and `cargo fmt --manifest-path .\musu-rs\Cargo.toml --check`. |
| 2026-06-01 v115 | Route evidence is now stored and queryable on the control plane. | `POST /api/v1/p2p/route-evidence` now stores valid authenticated evidence and returns `stored=true` plus `evidence_id`; `GET /api/v1/p2p/route-evidence` returns stored records with filters for source, target, route kind, result, and release-grade status. Hosted storage uses Vercel KV/Upstash Redis as a capped list; local/dev uses `MUSU_ROUTE_EVIDENCE_STORE_PATH`; production fails closed without KV unless an explicit persistent file path is set. This improves auditability but still does not close public release because rendezvous, peer identity, QUIC/TLS proof, account-scoped evidence ownership/export, two-machine CPU evidence, `musu@musu.pro` inbox evidence, and Store evidence remain open. Validation passed: `npx tsx --test src/app/api/v1/p2p/route-evidence/route.test.ts`, `npm run typecheck`, `git diff --check`, and indexer sync `1063 files / 1997 symbols`. |
| 2026-06-01 v116 | The first `musu.pro` rendezvous session API is implemented. | Added authenticated short-lived rendezvous endpoints in `musu-bee`: `POST /api/v1/p2p/rendezvous`, `GET /api/v1/p2p/rendezvous/:id`, `POST /:id/candidates`, `POST /:id/approve`, and `POST /:id/close`. Sessions store source/target candidate sets, relay capability, public key, capabilities, approval state, and close state using Vercel KV or explicit local/dev file fallback. This moves the control plane from route-evidence-only to candidate exchange, but public release is still blocked because bridge runtime route attempts do not yet create/use rendezvous sessions and transport identity/encryption proof is still missing. Validation passed: rendezvous + route-evidence API tests, `npm run typecheck`, `git diff --check`, and indexer sync `1072 files / 2034 symbols`. |
| 2026-06-01 v117 | Bridge runtime route attempts now use `musu.pro` rendezvous sessions. | Added `musu-rs/src/bridge/rendezvous.rs` and wired remote forwarding through the short-lived control-plane lifecycle: source creates/refreshes a rendezvous session, publishes its advertised bridge endpoint, attaches `rendezvous_session_id` to the forwarded task, target publishes local candidates on receipt, source closes the session after terminal success/failure, and route evidence records the session id when present. Added `rendezvous_target_node_id` so target candidate publish can match sessions even when source selected a manual addr fallback, and added `MUSU_P2P_RENDEZVOUS_CLIENT_TIMEOUT_MS` with bounded fail-open behavior to the selected peer path. `musu relay status --json` and `musu route --explain --json` now report `rendezvous_session_wired=true`. Public release remains blocked because transport is still legacy HTTP bearer, peer identity/QUIC-TLS proof and relay fallback are absent, and real second-PC two-sided rendezvous evidence is missing. Validation passed: `cargo check --manifest-path .\musu-rs\Cargo.toml -j 1`, targeted `rendezvous`, `route_evidence`, and `cli_commands` tests for lib/bin, `cargo build --manifest-path .\musu-rs\Cargo.toml --bin musu -j 1`, both diagnostic CLI JSON commands, `git diff --check`, `cargo fmt --check`, and indexer sync `1074 files / 2050 symbols`. |
| 2026-06-01 v118 | Rendezvous target candidates can affect actual forwarding. | `musu-rs/src/bridge/router.rs` now exposes `select_best_remote_candidate` for already-authorized concrete endpoints, and `musu-rs/src/bridge/rendezvous.rs` maps refreshed session target candidates into `ResolvedPeer`s, ignores relay/failed candidates until relay transport exists, and chooses LAN -> Tailscale -> direct public endpoint. `forward_to_peer_with_retry` now forwards to that route peer when present, falls back once to the original selected peer if the rendezvous candidate is stale/failing, and returns the actual route peer in success/failure reports, so circuit breaker updates and route evidence use the actual candidate address. `musu-bee/src/lib/p2pRendezvousStore.ts` now caches recent node candidate sets from `/candidates` updates and seeds new rendezvous sessions from that cache when available, so target candidates can affect a later route attempt instead of relying only on same-session timing. Public release remains blocked by legacy HTTP bearer transport, missing peer identity/QUIC-TLS proof, missing relay fallback, and missing real second-PC target-candidate evidence. Validation passed: `cargo check --manifest-path .\musu-rs\Cargo.toml -j 1`, targeted `rendezvous`, `router`, `route_evidence`, and `cli_commands` Rust tests for lib/bin, `cargo build --manifest-path .\musu-rs\Cargo.toml --bin musu -j 1`, `npx tsx --test src/app/api/v1/p2p/rendezvous/route.test.ts`, `npx tsx --test src/app/api/v1/p2p/route-evidence/route.test.ts`, `npm run typecheck`, both diagnostic CLI JSON commands, `cargo fmt --check`, `git diff --check`, and indexer sync `1074 files / 2064 symbols`. |
| 2026-06-01 v119 | Rendezvous and route evidence now carry advertised peer identity material. | `musu-rs/src/install/tls.rs` can compute the SHA-256 fingerprint of the local TLS certificate. Logged-in bridge startup registers that fingerprint as `cert_fingerprint`/`peer_public_key`, and `musu-rs/src/bridge/rendezvous.rs` publishes it as candidate `public_key` when `~/.musu/tls/cert.pem` or `MUSU_TLS_CERT` exists. Refreshed target candidates carry the advertised fingerprint into `ResolvedPeer.meta`, and `bridge::route_evidence` records `peer_identity_method=advertised_tls_cert_fingerprint_unverified` plus `peer_public_key` when present. `musu.pro` route-evidence validation now rejects release-grade claims where `peer_identity_verified=true` but identity method/key material is missing. This is still not release-grade identity: current route attempts remain `peer_identity_verified=false`, `encryption=none_http_bearer`, and release is blocked until the transport proves remote key ownership over QUIC/TLS on a real second-PC route. Validation passed: `cargo check --manifest-path .\musu-rs\Cargo.toml -j 1`, targeted Rust `tls`, `rendezvous`, `route_evidence`, and `cli_commands` tests for lib/bin, `cargo build --manifest-path .\musu-rs\Cargo.toml --bin musu -j 1`, route-evidence and rendezvous API tests, `npm run typecheck`, both diagnostic CLI JSON commands, `cargo fmt --check`, `git diff --check`, and indexer sync `1075 files / 2071 symbols`. |
| 2026-06-01 v120 | HTTPS bridge forwarding now verifies advertised peer certificate fingerprints. | Candidate endpoints and registry metadata now preserve `scheme`, TLS-enabled bridge advertisements use `https://`, and `forward_to_peer_attempt` uses a fingerprint-pinned rustls client for HTTPS peers with advertised `sha256:<hex>` identity material. Route evidence now records `peer_identity_verified=true`, `peer_identity_method=tls_cert_fingerprint_pin`, and `encryption=https_tls_fingerprint_pin` only after the actual HTTPS POST succeeds with a matching server certificate fingerprint. `musu.pro` still keeps this non-release-grade with `transport_not_release_grade_quic_tls`; public release remains blocked until final QUIC/TLS route proof, real second-PC route evidence, relay fallback, two-machine CPU evidence, `musu@musu.pro` inbox evidence, and Store evidence are recorded. Validation passed: `cargo check --manifest-path .\musu-rs\Cargo.toml -j 1`, targeted Rust `forward`, `route_evidence`, `rendezvous`, `services`, `discovery`, and `cli_commands` tests for lib/bin, `cargo build --manifest-path .\musu-rs\Cargo.toml --bin musu -j 1`, route-evidence and rendezvous API tests, and `npm run typecheck`. |
| 2026-06-01 v121 | Route and relay diagnostics now expose HTTPS fingerprint pinning without claiming release readiness. | `musu relay status --json` now reports `https_fingerprint_pinning_wired=true` and `release_grade_transport_required=quic_tls_1_3`. `musu route --explain --json` reports the same fields and now distinguishes advertised fingerprint availability from verified identity and release-grade transport. Candidate diagnostics can show `transport_scheme`, `peer_identity_method`, `peer_public_key_present`, and `https_fingerprint_pin_available` while keeping advertised-only candidates unverified. This makes the next operator step explicit: verify HTTPS fingerprint pinning on a real second-PC route, then replace bridge HTTP/TLS with QUIC/TLS route proof. Validation passed: `cargo check --manifest-path .\musu-rs\Cargo.toml -j 1`, targeted `cli_commands` tests for lib/bin, `cargo build --manifest-path .\musu-rs\Cargo.toml --bin musu -j 1`, `musu relay status --json`, `musu route --explain --json "MUSU_ROUTE_EXPLAIN_CHECK"`, and `cargo fmt --check`. |
| 2026-06-01 v122 | Route evidence storage is now token-owner scoped. | `musu-bee/src/lib/p2pControlAuth.ts` now derives an `owner_key` as a SHA-256 hash of the accepted Bearer token, and `musu-bee/src/lib/routeEvidenceStore.ts` stores/query-filters route evidence by that owner key. `POST /api/v1/p2p/route-evidence` returns `owner_scoped=true`; `GET` only returns records for the caller's token-derived owner key and omits the stored key from response records. This closes the shared-bucket leakage gap in the current stub, while real account-id mapping, UI/export, retention policy, second-PC proof, QUIC/TLS route proof, relay fallback, two-machine CPU evidence, support inbox evidence, and Store evidence remain open. Validation passed: `npx tsx --test src/app/api/v1/p2p/route-evidence/route.test.ts`, `npm run typecheck`, `git diff --check`, and indexer sync `1078 files / 2107 symbols`. |
| 2026-06-01 v123 | Relay fallback now has a fail-closed control-plane lease boundary. | Added `musu-bee/src/app/api/v1/p2p/relay/lease/route.ts` and `musu-bee/src/lib/p2pRelayLeaseStore.ts`. `POST /api/v1/p2p/relay/lease` validates fallback requests, defaults to `lease_issued=false`, and only issues owner-scoped leases when direct routes failed and relay env policy is explicitly enabled: `MUSU_P2P_RELAY_ENABLED=1`, `MUSU_P2P_RELAY_TRANSPORT_WIRED=1`, `MUSU_P2P_RELAY_URL`, and `MUSU_P2P_RELAY_ENTITLEMENT=connect|pro|enterprise`. `GET` returns only the caller's token-owner scoped leases and omits `owner_key`. Rust cloud DTOs now include relay lease request/response plus `request_relay_lease`, and `musu relay status --json` reports `relay_control_plane_lease_wired=true`, `relay_lease_endpoint=/api/v1/p2p/relay/lease`, `relay_default_data_path=false`, and still `relay_transport_wired=false`. This prevents relay from becoming a silent default data path, but actual relay/tunnel transport, real account entitlement mapping, QUIC/TLS route proof, real second-PC proof, two-machine CPU evidence, support inbox evidence, and Store evidence remain open. Validation passed: relay lease API tests, combined P2P API tests, `npm run typecheck`, `cargo test --manifest-path .\musu-rs\Cargo.toml -j 1 --lib cloud::tests -- --nocapture`, `cargo check --manifest-path .\musu-rs\Cargo.toml -j 1`, `cargo build --manifest-path .\musu-rs\Cargo.toml --bin musu -j 1`, `cargo fmt --check`, `musu relay status --json`, and indexer sync `1082 files / 2141 symbols`. |
| 2026-06-01 v124 | Current primary desktop-open runtime CPU evidence was refreshed after the relay lease control-plane commit. | Opened the installed `Yellowhama.MUSU_1.15.0.0_x64__ygcjq669as2b6` Start app and ran `measure-musu-idle-cpu.ps1` for the release gate scenario: `-SampleSeconds 60 -Scenario desktop-open -RequireOwnedWebView2 -MaxOneCorePercent 5 -MaxOwnedProcessCount 16 -MaxOwnedWebView2ProcessCount 8 -MaxTotalWorkingSetMb 1024 -IncludeNode -IncludeWebView2 -FailOnHot`. Evidence `docs\evidence\runtime-idle-cpu\1.15.0-rc.1\20260601-082822-HUGH_SECOND.desktop-open.evidence.json` passed with `git_commit=cdefc1226481d554d2d151e6a08af4dc81572247`, `git_dirty=false`, 60.015s sample, one `musu-desktop` process, six owned WebView2 helpers, zero owned Node helpers, max one-core CPU `musu=0` and `webview2=0.16`, total working set `339.85MB`, total private memory `184.58MB`, and no resource budget violations. Post-commit go/no-go accepted this evidence as one valid machine (`valid_machine_count=1`, `HUGH_SECOND`) with `manifest_dirty=false`; release still needs second-PC desktop-open evidence, fresh single-machine smoke after the latest code commit, hardened route proof, support inbox evidence, and Store evidence. Indexer sync recorded `1084 files / 2141 symbols`. |
| 2026-06-01 v125 | Current single-machine smoke evidence was refreshed after the latest evidence/docs commit. | Started Next dev on `127.0.0.1:3000`, warmed `/app` after the first smoke attempt found dashboard readiness `warn`, then reran `smoke-single-machine-beta.ps1` against `musu-rs\target\debug\musu.exe`. Recorded evidence `docs\evidence\single-machine\1.15.0-rc.1\20260601-084028-HUGH_SECOND.evidence.json` with verification and summary. It passed on commit `a1ee33fa0c8e3b68e85dc4b48077134ec5dd99ac` with dashboard output `MUSU_RELEASE_SMOKE_OK_20260601_084005`, CLI output `MUSU_CLI_ROUTE_OK_20260601_084005`, dashboard task `5ac5baa6-471f-4633-9a57-9e3a87a20c7a`, bridge `http://127.0.0.1:13167`, and `cli_route_checked=true`. Go/no-go now reports `single_machine_verified=true`; release remains blocked by real second-PC multi-device evidence, two-machine desktop-open CPU evidence, `musu@musu.pro` delivery evidence, Store evidence, and dirty state until this evidence is committed. |
| 2026-06-01 v126 | The repeated Tailscale IPv6 mDNS errors were classified against current source defaults. | The operator re-supplied logs where `mdns_sd::service_daemon` repeatedly attempted `[ff02::fb%9]:5353` on the Windows `Tailscale` adapter and failed with `os error 10065`, then `sending on a closed channel`. Current source already treats this as an idle CPU/log-spam risk: bridge mDNS is opt-in through `MUSU_ENABLE_MDNS=1`, IPv6 mDNS through `MUSU_MDNS_ENABLE_IPV6=1`, and Tailscale mDNS interfaces through `MUSU_MDNS_ENABLE_TAILSCALE=1`. The release docs now state that default Store-candidate runs should not hit Tailscale IPv6 mDNS; if an installed desktop still emits this with defaults, assume stale installed bits or an explicit mDNS/IPv6/Tailscale opt-in environment until proven otherwise. |
| 2026-06-01 v127 | Runtime idle CPU gate reporting now exposes the machine count at top level. | `write-release-go-no-go.ps1` now emits `runtime_idle_cpu_min_machine_count`, `runtime_idle_cpu_valid_machine_count`, `runtime_idle_cpu_valid_machines`, and `runtime_idle_cpu_candidate_count` next to `runtime_idle_cpu_verified`, and its text output prints `runtime_idle_cpu_valid_machines: valid/min [machines]`. `show-final-release-handoff-status.ps1` carries the same fields under `gates`. This prevents operator/agent summaries from missing the real state: current HEAD reports `runtime_idle_cpu_verified=false`, `valid_machine_count=1`, `min_machine_count=2`, `valid_machines=[HUGH_SECOND]`, and `candidate_count=24`. Validation passed: PowerShell parser checks for both scripts, go/no-go JSON smoke, handoff-status JSON smoke, and `git diff --check`. |
| 2026-06-01 v128 | Current single-machine smoke evidence was refreshed after runtime CPU gate reporting code changed. | Started Next dev on `127.0.0.1:3000`, warmed `/app` and `/api/doctor`, then reran `smoke-single-machine-beta.ps1` against `musu-rs\target\debug\musu.exe`. Recorded evidence `docs\evidence\single-machine\1.15.0-rc.1\20260601-090548-HUGH_SECOND.evidence.json` with verification and summary. It passed on commit `da4999081073018ab3b1b72a26645140ad2e68f7` with dashboard output `MUSU_RELEASE_SMOKE_OK_20260601_090528`, CLI output `MUSU_CLI_ROUTE_OK_20260601_090528`, dashboard task `38d1eb2c-1905-493b-b536-459866c25c78`, bridge `http://127.0.0.1:5089`, evidence SHA-256 `52f34b6bc377404e118ec429bdc2c3d9c781d622870debee0d2d62a67e6eaae7`, and verification SHA-256 `70f8665ddcc015305b539bf8832bcf57b4d4507c1f07d7284a1d0bfd59f7f22a`. Primary desktop-open CPU evidence must be refreshed next because the reporting code commit made the previous CPU evidence stale. |
| 2026-06-01 v129 | mDNS default filtering now excludes common VPN/virtual adapters, not only Tailscale/IPv6. | The repeated `ff02::fb%9` Tailscale log remains a real stale/opt-in mDNS failure class. `musu-rs/src/peer/mdns.rs` now also filters common VPN/virtual adapters by default (`Tailscale`, `NordLynx`, WireGuard/wg, ZeroTier, vEthernet/WSL/Hyper-V, Docker, VMware, VirtualBox, tun/tap/utun/VPN names) and exposes `MUSU_MDNS_ENABLE_VIRTUAL_INTERFACES=1` only for explicit widening. Validation passed: `cargo test --lib -j 1 peer::mdns::tests::`, `cargo build --bin musu -j 1`, and `RUST_LOG=debug musu discover --timeout 2`; the discover run disabled 9 virtual/VPN interfaces and sent only on the physical `이더넷 2` LAN adapter, with no Tailscale/NordLynx/vEthernet/`ff02::fb`/`10065`/`closed channel` output. |
| 2026-06-01 v130 | Current smoke and primary desktop-open CPU evidence were refreshed after mDNS virtual-interface filtering. | Current smoke evidence `docs\evidence\single-machine\1.15.0-rc.1\20260601-093958-HUGH_SECOND.evidence.json` passed from commit `4ad4b5591bba3c03fffe7eb2d054a4e191b67bea` with dashboard output `MUSU_RELEASE_SMOKE_OK_20260601_093933`, CLI output `MUSU_CLI_ROUTE_OK_20260601_093933`, dashboard task `61f5d95d-ec59-418d-a583-a47336cdf126`, bridge `http://127.0.0.1:9189`, evidence SHA-256 `8ad3d4031f1b9592f6d6cabdd51a0ace8a9e78a08a527fde5716ecca2529d953`, and verification SHA-256 `b4695c68131c47627bbc08dc1d45284db5a0ae1ba07954a4627e8a413ac7279c`. Current primary packaged `desktop-open` CPU evidence `docs\evidence\runtime-idle-cpu\1.15.0-rc.1\20260601-094127-HUGH_SECOND.desktop-open.evidence.json` passed from clean commit `931e771650cda3026630cf0b2394c83211490dc6` with 60.029s sample, one `musu-desktop`, six owned WebView2 helpers, owned Node `0`, max one-core CPU `musu=0`, `webview2=0.08`, total working set `340.49MB`, private memory `181.8MB`, and no resource-budget violations. Release still needs second-PC desktop-open CPU evidence, hardened second-PC route evidence, `musu@musu.pro` inbox delivery evidence, and Store evidence. |
| 2026-06-01 v131 | Runtime CPU scenario matrix diagnostics were added after the latest mDNS log report. | Added wiki/529 `RUNTIME_CPU_SCENARIO_MATRIX_AND_MDNS_LOG_AUDIT_2026_06_01.md` and `scripts\windows\measure-musu-runtime-cpu-scenarios.ps1`. The matrix writes schema `musu.runtime_cpu_scenario_matrix.v1`, measures `runtime-started`, `dashboard-open`, `desktop-open`, and `post-route`, and delegates per-process CPU attribution to `measure-musu-idle-cpu.ps1` with Node/WebView2 budgets. `measure-musu-idle-cpu.ps1` now accepts `runtime-started`, `dashboard-open`, and `startup-open` scenario labels. The wrapper uses timeout-bounded `Start-Process` temp-file command capture for `musu up --json` to avoid the known bridge stdout pipe-handle hang. Parser checks passed for both scripts; a 3s local `runtime-started` smoke wrote `.local-build\runtime-cpu-scenarios\20260601-100515-HUGH_SECOND\20260601-100515-HUGH_SECOND.runtime-cpu-scenario-matrix.json` with `ok=true`, one MUSU process, owned Node `0`, owned WebView2 `0`, max one-core CPU `0`, and working set `27.72MB`. This is diagnostic-only and does not close the two-machine 60s `desktop-open` release CPU gate. |
| 2026-06-01 v132 | Code and docs were re-indexed after the runtime CPU scenario matrix update. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1102 files and 2147 symbols after wiki/529, the scenario matrix script, the idle sampler scenario labels, and the CoS memory note were added. Search terms include `musu.runtime_cpu_scenario_matrix.v1`, `measure-musu-runtime-cpu-scenarios.ps1`, `runtime-started`, `dashboard-open`, `post-route`, `20260601-100515-HUGH_SECOND.runtime-cpu-scenario-matrix.json`, and `bridge stdout pipe-handle hang`. |
| 2026-06-01 v133 | Current single-machine and primary CPU evidence were refreshed after the runtime CPU scenario matrix commit. | Current single-machine smoke evidence `docs\evidence\single-machine\1.15.0-rc.1\20260601-101859-HUGH_SECOND.evidence.json` passed from commit `9d39ab2f7a02aca75beaaeb5d35198d850bbad98` with dashboard output `MUSU_RELEASE_SMOKE_OK_20260601_101833`, CLI output `MUSU_CLI_ROUTE_OK_20260601_101833`, dashboard task `fae480fc-b2e4-4c4f-b5f9-7c452c6e0ec5`, bridge `http://127.0.0.1:1126`, evidence SHA-256 `eb1074d2f7a77b1b3dca94a2ffa437b38a17335288b5951ed52f3a866644a487`, and verification SHA-256 `33d920ccc0d208dba0d33d0b21e9b1e419eca2babbc3af6a4c433f694d0ea4cb`. Current primary packaged `desktop-open` CPU evidence `docs\evidence\runtime-idle-cpu\1.15.0-rc.1\20260601-101541-HUGH_SECOND.desktop-open.evidence.json` passed from clean commit `9d39ab2f7a02aca75beaaeb5d35198d850bbad98` with 60.032s sample, one `musu-desktop`, six owned WebView2 helpers, owned Node `0`, max one-core CPU `musu=0`, `webview2=0.13`, working set `342.44MB`, private memory `184.04MB`, and evidence SHA-256 `84a3458aabb7f49f7a3853588f44ee09e496c0f9d307a2dab75d8a2e030d59c0`. Go/no-go after these files reports `single_machine_verified=true`, `runtime_idle_cpu_valid_machine_count=1/2` with `HUGH_SECOND`, and public release still No-Go. |
| 2026-06-01 v134 | Code and docs were re-indexed after current evidence refresh. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1107 files and 2147 symbols after current single-machine smoke evidence, current primary desktop-open CPU evidence, wiki/529, and the new CoS evidence note. Search terms include `20260601-101859-HUGH_SECOND.evidence.json`, `20260601-101541-HUGH_SECOND.desktop-open.evidence.json`, `fae480fc-b2e4-4c4f-b5f9-7c452c6e0ec5`, `MUSU_RELEASE_SMOKE_OK_20260601_101833`, `webview2=0.13`, `342.44MB`, and `runtime CPU 1/2`. |
| 2026-06-01 v135 | Second-PC return flow now carries the CPU scenario matrix without weakening the release CPU gate. | `run-second-pc-release-check.ps1` now captures `musu.runtime_cpu_scenario_matrix.v1` by default, zips matrix files into `.local-build\second-pc-return\*.zip`, and exposes `-SkipRuntimeCpuScenarioMatrix` plus `-FailOnRuntimeCpuScenarioMatrix`. `import-second-pc-return.ps1` imports the matrix to `.local-build\runtime-cpu-scenarios\` and selects release CPU evidence only from `.local-build\runtime-idle-cpu\` with `scenario=desktop-open` and `require_owned_webview2=true`, so diagnostic matrix evidence cannot shadow the release gate sample. The multi-device kit, final operator packet, operator action-pack verifier, and readiness audit now include `measure-musu-runtime-cpu-scenarios.ps1`. Validation passed: parser checks for touched scripts, generated kit `musu-multidevice-1.15.0-rc.1-20260601-103622.zip` includes the matrix script, and a synthetic return zip imported both `runtime_idle_cpu_evidence_path` and `runtime_cpu_scenario_matrix_path`. |
| 2026-06-01 v136 | Code and docs were re-indexed after second-PC CPU matrix return flow wiring. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1108 files and 2147 symbols after second-PC wrapper/importer/kit/final-packet/action-pack wiring for CPU scenario matrices. Search terms include `SkipRuntimeCpuScenarioMatrix`, `runtime_cpu_scenario_matrix_path`, `musu.runtime_cpu_scenario_matrix.v1`, `Resolve-LatestRuntimeIdleReleaseEvidence`, `musu-multidevice-1.15.0-rc.1-20260601-103622.zip`, and `second-PC CPU matrix return flow`. |
| 2026-06-01 v137 | Current single-machine and primary CPU evidence were refreshed after the second-PC CPU matrix return-flow commit. | Current single-machine smoke evidence `docs\evidence\single-machine\1.15.0-rc.1\20260601-105019-HUGH_SECOND.evidence.json` passed from commit `04d1ab13f1960d9f7adb5fb2d389ccd39c63923d` with dashboard output `MUSU_RELEASE_SMOKE_OK_20260601_104959`, CLI output `MUSU_CLI_ROUTE_OK_20260601_104959`, dashboard task `e2f4b35b-7b79-4621-abbc-658413665d0b`, bridge `http://127.0.0.1:4980`, evidence SHA-256 `0b14f1e429b81a3b765b8ad6695a4bd02ce3685094ff127eda94af07a8ec66b8`, and verification SHA-256 `3a1fbfe7a38949b088004dbf9a2f1ea6a0cacdbe8a69e95ebadd74002429ee08`. Current primary packaged `desktop-open` CPU evidence `docs\evidence\runtime-idle-cpu\1.15.0-rc.1\20260601-104726-HUGH_SECOND.desktop-open.evidence.json` passed from clean commit `04d1ab13f1960d9f7adb5fb2d389ccd39c63923d` with 60.022s sample, one `musu-desktop`, six owned WebView2 helpers, owned Node `0`, max one-core CPU `musu=0`, `webview2=0.18`, working set `341.1MB`, private memory `185.71MB`, and evidence SHA-256 `fa62a7b93b0475e8b59c89fbe8bdb9c907e6dfd1f94aa2c7ad34e4bde633346c`. |
| 2026-06-01 v138 | Code and docs were re-indexed after the current evidence refresh. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1113 files and 2147 symbols after current single-machine smoke evidence, current primary desktop-open CPU evidence, BETA checklist pointer updates, WIKI/WIKI_INDEX state refresh, and the new CoS evidence note. Search terms include `20260601-105019-HUGH_SECOND.evidence.json`, `20260601-104726-HUGH_SECOND.desktop-open.evidence.json`, `e2f4b35b-7b79-4621-abbc-658413665d0b`, `MUSU_RELEASE_SMOKE_OK_20260601_104959`, `webview2=0.18`, `341.1MB`, and `04d1ab13f1960d9f7adb5fb2d389ccd39c63923d`. |
| 2026-06-01 v139 | Runtime direct-route failures now request a fail-closed relay lease. | `musu-rs/src/bridge/handlers/forward.rs` now calls `request_relay_lease_after_direct_failure` after all direct route attempts fail and before closing the rendezvous session. The request is skipped without a rendezvous session or account token, records attempted route kinds and `direct_path_failed=true`, and does not replace the original direct-route failure. `musu relay status --json` now reports `relay_runtime_fallback_lease_request_wired=true` while `relay_transport_wired=false` and `relay_default_data_path=false`. This advances the `musu.pro` P2P control-plane objective without making relay the default payload path. Validation passed: targeted rendezvous relay-request test, relay lease serialization test, `cargo fmt --check`, `cargo build --bin musu -j 1`, and relay status JSON. |
| 2026-06-01 v140 | Code and docs were re-indexed after runtime relay fallback wiring. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1115 files and 2157 symbols after wiki/530, runtime relay fallback docs, `relay_runtime_fallback_lease_request_wired`, and the CoS memory note were added. Search terms include `request_relay_lease_after_direct_failure`, `relay_runtime_fallback_lease_request_wired`, `direct_path_failed=true`, `RUNTIME_RELAY_FALLBACK_NEXT_STEPS_2026_06_01.md`, and `2026-06-01_1120_kst_runtime_relay_fallback_lease_request.md`. |
| 2026-06-01 v141 | Current single-machine and primary CPU evidence were refreshed after the runtime relay fallback commit. | Current single-machine smoke evidence `docs\evidence\single-machine\1.15.0-rc.1\20260601-113438-HUGH_SECOND.evidence.json` passed from commit `52698c4406de4747b4e1ce1834cfbad1cb0c75c1` with dashboard output `MUSU_RELEASE_SMOKE_OK_20260601_113412`, CLI route checked, dashboard task `7527eda1-b7f6-4a0d-8447-b17e76e756fe`, bridge `http://127.0.0.1:1473`, evidence SHA-256 `4ddb66e16f4714f0ea40e4d6dc3c436e713bc6434be713433f9d26e67768f2e5`, and verification SHA-256 `3561358fc9fd767237a5a738a829ea384040e90de39d490407b65a50c68e83cf`. Current primary packaged `desktop-open` CPU evidence `docs\evidence\runtime-idle-cpu\1.15.0-rc.1\20260601-113022-HUGH_SECOND.desktop-open.evidence.json` passed from clean commit `52698c4406de4747b4e1ce1834cfbad1cb0c75c1` with 60.029s sample, one `musu-desktop`, six owned WebView2 helpers, owned Node `0`, max one-core CPU `musu=0`, `webview2=0.16`, working set `343.16MB`, private memory `182.96MB`, and evidence SHA-256 `15b663f4a0059a6d4620fc17c8bfaa5a7111e7eab806ff6a3173de72b3ed7f72`. |
| 2026-06-01 v142 | Code and docs were re-indexed after the post-runtime-relay evidence refresh. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1120 files and 2157 symbols after the current CPU/smoke evidence, wiki/522/wiki/524/wiki/528/wiki/530 updates, BETA checklist pointer refresh, and CoS evidence note. Search terms include `20260601-113438-HUGH_SECOND.evidence.json`, `20260601-113022-HUGH_SECOND.desktop-open.evidence.json`, `7527eda1-b7f6-4a0d-8447-b17e76e756fe`, `MUSU_RELEASE_SMOKE_OK_20260601_113412`, `webview2=0.16`, `343.16MB`, and `52698c4406de4747b4e1ce1834cfbad1cb0c75c1`. |
| 2026-06-01 v143 | Relay fallback evaluation is now persisted in route evidence. | Added `relay_fallback` to Rust local/cloud route evidence, bridge forwarding failure reports, and the Next `/api/v1/p2p/route-evidence` contract. After terminal direct-route failure, bridge forwarding still requests a fail-closed relay lease only when a rendezvous session/account token exists, but failed route evidence now records whether the fallback was skipped/requested/denied/issued/failed/timed out, attempted route kinds, requested capability, policy/blockers, optional lease id, and relay failure class. This remains audit/control-plane evidence only: `relay_transport_wired=false`, `relay_default_data_path=false`, and relay payload transport is still not wired. Validation passed: `cargo fmt --check`, `cargo test --lib route_evidence`, targeted relay lease fallback test, `cargo build --bin musu -j 1`, route-evidence API tests including relay fallback persistence, `npm run typecheck`, and `musu relay status --json`. Public release remains No-Go pending second-PC desktop-open CPU evidence, real multi-device route proof, `musu@musu.pro` inbox delivery evidence, Store evidence, QUIC/TLS proof, and relay/tunnel transport. |
| 2026-06-01 v144 | Code and docs were re-indexed after relay fallback route-evidence persistence. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1121 files and 2163 symbols after Rust/Next route-evidence contract changes, wiki/522/wiki/524/wiki/528/wiki/530 updates, BETA checklist update, WIKI/WIKI_INDEX refresh, and CoS memory note `2026-06-01_1203_kst_relay_fallback_route_evidence.md`. Search terms include `relay_fallback`, `lease_requested`, `lease_issued`, `skipped_no_session`, `skipped_no_token`, `relay_lease_denied`, `stores relay fallback evidence after failed direct route`, and `RouteRelayFallbackEvidence`. |
| 2026-06-01 v145 | Current single-machine and primary desktop-open CPU evidence were refreshed after relay fallback route-evidence persistence. | Single-machine smoke evidence `docs\evidence\single-machine\1.15.0-rc.1\20260601-121339-HUGH_SECOND.evidence.json` verifies against current code because the only later commits are docs/evidence refreshes. It passed with dashboard output `MUSU_RELEASE_SMOKE_OK_20260601_121339`, CLI route checked, dashboard task `34375312-8b16-4015-ad1a-7bd5f2ccb19c`, bridge `http://127.0.0.1:9157`, and evidence SHA-256 `f05c5a8babf474a3a13069b033c9093079c22c72f70026de8316c607ccf2714d`. Primary packaged `desktop-open` CPU evidence `docs\evidence\runtime-idle-cpu\1.15.0-rc.1\20260601-121701-HUGH_SECOND.desktop-open.evidence.json` passed from clean git state with 60.023s sample, two MUSU processes (`musu` runtime plus `musu-desktop`), six owned WebView2 helpers, owned Node `0`, max one-core CPU `musu=0`, `webview2=0.03`, total working set `378.16MB`, private memory `192.07MB`, and evidence SHA-256 `21280f882fc05ed9b7284a268847ab150585c4b0d24d90dce91a0a03568d0fa2`. Go/no-go after these commits reports `single_machine_verified=true`, `runtime_idle_cpu_valid_machine_count=1/2` with `HUGH_SECOND`, and public release still No-Go. |
| 2026-06-01 v146 | Code and docs were re-indexed after current evidence refresh. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1124 files and 2163 symbols after current single-machine evidence, current primary desktop-open CPU evidence, wiki/522/WIKI/WIKI_INDEX/BETA checklist updates, and CoS memory note `2026-06-01_1218_kst_post_relay_fallback_current_evidence.md`. Search terms include `20260601-121339-HUGH_SECOND.evidence.json`, `20260601-121701-HUGH_SECOND.desktop-open.evidence.json`, `MUSU_RELEASE_SMOKE_OK_20260601_121339`, `34375312-8b16-4015-ad1a-7bd5f2ccb19c`, `webview2=0.03`, `378.16MB`, and `runtime CPU 1/2`. |
| 2026-06-01 v147 | Multi-device release verifier now requires QUIC/TLS route evidence, not merely non-legacy encryption. | `scripts\windows\verify-multidevice-evidence.ps1` now requires `peer_identity_method`, `peer_public_key`, and `encryption=quic_tls_1_3` for passing route evidence. `verify-final-operator-gate-packet.ps1` now rejects packets whose bundled multi-device verifier lacks those checks, and `prepare-final-operator-gate-packet.ps1` describes HTTPS fingerprint-pinned bridge evidence as diagnostic-only. This closes the gap where HTTPS fingerprint-pinned bridge evidence could be treated as hardened enough by the multi-device script even though the control-plane route-evidence API correctly marks it non-release-grade. Synthetic verifier validation passed: a `quic_tls_1_3` evidence fixture exited `0`, while an otherwise identical `https_tls_fingerprint_pin` fixture exited `1` with `route encryption release-grade`. |
| 2026-06-01 v148 | Code and docs were re-indexed after multi-device verifier hardening. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1125 files and 2163 symbols after the QUIC/TLS verifier gate, final packet stale-verifier guard, BETA checklist/current status/WIKI/WIKI_INDEX updates, and CoS memory note `2026-06-01_1228_kst_multidevice_verifier_quic_tls_gate.md`. Search terms include `route encryption release-grade`, `quic_tls_1_3`, `peer_identity_method`, `peer_public_key`, `https_tls_fingerprint_pin`, and `multi-device verifier QUIC/TLS gate`. |
| 2026-06-01 v149 | Current single-machine and primary desktop-open CPU evidence were refreshed after the QUIC/TLS verifier gate. | Single-machine smoke evidence `docs\evidence\single-machine\1.15.0-rc.1\20260601-124055-HUGH_SECOND.evidence.json` passed on commit `10548e70a85c38d864bf28e2a2d347d80135e9c9` with dashboard output `MUSU_RELEASE_SMOKE_OK_20260601_124032`, CLI route checked, dashboard task `e1c03dce-0d13-4482-ab9d-7988b10a9d2b`, and bridge `http://127.0.0.1:6483`. Primary packaged `desktop-open` CPU evidence `docs\evidence\runtime-idle-cpu\1.15.0-rc.1\20260601-124454-HUGH_SECOND.desktop-open.evidence.json` passed from clean commit `4d9435c3d4d97f7d3429b5eaf2b7154612e5eedb` with `git_dirty=false`, 60.018s sample, two MUSU processes, six owned WebView2 helpers, owned Node `0`, max one-core CPU `musu=0`, `webview2=0.10`, total working set `387.75MB`, and private memory `183.16MB`. This restores current local evidence after the verifier script code change; public release remains No-Go pending second-PC CPU, real QUIC/TLS route proof, support inbox, and Store evidence. |
| 2026-06-01 v150 | Code and docs were re-indexed after the post-QUIC/TLS-verifier evidence refresh. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1130 files and 2163 symbols after current single-machine evidence, current primary desktop-open CPU evidence, BETA checklist/current status/WIKI/WIKI_INDEX updates, and CoS memory note `2026-06-01_1248_kst_post_quic_tls_verifier_current_evidence.md`. Search terms include `20260601-124055-HUGH_SECOND.evidence.json`, `20260601-124454-HUGH_SECOND.desktop-open.evidence.json`, `MUSU_RELEASE_SMOKE_OK_20260601_124032`, `e1c03dce-0d13-4482-ab9d-7988b10a9d2b`, `webview2=0.10`, `387.75MB`, and `runtime CPU 1/2`. |
| 2026-06-01 v151 | Route evidence now requires a local transport verifier, not only a QUIC/TLS-looking encryption string. | Added `transport_verified_by` to Rust local/cloud route evidence and the `musu.pro` route-evidence API. The API and `verify-multidevice-evidence.ps1` now require `transport_verified_by=musu_quic_tls_transport` for release-grade evidence. Bridge HTTPS fingerprint-pinned forwarding writes `musu_bridge_forward_fingerprint_pinned_client` through an explicit local `RouteTransportProof`, which proves the interim HTTPS pin but remains non-release-grade. `bridge::route_evidence` now treats registry/rendezvous peer metadata as advertised material only: even metadata that claims `peer_identity_verified=true`, `encryption=quic_tls_1_3`, and `transport_verified_by=musu_quic_tls_transport` is downgraded unless the runtime transport code passes local proof. Validation passed: Rust `route_evidence` and `forward` tests, route-evidence API tests, PowerShell parser checks, synthetic multi-device verifier pass/fail fixture, `cargo fmt --check`, and `npm run typecheck`. |
| 2026-06-01 v152 | Code and docs were re-indexed after route transport-proof hardening and the repeated Tailscale IPv6 mDNS log classification. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1131 files and 2165 symbols after `transport_verified_by` was added to route evidence, the Windows multi-device/final-packet verifiers were tightened, WIKI/WIKI_INDEX/current-status docs were updated, and the operator-supplied `ff02::fb%9` Tailscale `os error 10065` mDNS pattern was classified as stale-binary or explicit mDNS opt-in unless reproduced on a current build with all mDNS opt-in variables unset. Search terms include `transport_verified_by`, `RouteTransportProof`, `musu_quic_tls_transport`, `missing_release_grade_transport_proof`, `route transport proof`, `musu_bridge_forward_fingerprint_pinned_client`, `ff02::fb%9`, `os error 10065`, and `MUSU_MDNS_ENABLE_TAILSCALE`. |
| 2026-06-01 v153 | Current rebuilt binary was checked against the re-supplied Tailscale IPv6 mDNS log pattern. | `cargo build --manifest-path .\musu-rs\Cargo.toml --bin musu -j 1` passed, then `RUST_LOG=debug musu discover --timeout 2` on `HUGH_SECOND` matched no `Failed to send|ff02::fb|10065|closed channel`, logged `mDNS Tailscale interfaces disabled by default`, disabled 9 virtual/VPN interfaces, and sent outbound multicast only on physical `이더넷 2`. `RUNTIME_ROUTE_EVIDENCE_MDNS_AUDIT_2026_06_01.md` and CoS memory `2026-06-01_1312_kst_route_evidence_transport_verifier.md` were updated with that evidence, then `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1131 files and 2165 symbols again. |
| 2026-06-01 v154 | Current single-machine evidence was refreshed after local transport-proof hardening. | Single-machine smoke evidence `docs\evidence\single-machine\1.15.0-rc.1\20260601-134022-HUGH_SECOND.evidence.json` passed from commit `87e5e5c24a6a062d8708d494dea3093cc516d92e` with dashboard output `MUSU_RELEASE_SMOKE_OK_20260601_134000`, CLI route checked, dashboard task `1f487f6b-7448-4745-b628-bfb1753cea1f`, bridge `http://127.0.0.1:4750`, evidence SHA-256 `1fa3ccc6383dcc68c9f7430e34855e843066723b7817c2d1c2ea0a2e340305bc`, and verification SHA-256 `33eaf6e626a98d7e30f33b816c84956dbd8a447334af411d4f48db343e985237`. |
| 2026-06-01 v155 | Current primary desktop-open CPU evidence was refreshed after committing the new single-machine evidence. | Primary packaged `desktop-open` CPU evidence `docs\evidence\runtime-idle-cpu\1.15.0-rc.1\20260601-134219-HUGH_SECOND.desktop-open.evidence.json` passed from clean commit `c316b8ba357235a9d1ea1c1ff437d78194d5b82e` with `git_dirty=false`, 60.022s sample, two MUSU processes, six owned WebView2 helpers, owned Node `0`, max one-core CPU `musu=0`, `webview2=0.18`, total working set `372.88MB`, and private memory `189.97MB`. This restores current local evidence after the route-evidence transport-proof code change; public release remains No-Go pending second-PC CPU, real QUIC/TLS route proof, support inbox, and Store evidence. |
| 2026-06-01 v156 | Code and docs were re-indexed after the post-transport-proof evidence refresh. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1136 files and 2165 symbols after the new single-machine evidence, primary desktop-open CPU evidence, BETA checklist/current status/WIKI/WIKI_INDEX updates, and CoS memory note `2026-06-01_1343_kst_post_transport_proof_current_evidence.md`. Search terms include `20260601-134022-HUGH_SECOND.evidence.json`, `20260601-134219-HUGH_SECOND.desktop-open.evidence.json`, `MUSU_RELEASE_SMOKE_OK_20260601_134000`, `1f487f6b-7448-4745-b628-bfb1753cea1f`, `webview2=0.18`, `372.88MB`, and `runtime CPU 1/2`. |
| 2026-06-01 v157 | Runtime CPU scenario matrix became a separate go/no-go attribution gate. | Added `verify-runtime-cpu-scenario-matrix.ps1` and wired it into `write-release-go-no-go.ps1`, second-PC wrapper verification, readiness audit, multi-device kit, final operator packet, and action-pack verification. Public release now requires clean/current 60s `musu.runtime_cpu_scenario_matrix.v1` evidence on two machines for `runtime-started`, `dashboard-open`, `desktop-open`, and `post-route`, with a successful post-route probe. This does not replace the existing two-machine `desktop-open` runtime idle CPU gate; it closes the gap where a busy-loop could hide in another runtime state. |
| 2026-06-01 v158 | Code and docs were re-indexed after promoting the CPU scenario matrix gate. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1138 files and 2165 symbols after the matrix verifier, go/no-go gate, second-PC wrapper, final packet/action-pack checks, wiki/529 update, and CoS memory note `2026-06-01_1415_kst_runtime_cpu_scenario_matrix_gate.md`. Search terms include `verify-runtime-cpu-scenario-matrix.ps1`, `runtime_cpu_scenario_matrix_verified`, `RunRuntimeCpuRouteProbe`, `RequirePostRouteProbe`, `musu.runtime_cpu_scenario_matrix.v1`, `runtime-started`, `dashboard-open`, `desktop-open`, and `post-route`. |
| 2026-06-01 v159 | Current single-machine and primary desktop-open CPU evidence were refreshed after the matrix gate commit. | Single-machine smoke `docs\evidence\single-machine\1.15.0-rc.1\20260601-141622-HUGH_SECOND.evidence.json` passed with dashboard output `MUSU_RELEASE_SMOKE_OK_20260601_141601`, CLI route checked, dashboard task `ab8b05fd-3a66-47c4-9f88-d151e1056b29`, bridge `http://127.0.0.1:7328`, evidence SHA-256 `8c31a691b50d34dfde6cc974380f5f43642f7d85c5c93d46593bce019817c73e`, and verification SHA-256 `96da6ccdd908501b32fa3ce0b5e85188d43cfd963763de34d3385c59e5d46e83`. Primary packaged `desktop-open` CPU evidence `docs\evidence\runtime-idle-cpu\1.15.0-rc.1\20260601-141730-HUGH_SECOND.desktop-open.evidence.json` passed from clean commit `668362afe47b7e78985dd12e22a5fb9cbde3bb0b` with 60.027s sample, two MUSU processes, six owned WebView2 helpers, owned Node `0`, max one-core CPU `musu=0`, `webview2=0.16`, working set `374.19MB`, and private memory `183.35MB`. |
| 2026-06-01 v160 | Code and docs were re-indexed after the post-matrix-gate evidence refresh. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1143 files and 2165 symbols after current single-machine evidence, current primary desktop-open CPU evidence, WIKI/WIKI_INDEX/BETA updates, and CoS memory note `2026-06-01_1420_kst_post_matrix_gate_current_evidence.md`. Search terms include `20260601-141622-HUGH_SECOND.evidence.json`, `20260601-141730-HUGH_SECOND.desktop-open.evidence.json`, `MUSU_RELEASE_SMOKE_OK_20260601_141601`, `ab8b05fd-3a66-47c4-9f88-d151e1056b29`, `webview2=0.16`, `374.19MB`, and `runtime_cpu_scenario_matrix_verified`. |
| 2026-06-01 v161 | Dashboard-open CPU matrix state can no longer be a no-op. | `measure-musu-runtime-cpu-scenarios.ps1` now stores the dashboard URL discovered from `musu up --json` and launches it for `dashboard-open` when no `-DashboardUrl` argument is supplied; if `dashboard-open` is run out of order, it performs its own bounded `musu up --json` discovery first. `verify-runtime-cpu-scenario-matrix.ps1` rejects dashboard-open entries whose preparation did not launch a dashboard URL, and `post-route` now requires the exact per-run route token. This makes the 4-state CPU matrix closer to the operator-reported states: runtime started, dashboard opened, desktop opened, and post-route. |
| 2026-06-01 v162 | Dashboard-open matrix harness smoke was checked after tightening. | Dirty-tree diagnostic matrix `.local-build\runtime-cpu-scenarios\20260601-143309-HUGH_SECOND\20260601-143309-HUGH_SECOND.runtime-cpu-scenario-matrix.json` ran `dashboard-open` alone with 3.018s sample. It performed bounded `musu up --json` discovery, launched `http://127.0.0.1:3000/app`, recorded `dashboard_url_source=musu_up_dashboard_open`, sampled one MUSU process, owned Node `0`, owned WebView2 `0`, and max one-core CPU `0`. This confirms harness behavior only; it is not release evidence because git was dirty and sample duration was 3s. |
| 2026-06-01 v163 | Code and docs were re-indexed after dashboard-open matrix gate tightening. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1144 files and 2165 symbols after the no-op dashboard-open guard, out-of-order dashboard URL discovery, exact per-run post-route token check, wiki/529 update, and CoS memory note `2026-06-01_1430_kst_dashboard_open_matrix_gate.md`. Search terms include `musu_up_dashboard_open`, `dashboard_url_source`, `expected_token`, `dashboard opened`, `20260601-143309-HUGH_SECOND`, and `MUSU_CPU_SCENARIO_ROUTE_OK`. |
| 2026-06-01 v164 | Current single-machine and primary desktop-open CPU evidence were refreshed after dashboard-open matrix gate tightening. | Single-machine smoke `docs\evidence\single-machine\1.15.0-rc.1\20260601-144154-HUGH_SECOND.evidence.json` passed with dashboard output `MUSU_RELEASE_SMOKE_OK_20260601_144129`, CLI route checked, dashboard task `a55012e1-adab-48e6-950f-e6953cd1566b`, bridge `http://127.0.0.1:2502`, evidence SHA-256 `2dc3311862fc4ea4f76b92df0592d1db8ddd9b2641a81b7552b138ec9e846c1f`, and verification SHA-256 `06766b45a72efcb291ede7bef7e22901a19b9d01d06f0821a42ea608548b5977`. Primary packaged `desktop-open` CPU evidence `docs\evidence\runtime-idle-cpu\1.15.0-rc.1\20260601-144311-HUGH_SECOND.desktop-open.evidence.json` passed from clean commit `aeb6c10d366b9759773bea5491604f79a594d81a` with 60.029s sample, one `musu-desktop`, six owned WebView2 helpers, owned Node `0`, max one-core CPU `musu=0`, `webview2=0.18`, working set `341.41MB`, and private memory `183.8MB`. |
| 2026-06-01 v165 | Code and docs were re-indexed after the current evidence refresh. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1149 files and 2165 symbols after current single-machine evidence, current primary desktop-open CPU evidence, BETA/WIKI/WIKI_INDEX updates, and CoS memory note `2026-06-01_1445_kst_post_dashboard_open_gate_current_evidence.md`. Search terms include `20260601-144154-HUGH_SECOND.evidence.json`, `20260601-144311-HUGH_SECOND.desktop-open.evidence.json`, `MUSU_RELEASE_SMOKE_OK_20260601_144129`, `a55012e1-adab-48e6-950f-e6953cd1566b`, `webview2=0.18`, `341.41MB`, and `musu_up_dashboard_open`. |
| 2026-06-01 v166 | Manual CPU matrix scenario arguments were hardened after a command-binding audit. | A dirty-tree harness check showed that `powershell -File ... -Scenario runtime-started dashboard-open ...` binds only `runtime-started`; the extra scenario tokens are not recoverable as unbound script arguments. `measure-musu-runtime-cpu-scenarios.ps1` now normalizes comma-separated scenario values, and operator packet/runbook/status commands now use `-Scenario runtime-started,dashboard-open,desktop-open,post-route`. Dirty-tree comma smoke `20260601-145441-HUGH_SECOND` confirmed `requested_scenarios=[runtime-started,dashboard-open]`. |
| 2026-06-01 v167 | Code and docs were re-indexed after the CPU matrix scenario argument fix. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1150 files and 2165 symbols after comma-separated CPU matrix command hardening, operator packet/kit/status/runbook updates, wiki/529 update, and CoS memory note `2026-06-01_1455_kst_cpu_matrix_scenario_argument_fix.md`. Search terms include `runtime-started,dashboard-open,desktop-open,post-route`, `20260601-145441-HUGH_SECOND`, `requested_scenarios`, `comma-separated scenarios`, and `CPU matrix scenario argument fix`. |
| 2026-06-01 v168 | The public site scroll, logo, and favicon-accent branding were fixed for production deployment. | `musu-bee/src/app/globals.css` no longer locks `html, body` with `overflow:hidden`; public pages now scroll via `overflow-y:auto`. `musu-bee/src/app/layout.tsx`, `musu-bee/src/app/page.tsx`, and `musu-bee/src/components/PublicSiteShell.tsx` now use `/images/favicon-header.png` as the browser/site logo, and the sampled favicon teal `#24C8DB` is exposed as `--musu-color-brand-emerald` plus used as a restrained public-site accent. `npm run typecheck` and `npm run build` passed. Production-mode Playwright checks against `next start -p 3001` confirmed `/`, `/landing`, `/pricing`, and `/install` scroll on desktop and mobile, render the favicon logo, and load `#24C8DB`. Deployment target is the existing Vercel workflow: push to `main` triggers `Deploy musu-bee to Vercel` with production `musu.pro`. |
| 2026-06-01 v169 | Code and docs were re-indexed after the public site scroll/logo/accent fix. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1154 files and 2165 symbols after the public site scroll fix, favicon logo wiring, `#24C8DB` accent token, GOAL/WIKI updates, and CoS memory note `2026-06-01_1520_kst_public_site_scroll_logo_accent.md`. Search terms include `overflow-y:auto`, `favicon-header.png`, `--musu-color-brand-emerald`, `#24C8DB`, `PublicSiteShell`, and `Deploy musu-bee to Vercel`. |
| 2026-06-01 v170 | Runtime CPU attribution now counts repo-related Node.js by command line, and dashboard-open matrix proof no longer accepts non-reachable dashboard URL fallbacks. | `measure-musu-idle-cpu.ps1` reads `Win32_Process` command-line metadata for matched executable names, so the local Next `node.exe` on `3001` is counted as `repo_related_node`. `measure-musu-runtime-cpu-scenarios.ps1` now uses only `dashboard.reachable_url` from `musu up --json` unless an explicit `-DashboardUrl` is supplied. This fixes the operator-reported "many Node.js processes" measurement blind spot and removes the earlier weak dashboard-open matrix proof. Validation: 3s diagnostic measured Node `1`, and the full matrix verifier passed after the clean run. |
| 2026-06-01 v171 | Current primary 4-state runtime CPU scenario matrix evidence now passes with Node attributed. | Evidence `docs\evidence\runtime-cpu-scenarios\1.15.0-rc.1\20260601-154503-HUGH_SECOND.runtime-cpu-scenario-matrix.json` verifies with `ok=true`, `fail_count=0`, clean source commit `68d183e0d285b3578b75e5c243a64855e64683bd`, and required scenarios `runtime-started`, `dashboard-open`, `desktop-open`, and `post-route`. It launched `http://127.0.0.1:3001/app`, counted repo Node `1`, and stayed under budget: max one-core CPU `runtime-started webview2=0.05`, `dashboard-open webview2=0.05`, `desktop-open node=0.03/webview2=0.05`, `post-route webview2=0.16`. The gate remains `1/2` until a second PC returns matching clean/current matrix evidence. |
| 2026-06-01 v172 | Current single-machine smoke and primary `desktop-open` CPU evidence were refreshed after the CPU attribution fix. | Single-machine smoke `docs\evidence\single-machine\1.15.0-rc.1\20260601-155630-HUGH_SECOND.evidence.json` passed on source commit `f4c2e0fd6565a81a21d5537e146a0f098ff763bd` with dashboard output `MUSU_RELEASE_SMOKE_OK_20260601_155610`, CLI route checked, dashboard task `e6757818-7dc2-432d-b9fb-19143cded009`, and bridge `http://127.0.0.1:4747`. Primary `desktop-open` CPU evidence `docs\evidence\runtime-idle-cpu\1.15.0-rc.1\20260601-160102-HUGH_SECOND.desktop-open.evidence.json` passed from clean source commit `34827ff4ecfdfe655e543b2635474308bb1ca53c` with MUSU `2`, repo Node `1`, owned WebView2 `6`, max one-core CPU `musu=0`, `node=0`, `webview2=0.08`, working set `504.02MB`, and private memory `330.43MB`. Go/no-go reports single-machine verified, runtime idle CPU `1/2`, runtime CPU scenario matrix `1/2`, public metadata ok, MSIX install ok, MSIX desktop entrypoint ok, and public release still No-Go. |
| 2026-06-01 v173 | Code and docs were re-indexed after the CPU attribution/current evidence refresh. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1160 files and 2165 symbols after Node command-line attribution, reachable-dashboard matrix tightening, current primary 4-state CPU matrix evidence, current single-machine smoke, current primary `desktop-open` CPU evidence, wiki/522/wiki/529/BETA/WIKI/WIKI_INDEX/qual-audit updates, and CoS memory note `2026-06-01_1605_kst_cpu_matrix_node_attribution_current_evidence.md`. Search terms include `repo_related_node`, `reachable_url`, `20260601-154503-HUGH_SECOND.runtime-cpu-scenario-matrix.json`, `20260601-155630-HUGH_SECOND.evidence.json`, `20260601-160102-HUGH_SECOND.desktop-open.evidence.json`, `MUSU_CPU_SCENARIO_ROUTE_OK_20260601_154503`, `webview2=0.08`, and `runtime CPU scenario matrix 1/2`. |
| 2026-06-01 v174 | Relay lease audit CLI was added and live `musu.pro` production auth was audited. | `musu relay leases --json` now queries `GET /api/v1/p2p/relay/lease` with optional `--session-id`, `--source-node-id`, `--target-node-id`, and bounded `--limit`, emits schema `musu.relay_leases.v1`, includes `owner_scope_verified`, and keeps `relay_transport_wired=false` plus `relay_default_data_path=false`. `musu relay status --json` now reports the cloud base URL through the shared cloud config helper. Live `https://musu.pro` diagnostics show `logged_in=true` and relay control-plane wiring, but `musu relay leases --json` fails with `p2p_control_auth_not_configured`; production P2P control auth must validate the runtime account/device token or a scoped control token before relay lease evidence is release-grade. The public site deploy question is also closed: Vercel production workflow run `26738950440` deployed `04929fd`, and live Playwright checks passed for scroll, favicon logo/browser icon, and `#24C8DB` accent on `/`, `/landing`, `/pricing`, and `/install` across desktop/mobile. |
| 2026-06-01 v175 | Code and docs were re-indexed after the relay lease audit CLI and live auth blocker docs. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1161 files and 2170 symbols after `musu.relay_leases.v1`, `owner_scope_verified`, `query_relay_leases`, live `p2p_control_auth_not_configured` documentation, public `musu.pro` deployment QA documentation, wiki/524/wiki/530/BETA/current-status/qual-audit updates, and CoS memory note `2026-06-01_1636_kst_relay_lease_audit_cli_live_auth_blocker.md`. Search terms include `musu relay leases --json`, `owner_scope_verified`, `p2p_control_auth_not_configured`, `26738950440`, `04929fd`, `relay_default_data_path=false`, and `relay_transport_wired=false`. |
| 2026-06-01 v176 | P2P control auth now supports SHA-256 runtime-token allowlisting. | `musu-bee/src/lib/p2pControlAuth.ts` accepts `MUSU_P2P_CONTROL_TOKEN_SHA256S` / `MUSU_P2P_CONTROL_TOKEN_SHA256` in addition to raw static control tokens, using timing-safe comparison and continuing to derive owner scope from the presented Bearer token hash. `scripts\windows\show-p2p-control-token-hash.ps1` computes the Vercel env value from `~\.musu\token` without printing the raw account token. Validation passed: 25 targeted P2P API/auth tests covering rendezvous, route evidence, relay lease, and hash allowlist; `npm run typecheck`; and the token-hash helper with a fake token. Remaining open work: deploy this `musu-bee/**` change, configure production `MUSU_P2P_CONTROL_TOKEN_SHA256S`, and re-run live `musu relay leases --json` until it returns an owner-scoped response instead of `p2p_control_auth_not_configured`. |
| 2026-06-01 v177 | Code and docs were re-indexed after P2P auth hash-allowlist wiring. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1164 files and 2177 symbols after `p2pControlAuth.test.ts`, `MUSU_P2P_CONTROL_TOKEN_SHA256S`, `show-p2p-control-token-hash.ps1`, wiki/522/wiki/524/wiki/530/BETA/CONFIG/WIKI/WIKI_INDEX updates, and CoS memory note `2026-06-01_1710_kst_p2p_control_auth_hash_allowlist.md`. Search terms include `sha256_bearer_token_allowlist`, `accepted_auth_modes`, `MUSU_P2P_CONTROL_TOKEN_SHA256`, `MUSU_P2P_CONTROL_TOKEN_SHA256S`, `raw_token_printed=false`, and `show-p2p-control-token-hash.ps1`. |
| 2026-06-01 v178 | P2P auth hash-allowlist code deployed to `musu.pro`; production env remains missing. | GitHub Actions for commit `b1c4378` passed: `Tests` run `26742743243`, `E2E Tests — musu-bee` run `26742743299`, and Vercel production deploy run `26742743319`. Live `musu relay leases --json` now returns `p2p_control_auth_not_configured` with `accepted_auth_modes=[]`, proving the new auth code is deployed but no raw token or SHA-256 allowlist is configured in production. Next required action is to set `MUSU_P2P_CONTROL_TOKEN_SHA256S` in the Vercel production environment using the value from `scripts\windows\show-p2p-control-token-hash.ps1 -Json`, then re-run `musu relay leases --json`. |
| 2026-06-01 v179 | Code and docs were re-indexed after recording the live P2P auth deploy/env check. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1165 files and 2177 symbols after GOAL/WIKI/WIKI_INDEX/wiki/522/wiki/530 updates and CoS memory note `2026-06-01_1718_kst_p2p_auth_deployed_env_missing.md`. Search terms include `b1c4378`, `26742743319`, `accepted_auth_modes=[]`, `p2p_control_auth_not_configured`, and `production env missing`. |
| 2026-06-01 v180 | Public site scroll/logo/accent was hardened with a focused regression test. | The public homepage and `PublicSiteShell` now share `musu-public-scroll-root`, which keeps public pages document-scrollable across desktop/mobile. Header/footer branding now uses `MusuLogo`, whose header size is tuned to the favicon-quality mark at `/images/favicon-header.png`. The homepage adds explicit `data-brand-accent=emerald` points and uses the favicon teal `#24C8DB` for the waitlist CTA and device dots. Added `playwright.public-site.config.ts` plus `e2e/public-site-scroll-brand.spec.ts` to assert `/` scrolls on desktop/mobile, has no horizontal overflow, renders the favicon mark, and exposes `--musu-color-brand-emerald=#24C8DB`. Validation passed: `npm run typecheck`, `npx playwright test --config=playwright.public-site.config.ts`, and `npm run build`. Deployment target remains the existing GitHub/Vercel production workflow on push to `main`; live `musu.pro` must be rechecked after this commit deploys. |
| 2026-06-01 v181 | Code and docs were re-indexed after the public site follow-up hardening. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1168 files and 2177 symbols after `MusuLogo` homepage/shell wiring, `musu-public-scroll-root`, missing brand RGB tokens, `public-site-scroll-brand.spec.ts`, `playwright.public-site.config.ts`, BETA/WIKI/WIKI_INDEX updates, and CoS memory note `2026-06-01_1722_kst_public_site_scroll_logo_followup.md`. Search terms include `public-site-scroll-brand`, `musu-public-scroll-root`, `MusuLogo`, `favicon-header.png`, `#24C8DB`, and `musu-color-brand-emerald`. |
| 2026-06-01 v182 | Public site follow-up commit deployed and live `musu.pro` QA passed. | Commit `674f501` (`Harden public site scroll and logo`) was pushed to `main`. GitHub Actions passed for `Tests` run `26743680160`, `E2E Tests — musu-bee` run `26743680172`, and `Deploy musu-bee to Vercel` run `26743680165`. Live Playwright checks against `https://musu.pro` with cachebuster `qa=674f501` passed on `/`, `/landing`, `/pricing`, and `/install` across desktop `1280x720` and mobile `390x844`: each route had `canScroll=true`, no horizontal overflow, `bodyOverflowY=auto`, `htmlOverflowY=auto`, `.musu-public-scroll-root=true`, favicon logo source containing `favicon-header.png`, and `--musu-color-brand-emerald=#24C8DB`. |
| 2026-06-01 v183 | Code and docs were re-indexed after recording live public-site deployment QA. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1168 files and 2177 symbols after GOAL/WIKI/WIKI_INDEX/BETA and CoS memory were updated with commit `674f501`, run IDs `26743680160`, `26743680172`, `26743680165`, and live `musu.pro` QA results. Search terms include `qa=674f501`, `canScroll=true`, `bodyOverflowY=auto`, `htmlOverflowY=auto`, `.musu-public-scroll-root=true`, and `favicon-header.png`. |
| 2026-06-01 v184 | Frontend polling hot-loop candidates were moved to the shared cancellable low-duty poller. | Removed direct `setInterval(` usage from `musu-bee/src` by migrating workflow run status, remote screen device refresh, agents surface refresh, and onboarding research polling to `useLowDutyPolling`. This adds AbortController cancellation on unmount/route change, hidden-tab throttling, in-flight request suppression, failure backoff, and terminal workflow stop behavior. Added `musu-bee/src/app/runtime-polling-contract.test.ts` so those surfaces cannot silently regress to direct intervals. Validation passed: `rg -n "setInterval\\(" musu-bee\\src -S` returned no matches, `npx tsx --test src/app/runtime-polling-contract.test.ts` passed 4/4, `npm run typecheck` passed, and `npm run build` passed. This is a busy-loop root-cause reduction; release CPU gates still require fresh 60s evidence on primary and second PC. |
| 2026-06-01 v185 | Code and docs were re-indexed after frontend polling hot-loop hardening. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1170 files and 2176 symbols after `runtime-polling-contract.test.ts`, `RunPanel`, `app/screen`, `useAgentsSurface`, `useOnboardingFlow`, BETA/WIKI/WIKI_INDEX/runtime CPU audit/runtime hardening roadmap updates, and CoS memory note `2026-06-01_1738_kst_frontend_polling_hot_loop_hardening.md`. Search terms include `no setInterval`, `useLowDutyPolling`, `frontend polling hot-loop`, `AbortSignal`, `runtime-polling-contract.test.ts`, and `setInterval\\(`. |
| 2026-06-01 v186 | Tauri desktop `Start Runtime` no longer waits on inherited stdout pipes indefinitely. | `musu-bee/src-tauri/src/lib.rs` now runs `musu up --json` through a timeout-bounded helper with temp-file stdout/stderr capture, `stdin=null`, a 45s deadline, and 200ms wait sleeps. This mirrors the safer Windows harness pattern and prevents the desktop shell from staying busy forever if a long-lived bridge child inherits output handles. Added a unit test proving command output capture without `.output()` pipes. Diagnostic process audit on `HUGH_SECOND` saw 16 machine-wide Node.js processes, but all were non-MUSU Codex/MCP/npx helpers; MUSU-owned Node `0`, repo-related orphan helpers `0`. The audit was not release evidence because no MUSU runtime was running and the bridge registry pointed at a dead PID. Validation passed: `cargo test --manifest-path .\musu-bee\src-tauri\Cargo.toml -j 1` passed 3/3 Tauri shell tests, and `rg -n "\.output\(\)|wait_with_output" musu-bee\src-tauri\src\lib.rs -S` found no direct output-pipe wait in the desktop shell. |
| 2026-06-01 v187 | Code and docs were re-indexed after Tauri desktop start-runtime hardening. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1171 files and 2184 symbols after `START_RUNTIME_TIMEOUT`, `run_command_with_timeout`, `timed_command_captures_stdout_without_output_pipes`, BETA/WIKI/WIKI_INDEX/runtime CPU audit/runtime hardening roadmap updates, and CoS memory note `2026-06-01_1751_kst_tauri_start_runtime_timeout_hardening.md`. Search terms include `musu up timed out`, `desktop Start Runtime busy`, `no .output()`, `machine-wide Node.js`, `MUSU-owned Node 0`, and `dead bridge PID`. |
| 2026-06-01 v188 | Tauri desktop status now cleans stale bridge registry entries before probing health. | `musu-bee/src-tauri/src/lib.rs` reads `~/.musu/services/bridge.json`, checks the recorded PID on Windows through `OpenProcess(PROCESS_QUERY_LIMITED_INFORMATION)`, removes the registry file when the PID is dead, and returns `bridge_url=null` with a stale-registry detail instead of probing a dead port. This turns the 17:53 KST audit failure class into desktop failure handling: a stale `bridge.json` no longer makes the shell look at an obsolete bridge endpoint before `Start Runtime`. Added tests for stale cleanup and live registry URL preservation. Validation passed: `cargo test --manifest-path .\musu-bee\src-tauri\Cargo.toml -j 1` passed 5/5 Tauri shell tests. |
| 2026-06-01 v189 | Code and docs were re-indexed after stale bridge registry cleanup. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1172 files and 2191 symbols after `bridge_registry_status`, `PROCESS_QUERY_LIMITED_INFORMATION`, stale registry cleanup tests, Cargo dependency updates, BETA/WIKI/WIKI_INDEX/runtime CPU audit/runtime hardening roadmap updates, and CoS memory note `2026-06-01_1807_kst_tauri_stale_bridge_registry_cleanup.md`. Search terms include `stale_bridge_registry_is_removed_before_status_probe`, `live_bridge_registry_returns_loopback_url`, `bridge_url=null`, `dead bridge PID`, and `stale bridge registry removed`. |
| 2026-06-01 v190 | `musu doctor --json` now reports the desktop background feature profile. | Added a `background` section to doctor output covering mDNS, IPv6 mDNS, Tailscale mDNS, VPN/virtual mDNS interfaces, clipboard polling, cloud registration, effective cloud heartbeat interval/floor, file watcher roots/writable state, and autonomous planner status. Doctor marks the background profile `ok` when optional hot-loop-prone features are off, and `warn` when opt-ins such as mDNS, clipboard sync, file sync, or planner are enabled so idle CPU evidence can explicitly include them. Validation passed: targeted doctor background unit tests 3/3, `cargo build --manifest-path .\musu-rs\Cargo.toml --bin musu -j 1`, `git diff --check`, and live `musu doctor --json` on `HUGH_SECOND` reported `background.status=ok`, mDNS/clipboard/file sync/planner off, `cloud_heartbeat_interval_sec=300`, and `cloud_heartbeat_floor_sec=60`. |
| 2026-06-01 v191 | Code and docs were re-indexed after doctor background profile wiring. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1173 files and 2200 symbols after `DoctorBackground`, `check_background_features`, doctor background tests, BETA/WIKI/WIKI_INDEX/runtime CPU audit/runtime hardening roadmap updates, and CoS memory note `2026-06-01_1835_kst_doctor_background_profile.md`. Search terms include `background.status=ok`, `cloud_heartbeat_interval_sec`, `MUSU_ENABLE_CLIPBOARD_SYNC`, `MUSU_MDNS_ENABLE_TAILSCALE`, `file_serve_root_count`, and `planner`. |
| 2026-06-01 v192 | Optional autonomous planner loop now has enforced idle-budget bounds. | `musu-rs/src/brain/planner.rs` floors `MUSU_PLANNER_INTERVAL_SEC` at 60s, clamps `MUSU_PLANNER_COMMAND_TIMEOUT_SEC` to 5s..120s, and runs the crawler through timeout-bounded `tokio::process::Command` with `stdin=null`, piped output, and `kill_on_drop(true)`. `musu doctor --json` now reports `planner_interval_sec`, planner interval floor, planner command timeout, floor, and ceiling. Live bad-env verification with `MUSU_ENABLE_PLANNER=1`, `MUSU_PLANNER_INTERVAL_SEC=0`, and `MUSU_PLANNER_COMMAND_TIMEOUT_SEC=9999` returned `planner_interval_sec=60`, `planner_command_timeout_sec=120`, and `background.status=warn`. Validation passed: planner tests 2/2, doctor background tests 4/4, `cargo build --manifest-path .\musu-rs\Cargo.toml --bin musu -j 1`, and `git diff --check`. |
| 2026-06-01 v193 | Code and docs were re-indexed after planner loop budget hardening. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1174 files and 2207 symbols after `PLANNER_MIN_INTERVAL_SEC`, planner timeout clamping, doctor planner budget fields, BETA/WIKI/WIKI_INDEX/runtime CPU audit/runtime hardening roadmap/current status updates, and CoS memory note `2026-06-01_1856_kst_planner_loop_budget_hardening.md`. Search terms include `MUSU_PLANNER_COMMAND_TIMEOUT_SEC`, `planner_command_timeout_sec`, `planner_interval_sec`, `planner crawler timed out`, `planner_command_timeout_is_bounded`, and `doctor_background_floors_planner_loop_budget`. |
| 2026-06-01 v194 | Live `musu.pro` scroll/logo/accent deployment was rechecked and cloud heartbeat hardware probes were bounded. | `https://musu.pro`, `/privacy`, and `/support` returned HTTP 200. Live browser QA on desktop `1280x720` and mobile `390x844` confirmed homepage scroll movement, no horizontal overflow, favicon-header logo source, `data-brand-accent=emerald`, and `--musu-color-brand-emerald=#24C8DB`, so no manual web deploy remains for that fix. `musu-rs/src/peer/hardware.rs` now routes Windows PowerShell/WMIC, macOS `sysctl`, and `nvidia-smi` probes through a 5s `command_stdout_with_timeout()` helper with `stdin=null`, stderr discarded, and fallback metadata on failure/timeout. Validation passed: hardware tests 2/2, `cargo build --manifest-path .\musu-rs\Cargo.toml --bin musu -j 1`, `cargo fmt --check`, and `git diff --check`. |
| 2026-06-01 v195 | Code and docs were re-indexed after hardware probe timeout hardening. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1175 files and 2212 symbols after live `musu.pro` deploy recheck, `HARDWARE_PROBE_TIMEOUT`, `command_stdout_with_timeout`, hardware probe timeout tests, BETA/WIKI/WIKI_INDEX/runtime CPU audit/runtime hardening roadmap/current status updates, and CoS memory note `2026-06-01_1919_kst_hardware_probe_timeout_hardening.md`. Search terms include `hardware-probe-ok`, `nvidia-smi`, `Win32_PhysicalMemory`, `machdep.cpu.brand_string`, `live-brand-scroll-desktop`, and `#24C8DB`. |
| 2026-06-01 v196 | Post-push go/no-go was refreshed for commit `4f099bf`. | `Tests` run `26749151136` passed for `4f099bf`. Local go/no-go summary on clean manifest commit `4f099bf701664c255c37f3a875fb094a7d9b9811` reports `ready_for_public_desktop_release=false`, public metadata ok, MSIX install ok, support false, Store false, multi-device false, and stale evidence reset: `single_machine_verified=false`, runtime idle CPU `0/2`, runtime CPU scenario matrix `0/2`. The next evidence step is not just second-PC capture; primary single-machine smoke, primary desktop-open CPU, and primary 4-state CPU matrix must also be refreshed after this new source commit before final handoff. |
| 2026-06-01 v197 | Code and docs were re-indexed after recording the post-push No-Go state. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1175 files and 2212 symbols after GOAL/WIKI/WIKI_INDEX/wiki/522/wiki/529/CoS memory updates recorded `4f099bf`, `26749151136`, `single_machine_verified=false`, runtime idle CPU `0/2`, and runtime CPU scenario matrix `0/2`. |
| 2026-06-01 v198 | Primary smoke and CPU evidence were refreshed after the hardware-probe commit. | Started `next start -p 3001`, repaired the stale bridge registry by running `musu up --json`, and refreshed primary evidence on `HUGH_SECOND`. Single-machine evidence passes at `docs\evidence\single-machine\1.15.0-rc.1\20260601-194130-HUGH_SECOND.evidence.json` with dashboard task `60cb73e5-ea3c-42c8-bcd6-41f09e618a16`, bridge `http://127.0.0.1:4752`, and CLI route checked. Clean `desktop-open` CPU evidence passes at `docs\evidence\runtime-idle-cpu\1.15.0-rc.1\20260601-194410-HUGH_SECOND.desktop-open.evidence.json` from commit `42cf892e41517f6c6d3a1066c6b0b4609a59f907`: 60.056s, `git_dirty=false`, `musu=2`, repo Node `1`, owned WebView2 `6`, max one-core CPU `musu=0`, `node=0.03`, `webview2=0.08`, working set `506.72MB`, no budget violations. Clean 4-state CPU matrix passes at `docs\evidence\runtime-cpu-scenarios\1.15.0-rc.1\20260601-194528-HUGH_SECOND.runtime-cpu-scenario-matrix.json`; all four required scenarios passed 60s samples and post-route token `MUSU_CPU_SCENARIO_ROUTE_OK_20260601_194528`. Go/no-go now reports single-machine true, runtime idle CPU `1/2` with `HUGH_SECOND`, runtime CPU matrix `1/2`, public metadata ok, MSIX install ok, support false, Store false, and public release still No-Go. |
| 2026-06-01 v199 | Code, docs, and refreshed evidence were re-indexed. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1181 files and 2212 symbols after primary smoke evidence, primary desktop-open CPU evidence, primary 4-state CPU matrix evidence, BETA/wiki/522/wiki/529/WIKI/WIKI_INDEX updates, and CoS memory note `2026-06-01_1950_kst_primary_smoke_cpu_evidence_refresh.md`. Search terms include `20260601-194130-HUGH_SECOND`, `20260601-194410-HUGH_SECOND.desktop-open`, `20260601-194528-HUGH_SECOND.runtime-cpu-scenario-matrix`, `60cb73e5-ea3c-42c8-bcd6-41f09e618a16`, and `MUSU_CPU_SCENARIO_ROUTE_OK_20260601_194528`. |
| 2026-06-01 v200 | Frontend polling hardening was extended to dashboard and node panel refresh loops. | `DashboardClient.tsx` and `NodePanel.tsx` moved from hand-written visibility/timer refresh loops to shared `useLowDutyPolling`. Dashboard agents/tasks/watchdog/runs refresh and node panel nodes/registry/discovery refresh now get AbortController cancellation, in-flight suppression, hidden-tab throttling, and failure backoff. `runtime-polling-contract.test.ts` now covers six surfaces. Validation passed: 6/6 polling contract tests, `npm run typecheck`, `npm run build`, `git diff --check`, and no direct `setInterval(` matches in `musu-bee/src`. Because this is a source change, previous CPU evidence is no longer release-current until primary and second-PC CPU samples are refreshed after the commit. |
| 2026-06-01 v201 | Code and docs were re-indexed after dashboard/node polling hardening. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1182 files and 2212 symbols after `DashboardClient.tsx`, `NodePanel.tsx`, polling contract tests, BETA/current-status/runtime CPU audit/runtime hardening roadmap/WIKI updates, and CoS memory note `2026-06-01_2010_kst_dashboard_node_polling_hardening.md`. Search terms include `dashboard main refresh`, `node panel registry/discovery refresh`, `useLowDutyPolling`, `runtime-polling-contract.test.ts`, `no setInterval`, and `frontend busy-loop risk`. |
| 2026-06-01 v202 | Post-polling-hardening go/no-go was refreshed for commit `a1082dec`. | Clean go/no-go on `a1082deca7cf33e16556b588181e56a531e519c3` reports `ready_for_public_desktop_release=false`, `manifest_dirty=false`, public metadata true, MSIX install true, MSIX desktop entrypoint true, process ownership true, startup single-instance true, but `single_machine_verified=false`, runtime idle CPU `0/2`, runtime CPU scenario matrix `0/2`, multi-device false, support false, and Store false. The drop from `1/2` to `0/2` is expected source freshness after the frontend polling source commit; next evidence step is to refresh primary smoke, primary `desktop-open` CPU, primary 4-state matrix, then import second-PC CPU/matrix/route evidence. |
| 2026-06-01 v203 | Code and docs were re-indexed after recording the post-polling No-Go state. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1182 files and 2213 symbols after GOAL v202, current-status, runtime CPU audit, WIKI/WIKI_INDEX, and the CoS memory note were updated with commit `a1082dec`, `manifest_dirty=false`, `single_machine_verified=false`, runtime idle CPU `0/2`, and runtime CPU scenario matrix `0/2`. |
| 2026-06-01 v204 | Public-site common-shell accent coverage and current primary evidence were refreshed after the polling hardening source change. | `PublicSiteShell.tsx` now marks the shared emerald `Open App` CTA with `data-brand-accent="emerald"`, and `public-site-scroll-brand.spec.ts` now checks `/`, `/landing`, `/pricing`, and `/install` on desktop/mobile; local Playwright passed 8/8 and `npm run typecheck` passed. Current single-machine smoke passes at `docs\evidence\single-machine\1.15.0-rc.1\20260601-203715-HUGH_SECOND.evidence.json` with dashboard task `4345c783-ddc2-4f7c-9328-6a721e7b3f1c`. Current primary desktop-open CPU passes at `docs\evidence\runtime-idle-cpu\1.15.0-rc.1\20260601-203537-HUGH_SECOND.desktop-open.evidence.json` with `musu=0`, `node=0.03`, `webview2=0.05` max one-core CPU and no hot processes. Current primary 4-state matrix passes at `docs\evidence\runtime-cpu-scenarios\1.15.0-rc.1\20260601-203835-HUGH_SECOND.runtime-cpu-scenario-matrix.json` with route token `MUSU_CPU_SCENARIO_ROUTE_OK_20260601_203835`; `desktop-open` peaks at `node=0.18`, `webview2=0.18`. The first desktop-open sample also showed stale packaged desktop shells can accumulate under repeated manual launches, so desktop window reactivation/single-instance hardening remains open. |
| 2026-06-01 v205 | Code, docs, and refreshed primary evidence were re-indexed. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1191 files and 2213 symbols after the public-site common-shell accent follow-up, latest single-machine/CPU/matrix evidence, BETA/current-status/runtime CPU audit/runtime hardening roadmap/qual audit/WIKI/WIKI_INDEX updates, and CoS memory note `2026-06-01_2043_kst_public_site_primary_evidence_refresh.md`. Search terms include `20260601-203715-HUGH_SECOND`, `20260601-203537-HUGH_SECOND.desktop-open`, `20260601-203835-HUGH_SECOND.runtime-cpu-scenario-matrix`, `MUSU_CPU_SCENARIO_ROUTE_OK_20260601_203835`, `data-brand-accent="emerald"`, and `desktop shell accumulation`. |
| 2026-06-01 v206 | Clean go/no-go was rerun after the post-polling primary evidence documentation commit. | On commit `1ab5a824be1291ac56667bc331474a7d63864d29`, go/no-go reports `ready=false`, `single_machine=true`, runtime idle CPU `1/2` with `HUGH_SECOND`, runtime CPU scenario matrix `1/2` with `HUGH_SECOND`, public metadata true, MSIX install true, MSIX desktop entrypoint true, process ownership true, startup single-instance true, multi-device false, support false, Store false, and `manifest_dirty=false`. |
| 2026-06-01 v207 | `musu.pro` deploy workflow was hardened and live public-site QA passed. | Vercel production run `26753317276` for commit `96303af352c489429bbd50d4a54f09fce1bb65b1` was stuck and canceled. Commit `659503846c30fcfb1d1c8836c94b27d3464b7d96` added workflow job/step timeouts and switched deploy to `vercel deploy --prebuilt --yes`; replacement Vercel run `26753908889` passed and `Tests` run `26753908911` passed. Live Playwright QA against `https://musu.pro` with `qa=65950384` passed on `/`, `/landing`, `/pricing`, and `/install` for desktop/mobile: scroll works, no horizontal overflow, favicon-header logo renders, `.musu-public-scroll-root` exists, and the emerald accent is `#24C8DB` / `36,200,219`. |
| 2026-06-01 v208 | Final primary evidence was refreshed after deploy workflow hardening. | Single-machine smoke passes at `docs\evidence\single-machine\1.15.0-rc.1\20260601-211031-HUGH_SECOND.evidence.json` with dashboard task `6a339c65-4d1a-46f3-894a-5a50cc69cd0e`, bridge `http://127.0.0.1:4752`, and output `MUSU_RELEASE_SMOKE_OK_20260601_211010`. Primary `desktop-open` CPU evidence passes at `docs\evidence\runtime-idle-cpu\1.15.0-rc.1\20260601-211132-HUGH_SECOND.desktop-open.evidence.json` from clean commit `a0184e89851d7ac99e1162a301f9219104a4df04` with MUSU `2`, repo Node `1`, owned WebView2 `6`, max one-core CPU `musu=0`, `node=0`, `webview2=0.23`, working set `506.71MB`, and no hot processes. Primary 4-state matrix passes at `docs\evidence\runtime-cpu-scenarios\1.15.0-rc.1\20260601-211252-HUGH_SECOND.runtime-cpu-scenario-matrix.json` with route token `MUSU_CPU_SCENARIO_ROUTE_OK_20260601_211252`. Public release remains No-Go until second-PC CPU/matrix, release-grade route, production P2P env/live verification, `musu@musu.pro`, Store evidence, and relay/tunnel transport are complete. |
| 2026-06-01 v209 | Code and docs were re-indexed after deploy workflow/final evidence documentation. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1197 files and 2213 symbols after `musu.pro` deploy workflow hardening documentation, live `qa=65950384` browser QA, final primary single-machine smoke, final primary desktop-open CPU evidence, final primary 4-state matrix evidence, BETA/current-status/runtime CPU audit/runtime hardening roadmap/qual audit/WIKI/WIKI_INDEX updates, and CoS memory note `2026-06-01_2122_kst_deploy_workflow_final_evidence_refresh.md`. Search terms include `26753317276`, `26753908889`, `26753908911`, `20260601-211031-HUGH_SECOND`, `20260601-211132-HUGH_SECOND.desktop-open`, `20260601-211252-HUGH_SECOND.runtime-cpu-scenario-matrix`, `MUSU_CPU_SCENARIO_ROUTE_OK_20260601_211252`, and `qa=65950384`. |
| 2026-06-01 v210 | Hosted P2P control-plane live evidence became a go/no-go gate. | Added `scripts\windows\record-p2p-control-plane-evidence.ps1` and `scripts\windows\verify-p2p-control-plane-evidence.ps1`; `write-release-go-no-go.ps1` now reports `p2p_control_plane_verified` and adds a `p2p-control-plane` blocker when live owner-scoped relay lease queries fail. Final packet verification and desktop release readiness script coverage now include the new scripts. Current live evidence `docs\evidence\p2p-control-plane\1.15.0-rc.1\20260601-214149-musu.pro.evidence.json` records `musu relay status --json` and `musu relay leases --json`: relay status is logged in and wired, but relay lease query still reports `ok=false`, `owner_scope_verified=false`, `owner_scoped=false`, and `p2p_control_auth_not_configured` with no accepted auth modes. The next action is production `MUSU_P2P_CONTROL_TOKEN_SHA256S` configuration and re-recording passing evidence. |
| 2026-06-01 v211 | Code and docs were re-indexed after the P2P control-plane live gate. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1203 files and 2213 symbols after the P2P recorder/verifier scripts, failing live `musu.pro` evidence, go/no-go blocker wiring, BETA/current-status/runtime hardening/qual audit/WIKI/WIKI_INDEX updates, and CoS memory note `2026-06-01_2141_kst_p2p_control_plane_live_gate.md`. Search terms include `musu.p2p_control_plane_live_evidence.v1`, `p2p_control_plane_verified`, `p2p-control-plane`, `20260601-214149-musu.pro`, `owner_scope_verified=false`, and `MUSU_P2P_CONTROL_TOKEN_SHA256S`. |
| 2026-06-01 v212 | Post-P2P-gate clean go/no-go was recorded for `a6e41609`. | After commit `a6e41609d1c9ceaaf13ce73119f25e62471bfb5b`, clean go/no-go reports `ready=false`, `manifest_dirty=false`, `single_machine=false`, runtime idle CPU `0/2`, runtime CPU scenario matrix `0/2`, `p2p_control_plane_verified=false`, relay leases `ok=false`, and `owner_scope_verified=false`. Blockers are `single-machine`, `multi-device`, `runtime-idle-cpu`, `runtime-cpu-scenario-matrix`, `support-mailbox`, `store-release`, and `p2p-control-plane`. The evidence reset is expected because the P2P gate commit changed release scripts; primary smoke/CPU/matrix must be refreshed again on the final commit before second-PC evidence can close the runtime gates. |
| 2026-06-01 v213 | Code and docs were re-indexed after the post-P2P-gate No-Go documentation. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1203 files and 2213 symbols after GOAL v212, current-status/BETA/qual-audit/runtime-hardening/WIKI/WIKI_INDEX updates, and CoS memory updates recorded the clean `a6e41609` No-Go state. Search terms include `a6e41609`, `single_machine=false`, `runtime idle CPU 0/2`, `runtime CPU scenario matrix 0/2`, `p2p_control_plane_verified=false`, and `owner_scope_verified=false`. |
| 2026-06-01 v214 | Primary smoke, desktop idle CPU, and 4-state CPU matrix were refreshed after the P2P gate commits. | Single-machine evidence passes at `docs\evidence\single-machine\1.15.0-rc.1\20260601-221225-HUGH_SECOND.evidence.json` with dashboard task `927874c7-ce4d-4eb1-a84d-1bd7517ff844`, bridge `http://127.0.0.1:4752`, and CLI route checked. Production-dashboard `desktop-open` CPU evidence passes at `docs\evidence\runtime-idle-cpu\1.15.0-rc.1\20260601-221918-HUGH_SECOND.desktop-open.evidence.json` from clean commit `bcfdbf3222166aca05bf3196184311180375c5f9`: MUSU `2`, repo Node `1`, owned WebView2 `6`, max one-core CPU `musu=0`, `node=0`, `webview2=0.13`, working set `469.28MB`, and no hot processes. Primary 4-state matrix passes at `docs\evidence\runtime-cpu-scenarios\1.15.0-rc.1\20260601-222043-HUGH_SECOND.runtime-cpu-scenario-matrix.json` from clean commit `278372551f12f54816db9a0f47e677e2ac0125da`, with route token `MUSU_CPU_SCENARIO_ROUTE_OK_20260601_222043`. Clean go/no-go on `5b8650f084a0df9cf5cabde77af31dd11b366c0a` now reports `single_machine=true`, runtime idle CPU `1/2`, runtime CPU scenario matrix `1/2`, `manifest_dirty=false`, `p2p_control_plane_verified=false`, and `ready=false`. |
| 2026-06-01 v215 | Code, docs, and refreshed primary evidence were re-indexed. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1209 files and 2213 symbols after the latest single-machine, desktop-open CPU, runtime CPU matrix evidence, current-status/BETA/qual-audit/runtime-hardening/WIKI/WIKI_INDEX updates, and CoS memory note `2026-06-01_2226_kst_primary_evidence_refresh_after_p2p_gate.md`. Search terms include `20260601-221225-HUGH_SECOND`, `20260601-221918-HUGH_SECOND.desktop-open`, `20260601-222043-HUGH_SECOND.runtime-cpu-scenario-matrix`, `MUSU_CPU_SCENARIO_ROUTE_OK_20260601_222043`, `webview2=0.13`, and `runtime CPU scenario matrix 1/2`. |
| 2026-06-01 v216 | Public site scroll hardening and manual production redeploy support were added. | Live `https://musu.pro` was checked before this change and already allowed actual scroll movement on desktop/mobile with the favicon-header logo and `--musu-color-brand-emerald=#24C8DB`. The remaining deployment gap was operational: GitHub/Vercel deploys had no `workflow_dispatch`, and local Vercel CLI access could not see the `musu.pro` project/domain under the default scope. `.github/workflows/deploy-musu-bee.yml` now supports manual dispatch, and `.musu-public-scroll-root` now explicitly opts into viewport scrolling/touch panning via `height:auto`, `max-height:none`, `touch-action:pan-y`, `-webkit-overflow-scrolling:touch`, plus `html/body:has(.musu-public-scroll-root)` overflow rules. Validation passed: `npm run typecheck`, `npx playwright test --config=playwright.public-site.config.ts` (8/8), `npm run build`, and `git diff --check`. Public desktop release remains No-Go because this does not close second-PC runtime, release-grade route, production P2P auth, support inbox, Store, or relay/tunnel gates. |
| 2026-06-01 v217 | Code and docs were re-indexed after public-site manual deploy hardening. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1210 files and 2213 symbols after `workflow_dispatch`, `.musu-public-scroll-root` touch/viewport scroll hardening, current-status/WIKI/WIKI_INDEX updates, and CoS memory note `2026-06-01_2248_kst_public_site_manual_deploy_scroll_hardening.md`. Search terms include `workflow_dispatch`, `musu-public-scroll-root`, `touch-action:pan-y`, `-webkit-overflow-scrolling`, `favicon-header.png`, `#24C8DB`, and `manual production redeploy`. |
| 2026-06-01 v218 | Public site manual-deploy hardening was deployed to `musu.pro` and live QA passed. | Commit `b08ed746` was pushed to `main`; GitHub `Tests` run `26759256487`, `E2E Tests - musu-bee` run `26759256574`, and Vercel production deploy run `26759256616` all passed. Live Playwright QA against `https://musu.pro` with `qa=b08ed746` passed on `/`, `/landing`, `/pricing`, and `/install` across desktop `1280x720` and mobile `390x844`: actual scroll movement after `window.scrollTo`, no horizontal overflow, `.musu-public-scroll-root=true`, favicon-header logo source, `data-brand-accent=emerald`, and `--musu-color-brand-emerald=#24C8DB`. |
| 2026-06-01 v219 | Final go/no-go after the public-site deploy remains No-Go and resets primary runtime evidence freshness. | `write-release-go-no-go.ps1 -Json` on commit `bce29066` reports `ready_for_public_desktop_release=false`, `manifest_dirty=false`, `public_metadata_ok=true`, `msix_install_verified=true`, `msix_desktop_entrypoint_verified=true`, but `single_machine_verified=false`, runtime idle CPU `0/2`, runtime CPU scenario matrix `0/2`, `p2p_control_plane_verified=false`, support false, and Store false. The drop from the previous `single_machine=true` / runtime `1/2` is expected because commit `b08ed746` changed public-site CSS and deploy workflow source after the last primary evidence. Next release-gate work is to refresh primary smoke, primary desktop-open CPU, and primary 4-state matrix on the new source, then continue second-PC runtime/route evidence and live P2P auth. |
| 2026-06-01 v220 | Primary smoke, desktop-open CPU, and 4-state CPU matrix were refreshed after the final public-site/docs source change. | Single-machine evidence `docs\evidence\single-machine\1.15.0-rc.1\20260601-231612-HUGH_SECOND.evidence.json` passed with dashboard output `MUSU_RELEASE_SMOKE_OK_20260601_231552`, dashboard task `900260dc-d0d1-4ac6-9503-0001b4a2f451`, bridge `http://127.0.0.1:4752`, and CLI route checked. Desktop-open CPU evidence `docs\evidence\runtime-idle-cpu\1.15.0-rc.1\20260601-231939-HUGH_SECOND.desktop-open.evidence.json` passed from clean commit `afd1ceab2db2b234c6c4d9f50a5a165830bfae65` with MUSU `2`, repo Node `1`, owned WebView2 `6`, max one-core CPU `musu=0`, `node=0`, `webview2=0.1`, working set `510.13MB`, and no hot processes. Runtime CPU matrix `docs\evidence\runtime-cpu-scenarios\1.15.0-rc.1\20260601-233638-HUGH_SECOND.runtime-cpu-scenario-matrix.json` passed from clean commit `5434b4caf0c5e0f5a8dd345e775cb45259e4ebbd` with all four scenarios, dashboard URL launched, and route token `MUSU_CPU_SCENARIO_ROUTE_OK_20260601_233638`. |
| 2026-06-01 v221 | Code and docs were re-indexed after wiki/531 and current evidence documentation. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1217 files and 2213 symbols after current primary evidence, wiki/531 qualitative/code audit, current-status/runtime-hardening/runtime-CPU audit updates, WIKI/WIKI_INDEX/BETA updates, and CoS memory note `2026-06-01_2345_kst_current_evidence_code_audit_roadmap.md`. Search terms include `20260601-231612-HUGH_SECOND`, `20260601-231939-HUGH_SECOND.desktop-open`, `20260601-233638-HUGH_SECOND.runtime-cpu-scenario-matrix`, `MUSU_CPU_SCENARIO_ROUTE_OK_20260601_233638`, `wiki/531`, and `machine-wide Node.js`. |
| 2026-06-02 v222 | Packaged desktop repeated activation was reproduced and source hardening was added. | Current installed package still has the desktop shell duplicate-launch bug: repeated Start-menu activation of `Yellowhama.MUSU_ygcjq669as2b6!MUSU` expanded one `musu-desktop.exe` shell to three PIDs (`5744`, `27512`, `31496`). Source now uses `tauri-plugin-single-instance = 2.4.2` and focuses/unminimizes/shows the existing `main` window on repeat activation. `cargo test --manifest-path .\musu-bee\src-tauri\Cargo.toml -j 1` passed 5/5. Duplicate desktop shells from the repro were stopped. This source change makes previous packaged desktop evidence stale until a fresh MSIX is built/installed and repeated-activation/process-ownership/CPU evidence is rerun. |
| 2026-06-02 v223 | Public site source was updated for favicon-mark logo, explicit scroll rules, and emerald accent before `musu.pro` deploy. | `MusuLogo` now renders the favicon mark without the wordmark, `.musu-public-scroll-root` has explicit width/auto height/`100svh`/`100dvh`/`overflow-y:auto`/touch scrolling rules, and the homepage `Open App` CTA uses the emerald `#24C8DB` point color. Local validation passed `npm run typecheck`, `npx playwright test --config=playwright.public-site.config.ts` 8/8, `npm run build`, `cargo test --manifest-path .\musu-bee\src-tauri\Cargo.toml -j 1` 5/5, and `git diff --check`. The next operational step is push, Vercel production deploy to `musu.pro`, and live QA on `/`, `/landing`, `/pricing`, and `/install`. |
| 2026-06-02 v224 | Code and docs were re-indexed after desktop single-instance and public-site follow-up. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1218 files and 2213 symbols after Tauri single-instance source, favicon-mark logo/scroll/accent site source, current-status/runtime-hardening/BETA/WIKI/WIKI_INDEX updates, and CoS memory note `2026-06-02_0017_kst_desktop_single_instance_public_site_followup.md`. Search terms include `tauri-plugin-single-instance`, `desktop repeated activation`, `favicon mark only`, `scrollbar-gutter: stable`, `100svh`, `#24C8DB`, and `musu.pro deploy verified`. |
| 2026-06-02 v225 | Public-site follow-up deployed to `musu.pro` and production QA passed. | Commit `0ed3673a27b058ad1fc5d050434bf8435cb21e5d` passed Vercel production deploy run `26764307713`, GitHub `Tests` run `26764309477`, and `E2E Tests - musu-bee` run `26764310368`. Production Playwright QA against `https://musu.pro` passed 8/8 on `/`, `/landing`, `/pricing`, and `/install`, verifying desktop/mobile scroll, no horizontal overflow, favicon-mark logo, and `#24C8DB` accent. The desktop single-instance fix remains unproven for users until a fresh MSIX is built and installed. |
| 2026-06-02 v226 | Code and docs were re-indexed after live `musu.pro` deploy verification. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1218 files and 2213 symbols after Vercel/GitHub run IDs, production Playwright QA results, WIKI/WIKI_INDEX/current-status/runtime-hardening/BETA updates, and CoS memory update. Search terms include `0ed3673a`, `26764307713`, `26764309477`, `26764310368`, `production Playwright QA 8/8`, and `musu.pro deploy verified`. |
| 2026-06-02 v227 | Packaged desktop repeated activation became a release gate and current installed package failed it. | Added `scripts\windows\audit-musu-desktop-single-instance.ps1` with schema `musu.desktop_single_instance_audit.v1`, wired `desktop_single_instance_verified` into go/no-go, final handoff status, final operator packet generation/verification, and desktop release readiness script coverage. Current installed package `Yellowhama.MUSU_1.15.0.0_x64__ygcjq669as2b6` fails: three Start-menu/AppsFolder activations expanded one `musu-desktop.exe` to four (`new_desktop_shell=3`), evidence `.local-build\desktop-single-instance\musu-desktop-single-instance-20260602-005439-HUGH_SECOND.json`, `ok=false`; cleanup stopped the extras. Next step is fresh MSIX build/install from current source, then rerun desktop single-instance, process ownership, smoke, CPU, and matrix evidence. |
| 2026-06-02 v228 | Code and docs were re-indexed after the desktop single-instance gate. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1221 files and 2213 symbols after wiki/532, the desktop single-instance audit script, go/no-go/handoff/packet wiring, current-status/runtime-hardening/BETA/WIKI/WIKI_INDEX updates, and CoS memory note `2026-06-02_0055_kst_desktop_single_instance_gate.md`. Search terms include `musu.desktop_single_instance_audit.v1`, `desktop_single_instance_verified`, `desktop-single-instance`, `audit-musu-desktop-single-instance.ps1`, `shell:AppsFolder`, and `20260602-005439-HUGH_SECOND`. |
| 2026-06-02 v229 | Fresh release MSIX primary evidence was recorded after the desktop single-instance source fix. | Release MSIX build/install succeeded with `CARGO_BUILD_JOBS=1`; installed package `Yellowhama.MUSU_1.15.0.0_x64__ygcjq669as2b6` now passes packaged desktop repeated activation at `docs\evidence\desktop-single-instance\1.15.0-rc.1\20260602-014803-HUGH_SECOND.evidence.json` with three activations and one desktop shell. Current primary single-machine smoke `20260602-015347-HUGH_SECOND`, desktop-open CPU `20260602-015358-HUGH_SECOND.desktop-open`, four-state CPU matrix `20260602-015510-HUGH_SECOND`, and process ownership `20260602-020031-HUGH_SECOND` all pass on commit `2e97d135538f063252577c49762f8018bc366843`. This closes the primary installed desktop single-instance blocker, but public release remains No-Go because second-PC CPU/matrix, release-grade multi-device route, live `musu.pro` P2P control-plane auth, `musu@musu.pro`, and Store evidence are still missing. |
| 2026-06-02 v230 | Code and docs were re-indexed after fresh MSIX primary evidence documentation. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1230 files and 2213 symbols after wiki/533, fresh MSIX evidence files, current-status/runtime-hardening/BETA/WIKI/WIKI_INDEX updates, and CoS memory note `2026-06-02_0205_kst_fresh_msix_primary_evidence.md`. Search terms include `wiki/533`, `fresh MSIX primary evidence`, `20260602-014803-HUGH_SECOND`, `20260602-015347-HUGH_SECOND`, `20260602-015358-HUGH_SECOND.desktop-open`, `20260602-015510-HUGH_SECOND.runtime-cpu-scenario-matrix`, `20260602-020031-HUGH_SECOND`, and `MUSU_CPU_SCENARIO_ROUTE_OK_20260602_015510`. |
| 2026-06-02 v231 | Direct `musu up --json` PowerShell pipe hang was fixed in source and live `musu.pro` scroll/logo/accent was rechecked. | `spawn_bridge_process()` now clears Windows standard-handle inheritance before launching the bridge and uses `DETACHED_PROCESS | CREATE_NEW_PROCESS_GROUP | CREATE_NO_WINDOW`; bridge stdout/stderr remain redirected to `~/.musu/logs/bridge.log`. Validation passed `cargo check --manifest-path .\musu-rs\Cargo.toml --bin musu -j 1`, `cargo build --manifest-path .\musu-rs\Cargo.toml --bin musu -j 1`, `cargo fmt --manifest-path .\musu-rs\Cargo.toml --check`, `git diff --check`, and the fresh debug command `.\musu-rs\target\debug\musu.exe up --json --timeout-sec 10 | ConvertFrom-Json` returned `ok=true`, `bridge_started=true`, bridge PID `37284`, bridge status `ok`, URL `http://127.0.0.1:5692`, without hanging. PID `37284` was stopped after verification. Live `https://musu.pro` QA passed 8/8 on `/`, `/landing`, `/pricing`, and `/install` for desktop/mobile scroll, no horizontal overflow, favicon-header logo, `.musu-public-scroll-root`, and `#24C8DB` accent. Public release remains No-Go pending packaged CLI pipe proof, second-PC CPU/matrix, release-grade route, live P2P auth, `musu@musu.pro`, and Store evidence. |
| 2026-06-02 v232 | Code and docs were re-indexed after wiki/534 CLI pipe/site deploy audit. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1232 files and 2214 symbols after `clear_standard_handle_inheritance`, `DETACHED_PROCESS`, wiki/534, product/network boundary notes, current-status/runtime-hardening/BETA/WIKI/WIKI_INDEX updates, and CoS memory note `2026-06-02_0242_kst_cli_pipe_site_deploy_audit.md`. Search terms include `wiki/534`, `ConvertFrom-Json`, `stdout pipe`, `bridge_pid 37284`, `http://127.0.0.1:5692`, `musu.pro live QA 8/8`, and `packaged CLI pipe proof`. |
| 2026-06-02 v233 | Clean go/no-go was rerun after the CLI pipe hardening commit. | The clean post-commit run reports `ready_for_public_desktop_release=false`, `manifest_dirty=false`, `local_artifacts_ready=true`, `public_metadata_ok=true`, `msix_install_verified=true`, `msix_desktop_entrypoint_verified=true`, `desktop_single_instance_verified=true`, `startup_single_instance_verified=true`, `process_ownership_verified=true`, but `single_machine_verified=false`, runtime idle CPU `0/2`, runtime CPU scenario matrix `0/2`, `p2p_control_plane_verified=false`, `support_mailbox_verified=false`, and `store_release_verified=false`. The drop from the prior fresh MSIX primary evidence is expected because the Rust CLI source changed after that package/evidence. Next step is fresh MSIX build/install with this fix, packaged CLI pipe proof, then primary smoke/CPU/matrix evidence refresh. |
| 2026-06-02 v234 | Code and docs were re-indexed after recording the clean post-commit No-Go state. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1232 files and 2214 symbols after GOAL v233, wiki/534, WIKI/WIKI_INDEX/BETA/current-status/runtime-hardening updates, and CoS memory note `2026-06-02_0242_kst_cli_pipe_site_deploy_audit.md` were updated with `manifest_dirty=false`, `single_machine_verified=false`, runtime idle CPU `0/2`, and runtime CPU scenario matrix `0/2`. |
| 2026-06-02 v235 | Fresh MSIX packaged CLI/runtime evidence was recorded after the pipe fix. | Fresh local-sideload package `Yellowhama.MUSU_1.15.0.0_x64__ygcjq669as2b6` passes explicit WindowsApps CLI pipe evidence at `docs\evidence\cli-pipe\1.15.0-rc.1\20260602-032728-HUGH_SECOND.packaged-cli-pipe.evidence.json` with `returned_without_hang=true`, duration `7544ms`, and bridge status `ok`. Current primary evidence also passes: single-machine `20260602-033029-HUGH_SECOND`, desktop single-instance `20260602-033145-HUGH_SECOND`, startup single-instance `20260602-033225-HUGH_SECOND`, process ownership `20260602-033257-HUGH_SECOND`, desktop-open CPU `20260602-033412-HUGH_SECOND.desktop-open`, and matrix `20260602-033636-HUGH_SECOND.runtime-cpu-scenario-matrix`. CPU max one-core is `musu=0`, `node=0`, `webview2=0.23`; machine-wide Node is `19` but MUSU-owned Node is `0`. |
| 2026-06-02 v236 | Live `musu.pro` P2P control-plane was rechecked and remains env-blocked. | Evidence `docs\evidence\p2p-control-plane\1.15.0-rc.1\20260602-034756-musu.pro.evidence.json` records logged-in/wired relay status, but relay leases still fail with `p2p_control_auth_not_configured`, `accepted_auth_modes=[]`, `owner_scope_verified=false`, and `relay_default_data_path=false`. The public site scroll/logo/emerald deploy is already live; the next hosted action is production `MUSU_P2P_CONTROL_TOKEN_SHA256S` or equivalent scoped auth plus Vercel production reload/redeploy. Added wiki/535 and CoS memory `2026-06-02_0348_kst_packaged_cli_runtime_evidence.md`. |
| 2026-06-02 v237 | Code, docs, and evidence were re-indexed after wiki/535. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1247 files and 2214 symbols after packaged CLI pipe evidence, primary runtime evidence, live P2P control-plane recheck evidence, wiki/535, WIKI/WIKI_INDEX/BETA/current-status/runtime-hardening updates, and CoS memory `2026-06-02_0348_kst_packaged_cli_runtime_evidence.md`. Search terms include `packaged-cli-pipe`, `returned_without_hang=true`, `20260602-033412-HUGH_SECOND.desktop-open`, `MUSU_CPU_SCENARIO_ROUTE_OK_20260602_033636`, `p2p_control_auth_not_configured`, and `owned Node 0`. |
| 2026-06-02 v238 | Hosted P2P auth was deployed to `musu.pro`; blocker shifted to relay lease KV storage. | Commit `3be37e54a30bbd0bee95e9b2e22ce27d0450846c` synced `MUSU_P2P_CONTROL_TOKEN_SHA256S` through the deploy workflow; manual production run `26776054030` succeeded and `Tests` run `26775836294` passed. Fresh live evidence `docs\evidence\p2p-control-plane\1.15.0-rc.1\20260602-041225-musu.pro.evidence.json` now fails with `relay_lease_query_failed` / `p2p_relay_lease_kv_not_configured`, not `p2p_control_auth_not_configured`. The next hosted action is provisioning Vercel KV/Upstash and setting `KV_REST_API_URL` plus `KV_REST_API_TOKEN`; current GitHub repo secrets lack those names and repo variables are empty. |
| 2026-06-02 v239 | Code, docs, and evidence were re-indexed after the hosted P2P auth/KV blocker update. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1255 files and 2214 symbols after the workflow env-sync patch, live P2P evidence `20260602-040849-musu.pro` and `20260602-041225-musu.pro`, `P2P_CONTROL_PLANE_MUSU_PRO_NEXT_ACTIONS_2026_06_02.md`, config/docs updates, and CoS memory `2026-06-02_0418_kst_p2p_auth_deploy_kv_blocker.md`. Search terms include `p2p_relay_lease_kv_not_configured`, `relay_lease_query_failed`, `KV_REST_API_URL`, `KV_REST_API_TOKEN`, `26776054030`, and `1255 files`. |
| 2026-06-02 v240 | Final remote verification passed for the P2P env-sync workflow. | Commit `9a3ec52df102d36075f245bdab526dc57fb99e08` passed `Tests` run `26776909221` and `Deploy musu-bee to Vercel` run `26776909275`. The deploy synced `MUSU_P2P_CONTROL_TOKEN_SHA256S`, skipped missing `KV_REST_API_URL`, `KV_REST_API_TOKEN`, and relay env values by name, built production, and aliased `https://musu.pro`. This validates the workflow patch and confirms the remaining hosted blocker is KV/Upstash provisioning, not auth or website deployment. |
| 2026-06-02 v241 | Hosted P2P env status became a repeatable local preflight. | Added `scripts\windows\show-musu-pro-p2p-env-status.ps1` with schema `musu.p2p_control_plane_env_status.v1`. It checks GitHub secret/variable names and latest live P2P evidence without printing secret values. Current JSON reports `MUSU_P2P_CONTROL_TOKEN_SHA256S` present, `KV_REST_API_URL` and `KV_REST_API_TOKEN` missing, evidence `20260602-041225-musu.pro`, and blockers `missing_kv_rest_api_token`, `missing_kv_rest_api_url`, and `live_evidence_p2p_relay_lease_kv_not_configured`. |
| 2026-06-02 v242 | Code and docs were re-indexed after the hosted P2P env preflight script. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1256 files and 2214 symbols after adding `show-musu-pro-p2p-env-status.ps1` and updating the P2P next-action docs, beta checklist, WIKI, WIKI_INDEX, GOAL, and CoS memory. Validation passed `git diff --check`, script JSON output, and `-FailOnProblem` failure behavior. |
| 2026-06-02 v243 | Latest HEAD was manually redeployed to `musu.pro` and live site QA passed. | `Deploy musu-bee to Vercel` workflow_dispatch run `26777905910` succeeded for commit `00694a2e766da8e0a79dd6dd7bb82fdadb6c39d1`. Live browser QA against `https://musu.pro` passed on `/`, `/landing`, `/pricing`, and `/install` across desktop `1280x720` and mobile `390x844`: actual scroll movement, no horizontal overflow, visible logo source contains `favicon-header.png`, `--musu-color-brand-emerald=#24C8DB`, and emerald accent color `36, 200, 219`. |
| 2026-06-02 v244 | Release evidence freshness gates were narrowed to docs/status/tooling-only deltas. | `write-release-go-no-go.ps1`, `verify-single-machine-evidence.ps1`, and `verify-runtime-cpu-scenario-matrix.ps1` now allow only `docs/*`, `.github/workflows/deploy-musu-bee.yml`, `scripts/windows/show-musu-pro-p2p-env-status.ps1`, and the exact release-gate verifier scripts as non-runtime-affecting deltas after a recorded evidence commit. Direct single-machine and CPU matrix verifier checks pass again for `20260602-033029-HUGH_SECOND` and `20260602-033636-HUGH_SECOND`; full go/no-go now reports `single_machine_verified=true`, runtime idle CPU `1/2 [HUGH_SECOND]`, runtime CPU scenario matrix `1/2 [HUGH_SECOND]`, and public release still No-Go pending second-PC/multi-device/P2P KV/support/Store evidence. |
| 2026-06-02 v245 | Code and docs were re-indexed after redeploy/freshness-gate documentation. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1257 files and 2214 symbols after the Vercel run `26777905910` note, live `musu.pro` QA results, freshness-gate script updates, BETA/WIKI/WIKI_INDEX/GOAL updates, and CoS memory `2026-06-02_0458_kst_musu_pro_redeploy_freshness_gate.md`. |
| 2026-06-02 v246 | Post-push freshness gate was corrected to include exact release-gate tooling paths. | After commit `da525769`, post-commit go/no-go briefly fell back to `single_machine=false`, runtime idle CPU `0/2`, and matrix `0/2` because the release-gate scripts themselves were not in the non-runtime-affecting allowlist. The allowlist now includes only the exact gate tooling paths, not arbitrary `scripts/*`; go/no-go from dirty state returns `single_machine_verified=true`, runtime idle CPU `1/2 [HUGH_SECOND]`, runtime CPU scenario matrix `1/2 [HUGH_SECOND]`, and the expected dirty-git blocker. `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1257 files and 2214 symbols. |
| 2026-06-02 v247 | The returned second-PC zip was imported and classified as incomplete for runtime release gates. | `import-second-pc-return.ps1 -ReturnZipPath F:\Aisaak\Projects\localsend\second-pc-return\20260531-165240-HUGH-MAIN.second-pc-return.zip -Json` returned `ok=true` for remote `HUGH-MAIN` at `192.168.1.192:8949`, imported MSIX install evidence to `.local-build\msix-install\20260531-165207-HUGH-MAIN.evidence.json`, and imported handoff to `.local-build\second-pc-handoff\20260531-165230-HUGH-MAIN.handoff.json`. The archive contains no release-grade runtime idle CPU evidence, no runtime CPU scenario matrix, and no release-check JSON, so go/no-go correctly remains `runtime idle CPU 1/2 [HUGH_SECOND]`, `runtime CPU matrix 1/2 [HUGH_SECOND]`, and `multi_device_verified=false`. |
| 2026-06-02 v248 | Fresh current-HEAD second-PC handoff artifacts were regenerated and verified. | `prepare-multidevice-test-kit.ps1` generated `.local-build\multi-device-test-kit\musu-multidevice-1.15.0-rc.1-20260602-052353.zip`; `prepare-final-operator-gate-packet.ps1 -Json` generated `.local-build\final-operator-gates\musu-final-operator-gates-1.15.0-rc.1-20260602-052411.zip`, and `verify-final-operator-gate-packet.ps1` passed with `ok=true`, `fail_count=0`, `kit_count=1`. `prepare-operator-action-pack.ps1` then generated `.local-build\operator-action-pack\MUSU-1.15.0-rc.1-operator-action-pack-20260602-052442.zip`; `verify-operator-action-pack.ps1` passed with `ok=true`, `fail_count=0`. The second-PC transfer zip to run next is `.local-build\operator-action-pack\MUSU-1.15.0-rc.1-operator-action-pack-20260602-052442\second-pc\MUSU-second-PC-transfer-1.15.0-rc.1-20260602-052442.zip`. |
| 2026-06-02 v249 | Code and docs were re-indexed after second-PC return classification. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1258 files and 2214 symbols after recording the incomplete `HUGH-MAIN` return zip classification, fresh second-PC kit/final-packet/action-pack paths, and CoS memory `2026-06-02_0524_kst_second_pc_return_classification.md`. |
| 2026-06-02 v250 | Node/WebView2 process attribution was made operator-visible and added to second-PC handoff flow. | Added `scripts\windows\show-musu-process-attribution.ps1` with schema `musu.process_attribution_summary.v1`, wired it into `run-second-pc-release-check.ps1`, `import-second-pc-return.ps1`, the multi-device kit, final operator packet, operator action-pack verifier, and desktop readiness audit. Current local diagnostic output on `HUGH_SECOND` reports machine-wide `node.exe=16`, MUSU-owned Node `0`, unowned Node `16`, machine-wide WebView2 `12`, MUSU-owned WebView2 `6`, unowned WebView2 `6`, repo-related orphan helpers `0`, and process ownership checks passing. The release policy is now explicit: raw machine-wide Node count is diagnostic; release blockers are MUSU-owned helpers, repo-owned orphan helpers, hot CPU/resource samples, and two-machine runtime evidence. The release freshness allowlist now includes only the exact operator packet/import/action-pack/process-attribution scripts touched here so this non-runtime tooling change does not invalidate current primary runtime evidence while still rejecting arbitrary `scripts/*` changes. |
| 2026-06-02 v251 | Code and docs were re-indexed after process attribution wiring. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1261 files and 2214 symbols after adding wiki/536 process attribution audit, CoS memory `2026-06-02_0540_kst_process_attribution_node_count.md`, the `musu.process_attribution_summary.v1` script, second-PC return/import wiring, packet/action-pack verification updates, and release evidence freshness allowlist updates. Search terms include `process-attribution-summary`, `machine-wide node.exe=16`, `owned_node=0`, `owned_webview2=6`, `show-musu-process-attribution.ps1`, and `wiki/536`. |
| 2026-06-02 v252 | Hosted P2P KV env configuration became an operator-safe script. | Added `scripts\windows\configure-musu-pro-p2p-env.ps1` with schema `musu.configure_musu_pro_p2p_env.v1`; it sets `KV_REST_API_URL` as a GitHub variable by default, `KV_REST_API_TOKEN` as a GitHub secret, optional relay policy env names, and can trigger `deploy-musu-bee.yml` without printing values. Dry-run validation with redacted KV inputs returned `ok=true`, `settings_requested=[KV_REST_API_URL variable, KV_REST_API_TOKEN secret]`, no missing required values, and no errors. Final packet generation/verification and desktop readiness script coverage now include the configurator. This does not provision KV by itself; it turns the remaining `musu.pro` P2P blocker into a safe runbook once the operator has Upstash/Vercel KV values. |
| 2026-06-02 v253 | Code and docs were re-indexed after hosted P2P KV env configurator wiring. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1263 files and 2214 symbols after adding `configure-musu-pro-p2p-env.ps1`, P2P next-action/CONFIG/BETA/WIKI/WIKI_INDEX updates, and CoS memory `2026-06-02_0605_kst_p2p_kv_env_configurator.md`. Search terms include `musu.configure_musu_pro_p2p_env.v1`, `KV_REST_API_URL variable`, `KV_REST_API_TOKEN secret`, `Deploy`, `DryRun`, and `p2p_relay_lease_kv_not_configured`. |
| 2026-06-02 v254 | mDNS discovery now exits on disconnected browse receivers instead of spinning for the full window. | `musu-rs/src/peer/mdns.rs` now distinguishes `flume::RecvTimeoutError::Timeout` from `Disconnected` when browsing `_musu._tcp.local.`. A timeout keeps the bounded discovery window alive; a disconnected receiver logs and breaks immediately. This preserves the default-off mDNS posture while hardening explicit `MUSU_ENABLE_MDNS=1` and `musu discover` paths against the operator-observed Windows/Tailscale `closed channel` failure class becoming a short busy-loop. Added a unit test for the receive-error classifier. Validation passed: `cargo test --manifest-path .\musu-rs\Cargo.toml --lib -j 1 peer::mdns::tests::` passed 3/3. The earlier non-`--lib` filtered run also passed the mDNS tests but failed when Cargo attempted the unrelated `r6_auto_update` integration harness, which requires elevation on this Windows host. |
| 2026-06-02 v255 | Code and docs were re-indexed after mDNS disconnected-receiver hardening. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1264 files and 2217 symbols after the mDNS receive-loop patch, BETA/WIKI/WIKI_INDEX updates, and CoS memory `2026-06-02_0633_kst_mdns_disconnected_receiver_hardening.md`. Search terms include `MdnsRecvTimeoutKind`, `flume::RecvTimeoutError`, `mDNS browse receiver disconnected`, `sending on a closed channel`, `MUSU_ENABLE_MDNS=1`, and `peer::mdns::tests::`. |
| 2026-06-02 v256 | Fresh MSIX and primary runtime evidence were refreshed after mDNS disconnected-receiver hardening. | Fresh `local-sideload-manual` MSIX build/install succeeded for `Yellowhama.MUSU_1.15.0.0_x64__ygcjq669as2b6`. Single-machine smoke `docs\evidence\single-machine\1.15.0-rc.1\20260602-070642-HUGH_SECOND.evidence.json` passed with dashboard output `MUSU_RELEASE_SMOKE_OK_20260602_070616`, task `9968e62c-5f42-43ce-86ac-7b9a57a0d120`, bridge `http://127.0.0.1:12438`, and CLI route checked. Desktop-open CPU `docs\evidence\runtime-idle-cpu\1.15.0-rc.1\20260602-070807-HUGH_SECOND.desktop-open.evidence.json` passed from clean commit `39a9adf9833acb4324c46c646001c8c1ab622bfa` with MUSU `2`, repo Node `1`, owned WebView2 `6`, hot `0`, max one-core CPU `musu=0`, `node=0.05`, `webview2=0.26`, and working set `534.5MB`. Four-state matrix `docs\evidence\runtime-cpu-scenarios\1.15.0-rc.1\20260602-070927-HUGH_SECOND.runtime-cpu-scenario-matrix.json` passed with route token `MUSU_CPU_SCENARIO_ROUTE_OK_20260602_070927`; scenario max one-core CPU stayed at or below `musu=0.03`, `node=0.03`, `webview2=0.39`. Current go/no-go remains No-Go: single-machine true, local artifacts true, MSIX true, public metadata true, runtime CPU `1/2 [HUGH_SECOND]`, matrix `1/2 [HUGH_SECOND]`, but multi-device/support/Store/P2P-control-plane false plus a temporary dirty-git blocker until this evidence is committed. |
| 2026-06-02 v257 | Code, docs, and evidence were re-indexed after the fresh mDNS runtime evidence audit. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1271 files and 2217 symbols after adding wiki/537, fresh primary evidence files, current-status/BETA/runtime-hardening/WIKI/WIKI_INDEX updates, and CoS memory `2026-06-02_0716_kst_fresh_mdns_runtime_evidence_refresh.md`. Search terms include `wiki/537`, `20260602-070642-HUGH_SECOND`, `20260602-070807-HUGH_SECOND.desktop-open`, `20260602-070927-HUGH_SECOND.runtime-cpu-scenario-matrix`, `MUSU_CPU_SCENARIO_ROUTE_OK_20260602_070927`, `runtime_idle_cpu_valid=1/2`, and `p2p_relay_lease_kv_not_configured`. |
| 2026-06-02 v258 | Current operator handoff artifacts were regenerated from clean HEAD. | `prepare-final-operator-gate-packet.ps1 -Json` generated `.local-build\final-operator-gates\musu-final-operator-gates-1.15.0-rc.1-20260602-073317.zip` from clean commit `1228cb0396c76d2438f4a814e33eb4b38f398198`; verifier passed with `ok=true`, `fail_count=0`, `kit_count=1`. `prepare-operator-action-pack.ps1 -Json` generated `.local-build\operator-action-pack\MUSU-1.15.0-rc.1-operator-action-pack-20260602-073356.zip`; verifier passed with `ok=true`, `fail_count=0`. The current second-PC transfer zip is `.local-build\operator-action-pack\MUSU-1.15.0-rc.1-operator-action-pack-20260602-073356\second-pc\MUSU-second-PC-transfer-1.15.0-rc.1-20260602-073356.zip`, and the Partner Center zip is `.local-build\operator-action-pack\MUSU-1.15.0-rc.1-operator-action-pack-20260602-073356\partner-center\MUSU-1.15.0-rc.1-store-submission-20260602-073356.zip`. `show-final-release-handoff-status.ps1 -Json` reports packet verified and manifest git clean while public release remains No-Go pending second-PC CPU/matrix, release-grade route, support mailbox, Store, and P2P-control-plane evidence. |
| 2026-06-02 v259 | Code and docs were re-indexed after current operator action pack documentation. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1272 files and 2217 symbols after recording the `20260602-073317` final packet, `20260602-073356` action pack, current second-PC transfer zip, current status audit addendum, BETA/WIKI/WIKI_INDEX updates, and CoS memory `2026-06-02_0736_kst_current_operator_action_pack_refresh.md`. Search terms include `MUSU-second-PC-transfer-1.15.0-rc.1-20260602-073356`, `operator-action-pack-20260602-073356`, `musu-final-operator-gates-1.15.0-rc.1-20260602-073317`, and `run-second-pc-release-check.ps1`. |
| 2026-06-02 v260 | Second-PC return import now has a release-gate evidence requirement. | `import-second-pc-return.ps1` gained `-RequireReleaseGateEvidence`. Default import remains diagnostic/backward-compatible, but release imports now return `ok=false` and exit 1 if runtime idle CPU evidence, runtime CPU scenario matrix, process attribution summary, or second-PC release-check JSON is missing, or if the release-check reports CPU/matrix/process attribution/return zip failure. Final packet and operator action-pack templates now include the flag in primary-side import commands, and `SECOND_PC_MSIX_INSTALL_OPERATOR_RUNBOOK_2026_05_31.md` documents the failure behavior. Validation against the known incomplete `20260531-165240-HUGH-MAIN.second-pc-return.zip` correctly failed with `missing_runtime_idle_cpu_evidence`, `missing_runtime_cpu_scenario_matrix`, `missing_process_attribution_summary`, and `missing_second_pc_release_check`; running without the flag still returned `ok=true` for diagnostic import while exposing the same release-gate issues. |
| 2026-06-02 v261 | Code and docs were re-indexed after second-PC import release-gate hardening. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1273 files and 2217 symbols after `-RequireReleaseGateEvidence`, final packet/action-pack command template updates, second-PC runbook/BETA/WIKI/WIKI_INDEX updates, and CoS memory `2026-06-02_0746_kst_second_pc_import_release_gate_requirement.md`. Search terms include `RequireReleaseGateEvidence`, `missing_runtime_idle_cpu_evidence`, `missing_runtime_cpu_scenario_matrix`, `missing_process_attribution_summary`, `missing_second_pc_release_check`, and `release_gate_evidence_ok=false`. |
| 2026-06-02 v262 | Release evidence verifiers now have a local fail-closed regression harness. | Added `scripts\windows\test-release-evidence-verifiers.ps1` and wiki/538. Validation passed 9/9 cases at `.local-build\release-evidence-verifier-tests\20260602-080146`: positive P2P and multi-device fixtures pass; non-`musu.pro` base URL, unverified owner scope, relay default data path, non-release-grade transport proof, `route_kind=failed`, and incorrect direct/relay payload transit semantics fail. `audit-desktop-release-readiness.ps1` now checks that the harness exists, and the exact harness path is allowed as non-runtime-affecting release-gate tooling in the evidence freshness allowlists. This is a code-audit guard only; public release remains No-Go until real second-PC CPU/matrix/route evidence, live KV-backed `musu.pro` P2P evidence, `musu@musu.pro` mailbox evidence, and Store evidence are recorded. |
| 2026-06-02 v263 | Code and docs were re-indexed after the verifier regression audit. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1276 files and 2217 symbols after adding wiki/538, CoS memory `2026-06-02_0757_kst_release_evidence_verifier_regression.md`, the verifier regression harness, freshness/readiness audit wiring, and BETA/WIKI/WIKI_INDEX/current-status updates. Search terms include `musu.release_evidence_verifier_regression.v1`, `test-release-evidence-verifiers.ps1`, `20260602-080146`, `owner_scope_verified=false`, `relay_default_data_path=true`, and `route_kind=failed`. |
| 2026-06-02 v264 | Frontend background polling now has bounded per-task timeouts. | Added `taskTimeoutMs` to `musu-bee/src/lib/useLowDutyPolling.ts` and applied 5s/8s/10s task timeouts to dashboard aggregate refresh, dashboard relay-token lookup, service health, device discovery, node mesh, process, agents surface, and bridge-task SSE fallback polling. Runtime-polling contract now asserts dashboard timeout wiring and shared hook timeout support. Validation passed `npx tsx --test src/app/runtime-polling-contract.test.ts` 7/7, `npm run typecheck`, `npm run build`, and `npm run lint -- --quiet`. This reduces the frontend polling/refetch busy-loop candidate but is runtime source, so fresh MSIX install and primary smoke/process/CPU/matrix evidence are required after commit before current-HEAD release evidence can be claimed. |
| 2026-06-02 v265 | Code and docs were re-indexed after frontend polling timeout hardening. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1278 files and 2217 symbols after wiki/539, CoS memory `2026-06-02_0817_kst_frontend_polling_timeout_hardening.md`, frontend polling timeout wiring, runtime-polling contract updates, and BETA/WIKI/WIKI_INDEX/current-status updates. Search terms include `GOAL v264`, `wiki/539`, `taskTimeoutMs`, `AbortSignal.timeout`, `DASHBOARD_REFRESH_TIMEOUT_MS`, `POLL_TIMEOUT_MS`, and `frontend polling timeout`. |
| 2026-06-02 v266 | Fresh MSIX and primary runtime evidence were refreshed after frontend polling timeout hardening. | Fresh `local-sideload-manual` MSIX build/install succeeded for `Yellowhama.MUSU_1.15.0.0_x64__ygcjq669as2b6` from clean source commit `22ba6c313dea4dd32ae43a46dca424b3443edf85`. Desktop repeated activation passed at `docs\evidence\desktop-single-instance\1.15.0-rc.1\20260602-0832-HUGH_SECOND.desktop-single-instance.json` with repeat count `3`, before `1`, after `1`, new shells `0`. Process ownership passed at `docs\evidence\process-ownership\1.15.0-rc.1\20260602-0832-HUGH_SECOND.process-ownership.json` with one runtime, one desktop shell, owned Node `0`, owned WebView2 `6`, and bridge `127.0.0.1:9967`. Single-machine smoke passed at `docs\evidence\single-machine\1.15.0-rc.1\20260602-083131-HUGH_SECOND.evidence.json` with dashboard task `4ae56776-f54d-4955-98cb-d6774626d072`, bridge `http://127.0.0.1:9967`, output `MUSU_RELEASE_SMOKE_OK_20260602_083131`, and CLI route checked. Desktop-open CPU passed at `docs\evidence\runtime-idle-cpu\1.15.0-rc.1\20260602-0833-HUGH_SECOND.desktop-open.evidence.json`: 60.063s, `git_dirty=false`, MUSU `2`, repo Node `1`, owned WebView2 `6`, max one-core CPU `musu=0`, `node=0.08`, `webview2=0.34`, working set `498.42MB`, private memory `329MB`, hot `0`. Four-state matrix passed at `docs\evidence\runtime-cpu-scenarios\1.15.0-rc.1\20260602-083314-HUGH_SECOND.runtime-cpu-scenario-matrix.json` with route token `MUSU_CPU_SCENARIO_ROUTE_OK_20260602_083314`. The test-only `next start -p 3001` process tree was stopped afterward. Local caveat: this dev shell still shadows the WindowsApps alias with `C:\Users\empty\.cargo\bin\musu.exe`; packaged evidence used the explicit WindowsApps alias. |
| 2026-06-02 v267 | Code, docs, and fresh primary evidence were re-indexed. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1287 files and 2217 symbols after wiki/540, fresh primary evidence files, BETA/current-status/frontend-polling-audit/WIKI/WIKI_INDEX updates, and CoS memory `2026-06-02_0840_kst_frontend_polling_primary_evidence_refresh.md`. Search terms include `GOAL v266`, `wiki/540`, `20260602-083131-HUGH_SECOND`, `20260602-0833-HUGH_SECOND.desktop-open`, `20260602-083314-HUGH_SECOND.runtime-cpu-scenario-matrix`, `MUSU_CPU_SCENARIO_ROUTE_OK_20260602_083314`, `1287 files`, and `2217 symbols`. |
| 2026-06-02 v268 | Clean post-evidence go/no-go was rerun. | On commit `300b5b4419d728ceac58c38fc1597b02acb5d3c3`, go/no-go reports `ready=false`, `manifest_dirty=false`, `single_machine=true`, runtime idle CPU `1/2 [HUGH_SECOND]`, runtime CPU scenario matrix `1/2 [HUGH_SECOND]`, public metadata ok, MSIX install ok, MSIX desktop entrypoint ok, process ownership ok, startup single-instance ok, desktop single-instance ok, multi-device false, support false, Store false, and P2P control-plane false. Blockers remain: second-PC multi-device evidence, second-PC runtime idle CPU, second-PC CPU matrix, `musu@musu.pro` delivery evidence, Store/Partner Center evidence, and live `musu.pro` P2P owner-scoped relay lease evidence. |
| 2026-06-02 v269 | Code and docs were re-indexed after post-evidence go/no-go documentation. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1287 files and 2217 symbols after recording clean post-evidence go/no-go state in current-status, wiki/540, WIKI_INDEX, and GOAL. Search terms include `GOAL v268`, `post-evidence go/no-go`, `runtime idle CPU 1/2`, `runtime CPU scenario matrix 1/2`, `support-mailbox`, `store-release`, and `p2p-control-plane`. |
| 2026-06-02 v270 | MSIX legacy conflict preflight became machine-readable and second-PC-return aware. | `check-msix-legacy-conflicts.ps1` now emits `musu.msix_legacy_conflicts.v1`, supports `-Json`, `-OutputPath`, and `-FailOnProblem`, and reports startup helpers, scheduled tasks, legacy `~\.musu\bin` binaries, WindowsApps alias presence, `Get-Command -All` sources, and alias shadowing. `run-second-pc-release-check.ps1` includes this evidence in return zips when present, and `import-second-pc-return.ps1` preserves it. Local validation shows `HUGH_SECOND` has no startup/scheduled/legacy-bin conflicts but does have one dev-shell alias shadow: `C:\Users\empty\.cargo\bin\musu.exe` before the WindowsApps alias. This is release handoff hardening, not runtime source, so current primary CPU evidence remains valid through exact tooling freshness allowlists. |
| 2026-06-02 v271 | Code and docs were re-indexed after MSIX legacy conflict preflight documentation. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1289 files and 2217 symbols after wiki/541, CoS memory `2026-06-02_0905_kst_msix_legacy_conflict_preflight.md`, BETA/current-status/WIKI/WIKI_INDEX updates, and second-PC return/import tooling. Search terms include `GOAL v270`, `wiki/541`, `musu.msix_legacy_conflicts.v1`, `msix_legacy_conflicts_path`, `alias_shadowing_count`, `WindowsApps alias present`, and `1289 files`. |
| 2026-06-02 v272 | Runtime CPU scenario matrix verification became fail-closed for resource-budget fields. | `verify-runtime-cpu-scenario-matrix.ps1` now requires matrix-level owned process/WebView2/working-set budgets and per-scenario `resource_budget_violations`, `process_counts_by_role`, `total_working_set_mb_after`, and `total_private_memory_mb_after`; it also exits nonzero when `ok=false`. `test-release-evidence-verifiers.ps1` now covers the runtime matrix verifier and passed 13/13 cases at `.local-build\release-evidence-verifier-tests\20260602-093028`, including rejection of missing resource-budget fields, working set over budget, and WebView2 count over budget. `verify-final-operator-gate-packet.ps1` now self-checks that packets include the hardened matrix verifier. Latest primary matrix `20260602-083314-HUGH_SECOND` still verifies with `ok=true`, `fail_count=0`, so current primary matrix evidence remains valid at `1/2`. |
| 2026-06-02 v273 | Code and docs were re-indexed after runtime matrix verifier hardening. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1291 files and 2217 symbols after wiki/542, CoS memory `2026-06-02_0925_kst_runtime_matrix_resource_budget_verifier.md`, BETA/current-status/WIKI/WIKI_INDEX updates, runtime matrix verifier hardening, verifier regression updates, and final operator packet verifier self-check updates. Search terms include `GOAL v272`, `wiki/542`, `resource_budget_violations`, `total_working_set_mb_after`, `total_private_memory_mb_after`, `WebView2 process budget`, `13/13`, and `1291 files`. |
| 2026-06-02 v274 | Current operator handoff artifacts were regenerated after runtime matrix verifier hardening. | From clean HEAD `c3adf9f3`, `prepare-final-operator-gate-packet.ps1 -Json` generated `.local-build\final-operator-gates\musu-final-operator-gates-1.15.0-rc.1-20260602-093212.zip`, and `verify-final-operator-gate-packet.ps1` passed with `ok=true`, `fail_count=0`. `prepare-operator-action-pack.ps1 -Json` then generated `.local-build\operator-action-pack\MUSU-1.15.0-rc.1-operator-action-pack-20260602-093312.zip`, and `verify-operator-action-pack.ps1` passed with `ok=true`, `fail_count=0`. The current second-PC transfer zip is `.local-build\operator-action-pack\MUSU-1.15.0-rc.1-operator-action-pack-20260602-093312\second-pc\MUSU-second-PC-transfer-1.15.0-rc.1-20260602-093312.zip`. |
| 2026-06-02 v275 | Code and docs were re-indexed after operator packet refresh documentation. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1291 files and 2217 symbols after documenting final operator packet `20260602-093212`, action pack `20260602-093312`, and the current second-PC transfer zip. Search terms include `GOAL v274`, `GOAL v275`, `20260602-093212`, `20260602-093312`, `MUSU-second-PC-transfer-1.15.0-rc.1-20260602-093312`, `1291 files`, and `2217 symbols`. |
| 2026-06-02 v276 | Local API auth contract docs and tooling were corrected. | Rust bridge source already required bearer auth on localhost by default; stale current docs still described localhost bypass or `MUSU_BRIDGE_LOCALHOST_AUTH=1` as the way to require auth. Added wiki/543, CoS memory `2026-06-02_0947_kst_local_api_auth_contract_audit.md`, and `scripts\windows\audit-local-api-auth-contract.ps1` with schema `musu.local_api_auth_contract.v1`. Current API/config/getting-started/manual/production/troubleshooting/architecture docs now state that localhost requires the bearer token by default and `MUSU_BRIDGE_LOCALHOST_AUTH=0` is only an explicit trusted local dev bypass. Audit validation passed with `ok=true`, `fail_count=0`, `stale_doc_hit_count=0`. Final operator packets now include and self-check the audit. Public release remains No-Go on the existing second-PC CPU/matrix/route, P2P control-plane, `musu@musu.pro`, and Store evidence blockers. |
| 2026-06-02 v277 | Code and docs were re-indexed after the local API auth contract audit. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1294 files and 2217 symbols after wiki/543, CoS memory `2026-06-02_0947_kst_local_api_auth_contract_audit.md`, the auth-doc correction, and release packet audit wiring. Search terms include `GOAL v276`, `GOAL v277`, `wiki/543`, `musu.local_api_auth_contract.v1`, `MUSU_BRIDGE_LOCALHOST_AUTH=0`, `1294 files`, and `2217 symbols`. |
| 2026-06-02 v278 | Current operator handoff artifacts were regenerated after local API auth contract audit wiring. | From clean HEAD `fbcc2a6f`, `prepare-final-operator-gate-packet.ps1 -Json` generated `.local-build\final-operator-gates\musu-final-operator-gates-1.15.0-rc.1-20260602-095328.zip`, and `verify-final-operator-gate-packet.ps1` passed with `ok=true`, `fail_count=0`. `prepare-operator-action-pack.ps1 -Json` then generated `.local-build\operator-action-pack\MUSU-1.15.0-rc.1-operator-action-pack-20260602-095354.zip`, and `verify-operator-action-pack.ps1` passed with `ok=true`, `fail_count=0`. The current second-PC transfer zip is `.local-build\operator-action-pack\MUSU-1.15.0-rc.1-operator-action-pack-20260602-095354\second-pc\MUSU-second-PC-transfer-1.15.0-rc.1-20260602-095354.zip`. |
| 2026-06-02 v279 | Code and docs were re-indexed after the local auth operator packet refresh. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1295 files and 2217 symbols after CoS memory `2026-06-02_0954_kst_local_auth_operator_packet_refresh.md`, packet refresh documentation, and WIKI/WIKI_INDEX/current-status updates. Search terms include `GOAL v278`, `GOAL v279`, `20260602-095328`, `20260602-095354`, `MUSU-second-PC-transfer-1.15.0-rc.1-20260602-095354`, `1295 files`, and `2217 symbols`. |
| 2026-06-02 v280 | Bridge health polling was hardened with capped backoff. | `musu up` bridge startup wait and auto-update post-swap `/health` polling now use 250ms -> 500ms -> 1s -> 2s capped backoff, replacing the previous fixed 500ms retry cadence while preserving existing deadlines. Targeted Rust validation passed 2/2 with `cargo test --manifest-path .\musu-rs\Cargo.toml --lib -j 1 health_poll_delay`, and `git diff --check` passed. This reduces a local busy-loop candidate but resets current-HEAD runtime evidence freshness until a fresh MSIX build/install and primary smoke/CPU/matrix evidence are regenerated. |
| 2026-06-02 v281 | Code and docs were re-indexed after health poll backoff hardening. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1297 files and 2221 symbols after wiki/544, CoS memory `2026-06-02_1012_kst_health_poll_backoff_hardening.md`, Rust health poll backoff tests, WIKI/WIKI_INDEX/current-status/BETA/runtime-roadmap updates, and the health poll backoff assessment doc. Search terms include `GOAL v280`, `GOAL v281`, `wiki/544`, `health_poll_delay`, `bridge_health_poll_delay`, `1297 files`, and `2221 symbols`. |
| 2026-06-02 v282 | Fresh primary MSIX/runtime evidence was refreshed after health poll backoff hardening. | Release MSIX build/install from clean commit `1990b60b7e0b9f093c62bc48fa9b101a3f035c1b` succeeded with package `Yellowhama.MUSU_1.15.0.0_x64__ygcjq669as2b6`. Desktop single-instance `20260602-104113-HUGH_SECOND` passed with one shell; process ownership passed with runtime `1`, desktop `1`, owned Node `0`, owned WebView2 `6`, machine-wide Node `18`; single-machine smoke `20260602-104202-HUGH_SECOND` passed with task `4204b978-d662-4762-938f-05519e6bfa85`; desktop-open CPU `20260602-104113-HUGH_SECOND.desktop-open` passed with `git_dirty=false`, hot `0`, MUSU `0`, Node `0.03`, WebView2 `0.18`, working set `501.1MB`; runtime CPU matrix `20260602-104331-HUGH_SECOND` passed with route token `MUSU_CPU_SCENARIO_ROUTE_OK_20260602_104331` and max WebView2 `0.31`. |
| 2026-06-02 v283 | Code, docs, and fresh primary evidence were re-indexed after health poll primary evidence refresh. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1306 files and 2221 symbols after wiki/545, fresh primary evidence files, CoS memory `2026-06-02_1051_kst_health_poll_primary_evidence_refresh.md`, BETA/current-status/runtime-roadmap/WIKI/WIKI_INDEX updates, and the health poll primary evidence refresh doc. Search terms include `GOAL v282`, `GOAL v283`, `wiki/545`, `20260602-104113-HUGH_SECOND.desktop-open`, `20260602-104331-HUGH_SECOND.runtime-cpu-scenario-matrix`, `MUSU_CPU_SCENARIO_ROUTE_OK_20260602_104331`, `1306 files`, and `2221 symbols`. |
| 2026-06-02 v284 | Current operator packet and action pack were regenerated after the primary evidence refresh. | Clean HEAD `f68806cc026cabfea6706ced31134001d4847016` generated final operator packet `.local-build\final-operator-gates\musu-final-operator-gates-1.15.0-rc.1-20260602-110033.zip` and action pack `.local-build\operator-action-pack\MUSU-1.15.0-rc.1-operator-action-pack-20260602-110105.zip`; both verify with `ok=true`, `fail_count=0`, and final packet `kit_count=1`. Current second-PC transfer zip is `.local-build\operator-action-pack\MUSU-1.15.0-rc.1-operator-action-pack-20260602-110105\second-pc\MUSU-second-PC-transfer-1.15.0-rc.1-20260602-110105.zip`; Partner Center zip is `.local-build\operator-action-pack\MUSU-1.15.0-rc.1-operator-action-pack-20260602-110105\partner-center\MUSU-1.15.0-rc.1-store-submission-20260602-110105.zip`; support verification id is `musu-store-support-1.15.0-rc.1-20260602-110033`. |
| 2026-06-02 v285 | Code and docs were re-indexed after the current operator packet/action pack refresh. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1307 files and 2221 symbols after CoS memory `2026-06-02_1101_kst_operator_packet_refresh_after_primary_evidence.md`, WIKI/WIKI_INDEX/GOAL/BETA/current-status updates, and current packet/action-pack documentation. Search terms include `GOAL v284`, `GOAL v285`, `20260602-110033`, `20260602-110105`, `MUSU-second-PC-transfer-1.15.0-rc.1-20260602-110105`, `1307 files`, and `2221 symbols`. |
| 2026-06-02 v286 | Dashboard cloud relay became on-demand instead of mount-time background work. | Added wiki/546. `musu-bee/src/components/dashboard/DashboardClient.tsx` no longer fetches `/api/account/relay-token` on mount and no longer auto-connects the relay WebSocket when `relayInfo` and `selectedNode` exist. `Connect` lazily fetches the token with the existing `5s` timeout; selected-node changes and unmount abort pending token fetches, clear retry timers, and close relay WebSocket state. `runtime-polling-contract.test.ts` now guards the on-demand relay contract. Validation passed: `npx tsx --test src/app/runtime-polling-contract.test.ts` 8/8, `npm run typecheck`, `npm run lint -- --quiet`, `npm run build`, and `git diff --check`. This is runtime source, so fresh MSIX smoke/process/desktop-open CPU/matrix evidence is required after commit before current-HEAD release evidence can be claimed. |
| 2026-06-02 v287 | Code and docs were re-indexed after relay idle hardening. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1309 files and 2221 symbols after wiki/546, CoS memory `2026-06-02_1114_kst_relay_idle_hardening.md`, relay idle audit doc, WIKI/WIKI_INDEX/GOAL/BETA/current-status/frontend-polling audit updates, and dashboard relay source/test changes. Search terms include `GOAL v286`, `GOAL v287`, `wiki/546`, `1309 files`, `2221 symbols`, `handleRelayConnect`, `fetchRelayToken`, `relay idle hardening`, and `on-demand fallback`. |
| 2026-06-02 v288 | `musu.pro` deployment succeeded after relay idle hardening. | Commit `77ba7a112581dfd3a2e05d62d7ba0b6a0ce2a0d6` passed GitHub Actions `Tests` run `26794342633`, `E2E Tests - musu-bee` run `26794342638`, and `Deploy musu-bee to Vercel` run `26794342631`. Vercel deployed production URL `https://musu-9wn2j1cat-yellowhamas-projects.vercel.app` and aliased it to `https://musu.pro`; deploy log reported `Ready in 19s`. This is web/control-plane deploy evidence only; live P2P route/relay lease evidence, Store evidence, support mailbox evidence, second-PC CPU/matrix evidence, and fresh-current-HEAD MSIX runtime evidence remain open. |
| 2026-06-02 v289 | Code and docs were re-indexed after `musu.pro` deploy evidence. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1311 files and 2221 symbols after CoS memory `2026-06-02_1122_kst_musu_pro_deploy_after_relay_idle_hardening.md`, deploy evidence doc, WIKI/WIKI_INDEX/GOAL/BETA/current-status updates, and the `musu.pro` deployment evidence. Search terms include `GOAL v288`, `GOAL v289`, `1311 files`, `2221 symbols`, `26794342631`, `26794342633`, `26794342638`, `musu.pro`, and `Ready in 19s`. |
| 2026-06-02 v290 | Current-head primary evidence and qualitative/code audit were refreshed after relay idle hardening. | Current primary evidence now passes desktop single-instance `docs\evidence\desktop-single-instance\1.15.0-rc.1\20260602-113614-HUGH_SECOND.desktop-single-instance.json`, process ownership `docs\evidence\process-ownership\1.15.0-rc.1\20260602-113702-HUGH_SECOND.process-ownership.json`, single-machine smoke `docs\evidence\single-machine\1.15.0-rc.1\20260602-113759-HUGH_SECOND.evidence.json`, desktop-open CPU `docs\evidence\runtime-idle-cpu\1.15.0-rc.1\20260602-114149-HUGH_SECOND.desktop-open.evidence.json`, and four-state CPU matrix `docs\evidence\runtime-cpu-scenarios\1.15.0-rc.1\20260602-115359-HUGH_SECOND.runtime-cpu-scenario-matrix.json`. The matrix route token is `MUSU_CPU_SCENARIO_ROUTE_OK_20260602_115359`; idle CPU maxes at MUSU `0`, Node `0`, WebView2 `0.13`, hot `0`. Clean go/no-go on `9b836bd1e5702a4a6b40b30fb7f15b5aa76be05a` reports `ready=false`, `local_artifacts_ready=true`, `single_machine=true`, runtime idle CPU `1/2`, runtime CPU matrix `1/2`, `p2p_control_plane=false`, `support_mailbox=false`, `store_release=false`, and `manifest_dirty=false`. Public release remains blocked by second-PC evidence, live `musu.pro` P2P owner-scoped control-plane evidence, `musu@musu.pro` mailbox evidence, and Store evidence. |
| 2026-06-02 v291 | Code and docs were re-indexed after current-head evidence and qual audit documentation. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1320 files and 2221 symbols after wiki/547, current-head evidence/qual audit/roadmap report, CoS memories `2026-06-02_1205_kst_current_head_evidence_qual_audit_next_steps.md` and `2026-06-02_1210_kst_current_head_evidence_index_refresh.md`, WIKI/WIKI_INDEX/GOAL/BETA/current-status updates, P2P control-plane spec updates, and network boundary spec updates. Search terms include `GOAL v290`, `GOAL v291`, `1320 files`, `2221 symbols`, `20260602-115359-HUGH_SECOND.runtime-cpu-scenario-matrix`, `MUSU_CPU_SCENARIO_ROUTE_OK_20260602_115359`, `musu@musu.pro`, and `p2p_control_plane=false`. |
| 2026-06-02 v292 | Current operator packet and action pack were regenerated after the evidence audit. | Clean HEAD `ef80aa94d76db4b08ca0866f6bc29c2ed889bdc4` generated final operator packet `.local-build\final-operator-gates\musu-final-operator-gates-1.15.0-rc.1-20260602-121850.zip`; `verify-final-operator-gate-packet.ps1` passed with `ok=true`, `fail_count=0`, `kit_count=1`. `prepare-operator-action-pack.ps1` generated `.local-build\operator-action-pack\MUSU-1.15.0-rc.1-operator-action-pack-20260602-121918.zip`; `verify-operator-action-pack.ps1` passed with `ok=true`, `fail_count=0`. The current second-PC transfer zip is `.local-build\operator-action-pack\MUSU-1.15.0-rc.1-operator-action-pack-20260602-121918\second-pc\MUSU-second-PC-transfer-1.15.0-rc.1-20260602-121918.zip`; Partner Center zip is `.local-build\operator-action-pack\MUSU-1.15.0-rc.1-operator-action-pack-20260602-121918\partner-center\MUSU-1.15.0-rc.1-store-submission-20260602-121918.zip`; support verification id is `musu-store-support-1.15.0-rc.1-20260602-121850`. |
| 2026-06-02 v293 | Code and docs were re-indexed after the current operator action pack documentation. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1322 files and 2221 symbols after CoS memories `2026-06-02_1222_kst_current_operator_action_pack_after_evidence_audit.md` and `2026-06-02_1224_kst_operator_action_pack_index_refresh.md`, WIKI/WIKI_INDEX/GOAL/BETA/current-status updates, and the current-head evidence report update. Search terms include `GOAL v292`, `GOAL v293`, `1322 files`, `2221 symbols`, `20260602-121850`, `20260602-121918`, `MUSU-second-PC-transfer-1.15.0-rc.1-20260602-121918`, `MUSU-1.15.0-rc.1-store-submission-20260602-121918`, and `musu-store-support-1.15.0-rc.1-20260602-121850`. |
| 2026-06-02 v294 | Current mDNS/Tailscale regression was rechecked and the P2P KV blocker was isolated. | Current clean source `6f3f598271ec0b6225524c7d63bbd8da068e7ae5` passed `cargo test --manifest-path .\musu-rs\Cargo.toml --lib -j 1 peer::mdns::tests::` 3/3 and `cargo build --manifest-path .\musu-rs\Cargo.toml --bin musu -j 1` in `8m 38s`. `RUST_LOG=debug .\musu-rs\target\debug\musu.exe discover --timeout 2` with mDNS opt-in env vars unset exited 0, disabled IPv6, Tailscale, and 9 virtual/VPN interfaces, sent only on physical LAN `이더넷 2`, and emitted no `Failed to send`, `ff02::fb`, `10065`, or `closed channel`. `show-musu-pro-p2p-env-status.ps1 -Json` still reports `ok=false`: GitHub has `MUSU_P2P_CONTROL_TOKEN_SHA256S`, but required `KV_REST_API_TOKEN` and `KV_REST_API_URL` are missing, latest live evidence remains `p2p_relay_lease_kv_not_configured`, and local env/files do not contain usable KV credentials. |
| 2026-06-02 v295 | Code and docs were re-indexed after the mDNS/P2P KV blocker audit. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1325 files and 2221 symbols after wiki/548, current mDNS/P2P KV blocker audit, CoS memories `2026-06-02_1238_kst_mdns_regression_p2p_kv_blocker.md` and `2026-06-02_1240_kst_mdns_p2p_kv_index_refresh.md`, WIKI/WIKI_INDEX/GOAL/BETA/current-status updates. Search terms include `GOAL v294`, `GOAL v295`, `1325 files`, `2221 symbols`, `peer::mdns::tests::`, `이더넷 2`, `p2p_relay_lease_kv_not_configured`, `KV_REST_API_TOKEN`, and `KV_REST_API_URL`. |
| 2026-06-02 v296 | Operator API worker proxy routes were hardened after code audit. | Added wiki/549, `musu-bee/src/lib/operator-api-security.ts`, route tests, CI `npm run test:routes`, and `scripts\windows\audit-operator-api-security-contract.ps1` with schema `musu.operator_api_security_contract.v1`. `/api/nodes/execute`, `/api/processes`, `/api/processes/start`, and `/api/processes/kill` now require authenticated operator identity. Node execute is command-allowlisted, process start fails closed unless `MUSU_PROCESS_START_ALLOWLIST` is configured, process kill fails closed unless `MUSU_ENABLE_PROCESS_KILL=1`, remote worker proxying fails closed unless `MUSU_ENABLE_REMOTE_WORKER_PROXY=1`, and user-supplied env is no longer forwarded to process start. Validation passed `npm run test:routes` 12/12, `npm run typecheck`, `npm run build`, `git diff --check`, and operator API audit `ok=true`, `fail_count=0`. Public release remains No-Go on second-PC CPU/matrix/route evidence, P2P KV/live owner-scope evidence, `musu@musu.pro` mailbox evidence, and Store evidence. |
| 2026-06-02 v297 | Code and docs were re-indexed after operator API security hardening. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1334 files and 2239 symbols after wiki/549, CoS memories `2026-06-02_1305_kst_operator_api_security_hardening.md` and `2026-06-02_1310_kst_operator_api_security_index_refresh.md`, operator API route source/test/CI changes, `audit-operator-api-security-contract.ps1`, CONFIG/BETA/WIKI/WIKI_INDEX/GOAL/current-head report updates. Search terms include `GOAL v296`, `GOAL v297`, `wiki/549`, `musu.operator_api_security_contract.v1`, `operator-api-security.ts`, `MUSU_NODE_EXECUTE_ALLOWLIST`, `MUSU_PROCESS_START_ALLOWLIST`, `MUSU_ENABLE_PROCESS_KILL`, `MUSU_ENABLE_REMOTE_WORKER_PROXY`, `1334 files`, and `2239 symbols`. |
| 2026-06-02 v298 | Post-operator-security go/no-go is No-Go because current primary runtime evidence is stale. | Clean commit `94ecda1caceba4a40f091071e8d64825ce7a7b29` reports `ready=false`, `local_artifacts_ready=true`, `single_machine=false`, `multi_device=false`, `msix_install=true`, `msix_desktop_entrypoint=true`, runtime idle CPU `0/2`, runtime CPU matrix `0/2`, `p2p_control_plane=false`, `support_mailbox=false`, `store_release=false`, and `manifest_git.dirty=false`. This is expected after runtime/web source hardening: fresh primary MSIX install, single-machine smoke, desktop-open idle CPU, and four-state runtime CPU matrix evidence must be rerun before final release claims. External blockers remain second-PC route/CPU/matrix, KV-backed `musu.pro` P2P owner-scope evidence, `musu@musu.pro` mailbox evidence, and Store evidence. |
| 2026-06-02 v299 | Code and docs were re-indexed after post-operator-security go/no-go documentation. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1336 files and 2239 symbols after CoS memory `2026-06-02_1320_kst_post_operator_security_go_no_go_index_refresh.md`, BETA/WIKI/WIKI_INDEX/GOAL/current-head report updates, and post-security go/no-go documentation. Search terms include `GOAL v298`, `GOAL v299`, `94ecda1c`, `single_machine=false`, `runtime idle CPU 0/2`, `runtime CPU matrix 0/2`, `1336 files`, and `2239 symbols`. |
| 2026-06-02 v300 | Current primary evidence was refreshed after operator API security hardening. | Fresh MSIX workflow succeeded and installed `Yellowhama.MUSU_1.15.0.0_x64__ygcjq669as2b6`; explicit WindowsApps alias `musu up --json` restored bridge `/health` at `http://127.0.0.1:1065`. Desktop repeated activation passed with one `musu-desktop.exe` shell at `.local-build\desktop-single-instance\musu-desktop-single-instance-20260602-132523-HUGH_SECOND.json`; process ownership passed at `.local-build\process-ownership\musu-process-ownership-20260602-132447.json` with one runtime, zero owned Node helpers, and bridge health 200. Current single-machine smoke is committed at `docs\evidence\single-machine\1.15.0-rc.1\20260602-132814-HUGH_SECOND.evidence.json`; desktop-open CPU is committed at `docs\evidence\runtime-idle-cpu\1.15.0-rc.1\20260602-132531-HUGH_SECOND.desktop-open.evidence.json` with 60.05s, `git_dirty=false`, MUSU `0`, owned Node `0`, WebView2 `0.52`, working set `366.38MB`, and hot `0`; four-state matrix is committed at `docs\evidence\runtime-cpu-scenarios\1.15.0-rc.1\20260602-132921-HUGH_SECOND.runtime-cpu-scenario-matrix.json` with route token `MUSU_CPU_SCENARIO_ROUTE_OK_20260602_132921`. Clean go/no-go on `6f7fe937fcc5dd7e9665bf374aee1bdd1be0e48c` reports `ready=false`, `single_machine=true`, runtime idle CPU `1/2 [HUGH_SECOND]`, runtime CPU matrix `1/2 [HUGH_SECOND]`, process/startup/desktop single-instance true, P2P/support/Store false, and `manifest_dirty=false`. Current final packet `musu-final-operator-gates-1.15.0-rc.1-20260602-134019.zip` and action pack `MUSU-1.15.0-rc.1-operator-action-pack-20260602-134035.zip` verify with `ok=true`; the current second-PC transfer zip is `.local-build\operator-action-pack\MUSU-1.15.0-rc.1-operator-action-pack-20260602-134035\second-pc\MUSU-second-PC-transfer-1.15.0-rc.1-20260602-134035.zip`. Public release remains No-Go until the second PC returns matching CPU/matrix and release-grade route evidence, KV-backed `musu.pro` P2P owner-scope evidence passes, `musu@musu.pro` mailbox evidence is recorded, and Store evidence exists. |
| 2026-06-02 v301 | Code and docs were re-indexed after post-security primary evidence documentation. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1343 files and 2239 symbols after wiki/550, current post-security primary evidence files, final packet/action-pack paths, BETA/current-status/current-head report/WIKI/WIKI_INDEX/GOAL updates, and CoS memory `2026-06-02_1340_kst_post_operator_security_primary_evidence_refresh.md`. Search terms include `GOAL v300`, `GOAL v301`, `wiki/550`, `1343 files`, `20260602-132814-HUGH_SECOND`, `20260602-132531-HUGH_SECOND.desktop-open`, `20260602-132921-HUGH_SECOND.runtime-cpu-scenario-matrix`, `MUSU_CPU_SCENARIO_ROUTE_OK_20260602_132921`, `20260602-134019`, and `20260602-134035`. |
| 2026-06-02 v302 | P2P control-plane route contracts were added to web CI. | Added `npm run test:p2p` for `src/app/api/v1/p2p/route-evidence/route.test.ts`, `src/app/api/v1/p2p/rendezvous/route.test.ts`, and `src/app/api/v1/p2p/relay/lease/route.test.ts`, and wired it into `.github/workflows/test.yml` after `npm run test:routes`. Local validation passed `npm run test:p2p` 21/21, `npm run test:routes` 12/12, and `git diff --check`. This improves hardening by making owner-scoped route evidence, rendezvous session flow, relay fallback policy, and fail-closed bearer auth part of the required web CI contract. It does not close the live `musu.pro` P2P release gate; production still needs `KV_REST_API_URL` and `KV_REST_API_TOKEN` plus fresh owner-scope evidence. |
| 2026-06-02 v303 | Code and docs were re-indexed after P2P control-plane CI coverage. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1346 files and 2239 symbols after wiki/551, the `test:p2p` script, GitHub Actions `P2P control-plane tests`, BETA/WIKI/WIKI_INDEX/GOAL/current-head report updates, and CoS memories `2026-06-02_1357_kst_p2p_control_plane_ci_coverage.md` and `2026-06-02_1403_kst_p2p_ci_index_refresh.md`. Search terms include `GOAL v302`, `GOAL v303`, `wiki/551`, `test:p2p`, `route-evidence`, `rendezvous`, `relay lease`, `owner-scoped route evidence`, `KV_REST_API_URL`, `KV_REST_API_TOKEN`, `1346 files`, and `2239 symbols`. |
| 2026-06-02 v304 | Release gate status scripts were hardened against child-script hangs and Windows PowerShell hash gaps. | `write-release-candidate-manifest.ps1` now uses a .NET SHA256 fallback when `Get-FileHash` is unavailable in the child Windows PowerShell host. `write-release-go-no-go.ps1` and `show-final-release-handoff-status.ps1` now accept `-ScriptTimeoutSeconds` and run child verifier scripts through a bounded `.NET ProcessStartInfo` wrapper that captures stdout/stderr, elapsed time, timeout state, and kills timed-out children. Status-only freshness allowlists in `write-release-go-no-go.ps1`, `verify-single-machine-evidence.ps1`, and `verify-runtime-cpu-scenario-matrix.ps1` now include `show-final-release-handoff-status.ps1` and `write-release-candidate-manifest.ps1`, plus content-specific tooling-only recognition for the exact `test:p2p` package/workflow diff. `show-final-release-handoff-status.ps1 -ScriptTimeoutSeconds 120 -Json` now completes with packet/action pack verification included; packet and action pack verify true, while release remains No-Go because second-PC, CPU 2/2, P2P, support, Store, and current dirty git gates are open. A forced `write-release-go-no-go.ps1 -ScriptTimeoutSeconds 1 -Json` exits nonzero in about 2.9s with `Script timed out after 1s`, proving the no-hang guard. |
| 2026-06-02 v305 | Code and docs were re-indexed after release gate status script hardening. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1348 files and 2239 symbols after wiki/552, `-ScriptTimeoutSeconds`, bounded child verifier execution, `.NET ProcessStartInfo`, `.NET SHA256` fallback, BETA/WIKI/WIKI_INDEX/GOAL/current-head report updates, and CoS memories `2026-06-02_1425_kst_release_gate_status_script_hardening.md` and `2026-06-02_1428_kst_release_gate_script_index_refresh.md`. Search terms include `GOAL v304`, `GOAL v305`, `wiki/552`, `Script timed out after 1s`, `packet_verified true`, `action_pack_verified true`, `write-release-go-no-go.ps1`, `show-final-release-handoff-status.ps1`, `write-release-candidate-manifest.ps1`, `1348 files`, and `2239 symbols`. |
| 2026-06-02 v306 | Clean post-hardening go/no-go restored primary evidence freshness. | Clean `write-release-go-no-go.ps1 -ScriptTimeoutSeconds 120 -Json` after the release gate script hardening reports `ready=false`, `single_machine=true`, runtime idle CPU `1/2`, runtime CPU scenario matrix `1/2`, `process_ownership=true`, `startup_single_instance=true`, `desktop_single_instance=true`, and `manifest_dirty=false`. Current public release blockers are `multi-device`, `runtime-idle-cpu`, `runtime-cpu-scenario-matrix`, `support-mailbox`, `store-release`, and `p2p-control-plane`. This proves the release-gate script hardening did not stale current primary evidence; second-PC/P2P/support/Store evidence are still required. |
| 2026-06-02 v307 | Frontend reconnect paths were hardened against fixed-delay retry loops. | Added wiki/553 and `docs\RELEASE_1_15_0_RC1_RUNTIME_RECONNECT_BACKOFF_HARDENING_2026_06_02.md`. Dashboard cloud relay WebSocket reconnect now uses capped backoff `5s -> 10s -> 20s -> 40s -> 60s` while preserving the existing 5-attempt limit and cleanup on disconnect/node change/unmount. Chat task SSE reconnect now has explicit `1s` initial delay, `2x` multiplier, `10s` cap, `EventSource.CONNECTING` suppression, timer cleanup, and a generation guard so stale timers cannot reconnect after channel/node changes or unmount. Added `npm run test:runtime-polling`, wired it into GitHub Actions before route/P2P tests, and expanded `runtime-polling-contract.test.ts` to 10 tests. Validation passed `npm run test:runtime-polling` 10/10, `npm run typecheck`, `npm run test:routes` 12/12, `npm run test:p2p` 21/21, `npm run build`, `npm run lint` with 0 errors and warning-only existing state, and `git diff --check`. This is runtime web source, so current primary MSIX CPU evidence must be refreshed after commit before current-HEAD release evidence can be claimed. |
| 2026-06-02 v308 | Code and docs were re-indexed after runtime reconnect backoff hardening. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1351 files and 2240 symbols after wiki/553, runtime reconnect backoff hardening, runtime polling contract CI wiring, product network boundary update, runtime stabilization plan update, current-head qualitative audit update, BETA checklist update, WIKI/WIKI_INDEX/GOAL updates, and CoS memories `2026-06-02_1500_kst_runtime_reconnect_backoff_hardening.md` and `2026-06-02_1508_kst_runtime_reconnect_backoff_index_refresh.md`. Search terms include `GOAL v307`, `GOAL v308`, `wiki/553`, `RELAY_RECONNECT_INITIAL_MS`, `relayReconnectDelayMs`, `SSE_RECONNECT_INITIAL_MS`, `reconnectGenerationRef`, `EventSource.CONNECTING`, `test:runtime-polling`, `1351 files`, and `2240 symbols`. |
| 2026-06-02 v309 | Post-push CI passed and clean go/no-go confirms fresh MSIX evidence is required. | Commit `faf199efafb020e11d304ead5b1d3c617d3c71ea` passed GitHub Actions `Tests` run `26801850077`, `E2E Tests - musu-bee` run `26801850121`, and `Deploy musu-bee to Vercel` run `26801850075`. Clean post-push go/no-go reports `ready=false`, `single_machine=false`, runtime idle CPU `false`, runtime CPU matrix `false`, process/startup/desktop single-instance true, and `manifest_dirty=false`; blockers are `single-machine`, `multi-device`, `runtime-idle-cpu`, `runtime-cpu-scenario-matrix`, `p2p-control-plane`, `support-mailbox`, and `store-release`. The `single_machine` drop is expected because runtime web source changed; next release work is fresh current-HEAD MSIX smoke, desktop-open CPU, and runtime matrix evidence before second-PC/P2P/support/Store gates can close. |
| 2026-06-02 v310 | Code and docs were re-indexed after post-push reconnect hardening status. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1352 files and 2240 symbols after GOAL v309, WIKI/WIKI_INDEX/current-head report updates, and CoS memory `2026-06-02_1510_kst_runtime_reconnect_post_push_status.md`. Search terms include `GOAL v309`, `GOAL v310`, `faf199ef`, `26801850077`, `26801850121`, `26801850075`, `single_machine=false`, `runtime source evidence stale`, `1352 files`, and `2240 symbols`. |
| 2026-06-02 v311 | Fresh current-head primary MSIX/runtime evidence was restored after reconnect hardening. | Rebuilt and installed `musu_1.15.0.0_x64_local-sideload-manual.msix` as `Yellowhama.MUSU_1.15.0.0_x64__ygcjq669as2b6`; the `-MachineTrust` wrapper waited for elevation and was stopped because machine trust already existed, then current-user reinstall verified successfully. Explicit WindowsApps alias `musu up --json` restored bridge health at `http://127.0.0.1:14397`; dashboard evidence used `http://127.0.0.1:3001/app` because production `next start` was started for the smoke. Desktop single-instance passes at `docs\evidence\desktop-single-instance\1.15.0-rc.1\20260602-152526-HUGH_SECOND.desktop-single-instance.json`; process ownership passes at `docs\evidence\process-ownership\1.15.0-rc.1\20260602-152537-HUGH_SECOND.process-ownership.json` with runtime `1`, desktop `1`, owned Node `0`, owned WebView2 `6`, machine-wide Node `18`, and orphan repo helpers `0`; single-machine smoke passes at `docs\evidence\single-machine\1.15.0-rc.1\20260602-152615-HUGH_SECOND.evidence.json`; desktop-open CPU passes at `docs\evidence\runtime-idle-cpu\1.15.0-rc.1\20260602-152845-HUGH_SECOND.desktop-open.evidence.json` with 60.061s, MUSU `0`, repo Node `0.05`, WebView2 `0.13`, working set `500.86MB`, and hot `0`; runtime CPU matrix passes at `docs\evidence\runtime-cpu-scenarios\1.15.0-rc.1\20260602-153038-HUGH_SECOND.runtime-cpu-scenario-matrix.json` with token `MUSU_CPU_SCENARIO_ROUTE_OK_20260602_153038` and all four scenarios under the 5% one-core budget. Clean go/no-go remains `ready=false`: primary evidence is restored on `HUGH_SECOND`, but public release still needs second-PC CPU/matrix/route evidence, live `musu.pro` P2P owner-scope evidence, `musu@musu.pro` mailbox evidence, and Store evidence. |
| 2026-06-02 v312 | Code, docs, evidence, and specs were re-indexed after the post-reconnect primary evidence refresh. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1361 files and 2240 symbols after GOAL v311, wiki/554, the current-head evidence report update, BETA checklist update, network/P2P spec boundary updates, desktop/process evidence files, and CoS memories `2026-06-02_1545_kst_post_reconnect_primary_evidence_refresh.md` and `2026-06-02_1548_kst_post_reconnect_evidence_index_refresh.md`. Search terms include `GOAL v311`, `GOAL v312`, `wiki/554`, `1361 files`, `2240 symbols`, `20260602-152526-HUGH_SECOND.desktop-single-instance`, `20260602-152537-HUGH_SECOND.process-ownership`, `20260602-152615-HUGH_SECOND`, `20260602-152845-HUGH_SECOND.desktop-open`, `20260602-153038-HUGH_SECOND.runtime-cpu-scenario-matrix`, `MUSU_CPU_SCENARIO_ROUTE_OK_20260602_153038`, `owned Node 0`, `machine-wide Node 18`, and `post-reconnect primary evidence refresh`. |
| 2026-06-02 v313 | Current HEAD operator handoff artifacts were regenerated after the post-reconnect evidence commit. | Clean HEAD `7bb367988d1ae5cbc41bbcd7ce68f4eeb4f57d10` generated final packet `.local-build\final-operator-gates\musu-final-operator-gates-1.15.0-rc.1-20260602-155746.zip` and latest alias `.local-build\final-operator-gates\musu-final-operator-gates-1.15.0-rc.1-latest.zip`; `verify-final-operator-gate-packet.ps1` reports `ok=true`, `fail_count=0`, `kit_count=1`. It generated action pack `.local-build\operator-action-pack\MUSU-1.15.0-rc.1-operator-action-pack-20260602-155815.zip` and latest alias `.local-build\operator-action-pack\MUSU-1.15.0-rc.1-operator-action-pack-latest.zip`; `verify-operator-action-pack.ps1` reports `ok=true`, `fail_count=0`. Current second-PC transfer zip is `.local-build\operator-action-pack\MUSU-1.15.0-rc.1-operator-action-pack-20260602-155815\second-pc\MUSU-second-PC-transfer-1.15.0-rc.1-20260602-155815.zip`; Partner Center zip is `.local-build\operator-action-pack\MUSU-1.15.0-rc.1-operator-action-pack-20260602-155815\partner-center\MUSU-1.15.0-rc.1-store-submission-20260602-155815.zip`; support verification id is `musu-store-support-1.15.0-rc.1-20260602-155746`. `write-release-go-no-go.ps1 -ScriptTimeoutSeconds 120 -Json` still reports `ready=false`, `single_machine=true`, process/startup/desktop single-instance true, and blockers `multi-device`, `runtime-idle-cpu`, `runtime-cpu-scenario-matrix`, `p2p-control-plane`, `support-mailbox`, and `store-release`. `show-final-release-handoff-status.ps1` is heavy after action-pack checksum verification: concurrent 120s status execution timed out, while a single 240s run completed with `action_pack.verified=true`. |
| 2026-06-02 v314 | Code, docs, and current operator handoff references were re-indexed. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1363 files and 2240 symbols after GOAL v313/v314, wiki/555, BETA checklist update, current-head report update, and CoS memories `2026-06-02_1558_kst_current_operator_action_pack_after_post_reconnect_evidence.md` and `2026-06-02_1600_kst_operator_action_pack_index_refresh.md`. Search terms include `GOAL v314`, `wiki/555`, `1363 files`, `2240 symbols`, `20260602-155746`, `20260602-155815`, `MUSU-second-PC-transfer-1.15.0-rc.1-20260602-155815`, `MUSU-1.15.0-rc.1-store-submission-20260602-155815`, `ScriptTimeoutSeconds 240`, and `action_pack.verified=true`. |
| 2026-06-02 v315 | Release handoff status and go/no-go were optimized for large evidence history. | `show-final-release-handoff-status.ps1` now defaults packet/action-pack verification to quick metadata/required-entry/private-key checks and exposes `-PacketVerificationMode quick|deep|skip` plus `-ActionPackVerificationMode quick|deep|skip`; old `-SkipPacketVerification` and `-SkipActionPackVerification` still map to `skip`. `write-release-go-no-go.ps1` now preselects latest evidence candidates per machine before verifier execution, exposing `available_candidate_count` and `candidate_selection=latest-per-machine` so old evidence history does not force every stale sample through child verifiers. Validation from dirty worktree: go/no-go with `-SkipPublicMetadata` completed in 41.733s while selecting runtime idle `4/59`, runtime matrix `3/38`, and process ownership `3/36`; default handoff status with `-SkipPublicMetadata` completed in 44.050s with packet/action quick `fail_count=0`; deep handoff status completed in 50.663s with packet/action deep `fail_count=0`; public metadata default handoff status completed in 47.182s with `public_metadata_ok=true`. `test-release-evidence-verifiers.ps1` passed all 13 regression cases. |
| 2026-06-02 v316 | Code and docs were re-indexed after release status fast/deep verification hardening. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1364 files and 2240 symbols after GOAL v315/v316, wiki/556, BETA checklist update, current-head report update, WIKI_INDEX update, scripts `show-final-release-handoff-status.ps1` and `write-release-go-no-go.ps1`, and CoS memory `2026-06-02_1630_kst_release_status_fast_path.md`. Search terms include `GOAL v315`, `GOAL v316`, `wiki/556`, `1364 files`, `2240 symbols`, `PacketVerificationMode`, `ActionPackVerificationMode`, `latest-per-machine`, `available_candidate_count`, `runtime idle 4/59`, `runtime matrix 3/38`, `process ownership 3/36`, and `test-release-evidence-verifiers.ps1 13/13`. |
| 2026-06-02 v317 | Optional file sync watcher storms were bounded for desktop resource-budget hardening. | `musu-rs/src/install/sync.rs` now uses a bounded `tokio::sync::mpsc` queue with capacity `1024`, caps each batch at `256` events or `2s`, coalesces same-path events to the latest event before processing, and yields `50ms` after a batch cap hit. This closes an optional background-worker storm risk when file sync roots are configured; file sync remains off unless shared roots exist. Validation passed `cargo fmt --manifest-path .\musu-rs\Cargo.toml --check`, `cargo test --manifest-path .\musu-rs\Cargo.toml install::sync --lib -- --test-threads=1`, and `git diff --check`. This is runtime Rust source, so fresh primary MSIX/smoke/desktop-open CPU/runtime-matrix evidence is required after commit before current-HEAD release claims. |
| 2026-06-02 v318 | Code, docs, and runtime hardening notes were re-indexed after file sync watcher storm hardening. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1366 files and 2242 symbols after GOAL v317/v318, wiki/557, `RELEASE_1_15_0_RC1_FILE_SYNC_WATCHER_STORM_HARDENING_2026_06_02.md`, runtime hardening roadmap update, BETA/current-head report updates, WIKI/WIKI_INDEX updates, `musu-rs/src/install/sync.rs`, and CoS memory `2026-06-02_1650_kst_file_sync_watcher_storm_hardening.md`. Search terms include `GOAL v317`, `GOAL v318`, `wiki/557`, `1366 files`, `2242 symbols`, `SYNC_EVENT_QUEUE_CAPACITY`, `SYNC_BATCH_MAX_EVENTS`, `SYNC_BATCH_MAX_WINDOW`, `SYNC_BATCH_COOLDOWN`, `coalesce_sync_batch`, and `coalesce_sync_batch_keeps_latest_event_for_each_path`. |
| 2026-06-02 v319 | Fresh primary MSIX/runtime evidence was restored after file sync watcher storm hardening. | Rebuilt and installed `musu_1.15.0.0_x64_local-sideload-manual.msix` for commit `62381f7f`; explicit WindowsApps alias `musu up --json` reached bridge `http://127.0.0.1:8155`. Current evidence passes single-machine smoke `docs\evidence\single-machine\1.15.0-rc.1\20260602-171420-HUGH_SECOND.evidence.json`, desktop single-instance and process ownership `20260602-171500-HUGH_SECOND`, desktop-open CPU `docs\evidence\runtime-idle-cpu\1.15.0-rc.1\20260602-171538-HUGH_SECOND.desktop-open.evidence.json`, and runtime CPU matrix `docs\evidence\runtime-cpu-scenarios\1.15.0-rc.1\20260602-171659-HUGH_SECOND.runtime-cpu-scenario-matrix.json`. Single-machine task id is `60884022-fa9f-4e81-b0fc-775045bb63d0`; matrix route token is `MUSU_CPU_SCENARIO_ROUTE_OK_20260602_171659`; desktop-open CPU reports MUSU `0`, repo Node `0.03`, WebView2 `0.57`, hot `0`; process ownership reports runtime `1`, desktop `1`, MUSU-owned Node `0`, MUSU-owned WebView2 `7`, machine-wide Node `18`, orphan repo helpers `0`. Public release remains No-Go on second-PC CPU/matrix/route evidence, live `musu.pro` P2P owner-scope evidence, `musu@musu.pro` mailbox evidence, and Store evidence. |
| 2026-06-02 v320 | Code, docs, evidence, and specs were re-indexed after the post-file-sync primary evidence refresh. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1379 files and 2242 symbols after GOAL v319/v320, wiki/558, post-file-sync primary evidence report, current-head report, runtime hardening roadmap, network boundary spec, BETA checklist, WIKI/WIKI_INDEX updates, new evidence files, and CoS memory `2026-06-02_1725_kst_post_file_sync_primary_evidence_refresh.md`. Search terms include `GOAL v319`, `GOAL v320`, `wiki/558`, `1379 files`, `2242 symbols`, `20260602-171420-HUGH_SECOND`, `20260602-171538-HUGH_SECOND.desktop-open`, `20260602-171659-HUGH_SECOND.runtime-cpu-scenario-matrix`, `MUSU_CPU_SCENARIO_ROUTE_OK_20260602_171659`, `MUSU-owned Node 0`, `machine-wide Node 18`, and `busy-loop not reproduced`. |
| 2026-06-02 v321 | Runtime stop/down command was added for process ownership cleanup. | Added `musu stop` and `musu down`, both backed by `StopOpts` and `run_stop`, with `--json` schema `musu.stop_report.v1`. The command reads the registered bridge PID from `~\.musu\services\bridge.json`, terminates it only when the PID belongs to a MUSU runtime binary, refuses non-MUSU PIDs, removes stale bridge registry records, and waits for PID exit with bounded backoff via `wait_for_pid_exit`. Validation passed cargo fmt, `cargo check --manifest-path .\musu-rs\Cargo.toml --bin musu -j 1`, `cargo test --manifest-path .\musu-rs\Cargo.toml install::cli_commands --lib -- --test-threads=1` 14/14, `cargo build --manifest-path .\musu-rs\Cargo.toml --bin musu -j 1`, temporary-home `up --json` then `down --json` smoke with bridge PID `37292`, `registry_deregistered=true`, `pid_alive_after=false`, and idempotent `stop --json` no-op. This improves process cleanup but is Rust source, so primary MSIX evidence is stale again after commit. |
| 2026-06-02 v322 | Code and docs were re-indexed after runtime stop/down command hardening. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1382 files and 2245 symbols after GOAL v321/v322, wiki/559, `musu-rs/src/main.rs`, `musu-rs/src/install/cli_commands.rs`, runtime stop command report, BETA/current-head/runtime roadmap/WIKI/WIKI_INDEX updates, and CoS memory `2026-06-02_1815_kst_runtime_stop_command_hardening.md`. Search terms include `GOAL v321`, `GOAL v322`, `wiki/559`, `1382 files`, `2245 symbols`, `musu.stop_report.v1`, `StopOpts`, `run_stop`, `wait_for_pid_exit`, `musu down --json`, `musu stop --json`, and `process ownership cleanup`. |
| 2026-06-02 v323 | Fresh current-head primary MSIX/runtime evidence was restored after runtime stop/down hardening. | Rebuilt and installed `musu_1.15.0.0_x64_local-sideload-manual.msix` as `Yellowhama.MUSU_1.15.0.0_x64__ygcjq669as2b6`; explicit WindowsApps alias `musu up --json` / `musu down --json` proved `musu.stop_report.v1` cleanup with `registry_deregistered=true` and `pid_alive_after=false`. Current clean evidence passes single-machine smoke `docs\evidence\single-machine\1.15.0-rc.1\20260602-183133-HUGH_SECOND.evidence.json`, desktop single-instance and process ownership `20260602-183056-HUGH_SECOND`, desktop-open CPU `docs\evidence\runtime-idle-cpu\1.15.0-rc.1\20260602-183056-HUGH_SECOND.desktop-open.evidence.json`, and runtime CPU matrix `docs\evidence\runtime-cpu-scenarios\1.15.0-rc.1\20260602-183240-HUGH_SECOND.runtime-cpu-scenario-matrix.json`. Single-machine task id is `74e0a4fa-64ce-4463-a288-7b4ed2f7ba3a`; matrix route token is `MUSU_CPU_SCENARIO_ROUTE_OK_20260602_183240`; desktop-open CPU reports MUSU `0`, repo Node `0.03`, WebView2 `0`, working set `497.57MB`, hot `0`; process ownership reports runtime `1`, desktop `1`, MUSU-owned Node `0`, MUSU-owned WebView2 `6`, machine-wide Node `18`, orphan repo helpers `0`. Public release remains No-Go on second-PC CPU/matrix/route evidence, live `musu.pro` P2P owner-scope evidence, `musu@musu.pro` mailbox evidence, and Store evidence. |
| 2026-06-02 v324 | Code, docs, evidence, and specs were re-indexed after the post-stop/down primary evidence refresh. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1407 files and 2245 symbols after GOAL v323/v324, wiki/560, the post-stop/down primary evidence report, BETA/current-head/runtime roadmap/runtime stop command updates, WIKI/WIKI_INDEX updates, new evidence files, and CoS memory `2026-06-02_1820_kst_post_stop_down_primary_evidence_refresh.md`. Search terms include `GOAL v323`, `GOAL v324`, `wiki/560`, `1407 files`, `2245 symbols`, `20260602-183133-HUGH_SECOND`, `20260602-183056-HUGH_SECOND`, `20260602-183056-HUGH_SECOND.desktop-open`, `20260602-183240-HUGH_SECOND.runtime-cpu-scenario-matrix`, `MUSU_CPU_SCENARIO_ROUTE_OK_20260602_183240`, `musu.stop_report.v1`, `registry_deregistered=true`, `pid_alive_after=false`, `MUSU-owned Node 0`, `machine-wide Node 18`, and `busy-loop not reproduced`. |
| 2026-06-02 v325 | Second-PC release wrapper cleanup now records runtime cleanup evidence. | `run-second-pc-release-check.ps1` now runs cleanup in `finally`, writes `.local-build\runtime-cleanup\*.runtime-cleanup.json` with schema `musu.second_pc_runtime_cleanup.v1`, calls packaged `musu down --json --timeout-sec 5`, stops packaged `musu-desktop.exe` shells opened by the evidence run, includes cleanup JSON in the second-PC return zip, and requires cleanup success for wrapper `ok=true`. The multidevice kit README and operator action-pack quickstart now list cleanup evidence, and `verify-operator-action-pack.ps1` checks those instructions. Parser validation passed for the wrapper, kit generator, action-pack generator, and verifier; `test-release-evidence-verifiers.ps1` passed 13/13. Short local wrapper smoke with CPU/matrix skipped failed at MSIX install evidence capture because HUGH_SECOND still has known dev alias shadowing, but cleanup still produced `.local-build\runtime-cleanup\20260602-185052-HUGH_SECOND.runtime-cleanup.json` with `ok=true`, `stop_exit_code=0`, and `remaining_desktop_shell_count=0`. Public release remains No-Go until real current second-PC CPU/matrix/route, live P2P, support mailbox, and Store evidence pass. |
| 2026-06-02 v326 | Code, docs, and cleanup hardening notes were re-indexed after second-PC cleanup wiring. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1409 files and 2245 symbols after GOAL v325/v326, wiki/561, second-PC runtime cleanup hardening report, `run-second-pc-release-check.ps1`, `prepare-multidevice-test-kit.ps1`, `prepare-operator-action-pack.ps1`, `verify-operator-action-pack.ps1`, BETA/current-head/runtime roadmap/WIKI/WIKI_INDEX updates, and CoS memory `2026-06-02_1852_kst_second_pc_runtime_cleanup_hardening.md`. Search terms include `GOAL v325`, `GOAL v326`, `wiki/561`, `1409 files`, `2245 symbols`, `musu.second_pc_runtime_cleanup.v1`, `.local-build\runtime-cleanup`, `runtime_cleanup_ok`, `musu down --json --timeout-sec 5`, and `20260602-185052-HUGH_SECOND.runtime-cleanup.json`. |
| 2026-06-02 v327 | Current operator packet and action pack were regenerated after second-PC cleanup hardening. | Clean HEAD `a3cfdb5c153da2f3e2fca0f7ad337890290a2ff4` generated final operator packet `.local-build\final-operator-gates\musu-final-operator-gates-1.15.0-rc.1-20260602-185745.zip`; `verify-final-operator-gate-packet.ps1` passed with `ok=true`, `fail_count=0`, and `kit_count=1`. `prepare-operator-action-pack.ps1` generated `.local-build\operator-action-pack\MUSU-1.15.0-rc.1-operator-action-pack-20260602-185802.zip`; `verify-operator-action-pack.ps1` passed with `ok=true` and `fail_count=0`. Current second-PC transfer zip is `.local-build\operator-action-pack\MUSU-1.15.0-rc.1-operator-action-pack-20260602-185802\second-pc\MUSU-second-PC-transfer-1.15.0-rc.1-20260602-185802.zip`; Partner Center zip is `.local-build\operator-action-pack\MUSU-1.15.0-rc.1-operator-action-pack-20260602-185802\partner-center\MUSU-1.15.0-rc.1-store-submission-20260602-185802.zip`; support verification id is `musu-store-support-1.15.0-rc.1-20260602-185745` for `musu@musu.pro`. The action-pack verifier confirms the second-PC quickstart and nested kit README list runtime cleanup evidence for return. |
| 2026-06-02 v328 | Code, docs, and current handoff notes were re-indexed after the operator action pack refresh. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1410 files and 2245 symbols after GOAL v327/v328, wiki/562, BETA/current-head/WIKI/WIKI_INDEX updates, the current operator action pack refresh, and CoS memory `2026-06-02_1859_kst_current_operator_action_pack_after_cleanup.md`. Search terms include `GOAL v327`, `GOAL v328`, `wiki/562`, `1410 files`, `2245 symbols`, `20260602-185745`, `20260602-185802`, `MUSU-second-PC-transfer-1.15.0-rc.1-20260602-185802`, `MUSU-1.15.0-rc.1-store-submission-20260602-185802`, and `current operator action pack after cleanup`. |
| 2026-06-02 v329 | Runtime stop/down can now explicitly clean up desktop shells. | Added `--include-desktop` to `musu stop` / `musu down`. Default behavior remains bridge-runtime-only; with the flag, `musu.stop_report.v1` records `include_desktop`, `desktop_cleanup_attempted`, `desktop_pids_before`, `desktop_terminate_requested_pids`, `desktop_pids_after`, and `desktop_errors`. `run-second-pc-release-check.ps1` now calls `musu down --json --timeout-sec 5 --include-desktop` before its existing packaged desktop fallback. Validation passed cargo fmt, `cargo check --manifest-path .\musu-rs\Cargo.toml --bin musu -j 1`, services tests 15/15, install CLI tests 14/14, parser check for the second-PC wrapper, `git diff --check`, and a source CLI no-op smoke with `ok=true`, `desktop_cleanup_attempted=true`, and `desktop_errors=[]`. This improves process cleanup but makes packaged evidence stale until current MSIX evidence is refreshed after commit. |
| 2026-06-02 v330 | Code, docs, and stop/desktop cleanup notes were re-indexed. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1412 files and 2251 symbols after GOAL v329/v330, wiki/563, `RELEASE_1_15_0_RC1_STOP_DESKTOP_CLEANUP_HARDENING_2026_06_02.md`, `musu-rs\src\bridge\services.rs`, `musu-rs\src\install\cli_commands.rs`, `scripts\windows\run-second-pc-release-check.ps1`, BETA/current-head/runtime roadmap/WIKI/WIKI_INDEX updates, and CoS memory `2026-06-02_1926_kst_stop_desktop_cleanup_hardening.md`. Search terms include `GOAL v329`, `GOAL v330`, `wiki/563`, `1412 files`, `2251 symbols`, `--include-desktop`, `desktop_cleanup_attempted`, `desktop_pids_before`, `desktop_pids_after`, `desktop_errors`, `musu_desktop_pids`, and `wait_for_pids_exit`. |
| 2026-06-02 v331 | Fresh primary evidence was restored after stop/desktop cleanup hardening. | Rebuilt and installed `musu_1.15.0.0_x64_local-sideload-manual.msix` as `Yellowhama.MUSU_1.15.0.0_x64__ygcjq669as2b6`. Current evidence passes single-machine smoke `docs\evidence\single-machine\1.15.0-rc.1\20260602-195914-HUGH_SECOND.evidence.json`, desktop single-instance `docs\evidence\desktop-single-instance\1.15.0-rc.1\20260602-195058-HUGH_SECOND.desktop-single-instance.json`, process ownership `docs\evidence\process-ownership\1.15.0-rc.1\20260602-195129-HUGH_SECOND.process-ownership.json`, desktop-open CPU `docs\evidence\runtime-idle-cpu\1.15.0-rc.1\20260602-195140-HUGH_SECOND.desktop-open.evidence.json`, and runtime CPU matrix `docs\evidence\runtime-cpu-scenarios\1.15.0-rc.1\20260602-200531-HUGH_SECOND.runtime-cpu-scenario-matrix.json`. CPU reports MUSU `0`, WebView2 `0.39`, owned Node `0`, working set `362.27MB`, hot `0`; matrix route token is `MUSU_CPU_SCENARIO_ROUTE_OK_20260602_200531`. Packaged `musu down --json --timeout-sec 5 --include-desktop` stopped bridge PID `12472` and desktop PID `16460` with `desktop_pids_after=[]`. Clean go/no-go is still No-Go: single-machine true, runtime idle CPU `1/2`, runtime matrix `1/2`, process/desktop single-instance true, and blockers are second-PC route/CPU/matrix, live P2P, `musu@musu.pro`, and Store evidence. |
| 2026-06-02 v332 | Code, docs, evidence, and wiki were re-indexed after the post stop/desktop cleanup primary evidence refresh. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1421 files and 2251 symbols after GOAL v331/v332, wiki/564, `RELEASE_1_15_0_RC1_POST_STOP_DESKTOP_CLEANUP_PRIMARY_EVIDENCE_2026_06_02.md`, BETA/current-head/runtime roadmap/stop cleanup report updates, WIKI/WIKI_INDEX updates, new evidence files, and CoS memory `2026-06-02_2010_kst_post_stop_desktop_primary_evidence_refresh.md`. Search terms include `GOAL v331`, `GOAL v332`, `wiki/564`, `1421 files`, `2251 symbols`, `20260602-195914-HUGH_SECOND`, `20260602-195058-HUGH_SECOND.desktop-single-instance`, `20260602-195129-HUGH_SECOND.process-ownership`, `20260602-195140-HUGH_SECOND.desktop-open`, `20260602-200531-HUGH_SECOND.runtime-cpu-scenario-matrix`, `MUSU_CPU_SCENARIO_ROUTE_OK_20260602_200531`, `desktop_pids_after=[]`, `MUSU-owned Node 0`, and `machine-wide Node 16`. |
| 2026-06-02 v333 | Desktop activation now starts or reuses the bridge runtime. | `musu-bee\src-tauri\src\lib.rs` now spawns one background `musu-runtime-autostart` during Tauri setup when bridge health is missing or failed. The autostart/manual `Start Runtime` path now prefers the packaged sibling `musu.exe` next to `musu-desktop.exe` before falling back to PATH, reducing developer alias shadowing from `C:\Users\empty\.cargo\bin\musu.exe`. Validation passed `cargo fmt --manifest-path .\musu-bee\src-tauri\Cargo.toml`, `cargo test --manifest-path .\musu-bee\src-tauri\Cargo.toml -- --test-threads=1` 7/7, and `git diff --check`. This is Tauri source, so current packaged primary evidence is stale until MSIX rebuild/install proves desktop activation leaves bridge runtime running without manual `musu up`. |
| 2026-06-02 v334 | Code, docs, and desktop autostart spec were re-indexed. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1423 files and 2257 symbols after GOAL v333/v334, wiki/565, `RELEASE_1_15_0_RC1_DESKTOP_RUNTIME_AUTOSTART_HARDENING_2026_06_02.md`, Tauri source updates, BETA/current-head/runtime roadmap/WIKI/WIKI_INDEX updates, and CoS memory `2026-06-02_2030_kst_desktop_runtime_autostart_hardening.md`. Search terms include `GOAL v333`, `GOAL v334`, `wiki/565`, `1423 files`, `2257 symbols`, `musu-runtime-autostart`, `musu_command_path_for_current_exe`, `packaged sibling musu.exe`, `desktop activation starts bridge`, and `developer alias shadowing`. |
| 2026-06-02 v335 | Fresh primary MSIX evidence proves desktop runtime autostart. | Rebuilt and installed `musu_1.15.0.0_x64_local-sideload-manual.msix` from commit `47bc8f8d`. Desktop activation now passes process ownership without manual `musu up`: runtime `1`, desktop `1`, owned Node `0`, owned WebView2 `6`, bridge `127.0.0.1:14805` HTTP 200, installed runtime path under `C:\Program Files\WindowsApps\Yellowhama.MUSU_1.15.0.0_x64__ygcjq669as2b6\musu.exe`. Current evidence passes single-machine `docs\evidence\single-machine\1.15.0-rc.1\20260602-204104-HUGH_SECOND.evidence.json`, desktop single-instance `docs\evidence\desktop-single-instance\1.15.0-rc.1\20260602-203815-HUGH_SECOND.desktop-single-instance.json`, process ownership `docs\evidence\process-ownership\1.15.0-rc.1\20260602-203833-HUGH_SECOND.process-ownership.json`, desktop-open CPU `docs\evidence\runtime-idle-cpu\1.15.0-rc.1\20260602-203858-HUGH_SECOND.desktop-open.evidence.json`, and runtime matrix `docs\evidence\runtime-cpu-scenarios\1.15.0-rc.1\20260602-204112-HUGH_SECOND.runtime-cpu-scenario-matrix.json`. CPU reports MUSU `0`, WebView2 `0.42`, working set `364.02MB`, hot `0`; matrix route token is `MUSU_CPU_SCENARIO_ROUTE_OK_20260602_204112`. Public release remains No-Go on second-PC CPU/matrix/route, live P2P, support, and Store evidence. |
| 2026-06-02 v336 | Code, docs, evidence, and wiki were re-indexed after the post desktop autostart primary evidence refresh. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1432 files and 2257 symbols after GOAL v335/v336, wiki/566, `RELEASE_1_15_0_RC1_POST_DESKTOP_AUTOSTART_PRIMARY_EVIDENCE_2026_06_02.md`, desktop autostart report updates, BETA/current-head/runtime roadmap/WIKI/WIKI_INDEX updates, new evidence files, and CoS memory `2026-06-02_2045_kst_post_desktop_autostart_primary_evidence.md`. Search terms include `GOAL v335`, `GOAL v336`, `wiki/566`, `1432 files`, `2257 symbols`, `20260602-204104-HUGH_SECOND`, `20260602-203815-HUGH_SECOND.desktop-single-instance`, `20260602-203833-HUGH_SECOND.process-ownership`, `20260602-203858-HUGH_SECOND.desktop-open`, `20260602-204112-HUGH_SECOND.runtime-cpu-scenario-matrix`, `MUSU_CPU_SCENARIO_ROUTE_OK_20260602_204112`, `desktop autostart evidence`, and `bridge 127.0.0.1:14805`. |
| 2026-06-02 v337 | Cloud heartbeat hardware metadata was hardened for idle desktop use. | `musu-rs/src/peer/hardware.rs` added process-cached `gather_hardware_info_cached()` and Windows native RAM/CPU metadata probes using `GlobalMemoryStatusEx` and registry `RegGetValueW`; `musu-rs/src/bridge/mod.rs` now uses the cached hardware metadata in the logged-in `musu.pro` cloud registration loop. PowerShell/WMIC are no longer the default Windows RAM/CPU metadata path, and `nvidia-smi` remains timeout-bounded behind the cache so recurring heartbeats do not repeatedly spawn it. Validation passed `cargo fmt --manifest-path .\musu-rs\Cargo.toml`, `cargo check --manifest-path .\musu-rs\Cargo.toml --bin musu -j 1`, and `cargo test --manifest-path .\musu-rs\Cargo.toml peer::hardware --lib -- --test-threads=1` 3/3. This reduces a logged-in idle CPU candidate but makes packaged primary evidence stale until MSIX evidence is refreshed. |
| 2026-06-02 v338 | Code, docs, specs, and wiki were re-indexed after cloud hardware probe idle hardening. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1434 files and 2262 symbols after GOAL v337/v338, wiki/567, `RELEASE_1_15_0_RC1_CLOUD_HARDWARE_PROBE_IDLE_HARDENING_2026_06_02.md`, `musu-rs\src\peer\hardware.rs`, `musu-rs\src\bridge\mod.rs`, P2P control-plane spec, BETA/current-head/runtime roadmap/WIKI/WIKI_INDEX updates, and CoS memory `2026-06-02_2110_kst_cloud_hardware_probe_idle_hardening.md`. Search terms include `GOAL v338`, `wiki/567`, `1434 files`, `2262 symbols`, `gather_hardware_info_cached`, `OnceLock`, `GlobalMemoryStatusEx`, `RegGetValueW`, and `cloud hardware probe idle hardening`. |
| 2026-06-02 v339 | Fresh primary packaged evidence was restored after cloud hardware probe idle hardening. | Rebuilt and installed `musu_1.15.0.0_x64_local-sideload-manual.msix` from commit `9fff34aa`. Current evidence passes single-machine `docs\evidence\single-machine\1.15.0-rc.1\20260602-213655-HUGH_SECOND.evidence.json`, desktop single-instance `docs\evidence\desktop-single-instance\1.15.0-rc.1\20260602-213404-HUGH_SECOND.desktop-single-instance.json`, process ownership `docs\evidence\process-ownership\1.15.0-rc.1\20260602-213412-HUGH_SECOND.process-ownership.json`, desktop-open CPU `docs\evidence\runtime-idle-cpu\1.15.0-rc.1\20260602-213436-HUGH_SECOND.desktop-open.evidence.json`, and runtime matrix `docs\evidence\runtime-cpu-scenarios\1.15.0-rc.1\20260602-213706-HUGH_SECOND.runtime-cpu-scenario-matrix.json`. Desktop-open CPU reports MUSU `0`, Node `0`, WebView2 `0.49`, working set `363.18MB`, hot `0`; matrix route token is `MUSU_CPU_SCENARIO_ROUTE_OK_20260602_213706`. Public release remains No-Go on second-PC CPU/matrix/route, live P2P, support, and Store evidence. |
| 2026-06-02 v340 | Code, docs, evidence, and wiki were re-indexed after the post cloud hardware probe primary evidence refresh. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1443 files and 2262 symbols after GOAL v339/v340, wiki/568, `RELEASE_1_15_0_RC1_POST_CLOUD_HARDWARE_PROBE_PRIMARY_EVIDENCE_2026_06_02.md`, fresh evidence files, BETA/current-head/runtime roadmap/WIKI/WIKI_INDEX updates, and CoS memory `2026-06-02_2145_kst_post_cloud_hardware_probe_primary_evidence.md`. Search terms include `GOAL v340`, `wiki/568`, `1443 files`, `2262 symbols`, `20260602-213655-HUGH_SECOND`, `20260602-213436-HUGH_SECOND.desktop-open`, `20260602-213706-HUGH_SECOND.runtime-cpu-scenario-matrix`, and `MUSU_CPU_SCENARIO_ROUTE_OK_20260602_213706`. |
| 2026-06-02 v341 | Hosted P2P KV and second-PC reachability were rechecked. | Fresh P2P evidence `docs\evidence\p2p-control-plane\1.15.0-rc.1\20260602-215651-musu.pro.evidence.json` still verifies false: logged in, rendezvous wired, relay lease control-plane wired, runtime fallback wired, and `relay_default_data_path=false` pass, but relay leases are `ok=false`, `owner_scope_verified=false`, `owner_scoped=false` with `p2p_relay_lease_kv_not_configured`. `show-musu-pro-p2p-env-status.ps1 -Json` shows GitHub has `MUSU_P2P_CONTROL_TOKEN_SHA256S` but lacks `KV_REST_API_URL` and `KV_REST_API_TOKEN`; local process/user/machine env has no KV/Upstash values. `Test-NetConnection 192.168.1.192 -Port 8949` failed with `TcpTestSucceeded=false` and ping timeout, so the previous HUGH-MAIN target cannot produce fresh two-machine evidence right now. |
| 2026-06-02 v342 | Code, docs, evidence, and wiki were re-indexed after the P2P KV/second-PC recheck. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1448 files and 2262 symbols after GOAL v341/v342, wiki/569, `RELEASE_1_15_0_RC1_P2P_KV_SECOND_PC_RECHECK_2026_06_02.md`, fresh P2P evidence `20260602-215651-musu.pro`, P2P next-actions/spec updates, BETA/current-head/WIKI/WIKI_INDEX updates, and CoS memory `2026-06-02_2158_kst_p2p_kv_second_pc_recheck.md`. Search terms include `GOAL v342`, `wiki/569`, `1448 files`, `2262 symbols`, `p2p_relay_lease_kv_not_configured`, `KV_REST_API_URL`, `KV_REST_API_TOKEN`, and `TcpTestSucceeded=false`. |
| 2026-06-02 v343 | Route explain diagnostics were hardened against trusted-metadata confusion. | `musu route --explain` now treats registry/rendezvous candidate identity metadata as advertised-only: even if metadata claims `peer_identity_verified=true` or `encryption=https_tls_fingerprint_pin`, explain keeps `peer_identity_verified=false`, reports `peer_identity_method=advertised_tls_cert_fingerprint_unverified` when key material exists, and keeps `encryption=none_http_bearer` until runtime transport proof exists. Validation passed `cargo check --manifest-path .\musu-rs\Cargo.toml --bin musu -j 1`, `cargo test --manifest-path .\musu-rs\Cargo.toml install::cli_commands --lib -- --test-threads=1` 14/14, `cargo fmt --check`, and `git diff --check`. This improves the route-evidence trust boundary but does not implement QUIC/TLS; packaged primary evidence is stale until refreshed after this Rust source change. |
| 2026-06-02 v344 | Code, docs, specs, and wiki were re-indexed after route explain trust-boundary hardening. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1450 files and 2261 symbols after GOAL v343/v344, wiki/570, `RELEASE_1_15_0_RC1_ROUTE_EXPLAIN_TRUST_BOUNDARY_HARDENING_2026_06_02.md`, `musu-rs\src\install\cli_commands.rs`, BETA/current-head/P2P spec/WIKI/WIKI_INDEX updates, and CoS memory `2026-06-02_2218_kst_route_explain_trust_boundary_hardening.md`. Search terms include `GOAL v344`, `wiki/570`, `1450 files`, `2261 symbols`, `candidate_report_downgrades_verified_fingerprint_pin_metadata`, `advertised_tls_cert_fingerprint_unverified`, and `route explain trust boundary`. |
| 2026-06-02 v345 | Fresh packaged primary evidence was restored after route-explain hardening. | Rebuilt and installed `musu_1.15.0.0_x64_local-sideload-manual.msix` from commit `93025897`. Current evidence passes single-machine `docs\evidence\single-machine\1.15.0-rc.1\20260602-224345-HUGH_SECOND.evidence.json`, desktop single-instance `docs\evidence\desktop-single-instance\1.15.0-rc.1\20260602-223734-HUGH_SECOND.desktop-single-instance.json`, process ownership `docs\evidence\process-ownership\1.15.0-rc.1\20260602-223756-HUGH_SECOND.process-ownership.json`, desktop-open CPU `docs\evidence\runtime-idle-cpu\1.15.0-rc.1\20260602-223806-HUGH_SECOND.desktop-open.evidence.json`, and runtime matrix `docs\evidence\runtime-cpu-scenarios\1.15.0-rc.1\20260602-224917-HUGH_SECOND.runtime-cpu-scenario-matrix.json`. Desktop-open CPU reports MUSU `0`, Node `0`, WebView2 `0.39`, working set `365.49MB`, hot `0`; process ownership reports MUSU-owned Node `0` while machine-wide Node is `16`; matrix route token is `MUSU_CPU_SCENARIO_ROUTE_OK_20260602_224917`. Public release remains No-Go on second-PC CPU/matrix/route, live P2P KV/owner scope, support, and Store evidence. |
| 2026-06-02 v346 | Code, docs, evidence, and wiki were re-indexed after the post route-explain primary evidence refresh. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1459 files and 2261 symbols after GOAL v345/v346, wiki/571, `RELEASE_1_15_0_RC1_POST_ROUTE_EXPLAIN_PRIMARY_EVIDENCE_2026_06_02.md`, fresh evidence files, BETA/current-head/P2P spec/WIKI/WIKI_INDEX updates, and CoS memory `2026-06-02_2256_kst_post_route_explain_primary_evidence.md`. Search terms include `GOAL v346`, `wiki/571`, `1459 files`, `2261 symbols`, `20260602-224345-HUGH_SECOND`, `20260602-223806-HUGH_SECOND.desktop-open`, `20260602-224917-HUGH_SECOND.runtime-cpu-scenario-matrix`, and `MUSU_CPU_SCENARIO_ROUTE_OK_20260602_224917`. |
| 2026-06-02 v347 | Relay route evidence now requires issued lease proof for release grading. | `musu-bee/src/app/api/v1/p2p/route-evidence/route.ts` now keeps `route_kind=relay` evidence non-release-grade unless `relay_fallback` proves direct path failure, lease requested, `status=issued`, `lease_issued=true`, non-empty `lease_id`, a prior non-relay attempted route, and no relay policy blockers. Missing/denied proof adds blockers such as `relay_route_missing_lease_proof`, `relay_route_lease_not_issued`, and `relay_route_lease_blocked`. Validation passed `npm run test:p2p -- src/app/api/v1/p2p/route-evidence/route.test.ts` 23/23 and `npm run typecheck`. This is `musu.pro` verifier hardening; relay/tunnel transport and live KV-backed owner-scoped relay evidence remain open. |
| 2026-06-02 v348 | Code, docs, and wiki were re-indexed after relay route lease-proof hardening. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1461 files and 2261 symbols after GOAL v347/v348, wiki/572, `RELEASE_1_15_0_RC1_RELAY_ROUTE_LEASE_PROOF_HARDENING_2026_06_02.md`, `musu-bee\src\app\api\v1\p2p\route-evidence\route.ts`, route evidence tests, P2P spec/current-head/BETA/WIKI/WIKI_INDEX updates, and CoS memory `2026-06-02_relay_route_lease_proof_hardening.md`. Search terms include `GOAL v348`, `wiki/572`, `1461 files`, `2261 symbols`, `relay_route_missing_lease_proof`, `relay_route_lease_not_issued`, `relay_route_lease_blocked`, and `relay route lease proof`. |
| 2026-06-02 v349 | Current-commit single-machine evidence was refreshed after relay route lease-proof hardening. | Single-machine smoke passed for commit `f9beb79f3294ae73f049ad3649c71046179cee29` at `docs\evidence\single-machine\1.15.0-rc.1\20260602-231612-HUGH_SECOND.evidence.json` with dashboard task `a4ea114c-2483-4135-8dd0-756cf915d7a3`, bridge `http://127.0.0.1:13886`, and CLI route checked. `npm run build` also passed after `npm run test:p2p` 23/23 and `npm run typecheck`. Public release remains No-Go on second-PC route/CPU/matrix, live P2P KV/owner scope, support mailbox, Store evidence, and release-grade route transport. |
| 2026-06-02 v350 | Code, docs, evidence, and wiki were re-indexed after the relay route lease-proof single-machine refresh. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1465 files and 2261 symbols after GOAL v347-v350, wiki/572, route-evidence API hardening, `docs\evidence\single-machine\1.15.0-rc.1\20260602-231612-HUGH_SECOND.evidence.json`, BETA/current-head/P2P spec/WIKI/WIKI_INDEX updates, and CoS memories `2026-06-02_relay_route_lease_proof_hardening.md` and `2026-06-02_relay_route_lease_proof_single_machine_refresh.md`. Search terms include `GOAL v350`, `1465 files`, `2261 symbols`, `20260602-231612-HUGH_SECOND`, `relay_route_missing_lease_proof`, and `relay route lease-proof single-machine`. |
| 2026-06-02 v351 | P2P forwarded-task receipt now writes target-side audit rows. | `musu-rs/src/bridge/handlers/forward.rs` now writes an `audit_log` row after `/api/tasks/forward` accepts and spawns a forwarded cross-machine task. The row uses the real peer IP from `ConnectInfo`, `method=POST`, `path=/api/tasks/forward`, `status_code=202`, `cross_machine=true`, `company_id`, and a bounded note with task/source/rendezvous identifiers. The note excludes prompt text, cwd, callback URL, model, and adapter metadata. Added wiki/573 and CoS memory `2026-06-02_p2p_forwarded_task_audit_hardening.md`. Validation passed `cargo check --manifest-path .\musu-rs\Cargo.toml --bin musu -j 1` and focused unit test `forwarded_task_audit_note_is_bounded_and_excludes_prompt`. This improves P2P command forensics but does not close public release gates; packaged MSIX evidence is stale until rebuilt/refreshed after this Rust source change. |
| 2026-06-02 v352 | Code, docs, and wiki were re-indexed after P2P forwarded-task audit hardening. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1467 files and 2265 symbols after GOAL v351-v352, wiki/573, Rust forwarded task audit code/test updates, P2P spec/BETA/current-head/WIKI/WIKI_INDEX updates, and CoS memory `2026-06-02_p2p_forwarded_task_audit_hardening.md`. Search terms include `GOAL v352`, `wiki/573`, `1467 files`, `2265 symbols`, `forwarded_task_audit_note_is_bounded_and_excludes_prompt`, `/api/tasks/forward`, `cross_machine=true`, and `target-side audit`. |
| 2026-06-03 v353 | Fresh packaged primary evidence was restored after forwarded-task audit hardening. | Rebuilt and installed `musu_1.15.0.0_x64_local-sideload-manual.msix` from source commit `c25c109e`. Current evidence passes single-machine `docs\evidence\single-machine\1.15.0-rc.1\20260603-001225-HUGH_SECOND.evidence.json`, desktop single-instance `docs\evidence\desktop-single-instance\1.15.0-rc.1\20260603-000306-HUGH_SECOND.desktop-single-instance.json`, process ownership `docs\evidence\process-ownership\1.15.0-rc.1\20260603-000306-HUGH_SECOND.process-ownership.json`, desktop-open CPU `docs\evidence\runtime-idle-cpu\1.15.0-rc.1\20260603-001200-HUGH_SECOND.desktop-open.evidence.json`, and runtime matrix `docs\evidence\runtime-cpu-scenarios\1.15.0-rc.1\20260603-001416-HUGH_SECOND.runtime-cpu-scenario-matrix.json`. Desktop-open CPU reports MUSU `0.03`, Node `0`, WebView2 `0.08`, working set `454.06MB`, hot `0`; process ownership reports MUSU-owned Node `0` while machine-wide Node is `19`; matrix route token is `MUSU_CPU_SCENARIO_ROUTE_OK_20260603_001416`. Public release remains No-Go on second-PC CPU/matrix/route, live P2P KV/owner scope, support, and Store evidence. |
| 2026-06-03 v354 | Code, docs, evidence, and wiki were re-indexed after the post forwarded-task audit primary evidence refresh. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1480 files and 2265 symbols after GOAL v353-v354, wiki/574, fresh primary evidence files, `RELEASE_1_15_0_RC1_POST_FORWARDED_TASK_AUDIT_PRIMARY_EVIDENCE_2026_06_03.md`, BETA/current-head/P2P spec/WIKI/WIKI_INDEX updates, and CoS memory `2026-06-03_post_forwarded_task_audit_primary_evidence.md`. Search terms include `GOAL v354`, `wiki/574`, `1480 files`, `2265 symbols`, `20260603-001225-HUGH_SECOND`, `20260603-001416-HUGH_SECOND.runtime-cpu-scenario-matrix`, `MUSU_CPU_SCENARIO_ROUTE_OK_20260603_001416`, `MUSU-owned Node 0`, and `machine-wide Node 19`. |
| 2026-06-03 v355 | Hosted P2P storage now accepts Upstash REST env aliases. | Added `musu-bee/src/lib/p2pKvEnv.ts` and `p2pKvEnv.test.ts`; route-evidence, rendezvous, and relay lease stores now accept `UPSTASH_REDIS_REST_URL` / `UPSTASH_REDIS_REST_TOKEN` as alternatives to `KV_REST_API_URL` / `KV_REST_API_TOKEN` and map Upstash names into canonical KV names before `@vercel/kv` loads. `deploy-musu-bee.yml` syncs both name families, `configure-musu-pro-p2p-env.ps1` accepts Upstash input parameters, and `show-musu-pro-p2p-env-status.ps1` reports the combined missing-name blockers. Validation passed `npm run test:p2p` 27/27, `npm run build`, sequential `npm run typecheck`, Upstash dry-run configurator, and env-status JSON. Live P2P remains No-Go until real storage credentials are provisioned and owner-scoped evidence passes. |
| 2026-06-03 v356 | Code, docs, scripts, and wiki were re-indexed after P2P storage env alias hardening. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1484 files and 2272 symbols after GOAL v355-v356, wiki/575, `p2pKvEnv.ts`, `p2pKvEnv.test.ts`, P2P store updates, deploy workflow env-sync updates, configurator/status script updates, BETA/CONFIG/P2P spec/WIKI/WIKI_INDEX updates, and CoS memory `2026-06-03_p2p_storage_env_alias_hardening.md`. Search terms include `GOAL v356`, `wiki/575`, `1484 files`, `2272 symbols`, `UPSTASH_REDIS_REST_URL`, `UPSTASH_REDIS_REST_TOKEN`, and `P2P storage env alias hardening`. |
| 2026-06-03 v357 | Fresh packaged primary evidence was restored after P2P storage env alias hardening. | Rebuilt and installed `musu_1.15.0.0_x64_local-sideload-manual.msix` from source commit `fbd01746`. Current evidence passes single-machine `docs\evidence\single-machine\1.15.0-rc.1\20260603-005257-HUGH_SECOND.evidence.json`, desktop single-instance `docs\evidence\desktop-single-instance\1.15.0-rc.1\20260603-005000-HUGH_SECOND.desktop-single-instance.json`, process ownership `docs\evidence\process-ownership\1.15.0-rc.1\20260603-005010-HUGH_SECOND.process-ownership.json`, desktop-open CPU `docs\evidence\runtime-idle-cpu\1.15.0-rc.1\20260603-010000-HUGH_SECOND.desktop-open.evidence.json`, and runtime matrix `docs\evidence\runtime-cpu-scenarios\1.15.0-rc.1\20260603-010315-HUGH_SECOND.runtime-cpu-scenario-matrix.json`. Desktop-open CPU reports MUSU `0`, Node `0`, WebView2 `0.1`, working set `363.87MB`, hot `0`; process ownership reports MUSU-owned Node `0` while machine-wide Node is `16`; matrix route token is `MUSU_CPU_SCENARIO_ROUTE_OK_20260603_010315`. Public release remains No-Go on second-PC CPU/matrix/route, live P2P KV/Upstash owner scope, support, and Store evidence. |
| 2026-06-03 v358 | Code, docs, evidence, and wiki were re-indexed after the post P2P storage env alias primary evidence refresh. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1497 files and 2272 symbols after GOAL v357-v358, wiki/576, fresh primary evidence files, `RELEASE_1_15_0_RC1_POST_P2P_STORAGE_ENV_ALIAS_PRIMARY_EVIDENCE_2026_06_03.md`, BETA/current-head/WIKI/WIKI_INDEX updates, and CoS memory `2026-06-03_post_p2p_storage_alias_primary_evidence.md`. Search terms include `GOAL v358`, `wiki/576`, `1497 files`, `2272 symbols`, `20260603-010315-HUGH_SECOND`, `MUSU_CPU_SCENARIO_ROUTE_OK_20260603_010315`, and `post P2P storage env alias primary evidence`. |
| 2026-06-03 v359 | `musu status --json` was added for machine-readable fleet/process status. | Added `StatusOpts`, wired `Cmd::Status(StatusOpts)`, and made `run_status` emit schema `musu.fleet_status_cli.v1` with `ok`, `bridge_url`, and raw `/api/fleet/status` output when `--json` is supplied. Human-readable `musu status` is unchanged. Validation passed `cargo fmt --manifest-path .\musu-rs\Cargo.toml --check`, `cargo check --manifest-path .\musu-rs\Cargo.toml --bin musu -j 1`, `cargo test --manifest-path .\musu-rs\Cargo.toml install::cli_commands --lib -- --test-threads=1` 14/14, `cargo build --manifest-path .\musu-rs\Cargo.toml --bin musu -j 1`, binary parser test `status_cli_accepts_json_flag` 1/1, and debug runtime `up/status/down --json` smoke around bridge `127.0.0.1:6409`. Clean go/no-go on commit `f4ad68cb` reports `single_machine=false`, runtime idle CPU `0/2`, runtime matrix `0/2`, and `manifest_dirty=false` because Rust source changed and packaged evidence is stale again. |
| 2026-06-03 v360 | Code, docs, and wiki were re-indexed after status JSON hardening. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1499 files and 2274 symbols after GOAL v359-v360, wiki/577, `RELEASE_1_15_0_RC1_STATUS_JSON_HARDENING_2026_06_03.md`, `musu-rs\src\main.rs`, `musu-rs\src\install\cli_commands.rs`, BETA/current-head/WIKI/WIKI_INDEX updates, and CoS memory `2026-06-03_status_json_hardening.md`. Search terms include `GOAL v360`, `wiki/577`, `1499 files`, `2274 symbols`, `musu.fleet_status_cli.v1`, `StatusOpts`, `status_cli_accepts_json_flag`, and `status JSON hardening`. |
| 2026-06-03 v361 | Fresh packaged primary evidence was restored after status JSON hardening. | Rebuilt and installed `musu_1.15.0.0_x64_local-sideload-manual.msix` from packaged source commit `e2727025`. Current evidence passes single-machine `docs\evidence\single-machine\1.15.0-rc.1\20260603-021321-HUGH_SECOND.evidence.json`, desktop single-instance `docs\evidence\desktop-single-instance\1.15.0-rc.1\20260603-021134-HUGH_SECOND.desktop-single-instance.json`, process ownership `docs\evidence\process-ownership\1.15.0-rc.1\20260603-021134-HUGH_SECOND.process-ownership.json`, desktop-open CPU `docs\evidence\runtime-idle-cpu\1.15.0-rc.1\20260603-021134-HUGH_SECOND.desktop-open.evidence.json`, and runtime matrix `docs\evidence\runtime-cpu-scenarios\1.15.0-rc.1\20260603-021552-HUGH_SECOND.runtime-cpu-scenario-matrix.json`. Desktop-open CPU reports MUSU `0`, Node `0`, WebView2 `0.13`, working set `367.91MB`, hot `0`; process ownership reports MUSU-owned Node `0` while machine-wide Node is `16`; matrix route token is `MUSU_CPU_SCENARIO_ROUTE_OK_20260603_021552`. Clean go/no-go on manifest commit `043999d8` reports local artifacts and single-machine true, runtime idle CPU `1/2 [HUGH_SECOND]`, runtime CPU matrix `1/2 [HUGH_SECOND]`, and public release No-Go on second-PC, P2P, support, Store, and release-grade transport evidence. |
| 2026-06-03 v362 | Code, docs, evidence, and wiki were re-indexed after the post status JSON primary evidence refresh. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1512 files and 2274 symbols after GOAL v361-v362, wiki/578, fresh primary evidence files, `RELEASE_1_15_0_RC1_POST_STATUS_JSON_PRIMARY_EVIDENCE_2026_06_03.md`, BETA/current-head/WIKI/WIKI_INDEX updates, and CoS memory `2026-06-03_post_status_json_primary_evidence.md`. Search terms include `GOAL v362`, `wiki/578`, `1512 files`, `2274 symbols`, `20260603-021321-HUGH_SECOND`, `20260603-021552-HUGH_SECOND.runtime-cpu-scenario-matrix`, `MUSU_CPU_SCENARIO_ROUTE_OK_20260603_021552`, `MUSU-owned Node 0`, and `machine-wide Node 16`. |
| 2026-06-03 v363 | Current operator handoff packs were regenerated and P2P live state was rechecked. | From clean commit `f83174fb`, final packet `.local-build\final-operator-gates\musu-final-operator-gates-1.15.0-rc.1-20260603-023702.zip` verified with `ok=true`, `fail_count=0`, `kit_count=1`. Action pack `.local-build\operator-action-pack\MUSU-1.15.0-rc.1-operator-action-pack-20260603-023727.zip` verified with `ok=true`, `fail_count=0`. Current second-PC transfer zip is `.local-build\operator-action-pack\MUSU-1.15.0-rc.1-operator-action-pack-20260603-023727\second-pc\MUSU-second-PC-transfer-1.15.0-rc.1-20260603-023727.zip`; Partner Center zip is `.local-build\operator-action-pack\MUSU-1.15.0-rc.1-operator-action-pack-20260603-023727\partner-center\MUSU-1.15.0-rc.1-store-submission-20260603-023727.zip`. Fresh P2P evidence `docs\evidence\p2p-control-plane\1.15.0-rc.1\20260603-023834-musu.pro.evidence.json` records `ok=false`, relay status logged-in/wired, `relay_default_data_path=false`, relay leases `ok=false`, owner scope false, and live detail `p2p_relay_lease_kv_not_configured`; env status still lacks `KV_REST_API_URL_OR_UPSTASH_REDIS_REST_URL` and `KV_REST_API_TOKEN_OR_UPSTASH_REDIS_REST_TOKEN`. |
| 2026-06-03 v364 | Code, docs, evidence, and wiki were re-indexed after the operator pack/P2P recheck. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1517 files and 2274 symbols after GOAL v363-v364, wiki/579, fresh failing P2P evidence `20260603-023834-musu.pro`, `RELEASE_1_15_0_RC1_OPERATOR_PACK_P2P_RECHECK_2026_06_03.md`, BETA/current-head/WIKI/WIKI_INDEX updates, and CoS memory `2026-06-03_operator_pack_p2p_recheck.md`. Search terms include `GOAL v364`, `wiki/579`, `1517 files`, `2274 symbols`, `MUSU-second-PC-transfer-1.15.0-rc.1-20260603-023727.zip`, `20260603-023834-musu.pro.evidence.json`, and `p2p_relay_lease_kv_not_configured`. |
| 2026-06-03 v365 | Frontend shared polling now has a default task timeout. | `musu-bee/src/lib/useLowDutyPolling.ts` defines `DEFAULT_LOW_DUTY_POLL_TASK_TIMEOUT_MS = 10_000` and applies it when callers omit `taskTimeoutMs`, so shared low-duty polling tasks default to `AbortSignal.timeout(...)` / `AbortSignal.any(...)` cancellation. `runtime-polling-contract.test.ts` now asserts the constant and default binding. Validation passed `npx tsx --test src/app/runtime-polling-contract.test.ts` 10/10, `npm run typecheck`, `npm run build`, and `git diff --check`. Qualitative result: frontend polling hang risk is reduced, but the user's busy-loop report is not fully closed; fresh packaged primary evidence and second-PC CPU/matrix/route evidence are still required, and live `musu.pro` P2P remains blocked on owner-scoped KV/Upstash relay lease proof. |
| 2026-06-03 v366 | Code and docs were re-indexed after low-duty polling default timeout hardening. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1519 files and 2274 symbols after GOAL v365-v366, wiki/580, `useLowDutyPolling.ts`, `runtime-polling-contract.test.ts`, `RELEASE_1_15_0_RC1_LOW_DUTY_POLLING_DEFAULT_TIMEOUT_HARDENING_2026_06_03.md`, BETA/current-head/WIKI/WIKI_INDEX updates, and CoS memory `2026-06-03_low_duty_polling_default_timeout_hardening.md`. Search terms include `GOAL v366`, `1519 files`, `2274 symbols`, `DEFAULT_LOW_DUTY_POLL_TASK_TIMEOUT_MS`, `AbortSignal.timeout`, and `low-duty polling default timeout`. |
| 2026-06-03 v367 | Current packaged primary evidence was restored after low-duty polling hardening. | The local-sideload MSIX was rebuilt/replaced and primary local evidence was refreshed from clean commit `335f2836473137e2fae06f1f8ce0b0fc198678a9`. Current evidence passes single-machine `docs\evidence\single-machine\1.15.0-rc.1\20260603-031050-HUGH_SECOND.evidence.json`, desktop single-instance `docs\evidence\desktop-single-instance\1.15.0-rc.1\20260603-031229-HUGH_SECOND.desktop-single-instance.json`, process ownership `docs\evidence\process-ownership\1.15.0-rc.1\20260603-031234-HUGH_SECOND.process-ownership.json`, desktop-open CPU `docs\evidence\runtime-idle-cpu\1.15.0-rc.1\20260603-031248-HUGH_SECOND.desktop-open.evidence.json`, and runtime matrix `docs\evidence\runtime-cpu-scenarios\1.15.0-rc.1\20260603-031911-HUGH_SECOND.runtime-cpu-scenario-matrix.json`. Desktop-open CPU reports MUSU `0.03`, Node `0.05`, WebView2 `0.6`, working set `499.66MB`, hot `0`; process ownership reports MUSU-owned Node `0`, owned WebView2 `6`, machine-wide Node `18`, orphan repo helpers `0`; matrix route token is `MUSU_CPU_SCENARIO_ROUTE_OK_20260603_031911`. mDNS regression also emitted no `Failed to send`, `ff02::fb`, `10065`, or `closed channel`. Clean go/no-go is back to single-machine true, runtime idle CPU `1/2`, runtime CPU matrix `1/2`, process ownership true, desktop single-instance true, and public release No-Go on second-PC, P2P, support, Store, and release-grade transport evidence. |
| 2026-06-03 v368 | Code, docs, and fresh primary evidence were re-indexed. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1532 files and 2274 symbols after GOAL v367-v368, wiki/581, fresh primary evidence files, `RELEASE_1_15_0_RC1_POST_LOW_DUTY_POLLING_PRIMARY_EVIDENCE_2026_06_03.md`, BETA/current-head/WIKI/WIKI_INDEX updates, and CoS memory `2026-06-03_post_low_duty_polling_primary_evidence.md`. Search terms include `GOAL v368`, `1532 files`, `2274 symbols`, `20260603-031911-HUGH_SECOND.runtime-cpu-scenario-matrix`, `MUSU_CPU_SCENARIO_ROUTE_OK_20260603_031911`, and `post low-duty polling primary evidence`. |
| 2026-06-03 v369 | Current operator handoff packs were regenerated and P2P live state was rechecked. | From clean commit `aaf74ca2`, final packet `.local-build\final-operator-gates\musu-final-operator-gates-1.15.0-rc.1-20260603-033322.zip` verified with `ok=true`, `fail_count=0`, `kit_count=1`. Action pack `.local-build\operator-action-pack\MUSU-1.15.0-rc.1-operator-action-pack-20260603-033353.zip` verified with `ok=true`, `fail_count=0`. Current second-PC transfer zip is `.local-build\operator-action-pack\MUSU-1.15.0-rc.1-operator-action-pack-20260603-033353\second-pc\MUSU-second-PC-transfer-1.15.0-rc.1-20260603-033353.zip`; Partner Center zip is `.local-build\operator-action-pack\MUSU-1.15.0-rc.1-operator-action-pack-20260603-033353\partner-center\MUSU-1.15.0-rc.1-store-submission-20260603-033353.zip`. Second-PC `192.168.1.192:8949` remains unreachable with `TcpTestSucceeded=false` and ping timeout. Fresh P2P evidence `docs\evidence\p2p-control-plane\1.15.0-rc.1\20260603-033453-musu.pro.evidence.json` records verification `ok=false`, `fail_count=4`: relay leases not ok, owner scope not verified, owner-scoped false; env status still lacks `KV_REST_API_URL_OR_UPSTASH_REDIS_REST_URL` and `KV_REST_API_TOKEN_OR_UPSTASH_REDIS_REST_TOKEN`, live detail `p2p_relay_lease_kv_not_configured`, and `relay_default_data_path=false`. |
| 2026-06-03 v370 | Code, docs, evidence, and wiki were re-indexed after the operator pack/P2P recheck. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1537 files and 2274 symbols after GOAL v369-v370, wiki/582, fresh failing P2P evidence `20260603-033453-musu.pro`, `RELEASE_1_15_0_RC1_OPERATOR_PACK_P2P_RECHECK_2026_06_03_0335.md`, BETA/current-head/WIKI/WIKI_INDEX updates, and CoS memories `2026-06-03_operator_pack_p2p_recheck_0335.md` and `2026-06-03_operator_pack_p2p_recheck_index_refresh_0336.md`. Search terms include `GOAL v370`, `1537 files`, `2274 symbols`, `wiki/582 index refresh`, `operator pack P2P recheck index refresh`, and `p2p_relay_lease_kv_not_configured`. |
| 2026-06-03 v371 | Shared frontend low-duty polling now clamps accidental tight intervals and current primary evidence was restored. | `useLowDutyPolling` now defines `MIN_LOW_DUTY_POLL_INTERVAL_MS = 5_000`, `LOW_DUTY_HIDDEN_BACKOFF_MULTIPLIER = 4`, clamps `effectiveIntervalMs`/`effectiveMaxBackoffMs`, and guards visibility listener binding with `typeof document !== "undefined"`. Validation passed runtime-polling tests 11/11, `npm run typecheck`, `npm run build`, `npm run lint` with 0 errors and 74 existing warnings, and `git diff --check`. After rebuilding/replacing the local-sideload MSIX, current primary evidence passes single-machine `20260603-035325-HUGH_SECOND`, desktop single-instance `20260603-035450-HUGH_SECOND`, process ownership `20260603-035436-HUGH_SECOND`, desktop-open CPU `20260603-035458-HUGH_SECOND`, and runtime CPU matrix `20260603-035608-HUGH_SECOND`. Desktop-open CPU reports MUSU `0`, Node `0.03`, WebView2 `0.6`, working set `500.44MB`, hot `0`; matrix route token is `MUSU_CPU_SCENARIO_ROUTE_OK_20260603_035608`. Clean go/no-go remains public No-Go with runtime idle CPU and matrix `1/2 [HUGH_SECOND]`, multi-device/P2P/support/Store false, and runtime-package false because `C:\Users\empty\.cargo\bin\musu.exe` shadows the WindowsApps alias. |
| 2026-06-03 v372 | Code, docs, evidence, and wiki were re-indexed after polling interval clamp evidence. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1551 files and 2274 symbols after GOAL v371-v372, wiki/583, `useLowDutyPolling.ts`, `runtime-polling-contract.test.ts`, fresh primary evidence files, `RELEASE_1_15_0_RC1_POLLING_INTERVAL_CLAMP_PRIMARY_EVIDENCE_2026_06_03.md`, BETA/current-head/WIKI/WIKI_INDEX updates, and CoS memories `2026-06-03_polling_interval_clamp_primary_evidence.md` and `2026-06-03_polling_interval_clamp_index_refresh.md`. Search terms include `GOAL v372`, `1551 files`, `2274 symbols`, `wiki/583 index refresh`, `MIN_LOW_DUTY_POLL_INTERVAL_MS`, `20260603-035608-HUGH_SECOND.runtime-cpu-scenario-matrix`, and `MUSU_CPU_SCENARIO_ROUTE_OK_20260603_035608`. |
| 2026-06-03 v373 | Current operator handoff packs were regenerated after polling clamp evidence. | From clean HEAD `35eb9352`, final packet `.local-build\final-operator-gates\musu-final-operator-gates-1.15.0-rc.1-20260603-040654.zip` verified with `ok=true`, `fail_count=0`, `kit_count=1`. Action pack `.local-build\operator-action-pack\MUSU-1.15.0-rc.1-operator-action-pack-20260603-040714.zip` verified with `ok=true`, `fail_count=0`. Current second-PC transfer zip is `.local-build\operator-action-pack\MUSU-1.15.0-rc.1-operator-action-pack-20260603-040714\second-pc\MUSU-second-PC-transfer-1.15.0-rc.1-20260603-040714.zip`; Partner Center zip is `.local-build\operator-action-pack\MUSU-1.15.0-rc.1-operator-action-pack-20260603-040714\partner-center\MUSU-1.15.0-rc.1-store-submission-20260603-040714.zip`; support verification id is `musu-store-support-1.15.0-rc.1-20260603-040654`. |
| 2026-06-03 v374 | Code, docs, handoff notes, and wiki were re-indexed after the current operator pack refresh. | `musu indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1552 files and 2274 symbols after GOAL v373-v374, wiki/583 handoff updates, current final packet/action pack notes, BETA/current-head/WIKI/WIKI_INDEX updates, and CoS memory `2026-06-03_polling_interval_clamp_handoff_index_refresh.md`. Search terms include `GOAL v374`, `1552 files`, `2274 symbols`, `musu-final-operator-gates-1.15.0-rc.1-20260603-040654.zip`, and `MUSU-1.15.0-rc.1-operator-action-pack-20260603-040714.zip`. |
| 2026-06-03 v375 | MSIX alias shadowing release tooling was corrected and documented. | `msix-common.ps1` now treats `AliasShadowing` as true only when the first resolved `musu.exe` appears before the packaged WindowsApps alias, while still recording alternate aliases for operator visibility. `check-msix-legacy-conflicts.ps1`, `capture-msix-install-evidence.ps1`, and `verify-installed-msix-package.ps1` now surface `windowsapps_alias_invocation`, alias order, alternate alias sources, and remediation. Fresh validation shows the current local shadowing is real (`C:\Users\empty\.cargo\bin\musu.exe` before `C:\Users\empty\AppData\Local\Microsoft\WindowsApps\musu.exe`), but the installed package, manifest, Start menu entry, alias contract, and artifact contract match. `audit-desktop-release-readiness.ps1 -Json` reports `runtime_package_ready=True`, `desktop_shell_ready=True`, `single_machine_verified=True`; `write-release-go-no-go.ps1 -ScriptTimeoutSeconds 120` reports `local_artifacts_ready=True`, with public release still No-Go on second-PC route/CPU/matrix, live P2P owner scope, `musu@musu.pro`, Store evidence, and pre-commit dirty git. Canonical report is `docs\RELEASE_1_15_0_RC1_MSIX_ALIAS_SHADOWING_HARDENING_2026_06_03.md` (wiki/584). |
| 2026-06-03 v376 | Code, docs, specs, and wiki were re-indexed after MSIX alias shadowing hardening. | Explicit packaged alias indexing `& "$env:LOCALAPPDATA\Microsoft\WindowsApps\musu.exe" indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1555 files and 2274 symbols after GOAL v375-v376, wiki/584, MSIX alias shadowing tooling changes, `RELEASE_1_15_0_RC1_MSIX_ALIAS_SHADOWING_HARDENING_2026_06_03.md`, BETA/current-head/WIKI/WIKI_INDEX updates, and CoS memories `2026-06-03_msix_alias_shadowing_hardening.md` and `2026-06-03_msix_alias_shadowing_index_refresh.md`. Search terms include `GOAL v376`, `1555 files`, `2274 symbols`, `windowsapps_alias_invocation`, `AliasShadowing`, `AlternateAliasSources`, `runtime_package_ready=True`, and `local_artifacts_ready=True`. |
| 2026-06-03 v377 | Fresh external gate recheck confirms remaining blockers are still outside local artifact readiness. | `write-release-go-no-go.ps1 -ScriptTimeoutSeconds 120` on clean HEAD `c7b0d599` reports `local_artifacts_ready=True`, `single_machine_verified=True`, `msix_install_verified=True`, `msix_desktop_entrypoint_verified=True`, process/startup/desktop single-instance true, and public release No-Go on multi-device, runtime idle CPU `1/2`, runtime CPU scenario matrix `1/2`, support mailbox, Store, and P2P control-plane. `Test-NetConnection 192.168.1.192 -Port 8949` from source `192.168.1.154` on `이더넷 2` reports `PingSucceeded=False` and `TcpTestSucceeded=False`. Fresh P2P evidence `docs\evidence\p2p-control-plane\1.15.0-rc.1\20260603-043017-musu.pro.evidence.json` was recorded using the packaged WindowsApps alias; verification is still `ok=false`, `fail_count=4`, with route/rendezvous/lease wiring passing, `relay_default_data_path=false`, relay leases not ok, owner scope false, and live error `p2p_relay_lease_kv_not_configured`. Env status still lacks `KV_REST_API_URL_OR_UPSTASH_REDIS_REST_URL` and `KV_REST_API_TOKEN_OR_UPSTASH_REDIS_REST_TOKEN`. Canonical report is `docs\RELEASE_1_15_0_RC1_EXTERNAL_GATE_RECHECK_2026_06_03_0430.md` (wiki/585). |
| 2026-06-03 v378 | Code, docs, evidence, and wiki were re-indexed after the external gate recheck. | Explicit packaged alias indexing `& "$env:LOCALAPPDATA\Microsoft\WindowsApps\musu.exe" indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1561 files and 2274 symbols after GOAL v377-v378, wiki/585, fresh P2P evidence `20260603-043017-musu.pro`, `RELEASE_1_15_0_RC1_EXTERNAL_GATE_RECHECK_2026_06_03_0430.md`, BETA/current-head/WIKI/WIKI_INDEX updates, and CoS memories `2026-06-03_external_gate_recheck_0430.md` and `2026-06-03_external_gate_recheck_index_refresh_0430.md`. Search terms include `GOAL v378`, `1561 files`, `2274 symbols`, `wiki/585 index refresh`, `p2p_relay_lease_kv_not_configured`, `PingSucceeded=False`, and `TcpTestSucceeded=False`. |
| 2026-06-03 v379 | P2P control-plane evidence recorder now defaults to the packaged WindowsApps alias. | `scripts\windows\record-p2p-control-plane-evidence.ps1` now resolves MUSU in release-safe order: explicit `-MusuExe`, packaged WindowsApps alias, repo debug binary, then PATH. It records `musu_exe` and `musu_exe_source` in command output and evidence. Default-run validation generated `docs\evidence\p2p-control-plane\1.15.0-rc.1\20260603-044110-musu.pro.evidence.json` with `musu_exe_source=windowsapps_alias` and `musu_exe=C:\Users\empty\AppData\Local\Microsoft\WindowsApps\musu.exe`; verification still fails on `p2p_relay_lease_kv_not_configured`, proving the remaining P2P blocker is production KV/Upstash owner-scope, not local evidence binary selection. Freshness allowlists now include P2P recorder/verifier tooling, and dirty-tree go/no-go preserved `local_artifacts_ready=True` and `single_machine_verified=True`. Canonical report is `docs\RELEASE_1_15_0_RC1_P2P_EVIDENCE_RECORDER_ALIAS_HARDENING_2026_06_03.md` (wiki/586). |
| 2026-06-03 v380 | Code, docs, evidence, and wiki were re-indexed after P2P recorder alias hardening. | Explicit packaged alias indexing `& "$env:LOCALAPPDATA\Microsoft\WindowsApps\musu.exe" indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1567 files and 2274 symbols after GOAL v379-v380, wiki/586, `record-p2p-control-plane-evidence.ps1` alias hardening, fresh P2P evidence `20260603-044110-musu.pro`, `RELEASE_1_15_0_RC1_P2P_EVIDENCE_RECORDER_ALIAS_HARDENING_2026_06_03.md`, BETA/current-head/WIKI/WIKI_INDEX updates, and CoS memories `2026-06-03_p2p_evidence_recorder_alias_hardening.md` and `2026-06-03_p2p_evidence_recorder_alias_index_refresh.md`. Search terms include `GOAL v380`, `1567 files`, `2274 symbols`, `wiki/586 index refresh`, `musu_exe_source=windowsapps_alias`, and `20260603-044110-musu.pro`. |
| 2026-06-03 v381 | External release gate recheck is now a repeatable evidence artifact. | Added `scripts\windows\record-external-release-gate-recheck.ps1` with schema `musu.external_release_gate_recheck.v1`, operator packet/audit integration, and status-only freshness allowlists. Clean HEAD `d80e929e` generated `docs\evidence\external-gates\1.15.0-rc.1\20260603-050915-HUGH_SECOND.external-gates.evidence.json` plus P2P evidence `docs\evidence\p2p-control-plane\1.15.0-rc.1\20260603-051044-musu.pro.evidence.json`. Result: `local_artifacts_ready=True`, `single_machine_verified=True`, runtime idle CPU `1/2`, runtime CPU matrix `1/2`, second-PC `192.168.1.192:8949` unreachable from `192.168.1.154`, and P2P still blocked by `p2p_relay_lease_kv_not_configured`. Canonical report is `docs\RELEASE_1_15_0_RC1_EXTERNAL_RECHECK_RECORDER_2026_06_03.md` (wiki/587). |
| 2026-06-03 v382 | Code, docs, evidence, specs, and wiki were re-indexed after external recheck recorder clean evidence. | Explicit packaged alias indexing `& "$env:LOCALAPPDATA\Microsoft\WindowsApps\musu.exe" indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1576 files and 2274 symbols after GOAL v381-v382, wiki/587, external gate evidence `20260603-050915-HUGH_SECOND.external-gates`, P2P evidence `20260603-051044-musu.pro`, `RELEASE_1_15_0_RC1_EXTERNAL_RECHECK_RECORDER_2026_06_03.md`, P2P control-plane spec update, BETA/current-head/WIKI/WIKI_INDEX updates, and CoS memories `2026-06-03_external_recheck_recorder_clean_evidence.md` and `2026-06-03_external_recheck_recorder_index_refresh.md`. Search terms include `GOAL v382`, `1576 files`, `2274 symbols`, `wiki/587 index refresh`, and `musu.external_release_gate_recheck.v1`. |
| 2026-06-03 v383 | External gate second-PC reachability probe is now timeout-bounded. | `record-external-release-gate-recheck.ps1` now accepts `SecondPcProbeTimeoutMs` and records `probe_method=bounded_ping_and_tcp`, source IP/interface, bounded ping timing, bounded TCP timing, and timeout/error detail instead of relying on `Test-NetConnection` defaults. Clean HEAD `080bc6dc` generated `docs\evidence\external-gates\1.15.0-rc.1\20260603-052447-HUGH_SECOND.external-gates.evidence.json` and P2P evidence `docs\evidence\p2p-control-plane\1.15.0-rc.1\20260603-052547-musu.pro.evidence.json`. Result: `local_artifacts_ready=True`, `single_machine_verified=True`, second-PC `tcp_error=tcp_connect_timeout` after `3016ms`, and P2P still blocked by `p2p_relay_lease_kv_not_configured`. |
| 2026-06-03 v384 | Code, docs, evidence, specs, and wiki were re-indexed after bounded external gate probe evidence. | Explicit packaged alias indexing `& "$env:LOCALAPPDATA\Microsoft\WindowsApps\musu.exe" indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1583 files and 2274 symbols after GOAL v383-v384, wiki/588, bounded external gate evidence `20260603-052447-HUGH_SECOND.external-gates`, P2P evidence `20260603-052547-musu.pro`, BETA/current-head/spec/WIKI/WIKI_INDEX updates, and CoS memory `2026-06-03_bounded_external_probe_evidence.md`. Search terms include `GOAL v384`, `1583 files`, `2274 symbols`, `SecondPcProbeTimeoutMs`, `bounded_ping_and_tcp`, `ping_elapsed_ms=2887`, and `tcp_elapsed_ms=3016`. |
| 2026-06-03 v385 | Busy-loop and process attribution concerns were re-audited against current HEAD. | Current HEAD `6f32d490` passed `cargo test --manifest-path .\musu-rs\Cargo.toml --lib -j 1 peer::mdns::tests::` 3/3 and `npm run test:runtime-polling` 11/11. Code audit found no new fixed-delay busy-loop in the audited default paths: mDNS remains default-off, IPv6/Tailscale/VPN mDNS remain separately opt-in, disconnected mDNS browse receivers break immediately, frontend low-duty polling clamps intervals and cancels tasks, chat/relay reconnects remain bounded, and bridge `musu.pro` registration uses a low-duty heartbeat with 300s default, 60s minimum, backoff, and jitter. Live process attribution `.local-build\process-ownership\musu-process-ownership-20260603-053549.json` found MUSU not running (`musu_runtime=0`, missing bridge registry) and 16 machine-wide Node processes, all outside the MUSU process tree with orphan repo helpers `0`; this is diagnostic, not a MUSU-owned CPU release failure. Fresh go/no-go remains public No-Go while `local_artifacts_ready=True`, `single_machine_verified=True`, `public_metadata_ok=True`, `msix_install_verified=True`, and `msix_desktop_entrypoint_verified=True`. Canonical report is `docs\RELEASE_1_15_0_RC1_BUSY_LOOP_PROCESS_ATTRIBUTION_AUDIT_2026_06_03.md` (wiki/589). |
| 2026-06-03 v386 | Code, docs, and wiki were re-indexed after the busy-loop/process attribution audit. | Explicit packaged alias indexing `& "$env:LOCALAPPDATA\Microsoft\WindowsApps\musu.exe" indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1587 files and 2274 symbols after GOAL v385-v386, wiki/589, `RELEASE_1_15_0_RC1_BUSY_LOOP_PROCESS_ATTRIBUTION_AUDIT_2026_06_03.md`, BETA/current-head/WIKI/WIKI_INDEX updates, and CoS memories `2026-06-03_busy_loop_process_attribution_audit.md` and `2026-06-03_busy_loop_process_attribution_index_refresh.md`. Search terms include `GOAL v386`, `1587 files`, `2274 symbols`, `wiki/589 index refresh`, `busy-loop process attribution`, `musu-process-ownership-20260603-053549`, `machine-wide node 16`, and `owned_node=0`. |
| 2026-06-03 v387 | P2P relay lease storage is now explicit in API, CLI, and release evidence verification. | `p2pRelayLeaseStoreStatus` now reports `relay_lease_store_configured`, `relay_lease_store_backend`, and `relay_lease_store_release_grade` for KV, Upstash, explicit file, development-file, and unconfigured states. `/api/v1/p2p/relay/lease` includes those fields on policy denial, successful lease issue/query, and storage failure. `musu relay leases --json` maps the fields from successful responses and parses error JSON bodies so storage blockers remain visible on 503. `record-p2p-control-plane-evidence.ps1` summarizes the store fields, and `verify-p2p-control-plane-evidence.ps1` now fails missing/unconfigured/non-release-grade relay lease storage. `test-release-evidence-verifiers.ps1` added the `p2p-bad-store` regression. Validation passed `npm run test:p2p` 28/28, `npm run typecheck`, `cargo fmt --check`, `cargo check --bin musu -j 1`, Rust `install::cli_commands` tests 14/14, release evidence verifier regressions 14/14, and `git diff --check`. This is not relay payload transport; it makes the hosted KV/Upstash owner-scope blocker machine-verifiable. Fresh release evidence is required after commit/deploy because web API, Rust CLI, and verifier source changed. |
| 2026-06-03 v388 | Code, docs, specs, and wiki were re-indexed after P2P relay lease store status hardening. | Explicit packaged alias indexing `& "$env:LOCALAPPDATA\Microsoft\WindowsApps\musu.exe" indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1590 files and 2279 symbols after GOAL v387-v388, wiki/590, `RELEASE_1_15_0_RC1_P2P_RELAY_LEASE_STORE_STATUS_HARDENING_2026_06_03.md`, network boundary spec update, WIKI/WIKI_INDEX updates, and CoS memories `2026-06-03_p2p_relay_lease_store_status_hardening.md` and `2026-06-03_p2p_relay_lease_store_status_index_refresh.md`. Search terms include `GOAL v388`, `1590 files`, `2279 symbols`, `wiki/590 index refresh`, `relay_lease_store_configured`, `relay_lease_store_backend`, `relay_lease_store_release_grade`, and `p2p-bad-store`. |
| 2026-06-03 v389 | Live P2P evidence now proves the relay lease store blocker with current-source CLI fields. | Built current-source debug `musu.exe` with `cargo build --manifest-path .\musu-rs\Cargo.toml --bin musu -j 1`, then recorded live `musu.pro` P2P evidence using `record-p2p-control-plane-evidence.ps1 -MusuExe .\musu-rs\target\debug\musu.exe -AllowUnverified -Json`. Fresh artifacts are `docs\evidence\p2p-control-plane\1.15.0-rc.1\20260603-061246-musu.pro.evidence.json`, `.verification.json`, and `.summary.md`; evidence SHA256 is `889b16d305cd02dbe3c7894bb58026ebeefd4e76337bea62814f61c130d31ee0`. Verification remains `ok=false`, `fail_count=6`: relay status is logged in and `relay_default_data_path=false`, but relay leases are not ok, owner scope is not verified, owner-scoped is false, `relay_lease_store_configured=false`, `relay_lease_store_backend=unconfigured`, and `relay_lease_store_release_grade=false`. The live blocker remains `p2p_relay_lease_kv_not_configured`; relay payload transport also remains unwired. Canonical report is `docs\RELEASE_1_15_0_RC1_POST_RELAY_STORE_STATUS_LIVE_P2P_EVIDENCE_2026_06_03.md` (wiki/591). |
| 2026-06-03 v390 | Code, docs, evidence, and wiki were re-indexed after post relay-store-status live P2P evidence. | Explicit packaged alias indexing `& "$env:LOCALAPPDATA\Microsoft\WindowsApps\musu.exe" indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1596 files and 2279 symbols after GOAL v389-v390, wiki/591, fresh P2P evidence `20260603-061246-musu.pro`, `RELEASE_1_15_0_RC1_POST_RELAY_STORE_STATUS_LIVE_P2P_EVIDENCE_2026_06_03.md`, BETA/WIKI/WIKI_INDEX updates, and CoS memories `2026-06-03_post_relay_store_status_live_p2p_evidence.md` and `2026-06-03_post_relay_store_status_live_p2p_index_refresh.md`. Search terms include `GOAL v390`, `1596 files`, `2279 symbols`, `wiki/591 index refresh`, `musu_exe_source=parameter`, `relay_lease_store_backend=unconfigured`, and `relay_lease_store_release_grade=false`. |
| 2026-06-03 v391 | Fresh primary-machine release evidence was refreshed after relay store status live P2P evidence. | Single-machine evidence `docs\evidence\single-machine\1.15.0-rc.1\20260603-062456-HUGH_SECOND.evidence.json` passed with task `5fa8a73b-3d0b-4976-b234-0b9d256827c6` and output `MUSU_RELEASE_SMOKE_OK_20260603_062433`. Desktop-open CPU evidence `docs\evidence\runtime-idle-cpu\1.15.0-rc.1\20260603-062633-HUGH_SECOND.desktop-open.evidence.json` passed for `60.068s` with MUSU `0`, Node `0.05`, WebView2 `0.31`, working set `501.98MB`, and hot `0`. Runtime matrix `docs\evidence\runtime-cpu-scenarios\1.15.0-rc.1\20260603-063400-HUGH_SECOND.runtime-cpu-scenario-matrix.json` passed with route token `MUSU_CPU_SCENARIO_ROUTE_OK_20260603_063400`; all four scenarios were clean and under budget. Clean go/no-go on HEAD `85dec851` reports `local_artifacts_ready=True`, `single_machine_verified=True`, and public release No-Go because second-PC, runtime CPU 2/2, `musu.pro` owner-scoped KV/Upstash, `musu@musu.pro`, and Partner Center/Store evidence are still open. Canonical report is `docs\RELEASE_1_15_0_RC1_PRIMARY_EVIDENCE_REFRESH_AFTER_RELAY_STORE_STATUS_2026_06_03.md` (wiki/592). |
| 2026-06-03 v392 | Code, docs, evidence, specs, and wiki were re-indexed after the primary evidence refresh. | Explicit packaged alias indexing `& "$env:LOCALAPPDATA\Microsoft\WindowsApps\musu.exe" indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1607 files and 2279 symbols after GOAL v391, wiki/592, fresh primary evidence `20260603-062456-HUGH_SECOND`, `20260603-062633-HUGH_SECOND.desktop-open`, and `20260603-063400-HUGH_SECOND.runtime-cpu-scenario-matrix`, the primary evidence refresh report, BETA checklist, P2P/network boundary spec updates, WIKI/WIKI_INDEX updates, and CoS memories. Search terms include `GOAL v392`, `1607 files`, `2279 symbols`, `wiki/592 index refresh`, `primary evidence refresh index refresh`, and `MUSU_CPU_SCENARIO_ROUTE_OK_20260603_063400`. |
| 2026-06-03 v393 | External gate recheck can now use current-source CLI P2P evidence, and current operator packs were regenerated. | `record-external-release-gate-recheck.ps1` now accepts `-MusuExe` and records `p2p_evidence_musu_exe` plus `p2p_evidence_musu_exe_source`. PowerShell parse and `git diff --check` passed. Final packet `.local-build\final-operator-gates\musu-final-operator-gates-1.15.0-rc.1-20260603-065454.zip` verified with `ok=true`, `fail_count=0`, `kit_count=1`; action pack `.local-build\operator-action-pack\MUSU-1.15.0-rc.1-operator-action-pack-20260603-065519.zip` verified with `ok=true`, `fail_count=0`. Current second-PC transfer is `.local-build\operator-action-pack\MUSU-1.15.0-rc.1-operator-action-pack-20260603-065519\second-pc\MUSU-second-PC-transfer-1.15.0-rc.1-20260603-065519.zip`; Partner Center zip is `.local-build\operator-action-pack\MUSU-1.15.0-rc.1-operator-action-pack-20260603-065519\partner-center\MUSU-1.15.0-rc.1-store-submission-20260603-065519.zip`. Clean external evidence `20260603-065918-HUGH_SECOND.external-gates` used `.\musu-rs\target\debug\musu.exe` and recorded P2P evidence `20260603-070018-musu.pro` with `musu_exe_source=parameter`; release remains No-Go because second-PC is unreachable and P2P KV/Upstash storage is unconfigured. Canonical report is `docs\RELEASE_1_15_0_RC1_EXTERNAL_RECHECK_CLI_OVERRIDE_OPERATOR_PACK_2026_06_03.md` (wiki/593). |
| 2026-06-03 v394 | Code, docs, evidence, and wiki were re-indexed after the external recheck CLI override and operator pack refresh. | Explicit packaged alias indexing `& "$env:LOCALAPPDATA\Microsoft\WindowsApps\musu.exe" indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1615 files and 2279 symbols after GOAL v393, wiki/593, `record-external-release-gate-recheck.ps1` `-MusuExe` passthrough, fresh external evidence `20260603-065918-HUGH_SECOND.external-gates`, P2P evidence `20260603-070018-musu.pro`, the current operator pack report, BETA checklist, P2P spec, WIKI/WIKI_INDEX updates, and CoS memories. Search terms include `GOAL v394`, `1615 files`, `2279 symbols`, `wiki/593 index refresh`, `external recheck CLI override index refresh`, and `p2p_evidence_musu_exe_source=parameter`. |
| 2026-06-03 v395 | Fleet SSE lifecycle hardening closed a remaining frontend global-connection busy-loop risk. | Commit `aa23fc85c7caba0e05e3436df3aa3c64e3acfa39` updates `useFleetStore` with bounded Fleet EventSource reconnect (`1_000ms` initial, `10_000ms` max, multiplier `2`, max attempts `5`), stale reconnect cancellation through `fleetReconnectGeneration`, and explicit `closeSSE()` cleanup. `/dashboard/fleet` and `/dashboard/agent/[id]` now close the global SSE connection on unmount. Validation passed `npm run test:runtime-polling` 12/12, `npm run typecheck`, `npm run build`, and `git diff --check`. Clean go/no-go on `aa23fc85` remains public No-Go with `local_artifacts_ready=true`, `public_metadata_ok=true`, `msix_install_verified=true`, `msix_desktop_entrypoint_verified=true`, `manifest_git.dirty=false`, and `single_machine_verified=false` because frontend runtime source changed and fresh current-HEAD MSIX smoke/CPU/matrix evidence is required. Canonical report: `docs/RELEASE_1_15_0_RC1_FLEET_SSE_LIFECYCLE_HARDENING_2026_06_03.md` (wiki/594). |
| 2026-06-03 v396 | Code, docs, and wiki were re-indexed after Fleet SSE lifecycle hardening. | Explicit packaged alias indexing `& "$env:LOCALAPPDATA\Microsoft\WindowsApps\musu.exe" indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1618 files and 2283 symbols after GOAL v395, wiki/594, `RELEASE_1_15_0_RC1_FLEET_SSE_LIFECYCLE_HARDENING_2026_06_03.md`, Fleet SSE bounded reconnect code, dashboard `closeSSE()` cleanup, runtime-polling contract test update, BETA checklist, WIKI/WIKI_INDEX updates, and CoS memories `2026-06-03_fleet_sse_lifecycle_hardening.md` and `2026-06-03_fleet_sse_lifecycle_index_refresh.md`. Search terms include `GOAL v396`, `1618 files`, `2283 symbols`, `fleet SSE lifecycle hardening`, `FLEET_SSE_MAX_RETRIES`, `fleetReconnectGeneration`, `closeSSE`, `runtime-polling 12/12`, `aa23fc85`, and `single_machine_verified=false`. |
| 2026-06-03 v397 | Fresh primary evidence was restored after Fleet SSE lifecycle hardening. | Rebuilt and installed current local-sideload MSIX `.local-build\msix\output\musu_1.15.0.0_x64_local-sideload-manual.msix` as `Yellowhama.MUSU_1.15.0.0_x64__ygcjq669as2b6`. Single-machine evidence `docs\evidence\single-machine\1.15.0-rc.1\20260603-073941-HUGH_SECOND.evidence.json` passed with task `595585da-e3c5-43f4-8468-d1cec100133a` and output `MUSU_RELEASE_SMOKE_OK_20260603_073920`. Desktop-open CPU `docs\evidence\runtime-idle-cpu\1.15.0-rc.1\20260603-074231-HUGH_SECOND.desktop-open.evidence.json` passed for `60.061s` with MUSU `0`, Node `0.05`, WebView2 `0.16`, working set `500.12MB`, and hot `0`. Runtime matrix `docs\evidence\runtime-cpu-scenarios\1.15.0-rc.1\20260603-074415-HUGH_SECOND.runtime-cpu-scenario-matrix.json` passed with route token `MUSU_CPU_SCENARIO_ROUTE_OK_20260603_074415`. Clean go/no-go on `0428c200` reports `local_artifacts_ready=true`, `single_machine_verified=true`, runtime idle CPU valid machines `1`, runtime matrix valid machines `1`, and `manifest_git.dirty=false`; public release remains No-Go on second-PC, P2P, support, and Store evidence. `capture-msix-install-evidence.ps1` still fails on HUGH_SECOND because PATH shadows WindowsApps with `C:\Users\empty\.cargo\bin\musu.exe`, so runtime evidence used the explicit WindowsApps alias and no new MSIX install evidence was recorded to docs. Canonical report: `docs/RELEASE_1_15_0_RC1_POST_FLEET_SSE_PRIMARY_EVIDENCE_REFRESH_2026_06_03.md` (wiki/595). |
| 2026-06-03 v398 | Code, docs, evidence, and wiki were re-indexed after the post Fleet SSE primary evidence refresh. | Explicit packaged alias indexing `& "$env:LOCALAPPDATA\Microsoft\WindowsApps\musu.exe" indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1630 files and 2283 symbols after GOAL v397, wiki/595, fresh primary evidence `20260603-073941-HUGH_SECOND`, `20260603-074231-HUGH_SECOND.desktop-open`, and `20260603-074415-HUGH_SECOND.runtime-cpu-scenario-matrix`, the primary evidence refresh report, BETA checklist, runtime hardening roadmap, WIKI/WIKI_INDEX updates, and CoS memories `2026-06-03_post_fleet_sse_primary_evidence_refresh.md` and `2026-06-03_post_fleet_sse_primary_evidence_index_refresh.md`. Search terms include `GOAL v398`, `1630 files`, `2283 symbols`, `MUSU_CPU_SCENARIO_ROUTE_OK_20260603_074415`, `single_machine_verified=true`, `runtime idle CPU 1/2`, `runtime CPU matrix 1/2`, and `cargo bin shadows WindowsApps`. |
| 2026-06-03 v399 | MSIX alias-shadowing evidence now separates strict release gates from developer warning diagnostics. | Added `AliasShadowingMode=fail|warn-explicit-windowsapps` to `capture-msix-install-evidence.ps1`, `verify-msix-install-evidence.ps1`, and `record-msix-install-evidence.ps1`. Defaults remain strict: public release evidence still rejects PATH shadowing. Warning mode requires explicit opt-in, a discoverable packaged WindowsApps alias, `windowsapps_alias_invocation`, and alias-shadow-only legacy conflicts; startup/bin conflicts still fail. Real HUGH_SECOND warning capture `.local-build\msix-install-shadow-warning\20260603-080717-HUGH_SECOND.evidence.json` proves the installed package/manifest/startup/alias contract while recording `C:\Users\empty\.cargo\bin\musu.exe` before WindowsApps. Default verifier rejects it with `fail_count=4`; warning verifier accepts it with `fail_count=0`. Release verifier regressions passed `17/17`. Canonical report: `docs\RELEASE_1_15_0_RC1_MSIX_ALIAS_SHADOW_WARNING_POLICY_2026_06_03.md` (wiki/596). |
| 2026-06-03 v400 | Code, docs, specs, and wiki were re-indexed after the MSIX alias shadow warning policy. | Explicit packaged alias indexing `& "$env:LOCALAPPDATA\Microsoft\WindowsApps\musu.exe" indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1634 files and 2283 symbols after GOAL v399-v400, wiki/596, MSIX alias shadow warning tooling changes, the canonical report, BETA checklist, WIKI/WIKI_INDEX updates, and CoS memories `2026-06-03_msix_alias_shadow_warning_policy.md` and `2026-06-03_msix_alias_shadow_warning_index_refresh.md`. Search terms include `GOAL v400`, `1634 files`, `2283 symbols`, `wiki/596 index refresh`, `AliasShadowingMode=warn-explicit-windowsapps`, `alias_shadowing_accepted=true`, and `developer-warning-only`. |
| 2026-06-03 v401 | Hosted P2P release evidence now rejects lease-only relay readiness. | `verify-p2p-control-plane-evidence.ps1` now requires `relay_status.relay_transport_wired=true` and `relay_leases.relay_transport_wired=true`; `show-musu-pro-p2p-env-status.ps1` reports those fields and adds `live_evidence_relay_transport_not_wired`; `write-release-go-no-go.ps1` now states the hosted P2P gate requires both `relay_default_data_path=false` and `relay_transport_wired=true`. `test-release-evidence-verifiers.ps1` added `p2p-bad-relay-transport`, and regression validation passed `18/18`. Live evidence `docs\evidence\p2p-control-plane\1.15.0-rc.1\20260603-070018-musu.pro.evidence.json` now fails with `fail_count=8`, including relay status/lease transport failures, so KV/Upstash provisioning alone can no longer close the P2P gate. Clean post-commit go/no-go reports `manifest_git.dirty=false`, `local_artifacts_ready=false` due `runtime-package`, `single_machine_verified=true`, and `p2p_control_plane_verified=false`. Canonical report: `docs\RELEASE_1_15_0_RC1_P2P_RELAY_TRANSPORT_GATE_HARDENING_2026_06_03.md` (wiki/597). |
| 2026-06-03 v402 | Code, docs, specs, and wiki were re-indexed after P2P relay transport gate hardening. | Explicit packaged alias indexing `& "$env:LOCALAPPDATA\Microsoft\WindowsApps\musu.exe" indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1637 files and 2283 symbols after GOAL v401-v402, wiki/597, P2P verifier/env-status/go-no-go tooling, the relay transport gate report, P2P control-plane spec update, BETA checklist update, WIKI/WIKI_INDEX updates, and CoS memories `2026-06-03_p2p_relay_transport_gate_hardening.md` and `2026-06-03_p2p_relay_transport_gate_index_refresh.md`. Search terms include `GOAL v402`, `1637 files`, `2283 symbols`, `wiki/597 index refresh`, `relay_status.relay_transport_wired`, `relay_leases.relay_transport_wired`, and `live_evidence_relay_transport_not_wired`. |
| 2026-06-03 v403 | Release go/no-go no longer reports a false runtime-package blocker from PowerShell host mismatch. | `write-release-go-no-go.ps1`, `audit-desktop-release-readiness.ps1`, `verify-store-submission-bundle.ps1`, final-gate wrappers, and release verifier tests now reuse the current PowerShell executable instead of forcing `powershell.exe`; the Store submission bundle verifier also ensures `Get-FileHash` is available before checksum verification. This fixes the ProcessStartInfo + PowerShell 7 module-path failure where a valid Store bundle appeared to fail, making go/no-go report `runtime-package` even though standalone readiness audit passed. Validation passed parser checks, `git diff --check`, ProcessStartInfo Store bundle reproduction with `ok=true`/`fail_count=0`, readiness audit with `runtime_package_ready=true`, release verifier regressions `18/18`, and dirty-tree go/no-go with `local_artifacts_ready=true` and no `runtime-package` blocker. Public release remains No-Go on second-PC, two-machine CPU/matrix, support mailbox, Store, and hosted P2P relay transport evidence. Canonical report: `docs\RELEASE_1_15_0_RC1_RELEASE_GATE_POWERSHELL_HOST_HARDENING_2026_06_03.md` (wiki/598). |
| 2026-06-03 v404 | Code, docs, and wiki were re-indexed after release gate PowerShell host hardening. | Explicit packaged alias indexing `& "$env:LOCALAPPDATA\Microsoft\WindowsApps\musu.exe" indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1640 files and 2283 symbols after GOAL v403-v404, wiki/598, release gate PowerShell host hardening scripts, the canonical report, WIKI/WIKI_INDEX updates, and CoS memories `2026-06-03_release_gate_powershell_host_hardening.md` and `2026-06-03_release_gate_powershell_host_index_refresh.md`. Search terms include `GOAL v404`, `1640 files`, `2283 symbols`, `wiki/598 index refresh`, `Get-CurrentPowerShellExecutable`, `Get-FileHash`, `PSModulePath`, `runtime_package_ready=true`, and `local_artifacts_ready=true`. |
| 2026-06-03 v405 | Hosted P2P release evidence now requires owner-scoped relay route evidence. | Added `musu relay route-evidence --json` to query `/api/v1/p2p/route-evidence` with `route_kind=relay`, `result=success`, and `release_grade=true`; `record-p2p-control-plane-evidence.ps1` stores it as `relay_route_evidence`; `verify-p2p-control-plane-evidence.ps1` now fails unless `relay_route_evidence.count > 0` and `relay_route_evidence.relay_transport_proven=true`; `show-musu-pro-p2p-env-status.ps1` adds `live_evidence_relay_route_not_proven`. Validation passed parser checks, `cargo fmt --check`, `cargo check --bin musu -j 1`, Rust cloud tests `3/3`, Rust install CLI tests `14/14`, `npm run test:p2p` `28/28`, release verifier regressions `19/19`, and `git diff --check`. Fresh current-source live evidence `docs\evidence\p2p-control-plane\1.15.0-rc.1\20260603-093640-musu.pro.evidence.json` remains `ok=false`, `fail_count=13`, with `relay_lease_store_backend=unconfigured`, `relay_route_evidence_count=0`, and `relay_payload_transport_proven=false`. This blocks env-only relay transport claims; public release still needs real relay/tunnel payload transport and owner-scoped release-grade relay route evidence. Canonical report: `docs\RELEASE_1_15_0_RC1_P2P_RELAY_ROUTE_EVIDENCE_GATE_2026_06_03.md` (wiki/599). |
| 2026-06-03 v406 | Code, docs, evidence, and wiki were re-indexed after P2P relay route evidence gate hardening. | Explicit packaged alias indexing `& "$env:LOCALAPPDATA\Microsoft\WindowsApps\musu.exe" indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1646 files and 2291 symbols after GOAL v405-v406, wiki/599, `RELEASE_1_15_0_RC1_P2P_RELAY_ROUTE_EVIDENCE_GATE_2026_06_03.md`, fresh P2P evidence `20260603-093640-musu.pro`, Rust `musu relay route-evidence --json` CLI support, P2P verifier/recorder/env-status updates, WIKI/WIKI_INDEX updates, and CoS memories `2026-06-03_p2p_relay_route_evidence_gate.md` and `2026-06-03_p2p_relay_route_evidence_gate_index_refresh.md`. Search terms include `GOAL v406`, `1646 files`, `2291 symbols`, `wiki/599 index refresh`, `relay_route_evidence_count`, `relay_payload_transport_proven`, `live_evidence_relay_route_not_proven`, `case_count=19`, and `fail_count=13`. |
| 2026-06-03 v407 | Fresh primary-machine evidence was restored after P2P relay route evidence gate hardening. | Rebuilt and installed current local-sideload MSIX `.local-build\msix\output\musu_1.15.0.0_x64_local-sideload-manual.msix` as `Yellowhama.MUSU_1.15.0.0_x64__ygcjq669as2b6`; Rust release build took `9m 55s`, Tauri shell/MSIX packaging passed, and packaged runtime identity stayed `distribution=store-msix`. Single-machine evidence `docs\evidence\single-machine\1.15.0-rc.1\20260603-101716-HUGH_SECOND.evidence.json` passed with dashboard task `4e0a38e6-8daf-4435-998c-36590898feb7` and output `MUSU_RELEASE_SMOKE_OK_20260603_101654`. Desktop-open CPU `docs\evidence\runtime-idle-cpu\1.15.0-rc.1\20260603-100903-HUGH_SECOND.desktop-open.evidence.json` passed for `60.069s` with MUSU `0`, Node `0.03`, WebView2 `0.52`, hot `0`, and working set `496.76MB`. Runtime matrix `docs\evidence\runtime-cpu-scenarios\1.15.0-rc.1\20260603-101013-HUGH_SECOND.runtime-cpu-scenario-matrix.json` passed with route token `MUSU_CPU_SCENARIO_ROUTE_OK_20260603_101013`; all four scenarios were under budget. Dirty-tree go/no-go recognized `single_machine_verified=true`, runtime idle CPU valid machines `1`, and runtime CPU matrix valid machines `1`; public release remains No-Go on second-PC, P2P, support, and Store evidence. Canonical report: `docs\RELEASE_1_15_0_RC1_POST_RELAY_ROUTE_EVIDENCE_PRIMARY_REFRESH_2026_06_03.md` (wiki/600). |
| 2026-06-03 v408 | Code, docs, evidence, and wiki were re-indexed after the post relay-route-evidence primary refresh. | Explicit packaged alias indexing `& "$env:LOCALAPPDATA\Microsoft\WindowsApps\musu.exe" indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1654 files and 2291 symbols after GOAL v407-v408, wiki/600, fresh primary evidence `20260603-101716-HUGH_SECOND`, `20260603-100903-HUGH_SECOND.desktop-open`, and `20260603-101013-HUGH_SECOND.runtime-cpu-scenario-matrix`, the primary refresh report, BETA checklist, WIKI/WIKI_INDEX updates, and CoS memories `2026-06-03_post_relay_route_evidence_primary_refresh.md` and `2026-06-03_post_relay_route_evidence_primary_refresh_index_refresh.md`. Search terms include `GOAL v408`, `1654 files`, `2291 symbols`, `wiki/600 index refresh`, `MUSU_CPU_SCENARIO_ROUTE_OK_20260603_101013`, `WebView2 0.52`, `single_machine_verified=true`, `runtime idle CPU valid machines 1`, and `runtime CPU matrix valid machines 1`. |
| 2026-06-03 v409 | Release go/no-go now surfaces hosted P2P relay route evidence proof directly. | `write-release-go-no-go.ps1` now emits `p2p_owner_scope_verified`, `p2p_relay_lease_store_release_grade`, `p2p_relay_transport_wired`, `p2p_relay_route_evidence_ok`, `p2p_relay_route_evidence_count`, and `p2p_relay_payload_transport_proven` in JSON and text output. The `p2p-control-plane` blocker now requires owner-scoped release-grade relay lease storage, `relay_default_data_path=false`, `relay_transport_wired=true`, and owner-scoped release-grade relay route evidence with `relay_payload_transport_proven=true` and `count > 0`. Current output remains No-Go with route evidence count `0` and payload proof false. Validation passed PowerShell parser, JSON/non-JSON go/no-go output checks, `git diff --check`, and release evidence verifier regressions `19/19`. Canonical report: `docs\RELEASE_1_15_0_RC1_GO_NO_GO_P2P_ROUTE_EVIDENCE_OUTPUT_2026_06_03.md` (wiki/601). |
| 2026-06-03 v410 | Code, docs, and wiki were re-indexed after go/no-go P2P route evidence output hardening. | Explicit packaged alias indexing `& "$env:LOCALAPPDATA\Microsoft\WindowsApps\musu.exe" indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1657 files and 2291 symbols after GOAL v409-v410, wiki/601, `write-release-go-no-go.ps1` P2P output fields, `RELEASE_1_15_0_RC1_GO_NO_GO_P2P_ROUTE_EVIDENCE_OUTPUT_2026_06_03.md`, BETA checklist, WIKI/WIKI_INDEX updates, and CoS memories `2026-06-03_go_no_go_p2p_route_evidence_output.md` and `2026-06-03_go_no_go_p2p_route_evidence_output_index_refresh.md`. Search terms include `GOAL v410`, `1657 files`, `2291 symbols`, `wiki/601 index refresh`, `p2p_relay_route_evidence_count`, `p2p_relay_payload_transport_proven`, `p2p_relay_lease_store_release_grade`, and `release-grade relay route evidence`. |
| 2026-06-03 v411 | Runtime CPU scenario matrix now gates startup-open evidence. | `measure-musu-runtime-cpu-scenarios.ps1` now defaults to `startup-open,runtime-started,dashboard-open,desktop-open,post-route`; `verify-runtime-cpu-scenario-matrix.ps1` rejects `startup-open` unless the packaged desktop app was launched and sampling started within 3s; `write-release-go-no-go.ps1`, the second-PC wrapper, final operator packet, multi-device kit, handoff status, and release verifier fixture now require the five-scenario matrix. Validation passed PowerShell parser checks, `git diff --check`, release verifier regressions `20/20`, and fail-closed dashboard no-op verification for `20260603-105008-HUGH_SECOND` (`fail_count=1`). New primary matrix evidence `docs\evidence\runtime-cpu-scenarios\1.15.0-rc.1\20260603-105650-HUGH_SECOND.runtime-cpu-scenario-matrix.json` passed from clean commit `2defe28d9ff107813f476ae22720e2d715894f9e` with route token `MUSU_CPU_SCENARIO_ROUTE_OK_20260603_105650`, startup delay `2.026s`, startup WebView2 `1.51`, all five scenarios hot `0`, and verifier `fail_count=0`. Dirty-tree go/no-go reports `single_machine_verified=true`, runtime idle CPU `1/2`, and runtime CPU matrix `1/2`; public release remains No-Go on second-PC, P2P, support, and Store evidence. Canonical report: `docs\RELEASE_1_15_0_RC1_STARTUP_OPEN_CPU_MATRIX_GATE_2026_06_03.md` (wiki/602). |
| 2026-06-03 v412 | Code, docs, evidence, and wiki were re-indexed after the startup-open CPU matrix gate. | Explicit packaged alias indexing `& "$env:LOCALAPPDATA\Microsoft\WindowsApps\musu.exe" indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1660 files and 2291 symbols after GOAL v411-v412, wiki/602, startup-open CPU matrix gate scripts, primary evidence `20260603-105650-HUGH_SECOND.runtime-cpu-scenario-matrix`, the canonical report, BETA checklist, WIKI/WIKI_INDEX updates, and CoS memories `2026-06-03_startup_open_cpu_matrix_gate.md` and `2026-06-03_startup_open_cpu_matrix_gate_index_refresh.md`. Search terms include `GOAL v412`, `1660 files`, `2291 symbols`, `wiki/603 index refresh`, `startup-open`, `sample_delay_seconds`, `MUSU_CPU_SCENARIO_ROUTE_OK_20260603_105650`, and `runtime CPU matrix 1/2`. |
| 2026-06-03 v413 | Frontend polling busy-loop prevention is now a release go/no-go gate. | Added `scripts\windows\audit-frontend-polling-contract.ps1` with schema `musu.frontend_polling_contract.v1`; it verifies shared `useLowDutyPolling` timeout/abort/clamp/backoff behavior, dashboard/node/workflow/screen/agents/onboarding low-duty refresh wiring, dashboard relay on-demand connect plus capped reconnect, Chat/Fleet SSE bounded reconnect and cleanup, no direct non-test `setInterval(`, no direct `visibilitychange` listener outside the shared poller, package script, and CI wiring. `write-release-go-no-go.ps1` now emits `frontend_polling_contract_verified` and `frontend_polling_contract_audit`, and adds blocker area `frontend-polling` if the audit fails. Final operator packet generation/verification and handoff status now include the audit. Validation passed PowerShell parser checks, `audit-frontend-polling-contract.ps1 -Json` with `ok=true`/`fail_count=0`/direct interval hits `0`/direct visibility listener hits `0`, `npm run test:runtime-polling` 12/12, and release verifier regressions 20/20. This is release/status tooling, so current primary CPU evidence remains valid; public release remains No-Go on second-PC, two-machine CPU/matrix, hosted P2P, support mailbox, and Store evidence. Canonical report: `docs\RELEASE_1_15_0_RC1_FRONTEND_POLLING_CONTRACT_GO_NO_GO_GATE_2026_06_03.md` (wiki/604). |
| 2026-06-03 v414 | Code, docs, and wiki were re-indexed after the frontend polling contract go/no-go gate. | Explicit packaged alias indexing `& "$env:LOCALAPPDATA\Microsoft\WindowsApps\musu.exe" indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1664 files and 2291 symbols after GOAL v413, wiki/604, `audit-frontend-polling-contract.ps1`, `write-release-go-no-go.ps1` frontend polling fields/blocker, final operator packet/handoff status wiring, the canonical report, BETA checklist, WIKI/WIKI_INDEX updates, and CoS memories `2026-06-03_frontend_polling_contract_go_no_go_gate.md` and `2026-06-03_frontend_polling_contract_go_no_go_index_refresh.md`. Search terms include `GOAL v414`, `1664 files`, `2291 symbols`, `wiki/605 index refresh`, `musu.frontend_polling_contract.v1`, `frontend_polling_contract_verified`, `frontend-polling`, `direct_interval_hit_count`, and `runtime-polling 12/12`. |
| 2026-06-03 v415 | Relay route evidence now requires a matching owner-scoped stored relay lease. | `POST /api/v1/p2p/route-evidence` now imports `queryRelayLeases` and, for `route_kind=relay`, verifies that issued relay fallback proof references an actual stored relay lease matching owner key, `session_id`, `source_node_id`, `target_node_id`, `lease_id`, and attempted route kind set. Issued-looking relay JSON without a stored lease is non-release-grade with `relay_route_lease_not_found`; attempted route mismatch is non-release-grade with `relay_route_lease_attempts_mismatch`; lease store query failure is non-release-grade with `relay_route_lease_store_unavailable:<detail>`. Validation passed route-evidence API test 13/13, `npm run test:p2p` 29/29, `npm run typecheck`, and `git diff --check`. This hardens P2P evidence integrity but does not implement relay/tunnel payload transport; because runtime web source changed, fresh current-HEAD MSIX/smoke/CPU/matrix evidence is required before primary release evidence can be claimed current. Canonical report: `docs\RELEASE_1_15_0_RC1_RELAY_ROUTE_EVIDENCE_STORED_LEASE_GATE_2026_06_03.md` (wiki/606). |
| 2026-06-03 v416 | Code, docs, and wiki were re-indexed after the relay route evidence stored lease gate. | Explicit packaged alias indexing `& "$env:LOCALAPPDATA\Microsoft\WindowsApps\musu.exe" indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1667 files and 2296 symbols after GOAL v415, wiki/606, route-evidence stored lease verification source/tests, the P2P control-plane spec update, the canonical report, BETA checklist, WIKI/WIKI_INDEX updates, and CoS memories `2026-06-03_relay_route_evidence_stored_lease_gate.md` and `2026-06-03_relay_route_evidence_stored_lease_index_refresh.md`. Search terms include `GOAL v416`, `1667 files`, `2296 symbols`, `wiki/607 index refresh`, `relay_route_lease_not_found`, `relay_route_lease_attempts_mismatch`, `queryRelayLeases`, and `owner-scoped stored relay lease`. |
| 2026-06-03 v417 | Fresh primary-machine evidence was restored after the stored relay lease gate. | Rebuilt and installed current local-sideload MSIX `.local-build\msix\output\musu_1.15.0.0_x64_local-sideload-manual.msix` as `Yellowhama.MUSU_1.15.0.0_x64__ygcjq669as2b6`. Single-machine evidence `docs\evidence\single-machine\1.15.0-rc.1\20260603-120751-HUGH_SECOND.evidence.json` passed with task `afb7e08d-427b-4307-bdd5-4d5b165dd026` and output `MUSU_RELEASE_SMOKE_OK_20260603_120729`. Desktop-open CPU `docs\evidence\runtime-idle-cpu\1.15.0-rc.1\20260603-120903-HUGH_SECOND.desktop-open.evidence.json` passed for `60.054s` with MUSU `0`, Node `0.05`, WebView2 `0.08`, hot `0`, and working set `496.49MB`. Five-state runtime matrix `docs\evidence\runtime-cpu-scenarios\1.15.0-rc.1\20260603-121028-HUGH_SECOND.runtime-cpu-scenario-matrix.json` passed with route token `MUSU_CPU_SCENARIO_ROUTE_OK_20260603_121028`; all five scenarios were clean and under budget. Public release remains No-Go on second-PC, hosted P2P relay payload proof, support mailbox, and Store evidence. Canonical report: `docs\RELEASE_1_15_0_RC1_POST_STORED_LEASE_PRIMARY_EVIDENCE_REFRESH_2026_06_03.md` (wiki/608). |
| 2026-06-03 v418 | Code, docs, evidence, and wiki were re-indexed after the post stored-lease primary evidence refresh. | Explicit packaged alias indexing `& "$env:LOCALAPPDATA\Microsoft\WindowsApps\musu.exe" indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1675 files and 2296 symbols after GOAL v417, wiki/608, fresh primary evidence `20260603-120751-HUGH_SECOND`, `20260603-120903-HUGH_SECOND.desktop-open`, and `20260603-121028-HUGH_SECOND.runtime-cpu-scenario-matrix`, the primary refresh report, BETA checklist, WIKI/WIKI_INDEX updates, and CoS memories `2026-06-03_post_stored_lease_primary_evidence_refresh.md` and `2026-06-03_post_stored_lease_primary_evidence_index_refresh.md`. Search terms include `GOAL v418`, `1675 files`, `2296 symbols`, `wiki/609 index refresh`, `MUSU_CPU_SCENARIO_ROUTE_OK_20260603_121028`, and `post stored-lease primary evidence refresh`. |
| 2026-06-03 v419 | P2P relay transport descriptor/preflight evidence is now a required hosted gate. | Added authenticated `GET /api/v1/p2p/relay/transport` (`musu.p2p_relay_transport.v1`) and `musu relay transport --json` (`musu.relay_transport.v1`). The P2P evidence recorder now captures `relay_transport`, and the verifier now fails unless the descriptor is owner-scoped, `ok=true`, `relay_transport_descriptor_wired=true`, `relay_transport_wired=true`, `relay_default_data_path=false`, `payload_transit_requires_lease=true`, `relay_url` is `wss://`, and relay lease storage is release-grade, in addition to the existing lease and relay route evidence gates. Existing live P2P evidence `20260603-093640-musu.pro` now fails closed with `fail_count=31` because it lacks `relay_transport` and still has unconfigured storage and no relay route proof. Validation passed `npm run test:p2p` 34/34, `npm run typecheck`, `cargo check --bin musu -j 1`, PowerShell parser validation, release evidence verifier regressions 20/20, existing-evidence fail-closed verification, and `git diff --check`; debug `cargo build --bin musu` was stopped after more than six minutes with no output. This is not relay payload transport, and source changed after the latest primary evidence refresh, so current-HEAD MSIX/smoke/CPU/matrix evidence must be refreshed again after commit/build. Canonical report: `docs\RELEASE_1_15_0_RC1_P2P_RELAY_TRANSPORT_DESCRIPTOR_GATE_2026_06_03.md` (wiki/610). |
| 2026-06-03 v420 | Code, docs, and wiki were re-indexed after the P2P relay transport descriptor gate. | Explicit packaged alias indexing `& "$env:LOCALAPPDATA\Microsoft\WindowsApps\musu.exe" indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1681 files and 2307 symbols after GOAL v419, wiki/610, relay transport descriptor source/tests, P2P recorder/verifier updates, the canonical report, BETA/spec/WIKI/WIKI_INDEX updates, and CoS memories `2026-06-03_p2p_relay_transport_descriptor_gate.md` and `2026-06-03_p2p_relay_transport_descriptor_index_refresh.md`. Search terms include `GOAL v420`, `1681 files`, `2307 symbols`, `wiki/611 index refresh`, `musu.p2p_relay_transport.v1`, `musu.relay_transport.v1`, `relay_transport_descriptor_wired`, `payload_transit_requires_lease`, and `fail_count=31`. |
| 2026-06-03 v421 | The deployed relay transport descriptor endpoint is live and fail-closed. | After `654b9dcb` deployed to Vercel, a direct authenticated request to `https://musu.pro/api/v1/p2p/relay/transport` returned schema `musu.p2p_relay_transport.v1`, `owner_scoped=true`, `relay_transport_descriptor_wired=true`, `ok=false`, `relay_transport_wired=false`, `relay_default_data_path=false`, blank `relay_url`, `relay_lease_store_backend=unconfigured`, and blockers `relay_disabled`, `relay_transport_not_wired`, `relay_url_not_configured`, `connect_pro_entitlement_required`, `relay_lease_store_not_configured`, and `relay_lease_store_not_release_grade`. This confirms the deployed endpoint exists and fails closed; it is not a full `musu.p2p_control_plane_live_evidence.v1` capture and does not prove relay payload transport. |
| 2026-06-03 v422 | Fresh primary-machine evidence was restored after the relay transport descriptor gate. | Rebuilt and installed current local-sideload MSIX `.local-build\msix\output\musu_1.15.0.0_x64_local-sideload-manual.msix` as `Yellowhama.MUSU_1.15.0.0_x64__ygcjq669as2b6`. Single-machine evidence `docs\evidence\single-machine\1.15.0-rc.1\20260603-131556-HUGH_SECOND.evidence.json` passed with task `bba38031-b333-4b86-af61-64b65187a82b` and output `MUSU_RELEASE_SMOKE_OK_20260603_131531`. Desktop-open CPU `docs\evidence\runtime-idle-cpu\1.15.0-rc.1\20260603-131811-HUGH_SECOND.desktop-open.evidence.json` passed for `60.047s` with MUSU `0`, Node `0.05`, WebView2 `0.31`, hot `0`, and working set `497.94MB`. Five-state runtime matrix `docs\evidence\runtime-cpu-scenarios\1.15.0-rc.1\20260603-131938-HUGH_SECOND.runtime-cpu-scenario-matrix.json` passed with route token `MUSU_CPU_SCENARIO_ROUTE_OK_20260603_131938`; all five scenarios were clean and under budget. Clean go/no-go on `2fe8d220` reports `local_artifacts_ready=true`, `single_machine_verified=true`, runtime idle CPU valid machines `1/2`, runtime CPU matrix valid machines `1/2`, P2P control plane false, relay route evidence count `0`, relay payload proof false, and `git_dirty=false`. Public release remains No-Go on second-PC runtime/multi-device evidence, hosted P2P relay payload proof, support mailbox, and Store evidence. Canonical report: `docs\RELEASE_1_15_0_RC1_POST_TRANSPORT_DESCRIPTOR_PRIMARY_EVIDENCE_REFRESH_2026_06_03.md` (wiki/612). |
| 2026-06-03 v423 | Code, docs, evidence, and wiki were re-indexed after the post transport descriptor primary evidence refresh. | Explicit packaged alias indexing `& "$env:LOCALAPPDATA\Microsoft\WindowsApps\musu.exe" indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1689 files and 2307 symbols after GOAL v422, wiki/612, fresh primary evidence `20260603-131556-HUGH_SECOND`, `20260603-131811-HUGH_SECOND.desktop-open`, and `20260603-131938-HUGH_SECOND.runtime-cpu-scenario-matrix`, the primary refresh report, BETA/current-head/WIKI/WIKI_INDEX updates, and CoS memories `2026-06-03_post_transport_descriptor_primary_evidence_refresh.md` and `2026-06-03_post_transport_descriptor_primary_evidence_index_refresh.md`. Search terms include `GOAL v423`, `1689 files`, `2307 symbols`, `wiki/613 index refresh`, `MUSU_CPU_SCENARIO_ROUTE_OK_20260603_131938`, `post transport descriptor primary evidence refresh`, `runtime idle CPU 1/2`, and `runtime CPU matrix 1/2`. |
| 2026-06-03 v424 | Relay route evidence now requires explicit payload transport proof. | `POST /api/v1/p2p/route-evidence` now accepts optional `relay_transport_proof` with schema `musu.relay_transport_proof.v1`, and `route_kind=relay` can no longer become release-grade with only an issued stored lease. Relay release grading now requires proof lease/session match, `wss://` relay URL, positive payload byte transit, `payload_transited_musu_infra=true`, `encryption=quic_tls_1_3`, and `transport_verified_by=musu_quic_tls_transport`; missing or weak proof adds blockers such as `relay_route_missing_transport_proof`, `relay_route_transport_proof_lease_mismatch`, and `relay_route_transport_proof_not_verified`. Stored route-evidence queries with `release_grade=true` also exclude older relay records lacking the current proof contract. Rust cloud DTOs now include `RouteRelayTransportProof`; current bridge evidence explicitly submits no relay proof because relay payload transport is still not wired. Validation passed `npm run test:p2p` 35/35, `npm run typecheck`, `cargo check --bin musu -j 1`, focused Rust route-evidence serialization test, release verifier regressions 20/20, and `git diff --check`. This is evidence hardening, not relay transport implementation; packaged primary evidence is stale until MSIX/smoke/CPU/matrix are refreshed after this source change. Canonical report: `docs\RELEASE_1_15_0_RC1_RELAY_TRANSPORT_PROOF_GATE_2026_06_03.md` (wiki/614). |
| 2026-06-03 v425 | Code, docs, specs, and wiki were re-indexed after the relay transport proof gate. | Explicit packaged alias indexing `& "$env:LOCALAPPDATA\Microsoft\WindowsApps\musu.exe" indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1692 files and 2311 symbols after GOAL v424, wiki/614, `musu.relay_transport_proof.v1`, route-evidence source/tests, Rust cloud DTO updates, P2P spec updates, the canonical report, BETA/WIKI/WIKI_INDEX updates, and CoS memories `2026-06-03_relay_transport_proof_gate.md` and `2026-06-03_relay_transport_proof_index_refresh.md`. Search terms include `GOAL v425`, `1692 files`, `2311 symbols`, `wiki/615 index refresh`, `relay_route_missing_transport_proof`, `relay_route_transport_proof_not_verified`, and `musu.relay_transport_proof.v1`. |
| 2026-06-03 v426 | Fresh primary-machine evidence was restored after the relay transport proof gate. | Rebuilt and installed current local-sideload MSIX `.local-build\msix\output\musu_1.15.0.0_x64_local-sideload-manual.msix` as `Yellowhama.MUSU_1.15.0.0_x64__ygcjq669as2b6`. Single-machine evidence `docs\evidence\single-machine\1.15.0-rc.1\20260603-141358-HUGH_SECOND.evidence.json` passed with dashboard task `3e8522a2-73ef-4b51-bb3c-bb0b6bc251af` and output `MUSU_RELEASE_SMOKE_OK_20260603_141331`. Desktop-open CPU `docs\evidence\runtime-idle-cpu\1.15.0-rc.1\20260603-141524-HUGH_SECOND.desktop-open.evidence.json` passed for `60.059s` with MUSU `0`, Node `0.03`, WebView2 `0.44`, hot `0`, and working set `517.83MB`. Five-state runtime matrix `docs\evidence\runtime-cpu-scenarios\1.15.0-rc.1\20260603-141712-HUGH_SECOND.runtime-cpu-scenario-matrix.json` passed with route token `MUSU_CPU_SCENARIO_ROUTE_OK_20260603_141712`; all five scenarios were clean and under budget. Clean go/no-go on `2445c3bb` reports `local_artifacts_ready=true`, `single_machine_verified=true`, runtime idle CPU valid machines `1/2`, runtime CPU matrix valid machines `1/2`, P2P control plane false, relay route evidence count `0`, relay payload proof false, and `git_dirty=false`. Public release remains No-Go on second-PC runtime/multi-device evidence, hosted P2P relay payload proof, support mailbox, and Store evidence. Canonical report: `docs\RELEASE_1_15_0_RC1_POST_RELAY_TRANSPORT_PROOF_PRIMARY_EVIDENCE_REFRESH_2026_06_03.md` (wiki/616). |
| 2026-06-03 v427 | Code, docs, evidence, and wiki were re-indexed after the post relay transport proof primary evidence refresh. | Explicit packaged alias indexing `& "$env:LOCALAPPDATA\Microsoft\WindowsApps\musu.exe" indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1700 files and 2311 symbols after GOAL v426, wiki/616, fresh primary evidence `20260603-141358-HUGH_SECOND`, `20260603-141524-HUGH_SECOND.desktop-open`, and `20260603-141712-HUGH_SECOND.runtime-cpu-scenario-matrix`, the primary refresh report, BETA/WIKI/WIKI_INDEX updates, and CoS memories `2026-06-03_post_relay_transport_proof_primary_evidence_refresh.md` and `2026-06-03_post_relay_transport_proof_primary_evidence_index_refresh.md`. Search terms include `GOAL v427`, `1700 files`, `2311 symbols`, `wiki/617 index refresh`, `MUSU_CPU_SCENARIO_ROUTE_OK_20260603_141712`, `post relay transport proof primary evidence refresh`, `runtime idle CPU 1/2`, and `runtime CPU matrix 1/2`. |
| 2026-06-03 v428 | Targeted post-route CPU matrix diagnostics were wired for second-PC route attempts. | `measure-musu-runtime-cpu-scenarios.ps1` now accepts `-RouteTarget` and `-AllowFailedRouteProbe`, records `target`, command args, exit code, stdout/stderr, output, `ok`, and `failure_allowed` in the matrix `route_probe`, and still throws on nonzero route exit unless failure is explicitly allowed. `verify-runtime-cpu-scenario-matrix.ps1` now accepts `-ExpectedPostRouteTarget` and `-AllowFailedPostRouteProbe`; normal release verification still requires a successful post-route probe, while the diagnostic mode accepts an explicitly allowed failed target attempt and rejects target mismatch. `run-second-pc-release-check.ps1`, the multi-device kit README, and final handoff status commands now expose the same target-attempt path. Validation passed PowerShell parser checks, release evidence verifier regressions `22/22`, and `git diff --check`. Canonical report: `docs\RELEASE_1_15_0_RC1_TARGETED_POST_ROUTE_CPU_MATRIX_DIAGNOSTIC_2026_06_03.md` (wiki/618). This is CPU attribution tooling only; public release remains No-Go on second-PC runtime/multi-device evidence, hosted P2P relay payload proof, support mailbox, and Store evidence. |
| 2026-06-03 v429 | Code, docs, and wiki were re-indexed after the targeted post-route CPU matrix diagnostic. | Explicit packaged alias indexing `& "$env:LOCALAPPDATA\Microsoft\WindowsApps\musu.exe" indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1705 files and 2311 symbols after GOAL v428, wiki/618, targeted post-route CPU matrix scripts, the canonical report, BETA/WIKI/WIKI_INDEX updates, and CoS memories `2026-06-03_targeted_post_route_cpu_matrix_diagnostic.md` and `2026-06-03_targeted_post_route_cpu_matrix_index_refresh.md`. Search terms include `GOAL v429`, `1705 files`, `2311 symbols`, `wiki/619 index refresh`, `RouteTarget`, `AllowFailedRouteProbe`, `ExpectedPostRouteTarget`, `RuntimeCpuRouteTarget`, and `runtime-matrix-failed-target-route-attempt-allowed`. |
| 2026-06-03 v430 | Captured targeted post-route CPU evidence after a failed HUGH-MAIN route attempt and hardened the normal route-probe success check. | A clean detached worktree at commit `d26a2b78e3a8684e124aade5108887e261089487` ran `measure-musu-runtime-cpu-scenarios.ps1 -Scenario post-route -SampleSeconds 60 -RunRouteProbe -RouteTarget HUGH-MAIN -AllowFailedRouteProbe`. Evidence `docs\evidence\runtime-cpu-scenarios\1.15.0-rc.1\20260603-145454-HUGH_SECOND.runtime-cpu-scenario-matrix.json` and verification `20260603-145454-HUGH_SECOND.targeted-post-route.verification.json` passed with verifier `ok=true`, `fail_count=0`, expected target `HUGH-MAIN`, and allow-failed probe true. The route attempt timed out against `192.168.1.192:8949`, then the 60.049s CPU sample recorded hot process count `0`, MUSU CPU `0`, Node CPU `0`, WebView2 CPU `0.10`, WebView2 count `6`, and working set `402.69MB`; cleanup stopped bridge PID `29652` and desktop PID `37956`. `measure-musu-runtime-cpu-scenarios.ps1` now also rejects normal route probes when the expected per-run token is missing, so token-missing route output cannot pass as a successful release probe. Release verifier regressions still pass `22/22`. Canonical report: `docs\RELEASE_1_15_0_RC1_TARGETED_POST_ROUTE_CPU_EVIDENCE_2026_06_03.md` (wiki/620). This remains diagnostic CPU attribution only; public release still needs real second-PC runtime/multi-device evidence, hosted relay payload proof, support mailbox evidence, and Store evidence. |
| 2026-06-03 v431 | Code, docs, evidence, and wiki were re-indexed after the targeted post-route CPU evidence refresh. | Explicit packaged alias indexing `& "$env:LOCALAPPDATA\Microsoft\WindowsApps\musu.exe" indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1711 files and 2311 symbols after GOAL v430, wiki/620, targeted post-route CPU evidence JSON `20260603-145454-HUGH_SECOND`, the canonical evidence report, matrix route token-missing fail-fast hardening, BETA/WIKI/WIKI_INDEX updates, and CoS memories `2026-06-03_targeted_post_route_cpu_evidence.md` and `2026-06-03_targeted_post_route_cpu_evidence_index_refresh.md`. Search terms include `GOAL v431`, `1711 files`, `2311 symbols`, `wiki/621 index refresh`, `targeted-post-route.verification.json`, `HUGH-MAIN`, `192.168.1.192:8949`, `WebView2 0.10`, and `402.69MB`. |
| 2026-06-03 v432 | Relay transport readiness now fails closed on missing payload endpoint implementation. | `relayTransportWired()` now requires both `MUSU_P2P_RELAY_TRANSPORT_WIRED=1` and `RELAY_PAYLOAD_ENDPOINT_IMPLEMENTED=true`; current source keeps the marker false because `/api/v1/relay/connect` payload transport is not implemented. `GET /api/v1/p2p/relay/transport` reports `relay_payload_endpoint_wired=false`, transport preflight and relay lease policy add `relay_payload_endpoint_not_wired`, env-only fallback leases are denied, and relay route evidence adds `relay_route_transport_not_wired` plus `relay_route_payload_endpoint_not_wired` so stored leases plus proof-shaped JSON cannot become release-grade while the payload endpoint is absent. Validation passed `npm run test:p2p` `35/35`, `npm run typecheck`, and release evidence verifier regressions `22/22`. Canonical report: `docs\RELEASE_1_15_0_RC1_RELAY_PAYLOAD_ENDPOINT_FAIL_CLOSED_2026_06_03.md` (wiki/622). This is fail-closed release hardening, not relay payload transport implementation; public release remains No-Go on second-PC runtime/multi-device evidence, hosted relay payload proof, support mailbox evidence, and Store evidence. |
| 2026-06-03 v433 | Code, docs, and wiki were re-indexed after relay payload endpoint fail-closed hardening. | Explicit packaged alias indexing `& "$env:LOCALAPPDATA\Microsoft\WindowsApps\musu.exe" indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1715 files and 2312 symbols after GOAL v432, wiki/622, `RELAY_PAYLOAD_ENDPOINT_IMPLEMENTED`, `relay_payload_endpoint_wired`, `relay_payload_endpoint_not_wired`, route-evidence payload endpoint blockers, the canonical report, CONFIG/spec/BETA/WIKI/WIKI_INDEX updates, and CoS memories `2026-06-03_relay_payload_endpoint_fail_closed.md` and `2026-06-03_relay_payload_endpoint_fail_closed_index_refresh.md`. Search terms include `GOAL v433`, `1715 files`, `2312 symbols`, `wiki/623 index refresh`, `relay_route_payload_endpoint_not_wired`, and `RELAY_PAYLOAD_ENDPOINT_IMPLEMENTED=false`. |
| 2026-06-03 v434 | Startup helper source reproducibility was fixed and fresh primary-machine evidence was restored. | The root `.gitignore` `bin/` rule had hidden `musu-rs\src\bin\musu-startup.rs` from clean checkouts, causing clean MSIX release builds to fail with `can't find bin musu-startup`. Commit `79368c53` now tracks the startup helper source and unignores `musu-rs\src\bin\*.rs`; `cargo check --bin musu-startup -j 1` passed. A clean detached worktree at `79368c53` built and installed the local-sideload MSIX as `Yellowhama.MUSU_1.15.0.0_x64__ygcjq669as2b6`. Single-machine evidence `docs\evidence\single-machine\1.15.0-rc.1\20260603-160842-HUGH_SECOND.evidence.json` passed with task `5ed5bff5-6ac9-46b8-a2d5-b93d3211e447` and output `MUSU_RELEASE_SMOKE_OK_20260603_160819`. Desktop-open CPU `docs\evidence\runtime-idle-cpu\1.15.0-rc.1\20260603-161155-HUGH_SECOND.desktop-open.evidence.json` passed for `60.076s` with MUSU `0.03`, Node `0`, WebView2 `0.21`, hot `0`, and working set `461.69MB`. Five-state runtime matrix `docs\evidence\runtime-cpu-scenarios\1.15.0-rc.1\20260603-161836-HUGH_SECOND.runtime-cpu-scenario-matrix.json` passed with route token `MUSU_CPU_SCENARIO_ROUTE_OK_20260603_161836`; all five scenarios were clean and under budget. Dirty-tree go/no-go reports `local_artifacts_ready=true`, `single_machine_verified=true`, runtime idle CPU valid machines `1/2`, runtime CPU matrix valid machines `1/2`, P2P control plane false, relay payload proof false, and dirty git until this evidence is committed. Public release remains No-Go on second-PC runtime/multi-device evidence, hosted P2P relay payload proof, support mailbox, and Store evidence. Canonical report: `docs\RELEASE_1_15_0_RC1_POST_STARTUP_HELPER_SOURCE_PRIMARY_EVIDENCE_REFRESH_2026_06_03.md` (wiki/624). |
| 2026-06-03 v435 | Code, docs, evidence, and wiki were re-indexed after the startup helper source primary evidence refresh. | Explicit packaged alias indexing `& "$env:LOCALAPPDATA\Microsoft\WindowsApps\musu.exe" indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1728 files and 2315 symbols after GOAL v434, wiki/624, tracked `musu-rs\src\bin\musu-startup.rs`, fresh primary evidence `20260603-160842-HUGH_SECOND`, `20260603-161155-HUGH_SECOND.desktop-open`, and `20260603-161836-HUGH_SECOND.runtime-cpu-scenario-matrix`, the primary refresh report, BETA/WIKI/WIKI_INDEX updates, and CoS memory `2026-06-03_startup_helper_source_primary_evidence_refresh.md`. Search terms include `GOAL v435`, `1728 files`, `2315 symbols`, `wiki/625 index refresh`, `musu-startup.rs`, `MUSU_RELEASE_SMOKE_OK_20260603_160819`, and `MUSU_CPU_SCENARIO_ROUTE_OK_20260603_161836`. |
| 2026-06-03 v436 | Dashboard mount-time SSE reconnects are now bounded. | Added `musu-bee/src/lib/useBoundedEventSource.ts` and moved the fleet, company, machine, and `TasksPanel` mount-time streams off raw browser auto-retry. Failed streams are explicitly closed, reconnect on capped exponential backoff from `1s` to `10s`, stop after `5` retries, and close while the document is hidden. Low-duty polling remains the fallback. Runtime polling contract tests now assert the bounded hook and that these dashboard surfaces no longer contain raw `new EventSource(` calls or `Browser auto-retries` comments. Validation passed `npm run test:runtime-polling` `14/14`, `npm run typecheck`, `npm run build`, and `git diff --check` with only the existing CRLF normalization warning for `TasksPanel.tsx`. Go/no-go at 2026-06-03 16:51 KST still reports public release No-Go: local artifacts true, single-machine true, runtime idle CPU `1/2`, runtime CPU matrix `1/2`, multi-device false, P2P control plane false, relay transport false, relay payload proof false, and git dirty on this source change. Because this is runtime source, the previous clean packaged primary evidence is historical until a fresh MSIX/smoke/CPU/matrix refresh is captured after commit. Canonical report: `docs\RELEASE_1_15_0_RC1_BOUNDED_FRONTEND_SSE_HARDENING_2026_06_03.md` (wiki/626). |
| 2026-06-03 v437 | Code, docs, and wiki were re-indexed after bounded frontend SSE hardening. | Explicit packaged alias indexing `& "$env:LOCALAPPDATA\Microsoft\WindowsApps\musu.exe" indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1732 files and 2318 symbols after GOAL v436, wiki/626, `useBoundedEventSource`, fleet/company/machine/TasksPanel SSE migration, runtime-polling contract updates, the canonical report, BETA/WIKI/WIKI_INDEX updates, and CoS memory `2026-06-03_bounded_frontend_sse_hardening.md`. Search terms include `GOAL v437`, `1732 files`, `2318 symbols`, `wiki/627 index refresh`, `useBoundedEventSource`, `BOUNDED_SSE_MAX_RETRIES`, and `runtime-polling 14/14`. |
| 2026-06-03 v438 | Fresh primary-machine packaged evidence was restored after bounded frontend SSE hardening. | A clean package worktree at `4f52269e66c3e1ffdbde9d7936bcd83e4fb4a22c` rebuilt the release runtime, Tauri desktop shell, and local-sideload MSIX. The first generated cert stalled in non-interactive `certutil` and was not LocalMachine-trusted for same-version replacement, so the package was repacked with the existing LocalMachine-trusted `Yellowhama.MUSU` cert `9048EA2A9C0891A574C62F9DF2C1D0DA873952DE` and installed with explicit `-CertPath` plus `-SkipCertInstall`. Single-machine smoke `docs\evidence\single-machine\1.15.0-rc.1\20260603-173637-HUGH_SECOND.evidence.json` passed with dashboard task `90213081-7cc4-4144-872d-6c1da383259a`, output `MUSU_RELEASE_SMOKE_OK_20260603_173611`, bridge `http://127.0.0.1:9353`, and CLI route checked. Desktop-open CPU `docs\evidence\runtime-idle-cpu\1.15.0-rc.1\20260603-174002-HUGH_SECOND.desktop-open.evidence.json` passed for `60.044s` with MUSU `0`, Node `0`, WebView2 `0.29`, hot `0`, and working set `382.17MB`. Five-state matrix `docs\evidence\runtime-cpu-scenarios\1.15.0-rc.1\20260603-174322-HUGH_SECOND.runtime-cpu-scenario-matrix.json` passed with route token `MUSU_CPU_SCENARIO_ROUTE_OK_20260603_174322`, verifier `ok=true`, `fail_count=0`, max MUSU `0.03`, Node `0.03`, WebView2 `0.39`, and max working set `518.26MB`. Go/no-go at 2026-06-03 17:51 KST restored `single_machine_verified=true`, runtime idle CPU `1/2`, and runtime CPU matrix `1/2`. Residual: production Next stderr logged `ReferenceError: self is not defined` from `.next\server\app\m\[id]\workstation\page.js`; matrix did not exercise that route. Public release remains No-Go on second-PC runtime/multi-device evidence, hosted relay payload proof, support mailbox, and Store evidence. Canonical report: `docs\RELEASE_1_15_0_RC1_POST_BOUNDED_FRONTEND_SSE_PRIMARY_EVIDENCE_REFRESH_2026_06_03.md` (wiki/628). |
| 2026-06-03 v439 | Code, docs, fresh primary evidence, and wiki were re-indexed after the post bounded frontend SSE primary evidence refresh. | Explicit packaged alias indexing `& "$env:LOCALAPPDATA\Microsoft\WindowsApps\musu.exe" indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1745 files and 2318 symbols after GOAL v438, wiki/628, fresh primary evidence `20260603-173637-HUGH_SECOND`, `20260603-174002-HUGH_SECOND.desktop-open`, and `20260603-174322-HUGH_SECOND.runtime-cpu-scenario-matrix`, the primary refresh report, BETA/WIKI/WIKI_INDEX updates, and CoS memory `2026-06-03_post_bounded_frontend_sse_primary_evidence_refresh.md`. Search terms include `GOAL v439`, `1745 files`, `2318 symbols`, `wiki/629 index refresh`, `MUSU_RELEASE_SMOKE_OK_20260603_173611`, `MUSU_CPU_SCENARIO_ROUTE_OK_20260603_174322`, and `post bounded frontend SSE primary evidence refresh`. |
| 2026-06-03 v440 | CLI route evidence now preserves actual HTTPS fingerprint-pinned transport proof and bounded SSE visibility uses the shared low-duty poller. | Added `musu-rs\src\bridge\tls_pin.rs`, refactored bridge forwarding to share the pinned client helper, and taught `musu route` to use HTTPS peer metadata plus record `tls_cert_fingerprint_pin` only after a pinned request path succeeds. `useBoundedEventSource` now delegates visible reconnect checks through `useLowDutyPolling`, removing the direct `visibilitychange` listener. Validation passed Rust CLI route tests `17/17`, bridge forward tests `4/4`, route-evidence tests `7/7`, `cargo check --bin musu`, `npm run test:runtime-polling` `14/14`, frontend polling audit `ok=true`/`fail_count=0`, `npm run typecheck`, `npm run test:p2p` `35/35`, and `git diff --check`. Public release remains No-Go on multi-device, runtime CPU second-machine evidence, support mailbox, Store, hosted P2P relay payload proof, and current source git dirtiness; this is not QUIC/TLS relay payload implementation. Canonical report: `docs\RELEASE_1_15_0_RC1_CLI_ROUTE_PINNED_TRANSPORT_AND_BOUNDED_SSE_VISIBILITY_2026_06_03.md` (wiki/630). |
| 2026-06-03 v441 | Code, docs, and wiki were re-indexed after CLI route pinned transport and bounded SSE visibility hardening. | Explicit packaged alias indexing `& "$env:LOCALAPPDATA\Microsoft\WindowsApps\musu.exe" indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1749 files and 2326 symbols after GOAL v440, wiki/630, `musu-rs\src\bridge\tls_pin.rs`, CLI route HTTPS fingerprint-pinned evidence path updates, bounded SSE visible reconnect shared-poller updates, the canonical report, BETA/WIKI/WIKI_INDEX updates, and CoS memories `2026-06-03_cli_route_pinned_transport_and_bounded_sse_visibility.md` and `2026-06-03_cli_route_pinned_transport_and_bounded_sse_visibility_index_refresh.md`. Search terms include `GOAL v441`, `1749 files`, `2326 symbols`, `wiki/631 index refresh`, `tls_cert_fingerprint_pin`, `BOUNDED_SSE_VISIBILITY_RECONNECT_CHECK_MS`, and `runtime-polling 14/14`. |
| 2026-06-03 v442 | Fresh primary-machine packaged evidence was restored after CLI route pinned transport hardening. | Rebuilt and installed current local-sideload MSIX for commit `dded9eba67415cfdfd371f9c940fa2d59bd366ac` as `Yellowhama.MUSU_1.15.0.0_x64__ygcjq669as2b6`. Single-machine smoke `docs\evidence\single-machine\1.15.0-rc.1\20260603-190139-HUGH_SECOND.evidence.json` passed with output `MUSU_RELEASE_SMOKE_OK_20260603_190107`, CLI route output `MUSU_CLI_ROUTE_OK_20260603_190107`, bridge `http://127.0.0.1:6719`, and task `f95befcb-00c3-4430-b3d9-f31dfcef50df`. Desktop-open CPU `docs\evidence\runtime-idle-cpu\1.15.0-rc.1\20260603-190450-HUGH_SECOND.desktop-open.evidence.json` passed for `60.064s` with MUSU `0`, Node `0`, WebView2 `0.08`, hot `0`, and working set `466.26MB`. Five-state runtime matrix `docs\evidence\runtime-cpu-scenarios\1.15.0-rc.1\20260603-191447-HUGH_SECOND.runtime-cpu-scenario-matrix.json` passed verifier `ok=true`, `fail_count=0`, production dashboard URL `http://127.0.0.1:3001/app`, and route token `MUSU_CPU_SCENARIO_ROUTE_OK_20260603_191447`; max MUSU `0`, Node `0.03`, WebView2 `0.10`, and working set `595.78MB`. Dirty go/no-go now reports single-machine true, runtime idle CPU `1/2`, runtime CPU matrix `1/2`, and public release remains No-Go on second-PC runtime/multi-device evidence, hosted P2P relay payload proof, support mailbox, and Store evidence. Canonical report: `docs\RELEASE_1_15_0_RC1_POST_CLI_ROUTE_PINNED_TRANSPORT_PRIMARY_EVIDENCE_REFRESH_2026_06_03.md` (wiki/632). |
| 2026-06-03 v443 | Code, docs, fresh primary evidence, and wiki were re-indexed after the post CLI route pinned transport primary evidence refresh. | Explicit packaged alias indexing `& "$env:LOCALAPPDATA\Microsoft\WindowsApps\musu.exe" indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1760 files and 2326 symbols after GOAL v442, wiki/632, fresh primary evidence `20260603-190139-HUGH_SECOND`, `20260603-190450-HUGH_SECOND.desktop-open`, and `20260603-191447-HUGH_SECOND.runtime-cpu-scenario-matrix`, the primary refresh report, BETA/WIKI/WIKI_INDEX updates, and CoS memories `2026-06-03_post_cli_route_pinned_transport_primary_evidence_refresh.md` and `2026-06-03_post_cli_route_pinned_transport_primary_evidence_index_refresh.md`. Search terms include `GOAL v443`, `1760 files`, `2326 symbols`, `wiki/633 index refresh`, `MUSU_RELEASE_SMOKE_OK_20260603_190107`, `MUSU_CPU_SCENARIO_ROUTE_OK_20260603_191447`, `runtime idle CPU 1/2`, and `runtime CPU matrix 1/2`. |
| 2026-06-03 v444 | Relay connect now has an explicit fail-closed endpoint. | Added `musu-bee\src\app\api\v1\relay\connect\route.ts` and route tests so `/api/v1/relay/connect` returns `501` with schema `musu.relay_connect_unavailable.v1` instead of an opaque missing endpoint. The response keeps `relay_payload_endpoint_wired=false`, `relay_transport_wired=false`, `relay_default_data_path=false`, reports `relay_payload_transport_not_implemented`, and does not emit payload proof. Fresh local P2P evidence `.local-build\p2p-control-plane\20260603-193609-musu.pro.evidence.json` remains `ok=false` with `fail_count=19`; GitHub secret-name status has `MUSU_P2P_CONTROL_TOKEN_SHA256S` but lacks KV/Upstash URL/token names. Validation passed `npm run test:p2p` 37/37, `npm run typecheck`, `git diff --check`, dev `/app` 200, and dev `/api/v1/relay/connect` 501. Canonical report: `docs\RELEASE_1_15_0_RC1_RELAY_CONNECT_FAIL_CLOSED_ENDPOINT_2026_06_03.md` (wiki/634). This is failure handling, not relay payload transport; public release remains No-Go. |
| 2026-06-03 v445 | Code, docs, and wiki were re-indexed after relay connect fail-closed endpoint hardening. | Explicit packaged alias indexing `& "$env:LOCALAPPDATA\Microsoft\WindowsApps\musu.exe" indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1765 files and 2334 symbols after GOAL v444, wiki/634, `musu-bee\src\app\api\v1\relay\connect\route.ts`, `musu-bee\src\app\api\v1\relay\connect\route.test.ts`, the canonical report, BETA/WIKI/WIKI_INDEX updates, and CoS memory `2026-06-03_relay_connect_fail_closed_endpoint.md`. Search terms include `GOAL v445`, `1765 files`, `2334 symbols`, `wiki/635 index refresh`, `musu.relay_connect_unavailable.v1`, `relay_payload_transport_not_implemented`, and `test:p2p 37/37`. |
| 2026-06-03 v446 | Fresh primary-machine packaged evidence was restored after relay connect fail-closed endpoint hardening. | Rebuilt and installed current local-sideload MSIX for commit `e592bf608341f0461b03d55c7c0845ccf7781be0` as `Yellowhama.MUSU_1.15.0.0_x64__ygcjq669as2b6`. The prior packaged bridge PID `13732` was stopped so package replacement could continue. Single-machine smoke `docs\evidence\single-machine\1.15.0-rc.1\20260603-195528-HUGH_SECOND.evidence.json` passed with output `MUSU_RELEASE_SMOKE_OK_20260603_195506`, bridge `http://127.0.0.1:10502`, and task `c809f3ea-b9a5-4ae1-aed9-8b6a899c3d3a`. Desktop-open CPU `docs\evidence\runtime-idle-cpu\1.15.0-rc.1\20260603-195742-HUGH_SECOND.desktop-open.evidence.json` passed for `60.059s` with MUSU `0`, Node `0`, WebView2 `0.39`, hot `0`, and working set `521.48MB`. Five-state runtime matrix `docs\evidence\runtime-cpu-scenarios\1.15.0-rc.1\20260603-195917-HUGH_SECOND.runtime-cpu-scenario-matrix.json` passed verifier `ok=true`, `fail_count=0`, production dashboard URL `http://127.0.0.1:3001/app`, and route token `MUSU_CPU_SCENARIO_ROUTE_OK_20260603_195917`; max MUSU `0.42`, Node `0.05`, WebView2 `0.42`, and working set `527.72MB`. Dirty go/no-go sees single-machine true and runtime idle CPU `1/2`; public release remains No-Go on second-PC runtime/multi-device evidence, hosted relay payload proof, support mailbox, and Store evidence. Canonical report: `docs\RELEASE_1_15_0_RC1_POST_RELAY_CONNECT_PRIMARY_EVIDENCE_REFRESH_2026_06_03.md` (wiki/636). |
| 2026-06-03 v447 | Code, docs, fresh primary evidence, and wiki were re-indexed after the post relay connect primary evidence refresh. | Explicit packaged alias indexing `& "$env:LOCALAPPDATA\Microsoft\WindowsApps\musu.exe" indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1779 files and 2334 symbols after GOAL v446, wiki/636, fresh primary evidence `20260603-195528-HUGH_SECOND`, `20260603-195742-HUGH_SECOND.desktop-open`, and `20260603-195917-HUGH_SECOND.runtime-cpu-scenario-matrix`, the primary refresh report, BETA/WIKI/WIKI_INDEX updates, and CoS memory `2026-06-03_post_relay_connect_primary_evidence_refresh.md`. Search terms include `GOAL v447`, `1779 files`, `2334 symbols`, `wiki/637 index refresh`, `MUSU_RELEASE_SMOKE_OK_20260603_195506`, `MUSU_CPU_SCENARIO_ROUTE_OK_20260603_195917`, `runtime idle CPU 1/2`, and `runtime CPU matrix 1/2`. |
| 2026-06-03 v448 | Rust background-loop contracts are now release-gated. | Added `audit-rust-background-loop-contract.ps1` with schema `musu.rust_background_loop_contract.v1`. It verifies planner/clipboard/mDNS opt-in gates, mDNS IPv6/Tailscale/virtual-interface separate opt-ins, bounded mDNS browse receive, cloud registration heartbeat default `300s`/minimum `60s` plus failure backoff sleep, bounded file-sync queues/batches/timeouts/cooldown, auto-update minimum 5-minute supervise interval and health poll sleeps, and rejects new unaudited Rust loop constructs outside the allowlist. `write-release-go-no-go.ps1` now emits `rust_background_loop_contract_verified` and blocks public release with `rust-background-loops` if the audit fails; final handoff status and final operator packet generation/verification include the audit. Validation passed Rust background-loop audit `ok=true`/`fail_count=0`/`unaudited_loop_hit_count=0`, dirty-tree go/no-go showed `rust_background_loop_contract_verified=true` and `rust_fail_count=0`, `audit-desktop-release-readiness.ps1 -Json` still only fails on existing second-PC multi-device evidence, and `git diff --check` passed. Canonical report: `docs\RELEASE_1_15_0_RC1_RUST_BACKGROUND_LOOP_CONTRACT_GATE_2026_06_03.md` (wiki/638). |
| 2026-06-03 v449 | Code, docs, and wiki were re-indexed after the Rust background-loop contract gate. | Explicit packaged alias indexing `& "$env:LOCALAPPDATA\Microsoft\WindowsApps\musu.exe" indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1783 files and 2334 symbols after GOAL v448, wiki/638, `audit-rust-background-loop-contract.ps1`, go/no-go/handoff/packet wiring, the canonical report, BETA/WIKI/WIKI_INDEX updates, and CoS memories `2026-06-03_rust_background_loop_contract_gate.md` and `2026-06-03_rust_background_loop_contract_index_refresh.md`. Search terms include `GOAL v449`, `1783 files`, `2334 symbols`, `wiki/639 index refresh`, `musu.rust_background_loop_contract.v1`, `rust_background_loop_contract_verified`, `rust-background-loops`, and `unaudited_loop_hit_count=0`. |
| 2026-06-03 v450 | P2P relay status now reports the live transport descriptor and payload endpoint blockers. | `musu relay status --json` now queries hosted relay transport, mirrors preflight/descriptor/payload endpoint/store/blocker/error fields, and `musu relay transport --json` includes `relay_payload_endpoint_wired`. The P2P evidence verifier now requires status preflight, descriptor, payload endpoint, empty blockers, release-grade lease store, transport payload endpoint, and release-grade route proof before `relay_transport_wired` can be true; recorder and go/no-go output surface the same fields. Validation passed release evidence verifier regressions `22/22`, `cargo check --lib`, targeted Rust relay status test `1/1`, `npm run test:p2p` `37/37`, and `git diff --check`. Dirty go/no-go remains No-Go with single-machine true, runtime idle CPU `1/2`, runtime CPU matrix `1/2`, `p2p_control_plane_verified=false`, and all new relay status/transport payload endpoint fields false. Canonical report: `docs\RELEASE_1_15_0_RC1_P2P_RELAY_STATUS_DESCRIPTOR_GATE_2026_06_03.md` (wiki/640). |
| 2026-06-03 v451 | Code, docs, and wiki were re-indexed after the P2P relay status descriptor gate. | Explicit packaged alias indexing `& "$env:LOCALAPPDATA\Microsoft\WindowsApps\musu.exe" indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1786 files and 2339 symbols after GOAL v450, wiki/640, Rust relay status live transport descriptor mapping, P2P recorder/verifier/go-no-go field updates, the canonical report, BETA/WIKI/WIKI_INDEX updates, and CoS memory `2026-06-03_p2p_relay_status_descriptor_gate.md`. Search terms include `GOAL v451`, `1786 files`, `2339 symbols`, `wiki/641 index refresh`, `relay_status_transport_preflight_ok`, `relay_status_payload_endpoint_wired`, `relay_transport_payload_endpoint_wired`, and `relay_status_reflects_live_transport_descriptor`. |
| 2026-06-03 v452 | Fresh primary-machine packaged evidence was restored after the relay status descriptor gate. | Rebuilt and installed current local-sideload MSIX for commit `16b7373d383751932651c926225aedbf946a9b99` as `Yellowhama.MUSU_1.15.0.0_x64__ygcjq669as2b6`. Single-machine smoke `docs\evidence\single-machine\1.15.0-rc.1\20260603-213326-HUGH_SECOND.evidence.json` passed with output `MUSU_RELEASE_SMOKE_OK_20260603_213326`, dashboard `http://127.0.0.1:3001`, bridge `http://127.0.0.1:8290`, and task `87549f6a-dac2-4a75-9453-28b06a6dc58b`. Desktop-open CPU `docs\evidence\runtime-idle-cpu\1.15.0-rc.1\20260603-213716-HUGH_SECOND.desktop-open.evidence.json` passed for `60.05s` with MUSU `0`, Node `0`, WebView2 `0.21`, hot `0`, and working set `511.57MB`. Five-state runtime matrix `docs\evidence\runtime-cpu-scenarios\1.15.0-rc.1\20260603-213849-HUGH_SECOND.runtime-cpu-scenario-matrix.json` passed with route token `MUSU_CPU_SCENARIO_ROUTE_OK_20260603_213849`, verifier `ok=true`, `fail_count=0`, max WebView2 `0.29`, and max working set `518.07MB`. Dirty go/no-go restores single-machine true, runtime idle CPU `1/2`, and runtime CPU matrix `1/2`; public release remains No-Go on second-PC runtime/multi-device evidence, hosted relay payload proof, support mailbox, and Store evidence. Canonical report: `docs\RELEASE_1_15_0_RC1_POST_RELAY_STATUS_DESCRIPTOR_PRIMARY_EVIDENCE_REFRESH_2026_06_03.md` (wiki/642). |
| 2026-06-03 v453 | Code, docs, fresh primary evidence, and wiki were re-indexed after the post relay status descriptor primary evidence refresh. | Explicit packaged alias indexing `& "$env:LOCALAPPDATA\Microsoft\WindowsApps\musu.exe" indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` indexed 1794 files and 2339 symbols after GOAL v452, wiki/642, fresh primary evidence `20260603-213326-HUGH_SECOND`, `20260603-213716-HUGH_SECOND.desktop-open`, and `20260603-213849-HUGH_SECOND.runtime-cpu-scenario-matrix`, the primary refresh report, BETA/WIKI/WIKI_INDEX updates, and CoS memory `2026-06-03_post_relay_status_descriptor_primary_evidence_refresh.md`. Search terms include `GOAL v453`, `1794 files`, `2339 symbols`, `wiki/643 index refresh`, `MUSU_RELEASE_SMOKE_OK_20260603_213326`, `MUSU_CPU_SCENARIO_ROUTE_OK_20260603_213849`, `runtime idle CPU 1/2`, and `runtime CPU matrix 1/2`. |
| 2026-06-03 v454 | Relay fallback evidence now records the payload transport gap after issued leases. | Runtime `relay_fallback` evidence now includes `payload_transport_attempted`, `payload_transport_proven`, and `payload_transport_failure_class`; current bridge forwarding records `false/false/relay_payload_transport_not_implemented` when a relay lease is issued because the payload relay path is still not wired. Hosted route-evidence grading now adds blockers `relay_fallback_payload_transport_not_attempted`, `relay_fallback_payload_transport_not_proven`, and `relay_fallback_payload_transport_not_implemented`, keeping lease issuance separate from payload transit proof. Validation passed `git diff --check`, `cargo fmt --check`, `npm run test:p2p` `38/38`, `npm run typecheck`, `cargo check --lib`, and Rust route-evidence tests `10/10`. This is evidence hardening, not relay transport implementation; because runtime/web source changed, fresh clean MSIX/smoke/CPU/matrix evidence is required before current source can claim packaged primary evidence. Canonical report: `docs\RELEASE_1_15_0_RC1_RELAY_FALLBACK_PAYLOAD_GAP_GATE_2026_06_03.md` (wiki/644). |
| 2026-06-03 v455 | Relay transport proof is now bound to the stored relay lease and release transport contract. | `POST /api/v1/p2p/route-evidence` now rejects proof-shaped relay payload JSON when the proof `relay_url` differs from the stored owner-scoped relay lease URL, when `transport_kind` is not `quic_relay_tunnel`, or when proof timestamps are invalid/out of order. New blockers are `relay_route_transport_proof_relay_url_mismatch`, `relay_route_transport_proof_kind_not_release_grade`, `relay_route_transport_proof_opened_at_invalid`, `relay_route_transport_proof_closed_at_invalid`, and `relay_route_transport_proof_timestamp_order_invalid`. Validation passed `npm run test:p2p` `40/40`, `npm run typecheck`, and `git diff --check`. This is evidence hardening only; relay/tunnel payload transport is still unwired, and public release remains No-Go on real relay payload proof, second-PC runtime/multi-device evidence, support mailbox evidence, and Store evidence. Canonical report: `docs\RELEASE_1_15_0_RC1_RELAY_TRANSPORT_PROOF_BINDING_GATE_2026_06_03.md` (wiki/645). |
| 2026-06-03 v456 | Fresh primary-machine packaged evidence was restored after relay proof binding. | Rebuilt and installed current local-sideload MSIX for the proof-binding branch as `Yellowhama.MUSU_1.15.0.0_x64__ygcjq669as2b6`. Single-machine smoke `docs\evidence\single-machine\1.15.0-rc.1\20260603-225154-HUGH_SECOND.evidence.json` passed with output `MUSU_RELEASE_SMOKE_OK_20260603_225125`, dashboard `http://127.0.0.1:3001`, bridge `http://127.0.0.1:1037`, and task `44f5030f-41e9-47d8-af0f-2419a942fafa`. Desktop-open CPU `docs\evidence\runtime-idle-cpu\1.15.0-rc.1\20260603-225332-HUGH_SECOND.desktop-open.evidence.json` passed for `60.039s` with MUSU `0.03`, Node `0.03`, WebView2 `0.6`, hot `0`, and working set `455.37MB`. Five-state runtime matrix `docs\evidence\runtime-cpu-scenarios\1.15.0-rc.1\20260603-225507-HUGH_SECOND.runtime-cpu-scenario-matrix.json` passed verifier `ok=true`, `fail_count=0`, route token `MUSU_CPU_SCENARIO_ROUTE_OK_20260603_225507`, max MUSU `0.44`, Node `0.13`, WebView2 `0.44`, and max working set `460.51MB`. Clean go/no-go on `faef9398` reports `local_artifacts_ready=true`, `single_machine_verified=true`, runtime idle CPU `1/2`, runtime CPU matrix `1/2`, and six remaining public release blockers. Canonical report: `docs\RELEASE_1_15_0_RC1_POST_PROOF_BINDING_PRIMARY_EVIDENCE_REFRESH_2026_06_03.md` (wiki/646). |
| 2026-06-03 v457 | Relay transport proof is now bound to an owner-scoped proof store, not only inline JSON. | Added `p2pRelayTransportProofStore` and changed hosted route-evidence grading so `route_kind=relay` with `relay_transport_proof` must find a matching owner-scoped stored proof by session, lease, source/target node, tunnel, relay URL, transport kind, payload bytes, encryption, and verifier. File/development proof stores remain non-release-grade. New blockers are `relay_route_transport_proof_not_stored`, `relay_route_transport_proof_store_backend_not_release_grade`, `relay_route_transport_proof_store_not_release_grade`, and `relay_route_transport_proof_store_unavailable:<detail>`. Validation passed `npm run test:p2p` `41/41`, `npm run typecheck`, and `git diff --check`. This is evidence-chain hardening only; `/api/v1/relay/connect` still does not emit release-grade QUIC relay proof, and fresh packaged primary evidence is required after this source change. Canonical report: `docs\RELEASE_1_15_0_RC1_RELAY_TRANSPORT_PROOF_STORE_GATE_2026_06_03.md` (wiki/647). |
| 2026-06-03 v458 | Fresh primary-machine packaged evidence was restored after relay proof store binding. | Rebuilt and reinstalled the current local-sideload MSIX as `Yellowhama.MUSU_1.15.0.0_x64__ygcjq669as2b6`, then refreshed primary evidence. Single-machine smoke `docs\evidence\single-machine\1.15.0-rc.1\20260603-232213-HUGH_SECOND.evidence.json` passed with output `MUSU_RELEASE_SMOKE_OK_20260603_232146`, dashboard `http://127.0.0.1:3001`, bridge `http://127.0.0.1:11952`, and task `1948b1f1-da2d-4df8-abd6-21eff940ad8a`. Desktop-open CPU `docs\evidence\runtime-idle-cpu\1.15.0-rc.1\20260603-232423-HUGH_SECOND.desktop-open.evidence.json` passed for `60.046s` with MUSU `0`, Node `0.03`, WebView2 `0.39`, hot `0`, and working set `462.32MB`. Five-state runtime matrix `docs\evidence\runtime-cpu-scenarios\1.15.0-rc.1\20260603-232620-HUGH_SECOND.runtime-cpu-scenario-matrix.json` passed verifier `ok=true`, `fail_count=0`, route token `MUSU_CPU_SCENARIO_ROUTE_OK_20260603_232620`, startup MUSU `2.37`, runtime-started MUSU `2.03`, max WebView2 `0.34`, and max working set `472.9MB`. Clean go/no-go on `4ab4281f` restores `single_machine_verified=true`, runtime idle CPU `1/2`, runtime CPU matrix `1/2`, `manifest_dirty=false`, and six remaining public release blockers. Canonical report: `docs\RELEASE_1_15_0_RC1_POST_RELAY_PROOF_STORE_PRIMARY_EVIDENCE_REFRESH_2026_06_03.md` (wiki/648). |
| 2026-06-03 v459 | Hosted relay transport proof recording is now lease-bound and owner-scoped. | Added `POST/GET /api/v1/p2p/relay/transport-proof` so future relay/tunnel runtime code can record/query relay payload transport proof after actual transit. The POST route requires bearer auth, validates `musu.relay_transport_proof.v1`, refuses missing owner-scoped relay leases without storing proof, records non-release-grade blockers for invalid content or non-release-grade proof-store backend, and never exposes `owner_key` on query. Rust cloud client now includes `P2pRelayTransportProofRequest`, `P2pRelayTransportProofResponse`, `P2pRelayTransportProofStoredRecord`, and `MusuCloud::submit_relay_transport_proof`. Validation passed `npm run test:p2p` `45/45`, `npm run typecheck`, Rust cloud tests `4/4`, and `git diff --check`. This is the proof-recording contract only; relay payload transport remains unwired and fresh packaged primary evidence is required after this source change. Canonical report: `docs\RELEASE_1_15_0_RC1_RELAY_TRANSPORT_PROOF_RECORD_API_2026_06_03.md` (wiki/649). |
| 2026-06-04 v460 | Fresh primary-machine packaged evidence was restored after the relay transport proof record API. | Rebuilt and installed current local-sideload MSIX `.local-build\msix\output\musu_1.15.0.0_x64_local-sideload-manual.msix` as `Yellowhama.MUSU_1.15.0.0_x64__ygcjq669as2b6`. The source gate landed on 2026-06-03, but KST evidence capture crossed midnight, so fresh evidence uses `20260604-*` stamps. Single-machine smoke `docs\evidence\single-machine\1.15.0-rc.1\20260604-000322-HUGH_SECOND.evidence.json` passed with output `MUSU_RELEASE_SMOKE_OK_20260604_000259`, dashboard `http://127.0.0.1:3001`, bridge `http://127.0.0.1:3477`, and task `836ed892-5340-4be4-8f44-ca897c8c5f49`. Desktop-open CPU `docs\evidence\runtime-idle-cpu\1.15.0-rc.1\20260604-000405-HUGH_SECOND.desktop-open.evidence.json` passed for `60.059s` with MUSU `0.03`, Node `0.03`, WebView2 `0.57`, hot `0`, and working set `453.71MB`. Five-state runtime matrix `docs\evidence\runtime-cpu-scenarios\1.15.0-rc.1\20260604-000535-HUGH_SECOND.runtime-cpu-scenario-matrix.json` passed verifier `ok=true`, `fail_count=0`, route token `MUSU_CPU_SCENARIO_ROUTE_OK_20260604_000535`, max WebView2 `0.47`, and max working set `456.73MB`. Clean go/no-go at `2026-06-04T00:16:47.6824922+09:00` on `049a9a9a` reports `local_artifacts_ready=true`, `single_machine_verified=true`, runtime idle CPU `1/2`, runtime CPU matrix `1/2`, P2P relay route evidence count `0`, relay payload proof `false`, `manifest_dirty=false`, and six remaining public release blockers. Canonical report: `docs\RELEASE_1_15_0_RC1_POST_RELAY_TRANSPORT_PROOF_API_PRIMARY_EVIDENCE_REFRESH_2026_06_04.md` (wiki/650). |
| 2026-06-04 v461 | Relay fallback now has a lease-bound payload queue API and Rust client hook. | Added `POST/GET /api/v1/p2p/relay/payload` plus `p2pRelayPayloadStore` so the hosted P2P control plane can accept owner-scoped `musu.relay_payload_envelope.v1` records only after a matching stored relay lease exists. Payload records validate optional SHA-256, strip `owner_key`, omit `payload_base64` unless `include_payload=1`, keep `relay_default_data_path=false`, and remain non-release-grade with `transport_kind=http_store_forward_preview`. Rust cloud client now includes `P2pRelayPayloadRequest`, `P2pRelayPayloadResponse`, `P2pRelayPayloadStoredRecord`, and `MusuCloud::submit_relay_payload`. Validation passed relay payload route tests `5/5`, `npm run test:p2p` `50/50`, `npm run typecheck`, Rust fmt check, Rust cloud tests `5/5`, and `git diff --check`. This is the first concrete relay payload data-path slice, not public release QUIC/TLS relay transport; `relay_payload_endpoint_wired=false` and `relay_transport_wired=false` remain because target-side relay polling/execution and release-grade tunnel proof are still missing. Canonical report: `docs\RELEASE_1_15_0_RC1_RELAY_PAYLOAD_QUEUE_API_2026_06_04.md` (wiki/651). |
| 2026-06-04 v462 | Relay fallback runtime now enqueues payloads after issued leases. | Rust forwarding fallback now serializes the failed `ForwardedTask` envelope, base64-encodes it, records SHA-256, and calls `MusuCloud::submit_relay_payload` after direct peer attempts fail and `musu.pro` issues a relay lease. Runtime evidence now records queued fallback preview as `payload_transport_attempted=true`, `payload_transport_proven=false`, and `payload_transport_failure_class=relay_target_polling_not_implemented`; no issued lease still records attempted false, and queue failures record attempted-but-not-proven bounded classes such as `relay_payload_queue_failed`, `relay_payload_queue_timeout`, or `relay_payload_queue_not_stored`. Hosted route-evidence tests now assert queued fallback removes the `relay_fallback_payload_transport_not_attempted` blocker while retaining `relay_fallback_payload_transport_not_proven`. Validation passed Rust forward tests `6/6`, rendezvous tests `5/5`, cloud tests `5/5`, `cargo check --bin musu`, `npm run test:p2p` `51/51`, `npm run typecheck`, Rust fmt check, and `git diff --check`. This is not public release relay transport and does not make `musu.pro` the default central data path; target-side polling/execution, QUIC/TLS relay proof, and fresh packaged MSIX/smoke/CPU/matrix evidence are still required. Canonical report: `docs\RELEASE_1_15_0_RC1_RELAY_PAYLOAD_QUEUE_RUNTIME_HOOK_2026_06_04.md` (wiki/652). |
| 2026-06-04 v463 | Relay payload queue now has Rust query client and CLI visibility. | Added `P2pRelayPayloadQuery`, `P2pRelayPayloadQueryResponse`, optional `payload_base64` parsing on relay payload records, `MusuCloud::query_relay_payloads`, and `musu relay payloads`. The CLI supports owner-scoped queue diagnostics with `--session-id`, `--lease-id`, source/target filters, `--local-target`, tunnel/status filters, and explicit `--include-payload` for JSON only; human output does not print payload bytes. Validation passed Rust cloud tests `6/6`, install CLI relay payload tests `2/2`, `cargo check --bin musu`, CLI help smoke, JSON local-target smoke, Rust fmt check, and `git diff --check`. The live production `https://musu.pro/api/v1/p2p/relay/payload` query returned 404 during validation, so this is local Rust client/CLI wiring only until the hosted payload route is deployed. This remains on-demand visibility, not background polling, payload execution, or release-grade relay transport proof. Canonical report: `docs\RELEASE_1_15_0_RC1_RELAY_PAYLOAD_QUERY_CLIENT_CLI_2026_06_04.md` (wiki/653). |
| 2026-06-04 v464 | Relay payload queue now has claim/delivery state transitions for the hosted API. | Added `PATCH /api/v1/p2p/relay/payload` support for `musu.relay_payload_claim.v1` and `musu.relay_payload_delivery.v1`. The local/development file store now moves owner-scoped payloads through `queued -> claimed -> delivered`, records `claimed_by`, `claimed_at`, and `delivered_at`, rejects delivery before claim with `relay_payload_delivery_requires_claim`, strips `owner_key`, and only returns payload bytes on claim when `include_payload=true`. KV/Upstash claim/delivery fail closed with `relay_payload_claim_kv_not_implemented` and `relay_payload_delivery_kv_not_implemented` until atomic mutation is implemented. Validation passed `npm run test:p2p` `54/54` and `npm run typecheck`. This is claim/delivery API semantics only; background polling, payload execution, production deployment, release-grade QUIC/TLS relay proof, and fresh packaged evidence remain required. Canonical report: `docs\RELEASE_1_15_0_RC1_RELAY_PAYLOAD_CLAIM_DELIVERY_API_2026_06_04.md` (wiki/654). |
| 2026-06-04 v465 | Relay payload claim/delivery now has Rust client and manual CLI diagnostics. | Added `P2pRelayPayloadClaimRequest`, `P2pRelayPayloadClaimResponse`, `P2pRelayPayloadDeliveryRequest`, `P2pRelayPayloadDeliveryResponse`, `MusuCloud::claim_relay_payloads`, `MusuCloud::mark_relay_payload_delivered`, `musu relay payload-claim`, and `musu relay payload-deliver`. Claim/deliver commands require `--target-node-id` or `--local-target`, preserve `relay_default_data_path=false`, keep text output from printing payload bytes, and include claim bytes in JSON only with `--include-payload`. Validation passed Rust cloud tests `10/10`, install CLI relay payload tests `4/4`, `cargo check --bin musu`, Rust fmt check, and claim/deliver help smoke. No live mutation smoke was run because claim/deliver changes queue state. This is on-demand target-side diagnostics only; bounded polling, payload execution, production atomic claim/delivery, release-grade QUIC/TLS relay proof, and fresh packaged evidence remain required. Canonical report: `docs\RELEASE_1_15_0_RC1_RELAY_PAYLOAD_CLAIM_DELIVERY_CLIENT_CLI_2026_06_04.md` (wiki/655). |
| 2026-06-04 v466 | Deploy workflow no longer depends on the moving `vercel@latest` package. | PR #8 deploy failed because `npm install -g vercel@latest` resolved to `vercel@54.8.0`, which depended on `@vercel/express@0.1.96`; the GitHub runner then received an npm registry 404 for `@vercel/express-0.1.96.tgz`. `.github/workflows/deploy-musu-bee.yml` now pins `VERCEL_CLI_VERSION=54.7.1`, installs `vercel@${VERCEL_CLI_VERSION}`, prints `vercel --version`, and includes the workflow file in pull-request path filters. Validation confirmed the broken latest dependency via `npm view`; `44.7.3` installed but was rejected by the Vercel deploy endpoint as below `47.2.2`; `54.7.1` uses `@vercel/express=0.1.95` and `npx -y vercel@54.7.1 --version` printed `Vercel CLI 54.7.1`. This is deploy CI hardening only; public release remains No-Go until relay proof, two-machine runtime evidence, support mailbox, Store evidence, and fresh packaged evidence are complete. Canonical report: `docs\RELEASE_1_15_0_RC1_VERCEL_CLI_PIN_DEPLOY_WORKFLOW_2026_06_04.md` (wiki/656). |
| 2026-06-04 v467 | Relay payload claim/delivery now works on the hosted KV/Upstash store path. | `p2pRelayPayloadStore` now shares state-transition logic between file and KV-backed stores. KV claim/delivery loads fresh records with `lrange`, applies owner-scoped `queued -> claimed -> delivered` transitions, rewrites retained records with `del` plus `rpush`, rejects delivery before claim with `relay_payload_delivery_requires_claim`, and no longer throws `relay_payload_claim_kv_not_implemented` or `relay_payload_delivery_kv_not_implemented`. Validation passed focused relay payload route tests `10/10`, `npm run test:p2p` `56/56`, and `npm run typecheck`. The KV list rewrite remains non-release-grade atomic, so `relay_payload_store_release_grade=false` and public release still require bounded target polling, payload execution safety, concurrent claim hardening, release-grade QUIC/TLS relay proof, and fresh packaged evidence. Canonical report: `docs\RELEASE_1_15_0_RC1_RELAY_PAYLOAD_KV_CLAIM_DELIVERY_STORE_2026_06_04.md` (wiki/657). |
| 2026-06-04 v468 | Relay payloads now have a request-driven local target drain primitive. | Added local bridge route `POST /api/relay/payloads/drain` with response schema `musu.relay_payload_drain.v1`. The drain claims owner-scoped payloads for the local node, validates claimed `forwarded_task_envelope` payloads by target, claimant, kind, base64 bytes, byte count, SHA-256, source node, rendezvous session, and embedded target, accepts decoded `ForwardedTask` values through the existing local forwarded-task runner path, and marks delivery only after local acceptance. Manual drain `limit` defaults to `1` and clamps to `1..5`; `MUSU_P2P_RELAY_PAYLOAD_DRAIN_TIMEOUT_MS` defaults to `3000ms` and clamps to `250..10000ms`. Validation passed Rust fmt, Rust relay payload tests `14/14`, `cargo check --bin musu`, and Rust background-loop audit `ok=true` with `unaudited_loop_hit_count=0`. This is not a background poller, not task-completion proof, and not release-grade QUIC/TLS relay transport; public release still needs opt-in polling evidence, production atomic claim hardening, hosted relay proof, and fresh packaged evidence. Canonical report: `docs\RELEASE_1_15_0_RC1_RELAY_PAYLOAD_TARGET_DRAIN_2026_06_04.md` (wiki/658). |
| 2026-06-04 v469 | Relay payloads now have an opt-in low-duty target poller. | The Rust bridge now starts target-side relay payload polling only when `MUSU_ENABLE_RELAY_PAYLOAD_POLLER=1`; the default desktop profile remains request-driven for idle CPU evidence. HTTP drain and poller share `drain_relay_payloads_for_local_target(...)`, poll interval defaults to `60s` with a `30s` floor, empty/failure backoff defaults to `300s` with a `3600s` hard ceiling, and per-cycle claim limit defaults to `1` with the same `1..5` clamp as manual drain. The poller sleeps before first work, sleeps under `tokio::select!` with a `CancellationToken`, and resets backoff after delivered payloads. `musu doctor` reports the relay payload poller background profile, and the Rust background-loop audit now gates the opt-in env, interval floor, backoff cap, bounded limit, cancellation-aware sleep, and shared drain primitive. Validation passed relay payload tests `19/19`, doctor background tests `5/5`, `cargo check --bin musu`, Rust background-loop audit `ok=true`/`fail_count=0`/`unaudited_loop_hit_count=0`, and `git diff --check`. This is bounded opt-in target polling evidence, not release-grade relay transport; public release still needs production atomic claim hardening, QUIC/TLS relay proof, hosted proof evidence, fresh packaged evidence, second-PC evidence, support mailbox, and Store evidence. Canonical report: `docs\RELEASE_1_15_0_RC1_RELAY_PAYLOAD_TARGET_POLLER_2026_06_04.md` (wiki/659). |
| 2026-06-04 v470 | Relay payload KV/Upstash mutations are now atomic. | `p2pRelayPayloadStore` now performs hosted KV append, claim, and delivery through Redis Lua `EVAL` scripts instead of app-level `lrange` plus `del`/`rpush` rewrites. The claim script preserves owner scoping, target matching, optional session/lease/source/tunnel filters, and marks queued records `claimed` in one Redis operation; delivery similarly enforces `relay_payload_delivery_requires_claim` and marks delivered in one Redis operation. KV reads now accept both object records and JSON string records, and configured KV/Upstash stores report `relay_payload_store_release_grade=true` while individual payloads remain `release_grade=false` with `transport_kind=http_store_forward_preview`. Validation passed focused relay payload route tests `11/11`, `npm run test:p2p` `57/57`, and `npm run typecheck`. This closes the hosted concurrent claim hardening blocker but is not release-grade relay transport; public release still needs QUIC/TLS relay proof, hosted proof evidence, fresh packaged evidence, second-PC evidence, support mailbox, and Store evidence. Canonical report: `docs\RELEASE_1_15_0_RC1_RELAY_PAYLOAD_ATOMIC_KV_MUTATION_2026_06_04.md` (wiki/660). |
| 2026-06-04 v471 | External recheck and final handoff now surface relay proof counts. | `record-p2p-control-plane-evidence.ps1` now returns `relay_route_evidence_count`; `record-external-release-gate-recheck.ps1` promotes `p2p_relay_route_evidence_count`, `p2p_relay_payload_transport_proven`, and `p2p_relay_payload_delivery_proof_valid_count` to top-level JSON/summary/final output and adds blockers `p2p_relay_payload_transport_not_proven` and `p2p_relay_payload_delivery_proof_missing`; `show-final-release-handoff-status.ps1` adds owner scope, route evidence count, payload transport proof, and delivery proof count to the `gates` snapshot. Validation passed PowerShell parser checks, `git diff --check`, and release verifier regressions `24/24`. This is operator visibility hardening for the local-first web coordination roadmap; public release remains No-Go until second-PC evidence, live `musu.pro` hosted relay delivery proof, support mailbox evidence, and Store evidence are complete. Canonical report: `docs\RELEASE_1_15_0_RC1_EXTERNAL_RECHECK_RELAY_PROOF_OUTPUT_2026_06_04.md` (wiki/661). |
| 2026-06-04 v472 | Clean external recheck evidence recorded the new relay proof blockers. | After commit `1e1fc43cf0da04c4b71621e1b8329496d2c6b810`, `record-external-release-gate-recheck.ps1 -MusuExe "$env:LOCALAPPDATA\Microsoft\WindowsApps\musu.exe" -Json` recorded `docs\evidence\external-gates\1.15.0-rc.1\20260604-084033-HUGH_SECOND.external-gates.evidence.json` and live P2P evidence `docs\evidence\p2p-control-plane\1.15.0-rc.1\20260604-084136-musu.pro.evidence.json`. Result remains No-Go with `local_artifacts_ready=true`, `single_machine_verified=true`, runtime idle CPU `1/2`, runtime CPU matrix `1/2`, `second_pc_reachable=false`, `p2p_env_ok=false`, `p2p_evidence_ok=false`, `p2p_relay_route_evidence_count=0`, `p2p_relay_payload_transport_proven=false`, `p2p_relay_payload_delivery_proof_valid_count=0`, and blockers including `p2p_relay_payload_transport_not_proven` plus `p2p_relay_payload_delivery_proof_missing`. |
| 2026-06-04 v473 | Target-side relay payload drain now records hosted relay route evidence. | Added `record_relay_payload_delivery_route_evidence(...)` so confirmed target-side relay payload delivery can write explicit `route_kind=relay` evidence with `payload_transited_musu_infra=true`, `relay_fallback.payload_transport_proven=true`, and `relay_payload_delivery_proof`; `drain_relay_payloads_for_local_target(...)` now records/submits that evidence to `musu.pro` after delivery proof is confirmed, and drain items expose `route_evidence_recorded`, `route_evidence_submitted`, `route_evidence_path`, and `route_evidence_failure_class`. `RouteEvidencePayload` now includes `relay_payload_delivery_proof`. Validation passed Rust relay payload tests `24/24`, Rust route evidence tests `13/13`, `cargo check --bin musu`, `npm run typecheck`, route-evidence API tests `22/22`, and `git diff --check`. This closes the drain-to-route-evidence proof-chain gap but is still non-release-grade until QUIC/TLS relay transport proof and production proof stores are live. Canonical report: `docs\RELEASE_1_15_0_RC1_RELAY_PAYLOAD_DRAIN_ROUTE_EVIDENCE_2026_06_04.md` (wiki/663). |
| 2026-06-04 v474 | Post relay-drain route-evidence go/no-go is No-Go due source freshness. | `write-release-go-no-go.ps1 -Json` on commit `2777554e6ce80b73a5bc471629b47059595d126b` reports `ready_for_public_desktop_release=false`, `manifest_git.dirty=false`, `local_artifacts_ready=true`, and `single_machine_verified=false`. The drop is expected because Rust runtime source changed after the latest packaged primary evidence; fresh MSIX/smoke/idle CPU/runtime CPU matrix evidence is required before current source can reclaim primary runtime gates. External blockers still include second-PC runtime/multi-device evidence, hosted `musu.pro` relay proof, support mailbox evidence, and Store evidence. |
| 2026-06-04 v475 | Fresh packaged primary evidence was restored after relay-drain route evidence. | Rebuilt and installed the current local-sideload MSIX, moved the WindowsApps execution alias before `.cargo\bin` in User PATH, and recorded strict MSIX evidence `docs\evidence\msix-install\1.15.0-rc.1\20260604-093646-HUGH_SECOND.evidence.json` with `alias_shadowing_mode=fail`, first alias `C:\Users\empty\AppData\Local\Microsoft\WindowsApps\musu.exe`, and no legacy conflicts. Re-ran packaged bridge smoke after stopping the stale debug bridge; replacement evidence `docs\evidence\single-machine\1.15.0-rc.1\20260604-092446-HUGH_SECOND.evidence.json` passed with dashboard `http://127.0.0.1:3001`, bridge `http://127.0.0.1:3049`, task `6301345b-5163-41dc-a7ae-a7d3cd552360`, and output `MUSU_RELEASE_SMOKE_OK_20260604_092419`. Desktop-open CPU `docs\evidence\runtime-idle-cpu\1.15.0-rc.1\20260604-092544-HUGH_SECOND.desktop-open.evidence.json` passed for `60.071s` with MUSU `0`, Node `0`, WebView2 `0.57`, working set `506.43MB`, and hot `0`. Five-state matrix `docs\evidence\runtime-cpu-scenarios\1.15.0-rc.1\20260604-092758-HUGH_SECOND.runtime-cpu-scenario-matrix.json` passed verifier `ok=true`, `fail_count=0`, route token `MUSU_CPU_SCENARIO_ROUTE_OK_20260604_092758`, max WebView2 `1.41`, max working set `508.66MB`, and hot `0` in all scenarios. Clean go/no-go on `83e7e5db` restores `local_artifacts_ready=true`, `single_machine_verified=true`, and `msix_install_verified=true`; public release remains No-Go because second-PC runtime/multi-device evidence, live `musu.pro` relay proof, support mailbox evidence, and Store evidence remain open. Canonical report: `docs\RELEASE_1_15_0_RC1_POST_RELAY_DRAIN_PRIMARY_EVIDENCE_REFRESH_2026_06_04.md` (wiki/664). |
| 2026-06-04 v476 | Current operator handoff pack was regenerated after the post relay-drain primary evidence refresh. | Clean HEAD `8adbfaf1da9c76ba18af81542dec370b8d28fd6f` generated final operator packet `.local-build\final-operator-gates\musu-final-operator-gates-1.15.0-rc.1-20260604-094858.zip` and action pack `.local-build\operator-action-pack\MUSU-1.15.0-rc.1-operator-action-pack-20260604-094940.zip`; final packet verification passed with `ok=true`, `fail_count=0`, `kit_count=1`, and action-pack verification passed with `ok=true`, `fail_count=0`. Current second-PC transfer zip is `.local-build\operator-action-pack\MUSU-1.15.0-rc.1-operator-action-pack-20260604-094940\second-pc\MUSU-second-PC-transfer-1.15.0-rc.1-20260604-094940.zip`; Partner Center zip is `.local-build\operator-action-pack\MUSU-1.15.0-rc.1-operator-action-pack-20260604-094940\partner-center\MUSU-1.15.0-rc.1-store-submission-20260604-094940.zip`; support verification id is `musu-store-support-1.15.0-rc.1-20260604-094858`. Final handoff status at `2026-06-04T09:54:31.2686360+09:00` reports `packet.verified=true`, `action_pack.verified=true`, `ready_for_public_desktop_release=false`, runtime idle CPU `1/2 [HUGH_SECOND]`, runtime CPU matrix `1/2 [HUGH_SECOND]`, P2P route evidence count `0`, relay payload proof `false`, and six remaining blockers: second-PC multi-device, second-PC CPU/matrix, support mailbox, Store, and live owner-scoped `musu.pro` relay proof. Canonical report: `docs\RELEASE_1_15_0_RC1_CURRENT_OPERATOR_HANDOFF_PACK_2026_06_04.md` (wiki/665). |
| 2026-06-04 v477 | Relay route evidence queries now guard against stale release-grade relay records without current transport proof. | Investigated the live `musu.pro` P2P blocker with `show-musu-pro-p2p-env-status.ps1 -Json`: production KV/Upstash env is missing, live relay route evidence count is `0`, relay payload transport proof is `false`, and relay payload delivery proof valid count is `0`. Added a P2P route-evidence regression test that manually seeds a stale relay record with `release_grade=true` but no current `musu.relay_transport_proof.v1`; `GET /api/v1/p2p/route-evidence?release_grade=true` must exclude that stale relay record, preventing old/manual records from inflating release-grade relay route counts. Validation passed `npm run test:p2p` `61/61`, `npm run typecheck`, and `git diff --check`. This is release-gate hardening, not relay transport completion; public release still needs second-PC evidence, production KV/Upstash config, stored QUIC/TLS relay transport proof, stored relay payload delivery proof, support mailbox, and Store evidence. Canonical report: `docs\RELEASE_1_15_0_RC1_RELAY_ROUTE_EVIDENCE_STALE_PROOF_QUERY_GATE_2026_06_04.md` (wiki/666). |
| 2026-06-04 v478 | Fresh packaged primary evidence was restored after the stale-proof query hardening. | Current HEAD primary evidence now passes again after the P2P route-evidence test change. Single-machine smoke `docs\evidence\single-machine\1.15.0-rc.1\20260604-100843-HUGH_SECOND.evidence.json` passed with output `MUSU_RELEASE_SMOKE_OK_20260604_100843`, bridge `http://127.0.0.1:2751`, and task `e45ff280-80a3-41cf-92bd-54f9e9776c0a`. Desktop-open CPU `docs\evidence\runtime-idle-cpu\1.15.0-rc.1\20260604-101133-HUGH_SECOND.desktop-open.evidence.json` passed for `60.052s` from clean commit `04bcee52` with MUSU `0`, Node `0.03`, WebView2 `0.16`, working set `509.29MB`, and hot `0`. Five-state runtime matrix `docs\evidence\runtime-cpu-scenarios\1.15.0-rc.1\20260604-101925-HUGH_SECOND.runtime-cpu-scenario-matrix.json` passed verifier `ok=true`, `fail_count=0`, route token `MUSU_CPU_SCENARIO_ROUTE_OK_20260604_101925`, max CPU MUSU `0`, Node `0.08`, WebView2 `0.18`, max working set `509.47MB`, and hot `0`. Clean go/no-go on `a2087e6b` reports `local_artifacts_ready=true`, `single_machine_verified=true`, runtime idle CPU `1/2 [HUGH_SECOND]`, runtime CPU matrix `1/2 [HUGH_SECOND]`, `p2p_relay_route_evidence_count=0`, relay payload proof `false`, and `manifest_dirty=false`; public release remains No-Go on second-PC runtime/multi-device evidence, live owner-scoped `musu.pro` relay proof, support mailbox, and Store evidence. Canonical report: `docs\RELEASE_1_15_0_RC1_POST_STALE_PROOF_QUERY_PRIMARY_EVIDENCE_REFRESH_2026_06_04.md` (wiki/667). |
| 2026-06-04 v479 | Current operator handoff pack was regenerated after the stale-proof primary evidence refresh. | Clean HEAD `2d15c73e89439b01afb999de20ab907675f7b734` generated final operator packet `.local-build\final-operator-gates\musu-final-operator-gates-1.15.0-rc.1-20260604-103143.zip`; `verify-final-operator-gate-packet.ps1` passed with `ok=true`, `fail_count=0`, and `kit_count=1`. The operator action pack `.local-build\operator-action-pack\MUSU-1.15.0-rc.1-operator-action-pack-20260604-103216.zip` passed `verify-operator-action-pack.ps1` with `ok=true`, `fail_count=0`. Current second-PC transfer zip is `.local-build\operator-action-pack\MUSU-1.15.0-rc.1-operator-action-pack-20260604-103216\second-pc\MUSU-second-PC-transfer-1.15.0-rc.1-20260604-103216.zip`; Partner Center zip is `.local-build\operator-action-pack\MUSU-1.15.0-rc.1-operator-action-pack-20260604-103216\partner-center\MUSU-1.15.0-rc.1-store-submission-20260604-103216.zip`; support verification id is `musu-store-support-1.15.0-rc.1-20260604-103143`. Final handoff status at `2026-06-04T10:33:55.2730279+09:00` reports `packet.verified=true`, `action_pack.verified=true`, `ready_for_public_desktop_release=false`, `single_machine_verified=true`, runtime idle CPU `1/2`, runtime CPU matrix `1/2`, and six remaining blockers: second-PC multi-device, second-PC CPU/matrix, live owner-scoped `musu.pro` relay proof, support mailbox, and Store evidence. Canonical report: `docs\RELEASE_1_15_0_RC1_CURRENT_OPERATOR_HANDOFF_PACK_AFTER_STALE_PROOF_EVIDENCE_2026_06_04.md` (wiki/668). |
| 2026-06-04 v480 | CLI route wait and the web-input/local-executor roadmap were updated. | The runtime roadmap now states that `musu.pro` is the web input, project-room, rendezvous, fallback coordination, and evidence plane while local MUSU programs execute the work. `musu route --wait` now has `--wait-timeout-sec`, default `300s`, hard ceiling `3600s`, timeout-bound status requests, bounded poll sleeps, and timeout failure class `remote_task_wait_timeout`. The Rust background-loop audit now checks `cli-route-wait` and `cli-bridge-health` contracts. Validation passed `cargo fmt --check`, `cargo test --lib route_wait_timeout_is_bounded`, Rust background-loop audit `ok=true`/`fail_count=0`, and `git diff --check`. Dirty go/no-go reports `ready=false`, `local_artifacts_ready=true`, `single_machine_verified=true`, runtime idle CPU `1/2`, runtime CPU matrix `1/2`, `manifest_git.dirty=true`, and blocker count `7`; commit plus current-source primary evidence are required before this source change can support a clean release claim. Canonical report: `docs\RELEASE_1_15_0_RC1_CLI_ROUTE_WAIT_WEB_INPUT_ROADMAP_2026_06_04.md` (wiki/669). |
| 2026-06-04 v481 | Fresh primary evidence was restored after CLI route wait hardening. | Rebuilt and installed local-sideload MSIX evidence `docs\evidence\msix-install\1.15.0-rc.1\20260604-112129-HUGH_SECOND.evidence.json` with package `Yellowhama.MUSU_1.15.0.0_x64__ygcjq669as2b6`, `alias_shadowing_mode=fail`, and no alias shadowing. Single-machine smoke `docs\evidence\single-machine\1.15.0-rc.1\20260604-112308-HUGH_SECOND.evidence.json` passed with dashboard `http://127.0.0.1:3001`, bridge `http://127.0.0.1:3153`, task `051218dc-059c-4eda-9254-fee608e44701`, dashboard output `MUSU_RELEASE_SMOKE_OK_20260604_112241`, and CLI output `MUSU_CLI_ROUTE_OK_20260604_112241`. Desktop-open CPU `docs\evidence\runtime-idle-cpu\1.15.0-rc.1\20260604-112809-HUGH_SECOND.desktop-open.evidence.json` passed for `60.062s` with MUSU `0`, Node `0.03`, WebView2 `0.39`, owned WebView2 `6`, working set `489.98MB`, and hot `0`. Five-state runtime matrix `docs\evidence\runtime-cpu-scenarios\1.15.0-rc.1\20260604-112954-HUGH_SECOND.runtime-cpu-scenario-matrix.json` passed verifier `ok=true`, `fail_count=0`, route token `MUSU_CPU_SCENARIO_ROUTE_OK_20260604_112954`, max CPU MUSU `0.13`, Node `0.05`, WebView2 `0.13`, max working set `490.17MB`, and hot `0`. Clean go/no-go on `c9ada37b` reports `local_artifacts_ready=true`, `single_machine_verified=true`, runtime idle CPU `1/2`, runtime CPU matrix `1/2`, `manifest_dirty=false`, and six blockers: multi-device, second-PC CPU/matrix, support mailbox, Store, and P2P control plane. Canonical report: `docs\RELEASE_1_15_0_RC1_POST_CLI_ROUTE_WAIT_PRIMARY_EVIDENCE_REFRESH_2026_06_04.md` (wiki/670). |
| 2026-06-04 v482 | Current operator handoff pack was regenerated after the CLI route wait evidence refresh. | Clean HEAD `c956a5849981832a44e8dba2204e44bc4a3b57b4` generated final operator packet `.local-build\final-operator-gates\musu-final-operator-gates-1.15.0-rc.1-20260604-114250.zip` and action pack `.local-build\operator-action-pack\MUSU-1.15.0-rc.1-operator-action-pack-20260604-114319.zip`; final packet verification passed with `ok=true`, `fail_count=0`, and `kit_count=1`, and action-pack verification passed with `ok=true`, `fail_count=0`. Current second-PC transfer zip is `.local-build\operator-action-pack\MUSU-1.15.0-rc.1-operator-action-pack-20260604-114319\second-pc\MUSU-second-PC-transfer-1.15.0-rc.1-20260604-114319.zip`; Partner Center zip is `.local-build\operator-action-pack\MUSU-1.15.0-rc.1-operator-action-pack-20260604-114319\partner-center\MUSU-1.15.0-rc.1-store-submission-20260604-114319.zip`; support verification id is `musu-store-support-1.15.0-rc.1-20260604-114250`. Final handoff status at `2026-06-04T11:44:48.8706948+09:00` reports `packet.verified=true`, `action_pack.verified=true`, `ready_for_public_desktop_release=false`, `single_machine_verified=true`, runtime idle CPU `1/2`, runtime CPU matrix `1/2`, `p2p_relay_route_evidence_count=0`, relay payload proof `false`, and six remaining blockers: second-PC multi-device, second-PC CPU/matrix, live owner-scoped `musu.pro` relay proof, support mailbox, and Store evidence. Canonical report: `docs\RELEASE_1_15_0_RC1_CURRENT_OPERATOR_HANDOFF_PACK_AFTER_CLI_ROUTE_WAIT_EVIDENCE_2026_06_04.md` (wiki/671). |
| 2026-06-04 v483 | Chat SSE reconnect is now capped and the local-executor roadmap was clarified. | `useChat` had capped reconnect delay but no retry-count cap, so a missing bridge SSE endpoint could reconnect forever every 10s. Added `SSE_MAX_RETRIES=5`, `reconnectAttempts`, and `resetReconnectState()`; successful opens and lifecycle resets clear attempts, and failed streams stop after the cap. The frontend polling contract audit and runtime-polling test now require this chat SSE cap/reset guard. Validation passed `npm run test:runtime-polling` `14/14`, frontend polling audit `ok=true`/`fail_count=0`/direct interval hits `0`/direct visibility listener hits `0`, `npm run typecheck`, `npm run build`, and `git diff --check`. Clean go/no-go on `e92e0e55` reports `local_artifacts_ready=true`, `msix_install_verified=true`, `single_machine_verified=false`, runtime idle CPU `0/2`, runtime CPU matrix `0/2`, `manifest_git.dirty=false`, and seven blockers because frontend runtime source changed after current primary evidence. Roadmap remains: `musu.pro` is the remote input/project-room/rendezvous/fallback/evidence plane; local MUSU programs do the work; second-PC and P2P proof require current MUSU installed on another machine. Canonical report: `docs\RELEASE_1_15_0_RC1_CHAT_SSE_RETRY_CAP_HARDENING_2026_06_04.md` (wiki/672). |
| 2026-06-04 v484 | Fresh primary-machine evidence was restored after chat SSE retry-cap hardening. | Rebuilt and installed local-sideload MSIX evidence `docs\evidence\msix-install\1.15.0-rc.1\20260604-121733-HUGH_SECOND.evidence.json` with strict alias evidence and first alias `C:\Users\empty\AppData\Local\Microsoft\WindowsApps\musu.exe`. Single-machine smoke `docs\evidence\single-machine\1.15.0-rc.1\20260604-122357-HUGH_SECOND.evidence.json` passed with dashboard `http://127.0.0.1:3001`, bridge `http://127.0.0.1:8573`, task `06a935cf-31d1-46fb-ae42-70eb2b0f4578`, dashboard output `MUSU_RELEASE_SMOKE_OK_20260604_122333`, and CLI output `MUSU_CLI_ROUTE_OK_20260604_122333`. Clean desktop-open CPU `docs\evidence\runtime-idle-cpu\1.15.0-rc.1\20260604-124137-HUGH_SECOND.desktop-open.evidence.json` passed for `60.053s` with `git_dirty=false`, MUSU `0`, Node `0`, WebView2 `0.1`, owned WebView2 `6`, hot `0`, and working set `476.22MB`. Five-state matrix `docs\evidence\runtime-cpu-scenarios\1.15.0-rc.1\20260604-123317-HUGH_SECOND.runtime-cpu-scenario-matrix.json` passed verifier `ok=true`/`fail_count=0`, route token `MUSU_CPU_SCENARIO_ROUTE_OK_20260604_123317`, max CPU MUSU `0.1`, Node `0.03`, WebView2 `0.18`, max working set `478.47MB`, and hot `0`. Clean go/no-go on `d2c29ef9` reports `local_artifacts_ready=true`, `single_machine_verified=true`, runtime idle CPU `1/2 [HUGH_SECOND]`, runtime CPU matrix `1/2 [HUGH_SECOND]`, `manifest_dirty=false`, and six blockers: multi-device, second-PC CPU/matrix, support mailbox, Store, and P2P control plane. Canonical report: `docs\RELEASE_1_15_0_RC1_POST_CHAT_SSE_RETRY_CAP_PRIMARY_EVIDENCE_REFRESH_2026_06_04.md` (wiki/673). |
| 2026-06-04 v485 | Current operator handoff pack was regenerated after chat SSE evidence. | Clean HEAD `d2c29ef95c07e0a1d299289abe3f95358f4424dd` generated final operator packet `.local-build\final-operator-gates\musu-final-operator-gates-1.15.0-rc.1-20260604-124445.zip` and action pack `.local-build\operator-action-pack\MUSU-1.15.0-rc.1-operator-action-pack-20260604-124456.zip`. Final packet verification passed with `ok=true`, `fail_count=0`, `kit_count=1`; action-pack verification passed with `ok=true`, `fail_count=0`. Current second-PC transfer zip is `.local-build\operator-action-pack\MUSU-1.15.0-rc.1-operator-action-pack-20260604-124456\second-pc\MUSU-second-PC-transfer-1.15.0-rc.1-20260604-124456.zip`; Partner Center zip is `.local-build\operator-action-pack\MUSU-1.15.0-rc.1-operator-action-pack-20260604-124456\partner-center\MUSU-1.15.0-rc.1-store-submission-20260604-124456.zip`; support verification id is `musu-store-support-1.15.0-rc.1-20260604-124445`. Final handoff status reports `ready_for_public_desktop_release=false`, `single_machine_verified=true`, runtime idle CPU `1/2`, runtime CPU matrix `1/2`, P2P relay route evidence count `0`, relay payload proof `false`, delivery proof valid count `0`, and `manifest_git_dirty=false`. Canonical report: `docs\RELEASE_1_15_0_RC1_CURRENT_OPERATOR_HANDOFF_PACK_AFTER_CHAT_SSE_EVIDENCE_2026_06_04.md` (wiki/674). |
| 2026-06-04 v486 | Single-machine smoke now auto-discovers the packaged dashboard URL. | The `localhost` connection-refused root cause was a release smoke default to dev port `http://127.0.0.1:3000` while the packaged dashboard was reachable at `http://127.0.0.1:3001/app`. `smoke-single-machine-beta.ps1` now reads `dashboard.reachable_url` from `musu up --json` / `musu doctor --json`, records `dashboard_base_url_source` and `dashboard_reachable_url`, and the release audit/verifier require runtime URL discovery while rejecting the old dev-port default. New evidence `docs\evidence\single-machine\1.15.0-rc.1\20260604-130301-HUGH_SECOND.evidence.json` passed with dashboard `http://127.0.0.1:3001`, source `musu up.dashboard.reachable_url`, bridge `http://127.0.0.1:8573`, task `42c7678d-22dd-4126-8ec2-1a1f4a3e15e8`, and output `MUSU_RELEASE_SMOKE_OK_20260604_130238`. Clean go/no-go on `918f81d4` reports `single_machine_verified=true`, runtime idle CPU `1/2`, runtime CPU matrix `1/2`, `manifest_dirty=false`, and six blockers. Roadmap remains local-executor first: `musu.pro` is the web input/project room/rendezvous/fallback/evidence plane, while local MUSU programs execute work on each device. Canonical report: `docs\RELEASE_1_15_0_RC1_SINGLE_MACHINE_DASHBOARD_URL_DISCOVERY_2026_06_04.md` (wiki/675). |
| 2026-06-04 v487 | Multi-device smoke now records route explain path-selection evidence. | `smoke-multidevice-beta.ps1` now captures `musu.route_explain.v1` from `musu route --explain --json` before the executing remote route, and `verify-multidevice-evidence.ps1` separates explain commands from executing route commands. Passing multi-device evidence now requires selected candidate diagnostics, delegate endpoint, path priority `lan,tailscale,direct_quic,relay`, release transport requirement `quic_tls_1_3`, fallback-only relay policy, and the existing release-grade `musu.route_evidence.v1` execution proof. Regression coverage passed with `test-release-evidence-verifiers.ps1` `ok=true`/`case_count=25`; local diagnostic explain for `HUGH-MAIN` still shows LAN `http_bearer`, `peer_identity_verified=false`, and `none_http_bearer`, so it remains diagnostic only. New current handoff artifacts are `.local-build\final-operator-gates\musu-final-operator-gates-1.15.0-rc.1-20260604-132819.zip`, action pack `.local-build\operator-action-pack\MUSU-1.15.0-rc.1-operator-action-pack-20260604-132834.zip`, and second-PC transfer `.local-build\operator-action-pack\MUSU-1.15.0-rc.1-operator-action-pack-20260604-132834\second-pc\MUSU-second-PC-transfer-1.15.0-rc.1-20260604-132834.zip`; final packet and action pack verifiers passed. Clean go/no-go on `4ed47213` remains `single_machine_verified=true`, runtime idle CPU `1/2`, runtime CPU matrix `1/2`, `manifest_dirty=false`, and six blockers. Canonical report: `docs\RELEASE_1_15_0_RC1_MULTIDEVICE_ROUTE_EXPLAIN_EVIDENCE_2026_06_04.md` (wiki/676). |
| 2026-06-04 v488 | MUSU.PRO control-plane roadmap and control SSE audit were recorded. | The product roadmap now explicitly separates local-only dashboards from the real web control plane: `localhost`/`127.0.0.1` dashboards are local operator/dev surfaces, `musu.pro` is the web input/project room/company meeting room/rendezvous/path-selection/relay-fallback/evidence plane, and local MUSU programs receive authenticated web work orders then execute work on each device. This is the Codex/GitHub-style product shape: cloud identity/project context/work orders/presence/coordination, local execution/files/app-browser-shell automation/P2P mesh transport. Current validation remains one-machine until the same current MUSU build is installed on a second Windows PC. The Rust background-loop contract audit now explicitly covers `musu-rs\src\control\http_server.rs` control SSE heartbeat behavior with bounded 30s interval, heartbeat event, and interval-stream mapping checks. Validation passed PowerShell parser check and Rust background-loop audit `ok=true`, `fail_count=0`, `unaudited_loop_hit_count=0`. Canonical report: `docs\RELEASE_1_15_0_RC1_MUSU_PRO_CONTROL_PLANE_ROADMAP_AND_CONTROL_SSE_AUDIT_2026_06_04.md` (wiki/677). |
| 2026-06-04 v489 | Nested app docs are now release-freshness status-only paths. | A clean go/no-go after v488 exposed that `musu-bee/docs/P2P_CONTROL_PLANE.md` was being treated as runtime-affecting because the freshness classifiers only allowed root `docs/*`. `verify-single-machine-evidence.ps1`, `verify-runtime-cpu-scenario-matrix.ps1`, and `write-release-go-no-go.ps1` now classify `musu-bee/docs/*` as documentation/status-only too. This keeps app-level product docs from incorrectly staling packaged MSIX/smoke/CPU evidence. The roadmap/control-plane report was updated with this follow-up. |
| 2026-06-04 v490 | CEO dispatch run SSE streams now close on terminal, error, duplicate, and unmount paths. | `CeoChatClient` now stores active dispatch run streams in `runStreamsRef`, closes any previous stream for the same run id before subscribing, unregisters and closes streams on terminal messages and SSE errors, marks still-streaming runs as error on stream failure, and closes all active run streams on component unmount. `runtime-polling-contract.test.ts` now includes `CEO dispatch run streams are explicitly closed`; `audit-frontend-polling-contract.ps1` now checks shared bounded EventSource retry/visibility contracts and CEO dispatch stream tracking/registration/removal/unmount/error cleanup plus no interval polling. Validation passed `npm run test:runtime-polling` 15/15, frontend polling audit `ok=true`/`fail_count=0`/direct interval hits `0`, `npm run typecheck`, and `npm run build`. This is a frontend runtime source change, so packaged single-machine/CPU evidence must be refreshed after commit. Canonical report: `docs\RELEASE_1_15_0_RC1_CEO_DISPATCH_SSE_CLEANUP_HARDENING_2026_06_04.md` (wiki/678). |
| 2026-06-04 v491 | Fresh primary-machine evidence was restored after CEO dispatch SSE cleanup. | Strict MSIX install evidence `docs\evidence\msix-install\1.15.0-rc.1\20260604-140415-HUGH_SECOND.evidence.json` passed with alias shadowing count `0`. Single-machine smoke `docs\evidence\single-machine\1.15.0-rc.1\20260604-140717-HUGH_SECOND.evidence.json` passed with dashboard `http://127.0.0.1:3001`, source `musu up.dashboard.reachable_url`, bridge `http://127.0.0.1:7462`, output `MUSU_RELEASE_SMOKE_OK_20260604_140650`, and CLI route output `MUSU_CLI_ROUTE_OK_20260604_140650`. Clean desktop-open CPU `docs\evidence\runtime-idle-cpu\1.15.0-rc.1\20260604-141753-HUGH_SECOND.desktop-open.evidence.json` passed for `60.062s` from `f96e5cca` with `git_dirty=false`, MUSU `0`, Node `0.03`, WebView2 `0.16`, owned WebView2 `6`, hot `0`, and working set `485.51MB`. Five-state matrix `docs\evidence\runtime-cpu-scenarios\1.15.0-rc.1\20260604-141924-HUGH_SECOND.runtime-cpu-scenario-matrix.json` passed verifier `ok=true`, `fail_count=0`, route token `MUSU_CPU_SCENARIO_ROUTE_OK_20260604_141924`, max CPU MUSU `0`, Node `0.05`, WebView2 `0.23`, max working set `485.13MB`, and hot `0`. Clean go/no-go reports `local_artifacts_ready=true`, `single_machine_verified=true`, runtime idle CPU `1/2`, runtime CPU matrix `1/2`, `manifest_git.dirty=false`, and six remaining blockers: second-PC multi-device, second-PC CPU/matrix, hosted `musu.pro` P2P relay proof, support mailbox, and Store evidence. Canonical report: `docs\RELEASE_1_15_0_RC1_POST_CEO_DISPATCH_SSE_PRIMARY_EVIDENCE_REFRESH_2026_06_04.md` (wiki/679). |
| 2026-06-04 v492 | Current operator handoff pack was regenerated after CEO dispatch evidence. | Clean HEAD `a0e0836c` generated final operator packet `.local-build\final-operator-gates\musu-final-operator-gates-1.15.0-rc.1-20260604-143204.zip` and action pack `.local-build\operator-action-pack\MUSU-1.15.0-rc.1-operator-action-pack-20260604-143217.zip`; final packet verification and action-pack verification both passed with `ok=true`, `fail_count=0`. Current second-PC transfer zip is `.local-build\operator-action-pack\MUSU-1.15.0-rc.1-operator-action-pack-20260604-143217\second-pc\MUSU-second-PC-transfer-1.15.0-rc.1-20260604-143217.zip`; Partner Center zip is `.local-build\operator-action-pack\MUSU-1.15.0-rc.1-operator-action-pack-20260604-143217\partner-center\MUSU-1.15.0-rc.1-store-submission-20260604-143217.zip`; support verification id is `musu-store-support-1.15.0-rc.1-20260604-143204`. Final handoff status reports `packet_verified=true`, `action_pack_verified=true`, `single_machine_verified=true`, runtime idle CPU `1/2`, runtime CPU matrix `1/2`, P2P relay route evidence count `0`, relay payload proof `false`, delivery proof valid count `0`, and blocker count `6`. Canonical report: `docs\RELEASE_1_15_0_RC1_CURRENT_OPERATOR_HANDOFF_PACK_AFTER_CEO_DISPATCH_EVIDENCE_2026_06_04.md` (wiki/680). |
| 2026-06-04 v493 | External gate recheck after CEO dispatch evidence confirmed the remaining blockers. | Packaged-alias external recheck recorded `docs\evidence\external-gates\1.15.0-rc.1\20260604-143952-HUGH_SECOND.external-gates.evidence.json` and live P2P evidence `docs\evidence\p2p-control-plane\1.15.0-rc.1\20260604-144053-musu.pro.evidence.json` plus verification `20260604-144053-musu.pro.verification.json`. Local hardening audits remain clean: Rust background loops, frontend polling, process ownership, local API auth, and operator API security all report `ok=true`. External release remains No-Go with `local_artifacts_ready=true`, `single_machine_verified=true`, runtime idle CPU `1/2`, runtime CPU matrix `1/2`, second PC `192.168.1.192:8949` unreachable via `tcp_connect_timeout`, P2P env missing KV/Upstash URL/token, P2P verification `fail_count=27`, relay route evidence count `0`, relay payload proof `false`, and relay delivery proof valid count `0`. Roadmap remains local-executor first: `musu.pro` is the web input/project room/rendezvous/path-selection/relay-fallback/evidence plane while local MUSU programs execute work and prefer direct P2P mesh routes. Canonical report: `docs\RELEASE_1_15_0_RC1_EXTERNAL_GATE_RECHECK_AFTER_CEO_DISPATCH_EVIDENCE_2026_06_04.md` (wiki/681). |
| 2026-06-04 v494 | Relay drain preview route evidence is now explicitly kept non-release-grade. | Added hosted route-evidence regression `keeps target-drain preview relay evidence non release grade even with delivery proof`. The test seeds an owner-scoped relay lease, delivered relay payload, matching `musu.relay_payload_delivery_proof.v1`, and target-drain preview-shaped relay evidence using `transport_verified_by=musu_relay_payload_drain_preview` and `encryption=relay_payload_queue_preview`; it verifies the delivery proof is accepted as stored while blockers still include `transport_not_release_grade_quic_tls`, `relay_route_missing_transport_proof`, `relay_route_transport_not_wired`, and `relay_route_payload_endpoint_not_wired`. Validation passed `npm run test:p2p` `62/62`, `npm run typecheck`, and `git diff --check`. This is evidence-gate hardening only; release-grade relay/tunnel transport, second-PC evidence, hosted KV/Upstash, support mailbox, and Store evidence remain open. Canonical report: `docs\RELEASE_1_15_0_RC1_RELAY_DRAIN_PREVIEW_EVIDENCE_GATE_HARDENING_2026_06_04.md` (wiki/682). |
| 2026-06-04 v495 | Test/spec source files are now release-freshness status-only, and the web-input/local-executor roadmap was locked. | `write-release-go-no-go.ps1`, `verify-single-machine-evidence.ps1`, and `verify-runtime-cpu-scenario-matrix.ps1` now classify `*.test.ts`, `*.test.tsx`, `*.spec.ts`, and `*.spec.tsx` as status-only so test-only gate hardening does not stale packaged MSIX/smoke/CPU evidence. `test-release-evidence-verifiers.ps1` added three classifier contract cases; parser checks passed and verifier regression passed with `ok=true`, `case_count=28`, `failed_case_count=0`. Roadmap lock: `localhost` dashboards are local-only, `musu.pro` is the web input/project-room/company-meeting-room/rendezvous/path-selection/relay-fallback/evidence plane, and local MUSU programs execute work on each device while preferring direct P2P mesh after web-assisted rendezvous. Multi-device work still requires installing the same current MUSU build on a second Windows PC. Canonical report: `docs\RELEASE_1_15_0_RC1_TEST_FILE_FRESHNESS_AND_WEB_INPUT_ROADMAP_2026_06_04.md` (wiki/683). |
| 2026-06-04 v496 | Clean go/no-go confirmed the test-only freshness classifier restored primary evidence status. | Clean HEAD `dd4fb7efab643c52cc47bcbb6ddd921058ef437a` reports `local_artifacts_ready=true`, `single_machine_verified=true`, `msix_install_verified=true`, `msix_desktop_entrypoint_verified=true`, runtime idle CPU `1/2 [HUGH_SECOND]`, runtime CPU matrix `1/2 [HUGH_SECOND]`, frontend polling/rust loop/process/startup/desktop single-instance gates true, `public_metadata_ok=true`, and `manifest_git.dirty=false`. Public release remains No-Go on six unchanged blockers: multi-device, second-PC runtime idle CPU, second-PC runtime CPU scenario matrix, support mailbox, Store release, and hosted `musu.pro` P2P control-plane proof. Hosted P2P still has owner scope false, relay lease store not release-grade, relay transport/payload endpoint unwired, route evidence count `0`, relay payload proof false, and delivery proof count `0`. Canonical report updated: `docs\RELEASE_1_15_0_RC1_TEST_FILE_FRESHNESS_AND_WEB_INPUT_ROADMAP_2026_06_04.md` (wiki/683). |
| 2026-06-04 v497 | Relay status now separates release connect endpoint state from preview queue state. | Hosted relay transport/lease/connect responses, Rust `P2pRelayTransportResponse`, `musu relay status --json`, `musu relay transport --json`, and `record-p2p-control-plane-evidence.ps1` now expose `relay_connect_endpoint_wired` and `relay_payload_queue_endpoint_wired` separately. Current source keeps `relay_connect_endpoint_wired=false`, `relay_payload_endpoint_wired=false`, `relay_payload_queue_endpoint_wired=true`, `relay_transport_wired=false`, and `relay_default_data_path=false`, so preview queue progress is visible without claiming release-grade relay/tunnel transport. Validation passed `npm run test:p2p` `62/62`, `npm run typecheck`, Rust fmt, targeted Rust relay status test `1/1`, `cargo check --bin musu`, PowerShell parser check, and `git diff --check`. Because runtime/web/Rust source changed, fresh packaged primary evidence is required after commit before current-source local artifact readiness can be claimed again. Canonical report: `docs\RELEASE_1_15_0_RC1_RELAY_CONNECT_AND_QUEUE_STATUS_SPLIT_2026_06_04.md` (wiki/684). |
| 2026-06-04 v498 | Fresh primary-machine evidence was restored after the relay connect/queue status split, and the web-input/local-executor roadmap remains locked. | Rebuilt and installed local-sideload MSIX `Yellowhama.MUSU_1.15.0.0_x64__ygcjq669as2b6`. Strict MSIX evidence `docs\evidence\msix-install\1.15.0-rc.1\20260604-155606-HUGH_SECOND.evidence.json` passed with WindowsApps alias first and alias shadowing count `0`. Single-machine smoke `docs\evidence\single-machine\1.15.0-rc.1\20260604-154159-HUGH_SECOND.evidence.json` passed with dashboard `http://127.0.0.1:3001`, source `musu up.dashboard.reachable_url`, bridge `http://127.0.0.1:2817`, task `f16d5bb9-eed4-42c9-9b7c-ccda14e68786`, and output `MUSU_RELEASE_SMOKE_OK_20260604_154129`. Desktop-open CPU `docs\evidence\runtime-idle-cpu\1.15.0-rc.1\20260604-154401-HUGH_SECOND.desktop-open.evidence.json` passed for `60.055s` with MUSU `0`, Node `0.05`, WebView2 `1.09`, owned WebView2 `6`, working set `483.5MB`, and hot `0`. Five-state matrix `docs\evidence\runtime-cpu-scenarios\1.15.0-rc.1\20260604-154626-HUGH_SECOND.runtime-cpu-scenario-matrix.json` passed verifier `ok=true`, `fail_count=0`, route token `MUSU_CPU_SCENARIO_ROUTE_OK_20260604_154626`, and hot `0` across all states. Clean go/no-go on `c3d36a7b` reports `local_artifacts_ready=true`, `single_machine_verified=true`, `msix_install_verified=true`, runtime idle CPU `1/2 [HUGH_SECOND]`, runtime CPU matrix `1/2 [HUGH_SECOND]`, `public_metadata_ok=true`, `manifest_git.dirty=false`, and six blockers: second-PC multi-device, second-PC CPU/matrix, support mailbox, Store release, and hosted `musu.pro` P2P control-plane proof. Roadmap remains: MUSU.PRO is web input/project room/company meeting room/rendezvous/path-selection/relay-fallback/evidence plane; local MUSU programs execute work and prefer P2P mesh after web-assisted rendezvous. Canonical report: `docs\RELEASE_1_15_0_RC1_POST_RELAY_CONNECT_QUEUE_STATUS_PRIMARY_EVIDENCE_REFRESH_2026_06_04.md` (wiki/685). |
| 2026-06-04 v499 | MUSU.PRO work-order context now survives the web-input to local-executor boundary. | `/api/tasks/forward` now accepts bounded `company_id`, `project_id`, `room_id`, `work_order_id`, and `origin`, defaults `origin=musu.pro` for `musu.pro` hosts and `local_dashboard` for local dashboard calls, and forwards that context to the local bridge. Rust `/api/tasks/delegate` accepts the same fields, writes bounded audit notes without prompt/cwd leakage, and `ForwardedTask` carries the context through direct peer forwarding and relay payload preview serialization. MCP `delegate_task` exposes the same fields. Validation passed `npm run test:routes` `14/14`, specific forward route test `2/2`, `npm run typecheck`, `cargo fmt`, `cargo check --bin musu`, and three targeted Rust unit tests for delegate audit, forwarded-task audit, and relay payload context preservation. This is runtime/web/Rust source, so current packaged primary evidence is stale until MSIX/single-machine/CPU evidence is refreshed for this HEAD. Canonical report: `docs\RELEASE_1_15_0_RC1_MUSU_PRO_WORK_ORDER_CONTEXT_HARDENING_2026_06_04.md` (wiki/686). |
| 2026-06-04 v500 | Fresh primary-machine evidence was restored after MUSU.PRO work-order context hardening. | Rebuilt/reinstalled local-sideload MSIX and refreshed primary evidence. Strict MSIX evidence `docs\evidence\msix-install\1.15.0-rc.1\20260604-164153-HUGH_SECOND.evidence.json` passed with WindowsApps alias first and alias shadowing accepted `false`. Single-machine smoke `docs\evidence\single-machine\1.15.0-rc.1\20260604-164313-HUGH_SECOND.evidence.json` passed with dashboard `http://127.0.0.1:3001`, reachable URL `http://127.0.0.1:3001/app`, source `musu up.dashboard.reachable_url`, bridge `http://127.0.0.1:11480`, and output `MUSU_RELEASE_SMOKE_OK_20260604_164246`. Desktop-open CPU `docs\evidence\runtime-idle-cpu\1.15.0-rc.1\20260604-164620-HUGH_SECOND.desktop-open.evidence.json` passed for `60.065s` with MUSU `0`, Node `0`, WebView2 `0.18`, owned WebView2 `6`, working set `466.49MB`, and hot `0`. Five-state matrix `docs\evidence\runtime-cpu-scenarios\1.15.0-rc.1\20260604-164933-HUGH_SECOND.runtime-cpu-scenario-matrix.json` passed verifier `ok=true`, `fail_count=0`, route token `MUSU_CPU_SCENARIO_ROUTE_OK_20260604_164933`, max CPU MUSU `0.03`, Node `0.03`, WebView2 `0.18`, max working set `470.97MB`, and hot `0`. Clean go/no-go on `d8e91f0f` reports `local_artifacts_ready=true`, `single_machine_verified=true`, `msix_install_verified=true`, runtime idle CPU `1/2 [HUGH_SECOND]`, runtime CPU matrix `1/2 [HUGH_SECOND]`, `public_metadata_ok=true`, `manifest_git.dirty=false`, and six blockers: second-PC multi-device, second-PC CPU/matrix, support mailbox, Store release, and hosted `musu.pro` P2P control-plane proof. Roadmap remains: MUSU.PRO is web input/project room/company meeting room/rendezvous/path-selection/relay-fallback/evidence plane; local MUSU programs execute work and prefer P2P mesh after web-assisted rendezvous. Canonical report: `docs\RELEASE_1_15_0_RC1_POST_WORK_ORDER_CONTEXT_PRIMARY_EVIDENCE_REFRESH_2026_06_04.md` (wiki/687). |
| 2026-06-04 v501 | P2P release evidence now requires explicit relay connect endpoint proof. | `verify-p2p-control-plane-evidence.ps1` now requires both `relay_status.relay_connect_endpoint_wired=true` and `relay_transport.relay_connect_endpoint_wired=true`, and includes those fields in aggregate `relay_transport_descriptor_ok` and `relay_transport_wired`. `record-p2p-control-plane-evidence.ps1` now includes connect endpoint proof in both `statusOk` and `transportOk`. `test-release-evidence-verifiers.ps1` adds `p2p-bad-relay-connect-endpoint`, so relay transport evidence without a wired connect endpoint is rejected. Validation passed PowerShell parser checks and release evidence verifier regression with `ok=true`, `case_count=29`, `failed_case_count=0`. Current hosted P2P evidence `docs\evidence\p2p-control-plane\1.15.0-rc.1\20260604-144053-musu.pro.evidence.json` remains blocked with `fail_count=29`, `relay_status_connect_endpoint_wired=false`, `relay_transport_connect_endpoint_wired=false`, `relay_transport_payload_endpoint_wired=false`, lease store unconfigured, route evidence count `0`, and relay payload transport unproven. This is status/evidence-gate hardening only; packaged primary evidence remains current. Canonical report: `docs\RELEASE_1_15_0_RC1_P2P_CONNECT_ENDPOINT_EVIDENCE_GATE_HARDENING_2026_06_04.md` (wiki/688). |
| 2026-06-04 v502 | Primary-machine CPU stayed within budget after a failed HUGH-MAIN route attempt. | Captured targeted post-route CPU evidence with `musu route --target HUGH-MAIN --wait` using the packaged WindowsApps alias. The route probe failed by timeout to `http://192.168.1.192:8949/api/tasks/delegate`, and that failure was explicitly allowed for this CPU-only evidence. Matrix `docs\evidence\runtime-cpu-scenarios\1.15.0-rc.1\20260604-171623-HUGH_SECOND.runtime-cpu-scenario-matrix.json`, sample `20260604-171623-HUGH_SECOND.post-route.evidence.json`, target verification `20260604-171623-HUGH_SECOND.target-route.verification.json`, and summary `20260604-171623-HUGH_SECOND.target-route.summary.md` passed with verifier `ok=true`, `fail_count=0`, target `HUGH-MAIN`, and `allow_failed_post_route_probe=true`. CPU sample ran `60.058s` from clean commit `b96eadd1`, with MUSU `0`, Node `0.05`, WebView2 `0.18`, owned WebView2 `6`, working set `465.97MB`, and hot process count `0`. This proves the primary machine did not enter an idle busy-loop after the failed second-PC route attempt; it is not successful multi-device route evidence. Canonical report: `docs\RELEASE_1_15_0_RC1_HUGH_MAIN_ROUTE_ATTEMPT_CPU_EVIDENCE_2026_06_04.md` (wiki/689). |
| 2026-06-04 v503 | Local program and web input roadmap alignment was recorded. | The roadmap now explicitly separates installed local MUSU programs from the `musu.pro` website. Local programs execute work on each device: files, shell/app/browser automation, local bridge/runtime execution, and P2P mesh traffic. `musu.pro` is the remote user input, project room, company meeting room, presence, rendezvous, path-selection, fallback-relay coordination, and evidence plane. `localhost` dashboards are same-machine operator/developer surfaces, not cloud dashboard access. `musu.pro` sends authenticated bounded work-order envelopes and room events but must not execute local work or become the default data path. Devices use `musu.pro` to bootstrap discovery and exchange route offers, then prefer direct P2P mesh; hosted relay remains fallback after direct-route failure and the Connect/Pro boundary. Current validation remains one-machine until the same current build is installed on the second Windows PC. Canonical report: `docs\RELEASE_1_15_0_RC1_LOCAL_PROGRAM_WEB_INPUT_ROADMAP_ALIGNMENT_2026_06_04.md` (wiki/690). |
| 2026-06-04 v504 | MUSU.PRO rooms now have an explicit web work-order API. | Added `POST /api/rooms/[roomId]/work-orders` so company/project rooms can accept web-originated work orders while preserving the local-executor boundary. The route requires `roomId` and `instruction`, defaults `channel=company-room`, defaults `sender_id=musu.pro-room`, sets `origin=musu.pro`, takes `room_id` from the path, preserves bounded `company_id`, `project_id`, and `work_order_id`, generates a bounded `work_order_id` when omitted, normalizes `file://` workspace URIs, forwards the envelope to local bridge `/api/tasks/delegate`, and returns `room_id`, `work_order_id`, `origin`, plus bridge response. Validation passed `npm run test:routes` `18/18`, direct room route test `4/4`, `npm run typecheck`, `npm run build`, and `git diff --check`. This is web runtime source, so fresh packaged primary evidence is required after commit. Canonical report: `docs\RELEASE_1_15_0_RC1_MUSU_PRO_ROOM_WORK_ORDER_API_2026_06_04.md` (wiki/691). |
| 2026-06-04 v505 | Fresh primary-machine evidence was restored after the MUSU.PRO room work-order API. | Rebuilt/reinstalled local-sideload MSIX for current source; `install-and-verify-msix.ps1` passed, while HUGH_SECOND's fresh warning-mode install capture stayed diagnostic-only because `.cargo\bin\musu.exe` currently shadows WindowsApps in PATH. Single-machine smoke `docs\evidence\single-machine\1.15.0-rc.1\20260604-175043-HUGH_SECOND.evidence.json` passed with dashboard `http://127.0.0.1:3001`, bridge `http://127.0.0.1:2001`, task `ece8235e-e06b-4b45-9754-d3d79f5b0d05`, and output `MUSU_RELEASE_SMOKE_OK_20260604_175010`. Desktop-open CPU `docs\evidence\runtime-idle-cpu\1.15.0-rc.1\20260604-175223-HUGH_SECOND.desktop-open.evidence.json` passed for `60.052s` with MUSU `0.03`, Node `0.05`, WebView2 `0.6`, owned WebView2 `6`, working set `480.89MB`, and hot `0`. Five-state matrix `docs\evidence\runtime-cpu-scenarios\1.15.0-rc.1\20260604-175413-HUGH_SECOND.runtime-cpu-scenario-matrix.json` passed verifier `ok=true`, `fail_count=0`, route token `MUSU_CPU_SCENARIO_ROUTE_OK_20260604_175413`, max CPU MUSU `0.31`, Node `0.05`, WebView2 `0.47`, max working set `483.12MB`, and hot `0`. Clean go/no-go on `b3776f0c` reports `local_artifacts_ready=true`, `single_machine_verified=true`, `msix_install_verified=true`, runtime idle CPU `1/2 [HUGH_SECOND]`, runtime CPU matrix `1/2 [HUGH_SECOND]`, `public_metadata_ok=true`, `manifest_git.dirty=false`, and six blockers: second-PC multi-device, second-PC CPU/matrix, support mailbox, Store release, and hosted `musu.pro` P2P control-plane proof. Canonical report: `docs\RELEASE_1_15_0_RC1_POST_ROOM_WORK_ORDER_API_PRIMARY_EVIDENCE_REFRESH_2026_06_04.md` (wiki/692). |
| 2026-06-04 v506 | MUSU.PRO rooms now initiate P2P rendezvous sessions. | Added `POST /api/rooms/[roomId]/rendezvous` so a project/company room can create a P2P rendezvous session between `source_node_id` and `target_node_id` while preserving the local-executor boundary. The route requires P2P control bearer auth, stamps `origin=musu.pro`, uses the path room id as authoritative `room_id`, accepts optional `requested_capability`, `company_id`, `project_id`, and `work_order_id`, seeds both nodes from cached P2P candidate sets, and returns the existing path-selection order `lan`, `tailscale`, `direct_quic`, `relay`. `StoredP2pRendezvousSession` now preserves bounded `company_id`/`project_id`/`room_id`/`work_order_id`/`origin` context. Validation passed direct room rendezvous test `3/3`, `npm run test:p2p` `65/65`, `npm run test:routes` `18/18`, `npm run typecheck`, `npm run build`, and `git diff --check`. This is web runtime source, so fresh packaged primary evidence is required after commit. Canonical report: `docs\RELEASE_1_15_0_RC1_ROOM_SCOPED_RENDEZVOUS_API_2026_06_04.md` (wiki/693). |
| 2026-06-04 v507 | Fresh primary-machine evidence was restored after the MUSU.PRO room-scoped rendezvous API. | Rebuilt/reinstalled local-sideload MSIX for current source; `install-and-verify-msix.ps1` passed, while HUGH_SECOND's live warning-mode install output remains diagnostic-only because `.cargo\bin\musu.exe` currently shadows WindowsApps in PATH. Single-machine smoke `docs\evidence\single-machine\1.15.0-rc.1\20260604-182640-HUGH_SECOND.evidence.json` passed with dashboard `http://127.0.0.1:3001`, reachable URL `http://127.0.0.1:3001/app`, bridge `http://127.0.0.1:12502`, task `a5915ece-5383-4b8a-b805-d650937362d7`, and output `MUSU_RELEASE_SMOKE_OK_20260604_182613`. Desktop-open CPU `docs\evidence\runtime-idle-cpu\1.15.0-rc.1\20260604-182732-HUGH_SECOND.desktop-open.evidence.json` passed for `60.04s` with MUSU `0.13`, Node `0`, WebView2 `0.68`, owned WebView2 `6`, working set `486.19MB`, and hot `0`. Five-state matrix `docs\evidence\runtime-cpu-scenarios\1.15.0-rc.1\20260604-182915-HUGH_SECOND.runtime-cpu-scenario-matrix.json` passed verifier `ok=true`, `fail_count=0`, route token `MUSU_CPU_SCENARIO_ROUTE_OK_20260604_182915`, max CPU MUSU `0`, Node `0.05`, WebView2 `0.31`, max working set `489.12MB`, and route probe ok. Clean go/no-go on `5fb40731` reports `local_artifacts_ready=true`, `single_machine_verified=true`, `msix_install_verified=true`, `msix_desktop_entrypoint_verified=true`, runtime idle CPU `1/2 [HUGH_SECOND]`, runtime CPU matrix `1/2 [HUGH_SECOND]`, `public_metadata_ok=true`, `manifest_git.dirty=false`, and public release No-Go on remaining external/current-second-PC gates. Canonical report: `docs\RELEASE_1_15_0_RC1_POST_ROOM_SCOPED_RENDEZVOUS_API_PRIMARY_EVIDENCE_REFRESH_2026_06_04.md` (wiki/694). |
| 2026-06-04 v508 | MUSU.PRO rooms now have an owner-scoped event log for meeting coordination. | Added `POST /api/rooms/[roomId]/events` and `GET /api/rooms/[roomId]/events` so project/company rooms can record and query bounded room events while preserving the local-executor boundary. The route requires P2P control bearer auth, stores owner-scoped `musu.room_event.v1` events, supports `presence`/`status`/`message`/`decision`/`work_order`/`rendezvous`/`route`/`error`, preserves bounded company/project/work-order/source-node/source-agent/message/payload/origin context, returns `newest_first` events with scoped filters, and fails closed in production without KV/Upstash or explicit `MUSU_ROOM_EVENT_STORE_PATH`. Validation passed direct room event route test `5/5`, `npm run test:p2p` `70/70`, `npm run test:routes` `18/18`, `npm run typecheck`, `npm run build`, and `git diff --check`. This is web runtime source, so fresh packaged primary evidence is required after commit. Canonical report: `docs\RELEASE_1_15_0_RC1_ROOM_EVENT_API_2026_06_04.md` (wiki/695). |
| 2026-06-04 v509 | Fresh primary-machine evidence was restored after the MUSU.PRO room event API. | Rebuilt/reinstalled local-sideload MSIX for current source; `install-and-verify-msix.ps1` passed, while HUGH_SECOND's live warning-mode install output remains diagnostic-only because `.cargo\bin\musu.exe` currently shadows WindowsApps in PATH. Single-machine smoke `docs\evidence\single-machine\1.15.0-rc.1\20260604-185920-HUGH_SECOND.evidence.json` passed with dashboard `http://127.0.0.1:3001`, reachable URL `http://127.0.0.1:3001/app`, bridge `http://127.0.0.1:2555`, task `985b7bae-8a1d-4815-82e8-67202abe7938`, and output `MUSU_RELEASE_SMOKE_OK_20260604_185856`. Desktop-open CPU `docs\evidence\runtime-idle-cpu\1.15.0-rc.1\20260604-190029-HUGH_SECOND.desktop-open.evidence.json` passed for `60.063s` with MUSU `0.03`, Node `0`, WebView2 `0.49`, owned WebView2 `6`, working set `484.19MB`, and hot `0`. Five-state matrix `docs\evidence\runtime-cpu-scenarios\1.15.0-rc.1\20260604-190203-HUGH_SECOND.runtime-cpu-scenario-matrix.json` passed verifier `ok=true`, `fail_count=0`, route token `MUSU_CPU_SCENARIO_ROUTE_OK_20260604_190203`, max CPU MUSU `0.1`, Node `0.05`, WebView2 `0.55`, max working set `484.91MB`, and route probe ok. Clean go/no-go on `5d94c236` reports `local_artifacts_ready=true`, `single_machine_verified=true`, `msix_install_verified=true`, `msix_desktop_entrypoint_verified=true`, runtime idle CPU `1/2 [HUGH_SECOND]`, runtime CPU matrix `1/2 [HUGH_SECOND]`, `public_metadata_ok=true`, `manifest_git.dirty=false`, and public release No-Go on remaining second-PC, hosted P2P, support mailbox, and Store gates. Canonical report: `docs\RELEASE_1_15_0_RC1_POST_ROOM_EVENT_API_PRIMARY_EVIDENCE_REFRESH_2026_06_04.md` (wiki/696). |
| 2026-06-04 v510 | MUSU.PRO rooms now expose owner-scoped current presence for local executors and agents. | Added `POST /api/rooms/[roomId]/presence` and `GET /api/rooms/[roomId]/presence` so project/company rooms can show current node/agent status instead of relying only on an append-only event log. The route requires P2P control bearer auth, stores owner-scoped `musu.room_presence.v1` records, supports `online`/`idle`/`busy`/`offline`, preserves bounded company/project/source-agent/active-work-order context, node identity, app version, capabilities, public key, relay capability, route candidate endpoints, origin, `last_seen_at`, and `expires_at`, and returns `last_seen_desc` presence with scoped filters. `POST` also seeds the existing P2P rendezvous candidate cache through `saveNodeCandidateSet`, so room presence can feed path selection before a room-scoped rendezvous is created. Production fails closed without KV/Upstash or explicit `MUSU_ROOM_PRESENCE_STORE_PATH`. Validation passed direct room presence route test `6/6`, `npm run test:p2p` `76/76`, `npm run typecheck`, `npm run build`, and `git diff --check`. This is web runtime source, so fresh packaged primary evidence is required after commit. Canonical report: `docs\RELEASE_1_15_0_RC1_ROOM_PRESENCE_API_2026_06_04.md` (wiki/697). |
| 2026-06-04 v511 | Fresh primary-machine evidence was restored after the MUSU.PRO room presence API. | Rebuilt/reinstalled local-sideload MSIX for current source. Single-machine smoke `docs\evidence\single-machine\1.15.0-rc.1\20260604-193251-HUGH_SECOND.evidence.json` passed with dashboard `http://127.0.0.1:3001`, reachable URL `http://127.0.0.1:3001/app`, bridge `http://127.0.0.1:10358`, task `fcd02b2e-1516-4eae-b839-4758fe971bdd`, and output `MUSU_RELEASE_SMOKE_OK_20260604_193224`. Desktop-open CPU `docs\evidence\runtime-idle-cpu\1.15.0-rc.1\20260604-193347-HUGH_SECOND.desktop-open.evidence.json` passed for `60.162s` with MUSU `0`, Node `0.05`, WebView2 `0.78`, owned WebView2 `6`, working set `482.9MB`, and hot `0`. Five-state matrix `docs\evidence\runtime-cpu-scenarios\1.15.0-rc.1\20260604-193512-HUGH_SECOND.runtime-cpu-scenario-matrix.json` passed verifier `ok=true`, `fail_count=0`, route token `MUSU_CPU_SCENARIO_ROUTE_OK_20260604_193512`, max CPU MUSU `0.03`, Node `0.03`, WebView2 `0.34`, max working set `483.64MB`, and route probe ok. Clean go/no-go on `8e1dc11` reports `local_artifacts_ready=true`, `single_machine_verified=true`, `msix_install_verified=true`, `msix_desktop_entrypoint_verified=true`, runtime idle CPU `1/2 [HUGH_SECOND]`, runtime CPU matrix `1/2 [HUGH_SECOND]`, `public_metadata_ok=true`, `manifest_git.dirty=false`, and public release No-Go on remaining second-PC, hosted P2P, support mailbox, and Store gates. Roadmap remains: `musu.pro` is the remote input/project room/company meeting room/presence/rendezvous/path-selection/relay-fallback/evidence plane; local MUSU programs execute work and prefer P2P mesh after web-assisted rendezvous. Canonical report: `docs\RELEASE_1_15_0_RC1_POST_ROOM_PRESENCE_API_PRIMARY_EVIDENCE_REFRESH_2026_06_04.md` (wiki/698). |
| 2026-06-04 v512 | Local MUSU programs can publish and query MUSU.PRO room presence. | Added Rust cloud DTOs/client methods for `POST/GET /api/rooms/[roomId]/presence` and CLI commands `musu room presence publish <room-id>` / `musu room presence list <room-id>`. `publish` builds a local route candidate from the bridge URL or `--public-url`, classifies LAN/Tailscale/direct-QUIC, includes endpoint scheme, default capability `bridge_http_forward`, optional active work orders, optional relay capability, `origin=musu.local-program`, and the local TLS fingerprint public key when available. `list` supports owner-scoped company/project/node/source-agent/status/expired filters. This keeps the roadmap boundary explicit: `musu.pro` is web input/project room/company meeting room/presence/rendezvous/path-selection/relay-fallback/evidence; local MUSU programs execute work and prefer P2P mesh after web-assisted rendezvous. No background heartbeat, timer, or polling loop was added. Validation passed targeted Rust room presence lib tests `4/4`, CLI parser test `1/1`, `cargo check --bin musu`, debug binary build, room presence help checks, and `git diff --check`. This is Rust runtime source, so the current packaged MSIX/smoke/CPU/matrix evidence and operator packet/action pack are stale until regenerated from this commit. Canonical report: `docs\RELEASE_1_15_0_RC1_ROOM_PRESENCE_CLIENT_CLI_2026_06_04.md` (wiki/699). |
| 2026-06-04 v513 | Fresh primary-machine evidence was restored after the room presence client CLI. | Rebuilt local-sideload MSIX for current source and reinstalled it; install verification passed, while HUGH_SECOND's `.cargo\bin\musu.exe` PATH shadowing remains warning-mode and was not recorded as new strict MSIX install evidence. Single-machine smoke `docs\evidence\single-machine\1.15.0-rc.1\20260604-204006-HUGH_SECOND.evidence.json` passed with dashboard `http://127.0.0.1:3001`, reachable URL `http://127.0.0.1:3001/app`, bridge `http://127.0.0.1:11853`, task `5c2b5865-e468-4cd0-891a-160ca56600a2`, dashboard output `MUSU_RELEASE_SMOKE_OK_20260604_203939`, and CLI output `MUSU_CLI_ROUTE_OK_20260604_203939`. Desktop-open CPU `docs\evidence\runtime-idle-cpu\1.15.0-rc.1\20260604-205835-HUGH_SECOND.desktop-open.evidence.json` passed for `60.049s` from clean git state with `require_owned_webview2=true`, MUSU `0`, Node `0.03`, WebView2 `0.1`, owned WebView2 `6`, working set `488.93MB`, and hot `0`. Five-state matrix `docs\evidence\runtime-cpu-scenarios\1.15.0-rc.1\20260604-204423-HUGH_SECOND.runtime-cpu-scenario-matrix.json` passed verifier `ok=true`, `fail_count=0`, route token `MUSU_CPU_SCENARIO_ROUTE_OK_20260604_204423`, and hot `0` across required scenarios. Clean go/no-go on `75348c74` reports `local_artifacts_ready=true`, `single_machine_verified=true`, `msix_install_verified=true`, `msix_desktop_entrypoint_verified=true`, `manifest_git.dirty=false`, and public release No-Go on remaining second-PC, hosted P2P, support mailbox, and Store gates. Canonical report: `docs\RELEASE_1_15_0_RC1_POST_ROOM_PRESENCE_CLIENT_CLI_PRIMARY_EVIDENCE_REFRESH_2026_06_04.md` (wiki/700). |
| 2026-06-04 v514 | MCP app views no longer own direct polling intervals. | Added `musu-bee\views\shared\useLowDutyPolling.ts`, migrated `musu-bee\views\nodes\NodesView.tsx` and `musu-bee\views\tasks\TasksView.tsx` away from direct `setInterval`, expanded `audit-frontend-polling-contract.ps1` to scan both `musu-bee\src` and `musu-bee\views`, and added runtime-polling contract coverage for the Vite MCP app views. Validation passed `npm run test:runtime-polling` `16/16`, frontend polling audit `ok=true` with `direct_interval_hit_count=0` and `direct_visibility_listener_hit_count=0`, `npm run build` in `musu-bee\views`, `npx tsc --noEmit` in `musu-bee\views`, and `git diff --check`. This removes another frontend interval/refetch-loop candidate from the idle CPU investigation. This is runtime frontend source, so packaged primary evidence and operator packets are stale after commit until rebuilt/refreshed. Canonical report: `docs\RELEASE_1_15_0_RC1_MCP_APP_VIEWS_LOW_DUTY_POLLING_HARDENING_2026_06_04.md` (wiki/701). |
| 2026-06-04 v515 | Fresh primary-machine evidence was restored after MCP app views polling hardening. | Rebuilt local-sideload MSIX for current source and reinstalled it; install verification passed, while HUGH_SECOND's `.cargo\bin\musu.exe` PATH shadowing remains warning-mode and was not recorded as new strict MSIX install evidence. Single-machine smoke `docs\evidence\single-machine\1.15.0-rc.1\20260604-211929-HUGH_SECOND.evidence.json` passed with dashboard `http://127.0.0.1:3001`, bridge `http://127.0.0.1:10487`, task `f073acec-efa7-468e-a4f0-921fbdcd9811`, output `MUSU_RELEASE_SMOKE_OK_20260604_211856`, and CLI route checked. Desktop-open CPU `docs\evidence\runtime-idle-cpu\1.15.0-rc.1\20260604-212016-HUGH_SECOND.desktop-open.evidence.json` passed for `60.041s` from clean git state with `require_owned_webview2=true`, MUSU `0`, Node `0.05`, WebView2 `0.49`, owned WebView2 `6`, working set `490.13MB`, and hot `0`. Five-state matrix `docs\evidence\runtime-cpu-scenarios\1.15.0-rc.1\20260604-212147-HUGH_SECOND.runtime-cpu-scenario-matrix.json` passed verifier `ok=true`, `fail_count=0`, route token `MUSU_CPU_SCENARIO_ROUTE_OK_20260604_212147`, max role CPU MUSU `0.03`, Node `0.03`, WebView2 `0.39`, max working set `494.64MB`, and hot `0`. Public release remains No-Go on actual second-PC multi-device evidence, two-machine CPU/matrix evidence, hosted P2P control-plane proof, support mailbox, and Store evidence. Canonical report: `docs\RELEASE_1_15_0_RC1_POST_MCP_APP_VIEWS_LOW_DUTY_POLLING_PRIMARY_EVIDENCE_REFRESH_2026_06_04.md` (wiki/702). |
| 2026-06-04 v516 | MCP app view poller abort signals now reach actual MCP tool requests, and fresh primary-machine evidence was restored. | `NodesView.tsx` and `TasksView.tsx` now pass the low-duty poller's `AbortSignal` into `app.callServerTool` for `poll_agents` and `poll_tasks`, ignore stale results after abort, and the shared MCP view task API helpers now accept/pass optional abort signals. `audit-frontend-polling-contract.ps1` and `runtime-polling-contract.test.ts` now enforce the MCP view abort-signal contract. Validation passed `npm run test:runtime-polling` `16/16`, frontend polling audit `ok=true` with `direct_interval_hit_count=0` and `direct_visibility_listener_hit_count=0`, `npx tsc --noEmit` in `musu-bee\views`, `npm run build` in `musu-bee\views`, and `git diff --check`. Rebuilt/reinstalled local-sideload MSIX; install verification passed with HUGH_SECOND's `.cargo\bin\musu.exe` PATH shadowing remaining warning-mode. Single-machine smoke `docs\evidence\single-machine\1.15.0-rc.1\20260604-214647-HUGH_SECOND.evidence.json` passed with dashboard `http://127.0.0.1:3001`, bridge `http://127.0.0.1:4813`, task `18eae085-36a7-4049-b041-6cc15b4f4bbd`, and output `MUSU_RELEASE_SMOKE_OK_20260604_214623`. Desktop-open CPU `docs\evidence\runtime-idle-cpu\1.15.0-rc.1\20260604-214900-HUGH_SECOND.desktop-open.evidence.json` passed for `60.061s` from clean git state with MUSU `0`, Node `0.1`, WebView2 `0.1`, owned WebView2 `6`, working set `492.61MB`, and hot `0`. Five-state matrix `docs\evidence\runtime-cpu-scenarios\1.15.0-rc.1\20260604-215050-HUGH_SECOND.runtime-cpu-scenario-matrix.json` passed verifier `ok=true`, `fail_count=0`, route token `MUSU_CPU_SCENARIO_ROUTE_OK_20260604_215050`, max role CPU MUSU `0`, Node `0.03`, WebView2 `0.26`, max working set `495.13MB`, and hot `0`. Roadmap remains: `musu.pro` is remote input/project room/company meeting room/presence/rendezvous/path-selection/relay-fallback/evidence; local MUSU programs execute work and prefer P2P mesh after web-assisted rendezvous. Public release remains No-Go on second-PC, hosted P2P, support mailbox, and Store gates. Canonical report: `docs\RELEASE_1_15_0_RC1_MCP_APP_VIEW_ABORT_SIGNAL_HARDENING_AND_PRIMARY_EVIDENCE_REFRESH_2026_06_04.md` (wiki/703). |
| 2026-06-04 v517 | Rust loop allowlist contract hardening now audits the actual bounded-loop guarantees. | `audit-rust-background-loop-contract.ps1` now verifies the safety contracts for the remaining allowlisted Rust loop sites instead of relying only on a file allowlist. Added explicit checks for Claude adapter per-iteration stdout timeout/cancel/preempt deadline/shared kill path, file-sync bounded receive timeout and cooldown sleep, indexer watch notify/debounce/dirty/sidecar filter, CLI login expiry and 5s poll sleep, workflow executor 2s poll plus 1h max wait and terminal-state exit, hardware probe timeout kill plus 50ms sleep, PTY blocking read and websocket close exit, request-scoped WebRTC ffmpeg capture kill paths, finite Windows process snapshot enumeration, and writer runner admission/stream cancel-timeout loops. Validation passed `audit-rust-background-loop-contract.ps1 -Json` with `ok=true`, `fail_count=0`, and `unaudited_loop_hit_count=0`. This is audit/status-gate hardening only, so packaged runtime evidence remains current. Roadmap remains: `musu.pro` is remote input/project room/company meeting room/presence/rendezvous/path-selection/relay-fallback/evidence; local MUSU programs execute work and prefer P2P mesh after web-assisted rendezvous. Public release remains No-Go on second-PC, hosted P2P, support mailbox, and Store gates. Canonical report: `docs\RELEASE_1_15_0_RC1_RUST_LOOP_ALLOWLIST_CONTRACT_HARDENING_2026_06_04.md` (wiki/704). |
| 2026-06-04 v518 | Hosted P2P env status now separates source relay marker blockers from hosted KV/evidence blockers. | `show-musu-pro-p2p-env-status.ps1` now inspects `musu-bee\src\lib\p2pRelayPolicy.ts` and emits a `source` section with `relay_connect_endpoint_implemented`, `relay_payload_endpoint_implemented`, `relay_payload_queue_endpoint_implemented`, `relay_transport_kind`, and `release_grade_transport_required`. Current source reports connect endpoint false, payload endpoint false, preview queue true, `websocket_tunnel`, and `quic_tls_1_3`. The script now adds `source_relay_connect_endpoint_not_implemented` and `source_relay_payload_endpoint_not_implemented`, so operators can see that KV/Upstash provisioning and env flags are necessary but not sufficient while `/api/v1/relay/connect` remains a fail-closed placeholder. Validation passed PowerShell parser check and `show-musu-pro-p2p-env-status.ps1 -Json`, which now reports source blockers plus the existing hosted blockers: missing KV/Upstash URL/token, `live_evidence_p2p_relay_lease_kv_not_configured`, relay transport not wired, relay route not proven, and relay payload delivery proof missing. This is status/gate hardening only; packaged runtime evidence remains current. Public release remains No-Go on second-PC, hosted P2P, support mailbox, and Store gates. Canonical report: `docs\RELEASE_1_15_0_RC1_P2P_SOURCE_RELAY_MARKER_STATUS_2026_06_04.md` (wiki/705). |
| 2026-06-04 v519 | Final release status now exposes the full web-input hardening gate surface. | `write-release-go-no-go.ps1` now runs and blocks on `audit-local-api-auth-contract.ps1` and `audit-operator-api-security-contract.ps1`, emitting `local_api_auth_contract_verified` and `operator_api_security_contract_verified` plus audit payloads. `show-final-release-handoff-status.ps1` now prints frontend polling, Rust background-loop, local API auth, and operator API security gates alongside process ownership and single-instance gates. The final operator packet now includes and verifies `audit-operator-api-security-contract.ps1`. Validation passed parser checks, local API auth audit `ok=true`/`fail_count=0`/`stale_doc_hit_count=0`, operator API security audit `ok=true`/`fail_count=0`, dirty-tree go/no-go and handoff status with all four hardening gates true, and `git diff --check`. This is status/packet hardening only; packaged runtime evidence remains current. Roadmap remains: `musu.pro` is remote input/project room/company meeting room/presence/rendezvous/path-selection/relay-fallback/evidence, while local MUSU programs execute work and prefer P2P mesh after web-assisted rendezvous. Public release remains No-Go on second-PC, hosted P2P, support mailbox, and Store gates. Canonical report: `docs\RELEASE_1_15_0_RC1_HARDENING_GATE_SURFACE_ALIGNMENT_2026_06_04.md` (wiki/706). |
| 2026-06-04 v520 | Secret storage is now a first-class web-input release gate. | Added `audit-secret-storage-contract.ps1` with schema `musu.secret_storage_contract.v1` and wired it into go/no-go, final handoff status, final operator packet inclusion, packet verification, and desktop release readiness inventory. The audit verifies bridge-token CSPRNG/permissions/domain-qualified Windows ACL warnings, runner bridge-token ACLs, cloud account-token `~\.musu\token` Unix `0600` and Windows `icacls` restriction, server-only bridge-token loading, raw account-token redaction in P2P token-hash helpers, secret-safe hosted P2P env setup, runtime evidence command redaction, and production docs that exclude token-bearing files from ordinary backups. `musu-rs\src\cloud\token.rs` now restricts saved account-token files on Windows, install token ACL helpers now prefer `USERDOMAIN\USERNAME` when available, and `docs\PRODUCTION.md` now backs up only non-secret config by default. Validation passed parser checks, secret-storage audit `ok=true`/`fail_count=0`, targeted Rust token test `1/1`, `cargo fmt --check`, and `git diff --check` with only the existing `docs/PRODUCTION.md` CRLF normalization warning. This is Rust runtime source, so current packaged primary evidence and operator packets are stale until regenerated from this commit. Roadmap remains: `musu.pro` coordinates remote input/rooms/rendezvous/path-selection/evidence; local MUSU programs execute work and prefer P2P mesh after web-assisted rendezvous. Public release remains No-Go on second-PC, hosted P2P, support mailbox, and Store gates. Canonical report: `docs\RELEASE_1_15_0_RC1_SECRET_STORAGE_CONTRACT_HARDENING_2026_06_04.md` (wiki/707). |
| 2026-06-04 v521 | Fresh primary-machine packaged evidence was restored after secret storage hardening. | Rebuilt/reinstalled the local-sideload MSIX for commit `26294fa2` with `run-msix-workflow.ps1`; release runtime build, Tauri desktop build, MSIX packing/signing, packaged startup smoke, install, and installed package contract verification passed. HUGH_SECOND still has warning-mode `.cargo\bin\musu.exe` PATH shadowing, so packaged checks used the explicit WindowsApps alias. Single-machine smoke `docs\evidence\single-machine\1.15.0-rc.1\20260604-232809-HUGH_SECOND.evidence.json` passed with dashboard `http://127.0.0.1:3001`, bridge `http://127.0.0.1:1823`, task `8877c18b-866a-405c-9f61-a097cc5d0301`, output `MUSU_RELEASE_SMOKE_OK_20260604_232737`, and CLI route checked. Desktop-open CPU `docs\evidence\runtime-idle-cpu\1.15.0-rc.1\20260604-233024-HUGH_SECOND.desktop-open.evidence.json` passed for `60.063s` with MUSU `0.05`, Node `0.03`, WebView2 `0.6`, owned WebView2 `6`, working set `487.21MB`, and hot `0`. Five-state matrix `docs\evidence\runtime-cpu-scenarios\1.15.0-rc.1\20260604-233135-HUGH_SECOND.runtime-cpu-scenario-matrix.json` passed with route token `MUSU_CPU_SCENARIO_ROUTE_OK_20260604_233135`, max role CPU MUSU `0`, Node `0.03`, WebView2 `0.39`, max working set `490.08MB`, and route probe ok. Go/no-go sees primary runtime CPU/matrix valid on `HUGH_SECOND`; public release remains No-Go on second-PC, hosted P2P, support mailbox, and Store gates. Canonical report: `docs\RELEASE_1_15_0_RC1_POST_SECRET_STORAGE_CONTRACT_PRIMARY_EVIDENCE_REFRESH_2026_06_04.md` (wiki/708). |
| 2026-06-04 v522 | Relay connect fail-closed status is now behind P2P control auth. | `/api/v1/relay/connect` now calls `authorizeP2pControl(req)` for both `GET` and `POST` before returning its fail-closed relay status/preflight response. Route tests now prove missing bearer token returns `401 unauthorized`, while authenticated `GET`/`POST` still return fail-closed `501` and refuse payload transit while the endpoint is unwired. `audit-operator-api-security-contract.ps1` now audits the relay connect auth contract so the existing go/no-go security gate catches regressions. Validation passed PowerShell parser check, operator API security audit `ok=true`/`fail_count=0`, `npm run test:p2p` `77/77`, `npm run typecheck`, and `git diff --check`. This is P2P relay security hardening, not relay transport completion: `RELAY_CONNECT_ENDPOINT_IMPLEMENTED=false`, `RELAY_PAYLOAD_ENDPOINT_IMPLEMENTED=false`, `relay_transport_wired=false`, and `relay_default_data_path=false` remain intentional. This is web runtime source, so packaged primary evidence is stale until rebuilt/refreshed after commit. Public release remains No-Go on second-PC, hosted P2P, support mailbox, and Store gates. Canonical report: `docs\RELEASE_1_15_0_RC1_RELAY_CONNECT_AUTH_HARDENING_2026_06_04.md` (wiki/709). |
| 2026-06-05 v523 | Fresh primary-machine packaged evidence was restored after relay connect auth hardening, and the local/web roadmap boundary was restated. | Rebuilt/reinstalled the local-sideload MSIX for commit `68cc6f27407c68f1e0aac6615e21f86d19495568`; release runtime build, Tauri desktop build, MSIX packing/signing, packaged startup smoke, install, and installed package contract verification passed. HUGH_SECOND still has warning-mode `.cargo\bin\musu.exe` PATH shadowing, so packaged checks used the installed package/WindowsApps path. Single-machine smoke `docs\evidence\single-machine\1.15.0-rc.1\20260605-000624-HUGH_SECOND.evidence.json` passed with dashboard `http://127.0.0.1:3001`, bridge `http://127.0.0.1:13587`, task `25e07b77-7376-43f6-b122-c8aec6bcd23c`, output `MUSU_RELEASE_SMOKE_OK_20260605_000551`, and CLI route checked. Desktop-open CPU `docs\evidence\runtime-idle-cpu\1.15.0-rc.1\20260605-000707-HUGH_SECOND.desktop-open.evidence.json` passed for `60.054s` with MUSU `0`, Node `0.05`, WebView2 `0.52`, owned WebView2 `6`, working set `497.9MB`, and hot `0`. Five-state matrix `docs\evidence\runtime-cpu-scenarios\1.15.0-rc.1\20260605-000820-HUGH_SECOND.runtime-cpu-scenario-matrix.json` passed verifier `ok=true`, `fail_count=0`, route token `MUSU_CPU_SCENARIO_ROUTE_OK_20260605_000820`, and route probe ok; max scenario CPU was MUSU `0.29`, Node `0.05`, WebView2 `0.52`, max working set `500.49MB`, hot `0`. Handoff status now sees primary single-machine true, runtime idle CPU `1/2 [HUGH_SECOND]`, and runtime CPU matrix `1/2 [HUGH_SECOND]`. Roadmap remains explicit: `musu.pro` is the remote order/project-room/company-room/presence/rendezvous/path-selection/relay-fallback/evidence control plane, while each local MUSU program executes work on its own device and prefers P2P mesh after web-assisted rendezvous. Current testing is still one-machine only; another PC must install the current build before multi-device gates can close. Public release remains No-Go on second-PC, hosted P2P, support mailbox, and Store gates. Canonical report: `docs\RELEASE_1_15_0_RC1_POST_RELAY_CONNECT_AUTH_PRIMARY_EVIDENCE_REFRESH_2026_06_05.md` (wiki/710). |
| 2026-06-05 v524 | MUSU.PRO room work-order input is now P2P-control-auth gated before it can reach a local bridge. | `POST /api/rooms/[roomId]/work-orders` now calls `authorizeP2pControl(req)` before parsing or forwarding a room instruction to `/api/tasks/delegate`, so missing bearer auth returns `401 unauthorized` and the local bridge is not called. The route response records `owner_scoped=true`, and work-order context values for channel, sender, target node, and adapter type now use bounded normalization. `audit-operator-api-security-contract.ps1` now audits this web-input route and its auth regression test. Validation passed `npm run test:routes` `19/19`, operator API security audit `ok=true`/`fail_count=0`, `npm run typecheck`, `npm run test:p2p` `77/77`, `npm run build`, and `git diff --check`. This is a key local-program/web-input security hardening: `musu.pro` can accept authenticated remote work orders, but unauthenticated web input cannot command a local executor through the bridge token. This changes web runtime source, so packaged primary evidence is stale until rebuilt/refreshed after commit. Public release remains No-Go on second-PC, hosted P2P, support mailbox, and Store gates. Canonical report: `docs\RELEASE_1_15_0_RC1_ROOM_WORK_ORDER_AUTH_HARDENING_2026_06_05.md` (wiki/711). |
| 2026-06-05 v525 | Fresh primary-machine packaged evidence was restored after room work-order auth hardening, keeping the local/web roadmap current. | Rebuilt/reinstalled the local-sideload MSIX for commit `aa52b243cb6b1b8350f060516e72c26d730da059`; release runtime build, Tauri desktop build, MSIX packing/signing, packaged startup smoke, install, and installed package contract verification passed. HUGH_SECOND still has warning-mode `.cargo\bin\musu.exe` PATH shadowing, so packaged checks used the installed WindowsApps package path. Single-machine smoke `docs\evidence\single-machine\1.15.0-rc.1\20260605-004553-HUGH_SECOND.evidence.json` passed with dashboard `http://127.0.0.1:3001`, bridge `http://127.0.0.1:2877`, task `83f8f35a-4c69-43f1-aae0-f8a9c8e12636`, output `MUSU_RELEASE_SMOKE_OK_20260605_004448`, and CLI route output containing `MUSU_CLI_ROUTE_OK_20260605_004448`. Desktop-open CPU `docs\evidence\runtime-idle-cpu\1.15.0-rc.1\20260605-004657-HUGH_SECOND.desktop-open.evidence.json` passed for `60.057s` with MUSU `0`, Node `0`, WebView2 `0.39`, owned WebView2 `6`, working set `489.86MB`, and hot `0`. Five-state matrix `docs\evidence\runtime-cpu-scenarios\1.15.0-rc.1\20260605-004808-HUGH_SECOND.runtime-cpu-scenario-matrix.json` passed verifier `ok=true`, `fail_count=0`, route token `MUSU_CPU_SCENARIO_ROUTE_OK_20260605_004808`, and route probe ok; max scenario CPU was MUSU `0.57`, Node `0.08`, WebView2 `0.65`, max working set `492.2MB`, hot `0`. Roadmap remains explicit: `musu.pro` is remote input/project-room/company-room/presence/rendezvous/path-selection/relay-fallback/evidence, while each local MUSU program executes work on its own device and prefers P2P mesh after web-assisted rendezvous. Current testing is still one-machine only; another PC must install this current build before multi-device gates can close. Public release remains No-Go on second-PC, hosted P2P, support mailbox, and Store gates. Canonical report: `docs\RELEASE_1_15_0_RC1_POST_ROOM_WORK_ORDER_AUTH_PRIMARY_EVIDENCE_REFRESH_2026_06_05.md` (wiki/712). |
| 2026-06-05 v526 | MUSU.PRO rendezvous sessions and route-candidate cache are now owner-scoped. | `StoredP2pRendezvousSession` now stores `owner_key`; create/read/update/approve/close/candidate routes derive `p2pControlPrincipal(req).owner_key` and pass it into the rendezvous store; cross-owner reads/mutations return not found; node candidate cache keys now include owner and node id for file-store and KV paths; room rendezvous and room presence use the same owner-scoped cache. `audit-operator-api-security-contract.ps1` now checks the source contract and regression tests. Validation passed `npm run test:p2p` `79/79`, `npm run test:routes` `19/19`, `npm run typecheck`, `npm run build`, operator API security audit `ok=true`/`fail_count=0`, and `git diff --check`. This updates the roadmap: `musu.pro` remains the remote input/project room/company meeting room/presence/rendezvous/path-selection/relay-fallback/evidence plane, while local MUSU programs execute work and all control-plane state is owner-scoped. This is web runtime source, so packaged primary evidence is stale until rebuilt/refreshed after commit. Public release remains No-Go on second-PC, hosted P2P, support mailbox, and Store gates. Canonical report: `docs\RELEASE_1_15_0_RC1_RENDEZVOUS_OWNER_SCOPE_HARDENING_2026_06_05.md` (wiki/713). |
| 2026-06-05 v527 | Fresh primary-machine evidence was restored after rendezvous owner-scope hardening, and the MUSU.PRO control-plane roadmap was updated. | Rebuilt/reinstalled the local-sideload MSIX after source commit `164072656ad60766cadad267d8aa1c5476618c9d`; release runtime build, Tauri desktop shell build, MSIX packing/signing, packaged startup smoke, install, installed package contract, and packaged runtime identity checks passed. Installed package is `Yellowhama.MUSU_1.15.0.0_x64__ygcjq669as2b6`; HUGH_SECOND still has warning-mode `.cargo\bin\musu.exe` PATH shadowing, so packaged checks used the WindowsApps/installed package path. Single-machine smoke `docs\evidence\single-machine\1.15.0-rc.1\20260605-014639-HUGH_SECOND.evidence.json` passed with dashboard `http://127.0.0.1:3001`, bridge `http://127.0.0.1:13047`, task `da44c52d-6b0c-4c97-8003-176a717c0646`, output `MUSU_RELEASE_SMOKE_OK_20260605_014606`, and CLI route checked. Desktop-open CPU `docs\evidence\runtime-idle-cpu\1.15.0-rc.1\20260605-014927-HUGH_SECOND.desktop-open.evidence.json` passed for `60.066s` with MUSU `0.21`, Node `0.03`, WebView2 `0.34`, owned WebView2 `6`, working set `450.8MB`, and hot `0`. Five-state matrix `docs\evidence\runtime-cpu-scenarios\1.15.0-rc.1\20260605-015132-HUGH_SECOND.runtime-cpu-scenario-matrix.json` passed verifier `ok=true`, `fail_count=0`, route token `MUSU_CPU_SCENARIO_ROUTE_OK_20260605_015132`, max scenario CPU MUSU `0.47`, Node `0.13`, WebView2 `0.18`, max working set `454.36MB`, and hot `0`. Direct go/no-go reported `local_artifacts_ready=true`, `single_machine_verified=true`, `msix_install_verified=true`, `msix_desktop_entrypoint_verified=true`, `public_metadata_ok=true`, and `ready_for_public_desktop_release=false`. Roadmap remains explicit: `musu.pro` is the remote web-input/project-room/company-meeting-room/presence/rendezvous/path-selection/relay-fallback/evidence control plane; local MUSU programs execute work on each device after authenticated web input; web-assisted rendezvous bootstraps P2P mesh; relay is fallback-only. Current testing is still one-machine only; another PC must install this current build before multi-device gates can close. Public release remains No-Go on second-PC, hosted P2P, support mailbox, and Store gates. Canonical report: `docs\RELEASE_1_15_0_RC1_POST_RENDEZVOUS_OWNER_SCOPE_PRIMARY_EVIDENCE_REFRESH_2026_06_05.md` (wiki/714). |
| 2026-06-05 v528 | Relay payload delivery acknowledgement now returns canonical delivery proof. | `p2pRelayPayloadStore.ts` now exports `RelayPayloadDeliveryProof` and `relayPayloadDeliveryProofFromDeliveredPayload`, and `PATCH /api/v1/p2p/relay/payload` with `musu.relay_payload_delivery.v1` returns `delivery_proof` alongside the sanitized delivered payload. Rust `P2pRelayPayloadDeliveryResponse` accepts optional `delivery_proof`; target-side relay payload drain prefers the API-provided proof and falls back to deriving it from the delivered payload for older servers; `musu relay payload-deliver --json` now includes the proof. Validation passed `npm run test:p2p` `79/79`, `npm run typecheck`, `npm run build`, `cargo test --lib relay_payload` `24/24`, `cargo check --bin musu`, `cargo fmt --check`, Rust background-loop audit `ok=true`/`fail_count=0`/`unaudited_loop_hit_count=0`, `show-musu-pro-p2p-env-status.ps1 -Json`, and `git diff --check`. P2P status still correctly reports `RELAY_CONNECT_ENDPOINT_IMPLEMENTED=false`, `RELAY_PAYLOAD_ENDPOINT_IMPLEMENTED=false`, `RELAY_PAYLOAD_QUEUE_ENDPOINT_IMPLEMENTED=true`, missing KV/Upstash URL/token, relay transport not wired, relay route not proven, and live delivery proof missing. Direct go/no-go on this commit reports `ready_for_public_desktop_release=false`, `single_machine_verified=false`, `multi_device_verified=false`, and `manifest_git.dirty=false`. This is web/Rust runtime source, so packaged primary evidence is stale until rebuilt/refreshed after commit. This is not release-grade QUIC/TLS relay transport completion; public release remains No-Go on second-PC, hosted P2P, support mailbox, and Store gates. Canonical report: `docs\RELEASE_1_15_0_RC1_RELAY_PAYLOAD_DELIVERY_PROOF_RESPONSE_2026_06_05.md` (wiki/715). |
| 2026-06-05 v529 | Fresh primary-machine evidence was restored after relay payload delivery proof response hardening, and the MUSU.PRO/local-runtime roadmap was restated. | Rebuilt/reinstalled the local-sideload MSIX after source commit `bd36815a838aa7e5d76426ddf5e09b7da70d9b71`; release runtime build, Tauri desktop shell build, MSIX packing/signing, packaged startup smoke, sideload readiness, install/reinstall, installed package contract, and packaged runtime identity checks passed. Installed package is `Yellowhama.MUSU_1.15.0.0_x64__ygcjq669as2b6`; HUGH_SECOND still has warning-mode `.cargo\bin\musu.exe` PATH shadowing, so packaged checks used the installed package/WindowsApps path. Single-machine smoke `docs\evidence\single-machine\1.15.0-rc.1\20260605-025404-HUGH_SECOND.evidence.json` passed with dashboard `http://127.0.0.1:3001`, bridge `http://127.0.0.1:13047`, task `ca28ea07-d681-466a-9554-cd985a286dd3`, output `MUSU_RELEASE_SMOKE_OK_20260605_025339`, and CLI route checked. Desktop-open CPU `docs\evidence\runtime-idle-cpu\1.15.0-rc.1\20260605-025501-HUGH_SECOND.desktop-open.evidence.json` passed for `60.06s` with MUSU `0`, Node `0`, WebView2 `0.65`, owned WebView2 `6`, working set `432.91MB`, and hot `0`. Five-state matrix `docs\evidence\runtime-cpu-scenarios\1.15.0-rc.1\20260605-025643-HUGH_SECOND.runtime-cpu-scenario-matrix.json` passed verifier `ok=true`, `fail_count=0`, route token `MUSU_CPU_SCENARIO_ROUTE_OK_20260605_025643`, and route probe ok; max scenario CPU was MUSU `0.18`, Node `0.86`, WebView2 `0.16`, max working set `490.74MB`, hot `0`. Direct go/no-go at `2026-06-05T03:07:49+09:00` reported `local_artifacts_ready=true`, `single_machine_verified=true`, `multi_device_verified=false`, `msix_install_verified=true`, `msix_desktop_entrypoint_verified=true`, `public_metadata_ok=true`, `manifest_git.dirty=false`, and `ready_for_public_desktop_release=false`. Roadmap remains explicit: `musu.pro` is the remote web-input/project-room/company-meeting-room/presence/rendezvous/path-selection/relay-fallback/evidence control plane; local MUSU programs execute work on each device after authenticated web input; web-assisted rendezvous bootstraps P2P mesh; relay is fallback-only. Current testing is still one-machine only; another PC must install this current build before multi-device gates can close. Public release remains No-Go on second-PC, hosted P2P source marker/KV/evidence gaps, support mailbox, and Store gates. Canonical report: `docs\RELEASE_1_15_0_RC1_POST_RELAY_PAYLOAD_DELIVERY_PROOF_PRIMARY_EVIDENCE_REFRESH_2026_06_05.md` (wiki/716). |
| 2026-06-05 v530 | Release go/no-go now exposes the idle busy-loop candidate contract summary. | `write-release-go-no-go.ps1` now emits `idle_busy_loop_candidate_contract_verified` and `idle_busy_loop_candidate_status`, with blocker area `idle-busy-loop-candidates` if any candidate mapping fails. The summary maps the original idle CPU suspects to concrete audit checks: clipboard polling, mDNS discovery, health/readiness retry loops, frontend interval/refetch polling, relay payload target polling, and cloud heartbeat. Validation passed the PowerShell parser for `write-release-go-no-go.ps1`, `git diff --check`, Rust background-loop audit `ok=true`/`fail_count=0`/`unaudited_loop_hit_count=0`, frontend polling audit `ok=true`/`fail_count=0`/`direct_interval_hit_count=0`/`direct_visibility_listener_hit_count=0`, and dirty-tree go/no-go summary with `idle_busy_loop_candidate_contract_verified=true`, candidate count `6`, and no failed candidates. This is status/gate source only; it does not change packaged runtime behavior, replace 60-second CPU evidence, or close the two-machine idle CPU gate. Public release remains No-Go on second-PC, hosted P2P source marker/KV/evidence gaps, support mailbox, and Store gates. Canonical report: `docs\RELEASE_1_15_0_RC1_IDLE_BUSY_LOOP_CANDIDATE_STATUS_GATE_2026_06_05.md` (wiki/717). |
| 2026-06-05 v531 | Log/telemetry flush loops are now part of the idle busy-loop candidate gate. | `audit-rust-background-loop-contract.ps1` now reports `telemetry_flush_primitive_hit_count` and fails `logging-telemetry / no background telemetry flush worker primitives` if Rust source introduces common background telemetry/log flush worker primitives. `write-release-go-no-go.ps1` now adds the seventh `idle_busy_loop_candidate_status` candidate, `log/telemetry flush loop`, verified by the Rust source allowlist and telemetry primitive checks. Validation passed PowerShell parser checks for both scripts, Rust background-loop audit `ok=true`/`fail_count=0`/`unaudited_loop_hit_count=0`/`telemetry_flush_primitive_hit_count=0`, frontend polling audit `ok=true`/`fail_count=0`/`direct_interval_hit_count=0`/`direct_visibility_listener_hit_count=0`, and dirty-tree go/no-go summary with `idle_busy_loop_candidate_contract_verified=true`, candidate count `7`, and no failed candidates. This is source/gate hardening only; it does not change packaged runtime behavior, replace 60-second CPU evidence, or close the two-machine idle CPU gate. Public release remains No-Go on second-PC, hosted P2P source marker/KV/evidence gaps, support mailbox, and Store gates. Canonical report: `docs\RELEASE_1_15_0_RC1_LOG_TELEMETRY_IDLE_CANDIDATE_GATE_2026_06_05.md` (wiki/718). |
| 2026-06-05 v532 | P2P status now separates store-forward queue fallback from missing release tunnel endpoints. | `show-musu-pro-p2p-env-status.ps1` now reports `relay_payload_queue_fallback_implemented` plus component checks for the policy marker, owner-scoped web queue store/claim/deliver route, Rust enqueue-after-lease path, and Rust target drain/delivery-proof path. Local source status confirms the lease-bound store-forward queue fallback chain is wired (`policy_marker=true`, `web_queue_store_claim_deliver=true`, `rust_enqueue_after_lease=true`, `rust_target_drain_and_delivery_proof=true`) while release tunnel endpoints remain false. Generic source blockers were renamed to `source_release_relay_connect_endpoint_not_implemented` and `source_release_relay_payload_endpoint_not_implemented`, so the status no longer implies the queue fallback is absent. Validation passed the PowerShell parser and local source status summary with queue fallback `true`; public P2P release remains No-Go until production KV/Upstash, release-grade tunnel/connect payload transport proof, owner-scoped release relay route evidence, and per-record delivery proof are present. Canonical report: `docs\RELEASE_1_15_0_RC1_P2P_STORE_FORWARD_QUEUE_STATUS_ALIGNMENT_2026_06_05.md` (wiki/719). |
| 2026-06-05 v533 | P2P store-forward relay fallback now has a dedicated release contract gate, and the MUSU.PRO/local-runtime roadmap was updated. | Added `audit-p2p-store-forward-relay-contract.ps1` with schema `musu.p2p_store_forward_relay_contract.v1`, wired it into release go/no-go, desktop release readiness, final handoff status, and the single-machine/runtime-matrix verifier status-only freshness allowlists, and updated `P2P_CONTROL_PLANE.md` to restate the split: local MUSU programs execute work on each device; `musu.pro` handles remote web input, project/company rooms, presence, rendezvous, path selection, fallback lease policy, and evidence; web-assisted rendezvous bootstraps P2P mesh; relay remains fallback-only and non-default. Validation passed PowerShell parser checks for the new audit plus go/no-go/readiness/handoff/single-machine/matrix verifier scripts, the new audit reported `ok=true` and `fail_count=0`, clean go/no-go reported `p2p_store_forward_relay_contract_verified=true`, `p2p_control_plane_verified=false`, `p2p_relay_transport_wired=false`, `single_machine_verified=true`, `multi_device_verified=false`, `manifest_git.dirty=false`, and `ready_for_public_desktop_release=false`; final handoff no longer lists `p2p-store-forward-relay` as an operator step while still listing `p2p-control-plane`; P2P env status still reports `source.relay_payload_queue_fallback_implemented=true` with release tunnel, KV/Upstash, route evidence, and delivery proof blockers. This is source/gate and roadmap hardening only; public release remains No-Go until production KV/Upstash, release-grade relay tunnel proof, current-build second-PC evidence, support mailbox evidence, and Store evidence are complete. Canonical report: `docs\RELEASE_1_15_0_RC1_P2P_STORE_FORWARD_RELAY_CONTRACT_GATE_2026_06_05.md` (wiki/720). |
| 2026-06-05 v534 | Bridge readiness wait loop is now a separate idle busy-loop candidate gate. | `write-release-go-no-go.ps1` now splits the previous `health/readiness retry` candidate into `health check retry loop` and `bridge readiness wait loop`, so the idle busy-loop candidate summary matches the explicit CPU suspect list. The health candidate maps to auto-update health polling checks (`health poll initial backoff`, `health poll max backoff`, `health poll sleep`), while the bridge readiness candidate maps to CLI bridge health checks (`bridge health poll initial backoff`, `bridge health poll max backoff`, `bridge readiness deadline`, `bridge readiness backoff sleep`) in `audit-rust-background-loop-contract.ps1`. Validation passed the PowerShell parser, Rust background-loop audit `ok=true`/`fail_count=0`/`unaudited_loop_hit_count=0`, and dirty-tree go/no-go with `idle_busy_loop_candidate_contract_verified=true`, candidate count `8`, both new candidate names present, and no failed idle candidates. This is source/gate visibility only; it does not replace 60-second CPU evidence or close the two-machine idle CPU gate. Public release remains No-Go on second-PC CPU/matrix, multi-device route, hosted P2P, support mailbox, and Store evidence. Canonical report: `docs\RELEASE_1_15_0_RC1_BRIDGE_READINESS_IDLE_CANDIDATE_GATE_2026_06_05.md` (wiki/721). |
| 2026-06-05 v535 | Packaged local runtime identity is now enforced for localhost/process/startup release evidence. | `audit-musu-process-ownership.ps1` now records command lines plus packaged runtime identity, fails release mode when a MUSU runtime is not the packaged WindowsApps runtime, when the bridge registry PID points at debug/workspace runtime, or when port 3001 is served by a repo/workspace Next server; `-AllowDeveloperRuntime` is diagnostic-only. `audit-musu-startup-single-instance.ps1` now defaults to the WindowsApps `musu.exe` app execution alias and embeds the strict process ownership audit, while `write-release-go-no-go.ps1` rejects process/startup evidence that lacks packaged runtime identity proof. Current live HUGH_SECOND state correctly fails even though `http://127.0.0.1:3001/app` returns HTTP 200, because the bridge PID points at `musu-rs\target\debug\musu.exe` and the dashboard listener is workspace `next start -p 3001`. Roadmap restated: localhost is local loopback, not internet; `musu.pro` is remote input/project-room/rendezvous/control-plane, and installed local MUSU programs execute work on each device. Canonical report: `docs\RELEASE_1_15_0_RC1_PACKAGED_LOCAL_RUNTIME_IDENTITY_GATE_2026_06_05.md` (wiki/722). |
| 2026-06-05 v536 | Packaged local runtime repair now has an operator runbook and handoff step. | Added `scripts\windows\repair-packaged-local-runtime-state.ps1` with schema `musu.packaged_local_runtime_repair.v1`. The script records before/after process ownership evidence, runs exact WindowsApps `musu down --include-desktop`, optionally stops audit-identified repo/workspace orphan helpers with explicit `-StopRepoOrphanHelpers`, starts packaged `musu up --json`, and verifies process ownership again. Final handoff now adds a `packaged-local-runtime-state` step when process ownership or startup single-instance is blocked, and final packet/readiness/freshness allowlists include the script. Live validation on HUGH_SECOND: diagnostic repair without helper stop correctly failed after stopping debug bridge PID `42236`; release repair with `-StopRepoOrphanHelpers` stopped workspace Next PID `2812`, started packaged bridge PID `23860`, returned `ok=true`, and follow-up process ownership/startup audits both passed. `127.0.0.1:3001/app` is now connection-refused because the workspace dashboard was intentionally stopped; the packaged local runtime bridge is healthy at `127.0.0.1:7555`. Canonical report: `docs\RELEASE_1_15_0_RC1_PACKAGED_LOCAL_RUNTIME_REPAIR_RUNBOOK_2026_06_05.md` (wiki/723). |
| 2026-06-05 v537 | Runtime CPU scenario matrix evidence now requires packaged MUSU executable identity. | `measure-musu-runtime-cpu-scenarios.ps1` now defaults to the WindowsApps `musu.exe` alias, rejects repo/debug runtime paths unless `-AllowDeveloperRuntime` is explicitly supplied, and records `musu_exe`, `allow_developer_runtime`, and `musu_exe_release_identity`. `verify-runtime-cpu-scenario-matrix.ps1` now rejects release matrix evidence unless it proves a WindowsApps/installed `Yellowhama.MUSU_...` executable path, and the release verifier regression suite now includes a negative debug `musu-rs\target\debug\musu.exe` matrix fixture. Validation passed parser checks, `git diff --check`, a short diagnostic packaged runtime sample with `musu_exe=C:\Users\empty\AppData\Local\Microsoft\WindowsApps\musu.exe`, `musu_exe_release_identity=true`, bridge `http://127.0.0.1:7555`, max MUSU CPU `0`, and release evidence verifier regression `ok=true`/`case_count=30`/`failed_case_count=0`. Older CPU matrix evidence without packaged executable identity is no longer release-grade; a fresh clean 60-second packaged matrix is required after this commit. `127.0.0.1:3001/app` may remain connection-refused because the workspace dashboard is separate from the packaged local bridge. Public release remains No-Go on fresh packaged CPU/matrix, second-PC, hosted P2P, support mailbox, and Store evidence. Canonical report: `docs\RELEASE_1_15_0_RC1_RUNTIME_CPU_MATRIX_PACKAGED_EXE_IDENTITY_GATE_2026_06_05.md` (wiki/724). |
| 2026-06-05 v538 | Packaged runtime CPU matrix no longer depends on a repo/workspace dashboard URL, and fresh one-machine CPU evidence is restored. | `verify-runtime-cpu-scenario-matrix.ps1` now accepts `dashboard-open` when the matrix proves packaged MUSU executable identity, `musu up --json` attempted dashboard discovery, no dashboard URL was exposed, and the scenario measured packaged runtime state. This keeps `127.0.0.1:3001/app` out of packaged release CPU evidence while still rejecting debug executable matrices. Regression now includes `runtime matrix accepts packaged runtime without dashboard URL`, and the suite passed `ok=true`/`case_count=31`/`failed_case_count=0`. Fresh HUGH_SECOND evidence was captured after `7c3939e7`: matrix `docs\evidence\runtime-cpu-scenarios\1.15.0-rc.1\20260605-045524-HUGH_SECOND.runtime-cpu-scenario-matrix.json` passed with clean git, WindowsApps `musu.exe`, `musu_exe_release_identity=true`, route token `MUSU_CPU_SCENARIO_ROUTE_OK_20260605_045524`, route task `095647cf-83da-46eb-81ec-bd79a81402eb`, max role CPU MUSU `0.03`, Node `0`, WebView2 `1.07`; idle CPU `docs\evidence\runtime-idle-cpu\1.15.0-rc.1\20260605-050112-HUGH_SECOND.desktop-open.evidence.json` passed for `60.055s` with MUSU `0`, Node `0`, WebView2 `0.13`, hot `0`, and working set `360.32MB`. Dirty go/no-go saw runtime idle CPU `1/2 [HUGH_SECOND]`, runtime matrix `1/2 [HUGH_SECOND]`, process/startup/single-machine true. Public release remains No-Go on second-PC CPU/matrix, current-build multi-device, hosted P2P, support mailbox, and Store evidence. Canonical report: `docs\RELEASE_1_15_0_RC1_PACKAGED_RUNTIME_DASHBOARD_ABSENCE_CPU_GATE_2026_06_05.md` (wiki/725). |
| 2026-06-05 v539 | Release-grade relay route queries now revalidate payload delivery proof to exclude stale relay records. | `routeEvidenceStore.ts` now filters `release_grade=true` relay query results through current fallback, transport proof, and payload delivery proof shape checks, so an old stored relay record with `release_grade=true` and transport proof only can no longer appear as release-grade evidence. The route-evidence regression now seeds `stale-relay-transport-only-release-grade` and verifies it is excluded, and `audit-p2p-store-forward-relay-contract.ps1` now gates `release-grade query revalidates relay delivery proof`. Validation passed `npm run test:p2p` `79/79`, `npm run typecheck`, PowerShell parser for the audit, audit JSON `ok=true`/`fail_count=0`, and `git diff --check`. This is P2P evidence-chain hardening only; hosted P2P remains No-Go on release tunnel source markers, production KV/Upstash, live relay lease evidence, relay transport proof, relay route proof, and live payload delivery proof. Canonical report: `docs\RELEASE_1_15_0_RC1_RELAY_ROUTE_QUERY_DELIVERY_PROOF_HARDENING_2026_06_05.md` (wiki/726). |
| 2026-06-05 v540 | Packaged local runtime health no longer depends on workspace dashboard ports, aligning the local app / MUSU.PRO web split. | `musu up --json` and `musu doctor --json` now treat absent workspace dashboards as normal for `store-msix` packaged runtimes, returning `dashboard.status=ok`, `dashboard.required=false`, no dev/start URL fallback, and next steps that point to `MUSU.PRO` remote input or `musu route --wait`. Developer/direct-download mode keeps the dashboard readiness warning. `smoke-single-machine-beta.ps1` now defaults to the WindowsApps packaged `musu.exe`, rejects debug runtime unless `-AllowDeveloperRuntime`, records `musu_exe`, `allow_developer_runtime`, `dashboard_required`, and `single_machine_surface=local-bridge-only`, and skips dashboard API smoke when the package says dashboard is optional. `verify-single-machine-evidence.ps1` now requires packaged WindowsApps runtime identity and accepts bridge-only packaged evidence. Validation passed PowerShell parser checks, `cargo test -q --bin musu install::cli_commands` `28/28`, release evidence verifier regression `ok=true`/`case_count=32`, `git diff --check`, MSIX rebuild/install/packaged runtime identity verification, and actual WindowsApps `musu up --json` with bridge `http://127.0.0.1:3591` and `dashboard.required=false`. Root cause of `127.0.0.1:3001` confusion: that port is an optional workspace dashboard, not the packaged local program. Fresh current-commit single-machine/CPU/matrix evidence is still required after this commit. Canonical report: `docs\RELEASE_1_15_0_RC1_PACKAGED_LOCAL_RUNTIME_WEB_SPLIT_ALIGNMENT_2026_06_05.md` (wiki/727). |
| 2026-06-05 v541 | Fresh primary-machine evidence now proves the packaged local runtime / MUSU.PRO web split without relying on `localhost:3001`. | Current HUGH_SECOND single-machine smoke `docs\evidence\single-machine\1.15.0-rc.1\20260605-060842-HUGH_SECOND.evidence.json` passed with WindowsApps `musu.exe`, `dashboard_required=false`, `dashboard_base_url_source=bridge-only-packaged-runtime`, `single_machine_surface=local-bridge-only`, bridge `http://127.0.0.1:3591`, and CLI route checked. Desktop-open CPU `docs\evidence\runtime-idle-cpu\1.15.0-rc.1\20260605-061127-HUGH_SECOND.desktop-open.evidence.json` passed for `60.054s` with MUSU `0`, Node `0`, WebView2 `0.6`, owned WebView2 `6`, working set `362.17MB`, and hot `0`. Five-state runtime CPU matrix `docs\evidence\runtime-cpu-scenarios\1.15.0-rc.1\20260605-061306-HUGH_SECOND.runtime-cpu-scenario-matrix.json` passed with packaged executable identity, `allow_developer_runtime=false`, route task `75d47304-7992-4741-b85f-27b315e8dbdc`, route token `MUSU_CPU_SCENARIO_ROUTE_OK_20260605_061306`, max scenario CPU MUSU `0.03`, Node `0`, WebView2 `0.34`, max working set `366.44MB`, and hot `0`. Process ownership and startup single-instance audits passed on commit `7d3e28ea`, reusing bridge PID `39052`. Direct go/no-go reported local artifacts, single-machine, MSIX install, desktop entrypoint, public metadata, and clean manifest OK, while public release remains No-Go on second-PC, hosted P2P release proof, support mailbox, and Store evidence. Canonical report: `docs\RELEASE_1_15_0_RC1_POST_PACKAGED_LOCAL_RUNTIME_WEB_SPLIT_PRIMARY_EVIDENCE_REFRESH_2026_06_05.md` (wiki/728). |
| 2026-06-05 v542 | Second-PC handoff now matches the packaged local runtime / MUSU.PRO web split and no longer treats `localhost:3001` as required. | Commits `1813cc2a` and `034e3639` updated `prepare-operator-action-pack.ps1` plus the second-PC MSIX runbook so the operator quickstart says the installed MUSU package is the local executor, a `localhost:3001` workspace dashboard is optional, and the second-PC check uses WindowsApps `musu.exe` bridge-only evidence when `dashboard.required=false`. Clean commit `034e363988da7f25ea38f6606298d8e232245166` regenerated final packet `.local-build\final-operator-gates\musu-final-operator-gates-1.15.0-rc.1-20260605-064050.zip`, action pack `.local-build\operator-action-pack\MUSU-1.15.0-rc.1-operator-action-pack-20260605-064106.zip`, second-PC transfer `.local-build\operator-action-pack\MUSU-1.15.0-rc.1-operator-action-pack-20260605-064106\second-pc\MUSU-second-PC-transfer-1.15.0-rc.1-20260605-064106.zip`, and Partner Center zip `.local-build\operator-action-pack\MUSU-1.15.0-rc.1-operator-action-pack-20260605-064106\partner-center\MUSU-1.15.0-rc.1-store-submission-20260605-064106.zip`. Final packet verification passed with `ok=true`, `fail_count=0`, `kit_count=1`; action-pack verification passed with `ok=true`, `fail_count=0`; extracted quickstart checks confirmed `has_runtime_boundary=true`, `says_localhost_optional=true`, `says_dashboard_not_required=true`, and `says_bridge_only=true`. Final handoff status remains `ready_for_public_desktop_release=false` with packet/action pack verified, single-machine true, runtime idle CPU `1/2`, runtime matrix `1/2`, multi-device false, P2P false, and blockers `multi-device`, `runtime-idle-cpu`, `runtime-cpu-scenario-matrix`, `support-mailbox`, `store-release`, and `p2p-control-plane`. Second PC `192.168.1.192:8949` is unreachable (`TcpTestSucceeded=false`, ping timeout). P2P status remains No-Go: queue fallback implemented, but release tunnel endpoints, production KV/Upstash config, live relay route proof, and live payload delivery proof are missing. Canonical report: `docs\RELEASE_1_15_0_RC1_SECOND_PC_HANDOFF_PACKAGED_RUNTIME_ALIGNMENT_2026_06_05.md` (wiki/729). |
| 2026-06-05 v543 | Frontend polling coverage and fresh packaged local-runtime evidence now reinforce that `localhost:3001` is optional, not the MUSU app. | Commit `2517ffab` made `useBoundedEventSource` visibility reconnect abort-aware and expanded `audit-frontend-polling-contract.ps1` to inventory all non-test `useLowDutyPolling` call sites across `musu-bee/src` and `musu-bee/views`; validation passed with `low_duty_polling_call_site_count=29`, signal gaps `0`, direct intervals `0`, direct visibility listeners `0`, `npm run test:runtime-polling` `17/17`, `npm run typecheck`, parser checks, and `git diff --check`. Rebuilt/reinstalled the local-sideload MSIX `.local-build\msix\output\musu_1.15.0.0_x64_local-sideload-manual.msix`, recorded strict MSIX evidence `docs\evidence\msix-install\1.15.0-rc.1\20260605-070256-HUGH_SECOND.evidence.json`, and bridge-only single-machine evidence `docs\evidence\single-machine\1.15.0-rc.1\20260605-065900-HUGH_SECOND.evidence.json` with `dashboard_required=false`, `single_machine_surface=local-bridge-only`, and bridge `http://127.0.0.1:8370`. Desktop-open idle CPU `docs\evidence\runtime-idle-cpu\1.15.0-rc.1\20260605-070404-HUGH_SECOND.desktop-open.evidence.json` passed for `60.046s` with MUSU `0`, Node `0`, WebView2 `0.26`, working set `364.9MB`, and hot `0`. Runtime CPU matrix `docs\evidence\runtime-cpu-scenarios\1.15.0-rc.1\20260605-070552-HUGH_SECOND.runtime-cpu-scenario-matrix.json` passed verifier `ok=true`, `fail_count=0`, route token `MUSU_CPU_SCENARIO_ROUTE_OK_20260605_070552`, WindowsApps runtime identity, no developer runtime, and max WebView2 CPU `0.21`; `dashboard-open` measured packaged runtime state because no dashboard URL was exposed, so it did not depend on `localhost:3001`. Clean final handoff after commit `ff8fdf46` reports packet/action-pack verified, local artifacts true, single-machine true, MSIX true, frontend polling true, runtime idle CPU `1/2 [HUGH_SECOND]`, runtime matrix `1/2 [HUGH_SECOND]`, `manifest_git_dirty=false`, and public release still No-Go on second-PC, hosted P2P release proof, support mailbox, and Store gates. Canonical report: `docs\RELEASE_1_15_0_RC1_FRONTEND_POLLING_LOCAL_RUNTIME_EVIDENCE_REFRESH_2026_06_05.md` (wiki/730). |
| 2026-06-05 v544 | Relay route transport proof is now bound to fallback lease/session, and current packaged local-runtime evidence is refreshed. | Commit `9d1d9666` binds `hasCurrentRelayTransportProof` to the relay fallback `lease_id` and route `session_id`, extends route-evidence regression with lease/session mismatch stale records, and adds the audit check `release-grade query binds relay transport proof to fallback lease`; validation passed `npm run test:p2p` `79/79`, `npm run typecheck`, P2P store-forward relay contract audit `ok=true`/`fail_count=0`, parser checks, and `git diff --check`. Because source changed, strict MSIX and single-machine evidence were refreshed and committed in `cf92a3f2`: `docs\evidence\msix-install\1.15.0-rc.1\20260605-072911-HUGH_SECOND.evidence.json` and `docs\evidence\single-machine\1.15.0-rc.1\20260605-073044-HUGH_SECOND.evidence.json` with WindowsApps `musu.exe`, `dashboard_required=false`, `single_machine_surface=local-bridge-only`, and bridge `http://127.0.0.1:8186`. Runtime CPU evidence was refreshed and committed in `9b331698`: idle CPU `docs\evidence\runtime-idle-cpu\1.15.0-rc.1\20260605-074243-HUGH_SECOND.desktop-open.evidence.json` passed clean git for `60.061s` with MUSU `0`, Node `0`, WebView2 `0.03`, working set `368.98MB`, and hot `0`; matrix `docs\evidence\runtime-cpu-scenarios\1.15.0-rc.1\20260605-074400-HUGH_SECOND.runtime-cpu-scenario-matrix.json` passed verifier `ok=true`/`fail_count=0`, route token `MUSU_CPU_SCENARIO_ROUTE_OK_20260605_074400`, max MUSU `0.03`, Node `0`, WebView2 `0.16`, and `dashboard-open` measured packaged runtime state without `localhost:3001`. Clean final handoff reports single-machine/MSIX/frontend/process/startup/secret/P2P store-forward contract gates true, runtime idle CPU `1/2 [HUGH_SECOND]`, runtime matrix `1/2 [HUGH_SECOND]`, `manifest_git_dirty=false`, and public release still No-Go on second-PC, hosted `musu.pro` P2P release proof, support mailbox, and Store gates. P2P env status still reports queue fallback implemented but release connect/payload endpoints false, missing KV/Upstash, live relay route proof count `0`, and payload delivery proof missing. Canonical report: `docs\RELEASE_1_15_0_RC1_RELAY_ROUTE_PROOF_LINKAGE_HARDENING_2026_06_05.md` (wiki/731). |
| 2026-06-05 v545 | Rust background-loop audit now explicitly covers WebRTC RTCP request loops and non-blocking telemetry/log flush primitives. | Commit `cf722a15` expands `audit-rust-background-loop-contract.ps1` with WebRTC checks `rtcp reader request-scoped spawn`, `rtcp reader awaits inbound packets`, and `rtcp reader exits on read failure`, proving the RTCP reader in `musu-rs\src\io\webrtc.rs` is request-scoped, awaits `rtp_sender.read(...).await`, and exits on stream close/error. The telemetry/log flush primitive scanner now also rejects `non_blocking`, so a future background log appender cannot enter release source without failing the Rust background-loop contract. Validation passed PowerShell parser, Rust background-loop audit `ok=true`/`fail_count=0`/`unaudited_loop_hit_count=0`/`telemetry_flush_primitive_hit_count=0`/`check_count=118`/WebRTC checks `7`, dirty-tree go/no-go showed `rust_background_loop_contract_verified=true`, `idle_busy_loop_candidate_contract_verified=true`, and `log/telemetry flush loop` verified, and clean final handoff after commit reports `rust_background_loop_contract_verified=true`, runtime idle CPU `1/2 [HUGH_SECOND]`, runtime matrix `1/2 [HUGH_SECOND]`, `manifest_git_dirty=false`, and public release still No-Go on second-PC, hosted P2P, support mailbox, and Store gates. This is source-contract coverage hardening only; it does not replace two-machine CPU evidence. Canonical report: `docs\RELEASE_1_15_0_RC1_RUST_LOOP_AUDIT_WEBRTC_TELEMETRY_COVERAGE_2026_06_05.md` (wiki/732). |
| 2026-06-05 v546 | P2P candidate endpoint metadata now survives MUSU.PRO room presence and rendezvous seeding. | `P2pCandidateEndpoint` now preserves public endpoint, NAT type/observer, relay URL, and relay protocol metadata, with shared normalization in `p2pRendezvousStore.ts`. Room presence now reuses the shared candidate normalizer, and both `POST /api/v1/p2p/rendezvous/[id]/candidates` and `POST /api/rooms/[roomId]/presence` accept the same metadata. Regression coverage proves direct-QUIC public/NAT metadata and relay descriptors survive candidate update, room presence cache seeding, and room-scoped rendezvous creation. `audit-p2p-store-forward-relay-contract.ps1` now gates `candidate endpoint metadata is preserved through web control plane`. Validation passed `npm run test:p2p` `79/79`, `npm run typecheck`, P2P store-forward relay contract audit `ok=true`/`fail_count=0`, and `git diff --check`. Root cause for the reported browser error remains: `127.0.0.1:3001/app` is an optional workspace dashboard and was not listening, while the installed local bridge returned `200 OK` at `127.0.0.1:8186/health`; this change keeps the local MUSU program / MUSU.PRO web control-plane split intact. This is metadata/control-plane hardening only and does not close second-PC, hosted P2P release proof, support mailbox, or Store gates. Canonical report: `docs\RELEASE_1_15_0_RC1_P2P_CANDIDATE_ENDPOINT_METADATA_PRESERVATION_2026_06_05.md` (wiki/733). |
| 2026-06-05 v547 | Fresh primary-machine packaged runtime evidence was restored after P2P candidate metadata hardening. | After `9be40bc4`, the local-sideload MSIX was rebuilt/reinstalled and HUGH_SECOND evidence was refreshed for the current source. Single-machine smoke `docs\evidence\single-machine\1.15.0-rc.1\20260605-082350-HUGH_SECOND.evidence.json` passed with `dashboard_required=false`, `single_machine_surface=local-bridge-only`, bridge `http://127.0.0.1:10518`, and CLI route checked. Desktop-open idle CPU `docs\evidence\runtime-idle-cpu\1.15.0-rc.1\20260605-082546-HUGH_SECOND.desktop-open.evidence.json` passed for `60.058s` with MUSU `0.05`, Node `0`, WebView2 `0.73`, working set `365.65MB`, and hot `0`. Runtime CPU matrix `docs\evidence\runtime-cpu-scenarios\1.15.0-rc.1\20260605-082656-HUGH_SECOND.runtime-cpu-scenario-matrix.json` passed verifier `ok=true`/`fail_count=0`, route token `MUSU_CPU_SCENARIO_ROUTE_OK_20260605_082656`, route task `3b2b0137-e3ee-4548-ad45-cb33228d89a9`, max role CPU MUSU `0.03`, Node `0`, WebView2 `0.39`, max working set `367.52MB`, and `dashboard-open` measured packaged runtime state because no dashboard URL was exposed. Strict MSIX evidence capture remains blocked by local PATH alias shadowing (`C:\Users\empty\.cargo\bin\musu.exe`), so only WindowsApps-explicit warn evidence was kept in `.local-build`; do not promote it as strict release evidence until PATH is fixed. Handoff after the refresh removed the single-machine blocker but remains No-Go on multi-device, second-machine CPU/matrix, support mailbox, Store/Microsoft, hosted P2P, and dirty git until this evidence commit lands. Canonical report: `docs\RELEASE_1_15_0_RC1_POST_P2P_CANDIDATE_ENDPOINT_METADATA_PRIMARY_EVIDENCE_REFRESH_2026_06_05.md` (wiki/734). |
| 2026-06-05 v548 | Local room presence publishing now emits public/NAT/relay candidate metadata for MUSU.PRO rooms. | Rust cloud DTOs now expose `NatType`, `RelayProtocol`, and optional candidate fields `public_addr`, `nat_type`, `nat_observed_by`, `relay_url`, and `relay_protocol`, matching the web control-plane contract from wiki/733. `musu room presence publish` now accepts repeated `--candidate-url`, `--nat-type`, `--nat-observed-by`, `--relay-url`, and `--relay-protocol`, builds multiple route candidates, and reports both the first `candidate` and full `candidates` list in JSON. The default remains local-executor first: default local bridge candidate publishing still works, extra public/NAT metadata only attaches to direct/public candidates, and relay descriptors are fallback metadata, not default execution through `musu.pro`. Validation passed `cargo check --manifest-path .\musu-rs\Cargo.toml --bin musu`, Rust room-presence tests `6/6`, Rust rendezvous tests `5/5`, `npm run test:p2p` `79/79`, `npm run typecheck`, P2P store-forward relay contract audit `ok=true`/`fail_count=0`, and `git diff --check`. Because Rust source changed after the prior packaged evidence refresh, fresh MSIX/single-machine/CPU/matrix evidence is required before this change can be treated as current packaged release evidence. Canonical report: `docs\RELEASE_1_15_0_RC1_ROOM_PRESENCE_CANDIDATE_METADATA_CLIENT_CLI_2026_06_05.md` (wiki/735). |
| 2026-06-05 v549 | Fresh primary-machine packaged local-runtime evidence was restored after room presence CLI metadata hardening. | After `b6329f0d`, the release MSIX was rebuilt/reinstalled for local sideload and packaged-state verification passed with WindowsApps alias invocation available. Strict MSIX install evidence capture still failed because PATH resolves `C:\Users\empty\.cargo\bin\musu.exe` before the WindowsApps alias, so no new strict MSIX evidence was promoted. Packaged runtime repair passed at bridge `http://127.0.0.1:10325` with `dashboard.required=false`. Current single-machine evidence `docs\evidence\single-machine\1.15.0-rc.1\20260605-092924-HUGH_SECOND.evidence.json` passed with `single_machine_surface=local-bridge-only` and CLI route checked. Desktop-open idle CPU `docs\evidence\runtime-idle-cpu\1.15.0-rc.1\20260605-093206-HUGH_SECOND.desktop-open.evidence.json` passed for `60.053s` with MUSU `0`, Node `0`, WebView2 `0.44`, working set `364.26MB`, and hot `0`. Runtime CPU matrix `docs\evidence\runtime-cpu-scenarios\1.15.0-rc.1\20260605-094033-HUGH_SECOND.runtime-cpu-scenario-matrix.json` passed verifier `ok=true`/`fail_count=0`, route token `MUSU_CPU_SCENARIO_ROUTE_OK_20260605_094033`, route task `6b7e1ccc-97c1-466f-9354-fedd3ef3583d`, max role CPU MUSU `0.05`, Node `0`, WebView2 `0.16`, max working set `366.33MB`, and `dashboard-open` measured packaged runtime state without relying on `localhost:3001`. This restores current one-machine evidence only; public release remains blocked on second-PC multi-device/CPU/matrix, hosted `musu.pro` P2P release proof, support mailbox, and Store evidence. Canonical report: `docs\RELEASE_1_15_0_RC1_POST_ROOM_PRESENCE_CANDIDATE_METADATA_PRIMARY_EVIDENCE_REFRESH_2026_06_05.md` (wiki/736). |
| 2026-06-05 v550 | Rust rendezvous path selection now consumes public/NAT/relay candidate metadata from MUSU.PRO. | `musu-rs/src/bridge/rendezvous.rs` now uses a direct candidate's `public_addr` as the selected direct endpoint when present, preserves the original candidate `addr`, `selected_addr_source`, NAT type/observer, and public address in `ResolvedPeer.meta`, and carries relay descriptors as fallback metadata without selecting relay as the default path. Regression coverage proves LAN still wins over Tailscale/direct/relay, direct-only candidates select `public_addr`, NAT metadata survives, and relay descriptors are available for fallback diagnostics. Validation passed `cargo test --manifest-path .\musu-rs\Cargo.toml --bin musu rendezvous -- --nocapture` (`6/6`), `cargo check --manifest-path .\musu-rs\Cargo.toml --bin musu`, P2P store-forward relay contract audit `ok=true`/`fail_count=0`, and `git diff --check`. This closes the selector-side gap after wiki/733 and wiki/735, but Rust source changed after the current packaged evidence refresh, so fresh MSIX/single-machine/idle CPU/runtime matrix evidence is required before current-source local runtime gates can be claimed. Public release remains No-Go on second-PC evidence, hosted P2P release proof, support mailbox, Store evidence, and current packaged evidence freshness. Canonical report: `docs\RELEASE_1_15_0_RC1_RENDEZVOUS_SELECTOR_CANDIDATE_METADATA_2026_06_05.md` (wiki/737). |
| 2026-06-05 v551 | Desktop shell and `/app` gate no longer send users to an absent fixed localhost dashboard. | Root cause of the reported browser error was confirmed on HUGH_SECOND: packaged MUSU bridge was healthy at `127.0.0.1:10325`, while no process listened on `127.0.0.1:3001`, so `http://127.0.0.1:3001/app` correctly returned `ERR_CONNECTION_REFUSED`. `musu-bee/src-tauri/src/lib.rs` now returns no dashboard URL when `3000`/`3001` are absent instead of fabricating `http://127.0.0.1:3000/app`; `musu-bee/src-tauri-shell/main.js` disables `Open Dashboard` unless a live dashboard URL is reported; `musu-bee/src/app/app/page.tsx` no longer tells users to visit `localhost:3001/app` and now frames MUSU Desktop as the local executor with MUSU.PRO as the remote input/control-plane surface. Validation passed Tauri shell tests `7/7`, direct `tsc --noEmit`, and `git diff --check`. This is desktop/web source, so fresh MSIX, single-machine, idle CPU, and runtime matrix evidence are required before current-source local runtime gates can be claimed. Canonical report: `docs\RELEASE_1_15_0_RC1_DESKTOP_SHELL_DASHBOARD_URL_HARDENING_2026_06_05.md` (wiki/738). |
| 2026-06-05 v552 | Fresh primary-machine packaged local-runtime evidence was restored after desktop dashboard URL hardening. | Rebuilt/reinstalled the local-sideload MSIX after commit `7293afd6`; release runtime build, Tauri desktop shell build, MSIX package/signing, packaged startup smoke, install, installed package contract, and packaged runtime identity checks passed. PATH still shadows the WindowsApps alias with `C:\Users\empty\.cargo\bin\musu.exe`, so packaged checks used explicit WindowsApps alias invocation. Packaged runtime repair passed with bridge `http://127.0.0.1:1181`, `dashboard.required=false`, and after-process ownership OK. Single-machine evidence `docs\evidence\single-machine\1.15.0-rc.1\20260605-112337-HUGH_SECOND.evidence.json` passed with `single_machine_surface=local-bridge-only` and CLI route checked. Desktop-open idle CPU `docs\evidence\runtime-idle-cpu\1.15.0-rc.1\20260605-112710-HUGH_SECOND.desktop-open.evidence.json` passed clean git for `60.055s` with MUSU `0`, Node `0`, WebView2 `0.16`, working set `363.72MB`, and hot `0`. Runtime CPU matrix `docs\evidence\runtime-cpu-scenarios\1.15.0-rc.1\20260605-112906-HUGH_SECOND.runtime-cpu-scenario-matrix.json` passed verifier `ok=true`/`fail_count=0`, route token `MUSU_CPU_SCENARIO_ROUTE_OK_20260605_112906`, route task `37773a7f-6aa3-4f0c-90d7-0317558d044f`, max role CPU MUSU `0.03`, Node `0`, WebView2 `0.1`, and max working set `366.26MB`. This restores current one-machine evidence only; public release remains blocked on second-PC multi-device/CPU/matrix evidence, hosted P2P release proof, support mailbox, and Store evidence. Canonical report: `docs\RELEASE_1_15_0_RC1_POST_DESKTOP_DASHBOARD_URL_HARDENING_PRIMARY_EVIDENCE_REFRESH_2026_06_05.md` (wiki/739). |
| 2026-06-05 v553 | Runtime CPU matrix verification now requires full MUSU/node/WebView2/other role attribution. | `verify-runtime-cpu-scenario-matrix.ps1` now fails release matrix evidence unless `measurement.max_one_core_percent_by_role`, `measurement.process_counts_by_role`, and the CPU attribution `sample_count_by_role`, `total_cpu_seconds_by_role`, and `max_one_core_percent_by_role` all include `musu`, `node`, `webview2`, and `other`. This closes the gap where node helper attribution could be omitted while aggregate CPU still looked under budget. `test-release-evidence-verifiers.ps1` now includes `runtime matrix rejects CPU attribution without node role fields`; validation passed PowerShell parser checks, release verifier regression `ok=true`/`case_count=39`/`failed_case_count=0`, latest actual runtime matrix verification against `20260605-124652-HUGH_SECOND.runtime-cpu-scenario-matrix.json` with `fail_count=0`, and `git diff --check`. This is verifier hardening only; it does not replace required second-PC CPU/matrix evidence or hosted P2P/support/Store gates. Canonical report: `docs\RELEASE_1_15_0_RC1_RUNTIME_CPU_ATTRIBUTION_ROLE_GATE_2026_06_05.md`. |
| 2026-06-05 v554 | Multi-device evidence now rejects route_kind/candidate_addr mismatches. | `verify-multidevice-evidence.ps1` now classifies direct route candidate addresses the same way as hosted route evidence: loopback/private/link-local maps to `lan`, `100.64.0.0/10` maps to `tailscale`, and public IPs/hostnames map to `direct_quic`. The verifier applies this to both `route_explain.selected_candidate.route_kind` versus selected `addr` and `route_evidence.route_kind` versus `candidate_addr`, while leaving relay evidence to the relay proof gates. `test-release-evidence-verifiers.ps1` now includes `multidevice rejects route_kind candidate_addr mismatch`; validation passed PowerShell parser checks, release verifier regression `ok=true`/`case_count=40`/`failed_case_count=0`, and `git diff --check`. This is evidence-integrity hardening only; actual second-PC route/CPU evidence and hosted P2P/support/Store gates remain required. Canonical report: `docs\RELEASE_1_15_0_RC1_MULTIDEVICE_ROUTE_KIND_CANDIDATE_ADDR_GATE_2026_06_05.md`. |
| 2026-06-05 v555 | Packaged desktop now treats the localhost dashboard as debug-only opt-in. | Root cause is locked: `ERR_CONNECTION_REFUSED` at `127.0.0.1:3001/app` is a browser hitting the optional developer Next.js dashboard with no listener, not the local MUSU runtime failing or requiring internet. `musu-bee/src-tauri/src/lib.rs` now disables the developer dashboard surface in packaged desktop unless `MUSU_DESKTOP_ENABLE_DEV_DASHBOARD=1` is set, while debug builds can still use it. The shell labels it as `Debug Dashboard`; `verify-single-machine-evidence.ps1` now rejects packaged evidence tied to fixed dev dashboard defaults `127.0.0.1:3000` or `127.0.0.1:3001`; desktop readiness gates the opt-in source contract; frontend polling audit now covers Tauri shell JS for interval/timeout/animation loops. Validation passed parser checks, Tauri `cargo test` `8/8`, `npm run build:tauri-shell`, frontend polling audit `ok=true`/`fail_count=0`, release verifier regression `ok=true`/`case_count=41`/`failed_case_count=0` with new negative case `single-machine rejects packaged evidence tied to dev dashboard 3001`, desktop readiness new opt-in check pass, and `git diff --check`. This is source hardening only; fresh packaged MSIX/single-machine/idle CPU/runtime matrix evidence is required before installed-app behavior is current. Public release remains No-Go on second-PC multi-device/CPU/matrix evidence, hosted `musu.pro` P2P proof, support mailbox, and Store gates. Canonical report: `docs\RELEASE_1_15_0_RC1_PACKAGED_DEV_DASHBOARD_OPT_IN_GATE_2026_06_05.md`. |
| 2026-06-05 v556 | Fresh primary-machine packaged evidence was restored after the dev dashboard opt-in gate. | Rebuilt/reinstalled the local-sideload MSIX after `1eb4fe1a`; release runtime build, Tauri desktop shell build, MSIX package/signing, packaged startup smoke, install, installed package contract, and packaged runtime identity checks passed. Installed package is `Yellowhama.MUSU_1.15.0.0_x64__ygcjq669as2b6`; PATH still shadows the WindowsApps alias with `C:\Users\empty\.cargo\bin\musu.exe`, so packaged checks used explicit WindowsApps alias invocation. Packaged runtime repair passed with bridge `http://127.0.0.1:9422`, PID `29052`, `dashboard_required=false`, and after-process ownership OK. Single-machine evidence `docs\evidence\single-machine\1.15.0-rc.1\20260605-141236-HUGH_SECOND.evidence.json` passed with `single_machine_surface=local-bridge-only` and CLI route checked. Desktop-open idle CPU `docs\evidence\runtime-idle-cpu\1.15.0-rc.1\20260605-141514-HUGH_SECOND.desktop-open.evidence.json` passed clean git for `60.056s` with MUSU `0`, Node `0`, WebView2 `0.21`, owned WebView2 `6`, working set `364.67MB`, and hot `0`. Runtime CPU matrix `docs\evidence\runtime-cpu-scenarios\1.15.0-rc.1\20260605-141700-HUGH_SECOND.runtime-cpu-scenario-matrix.json` passed verifier `ok=true`/`fail_count=0`, route token `MUSU_CPU_SCENARIO_ROUTE_OK_20260605_141700`, route task `a33319ba-c7f5-400a-a178-eef2a9d94531`, max role CPU MUSU `0`, Node `0`, WebView2 `0.26`, and max working set `369.76MB`. Clean go/no-go after `8102191f` reports `local_artifacts_ready=true`, `single_machine_verified=true`, runtime idle CPU `1/2 [HUGH_SECOND]`, runtime CPU matrix `1/2 [HUGH_SECOND]`, loop contracts true, `manifest_git_dirty=false`, and public release still No-Go on second-PC multi-device/CPU/matrix, targeted second-PC post-route CPU attempt, hosted P2P, support mailbox, and Store gates. Canonical report: `docs\RELEASE_1_15_0_RC1_POST_PACKAGED_DEV_DASHBOARD_OPT_IN_PRIMARY_EVIDENCE_REFRESH_2026_06_05.md`. |
| 2026-06-05 v557 | Hosted P2P release evidence now rejects non-release relay transport kinds. | `routeEvidenceStore.ts` now revalidates `release_grade=true` relay query results against release tunnel proof kind `quic_relay_tunnel`, so stale/manual relay records with `websocket_tunnel` cannot survive release-grade filtering. As clarified by GOAL v588, `verify-p2p-control-plane-evidence.ps1` now treats `relay_transport.relay_transport_kind=quic_relay_tunnel` as the release relay kind and `quic_tls_1_3` as the separate release encryption/proof requirement; `test-release-evidence-verifiers.ps1` keeps the negative case `p2p rejects non-release relay transport kind`. `audit-p2p-store-forward-relay-contract.ps1` gates both protections. Validation passed PowerShell parser checks, `npm run test:p2p` `84/84`, `npm run typecheck`, P2P store-forward relay contract audit `ok=true`/`fail_count=0`, release verifier regression `ok=true`/`case_count=42`/`failed_case_count=0`, and `git diff --check`. Existing hosted evidence now explicitly fails when relay transport kind is `websocket_tunnel` instead of release-grade `quic_relay_tunnel`, which is intended; public release remains No-Go on second-PC CPU/matrix and multi-device route evidence, hosted release-grade P2P proof, support mailbox, and Store gates. Canonical report: `docs\RELEASE_1_15_0_RC1_P2P_RELAY_TRANSPORT_KIND_RELEASE_GATE_2026_06_05.md`. |
| 2026-06-05 v558 | Route evidence store changes are now classified as server-only P2P control-plane freshness changes. | Root cause: the previous relay transport hardening changed `musu-bee/src/lib/routeEvidenceStore.ts`, but the single-machine, runtime CPU matrix, and go/no-go freshness classifiers only treated `musu-bee/src/lib/p2p*.ts` as server-only P2P control-plane code. That incorrectly stale-marked local packaged desktop evidence even though the installed local runtime did not change. Added `musu-bee/src/lib/routeEvidence*.ts` to all three classifier allowlists and extended the release verifier regression source contract. Validation passed parser checks, `git diff --check`, and release verifier regression `ok=true`/`case_count=42`/`failed_case_count=0`. Recomputed go/no-go before commit restored `local_artifacts_ready=true`, `single_machine_verified=true`, runtime idle CPU `1/2`, runtime CPU matrix `1/2`, loop contracts true, and `p2p_store_forward_relay_contract_verified=true`; public release remains No-Go on second-PC multi-device/CPU/matrix/route-attempt evidence, hosted P2P control-plane proof, support mailbox, Store evidence, and current dirty git until this row is committed. |
| 2026-06-05 v559 | The logged-in cloud registration loop is now cancellation-aware. | The `musu.pro` cloud registration loop already had low-duty heartbeat defaults, a `60s` floor, failure backoff, and jitter; it now also owns a `CancellationToken`, cancels on Ctrl-C, and sleeps through `tokio::select!` so shutdown can interrupt the heartbeat wait. `audit-rust-background-loop-contract.ps1` now requires cloud-heartbeat cancellation token ownership and cancellation-aware sleep, and `bridge::tests::cloud_*` covers heartbeat normalization plus capped backoff/jitter sleep duration. Validation passed the PowerShell parser check, `cargo fmt --manifest-path .\musu-rs\Cargo.toml`, targeted Rust tests `2/2`, `cargo check --manifest-path .\musu-rs\Cargo.toml --bin musu`, Rust background-loop audit `ok=true`/`fail_count=0`/`unaudited_loop_hit_count=0`/`telemetry_flush_primitive_hit_count=0`, and `git diff --check`. Dirty go/no-go kept `single_machine_verified=true` and CPU/matrix `1/2`; clean go/no-go after commit kept loop contracts true and `local_artifacts_ready=true`, but correctly reset `single_machine_verified=false`, runtime idle CPU `0/2`, and runtime CPU matrix `0/2` because `musu-rs/src/bridge/mod.rs` changed after the latest packaged evidence refresh. Public release remains No-Go on fresh packaged local evidence, second-PC evidence, hosted P2P proof, support mailbox, and Store proof. Canonical report: `docs\RELEASE_1_15_0_RC1_CLOUD_REGISTRATION_LOOP_CANCELLATION_2026_06_05.md`. |
| 2026-06-05 v560 | Login completion guidance no longer sends users to a fixed localhost dashboard. | The repeated browser error was reconfirmed on HUGH_SECOND as `localhost:3001` being an optional developer/workspace dashboard with no listener, while the installed packaged bridge was healthy at `http://127.0.0.1:2414/health` with `status=ok`, `worker_ok=true`, and `auth_mode=production`. `musu login` now prints a shared connection checklist that points to `musu doctor`, `musu bridge`, MUSU Desktop, or a MUSU.PRO workspace instead of `http://127.0.0.1:3000/app` / `http://127.0.0.1:3001/app`. Regression coverage `login_connection_checklist_does_not_open_fixed_localhost_dashboard` rejects `127.0.0.1:3001` and `localhost:3001` in that checklist. Validation passed `cargo fmt --manifest-path .\musu-rs\Cargo.toml`, targeted Rust test `1/1`, `cargo check --manifest-path .\musu-rs\Cargo.toml --bin musu`, `git diff --check`, and live bridge health confirmation. This Rust CLI source change makes previous packaged single-machine/idle CPU/runtime matrix evidence stale for current-source release claims; fresh MSIX/single-machine/CPU/matrix evidence is required. Public release remains No-Go on fresh primary packaged evidence, second-PC route/CPU/matrix evidence, hosted P2P proof, support mailbox, and Store proof. Canonical report: `docs\RELEASE_1_15_0_RC1_LOGIN_CHECKLIST_LOCAL_RUNTIME_GUIDANCE_2026_06_05.md`. |
| 2026-06-05 v561 | Fresh primary packaged evidence was restored after the login checklist localhost fix. | Rebuilt/reinstalled the local-sideload MSIX after `6549f6e0`; release runtime build, Tauri shell build, MSIX package/signing, packaged startup smoke, install, installed package contract, and packaged runtime identity checks passed. PATH still shadows the WindowsApps alias with `C:\Users\empty\.cargo\bin\musu.exe`, so packaged checks used explicit WindowsApps alias invocation. Packaged runtime repair passed with bridge `http://127.0.0.1:13486`, `dashboard.required=false`, and after-process ownership OK. Single-machine evidence `docs\evidence\single-machine\1.15.0-rc.1\20260605-161147-HUGH_SECOND.evidence.json` passed with `single_machine_surface=local-bridge-only` and CLI route checked. Desktop-open idle CPU `docs\evidence\runtime-idle-cpu\1.15.0-rc.1\20260605-161246-HUGH_SECOND.desktop-open.evidence.json` passed clean git for `60.05s` with MUSU `0.03`, Node `0`, WebView2 `0.52`, working set `365.56MB`, and hot `0`. Runtime CPU matrix `docs\evidence\runtime-cpu-scenarios\1.15.0-rc.1\20260605-162127-HUGH_SECOND.runtime-cpu-scenario-matrix.json` passed verifier `ok=true`/`fail_count=0`, route token `MUSU_CPU_SCENARIO_ROUTE_OK_20260605_162127`, route task `a38d0adc-e760-4962-8303-f863c665124d`, max role CPU MUSU `0`, Node `0`, WebView2 `0.1`, and max working set `366.72MB`. Clean go/no-go after `b4c0a5a7` reports `local_artifacts_ready=true`, `single_machine_verified=true`, runtime idle CPU `1/2`, runtime CPU matrix `1/2`, targeted second-PC route CPU `0/1`, `p2p_control_plane_verified=false`, and `manifest_git.dirty=false`; public release remains No-Go on second-PC multi-device/CPU/matrix/target-route CPU, hosted P2P, support mailbox, and Store gates. Canonical report: `docs\RELEASE_1_15_0_RC1_POST_LOGIN_CHECKLIST_PRIMARY_EVIDENCE_REFRESH_2026_06_05.md`. |
| 2026-06-05 v562 | Targeted HUGH-MAIN post-route CPU evidence was restored after the latest packaged evidence refresh. | The installed peer list still contains `HUGH-MAIN` at `192.168.1.192:8949`, but TCP/ping route checks from HUGH_SECOND currently time out. A clean targeted runtime CPU scenario matrix `docs\evidence\runtime-cpu-scenarios\1.15.0-rc.1\20260605-163936-HUGH_SECOND.runtime-cpu-scenario-matrix.json` ran `musu route --target HUGH-MAIN --wait-timeout-sec 180 --wait "Reply exactly: MUSU_CPU_SCENARIO_ROUTE_OK_20260605_163936"` with `-AllowFailedRouteProbe`; the route attempt timed out against `http://192.168.1.192:8949/api/tasks/delegate`, then the post-route CPU sample passed for `60.034s` with hot `0`, MUSU `0`, Node `0`, WebView2 `0`, working set `21.59MB`, and verifier `docs\evidence\runtime-cpu-scenarios\1.15.0-rc.1\20260605-163936-HUGH_SECOND.target-route.verification.json` reports `ok=true`/`fail_count=0` for expected target `HUGH-MAIN`. Clean go/no-go after evidence commit `82834537` reports `local_artifacts_ready=true`, `single_machine_verified=true`, runtime idle CPU `1/2`, runtime CPU matrix `1/2`, targeted second-PC route CPU `1/1`, `multi_device_verified=false`, `p2p_control_plane_verified=false`, `support_mailbox_verified=false`, `store_release_verified=false`, and `manifest_git.dirty=false`; public release remains No-Go on real second-PC multi-device/CPU/matrix evidence, hosted P2P proof, support mailbox, and Store gates. Canonical report: `docs\RELEASE_1_15_0_RC1_TARGETED_HUGH_MAIN_ROUTE_CPU_EVIDENCE_2026_06_05.md` (wiki/742). |
| 2026-06-05 v563 | Relay transport proof is now bound to the route evidence source/target peer pair. | Hosted route evidence now requires inline `musu.relay_transport_proof.v1` to carry `source_node_id` and `target_node_id`, rejects source/target mismatches with explicit blockers, and release-grade relay query revalidation rejects records whose current transport proof is not bound to the same peer pair. Rust `RouteRelayTransportProof` now serializes the same peer-binding fields as the hosted transport proof API. Validation passed route-evidence tests `28/28`, `npm run typecheck`, Rust `--lib` peer-binding serialization test `1/1`, P2P store-forward relay contract audit `ok=true`/`fail_count=0`, P2P env status recheck, and `git diff --check`. P2P env status correctly remains `ok=false` because release relay endpoints, KV/Upstash, live relay route proof, and delivery proof are still missing. This is evidence-integrity hardening only; MUSU Desktop remains the local executor and `musu.pro` remains the remote input/rendezvous/control-plane surface. Canonical report: `docs\RELEASE_1_15_0_RC1_RELAY_TRANSPORT_PROOF_PEER_BINDING_GATE_2026_06_05.md`. |
| 2026-06-05 v564 | Fresh packaged local-runtime evidence and audit were restored after relay transport proof peer binding. | Evidence commits `a30d87e7`, `687d8634`, `0c7c059d`, and `b001924a` record the post-peer-binding HUGH_SECOND MSIX install, bridge-only single-machine smoke, desktop-open idle CPU, normal five-state runtime CPU matrix, and targeted HUGH-MAIN post-route CPU matrix. Single-machine evidence `docs\evidence\single-machine\1.15.0-rc.1\20260605-200449-HUGH_SECOND.evidence.json` passed with `single_machine_surface=local-bridge-only`, bridge `http://127.0.0.1:11648`, `dashboard_required=false`, and CLI route checked. Idle CPU `docs\evidence\runtime-idle-cpu\1.15.0-rc.1\20260605-201254-HUGH_SECOND.desktop-open.evidence.json` passed clean git for `60.036s` with MUSU `0`, Node `0`, WebView2 `0.1`, and hot `0`. Normal runtime matrix `docs\evidence\runtime-cpu-scenarios\1.15.0-rc.1\20260605-201430-HUGH_SECOND.runtime-cpu-scenario-matrix.json` passed with route token `MUSU_CPU_SCENARIO_ROUTE_OK_20260605_201430`, route task `f478b4b7-ac57-488c-93eb-0d046df4cabc`, max role CPU MUSU `0`, Node `0`, WebView2 `0.18`, and verifier `ok=true`. Targeted HUGH-MAIN matrix `docs\evidence\runtime-cpu-scenarios\1.15.0-rc.1\20260605-202107-HUGH_SECOND.runtime-cpu-scenario-matrix.json` verified the expected target with failed route allowed; post-route CPU remained MUSU `0.05`, Node `0`, WebView2 `0.08`, hot `0`. Clean go/no-go after `b001924a` reports `local_artifacts_ready=true`, `single_machine_verified=true`, `msix_install_verified=true`, `runtime_cpu_second_pc_route_attempt_verified=true`, `p2p_store_forward_relay_contract_verified=true`, `manifest_git.dirty=false`, and `ready_for_public_desktop_release=false` with six blockers: multi-device, second-PC idle CPU, second-PC runtime matrix, support mailbox, Store release, and hosted P2P control-plane proof. Code audit found no high/medium issue in the peer-binding change; validation rerun passed `npm run test:p2p` `85/85`, `npm run typecheck`, Rust peer-binding DTO test `1/1`, and P2P store-forward audit `ok=true`. Canonical report: `docs\RELEASE_1_15_0_RC1_POST_RELAY_TRANSPORT_PROOF_PEER_BINDING_EVIDENCE_AUDIT_NEXT_STEPS_2026_06_05.md` (wiki/744). |
| 2026-06-05 v565 | Code and document indexes were refreshed after the relay peer-binding wiki/spec update. | `bun run C:\Users\empty\.agents\skills\gstack\bin\gstack-gbrain-sync.ts --quiet` completed the gbrain code stage with source `gstack-code-musu-bee-8815b622` and `page_count=148`, and the memory stage with `573 written`, `0 failed`; the final gbrain sync state is `DONE_WITH_CONCERNS` because the brain-sync stage reported `gstack-brain-sync exited undefined` and Windows capability probes did not verify semantic/symbol search hits, so `AGENTS.md` GBrain Search Guidance was not added. The project-local indexer remains the reliable repo index evidence: `& "$env:LOCALAPPDATA\Microsoft\WindowsApps\musu.exe" indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` passed on the final run with `2404 files`, `2690 symbols`, `13070 ms`. Search terms should include `wiki/743`, `wiki/744`, `relay_transport_proof.source_node_id`, `relay_transport_proof.target_node_id`, `20260605-201430-HUGH_SECOND.runtime-cpu-scenario-matrix.json`, `20260605-202107-HUGH_SECOND.runtime-cpu-scenario-matrix.json`, `MUSU Desktop local executor`, `MUSU.PRO remote input rendezvous control plane`, and `gstack-code-musu-bee-8815b622`. |
| 2026-06-05 v566 | Rust background-loop audit now explicitly covers the WebSocket proxy request loops. | Commit `918ac7a6` extends `audit-rust-background-loop-contract.ps1` with `ws-proxy` checks for `musu-rs\src\bridge\handlers\ws_proxy.rs`: request upgrade binding, client-to-upstream `client_rx.next().await`, upstream-to-client `upstream_rx.next().await`, send-failure exits in both directions, and `tokio::select!` closure when either side ends. This is release-verifier coverage only; it does not change runtime behavior or the product boundary. MUSU Desktop remains the local executor, and MUSU.PRO remains remote input, project/company room, rendezvous, path selection, relay fallback policy, and evidence control plane. Validation passed PowerShell parser, Rust background-loop audit `ok=true`/`fail_count=0`/`unaudited_loop_hit_count=0`/`telemetry_flush_primitive_hit_count=0` with `ws-proxy` checks `6/6`, frontend polling audit `ok=true`/`fail_count=0`/`low_duty_polling_call_site_count=29`, and `git diff --check`. Clean go/no-go after `918ac7a6` reports `local_artifacts_ready=true`, `single_machine_verified=true`, `msix_install_verified=true`, `runtime_cpu_second_pc_route_attempt_verified=true`, `rust_background_loop_contract_verified=true`, `idle_busy_loop_candidate_contract_verified=true`, `manifest_git_dirty=false`, and `ready_for_public_desktop_release=false` with six blockers: multi-device, second-PC idle CPU, second-PC runtime matrix, support mailbox, Store release, and hosted P2P control-plane proof. Code audit found no high/medium issue in this verifier-only change. Canonical report: `docs\RELEASE_1_15_0_RC1_WS_PROXY_LOOP_AUDIT_COVERAGE_2026_06_05.md` (wiki/745). |
| 2026-06-05 v567 | Code and document indexing was refreshed after the WebSocket proxy loop audit docs. | MUSU local indexer succeeded via `& "$env:LOCALAPPDATA\Microsoft\WindowsApps\musu.exe" indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` with `2407 files`, `2690 symbols`, and `16395 ms`. gbrain sync was rerun non-quiet after the quiet run exited code `1`; it used `mode=incremental`, `engine=pglite`, completed code stage `OK` for source `gstack-code-musu-bee-8815b622` with `page_count=356`, and memory stage `OK` with `0 imported`, `1 unchanged`, `0 failed`, but final state remained `2 ok, 1 error` because `brain-sync` reported `gstack-brain-sync exited undefined`. gbrain import also reported `ZEROENTROPY_API_KEY` missing and generated/evidence file failures, so `sync.last_commit` did not advance and `AGENTS.md` GBrain Search Guidance remains intentionally absent. Search terms should include `GOAL v567`, `wiki/745 index refresh`, `2407 files`, `2690 symbols`, `16395 ms`, `page_count=356`, `ZEROENTROPY_API_KEY`, `gstack-brain-sync exited undefined`, and `AGENTS.md GBrain Search Guidance not added`. |
| 2026-06-05 v568 | Rust background-loop audit now gates new `while let` loop files and audits the current finite/request-scoped sites. | Commit `d1c3361b` extends `audit-rust-background-loop-contract.ps1` beyond `while true` / `loop {` detection by adding a `while let` source scanner with explicit allowlist and named checks for current sites. New contracts cover audit failure-window deque pruning, rate-limit window pruning, workflow executor/spec finite Kahn queues and cycle rejection, request-scoped file directory listing, forwarded-task multipart parsing, WebDAV PROPFIND directory listing, and WebRTC finite-buffer NAL splitting. Validation passed PowerShell parser, Rust background-loop audit `ok=true`/`fail_count=0`/`unaudited_loop_hit_count=0`/`telemetry_flush_primitive_hit_count=0`/`check_count=152`, selected scopes `audit-failure-window 2/2`, `rate-limit-window 2/2`, `workflow-executor 6/6`, `workflow-spec 3/3`, `files-api 2/2`, `forward-multipart 2/2`, `webdav-propfind 2/2`, `webrtc-screen-share 8/8`, and `ws-proxy 6/6`; frontend polling audit stayed `ok=true` with `29` low-duty call-site files and no direct intervals; `git diff --check` passed. Clean go/no-go after `d1c3361b` reports local artifacts, single-machine, MSIX install, targeted second-PC route CPU, Rust/idle/frontend contracts, and P2P store-forward contract true, `manifest_git_dirty=false`, and `ready_for_public_desktop_release=false` with six blockers: multi-device, second-PC idle CPU, second-PC runtime matrix, support mailbox, Store release, and hosted P2P control-plane proof. This is verifier coverage only and does not change runtime behavior or the MUSU Desktop local-executor / MUSU.PRO control-plane product boundary. Canonical report: `docs\RELEASE_1_15_0_RC1_RUST_WHILE_LET_LOOP_AUDIT_COVERAGE_2026_06_05.md` (wiki/746). |
| 2026-06-05 v569 | Code and document indexing was refreshed after the Rust while-let loop audit docs. | MUSU local indexer succeeded via `& "$env:LOCALAPPDATA\Microsoft\WindowsApps\musu.exe" indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` with `2409 files`, `2690 symbols`, and `8270 ms`. gbrain was not rerun for this small documentation refresh because the previous same-session run already identified the active blocker: missing `ZEROENTROPY_API_KEY`, generated/evidence import failures, `sync.last_commit` not advancing, and `brain-sync` exiting undefined. `AGENTS.md` GBrain Search Guidance remains intentionally absent until semantic/symbol search is verified on this Windows machine. Search terms should include `GOAL v569`, `wiki/746 index refresh`, `2409 files`, `2690 symbols`, `8270 ms`, `ZEROENTROPY_API_KEY`, and `AGENTS.md GBrain Search Guidance not added`. |
| 2026-06-05 v570 | Rust background-loop audit now gates Rust spawn/background-task entry points. | Commit `94a89614` extends `audit-rust-background-loop-contract.ps1` to audit current `tokio::spawn`, `tokio::task::spawn_blocking`, `std::thread::spawn`, and `thread::spawn` sites and to fail future spawn use in new Rust files unless explicitly allowlisted and contract-audited. New checks cover planner/cloud heartbeat cancellation watcher spawns, file-sync configured-root gating, control MCP cancellation, default cloud client timeout, clipboard opt-in blocking poller, relay payload poller task spawn, mDNS blocking receive timeout, indexer `spawn_blocking` await/no-workspace return, PTY request-scoped writes, WebRTC one-shot pong/RTCP/capture tasks, writer task registry/callback/musu-crawl one-shots, Claude stdin writer, company post-create sync, node health joins, route evidence submit, rendezvous publish/close timeouts, and workflow execution. Validation passed PowerShell parser, Rust background-loop audit `ok=true`/`fail_count=0`/`unaudited_loop_hit_count=0`/`unaudited_spawn_hit_count=0`/`telemetry_flush_primitive_hit_count=0`/`check_count=200`, frontend polling audit `ok=true`/`fail_count=0`/`low_duty_polling_call_site_count=29`, and `git diff --check`. Clean go/no-go after `94a89614` reports local artifacts, single-machine, MSIX install, targeted second-PC route CPU, Rust/idle/frontend contracts, and P2P store-forward contract true, `manifest_git_dirty=false`, and `ready_for_public_desktop_release=false` with six unchanged blockers: multi-device, second-PC idle CPU, second-PC runtime matrix, support mailbox, Store release, and hosted P2P control-plane proof. Code audit found no high/medium issue. This is verifier-only hardening and does not change runtime behavior or the MUSU Desktop local-executor / MUSU.PRO web-input control-plane boundary. Canonical report: `docs\RELEASE_1_15_0_RC1_RUST_SPAWN_CONTRACT_AUDIT_COVERAGE_2026_06_05.md` (wiki/747). |
| 2026-06-05 v571 | Code and document indexing was refreshed after the Rust spawn/background-task audit docs. | MUSU local indexer succeeded via `& "$env:LOCALAPPDATA\Microsoft\WindowsApps\musu.exe" indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` with `2414 files`, `2690 symbols`, and `10865 ms`. gbrain quiet sync exited code `1` with no output; non-quiet sync used `mode=incremental`, `engine=pglite`, found `3599` code files, completed code stage `OK` for source `gstack-code-musu-bee-8815b622` with `page_count=540`, imported `92` pages, skipped `3507` pages, created `5188` chunks, and left `sync.last_commit` unadvanced because `3059` files failed. Memory stage was `OK` with `0 imported`, `1 unchanged`, `0 failed`, but final state remained `2 ok, 1 error` because `brain-sync` reported `gstack-brain-sync exited undefined`; failures included missing `ZEROENTROPY_API_KEY`, `row.deleted_at`, and array-length import errors. `AGENTS.md` GBrain Search Guidance remains intentionally absent. Search terms should include `GOAL v571`, `wiki/747 index refresh`, `2414 files`, `2690 symbols`, `10865 ms`, `page_count=540`, `3059 file(s) failed`, `ZEROENTROPY_API_KEY`, `sync.last_commit NOT advanced`, and `AGENTS.md GBrain Search Guidance not added`. |
| 2026-06-05 v572 | Native RPC exec is now a fail-closed audited local-runtime command boundary. | Commit `fe25c5d8` hardens native bridge `/api/v1/rpc/exec`: `MUSU_RPC_EXEC_ALLOWLIST` defaults empty and must name an allowed bare command, command paths are rejected, user-supplied `cwd` is rejected to avoid executable path resolution ambiguity, command/arg sizes and control characters are bounded, stdout/stderr are capped at `64 KiB`, `MUSU_RPC_EXEC_TIMEOUT_SECS` defaults to `10` and clamps to `1..60`, child processes use `kill_on_drop(true)`, and rejected/spawn-failed/timed-out/completed attempts are audit-logged. `audit-operator-api-security-contract.ps1` now gates the native RPC exec contract. Validation passed Rust RPC exec tests `6/6`, `cargo check --manifest-path .\musu-rs\Cargo.toml --bin musu`, operator API security audit `ok=true`/`fail_count=0`/`check_count=44`, local API auth audit `ok=true`/`fail_count=0`/`check_count=39`, Rust background-loop audit `ok=true`/`fail_count=0`/`check_count=200`, and `git diff --check`. Code audit found and fixed one issue before commit: accepting even bounded `cwd` was unsafe for this remote-control boundary. Clean go/no-go after `fe25c5d8` reports `manifest_git.dirty=false`, hardening/source-contract gates true, `local_artifacts_ready=true`, `msix_install_verified=true`, but `single_machine_verified=false`, runtime idle CPU false, runtime matrix false, targeted second-PC route CPU false, hosted P2P false, support false, Store false, and `ready_for_public_desktop_release=false` with eight blockers. This changes native runtime source, so fresh packaged single-machine/CPU/matrix evidence is required. Canonical report: `docs\RELEASE_1_15_0_RC1_NATIVE_RPC_EXEC_HARDENING_2026_06_05.md` (wiki/748). |
| 2026-06-05 v573 | Code and document indexing was refreshed after native RPC exec hardening docs. | MUSU local indexer succeeded via `& "$env:LOCALAPPDATA\Microsoft\WindowsApps\musu.exe" indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` with `2415 files`, `2705 symbols`, and `67396 ms` after wiki/748, GOAL v572, the native RPC exec canonical report, network boundary spec, BETA checklist, WIKI/WIKI_INDEX, and CoS memory updates. gbrain was not rerun for this incremental refresh because the same-session active blocker is already known from the prior run: missing `ZEROENTROPY_API_KEY`, import failures, `sync.last_commit` not advancing, and `gstack-brain-sync exited undefined`; `AGENTS.md` GBrain Search Guidance remains intentionally absent. Search terms should include `GOAL v573`, `wiki/748 index refresh`, `2415 files`, `2705 symbols`, `67396 ms`, `MUSU_RPC_EXEC_ALLOWLIST`, `MUSU_RPC_EXEC_TIMEOUT_SECS`, `native RPC exec hardening`, `ZEROENTROPY_API_KEY`, and `AGENTS.md GBrain Search Guidance not added`. |
| 2026-06-05 v574 | Fresh primary-machine packaged evidence, code audit, qualitative evaluation, and next steps were restored after native RPC exec hardening. | Evidence commits `5c8f62bf`, `a29b52fa`, `f46862fa`, and `3b09dd73` record the post-native-RPC-HUGH_SECOND single-machine smoke, desktop-open idle CPU, five-scenario runtime CPU matrix, and targeted HUGH-MAIN post-route CPU matrix. Single-machine evidence `docs\evidence\single-machine\1.15.0-rc.1\20260605-230036-HUGH_SECOND.evidence.json` passed with `single_machine_surface=local-bridge-only`, `dashboard_required=false`, bridge `http://127.0.0.1:6540`, and CLI route checked. Idle CPU `docs\evidence\runtime-idle-cpu\1.15.0-rc.1\20260605-230300-HUGH_SECOND.desktop-open.evidence.json` passed clean git for `60.032s` with MUSU `0.03`, Node `0`, WebView2 `0.16`, working set `361.22MB`, and hot `0`. Runtime matrix `docs\evidence\runtime-cpu-scenarios\1.15.0-rc.1\20260605-231115-HUGH_SECOND.runtime-cpu-scenario-matrix.json` passed verifier `ok=true`/`fail_count=0`, route token `MUSU_CPU_SCENARIO_ROUTE_OK_20260605_231115`, route task `c0e4c3f1-3e79-44ef-846e-475449e1819e`, max MUSU `0`, Node `0`, WebView2 `0.1`, and max working set `364.89MB`. Targeted HUGH-MAIN matrix `docs\evidence\runtime-cpu-scenarios\1.15.0-rc.1\20260605-231836-HUGH_SECOND.runtime-cpu-scenario-matrix.json` verified expected target `HUGH-MAIN` with failed route allowed; route timed out to `192.168.1.192:8949`, then post-route CPU stayed MUSU `0`, Node `0`, WebView2 `0.05`, hot `0`. Clean go/no-go after `3b09dd73` reports `local_artifacts_ready=true`, `single_machine_verified=true`, `msix_install_verified=true`, runtime idle CPU `1/2 [HUGH_SECOND]`, runtime CPU matrix `1/2 [HUGH_SECOND]`, targeted second-PC route CPU `1/1 [HUGH_SECOND]`, all hardening/source-contract gates true, and `ready_for_public_desktop_release=false` with six blockers: multi-device, second-PC idle CPU, second-PC runtime matrix, support mailbox, Store release, and hosted P2P control-plane proof. Code audit found no high/medium issue; validation rerun passed `cargo test rpc_exec --lib` `6/6` and operator API security audit `ok=true`/`fail_count=0`. Canonical report: `docs\RELEASE_1_15_0_RC1_POST_NATIVE_RPC_EXEC_PRIMARY_EVIDENCE_AUDIT_NEXT_STEPS_2026_06_05.md` (wiki/749). |
| 2026-06-05 v575 | Code and document indexing was refreshed after the post-native-RPC evidence audit and next-step docs. | MUSU local indexer succeeded via `& "$env:LOCALAPPDATA\Microsoft\WindowsApps\musu.exe" indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` with `2433 files`, `2705 symbols`, and `34984 ms` after wiki/749, GOAL v574, the post-native-RPC evidence/audit/next-step report, network boundary spec, BETA checklist, WIKI/WIKI_INDEX, and CoS memory updates. gbrain was not rerun because the same-session active blocker remains missing `ZEROENTROPY_API_KEY`, generated/evidence import failures, `sync.last_commit` not advancing, and `gstack-brain-sync exited undefined`; `AGENTS.md` GBrain Search Guidance remains intentionally absent. Search terms should include `GOAL v575`, `wiki/749 index refresh`, `2433 files`, `2705 symbols`, `34984 ms`, `3b09dd73`, `runtime_cpu_second_pc_route_attempt_verified=true`, `HUGH-MAIN 192.168.1.192:8949 timeout`, `MUSU Desktop local executor`, `MUSU.PRO remote input control plane`, `ZEROENTROPY_API_KEY`, and `AGENTS.md GBrain Search Guidance not added`. |
| 2026-06-05 v576 | Relay connect now has an authenticated owner-scoped preflight endpoint while payload transport stays release-blocked. | `/api/v1/relay/connect` now returns `musu.relay_connect.v1` status behind P2P control auth and `POST` validates `lease_id`, `session_id`, `source_node_id`, and `target_node_id` before querying owner-scoped relay leases. Lease store failures return shaped `503 relay_connect_store_failed`. Source markers now report `RELAY_CONNECT_ENDPOINT_IMPLEMENTED=true`, `RELAY_PAYLOAD_ENDPOINT_IMPLEMENTED=false`, preview queue true, `RELAY_TRANSPORT_KIND=websocket_tunnel`, and release requirement `quic_tls_1_3`, so the old source connect placeholder blocker is gone while payload endpoint, queue-only, transport-kind, KV, live relay route, and delivery-proof blockers remain. Validation passed parser checks, `npm run test:p2p` `85/85`, `npm run test:routes` `19/19`, `npm run typecheck`, P2P store-forward audit `ok=true`, operator API security audit `ok=true`, P2P env status recheck, go/no-go dirty-tree recheck, and `git diff --check`. Code audit found no high/medium issue; this is control-plane preflight progress, not release relay payload transport. Public release remains No-Go on second-PC evidence, hosted P2P proof, support mailbox, and Store evidence. Canonical report: `docs\RELEASE_1_15_0_RC1_RELAY_CONNECT_PREFLIGHT_ENDPOINT_AUDIT_NEXT_STEPS_2026_06_05.md` (wiki/751). |
| 2026-06-05 v577 | Code and document indexing was refreshed after the relay connect preflight wiki/spec update. | MUSU local indexer succeeded via `& "$env:LOCALAPPDATA\Microsoft\WindowsApps\musu.exe" indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` with `2435 files`, `2707 symbols`, and `10569 ms` after wiki/751, GOAL v576, the relay connect preflight report, P2P control-plane spec, CONFIG, BETA checklist, WIKI/WIKI_INDEX, and CoS memory updates. gbrain was not rerun because the same-session active blocker remains missing `ZEROENTROPY_API_KEY`, generated/evidence import failures, `sync.last_commit` not advancing, and `gstack-brain-sync exited undefined`; `AGENTS.md` GBrain Search Guidance remains intentionally absent. Search terms should include `GOAL v577`, `wiki/752`, `wiki/751 index refresh`, `2435 files`, `2707 symbols`, `10569 ms`, `musu.relay_connect.v1`, `relay_connect_store_failed`, `RELAY_CONNECT_ENDPOINT_IMPLEMENTED=true`, `RELAY_PAYLOAD_ENDPOINT_IMPLEMENTED=false`, `MUSU Desktop local executor`, and `MUSU.PRO remote input control plane`. |
| 2026-06-06 v578 | Release relay payload now has a distinct fail-closed preflight endpoint separate from the preview queue. | Added `RELAY_PAYLOAD_PATH=/api/v1/relay/payload` plus `GET/POST /api/v1/relay/payload` with schema `musu.relay_payload_preflight.v1`. The endpoint requires P2P control auth, validates owner-scoped relay lease metadata, and returns `release_payload_accepted=false`, `payload_stored=false`, `payload_transported=false`, and `relay_payload_endpoint_not_wired` while `RELAY_PAYLOAD_ENDPOINT_IMPLEMENTED=false`. It does not import or call queue storage helpers, so `/api/v1/p2p/relay/payload` remains the non-release-grade store-forward preview path. `show-musu-pro-p2p-env-status.ps1` now reports `release_payload_preflight_endpoint_implemented=true`, and the P2P relay contract audit gates this separation. Validation passed parser checks, `npm run test:p2p` `88/88`, `npm run typecheck`, P2P store-forward relay contract audit `ok=true`, P2P env status recheck, and `git diff --check`. This is release endpoint contract preparation, not relay payload transport completion; public release remains No-Go on second-PC evidence, hosted P2P proof, support mailbox, and Store evidence. Canonical report: `docs\RELEASE_1_15_0_RC1_RELEASE_RELAY_PAYLOAD_PREFLIGHT_ENDPOINT_2026_06_06.md` (wiki/753). |
| 2026-06-06 v579 | Code and document indexing was refreshed after the release relay payload preflight endpoint docs. | MUSU local indexer succeeded via `& "$env:LOCALAPPDATA\Microsoft\WindowsApps\musu.exe" indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` with `2439 files`, `2717 symbols`, and `15901 ms` after wiki/753, GOAL v578, the release relay payload preflight report, P2P control-plane spec, CONFIG, BETA checklist, WIKI/WIKI_INDEX, and CoS memory updates. gbrain was not rerun because the same-session active blocker remains missing `ZEROENTROPY_API_KEY`, generated/evidence import failures, `sync.last_commit` not advancing, and `gstack-brain-sync exited undefined`; `AGENTS.md` GBrain Search Guidance remains intentionally absent. Search terms should include `GOAL v579`, `wiki/754`, `wiki/753 index refresh`, `2439 files`, `2717 symbols`, `15901 ms`, `musu.relay_payload_preflight.v1`, `RELAY_PAYLOAD_PATH=/api/v1/relay/payload`, `release_payload_preflight_endpoint_implemented=true`, `payload_stored=false`, `payload_transported=false`, `MUSU Desktop local executor`, and `MUSU.PRO remote input control plane`. |
| 2026-06-06 v580 | Fresh HUGH_SECOND packaged evidence, code audit, qualitative evaluation, and next steps were restored after release relay payload preflight. | Evidence commits `ceacf0b6`, `a4c545fc`, `c688c25c`, and `e07f0191` record current MSIX install, bridge-only single-machine smoke, desktop-open idle CPU, normal five-scenario runtime CPU matrix, and targeted HUGH-MAIN post-route CPU diagnostic. Single-machine `docs\evidence\single-machine\1.15.0-rc.1\20260606-002102-HUGH_SECOND.evidence.json` passed with `single_machine_surface=local-bridge-only`, `dashboard_required=false`, bridge `http://127.0.0.1:1421`, and CLI route checked. Idle CPU `docs\evidence\runtime-idle-cpu\1.15.0-rc.1\20260606-002155-HUGH_SECOND.desktop-open.evidence.json` passed clean git for `60.041s` with MUSU `0`, Node `0`, WebView2 `0.18`, working set `361.21MB`, and hot `0`. Normal runtime matrix `docs\evidence\runtime-cpu-scenarios\1.15.0-rc.1\20260606-003003-HUGH_SECOND.runtime-cpu-scenario-matrix.json` passed verifier `ok=true`/`fail_count=0`, route token `MUSU_CPU_SCENARIO_ROUTE_OK_20260606_003003`, route task `b3583c26-6e3f-442e-bc78-f0654e6b03c0`, max MUSU `0`, Node `0`, WebView2 `0.08`, and max working set `361.8MB`. Targeted HUGH-MAIN matrix `docs\evidence\runtime-cpu-scenarios\1.15.0-rc.1\20260606-004121-HUGH_SECOND.runtime-cpu-scenario-matrix.json` verified expected target `HUGH-MAIN` with failed route allowed; route timed out to `192.168.1.192:8949`, then post-route CPU stayed MUSU `0`, Node `0`, WebView2 `0`, hot `0`, working set `359.39MB`. Clean go/no-go after `e07f0191` reports `local_artifacts_ready=true`, `single_machine_verified=true`, `msix_install_verified=true`, runtime idle CPU `1/2 [HUGH_SECOND]`, runtime CPU matrix `1/2 [HUGH_SECOND]`, targeted second-PC route CPU `1/1 [HUGH_SECOND]`, `p2p_store_forward_relay_contract_verified=true`, `public_metadata_ok=true`, `manifest_git.dirty=false`, and `ready_for_public_desktop_release=false` with six blockers: multi-device, second-PC idle CPU, second-PC runtime matrix, hosted P2P proof, support mailbox, and Store release. Code audit found no high/medium issue; validation rerun passed `npm run test:p2p` `88/88`, `npm run typecheck`, P2P store-forward relay contract audit `ok=true`/`fail_count=0`, and matrix verifiers `ok=true`. Canonical report: `docs\RELEASE_1_15_0_RC1_POST_RELEASE_RELAY_PAYLOAD_PREFLIGHT_PRIMARY_EVIDENCE_AUDIT_NEXT_STEPS_2026_06_06.md` (wiki/755). |
| 2026-06-06 v581 | Code and document indexing was refreshed after the post release relay payload preflight primary evidence audit docs. | MUSU local indexer succeeded via `& "$env:LOCALAPPDATA\Microsoft\WindowsApps\musu.exe" indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` with `2452 files`, `2717 symbols`, and `14293 ms` after wiki/755, GOAL v580, the post release relay payload preflight primary evidence audit report, P2P control-plane spec, network boundary spec, BETA checklist, WIKI/WIKI_INDEX, and CoS memory updates. gbrain was not rerun because the same-session active blocker remains missing `ZEROENTROPY_API_KEY`, generated/evidence import failures, `sync.last_commit` not advancing, and `gstack-brain-sync exited undefined`; `AGENTS.md` GBrain Search Guidance remains intentionally absent. Search terms should include `GOAL v581`, `wiki/756`, `wiki/755 index refresh`, `2452 files`, `2717 symbols`, `14293 ms`, `20260606-002102-HUGH_SECOND`, `20260606-003003-HUGH_SECOND.runtime-cpu-scenario-matrix`, `20260606-004121-HUGH_SECOND.runtime-cpu-scenario-matrix`, `runtime_cpu_second_pc_route_attempt_verified=true`, `MUSU Desktop local executor`, `MUSU.PRO remote input control plane`, `ZEROENTROPY_API_KEY`, and `AGENTS.md GBrain Search Guidance not added`. |
| 2026-06-06 v582 | Runtime CPU evidence now requires bridge/runtime/desktop/helper subrole attribution, and fresh HUGH_SECOND evidence was restored. | Commit `ef932b5a` adds CPU subrole attribution to idle CPU evidence, runtime CPU matrices, matrix verification, go/no-go, and verifier regressions. Evidence commits `5624f4fc`, `86b97f5a`, `039050fc`, and `d2296c4c` record fresh desktop-open idle CPU, normal five-scenario runtime CPU matrix, targeted HUGH-MAIN post-route CPU diagnostic, and single-machine local bridge-only smoke. Single-machine `docs\evidence\single-machine\1.15.0-rc.1\20260606-013337-HUGH_SECOND.evidence.json` passed with `single_machine_surface=local-bridge-only`, `dashboard_required=false`, bridge `http://127.0.0.1:1421`, and CLI route checked. Idle CPU `docs\evidence\runtime-idle-cpu\1.15.0-rc.1\20260606-011243-HUGH_SECOND.desktop-open.evidence.json` passed clean git for `60.039s` with MUSU `0`, Node `0`, WebView2 `0.08`, working set `364.66MB`, hot `0`, and subrole counts `bridge_runtime=1`, `desktop_shell=1`, `webview2_helper=6`. Normal matrix `docs\evidence\runtime-cpu-scenarios\1.15.0-rc.1\20260606-012030-HUGH_SECOND.runtime-cpu-scenario-matrix.json` verified `ok=true`/`fail_count=0`, route token `MUSU_CPU_SCENARIO_ROUTE_OK_20260606_012030`, route task `b0647b86-b491-4736-8a9e-11379be7179c`, max MUSU `0`, Node `0`, WebView2 `0.1`, max working set `364.52MB`, and the same subrole process counts. Targeted HUGH-MAIN matrix `docs\evidence\runtime-cpu-scenarios\1.15.0-rc.1\20260606-012740-HUGH_SECOND.runtime-cpu-scenario-matrix.json` verified expected target `HUGH-MAIN` with failed route allowed; route timed out to `192.168.1.192:8949`, then post-route CPU stayed MUSU `0`, Node `0`, WebView2 `0.1`, hot `0`, working set `364.18MB`. Clean go/no-go after `d2296c4c` reports local artifacts, single-machine, MSIX install, targeted second-PC route CPU, P2P store-forward relay contract, and public metadata true; runtime idle CPU and runtime matrix are valid for `1/2 [HUGH_SECOND]`; `manifest_git.dirty=false`; public release remains No-Go on multi-device, second-PC idle CPU, second-PC runtime matrix, hosted P2P, support mailbox, and Store. Validation passed verifier regressions `44/44`, Rust background-loop audit `ok=true`, frontend polling audit `ok=true`, and matrix verifiers `ok=true`. Canonical report: `docs\RELEASE_1_15_0_RC1_RUNTIME_CPU_SUBROLE_ATTRIBUTION_EVIDENCE_AUDIT_NEXT_STEPS_2026_06_06.md` (wiki/757). |
| 2026-06-06 v583 | Code and document indexing was refreshed after the runtime CPU subrole attribution evidence audit docs. | MUSU local indexer succeeded via `& "$env:LOCALAPPDATA\Microsoft\WindowsApps\musu.exe" indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` with `2463 files`, `2717 symbols`, and `35338 ms` after wiki/757, GOAL v582, the runtime CPU subrole attribution evidence audit report, P2P control-plane spec, network boundary spec, BETA checklist, WIKI/WIKI_INDEX, evidence JSONs, and CoS memory updates. gbrain was not rerun because the same-session blocker remains missing `ZEROENTROPY_API_KEY`, generated/evidence import failures, `sync.last_commit` not advancing, and `gstack-brain-sync exited undefined`; `AGENTS.md` GBrain Search Guidance remains intentionally absent. Search terms should include `GOAL v583`, `wiki/758`, `wiki/757 index refresh`, `2463 files`, `2717 symbols`, `35338 ms`, `process_counts_by_subrole`, `bridge_runtime=1`, `desktop_shell=1`, `webview2_helper=6`, `20260606-013337-HUGH_SECOND`, `20260606-012030-HUGH_SECOND.runtime-cpu-scenario-matrix`, `runtime_cpu_second_pc_route_attempt_verified=true`, `MUSU Desktop local executor`, `MUSU.PRO remote input control plane`, `ZEROENTROPY_API_KEY`, and `AGENTS.md GBrain Search Guidance not added`. |
| 2026-06-06 v584 | Second-PC return imports now require current runtime CPU subrole evidence. | `run-second-pc-release-check.ps1` now records `runtime_idle_cpu_subrole_summary`, `runtime_cpu_scenario_subrole_summary`, and `runtime_cpu_subrole_contract_ok`; `import-second-pc-return.ps1 -RequireReleaseGateEvidence` now directly parses returned idle CPU and runtime matrix JSONs and rejects stale returns missing `process_counts_by_subrole`, `max_one_core_percent_by_subrole`, `memory_totals_by_subrole_mb`, CPU attribution subrole fields, `top_processes[*].process_subrole`, `bridge_runtime`, `desktop_shell`, or `webview2_helper`. Operator action pack, final operator packet, and multi-device kit templates now state old second-PC return zips without subrole fields are diagnostic only. Validation passed PowerShell parser checks and release evidence verifier regressions `45/45`. Code audit fixed one medium issue: release import trusted returned release-check booleans and file presence too much after the subrole gate. Canonical report: `docs\RELEASE_1_15_0_RC1_SECOND_PC_RUNTIME_CPU_SUBROLE_IMPORT_GATE_2026_06_06.md` (wiki/759). This does not close second-PC runtime/multi-device, hosted P2P, support mailbox, or Store blockers; it makes the next second-PC return stricter. |
| 2026-06-06 v585 | Code and document indexing was refreshed after the second-PC runtime CPU subrole import gate. | MUSU local indexer succeeded via `& "$env:LOCALAPPDATA\Microsoft\WindowsApps\musu.exe" indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` with `2466 files`, `2717 symbols`, and `8983 ms` after wiki/759, GOAL v584, the second-PC runtime CPU subrole import gate report, P2P control-plane spec, network boundary spec, BETA checklist, WIKI/WIKI_INDEX, and CoS memory updates. gbrain was not rerun because the same-session blocker remains missing `ZEROENTROPY_API_KEY`, generated/evidence import failures, `sync.last_commit` not advancing, and `gstack-brain-sync exited undefined`; the MUSU local index remains the reliable current code/document index. Search terms should include `GOAL v585`, `wiki/760`, `wiki/759 index refresh`, `2466 files`, `2717 symbols`, `8983 ms`, `runtime_cpu_subrole_contract_ok`, `runtime_idle_cpu_subrole_summary`, `runtime_cpu_scenario_subrole_summary`, `release_check_runtime_cpu_subrole_contract_ok_missing`, `verifier regressions 45/45`, `MUSU Desktop local executor`, `MUSU.PRO remote input control plane`, `ZEROENTROPY_API_KEY`, and `AGENTS.md GBrain Search Guidance not added`. |
| 2026-06-06 v586 | Final operator packet and operator action pack were refreshed after the second-PC runtime CPU subrole import gate. | Clean HEAD `a45e6a1b75a51cba4276cdf60a452041069fd6c3` produced final operator packet `.local-build\final-operator-gates\musu-final-operator-gates-1.15.0-rc.1-20260606-020415.zip` and operator action pack `.local-build\operator-action-pack\MUSU-1.15.0-rc.1-operator-action-pack-20260606-020432.zip`; both verified with `ok=true` and `fail_count=0`. The nested second-PC transfer `.local-build\operator-action-pack\MUSU-1.15.0-rc.1-operator-action-pack-20260606-020432\second-pc\MUSU-second-PC-transfer-1.15.0-rc.1-20260606-020432.zip` carries the `runtime_cpu_subrole_contract_ok` release-check contract and the current import instructions. Clean go/no-go remains `ready_for_public_desktop_release=false` with local artifacts, single-machine, MSIX install, public metadata, and clean git true; blockers remain second-PC route/CPU/matrix, hosted P2P proof, support mailbox, and Store evidence. Canonical report: `docs\RELEASE_1_15_0_RC1_FINAL_OPERATOR_PACKET_AFTER_SUBROLE_GATE_2026_06_06.md` (wiki/761). |
| 2026-06-06 v587 | Code and document indexing was refreshed after the final operator packet/action pack docs. | MUSU local indexer succeeded via `& "$env:LOCALAPPDATA\Microsoft\WindowsApps\musu.exe" indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` with `2469 files`, `2717 symbols`, and `18185 ms` after wiki/761, GOAL v586, the final operator packet/action pack report, P2P control-plane spec, network boundary spec, BETA checklist, WIKI/WIKI_INDEX, and CoS memory updates. Release evidence verifier regressions passed `ok=true`, `case_count=45`, `failed_case_count=0`. gbrain was not rerun because the same-session blocker remains missing `ZEROENTROPY_API_KEY`, generated/evidence import failures, `sync.last_commit` not advancing, and `gstack-brain-sync exited undefined`. Search terms should include `GOAL v587`, `wiki/762`, `wiki/761 index refresh`, `2469 files`, `2717 symbols`, `18185 ms`, `20260606-020415`, `20260606-020432`, `MUSU-second-PC-transfer-1.15.0-rc.1-20260606-020432.zip`, `runtime_cpu_subrole_contract_ok`, `verifier regressions 45/45`, `MUSU Desktop local executor`, and `MUSU.PRO remote input control plane`. |
| 2026-06-06 v588 | P2P relay transport kind and release encryption proof are now separate source/evidence fields. | `p2pRelayPolicy.ts` now defines `RELEASE_GRADE_RELAY_TRANSPORT_KIND=quic_relay_tunnel` separately from `RELEASE_GRADE_TRANSPORT_REQUIRED=quic_tls_1_3`, and `relayTransportKindReleaseGrade()` compares `RELAY_TRANSPORT_KIND` against the relay kind instead of the encryption/proof requirement. Relay connect, release payload preflight, and P2P relay transport status APIs now return `release_grade_relay_transport_kind` alongside `release_grade_transport_required`; `verify-p2p-control-plane-evidence.ps1` and synthetic P2P fixtures require `relay_transport_kind=quic_relay_tunnel` separately from `quic_tls_1_3`. `show-musu-pro-p2p-env-status.ps1` reports both fields and still returns `ok=false` because current source is intentionally `RELAY_TRANSPORT_KIND=websocket_tunnel`, `RELAY_PAYLOAD_ENDPOINT_IMPLEMENTED=false`, hosted KV/Upstash env is missing, and live relay route/payload delivery proof is absent. Validation passed `npm run test:p2p` `88/88`, `npm run typecheck`, P2P store-forward relay contract audit `ok=true`/`fail_count=0`, P2P env status recheck with expected blockers, and release evidence verifier regressions `ok=true`/`case_count=45`/`failed_case_count=0`. Code audit found and fixed one medium audit-layer issue: the relay contract audit still searched for the old verifier condition that treated `quic_tls_1_3` as the relay kind. This is gate/spec hardening only, not release relay payload transport; public release remains No-Go on second-PC route/CPU/matrix evidence, hosted P2P proof, support mailbox proof, and Store evidence. Canonical report: `docs\RELEASE_1_15_0_RC1_P2P_RELAY_TRANSPORT_KIND_ENCRYPTION_SPLIT_2026_06_06.md` (wiki/763). |
| 2026-06-06 v589 | Code and document indexing was refreshed after the P2P relay transport kind/encryption split. | MUSU local indexer succeeded via `& "$env:LOCALAPPDATA\Microsoft\WindowsApps\musu.exe" indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` with `2471 files`, `2717 symbols`, and `9797 ms` after wiki/763, GOAL v588, the relay transport kind/encryption split report, P2P control-plane spec, network boundary spec, BETA checklist, WIKI/WIKI_INDEX, and CoS memory updates. gbrain was not rerun because the same-session blocker remains missing `ZEROENTROPY_API_KEY`, generated/evidence import failures, `sync.last_commit` not advancing, and `gstack-brain-sync exited undefined`; the MUSU local index remains the reliable current code/document index. Search terms should include `GOAL v589`, `wiki/764`, `wiki/763 index refresh`, `2471 files`, `2717 symbols`, `9797 ms`, `release_grade_relay_transport_kind`, `RELEASE_GRADE_RELAY_TRANSPORT_KIND=quic_relay_tunnel`, `RELEASE_GRADE_TRANSPORT_REQUIRED=quic_tls_1_3`, `relay_transport_kind=websocket_tunnel`, `MUSU Desktop local executor`, `MUSU.PRO remote input control plane`, `ZEROENTROPY_API_KEY`, and `AGENTS.md GBrain Search Guidance not added`. |
| 2026-06-06 v590 | Release relay payload preflight now rejects payload bytes before lease lookup. | `/api/v1/relay/payload` remains metadata-only while `RELAY_PAYLOAD_ENDPOINT_IMPLEMENTED=false`; it now rejects known byte fields `payload`, `payload_base64`, `payload_b64`, `payload_bytes`, and `body_base64` with `400 release_payload_bytes_not_accepted` before querying owner-scoped relay leases. Metadata-only preflight still verifies a lease and returns `409 relay_payload_endpoint_not_wired` because the release tunnel transport is not implemented. The route now bounds lease/session/node/tunnel metadata and requires `payload_sha256` to be a 64-character hex digest when supplied. `audit-p2p-store-forward-relay-contract.ps1` gates the byte-rejection contract so the release preflight endpoint cannot drift into accepting payload bytes or reuse queue storage. Validation passed `npm run test:p2p` `89/89`, `npm run typecheck`, P2P store-forward relay contract audit `ok=true`/`fail_count=0`, P2P env status recheck with expected blockers, and release evidence verifier regressions `ok=true`/`case_count=45`/`failed_case_count=0`. Code audit found no high/medium issue; this hardens the release/web boundary but does not close release relay payload transport, hosted P2P, second-PC, support mailbox, or Store gates. Canonical report: `docs\RELEASE_1_15_0_RC1_RELEASE_RELAY_PAYLOAD_PREFLIGHT_BYTE_REJECTION_2026_06_06.md` (wiki/765). |
| 2026-06-06 v591 | Code and document indexing was refreshed after release relay payload preflight byte rejection. | MUSU local indexer succeeded via `& "$env:LOCALAPPDATA\Microsoft\WindowsApps\musu.exe" indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` with `2474 files`, `2719 symbols`, and `11548 ms` after wiki/765, GOAL v590, the release relay payload preflight byte rejection report, P2P control-plane spec, BETA checklist, WIKI/WIKI_INDEX, and CoS memory update. gbrain was not rerun because the same-session blocker remains missing `ZEROENTROPY_API_KEY`, generated/evidence import failures, `sync.last_commit` not advancing, and `gstack-brain-sync exited undefined`; the MUSU local index remains the reliable current code/document index. Search terms should include `GOAL v591`, `wiki/766`, `wiki/765 index refresh`, `2474 files`, `2719 symbols`, `11548 ms`, `release_payload_bytes_not_accepted`, `FORBIDDEN_RELEASE_PAYLOAD_BYTE_FIELDS`, `payload_base64`, `metadata-only preflight`, `MUSU Desktop local executor`, `MUSU.PRO remote input control plane`, `ZEROENTROPY_API_KEY`, and `AGENTS.md GBrain Search Guidance not added`. |
| 2026-06-06 v592 | Release relay payload preflight now uses a strict metadata schema. | `/api/v1/relay/payload` changed from passthrough parsing to a strict metadata-only request contract while `RELAY_PAYLOAD_ENDPOINT_IMPLEMENTED=false`. It accepts only optional `schema=musu.relay_payload_preflight_request.v1`, `lease_id`, `session_id`, `source_node_id`, `target_node_id`, optional `tunnel_id`, optional `payload_kind`, and optional 64-hex `payload_sha256`. Known byte fields still fail first with `release_payload_bytes_not_accepted`; other unexpected fields now fail with `invalid_relay_payload_preflight_request` instead of reaching owner-scoped lease lookup. `audit-p2p-store-forward-relay-contract.ps1` now gates `.strict()`, the request schema literal, and unknown-field regression coverage. Validation passed `npm run test:p2p` `90/90`, `npm run typecheck`, P2P store-forward relay contract audit `ok=true`/`fail_count=0`, release evidence verifier regression `ok=true`/`case_count=45`/`failed_case_count=0`, and P2P env status recheck with expected release relay payload endpoint, KV/Upstash, live relay route, and relay payload delivery proof blockers. Code audit found no high/medium issue; this hardens the release/web boundary without closing release relay payload transport, hosted P2P, second-PC, support mailbox, or Store gates. Canonical report: `docs\RELEASE_1_15_0_RC1_RELEASE_RELAY_PAYLOAD_PREFLIGHT_STRICT_METADATA_SCHEMA_2026_06_06.md` (wiki/767). |
| 2026-06-06 v593 | Code and document indexing was refreshed after release relay payload strict metadata hardening. | MUSU local indexer succeeded via `& "$env:LOCALAPPDATA\Microsoft\WindowsApps\musu.exe" indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` with `2477 files`, `2719 symbols`, and `10589 ms` after wiki/767, GOAL v592, the strict metadata schema report, P2P control-plane spec, BETA checklist, WIKI/WIKI_INDEX, existing next-steps handoff note, and CoS memory updates. gbrain was not rerun because the same-session blocker remains missing `ZEROENTROPY_API_KEY`, generated/evidence import failures, `sync.last_commit` not advancing, and `gstack-brain-sync exited undefined`; the MUSU local index remains the reliable current code/document index. Search terms should include `GOAL v593`, `wiki/768`, `strict metadata index refresh`, `2477 files`, `2719 symbols`, `10589 ms`, `musu.relay_payload_preflight_request.v1`, `invalid_relay_payload_preflight_request`, `release verifier 45/45`, `P2P env status ok=false`, `ZEROENTROPY_API_KEY`, and `AGENTS.md GBrain Search Guidance not added`. |
| 2026-06-06 v594 | Runtime idle CPU go/no-go now requires full MUSU/node/WebView2/other role attribution. | `write-release-go-no-go.ps1` now uses `Test-ObjectHasPropertyNames` in `Test-RuntimeIdleCpuEvidence` and fails single `desktop-open` idle CPU evidence unless `process_counts_by_role`, `cpu_attribution.sample_count_by_role`, `cpu_attribution.total_cpu_seconds_by_role`, and `cpu_attribution.max_one_core_percent_by_role` all include `musu`, `node`, `webview2`, and `other`. Existing subrole checks for `bridge_runtime`, `desktop_shell`, `webview2_helper`, `node_helper`, `musu_runtime`, and `other` remain in force. `test-release-evidence-verifiers.ps1` added source-contract case `go-no-go runtime idle CPU requires full role attribution`, raising verifier regressions to `ok=true`/`case_count=46`/`failed_case_count=0`. Dirty-tree go/no-go with public metadata skipped confirms the stricter verifier still counts current HUGH_SECOND idle CPU evidence as `1/2`, runtime matrix `1/2`, targeted second-PC route CPU `true`, and idle busy-loop candidate contract `true`; public release remains No-Go on second-PC route/CPU/matrix, hosted P2P proof, support mailbox, Store proof, and current dirty git until committed. Code audit found no high/medium issue. Canonical report: `docs\RELEASE_1_15_0_RC1_RUNTIME_IDLE_CPU_FULL_ROLE_ATTRIBUTION_GATE_2026_06_06.md` (wiki/769). |
| 2026-06-06 v595 | Code and document indexing was refreshed after runtime idle CPU full-role attribution hardening. | MUSU local indexer succeeded via `& "$env:LOCALAPPDATA\Microsoft\WindowsApps\musu.exe" indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` with `2480 files`, `2719 symbols`, and `9798 ms` after wiki/769, GOAL v594, the runtime idle CPU full-role attribution report, BETA checklist, WIKI/WIKI_INDEX, release verifier source-contract update, and CoS memory update. gbrain was not rerun because the same-session blocker remains missing `ZEROENTROPY_API_KEY`, generated/evidence import failures, `sync.last_commit` not advancing, and `gstack-brain-sync exited undefined`; the MUSU local index remains the reliable current code/document index. Search terms should include `GOAL v595`, `wiki/770`, `runtime idle CPU full role attribution index refresh`, `2480 files`, `2719 symbols`, `9798 ms`, `Test-ObjectHasPropertyNames`, `go-no-go runtime idle CPU requires full role attribution`, `release verifier 46/46`, `runtime_idle_cpu_valid_machine_count=1/2`, `ZEROENTROPY_API_KEY`, and `AGENTS.md GBrain Search Guidance not added`. |
| 2026-06-06 v596 | Rust background-loop audit now gates filesystem watcher scope. | `audit-rust-background-loop-contract.ps1` now requires filesystem watcher primitives to stay in `musu-rs/src/indexer/watch.rs` or `musu-rs/src/install/sync.rs`, proves `musu indexer watch` remains an explicit CLI subcommand, proves the default bridge/runtime path does not call the indexer watcher, and allows file-sync watcher starts only from the configured bridge path or explicit `musu sync` CLI. The audit now emits `filesystem_watcher_primitive_hit_count` and `file_sync_watcher_start_hit_count`; both are `0` on current source. `test-release-evidence-verifiers.ps1` added source-contract case `rust background audit limits filesystem watcher scope`, raising verifier regressions to `ok=true`/`case_count=47`/`failed_case_count=0`; final operator packet verification now checks the strengthened watcher-scope audit strings. Dirty-tree go/no-go with public metadata skipped confirms `rust_background_loop_contract_verified=true`, runtime idle CPU `1/2`, runtime matrix `1/2`, and public release remains No-Go on second-PC, hosted P2P, support mailbox, and Store blockers. Code audit found no high/medium runtime issue; this closes an audit-layer gap only. Canonical report: `docs\RELEASE_1_15_0_RC1_FILESYSTEM_WATCHER_SCOPE_CONTRACT_GATE_2026_06_06.md` (wiki/771). |
| 2026-06-06 v597 | Code and document indexing was refreshed after filesystem watcher scope hardening. | MUSU local indexer succeeded via `& "$env:LOCALAPPDATA\Microsoft\WindowsApps\musu.exe" indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` with `2483 files`, `2719 symbols`, and `10455 ms` after wiki/771, GOAL v596, the filesystem watcher scope contract report, BETA checklist, network boundary spec, WIKI/WIKI_INDEX, changed release verifier scripts, and CoS memory update. gbrain was not rerun because the same-session blocker remains missing `ZEROENTROPY_API_KEY`, generated/evidence import failures, `sync.last_commit` not advancing, and `gstack-brain-sync exited undefined`; the MUSU local index remains the reliable current code/document index. Search terms should include `GOAL v597`, `wiki/772`, `filesystem watcher scope index refresh`, `2483 files`, `2719 symbols`, `10455 ms`, `filesystem_watcher_primitive_hit_count=0`, `file_sync_watcher_start_hit_count=0`, `rust background audit limits filesystem watcher scope`, `release verifier 47/47`, `ZEROENTROPY_API_KEY`, and `AGENTS.md GBrain Search Guidance not added`. |
| 2026-06-06 v598 | Rust background-loop audit now gates network watcher scope. | `audit-rust-background-loop-contract.ps1` now proves active mDNS discovery is scoped to explicit `musu discover`, scans for network watcher/poller primitives including `poll_device_token`, `discover_peers`, `auto_register_peers`, relay payload query/claim/deliver calls, relay payload poller calls, `tokio::time::interval`, `IntervalStream::new`, mDNS `recv_timeout`, and the low-duty cloud registration loop marker, and rejects new hits outside `$allowedNetworkWatcherFiles`. The audit now emits `network_watcher_primitive_hit_count`, which is `0` on current source. `test-release-evidence-verifiers.ps1` added source-contract case `rust background audit limits network watcher scope`, raising verifier regressions to `ok=true`/`case_count=48`/`failed_case_count=0`; final operator packet verification now checks the network watcher scope audit strings. Code audit found no high/medium runtime issue; this closes an audit-layer gap only. Canonical report: `docs\RELEASE_1_15_0_RC1_NETWORK_WATCHER_SCOPE_CONTRACT_GATE_2026_06_06.md` (wiki/773). |
| 2026-06-06 v599 | Code and document indexing was refreshed after network watcher scope hardening. | MUSU local indexer succeeded via `& "$env:LOCALAPPDATA\Microsoft\WindowsApps\musu.exe" indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` with `2486 files`, `2719 symbols`, and `10887 ms` after wiki/773, GOAL v598, the network watcher scope contract report, BETA checklist, network boundary spec, WIKI/WIKI_INDEX, changed release verifier scripts, and CoS memory update. gbrain was not rerun because the same-session blocker remains missing `ZEROENTROPY_API_KEY`, generated/evidence import failures, `sync.last_commit` not advancing, and `gstack-brain-sync exited undefined`; the MUSU local index remains the reliable current code/document index. Search terms should include `GOAL v599`, `wiki/774`, `network watcher scope index refresh`, `2486 files`, `2719 symbols`, `10887 ms`, `network_watcher_primitive_hit_count=0`, `rust background audit limits network watcher scope`, `release verifier 48/48`, `ZEROENTROPY_API_KEY`, and `AGENTS.md GBrain Search Guidance not added`. |
| 2026-06-06 v600 | Degraded/fallback truthfulness is now a release contract. | Added `audit-degraded-mode-contract.ps1` with schema `musu.degraded_mode_contract.v1` and wired it into go/no-go as `degraded_mode_contract_verified` with blocker area `degraded-mode`, plus handoff status, final operator packet generation/verification, readiness audit, and release verifier source-contract coverage. `/api/device-status` now returns local `/status` metrics, `/health` fallback, explicit `offline-fallback`, `recommended_for`, `degraded`, `degradedReason`, and `devices`; `useDeviceDiscovery` accepts the new envelope and legacy bare arrays. `npm run test:routes` now includes agents and device-status degraded/fallback regressions, with test-only `server-only` no-op shims for Node route tests. Validation passed PowerShell parser checks, `npm run test:routes` `28/28`, `npm run typecheck`, degraded-mode audit `ok=true`/`fail_count=0`, and release verifier regressions `ok=true`/`case_count=51`/`failed_case_count=0`. Code audit found and fixed one medium contract drift: `/api/device-status` returned only fleet array state while `@route` and tests expected local status/fallback fields. Public release remains No-Go on second-PC route/CPU/matrix evidence, hosted P2P proof, support mailbox, and Store evidence. Canonical report: `docs\RELEASE_1_15_0_RC1_DEGRADED_MODE_CONTRACT_GATE_2026_06_06.md` (wiki/775). |
| 2026-06-06 v601 | Code and document indexing was refreshed after the degraded-mode contract gate. | MUSU local indexer succeeded via `& "$env:LOCALAPPDATA\Microsoft\WindowsApps\musu.exe" indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` with `2491 files`, `2731 symbols`, and `11348 ms` after wiki/775, GOAL v600, the degraded-mode contract report, degraded-mode next-steps report, BETA checklist, network boundary spec, WIKI/WIKI_INDEX, release verifier source-contract updates, and CoS memory update. gbrain was not rerun because the same-session blocker remains missing `ZEROENTROPY_API_KEY`, generated/evidence import failures, `sync.last_commit` not advancing, and `gstack-brain-sync exited undefined`; the MUSU local index remains the reliable current code/document index. Search terms should include `GOAL v601`, `wiki/776`, `degraded mode contract index refresh`, `2491 files`, `2731 symbols`, `11348 ms`, `musu.degraded_mode_contract.v1`, `degraded_mode_contract_verified`, `health-fallback`, `offline-fallback`, `DeviceStatusResponse`, `release verifier 51/51`, `MUSU Desktop local executor`, `MUSU.PRO remote input control plane`, and `AGENTS.md GBrain Search Guidance not added`. |
| 2026-06-06 v602 | Clean go/no-go after the degraded-mode commit confirmed the new gate and reset current-source runtime evidence. | Clean HEAD `f8c8e4ed3ee23a00a4657e5753ed25954f38bcf8` with public metadata skipped reports `degraded_mode_contract_verified=true`, `frontend_polling_contract_verified=true`, `rust_background_loop_contract_verified=true`, `idle_busy_loop_candidate_contract_verified=true`, `local_api_auth_contract_verified=true`, `operator_api_security_contract_verified=true`, `p2p_store_forward_relay_contract_verified=true`, `secret_storage_contract_verified=true`, `local_artifacts_ready=true`, `msix_install_verified=true`, and `manifest_dirty=false`. Because this commit changed Next/API source, current-source evidence honestly reset to `single_machine_verified=false`, runtime idle CPU `0/2`, runtime CPU scenario matrix `0/2`, and targeted second-PC route CPU `false`. Next action is a fresh primary packaged single-machine/desktop-open CPU/runtime matrix/targeted-route CPU evidence refresh before the second-PC and hosted P2P gates. |
| 2026-06-06 v603 | Code and document indexing was refreshed after documenting clean go/no-go for the degraded-mode gate. | MUSU local indexer succeeded via `& "$env:LOCALAPPDATA\Microsoft\WindowsApps\musu.exe" indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` with `2493 files`, `2731 symbols`, and `10272 ms` after wiki/777, GOAL v602, clean go/no-go memory, degraded-mode report/next-steps updates, BETA checklist, WIKI/WIKI_INDEX, and CoS memory update. Search terms should include `GOAL v603`, `wiki/778`, `degraded mode clean go/no-go index refresh`, `2493 files`, `2731 symbols`, `10272 ms`, `f8c8e4ed3ee23a00a4657e5753ed25954f38bcf8`, `single_machine_verified=false`, `runtime idle CPU 0/2`, `runtime CPU scenario matrix 0/2`, `degraded_mode_contract_verified=true`, and `fresh primary packaged evidence required`. |
| 2026-06-06 v604 | Fresh primary packaged runtime evidence was restored after the degraded/fallback gate. | Packaged runtime repair restarted the WindowsApps runtime at bridge `http://127.0.0.1:4760` with PID `32780` and `dashboard.required=false`. Single-machine smoke passed as `local-bridge-only` at `docs\evidence\single-machine\1.15.0-rc.1\20260606-043041-HUGH_SECOND.evidence.json`. Desktop-open idle CPU passed at `docs\evidence\runtime-idle-cpu\1.15.0-rc.1\20260606-043051-HUGH_SECOND.desktop-open.evidence.json` with `git_dirty=false`, MUSU `0`, Node `0`, WebView2 `0.03`, hot `0`, and working set `369.01MB`. Full runtime matrix passed at `docs\evidence\runtime-cpu-scenarios\1.15.0-rc.1\20260606-043203-HUGH_SECOND.runtime-cpu-scenario-matrix.json` with route token `MUSU_CPU_SCENARIO_ROUTE_OK_20260606_043203`, max WebView2 `0.1`, and max working set `371.01MB`. Targeted `HUGH-MAIN` post-route CPU evidence passed CPU verification at `20260606-043811-HUGH_SECOND.runtime-cpu-scenario-matrix.json`; the actual route to `192.168.1.192:8949` still timed out, so this is not multi-device success evidence. Canonical report: `docs\RELEASE_1_15_0_RC1_POST_DEGRADED_GATE_PRIMARY_EVIDENCE_REFRESH_2026_06_06.md` (wiki/779). |
| 2026-06-06 v605 | Code, evidence, and documents were indexed after the post-degraded primary evidence refresh. | MUSU local indexer succeeded with `2510 files`, `2731 symbols`, and `18799 ms` after GOAL v604/wiki/779, fresh primary evidence, BETA checklist, network boundary spec, runtime hardening roadmap, WIKI/WIKI_INDEX, and CoS memories. Clean go/no-go on `02c24fd3d712b1fb10fb36cd59165427f57d722a` reports `local_artifacts_ready=true`, `single_machine_verified=true`, runtime idle CPU `1/2`, runtime CPU matrix `1/2`, targeted second-PC route CPU `true`, hardening contracts true, `p2p_control_plane_verified=false`, and `manifest_git.dirty=false`. Public release remains No-Go on second-PC multi-device/CPU/matrix evidence, public metadata recheck, support mailbox, Store, and hosted MUSU.PRO P2P/relay proof. Search terms should include `GOAL v605`, `wiki/780`, `2510 files`, `2731 symbols`, `18799 ms`, `02c24fd3`, `single_machine_verified=true`, `runtime idle CPU 1/2`, `runtime CPU matrix 1/2`, and `targeted second-PC route CPU true`. |
| 2026-06-06 v606 | Current P2P control-plane status, code audit, qualitative assessment, and next steps were documented. | Rechecked clean HEAD `eb8484ff4ab29a8db6c7f5b5f6841f7e246dd438`: go/no-go with public metadata skipped remains `ready_for_public_desktop_release=false`, `local_artifacts_ready=true`, `single_machine_verified=true`, runtime idle CPU `1/2`, runtime CPU matrix `1/2`, targeted second-PC route CPU `true`, `p2p_control_plane_verified=false`, and `manifest_git.dirty=false`. `show-musu-pro-p2p-env-status.ps1 -Json` remains `ok=false`: store-forward queue fallback and release payload preflight are audited, but release relay payload endpoint/tunnel transport, KV/Upstash, live relay route proof, and relay payload delivery proof are still missing. Validation passed `npm run test:p2p` `90/90`, `npm run typecheck`, P2P store-forward relay contract audit `ok=true`, Rust background-loop audit `ok=true` with unaudited loop/spawn/network watcher hits `0`, release verifier regressions `51/51`, `cargo test --lib relay_payload` `24/24`, `cargo check --bin musu`, and `git diff --check`. Code audit found no high/medium issue; residual risk is evidence/deployment: second-PC direct route/CPU/matrix, live hosted P2P proof, support mailbox, and Store. Product boundary stays unchanged: MUSU Desktop executes locally; MUSU.PRO is remote input/project room/rendezvous/path-selection/relay-fallback/evidence control plane and web-assisted P2P bootstrap. Canonical report: `docs\RELEASE_1_15_0_RC1_CURRENT_P2P_CONTROL_PLANE_CODE_AUDIT_NEXT_STEPS_2026_06_06.md` (wiki/781). |
| 2026-06-06 v607 | Code and documents were indexed after the current P2P control-plane audit docs/spec update. | MUSU local indexer succeeded via `& "$env:LOCALAPPDATA\Microsoft\WindowsApps\musu.exe" indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` with `2513 files`, `2731 symbols`, and `9304 ms` after GOAL v606/wiki/781, the current P2P control-plane code audit/next-steps report, P2P control-plane spec, network boundary spec, BETA checklist, WIKI/WIKI_INDEX, and CoS memory update. Search terms should include `GOAL v607`, `wiki/782`, `current P2P control-plane code audit index refresh`, `2513 files`, `2731 symbols`, `9304 ms`, `show-musu-pro-p2p-env-status ok=false`, `release verifier 51/51`, `MUSU Desktop local executor`, and `MUSU.PRO remote input control plane`. |
| 2026-06-06 v608 | Final operator packet generation now requires the current P2P audit report, and current operator artifacts were refreshed. | `prepare-final-operator-gate-packet.ps1` now copies `RELEASE_1_15_0_RC1_CURRENT_P2P_CONTROL_PLANE_CODE_AUDIT_NEXT_STEPS_2026_06_06.md`, and `verify-final-operator-gate-packet.ps1` requires both the doc entry and README reference. Validation passed parser checks, `git diff --check`, and release verifier regressions `51/51`. Clean source commit `b71c438bb764483b206d5da4e105744f796df58f` generated final operator packet `.local-build\final-operator-gates\musu-final-operator-gates-1.15.0-rc.1-20260606-051216.zip`, operator action pack `.local-build\operator-action-pack\MUSU-1.15.0-rc.1-operator-action-pack-20260606-051242.zip`, second-PC transfer `.local-build\operator-action-pack\MUSU-1.15.0-rc.1-operator-action-pack-20260606-051242\second-pc\MUSU-second-PC-transfer-1.15.0-rc.1-20260606-051242.zip`, Partner Center zip `MUSU-1.15.0-rc.1-store-submission-20260606-051242.zip`, and support id `musu-store-support-1.15.0-rc.1-20260606-051216`; final packet and action pack verifiers both passed `ok=true`, `fail_count=0`. Clean go/no-go remains No-Go with local artifacts/single-machine true, runtime idle CPU `1/2`, runtime matrix `1/2`, targeted second-PC route CPU true, `p2p_control_plane_verified=false`, and `manifest_git.dirty=false`. Canonical report: `docs\RELEASE_1_15_0_RC1_FINAL_OPERATOR_PACKET_AFTER_CURRENT_P2P_AUDIT_2026_06_06.md` (wiki/783). |
| 2026-06-06 v609 | Code and documents were indexed after the final operator packet/current P2P audit refresh. | MUSU local indexer succeeded with `2516 files`, `2731 symbols`, and `10322 ms` after GOAL v608/wiki/783, the final operator packet after current P2P audit report, BETA checklist, WIKI/WIKI_INDEX, and CoS memory update. Search terms should include `GOAL v609`, `wiki/784`, `final operator packet after current P2P audit index refresh`, `2516 files`, `2731 symbols`, `10322 ms`, `20260606-051216`, `20260606-051242`, `MUSU-second-PC-transfer-1.15.0-rc.1-20260606-051242.zip`, `MUSU Desktop local executor`, and `MUSU.PRO remote input control plane`. |
| 2026-06-06 v610 | MUSU.PRO room work-order handoff now writes privacy-preserving command audit events. | `POST /api/rooms/[roomId]/work-orders` now writes `rooms.work_orders` events to command-center audit JSONL after P2P control auth for accepted bridge forwarding, bridge errors, invalid JSON, and missing instruction. The event records owner key, room/work-order/company/project context, target node, `origin=musu.pro`, result, HTTP status, bridge status, and trace id while deliberately excluding `instruction`/`text`. `ControlAuditEvent` now supports optional P2P room/work-order fields, and `audit-operator-api-security-contract.ps1` gates the room work-order auth + command audit contract. Validation passed `npm run test:routes` `29/29`, `npm run test:p2p` `90/90`, `npm run typecheck`, operator API security audit `ok=true`/`fail_count=0`, release verifier regressions `51/51`, and `git diff --check`. Dirty-tree go/no-go with public metadata skipped kept `operator_api_security_contract_verified=true`, local artifacts/single-machine true, runtime idle CPU `1/2`, runtime matrix `1/2`, targeted second-PC route CPU true, `p2p_control_plane_verified=false`, and `ready_for_public_desktop_release=false`, with the expected dirty git blocker until commit. Canonical report: `docs\RELEASE_1_15_0_RC1_ROOM_WORK_ORDER_COMMAND_AUDIT_2026_06_06.md` (wiki/785). |
| 2026-06-06 v611 | Code and documents were indexed after room work-order command audit hardening. | MUSU local indexer succeeded with `2519 files`, `2732 symbols`, and `9705 ms` after GOAL v610/wiki/785, the room work-order command audit report, P2P control-plane spec, network boundary spec, BETA checklist, WIKI/WIKI_INDEX, and CoS memory updates. Search terms should include `GOAL v611`, `wiki/786`, `room work-order command audit index refresh`, `2519 files`, `2732 symbols`, `9705 ms`, `rooms.work_orders`, `room.work_order`, `appendControlAudit`, `p2pControlPrincipal`, `command-center.jsonl`, `instruction text excluded`, `operator_api_security_contract_verified=true`, `MUSU.PRO remote input`, and `MUSU Desktop local executor`. |
| 2026-06-06 v612 | Fresh primary packaged runtime evidence was restored after the room work-order command audit source change. | Evidence commits `b8708596`, `c3b39701`, `64000ae3`, and `7f3879fc` restored current HUGH_SECOND primary evidence after the API source change. Single-machine `docs\evidence\single-machine\1.15.0-rc.1\20260606-053851-HUGH_SECOND.evidence.json` passed as `local-bridge-only` with bridge `http://127.0.0.1:3622` and CLI route checked. Desktop-open idle CPU `docs\evidence\runtime-idle-cpu\1.15.0-rc.1\20260606-054220-HUGH_SECOND.desktop-open.evidence.json` passed for `60.04s` with MUSU `0`, Node `0`, WebView2 `0.08`, and hot `0`. Full runtime matrix `docs\evidence\runtime-cpu-scenarios\1.15.0-rc.1\20260606-054415-HUGH_SECOND.runtime-cpu-scenario-matrix.json` passed with `fail_count=0` and route token `MUSU_CPU_SCENARIO_ROUTE_OK_20260606_054415`. Targeted HUGH-MAIN CPU diagnostic `docs\evidence\runtime-cpu-scenarios\1.15.0-rc.1\20260606-055030-HUGH_SECOND.runtime-cpu-scenario-matrix.json` passed CPU verification with failed route allowed; the actual route still timed out to `192.168.1.192:8949`, so this is not multi-device success evidence. Clean go/no-go after `7f3879fc` reports local artifacts true, single-machine true, runtime idle CPU `1/2`, runtime CPU matrix `1/2`, targeted second-PC route CPU true, operator API security true, `p2p_control_plane_verified=false`, `manifest_git.dirty=false`, and public release No-Go. Canonical report: `docs\RELEASE_1_15_0_RC1_POST_ROOM_WORK_ORDER_COMMAND_AUDIT_PRIMARY_EVIDENCE_REFRESH_2026_06_06.md` (wiki/787). |
| 2026-06-06 v613 | Code, evidence, and documents were indexed after the post room work-order command audit primary evidence refresh. | MUSU local indexer succeeded with `2530 files`, `2732 symbols`, and `12816 ms` after GOAL v612/wiki/787, fresh single-machine/idle CPU/runtime matrix/targeted route CPU evidence, the post room work-order command audit primary evidence report, BETA checklist, WIKI/WIKI_INDEX, and CoS memory updates. Search terms should include `GOAL v613`, `wiki/788`, `post room work-order command audit primary evidence index refresh`, `2530 files`, `2732 symbols`, `12816 ms`, `20260606-053851-HUGH_SECOND`, `20260606-054220-HUGH_SECOND.desktop-open`, `20260606-054415-HUGH_SECOND.runtime-cpu-scenario-matrix`, `MUSU_CPU_SCENARIO_ROUTE_OK_20260606_054415`, `20260606-055030-HUGH_SECOND`, `targeted second-PC route CPU true`, and `p2p_control_plane_verified=false`. |
| 2026-06-06 v614 | Final operator packet and action pack were refreshed after the room work-order command audit evidence reset. | Clean source commit `847aa2c0cb6979a62c967c2f3c4a20a4195075f2` generated final operator packet `.local-build\final-operator-gates\musu-final-operator-gates-1.15.0-rc.1-20260606-060037.zip`, action pack `.local-build\operator-action-pack\MUSU-1.15.0-rc.1-operator-action-pack-20260606-060103.zip`, second-PC transfer `.local-build\operator-action-pack\MUSU-1.15.0-rc.1-operator-action-pack-20260606-060103\second-pc\MUSU-second-PC-transfer-1.15.0-rc.1-20260606-060103.zip`, Partner Center zip `MUSU-1.15.0-rc.1-store-submission-20260606-060103.zip`, and support id `musu-store-support-1.15.0-rc.1-20260606-060037`. Final packet verification passed `ok=true`, `fail_count=0`, `kit_count=1`; action pack generation and verification passed `ok=true`, `fail_count=0`; final handoff quick status verified both packet and action pack. Qualitative code audit found no new high/medium issue in the room work-order handoff/operator packet path. Public release remains No-Go with single-machine true, runtime idle CPU `1/2`, runtime matrix `1/2`, targeted second-PC route CPU true, `p2p_control_plane_verified=false`, public metadata skipped, and seven blockers. Canonical report: `docs\RELEASE_1_15_0_RC1_FINAL_OPERATOR_PACKET_AFTER_ROOM_WORK_ORDER_COMMAND_AUDIT_2026_06_06.md` (wiki/789). |
| 2026-06-06 v615 | Code and documents were indexed after the current final operator packet/action pack refresh. | MUSU local indexer succeeded with `2534 files`, `2732 symbols`, and `11243 ms` after GOAL v614/wiki/789, the current final operator packet/action pack report, BETA checklist, WIKI/WIKI_INDEX, and CoS memory updates. gbrain was not rerun because the same-session blocker remains missing `ZEROENTROPY_API_KEY`, generated/evidence import failures, `sync.last_commit` not advancing, and `gstack-brain-sync exited undefined`; the MUSU local index is the current reliable code/document index. Search terms should include `GOAL v615`, `wiki/790`, `final operator packet after room work-order command audit index refresh`, `2534 files`, `2732 symbols`, `11243 ms`, `20260606-060037`, `20260606-060103`, `MUSU-second-PC-transfer-1.15.0-rc.1-20260606-060103.zip`, `room work-order command audit`, `MUSU.PRO remote input`, `MUSU Desktop local executor`, and `p2p_control_plane_verified=false`. |
| 2026-06-06 v616 | Runtime CPU matrix evidence now requires scoped process metadata and was refreshed on HUGH_SECOND. | Commit `525f5b0bdbeff99ec89ff37b99cfe30c0a13b5b5` strengthened `verify-runtime-cpu-scenario-matrix.ps1` so every scenario must prove `process_metadata_available=true`, `process_metadata_timed_out=false`, `helper_process_scope=musu_process_tree_or_repo_related`, and `cpu_attribution.attribution_scope=musu_process_tree_or_repo_related`. `measure-musu-runtime-cpu-scenarios.ps1` now carries those fields into matrix summaries, and release verifier regressions added missing/timed-out/unscoped negative cases. Validation passed parser checks, release verifier regressions `ok=true`/`case_count=54`/`failed_case_count=0`, old matrix `20260606-054415-HUGH_SECOND` failed the new current verifier with `ok=false`/`fail_count=15`, and new evidence passed: full matrix `docs\evidence\runtime-cpu-scenarios\1.15.0-rc.1\20260606-061932-HUGH_SECOND.runtime-cpu-scenario-matrix.json` (`260` checks, route token `MUSU_CPU_SCENARIO_ROUTE_OK_20260606_061932`) and targeted HUGH-MAIN CPU diagnostic `20260606-062729-HUGH_SECOND.runtime-cpu-scenario-matrix.json` (`65` checks, failed route allowed, timeout to `192.168.1.192:8949`). Code audit found no high/medium issue; product boundary is unchanged: MUSU Desktop executes locally, MUSU.PRO coordinates remote input/rooms/rendezvous/path selection/relay fallback/evidence. This is stricter CPU attribution evidence, not successful second-PC route evidence. Canonical report: `docs\RELEASE_1_15_0_RC1_RUNTIME_CPU_MATRIX_PROCESS_METADATA_GATE_2026_06_06.md` (wiki/791). |
| 2026-06-06 v617 | Code, evidence, and documents were indexed after the runtime CPU process metadata gate. | MUSU local indexer succeeded via `& "$env:LOCALAPPDATA\Microsoft\WindowsApps\musu.exe" indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` with `2540 files`, `2732 symbols`, and `15551 ms` after GOAL v616/wiki/791, fresh runtime CPU matrix evidence, the runtime CPU matrix process metadata gate report, BETA checklist, network boundary spec, WIKI/WIKI_INDEX, changed verifier scripts, and CoS memory updates. gbrain was not rerun because the same-session blocker remains missing `ZEROENTROPY_API_KEY`, generated/evidence import failures, `sync.last_commit` not advancing, and `gstack-brain-sync exited undefined`; the MUSU local index is the current reliable code/document index. Search terms should include `GOAL v617`, `wiki/792`, `runtime CPU matrix process metadata gate index refresh`, `2540 files`, `2732 symbols`, `15551 ms`, `process_metadata_available`, `helper_process_scope=musu_process_tree_or_repo_related`, `cpu_attribution.attribution_scope=musu_process_tree_or_repo_related`, `20260606-061932-HUGH_SECOND`, `20260606-062729-HUGH_SECOND`, and `release verifier 54/54`. |
| 2026-06-06 v618 | Relay connect preflight now rejects payload bytes and unknown release fields. | `POST /api/v1/relay/connect` now accepts only strict metadata for release connect preflight: optional `schema=musu.relay_connect_request.v1`, `lease_id`, `session_id`, `source_node_id`, and `target_node_id`. Known byte fields (`payload`, `payload_base64`, `payload_b64`, `payload_bytes`, `body_base64`) are rejected before lease lookup with `relay_connect_payload_bytes_not_accepted`, and unknown fields now fail strict parsing instead of passing through. P2P store-forward relay contract audit now gates `release connect preflight fails closed and accepts metadata only` and `release connect preflight regression coverage`. Validation passed PowerShell parser, `npm run test:p2p -- --test-name-pattern "relay connect"` with `92/92`, `npm run typecheck`, and P2P store-forward relay audit `ok=true`/`fail_count=0`. Code audit found no high/medium issue. This hardens MUSU.PRO control-plane input and does not implement release relay tunnel payload transport; public release remains No-Go on second-PC route/CPU/matrix, hosted P2P release proof, support mailbox, and Store evidence. Canonical report: `docs\RELEASE_1_15_0_RC1_RELAY_CONNECT_PREFLIGHT_STRICT_METADATA_GATE_2026_06_06.md` (wiki/793). |
| 2026-06-06 v619 | Code and documents were indexed after relay connect preflight strict metadata hardening. | MUSU local indexer succeeded via `& "$env:LOCALAPPDATA\Microsoft\WindowsApps\musu.exe" indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` with `2543 files`, `2734 symbols`, and `9848 ms` after GOAL v618/wiki/793, the relay connect preflight strict metadata report, P2P control-plane spec, network boundary spec, BETA checklist, WIKI/WIKI_INDEX, source changes in `/api/v1/relay/connect`, the P2P relay contract audit, and CoS memory updates. gbrain was not rerun because the same-session blocker remains missing `ZEROENTROPY_API_KEY`, generated/evidence import failures, `sync.last_commit` not advancing, and `gstack-brain-sync exited undefined`; the MUSU local index is the current reliable code/document index. Search terms should include `GOAL v619`, `wiki/794`, `relay connect preflight strict metadata index refresh`, `2543 files`, `2734 symbols`, `9848 ms`, `musu.relay_connect_request.v1`, `relay_connect_payload_bytes_not_accepted`, `release connect preflight regression coverage`, and `P2P store-forward relay audit fail_count=0`. |
| 2026-06-06 v620 | Relay transport proof recording now rejects payload bytes and unknown release fields. | `POST /api/v1/p2p/relay/transport-proof` changed from passthrough parsing to a strict metadata-only proof request contract. It still accepts `payload_bytes_transited` as proof metadata, but raw payload byte fields (`payload`, `payload_base64`, `payload_b64`, `payload_bytes`, `body_base64`) are rejected before lease lookup with `relay_transport_proof_payload_bytes_not_accepted`; unknown fields now fail with `invalid_relay_transport_proof`. P2P store-forward relay contract audit now gates `relay transport proof request is strict metadata only` and `relay transport proof strict metadata regression coverage`. Validation passed `npm run test:p2p -- --test-name-pattern "transport proof"` with `94/94`, `npm run typecheck`, parser check, P2P store-forward relay audit `ok=true`/`fail_count=0`, release verifier regressions `ok=true`/`case_count=54`/`failed_case_count=0`, P2P env status recheck with expected blockers, and `git diff --check`. Code audit found no high/medium issue. This hardens hosted P2P evidence input and does not implement release relay tunnel payload transport; public release remains No-Go on second-PC route/CPU/matrix, hosted P2P release proof, support mailbox, and Store evidence. Canonical report: `docs\RELEASE_1_15_0_RC1_RELAY_TRANSPORT_PROOF_STRICT_METADATA_GATE_2026_06_06.md` (wiki/795). |
| 2026-06-06 v621 | Code and documents were indexed after relay transport proof strict metadata hardening. | MUSU local indexer succeeded via `& "$env:LOCALAPPDATA\Microsoft\WindowsApps\musu.exe" indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` with `2546 files`, `2735 symbols`, and `17094 ms` after GOAL v620/wiki/795, the relay transport proof strict metadata report, P2P control-plane spec, network boundary spec, BETA checklist, WIKI/WIKI_INDEX, source changes in `/api/v1/p2p/relay/transport-proof`, the P2P relay contract audit, and CoS memory updates. Search terms should include `GOAL v621`, `wiki/796`, `relay transport proof strict metadata index refresh`, `2546 files`, `2735 symbols`, `17094 ms`, `musu.relay_transport_proof.v1`, `relay_transport_proof_payload_bytes_not_accepted`, `payload_bytes_transited`, `FORBIDDEN_RELAY_TRANSPORT_PROOF_BYTE_FIELDS`, and `P2P store-forward relay audit fail_count=0`. |
| 2026-06-06 v622 | Current clean HEAD desktop-open CPU evidence was refreshed and audited on HUGH_SECOND. | Fresh `desktop-open` idle CPU evidence `docs\evidence\runtime-idle-cpu\1.15.0-rc.1\20260606-071122-HUGH_SECOND.desktop-open.evidence.json` was captured from clean commit `2387db2dea5fc983d0d3104b41037642b9939ccc` with `git_dirty=false`, `60.04s` sample time, hot process count `0`, owned process count `8`, MUSU runtime/shell `2`, owned WebView2 `6`, owned Node `0`, working set `363.83MB`, private memory `193.95MB`, role CPU MUSU `0`, Node `0`, WebView2 `0.08`, and other `0`. Rust background-loop, frontend polling, process ownership, and startup single-instance audits passed. Desktop readiness still fails only on the stale/missing second-PC evidence area. Qualitative audit found no high/medium code issue and no code changed. Clean local evidence remains one-machine only: go/no-go with public metadata skipped reports local artifacts true, single-machine true, runtime idle CPU `1/2 [HUGH_SECOND]`, runtime CPU matrix `1/2`, targeted second-PC route CPU true, `p2p_control_plane_verified=false`, and public release No-Go on second-PC route/CPU/matrix, public metadata recheck, support mailbox, Store, and hosted MUSU.PRO P2P/relay proof. Canonical report: `docs\RELEASE_1_15_0_RC1_CURRENT_HEAD_DESKTOP_OPEN_CPU_EVIDENCE_2026_06_06.md` (wiki/797). |
| 2026-06-06 v623 | Code, evidence, and documents were indexed after the current HEAD desktop-open CPU evidence audit. | MUSU local indexer succeeded via `& "$env:LOCALAPPDATA\Microsoft\WindowsApps\musu.exe" indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` with `2550 files`, `2735 symbols`, and `11702 ms` after GOAL v622/wiki/797, fresh `20260606-071122-HUGH_SECOND.desktop-open` CPU evidence, the current HEAD desktop-open CPU evidence report, BETA checklist, network boundary spec, MUSU.PRO P2P control-plane spec, WIKI/WIKI_INDEX, and CoS memory updates. Search terms should include `GOAL v623`, `wiki/798`, `current HEAD desktop-open CPU evidence index refresh`, `2550 files`, `2735 symbols`, `11702 ms`, `20260606-071122-HUGH_SECOND.desktop-open`, `runtime idle CPU 1/2`, `runtime CPU matrix 1/2`, `p2p_control_plane_verified=false`, `MUSU Desktop local executor`, and `MUSU.PRO remote input control plane`. |
| 2026-06-06 v624 | Route evidence submission is now strict metadata-only. | `POST /api/v1/p2p/route-evidence` now uses strict schemas for `RelayFallbackSchema`, `RelayTransportProofSchema`, `RelayPayloadDeliveryProofSchema`, and `RouteEvidenceSchema`. Top-level and nested raw payload fields (`payload`, `payload_base64`, `payload_b64`, `payload_bytes`, `body_base64`) are rejected before storage with `route_evidence_payload_bytes_not_accepted`, while `relay_payload_delivery_proof.payload_bytes` remains allowed only as numeric proof metadata. Unknown fields now fail strict parsing with `invalid_route_evidence`, and public Zod issues expand `unrecognized_keys` into concrete field paths. P2P store-forward relay contract audit now gates route evidence strict metadata and regression coverage. Validation passed route-evidence route test `31/31`, `npm run test:p2p` `97/97`, `npm run typecheck`, P2P store-forward relay audit `ok=true`/`fail_count=0`/`check_count=58`, release verifier regressions `54/54`, and `git diff --check`. Code audit found no high/medium issue. This hardens the hosted P2P evidence/control-plane input boundary; it does not implement release relay tunnel payload transport or close second-PC/hosted P2P/Store gates. Canonical report: `docs\RELEASE_1_15_0_RC1_ROUTE_EVIDENCE_STRICT_METADATA_GATE_2026_06_06.md` (wiki/799). |
| 2026-06-06 v625 | Code and documents were indexed after route evidence strict metadata hardening. | MUSU local indexer succeeded via `& "$env:LOCALAPPDATA\Microsoft\WindowsApps\musu.exe" indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` with `2553 files`, `2739 symbols`, and `16468 ms` after GOAL v624/wiki/799, route-evidence strict metadata source/test changes, P2P relay contract audit update, the route evidence strict metadata report, BETA checklist, network boundary spec, MUSU.PRO P2P control-plane spec, WIKI/WIKI_INDEX, and CoS memory updates. Search terms should include `GOAL v625`, `wiki/800`, `route evidence strict metadata index refresh`, `2553 files`, `2739 symbols`, `16468 ms`, `RouteEvidenceSchema strict`, `route_evidence_payload_bytes_not_accepted`, `FORBIDDEN_ROUTE_EVIDENCE_BYTE_FIELDS`, `publicZodIssues`, and `P2P store-forward relay audit check_count=58`. |
| 2026-06-06 v626 | P2P rendezvous creation and candidate exchange are now strict metadata-only. | `POST /api/v1/p2p/rendezvous` now uses strict `CreateRendezvousSchema` and rejects raw payload fields with `rendezvous_payload_bytes_not_accepted`. `POST /api/v1/p2p/rendezvous/[id]/candidates` now uses strict `CandidateEndpointSchema` and `CandidatesSchema`, rejecting raw payload fields with `rendezvous_candidates_payload_bytes_not_accepted`. Unknown fields now fail the existing invalid request errors with concrete field paths. Candidate metadata preservation, direct QUIC public endpoint/NAT requirements, relay-capable endpoint requirements, and owner-scoped isolation remain intact. P2P store-forward relay contract audit now gates rendezvous strict metadata boundaries. Validation passed rendezvous route test `14/14`, `npm run test:p2p` `101/101`, `npm run typecheck`, P2P store-forward relay audit `ok=true`/`fail_count=0`/`check_count=61`, release verifier regressions `54/54`, and `git diff --check`. Code audit found no high/medium issue. This hardens MUSU.PRO P2P bootstrap metadata; it does not implement release relay tunnel payload transport or close second-PC/hosted P2P/Store gates. Canonical report: `docs\RELEASE_1_15_0_RC1_RENDEZVOUS_STRICT_METADATA_GATE_2026_06_06.md` (wiki/801). |
| 2026-06-06 v627 | Code and documents were indexed after rendezvous strict metadata hardening. | MUSU local indexer succeeded via `& "$env:LOCALAPPDATA\Microsoft\WindowsApps\musu.exe" indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` with `2556 files`, `2745 symbols`, and `10569 ms` after GOAL v626/wiki/801, rendezvous strict metadata source/test changes, P2P relay contract audit update, the rendezvous strict metadata report, BETA checklist, network boundary spec, MUSU.PRO P2P control-plane spec, WIKI/WIKI_INDEX, and CoS memory updates. Search terms should include `GOAL v627`, `wiki/802`, `rendezvous strict metadata index refresh`, `2556 files`, `2745 symbols`, `10569 ms`, `CreateRendezvousSchema strict`, `CandidateEndpointSchema strict`, `CandidatesSchema strict`, `rendezvous_payload_bytes_not_accepted`, `rendezvous_candidates_payload_bytes_not_accepted`, and `P2P store-forward relay audit check_count=61`. |
| 2026-06-06 v628 | Room-scoped rendezvous and presence are now strict metadata-only. | `POST /api/rooms/[roomId]/rendezvous` now uses strict `RoomRendezvousSchema`, rejects raw payload byte fields with `room_rendezvous_payload_bytes_not_accepted`, and rejects body `room_id` as an unknown field so the path `roomId` is canonical. `POST /api/rooms/[roomId]/presence` now uses strict `CandidateEndpointSchema` and `RoomPresenceSchema`, rejecting raw payload byte fields with `room_presence_payload_bytes_not_accepted` before presence storage or candidate-cache seeding. Room events/work-orders remain separately bounded payload-capable surfaces. Validation passed room rendezvous route test `5/5`, room presence route test `8/8`, `npm run test:p2p` `105/105`, `npm run typecheck`, P2P store-forward relay audit `ok=true`/`fail_count=0`/`check_count=64`, release verifier regressions `54/54`, and `git diff --check`. Code audit found no high/medium issue. This hardens MUSU.PRO project/company room control-plane metadata; it does not implement release relay tunnel payload transport or close second-PC/hosted P2P/Store gates. Canonical report: `docs\RELEASE_1_15_0_RC1_ROOM_CONTROL_STRICT_METADATA_GATE_2026_06_06.md` (wiki/803). |
| 2026-06-06 v629 | Code and documents were indexed after room control strict metadata hardening. | MUSU local indexer succeeded via `& "$env:LOCALAPPDATA\Microsoft\WindowsApps\musu.exe" indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` with `2559 files`, `2751 symbols`, and `12944 ms` after GOAL v628/wiki/803, room rendezvous/presence strict metadata source/test changes, P2P relay contract audit update, the room control strict metadata report, BETA checklist, network boundary spec, MUSU.PRO P2P control-plane spec, WIKI/WIKI_INDEX, and CoS memory updates. Search terms should include `GOAL v629`, `wiki/804`, `room control strict metadata index refresh`, `2559 files`, `2751 symbols`, `12944 ms`, `RoomRendezvousSchema strict`, `RoomPresenceSchema strict`, `CandidateEndpointSchema strict`, `room_rendezvous_payload_bytes_not_accepted`, `room_presence_payload_bytes_not_accepted`, and `P2P store-forward relay audit check_count=64`. |
| 2026-06-06 v630 | Current clean HEAD desktop-open CPU and process ownership evidence were refreshed after room control hardening. | Fresh `desktop-open` idle CPU evidence `docs\evidence\runtime-idle-cpu\1.15.0-rc.1\20260606-080201-HUGH_SECOND.desktop-open.evidence.json` and process ownership evidence `docs\evidence\process-ownership\1.15.0-rc.1\20260606-080350-HUGH_SECOND.process-ownership.json` passed from clean commit `ade5b64f012c14a8de6f2c0fa99065de5db45f64` with `git_dirty=false`. CPU sample `60.045s`: hot `0`, process count `8`, MUSU `0`, Node `0`, WebView2 `0.18`, owned WebView2 `6`, owned Node `0`, working set `363.79MB`, private memory `193.86MB`, and process ownership `ok=true`/`fail_count=0` with packaged runtime `1`, desktop shell `1`, orphan repo helpers `0`, bridge `127.0.0.1:3622` PID `4204` health `HTTP 200`. Rust background-loop audit, frontend polling audit, P2P store-forward relay audit, and process ownership audit all passed; code audit found no high/medium issue and no source changed. Product boundary remains unchanged: MUSU Desktop executes locally, while MUSU.PRO is remote input, project/company room, presence, rendezvous, path-selection, relay-fallback, and evidence control plane. Public release remains No-Go on second-PC route/CPU/matrix, hosted MUSU.PRO P2P/relay proof, public metadata, support mailbox, and Store evidence. Canonical report: `docs\RELEASE_1_15_0_RC1_POST_ROOM_CONTROL_CURRENT_HEAD_CPU_AUDIT_2026_06_06.md` (wiki/805). |
| 2026-06-06 v631 | Code, evidence, specs, docs, and wiki were indexed after the post room-control current HEAD CPU audit. | MUSU local indexer succeeded via `& "$env:LOCALAPPDATA\Microsoft\WindowsApps\musu.exe" indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` with `2564 files`, `2751 symbols`, and `13062 ms` after GOAL v630/wiki/805, fresh CPU evidence `20260606-080201-HUGH_SECOND.desktop-open`, process ownership evidence `20260606-080350-HUGH_SECOND.process-ownership`, the post room-control current HEAD CPU audit report, next-step plan, BETA checklist, network boundary spec, MUSU.PRO P2P control-plane spec, WIKI/WIKI_INDEX, and CoS memory updates. Search terms should include `GOAL v631`, `wiki/806`, `post room-control current HEAD CPU audit index refresh`, `2564 files`, `2751 symbols`, `13062 ms`, `WebView2 0.18`, `process ownership fail_count=0`, `MUSU Desktop local executor`, and `MUSU.PRO remote input control plane`. |
| 2026-06-06 v632 | External release gates were rechecked without skipping public metadata. | Live public metadata verification for `https://musu.pro` passed with `ok=true`/`fail_count=0`; `/privacy` and `/support` returned HTTP `200` and both contain `musu@musu.pro`. Go/no-go without `-SkipPublicMetadata` now reports `public_metadata_ok=True`, so `store-public-metadata` is no longer a blocker. External gate recheck evidence `docs\evidence\external-gates\1.15.0-rc.1\20260606-082244-HUGH_SECOND.external-gates.evidence.json` records release ready `false`, local artifacts true, single-machine true, runtime idle CPU `1`, runtime CPU matrix `1`, second PC `192.168.1.192:8949` ping/TCP false with `tcp_connect_timeout`, P2P env false, P2P evidence false, relay route evidence count `0`, relay payload transport proven false, and delivery proof count `0`. Live P2P evidence `docs\evidence\p2p-control-plane\1.15.0-rc.1\20260606-082429-musu.pro.evidence.json` used the packaged WindowsApps alias but fails closed because relay status/transport/leases/route-evidence report `logged_in=false`, owner scope is not verified, relay lease storage is not configured/release-grade, and release relay transport/connect/payload endpoints are not wired. Remaining go/no-go blockers are now `multi-device`, `runtime-idle-cpu`, `runtime-cpu-scenario-matrix`, `support-mailbox`, `store-release`, and `p2p-control-plane`. Canonical report: `docs\RELEASE_1_15_0_RC1_EXTERNAL_GATE_RECHECK_2026_06_06.md` (wiki/807). |
| 2026-06-06 v633 | Code, evidence, specs, docs, and wiki were indexed after the external gate recheck. | MUSU local indexer succeeded via `& "$env:LOCALAPPDATA\Microsoft\WindowsApps\musu.exe" indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` with `2571 files`, `2751 symbols`, and `12705 ms` after GOAL v632/wiki/807, external gate evidence `20260606-082244-HUGH_SECOND.external-gates`, live P2P evidence `20260606-082429-musu.pro`, the external gate recheck report, BETA checklist, MUSU.PRO P2P control-plane spec, WIKI/WIKI_INDEX, and CoS memory updates. Search terms should include `GOAL v633`, `wiki/808`, `external gate recheck index refresh`, `2571 files`, `2751 symbols`, `12705 ms`, `public_metadata_ok=True`, `store-public-metadata no longer blocker`, `tcp_connect_timeout`, `logged_in=false`, and `relay payload transport proven False`. |
| 2026-06-06 v634 | External gate evidence now exposes actionable root-cause fields from clean HEAD. | `record-external-release-gate-recheck.ps1` now flattens public metadata, second-PC TCP error, P2P logged-in state, owner-scope status, relay lease store release-grade state, endpoint wiring, route-evidence count, and payload-proof count; helper property readers now tolerate null child JSON so failed child recorders stay evidence-friendly. `test-release-evidence-verifiers.ps1` adds the source contract `external gate recheck exposes actionable root-cause fields`. Validation passed PowerShell parser check, `git diff --check`, and release verifier regression `ok=true`/`case_count=55`/`failed_case_count=0`. Clean HEAD evidence from `f0b09139de93cfa98ab1b5d0d8f85e0115fea6b3` is `docs\evidence\external-gates\1.15.0-rc.1\20260606-090152-HUGH_SECOND.external-gates.evidence.json` with public metadata true, local artifacts true, single-machine true, second-PC `tcp_connect_timeout`, P2P env/evidence false, relay status/transport/leases/route-evidence logged in false, owner scope false, relay lease store release-grade false, relay transport/connect/payload endpoints false, route evidence count `0`, and delivery proof valid count `0`. Qualitative audit found no high/medium issue. Public release remains No-Go on second-PC route/CPU/matrix, hosted P2P login/auth/storage/relay proof, support mailbox, and Store evidence. Canonical report: `docs\RELEASE_1_15_0_RC1_EXTERNAL_GATE_RECHECK_2026_06_06.md` (wiki/809). |
| 2026-06-06 v635 | Code and documents were indexed after the external gate root-cause recheck. | MUSU local indexer succeeded via `& "$env:LOCALAPPDATA\Microsoft\WindowsApps\musu.exe" indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` with `2577 files`, `2751 symbols`, and `12476 ms` after GOAL v634/wiki/809, clean external evidence `20260606-090152-HUGH_SECOND.external-gates`, hosted P2P evidence `20260606-090333-musu.pro`, external gate recorder source contract changes, the external gate report, next-step plan, BETA checklist, MUSU.PRO P2P control-plane spec, network boundary spec, WIKI/WIKI_INDEX, and CoS memory updates. Search terms should include `GOAL v635`, `wiki/810`, `external gate root-cause index refresh`, `2577 files`, `2751 symbols`, `12476 ms`, `p2p_runtime_not_logged_in`, `p2p_relay_lease_store_not_release_grade`, `p2p_relay_payload_endpoint_not_wired`, `release verifier 55/55`, and `MUSU Desktop local executor`. |
| 2026-06-06 v636 | P2P env status now gives explicit packaged-runtime login remediation. | `show-musu-pro-p2p-env-status.ps1` now maps hosted P2P evidence `not_logged_in` to `p2p_runtime_not_logged_in` / `live_evidence_p2p_runtime_not_logged_in` instead of `live_evidence_unknown`, exposes relay status/transport/leases/route-evidence logged-in booleans, relay lease store configured/backend/release-grade fields, and relay transport descriptor/connect/payload endpoint wiring. Its next steps now explicitly require `& "$env:LOCALAPPDATA\Microsoft\WindowsApps\musu.exe" login`, reject the localhost developer dashboard as a way to satisfy the gate, and require rerunning hosted P2P evidence until all four logged-in checks are true. `test-release-evidence-verifiers.ps1` adds source contract `P2P env status exposes runtime login remediation`. Validation passed parser check, current env status JSON, `git diff --check`, and release verifier regression `ok=true`/`case_count=56`/`failed_case_count=0`. Canonical report: `docs\RELEASE_1_15_0_RC1_P2P_ENV_RUNTIME_LOGIN_REMEDIATION_2026_06_06.md` (wiki/811). |
| 2026-06-06 v637 | Code and documents were indexed after the P2P env runtime login remediation. | MUSU local indexer succeeded via `& "$env:LOCALAPPDATA\Microsoft\WindowsApps\musu.exe" indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` with `2580 files`, `2751 symbols`, and `12902 ms` after GOAL v636/wiki/811, `show-musu-pro-p2p-env-status.ps1` runtime login remediation, the new P2P env source contract, canonical report, BETA checklist, CONFIG, WIKI/WIKI_INDEX, and CoS memory updates. Search terms should include `GOAL v637`, `wiki/812`, `P2P env runtime login index refresh`, `2580 files`, `2751 symbols`, `12902 ms`, `live_evidence_p2p_runtime_not_logged_in`, `WindowsApps alias`, `musu.exe login`, `localhost developer dashboard`, and `20260606-090333-musu.pro`. |
| 2026-06-06 v638 | Idle busy-loop candidate status is now locked by a release verifier source contract. | `test-release-evidence-verifiers.ps1` now fails if `write-release-go-no-go.ps1` stops exposing the eight idle candidates (`clipboard polling`, `mDNS discovery`, `health check retry loop`, `bridge readiness wait loop`, `frontend interval/refetch`, `relay payload target poller`, `cloud heartbeat`, and `log/telemetry flush loop`), the `idle_busy_loop_candidate_status` output, the `idle_busy_loop_candidate_contract_verified` output, or the `idle-busy-loop-candidates` blocker. Validation passed parser check, release verifier regression `ok=true`/`case_count=57`/`failed_case_count=0`, Rust background-loop audit `ok=true` with unaudited loops/spawns/network watcher primitives/telemetry flush primitives all `0`, frontend polling audit `ok=true` with direct intervals `0`, P2P store-forward relay audit `ok=true`, and dirty-tree go/no-go showing idle candidate count `8` and failed candidate count `0`. Code audit found no high/medium issue. This is verifier-only hardening and does not change runtime behavior or close second-PC/hosted P2P/support/Store gates. Canonical report: `docs\RELEASE_1_15_0_RC1_IDLE_BUSY_LOOP_SOURCE_CONTRACT_AUDIT_2026_06_06.md` (wiki/813). |
| 2026-06-06 v639 | Code, specs, docs, wiki, and CoS memory were indexed after the idle busy-loop source contract audit. | MUSU local indexer succeeded via `& "$env:LOCALAPPDATA\Microsoft\WindowsApps\musu.exe" indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` with `2583 files`, `2751 symbols`, and `24515 ms` after GOAL v638/wiki/813, `test-release-evidence-verifiers.ps1` source-contract hardening, the idle busy-loop audit report, next-step plan, P2P control-plane spec, MUSU.PRO P2P control-plane spec, network boundary spec, BETA checklist, WIKI/WIKI_INDEX, and CoS memory updates. Search terms should include `GOAL v639`, `wiki/814`, `idle busy-loop source contract index refresh`, `2583 files`, `2751 symbols`, `24515 ms`, `go-no-go exposes all idle busy-loop candidate statuses`, `release verifier 57/57`, `candidate count 8`, `failed candidate count 0`, and `MUSU Desktop local executor`. |
| 2026-06-06 v640 | Current HEAD runtime CPU matrix evidence was refreshed on HUGH_SECOND. | Full runtime CPU matrix evidence `docs\evidence\runtime-cpu-scenarios\1.15.0-rc.1\20260606-094149-HUGH_SECOND.runtime-cpu-scenario-matrix.json` and verification `20260606-094149-HUGH_SECOND.verification.json` passed with `ok=true`, `fail_count=0`, clean commit `ac1e67a4dd8f610a6f09ff61d3107f556e2ac5e5`, and `git_dirty=false`. It covers `startup-open`, `runtime-started`, `dashboard-open`, `desktop-open`, and `post-route`; all five scenarios stayed under budget with hot `0`, MUSU CPU `0`, Node CPU `0`, WebView2 max `0.16`, owned process count `8`, WebView2 helpers `6`, and working set around `364MB`. Targeted HUGH-MAIN route-attempt diagnostic `docs\evidence\runtime-cpu-scenarios\1.15.0-rc.1\20260606-095252-HUGH_SECOND.runtime-cpu-scenario-matrix.json` and verification `20260606-095252-HUGH_SECOND.target-route.verification.json` also passed with `ok=true`, `fail_count=0`, clean git, expected target `HUGH-MAIN`, and allowed failed probe; route timed out against `http://192.168.1.192:8949/api/tasks/delegate`, then CPU stayed healthy with WebView2 `0.13`, hot `0`, and working set `364.24MB`. Code audit found no high/medium issue and no runtime code changed; this remains one-machine evidence and does not close second-PC/hosted P2P/support/Store gates. Canonical report: `docs\RELEASE_1_15_0_RC1_CURRENT_HEAD_RUNTIME_CPU_MATRIX_REFRESH_2026_06_06.md` (wiki/815). |
| 2026-06-06 v641 | Code, evidence, specs, docs, wiki, and CoS memory were indexed after the current HEAD runtime CPU matrix refresh. | MUSU local indexer succeeded via `& "$env:LOCALAPPDATA\Microsoft\WindowsApps\musu.exe" indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` with `2590 files`, `2751 symbols`, and `56258 ms` after GOAL v640/wiki/815, full runtime CPU matrix evidence `20260606-094149-HUGH_SECOND`, HUGH-MAIN target-route diagnostic evidence `20260606-095252-HUGH_SECOND`, the current HEAD runtime CPU matrix refresh report, next-step plan, BETA checklist, P2P control-plane spec, MUSU.PRO P2P control-plane spec, network boundary spec, WIKI/WIKI_INDEX, and CoS memory updates. Search terms should include `GOAL v641`, `wiki/816`, `current HEAD runtime CPU matrix index refresh`, `2590 files`, `2751 symbols`, `56258 ms`, `HUGH-MAIN`, `192.168.1.192:8949`, `WebView2 0.16`, `WebView2 0.13`, and `MUSU Desktop local executor`. |
| 2026-06-06 v642 | Room work-order rejected-input audit logging is now release-gated. | Added route regressions for invalid JSON and missing instruction after P2P control auth in `musu-bee/src/app/api/rooms/[roomId]/work-orders/route.test.ts`, proving rejected room work orders are audit-logged, do not call the bridge, and do not store `text` or `instruction` in command audit JSONL. `audit-operator-api-security-contract.ps1` now gates `room work order rejected input audit logging`, and `test-release-evidence-verifiers.ps1` adds source-contract case `operator API security gates rejected room work-order audit logging`. Validation passed parser check, `npm run test:routes` `30/30`, `npm run typecheck`, operator API security audit `ok=true`/`fail_count=0`, release verifier regression `ok=true`/`case_count=58`/`failed_case_count=0`, and `git diff --check`. Code audit found no high/medium issue. This is P2P command-audit hardening; it does not move execution into MUSU.PRO or close second-PC/hosted P2P/support/Store gates. Canonical report: `docs\RELEASE_1_15_0_RC1_ROOM_WORK_ORDER_REJECTED_AUDIT_GATE_2026_06_06.md` (wiki/817). |
| 2026-06-06 v643 | Code, docs, wiki, and CoS memory were indexed after the room work-order rejected audit gate. | MUSU local indexer succeeded via `& "$env:LOCALAPPDATA\Microsoft\WindowsApps\musu.exe" indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` with `2593 files`, `2752 symbols`, and `16401 ms` after GOAL v642/wiki/817, route regression updates for rejected room work-order audit logging, operator API security audit source-contract update, release evidence verifier source-contract update, the canonical report, BETA checklist, P2P control-plane spec, MUSU.PRO P2P control-plane spec, network boundary spec, WIKI/WIKI_INDEX, and CoS memory updates. Search terms should include `GOAL v643`, `wiki/818`, `room work-order rejected audit index refresh`, `2593 files`, `2752 symbols`, `16401 ms`, `operator API security gates rejected room work-order audit logging`, `invalid_json`, `instruction required`, `command-center.jsonl`, and `MUSU.PRO remote input control plane`. |
| 2026-06-06 v644 | Hosted P2P relay route evidence now requires bound route transport proof in the verifier and release-grade query filter. | `verify-p2p-control-plane-evidence.ps1` now independently validates returned release-grade relay route records and requires `relay_transport_proof` bound to route `session_id`, fallback `lease_id`, `source_node_id`, and `target_node_id`, with `transport_kind=quic_relay_tunnel`, `wss://` relay URL, `quic_tls_1_3`, and `musu_quic_tls_transport`. `routeEvidenceStore.ts` now filters stale/manual `release_grade=true` relay records unless fallback proof, transport proof, and payload delivery proof all bind to the same session/lease/source/target/tunnel chain. Route evidence tests add stale payload-only and missing-session cases, release verifier regressions add `p2p rejects relay route evidence without route transport proof`, and the P2P relay contract audit now gates required lease/session binding. Validation passed parser check, `npm run test:p2p -- --test-name-pattern "route evidence|P2P route evidence"` `105/105`, `npm run typecheck`, P2P store-forward relay audit `ok=true`/`fail_count=0`, release verifier regression `ok=true`/`case_count=59`/`failed_case_count=0`, and `git diff --check`. Code audit found no high/medium issue. This is hosted P2P evidence hardening, not release relay tunnel completion; public release remains No-Go on second-PC route/CPU/matrix, hosted MUSU.PRO proof, support mailbox, and Store. Canonical report: `docs\RELEASE_1_15_0_RC1_P2P_RELAY_ROUTE_TRANSPORT_PROOF_VERIFIER_GATE_2026_06_06.md` (wiki/819). |
| 2026-06-06 v645 | Code, docs, wiki, and CoS memory were indexed after the P2P relay route transport proof verifier gate. | MUSU local indexer succeeded via `& "$env:LOCALAPPDATA\Microsoft\WindowsApps\musu.exe" indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` with `2596 files`, `2752 symbols`, and `9181 ms` after GOAL v644/wiki/819, verifier/source/test changes for bound relay route transport proof, release verifier case `p2p rejects relay route evidence without route transport proof`, P2P relay contract audit update, canonical report, next-step plan, BETA checklist, P2P control-plane spec, MUSU.PRO P2P spec, network boundary spec, WIKI/WIKI_INDEX, and CoS memory updates. Search terms should include `GOAL v645`, `wiki/820`, `p2p relay route transport proof index refresh`, `2596 files`, `2752 symbols`, `9181 ms`, `relay_route_transport_proof_valid_count`, `stale-relay-missing-session-release-grade`, and `quic_relay_tunnel`. |
| 2026-06-06 v646 | Relay route transport proof status is now surfaced through all release handoff reports. | `write-release-go-no-go.ps1` now exposes `p2p_relay_route_transport_proof_valid_count` and requires that count in the P2P blocker message. `record-external-release-gate-recheck.ps1` records the same count and emits `p2p_relay_route_transport_proof_missing`; `show-musu-pro-p2p-env-status.ps1` exposes route transport proof valid/required/invalid counts, emits `live_evidence_relay_route_transport_proof_missing`, and gives next steps to rerun hosted evidence until `relay_route_transport_proof_valid_count > 0`; `show-final-release-handoff-status.ps1` forwards the count; release verifier source contracts lock the fields. Validation passed parser check, release evidence verifier regression `ok=true`/`case_count=59`/`failed_case_count=0`, `npm run test:p2p -- --test-name-pattern "route evidence|P2P route evidence"` `105/105`, `npm run typecheck`, P2P store-forward relay audit `ok=true`/`fail_count=0`, P2P env status with `live_evidence_relay_route_transport_proof_missing`, dirty-tree go/no-go exposing `p2p_relay_route_transport_proof_valid_count=0`, and `git diff --check`. Code audit found no high/medium issue. This is status/evidence hardening only; MUSU Desktop remains the local executor and MUSU.PRO remains remote input/control-plane/rendezvous/relay-proof infrastructure. Public release remains No-Go on second-PC route/CPU/matrix, hosted release relay proof, support mailbox, and Store. Canonical report: `docs\RELEASE_1_15_0_RC1_P2P_RELAY_ROUTE_TRANSPORT_PROOF_STATUS_SURFACE_2026_06_06.md` (wiki/821). |
| 2026-06-06 v647 | Code, specs, docs, and wiki were indexed after the P2P relay route transport proof status surface. | MUSU local indexer succeeded via `& "$env:LOCALAPPDATA\Microsoft\WindowsApps\musu.exe" indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` with `2598 files`, `2752 symbols`, and `13649 ms` after GOAL v646/wiki/821, status surface changes in go/no-go, external gate recheck, P2P env status, final handoff status, release verifier source contracts, the canonical report, next-step plan, BETA checklist, P2P control-plane spec, MUSU.PRO P2P spec, WIKI/WIKI_INDEX, and current product-boundary notes. Search terms should include `GOAL v647`, `wiki/822`, `p2p relay route transport proof status surface index refresh`, `2598 files`, `2752 symbols`, `13649 ms`, `p2p_relay_route_transport_proof_valid_count`, `p2p_relay_route_transport_proof_missing`, `live_evidence_relay_route_transport_proof_missing`, and `MUSU Desktop local executor`. |
| 2026-06-06 v648 | Rust bridge route evidence now carries relay transport proof to cloud route evidence. | `musu-rs/src/bridge/route_evidence.rs` now defines `RouteRelayTransportProof`, adds optional `relay_transport_proof` to `RouteAttemptEvidence` and `RouteAttemptEvidenceInput`, preserves it in local route-evidence JSON, and maps it to `crate::cloud::RouteRelayTransportProof` in `cloud_route_evidence()` instead of dropping it as `None`. `musu-rs/src/install/cli_commands.rs` keeps direct/CLI route evidence explicitly proof-free, and `audit-p2p-store-forward-relay-contract.ps1` adds source gate `route evidence carries relay transport proof to cloud`. Validation passed `cargo fmt --check`, `cargo test -p musu-rs route_evidence --lib` with `14 passed`, P2P store-forward relay audit `ok=true`/`fail_count=0`, release verifier regression `ok=true`/`case_count=59`/`failed_case_count=0`, and `git diff --check`. Code audit found no high/medium issue. This closes runtime evidence proof carry-path loss; it does not implement release `quic_relay_tunnel` transport or make queue fallback release-grade. Public release remains No-Go on second-PC route/CPU/matrix, hosted release relay proof, support mailbox, and Store. Canonical report: `docs\RELEASE_1_15_0_RC1_RUST_ROUTE_EVIDENCE_RELAY_TRANSPORT_PROOF_CARRY_2026_06_06.md` (wiki/823). |
| 2026-06-06 v649 | Code, specs, docs, and wiki were indexed after Rust route evidence relay transport proof carry. | MUSU local indexer succeeded via `& "$env:LOCALAPPDATA\Microsoft\WindowsApps\musu.exe" indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` with `2599 files`, `2754 symbols`, and `33284 ms` after GOAL v648/wiki/823, Rust route evidence carry-path changes, P2P relay contract audit update, the canonical report, next-step plan, BETA checklist, P2P control-plane spec, MUSU.PRO P2P spec, WIKI/WIKI_INDEX, and product-boundary notes. Search terms should include `GOAL v649`, `wiki/824`, `rust route evidence relay transport proof carry index refresh`, `2599 files`, `2754 symbols`, `33284 ms`, `RouteRelayTransportProof`, `route evidence carries relay transport proof to cloud`, `cloud_route_evidence`, and `MUSU Desktop local executor`. |
| 2026-06-06 v650 | Current MSIX alias shadowing is now a live go/no-go blocker. | `write-release-go-no-go.ps1` now runs `check-msix-legacy-conflicts.ps1 -Json`, exposes `msix_current_legacy_conflicts_ok` and `msix_current_legacy_conflicts`, and adds blocker `msix-current-legacy-conflicts` if active startup helpers, scheduled tasks, legacy bins, or PATH alias shadowing exist. The live `HUGH_SECOND` check found terminal `musu` resolving to `C:\Users\empty\.cargo\bin\musu.exe` before the WindowsApps packaged alias; `musu --version` returns `1.15.0-dev`, while the explicit WindowsApps alias returns `1.15.0-rc.1`. Running MUSU Desktop/bridge processes are packaged, so the issue is operator command ambiguity rather than desktop runtime identity. A 60s dirty-tree `desktop-open` CPU diagnostic passed with MUSU `0.03`, Node `0.0`, WebView2 `0.08`, `bridge_runtime=1`, `desktop_shell=1`, and `webview2_helper=6`; this did not reproduce the 20% busy-loop but is not clean release evidence. Validation passed parser check, release verifier regression `ok=true`/`case_count=60`/`failed_case_count=0`, frontend polling audit `ok=true`, Rust background-loop audit `ok=true`, and dirty-tree go/no-go exposed `msix_current_legacy_conflicts_ok=false`. Code audit found no high/medium issue. Public release remains No-Go on alias shadowing, fresh clean single-machine/CPU evidence, second-PC route/CPU/matrix, hosted P2P proof, support mailbox, and Store evidence. Canonical report: `docs\RELEASE_1_15_0_RC1_CURRENT_MSIX_ALIAS_SHADOW_LIVE_GATE_2026_06_06.md` (wiki/825). |
| 2026-06-06 v651 | Code, docs, wiki, and CoS memory were indexed after the current MSIX alias shadow live gate. | MUSU local indexer succeeded via explicit packaged alias `& "$env:LOCALAPPDATA\Microsoft\WindowsApps\musu.exe" indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` with `2601 files`, `2754 symbols`, and `13353 ms` after GOAL v650/wiki/825, the go/no-go live alias-shadow gate, release verifier source contract, canonical report, next-step plan, BETA checklist, WIKI/WIKI_INDEX, and CoS memory. Search terms should include `GOAL v651`, `wiki/826`, `current MSIX alias shadow live gate index refresh`, `2601 files`, `2754 symbols`, `13353 ms`, `msix_current_legacy_conflicts_ok`, `msix-current-legacy-conflicts`, `musu 1.15.0-dev`, `musu 1.15.0-rc.1`, and `MUSU Desktop local executor`. |
| 2026-06-06 v652 | MSIX alias conflict gate now separates persisted PATH from stale current-process PATH. | `check-msix-legacy-conflicts.ps1` now computes release pass/fail from persisted Machine PATH plus User PATH and emits `alias_path_scope=persisted_user_machine`; current process alias state is preserved separately as `current_process_alias_sources`, `current_process_first_alias_path`, `current_process_alias_shadowing_count`, and `current_process_path_stale`. On `HUGH_SECOND`, persisted PATH is clean with `alias_shadowing_count=0` and first alias `C:\Users\empty\AppData\Local\Microsoft\WindowsApps\musu.exe`, while the current Codex process remains stale with `current_process_first_alias_path=C:\Users\empty\.cargo\bin\musu.exe` and `current_process_path_stale=true`. Dirty-tree go/no-go now reports `msix_current_legacy_conflicts_ok=true` and no `msix-current-legacy-conflicts` blocker, with `git` dirty still blocking until commit. Validation passed parser check, legacy conflict JSON, release verifier regression `ok=true`/`case_count=61`/`failed_case_count=0`, and dirty-tree go/no-go summary. Code audit found no high/medium issue. Canonical report: `docs\RELEASE_1_15_0_RC1_MSIX_ALIAS_PERSISTED_PATH_GATE_2026_06_06.md` (wiki/827). |
| 2026-06-06 v653 | Code, docs, wiki, and CoS memory were indexed after the MSIX alias persisted PATH gate. | MUSU local indexer succeeded via explicit packaged alias `& "$env:LOCALAPPDATA\Microsoft\WindowsApps\musu.exe" indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` with `2604 files`, `2754 symbols`, and `17847 ms` after GOAL v652/wiki/827, persisted PATH gate changes, release verifier source contract, canonical report, next-step plan, BETA checklist, WIKI/WIKI_INDEX, and CoS memory. Search terms should include `GOAL v653`, `wiki/828`, `MSIX alias persisted PATH gate index refresh`, `2604 files`, `2754 symbols`, `17847 ms`, `alias_path_scope=persisted_user_machine`, `current_process_path_stale`, and `MUSU Desktop local executor`. |
| 2026-06-06 v654 | Current packaged local evidence was refreshed on HUGH_SECOND after the MSIX alias persisted PATH gate. | Evidence now includes single-machine packaged smoke `20260606-114258-HUGH_SECOND`, desktop-open idle CPU `20260606-114621-HUGH_SECOND.desktop-open`, full runtime CPU matrix `20260606-120547-HUGH_SECOND`, and targeted HUGH-MAIN failed-route CPU diagnostic `20260606-121806-HUGH_SECOND`. Clean go/no-go at `2026-06-06T12:21:29+09:00` on commit `168f4530` reports `git_dirty=false`, `local_artifacts_ready=true`, `single_machine_verified=true`, `msix_current_legacy_conflicts_ok=true`, `runtime_idle_cpu_valid_machine_count=1/2`, `runtime_cpu_scenario_matrix_valid_machine_count=1/2`, `runtime_cpu_second_pc_route_attempt_verified=true`, `runtime_cpu_second_pc_route_attempt_valid_machine_count=1/1`, and `ready_for_public_desktop_release=false`. Code/qualitative audit found no high/medium issue. Product boundary is unchanged: MUSU Desktop is the local executor; MUSU.PRO is remote input, project/company room, presence, rendezvous, path-selection, relay-fallback policy, and evidence/control plane. Public release remains No-Go on real second-PC multi-device evidence, two-machine CPU/matrix counts, support mailbox evidence, Store evidence, and live MUSU.PRO owner-scoped release relay proof. Canonical report: `docs\RELEASE_1_15_0_RC1_CURRENT_PACKAGED_LOCAL_EVIDENCE_REFRESH_2026_06_06.md` (wiki/829). |
| 2026-06-06 v655 | Code, evidence, specs, docs, wiki, and CoS memory were indexed after the current packaged local evidence refresh. | MUSU local indexer succeeded via explicit packaged alias `& "$env:LOCALAPPDATA\Microsoft\WindowsApps\musu.exe" indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` with `2621 files`, `2754 symbols`, and `28082 ms` after GOAL v654/wiki/829, current packaged evidence files `20260606-114258-HUGH_SECOND`, `20260606-114621-HUGH_SECOND.desktop-open`, `20260606-120547-HUGH_SECOND`, and `20260606-121806-HUGH_SECOND`, the current packaged local evidence report, next-step plan, BETA checklist, MUSU.PRO P2P control-plane spec, network boundary spec, WIKI/WIKI_INDEX, and CoS memory. Search terms should include `GOAL v655`, `wiki/830`, `current packaged local evidence index refresh`, `2621 files`, `2754 symbols`, `28082 ms`, `runtime_cpu_second_pc_route_attempt_verified`, `localhost:3001 not required`, `MUSU Desktop local executor`, and `MUSU.PRO remote input control plane`. |
| 2026-06-06 v656 | P2P env status now separates release payload preflight, missing release tunnel endpoint, and preview queue terminology. | `show-musu-pro-p2p-env-status.ps1` now reports `release_tunnel_payload_endpoint_missing=true` and `preview_store_forward_payload_queue_non_release_grade=true`, keeps `release_payload_endpoint_queue_only` only as a legacy alias, and replaces the ambiguous queue-only blocker with `source_preview_store_forward_payload_queue_non_release_grade`. The P2P store-forward relay contract audit gates the new status terms, and release verifier source contracts add `P2P env status separates release payload terminology`, raising regressions to `ok=true`/`case_count=62`/`failed_case_count=0`. Current status correctly remains `ok=false`: release payload preflight and preview queue fallback are wired, but `RELAY_PAYLOAD_ENDPOINT_IMPLEMENTED=false`, `RELAY_TRANSPORT_KIND=websocket_tunnel`, KV/Upstash env is missing, live runtime login is absent, and live relay route/transport/delivery proof is absent. Code audit found no high/medium issue. This is status/audit terminology hardening only; it does not implement release relay tunnel transport or move execution into MUSU.PRO. Public release remains No-Go on second-PC route/CPU/matrix evidence, hosted P2P proof, support mailbox, and Store evidence. Canonical report: `docs\RELEASE_1_15_0_RC1_P2P_ENV_STATUS_RELEASE_PAYLOAD_TERMINOLOGY_2026_06_06.md` (wiki/831). |
| 2026-06-06 v657 | Code, docs, wiki, specs, and CoS memory were indexed after P2P env status release payload terminology hardening. | MUSU local indexer succeeded via explicit packaged alias `& "$env:LOCALAPPDATA\Microsoft\WindowsApps\musu.exe" indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` with `2624 files`, `2754 symbols`, and `18024 ms` after GOAL v656/wiki/831, `show-musu-pro-p2p-env-status.ps1`, P2P store-forward relay contract audit, release verifier source-contract case `P2P env status separates release payload terminology`, canonical report, next-step plan, BETA checklist, MUSU.PRO P2P control-plane spec, network boundary spec, WIKI/WIKI_INDEX, and CoS memory. Search terms should include `GOAL v657`, `wiki/832`, `P2P env status release payload terminology index refresh`, `release_tunnel_payload_endpoint_missing`, `preview_store_forward_payload_queue_non_release_grade`, `source_preview_store_forward_payload_queue_non_release_grade`, `release verifier 62/62`, `MUSU Desktop local executor`, and `MUSU.PRO remote input control plane`. |
| 2026-06-06 v658 | Relay transport proof now binds peer identity to the release proof record. | `musu.relay_transport_proof.v1` now requires `peer_identity_verified`, `peer_identity_method`, and `peer_public_key`; release proof requires `peer_identity_method=quic_tls_cert_fingerprint`, a `sha256:` fingerprint, and identity fields matching the route evidence record for the same session/lease/source/target/tunnel chain. `p2pRelayTransportProofStore.ts`, route-evidence release grading, stale release-grade query filtering, hosted P2P verifier, Rust route evidence DTOs, cloud DTOs, route tests, release verifier fixtures, and the P2P relay contract audit now carry/gate the binding. Validation passed P2P targeted tests `105/105`, `npm run typecheck`, `cargo check --lib`, `cargo fmt --check`, `cargo test --lib route_evidence` `14 passed`, P2P store-forward relay audit `ok=true`, release verifier regressions `ok=true`/`case_count=63`/`failed_case_count=0`, and `git diff --check`. Code audit found no high/medium issue. This is proof-integrity hardening, not release relay tunnel payload implementation; public release remains No-Go on second-PC route/CPU/matrix evidence, hosted P2P release relay proof, support mailbox, and Store evidence. Canonical report: `docs\RELEASE_1_15_0_RC1_RELAY_TRANSPORT_PROOF_PEER_IDENTITY_BINDING_2026_06_06.md` (wiki/833). |
| 2026-06-06 v659 | Code, docs, specs, wiki, and CoS memory were indexed after relay transport proof peer identity binding. | MUSU local indexer succeeded via explicit packaged alias `& "$env:LOCALAPPDATA\Microsoft\WindowsApps\musu.exe" indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` with `2627 files`, `2754 symbols`, and `55082 ms` after GOAL v658/wiki/833, relay transport proof peer identity binding code changes, release verifier case `p2p rejects relay route evidence with transport proof identity mismatch`, P2P relay contract audit update, Rust route evidence/cloud DTO peer identity carry path, canonical report, next-step plan, BETA checklist, MUSU.PRO P2P control-plane spec, network boundary spec, WIKI/WIKI_INDEX, and CoS memory. Search terms should include `GOAL v659`, `wiki/834`, `relay transport proof peer identity binding index refresh`, `2627 files`, `2754 symbols`, `55082 ms`, `peer_identity_verified`, `peer_identity_method`, `peer_public_key`, `quic_tls_cert_fingerprint`, `release verifier 63/63`, `MUSU Desktop local executor`, and `MUSU.PRO remote input control plane`. |
| 2026-06-06 v660 | Relay payload delivery proof now carries release metadata and rejects preview queue proof as release-grade evidence. | `musu.relay_payload_delivery_proof.v1` now requires `relay_url`, `transport_kind`, `relay_default_data_path`, and `release_grade`; route evidence and hosted P2P verification require delivery proof and the stored payload record to use `transport_kind=quic_relay_tunnel`, `release_grade=true`, `relay_default_data_path=false`, and a `wss://` relay URL matching route transport proof. Preview queue payloads remain `transport_kind=http_store_forward_preview` and `release_grade=false`, so they can be recorded and drained but cannot satisfy hosted P2P release relay proof. Validation passed P2P targeted tests `105/105`, `npm run typecheck`, `cargo fmt --check`, `cargo check --lib`, `cargo test --lib route_evidence` `14 passed`, `cargo test --lib relay_payload` `24 passed`, P2P store-forward relay audit `ok=true`, release verifier regressions `ok=true`/`case_count=64`/`failed_case_count=0`, and `git diff --check`. Code audit found no high/medium issue. This is proof-boundary hardening, not release relay tunnel implementation; public release remains No-Go on second-PC route/CPU/matrix evidence, hosted P2P release relay proof, support mailbox, and Store evidence. Canonical report: `docs\RELEASE_1_15_0_RC1_RELAY_PAYLOAD_DELIVERY_PROOF_RELEASE_METADATA_2026_06_06.md` (wiki/835). |
| 2026-06-06 v661 | Code, docs, specs, wiki, and CoS memory were indexed after relay payload delivery proof release metadata hardening. | MUSU local indexer succeeded via explicit packaged alias `& "$env:LOCALAPPDATA\Microsoft\WindowsApps\musu.exe" indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` with `2630 files`, `2755 symbols`, and `20544 ms` after GOAL v660/wiki/835, relay payload delivery proof release metadata code changes, route evidence delivery proof record-level release blockers, Rust bridge/cloud DTO carry-path updates, hosted P2P verifier case `p2p rejects relay route evidence with preview payload delivery proof transport`, P2P relay contract audit update, canonical report, next-step plan, BETA checklist, MUSU.PRO P2P control-plane spec, network boundary spec, WIKI/WIKI_INDEX, and CoS memory. Search terms should include `GOAL v661`, `wiki/836`, `relay payload delivery proof release metadata index refresh`, `2630 files`, `2755 symbols`, `20544 ms`, `http_store_forward_preview`, `quic_relay_tunnel`, `release verifier 64/64`, `relay_fallback_payload_delivery_proof_stored_not_release_grade`, `MUSU Desktop local executor`, and `MUSU.PRO remote input control plane`. |
| 2026-06-06 v662 | Rust background-loop audit now gates telemetry/log flush scope. | `audit-rust-background-loop-contract.ps1` now scans actual Rust `stdout().flush()`/`stderr().flush()` calls plus telemetry/exporter flush primitives, keeps `musu-rs\src\install\uninstall.rs` as the only allowlisted one-shot prompt flush source, emits `allowed_telemetry_flush_primitive_hit_count`, and continues to fail on any disallowed `telemetry_flush_primitive_hit_count`. `test-release-evidence-verifiers.ps1` adds source-contract case `rust background audit limits telemetry flush scope`, raising release verifier regressions to `ok=true`/`case_count=65`/`failed_case_count=0`; `verify-final-operator-gate-packet.ps1` now requires final packets to carry the log-flush scope contract. Rust background-loop audit reports `ok=true`, `fail_count=0`, `telemetry_flush_primitive_hit_count=0`, and `allowed_telemetry_flush_primitive_hit_count=1`. Code audit found no high/medium issue after fixing a low-risk audit blind spot around plain Rust flush calls. Product boundary remains unchanged: MUSU Desktop is the local executor and MUSU.PRO is remote input/control/rendezvous/relay-proof infrastructure. Public release remains No-Go on second-PC route/CPU/matrix evidence, hosted P2P release relay proof, support mailbox, and Store evidence. Canonical report: `docs\RELEASE_1_15_0_RC1_TELEMETRY_FLUSH_SCOPE_AUDIT_2026_06_06.md` (wiki/837). |
| 2026-06-06 v663 | Code, docs, specs, wiki, and CoS memory were indexed after telemetry flush scope audit hardening. | MUSU local indexer succeeded via explicit packaged alias `& "$env:LOCALAPPDATA\Microsoft\WindowsApps\musu.exe" indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` with `2633 files`, `2755 symbols`, and `29667 ms` after GOAL v662/wiki/837, telemetry/log flush scope audit hardening, release verifier source-contract case `rust background audit limits telemetry flush scope`, final operator packet log-flush scope check, canonical report, next-step plan, BETA checklist, MUSU.PRO P2P control-plane spec, network boundary spec, WIKI/WIKI_INDEX, and CoS memory. Search terms should include `GOAL v663`, `wiki/838`, `telemetry flush scope index refresh`, `2633 files`, `2755 symbols`, `29667 ms`, `allowed_telemetry_flush_primitive_hit_count`, `telemetry_flush_primitive_hit_count=0`, `release verifier 65/65`, `musu-rs\src\install\uninstall.rs`, `MUSU Desktop local executor`, and `MUSU.PRO remote input control plane`. |
| 2026-06-06 v664 | Current HEAD packaged local evidence was refreshed after relay proof hardening. | Current HEAD `83e8bd415432529474930bcf54c6408847c0ad24` was rebuilt into the local-sideload MSIX, reinstalled, and refreshed on `HUGH_SECOND`. New evidence includes MSIX install `20260606-141418-HUGH_SECOND`, single-machine smoke `20260606-140158-HUGH_SECOND`, desktop-open idle CPU `20260606-140222-HUGH_SECOND.desktop-open`, full runtime CPU matrix `20260606-140335-HUGH_SECOND`, and targeted HUGH-MAIN route CPU diagnostic `20260606-140947-HUGH_SECOND`. Results: single-machine `ok=true`, `local-bridge-only`, bridge `http://127.0.0.1:8179`; desktop-open CPU `ok=true`, `git_dirty=false`, `60.038s`, WebView2 max `0.16`, hot process `0`, working set `372.87MB`; full matrix verified `ok=true`, `fail_count=0`, route token `MUSU_CPU_SCENARIO_ROUTE_OK_20260606_140335`, route task `041f5d11-e26c-4122-bb86-4c9b687848a5`; targeted HUGH-MAIN diagnostic verified `ok=true`, `fail_count=0`, failed route allowed, timeout to `192.168.1.192:8949`, post-route WebView2 max `0.05`, hot process `0`. Dirty worktree go/no-go before committing evidence restored `single_machine_verified=true`, runtime idle CPU `1/2 [HUGH_SECOND]`, runtime matrix `1/2 [HUGH_SECOND]`, and targeted second-PC route CPU diagnostic `1/1`; the only local blocker expected before commit is dirty git. Code/qualitative audit found no high/medium issue. Public release remains No-Go on real second-PC route/CPU/matrix evidence, hosted P2P release relay proof, support mailbox, and Store evidence. Canonical report: `docs\RELEASE_1_15_0_RC1_CURRENT_HEAD_PACKAGED_LOCAL_EVIDENCE_AFTER_RELAY_PROOF_HARDENING_2026_06_06.md` (wiki/839). |
| 2026-06-06 v665 | Code, evidence, docs, wiki, and CoS memory were indexed after the current HEAD packaged local evidence refresh. | MUSU local indexer succeeded via explicit packaged alias `& "$env:LOCALAPPDATA\Microsoft\WindowsApps\musu.exe" indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` with `2647 files`, `2755 symbols`, and `13328 ms` after GOAL v664/wiki/839, MSIX install evidence `20260606-141418-HUGH_SECOND`, single-machine evidence `20260606-140158-HUGH_SECOND`, desktop-open CPU evidence `20260606-140222-HUGH_SECOND.desktop-open`, full runtime CPU matrix `20260606-140335-HUGH_SECOND`, targeted HUGH-MAIN route CPU diagnostic `20260606-140947-HUGH_SECOND`, canonical report, next-step plan, BETA checklist, WIKI/WIKI_INDEX, and CoS memory. Search terms should include `GOAL v665`, `wiki/840`, `current HEAD packaged local evidence after relay proof hardening index refresh`, `2647 files`, `2755 symbols`, `13328 ms`, `MUSU_CPU_SCENARIO_ROUTE_OK_20260606_140335`, `runtime idle CPU 1/2`, `runtime matrix 1/2`, `targeted second-PC route CPU 1/1`, `MUSU Desktop local executor`, and `MUSU.PRO remote input control plane`. |
| 2026-06-06 v666 | MSIX install candidate selection was hardened after code audit. | `write-release-go-no-go.ps1` now scans recent MSIX install evidence candidates by machine, up to six per machine, and accepts the first candidate that passes the default strict verifier. This prevents developer warning-mode evidence with `AliasShadowingMode=warn-explicit-windowsapps` from masking older clean strict install evidence. `test-release-evidence-verifiers.ps1` adds source contract `go-no-go MSIX install selection scans recent candidates`; validation passed `git diff --check` and release verifier regression `ok=true`/`case_count=66`/`failed_case_count=0`. Dirty-tree go/no-go after the patch reports `msix_install_verified=true`, `single_machine_verified=true`, runtime idle CPU `1/2 [HUGH_SECOND]`, runtime matrix `1/2 [HUGH_SECOND]`, targeted second-PC route CPU `1/1`, and only dirty git plus external release blockers. Qualitative audit found no high/medium issue. Product boundary remains unchanged: MUSU Desktop is local executor and MUSU.PRO is remote input/project room/rendezvous/relay-fallback/evidence control plane. Canonical report addendum: `docs\RELEASE_1_15_0_RC1_CURRENT_HEAD_PACKAGED_LOCAL_EVIDENCE_AFTER_RELAY_PROOF_HARDENING_2026_06_06.md` (wiki/841). |
| 2026-06-06 v667 | Code, specs, docs, wiki, and CoS memory were indexed after MSIX install candidate selection hardening. | MUSU local indexer succeeded via explicit packaged alias `& "$env:LOCALAPPDATA\Microsoft\WindowsApps\musu.exe" indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` with `2649 files`, `2755 symbols`, and `12489 ms` after GOAL v666/wiki/841, `write-release-go-no-go.ps1` MSIX candidate selection hardening, release verifier source contract `go-no-go MSIX install selection scans recent candidates`, current packaged local evidence, P2P control-plane product split/spec addendum, BETA checklist, WIKI/WIKI_INDEX, and CoS memory. Search terms should include `GOAL v667`, `wiki/842`, `MSIX install candidate selection index refresh`, `2649 files`, `2755 symbols`, `12489 ms`, `latest-per-machine-up-to-6`, `warn-explicit-windowsapps`, `release verifier 66/66`, `msix_install_verified=true`, `MUSU Desktop local executor`, and `MUSU.PRO remote input control plane`. |
| 2026-06-06 v668 | Relay preflight failure responses now preserve release evidence fields. | Relay connect and release payload preflight invalid JSON/metadata responses now include structured status fields instead of terse error-only JSON. Relay connect failure paths return `schema=musu.relay_connect.v1`, `relay_connect_accepted=false`, `payload_transported=false`, and `lease_verified=false`; release payload failure paths return `schema=musu.relay_payload_preflight.v1`, `release_payload_accepted=false`, `payload_stored=false`, `payload_transported=false`, and `lease_verified=false`. The P2P relay contract audit now requires regression coverage for these paths. Validation passed P2P tests `107/107`, `npm run typecheck`, P2P relay contract audit `ok=true`/`fail_count=0`, release verifier `ok=true`/`case_count=66`/`failed_case_count=0`, and `git diff --check`. Qualitative audit found no high/medium issue. This is failure-handling and evidence hardening only; release relay tunnel payload transport remains unimplemented. Canonical report: `docs\RELEASE_1_15_0_RC1_RELAY_PREFLIGHT_FAILURE_EVIDENCE_HARDENING_2026_06_06.md` (wiki/843). |
| 2026-06-06 v669 | Code, specs, docs, wiki, and CoS memory were indexed after relay preflight failure evidence hardening. | MUSU local indexer succeeded via explicit packaged alias `& "$env:LOCALAPPDATA\Microsoft\WindowsApps\musu.exe" indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` with `2652 files`, `2757 symbols`, and `12802 ms` after GOAL v668/wiki/843, structured invalid JSON/metadata relay preflight failure responses, P2P relay contract audit updates, canonical report, next-step plan, P2P control-plane spec addendum, BETA checklist, WIKI/WIKI_INDEX, and CoS memory. Search terms should include `GOAL v669`, `wiki/844`, `relay preflight failure evidence index refresh`, `2652 files`, `2757 symbols`, `12802 ms`, `invalid_json`, `relay_connect_accepted=false`, `release_payload_accepted=false`, `payload_transported=false`, `lease_verified=false`, `MUSU Desktop local executor`, and `MUSU.PRO remote input control plane`. |
| 2026-06-06 v670 | Route evidence peer identity release gate was hardened. | Route evidence release grading now requires top-level peer identity proof to use `peer_identity_method=quic_tls_cert_fingerprint` and a `sha256:` fingerprint. The API adds blockers `peer_identity_method_not_release_grade` and `peer_public_key_not_fingerprint`; `routeEvidenceStore.ts` adds `hasCurrentPeerIdentityProof()` so `queryRouteEvidenceRecords({ release_grade: true })` revalidates stale/manual records before returning them. Validation passed P2P tests `108/108`, `npm run typecheck`, P2P relay contract audit `ok=true`/`fail_count=0`, release verifier `ok=true`/`case_count=66`/`failed_case_count=0`, and `git diff --check`. Qualitative audit found no high/medium issue. This is release evidence integrity hardening only; second-PC, hosted relay tunnel, support mailbox, and Store gates remain open. Canonical report: `docs\RELEASE_1_15_0_RC1_ROUTE_EVIDENCE_PEER_IDENTITY_GATE_2026_06_06.md` (wiki/845). |
| 2026-06-06 v671 | Code, specs, docs, wiki, and CoS memory were indexed after the route evidence peer identity gate. | MUSU local indexer succeeded via explicit packaged alias `& "$env:LOCALAPPDATA\Microsoft\WindowsApps\musu.exe" indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` with `2655 files`, `2758 symbols`, and `11366 ms` after GOAL v670/wiki/845, route evidence peer identity gate hardening, `peer_identity_method_not_release_grade`, `peer_public_key_not_fingerprint`, `hasCurrentPeerIdentityProof`, canonical report, next-step plan, P2P control-plane spec addendum, BETA checklist, WIKI/WIKI_INDEX, and CoS memory. Search terms should include `GOAL v671`, `wiki/846`, `route evidence peer identity gate index refresh`, `2655 files`, `2758 symbols`, `11366 ms`, `quic_tls_cert_fingerprint`, `sha256:`, `MUSU Desktop local executor`, and `MUSU.PRO remote input control plane`. |
| 2026-06-06 v672 | Current external release gates were rechecked with public metadata enabled. | Clean HEAD `0ba26d6d27a23a213240962517079d5fd817c7e8` was rechecked without `-SkipPublicMetadata`. Public metadata verifies `true`, local artifacts true, single-machine true, MSIX install true, runtime idle CPU `1/2`, runtime matrix `1/2`, targeted failed-route CPU diagnostic `1/1`, `manifest_git.dirty=false`, and public release remains No-Go on `multi-device`, `runtime-idle-cpu`, `runtime-cpu-scenario-matrix`, `support-mailbox`, `store-release`, and `p2p-control-plane`. New external evidence is `docs\evidence\external-gates\1.15.0-rc.1\20260606-151336-HUGH_SECOND.external-gates.evidence.json`; hosted P2P evidence is `docs\evidence\p2p-control-plane\1.15.0-rc.1\20260606-151527-musu.pro.evidence.json` with verifier `ok=false`/`fail_count=42`, logged-in false, owner scope false, relay lease store not release-grade, relay transport/payload endpoint not wired, route evidence count `0`, route transport proof count `0`, and payload delivery proof count `0`. Qualitative audit found no high/medium issue. Product boundary remains unchanged: MUSU Desktop executes locally; MUSU.PRO is remote input, room, rendezvous, relay fallback, and evidence/control plane. Canonical report: `docs\RELEASE_1_15_0_RC1_CURRENT_EXTERNAL_GATE_SNAPSHOT_2026_06_06.md` (wiki/847). |
| 2026-06-06 v673 | Code, evidence, docs, specs, wiki, and CoS memory were indexed after the current external gate snapshot. | MUSU local indexer succeeded via explicit packaged alias `& "$env:LOCALAPPDATA\Microsoft\WindowsApps\musu.exe" indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` with `2662 files`, `2758 symbols`, and `11836 ms` after GOAL v672/wiki/847, external gate evidence `20260606-151336-HUGH_SECOND.external-gates`, hosted P2P evidence `20260606-151527-musu.pro`, public metadata pass, second-PC `tcp_connect_timeout`, hosted P2P verifier `fail_count=42`, current external gate snapshot report, next-step plan, BETA checklist, P2P control-plane specs, WIKI/WIKI_INDEX, and CoS memory. Search terms should include `GOAL v673`, `wiki/848`, `current external gate snapshot index refresh`, `2662 files`, `2758 symbols`, `11836 ms`, `public_metadata_ok=true`, `P2P fail_count=42`, `relay_route_transport_proof_valid_count=0`, `relay_payload_delivery_proof_valid_count=0`, `MUSU Desktop local executor`, and `MUSU.PRO remote input control plane`. |
| 2026-06-06 v674 | Relay tunnel runtime readiness is now a distinct source gate. | `p2pRelayPolicy.ts` adds `RELAY_TUNNEL_RUNTIME_IMPLEMENTED=false`; `relayTransportWired()` now requires runtime implementation in addition to env flag, release transport kind, and release payload endpoint marker. Relay lease, transport, connect, and release payload preflight APIs expose `relay_tunnel_runtime_implemented=false`, and P2P env status now emits `source_release_relay_tunnel_runtime_not_implemented`. Validation passed P2P tests `108/108`, `npm run typecheck`, P2P store-forward relay audit `ok=true`/`fail_count=0`, release verifier `ok=true`/`case_count=66`/`failed_case_count=0`, and `git diff --check`. Qualitative audit found no high/medium issue. This prevents fake release enablement; it does not implement the actual `quic_relay_tunnel` runtime. Canonical report: `docs\RELEASE_1_15_0_RC1_RELAY_TUNNEL_RUNTIME_SOURCE_GATE_2026_06_06.md` (wiki/849). |
| 2026-06-06 v675 | Code, docs, specs, wiki, and CoS memory were indexed after the relay tunnel runtime source gate. | MUSU local indexer succeeded via explicit packaged alias `& "$env:LOCALAPPDATA\Microsoft\WindowsApps\musu.exe" indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` with `2665 files`, `2759 symbols`, and `12696 ms` after GOAL v674/wiki/849, `RELAY_TUNNEL_RUNTIME_IMPLEMENTED`, `relay_tunnel_runtime_implemented`, `source_release_relay_tunnel_runtime_not_implemented`, relay API status updates, P2P env status source contract, canonical report, next-step plan, BETA checklist, P2P control-plane specs, WIKI/WIKI_INDEX, and CoS memory. Search terms should include `GOAL v675`, `wiki/850`, `relay tunnel runtime source gate index refresh`, `2665 files`, `2759 symbols`, `12696 ms`, `RELAY_TUNNEL_RUNTIME_IMPLEMENTED=false`, `source_release_relay_tunnel_runtime_not_implemented`, `MUSU Desktop local executor`, and `MUSU.PRO remote input control plane`. |
| 2026-06-06 v676 | Current HEAD desktop-open CPU evidence was refreshed after the relay tunnel runtime source gate. | Clean HEAD `dd0e409ee3a8ade2153bb858f74c4c5a0abf5bc2` was sampled on `HUGH_SECOND` in actual packaged `desktop-open` state. Evidence `docs\evidence\runtime-idle-cpu\1.15.0-rc.1\20260606-154524-HUGH_SECOND.desktop-open.evidence.json` reports `ok=true`, `git_dirty=false`, `60.028s`, hot process `0`, max one-core CPU MUSU `0`, Node `0`, WebView2 `0.03`, bridge runtime `1`, desktop shell `1`, WebView2 helper `6`, working set `366.33MB`, and no resource budget violations. Frontend polling, Rust background-loop, and process ownership audits all passed with `fail_count=0`. A 4-state matrix diagnostic at `.local-build\runtime-cpu-scenarios\20260606-154644-HUGH_SECOND` also showed no hot process but is not release evidence because it records `git_dirty=true`. The reported 20% idle CPU loop is not reproduced on this current one-machine packaged desktop state. Public release still requires second-PC CPU/matrix, real route evidence, hosted P2P control-plane/relay proof, support mailbox, and Store evidence. Updated reports: `docs\RELEASE_1_15_0_RC1_CURRENT_HEAD_DESKTOP_OPEN_CPU_EVIDENCE_2026_06_06.md` and `docs\RELEASE_1_15_0_RC1_CURRENT_HEAD_RUNTIME_CPU_MATRIX_REFRESH_2026_06_06.md` (wiki/851). |
| 2026-06-06 v677 | Code, evidence, docs, wiki, and CoS memory were indexed after the current HEAD desktop-open CPU refresh. | MUSU local indexer succeeded via explicit packaged alias `& "$env:LOCALAPPDATA\Microsoft\WindowsApps\musu.exe" indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` with `2668 files`, `2759 symbols`, and `15241 ms` after GOAL v676/wiki/851, evidence `20260606-154524-HUGH_SECOND.desktop-open`, dirty diagnostic matrix `20260606-154644-HUGH_SECOND`, updated CPU reports, next-step plan, BETA checklist, WIKI/WIKI_INDEX, and CoS memory. Search terms should include `GOAL v677`, `wiki/852`, `current HEAD desktop-open CPU after relay runtime gate index refresh`, `2668 files`, `2759 symbols`, `15241 ms`, `20260606-154524-HUGH_SECOND.desktop-open`, `WebView2 0.03`, `hot process count 0`, `git_dirty=false`, `diagnostic matrix git_dirty=true`, `MUSU Desktop local executor`, and `MUSU.PRO remote input control plane`. |
| 2026-06-06 v678 | Relay fallback route evidence now proves candidate coverage before release fallback. | `relay_fallback.candidate_route_kinds` records rendezvous/path-selection candidate availability, and release grading now blocks missing candidate sets, missing relay fallback availability, missing direct candidates, skipped available direct candidates, attempted unavailable direct candidates, duplicate/failed candidates, and candidate/attempt order mismatches before a relay route can be release-grade. `queryRouteEvidenceRecords({ release_grade: true })` revalidates this via `hasCurrentRelayCandidateCoverage()`. Validation passed P2P tests `111/111`, `npm run typecheck`, P2P store-forward relay audit `ok=true`/`fail_count=0`, release verifier `ok=true`/`case_count=66`/`failed_case_count=0`, and `git diff --check`. Qualitative audit found no high/medium issue. This is evidence/path-selection hardening only; second-PC route proof and release relay tunnel runtime remain open. Canonical report: `docs\RELEASE_1_15_0_RC1_RELAY_FALLBACK_CANDIDATE_COVERAGE_GATE_2026_06_06.md` (wiki/853). |
| 2026-06-06 v679 | Code, specs, docs, wiki, and CoS memory were indexed after the relay fallback candidate coverage gate. | MUSU local indexer succeeded via explicit packaged alias `& "$env:LOCALAPPDATA\Microsoft\WindowsApps\musu.exe" indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` with `2671 files`, `2763 symbols`, and `26393 ms` after GOAL v678/wiki/853, `relay_fallback.candidate_route_kinds`, `relay_route_candidate_set_missing`, `relay_route_skipped_available_direct_candidate`, `relay_route_attempted_unavailable_direct_candidate`, `relay_route_candidate_attempt_order_mismatch`, `hasCurrentRelayCandidateCoverage`, canonical report, next-step plan, BETA checklist, P2P control-plane specs, WIKI/WIKI_INDEX, and CoS memory. Search terms should include `GOAL v679`, `wiki/854`, `relay fallback candidate coverage gate index refresh`, `2671 files`, `2763 symbols`, `26393 ms`, `candidate_route_kinds`, `hasCurrentRelayCandidateCoverage`, `MUSU Desktop local executor`, and `MUSU.PRO remote input control plane`. |
| 2026-06-06 v680 | Runtime relay candidate coverage now carries through local source and relay payload metadata. | Rust rendezvous now preserves `candidate_route_kinds`, forwarding attempts ordered direct candidates before relay fallback, `RouteRelayFallbackEvidence` submits candidate coverage to MUSU.PRO, relay payload enqueue/store preserves `candidate_route_kinds` and `attempted_route_kinds`, and target-side relay delivery route evidence reuses the stored route metadata instead of the old `failed`/`relay` placeholder. Validation passed Rust route evidence tests `14/14`, Rust relay payload tests `24/24`, Rust rendezvous tests `6/6`, Rust router candidate test `1/1`, `cargo check --bin musu`, P2P tests `111/111`, `npm run typecheck`, P2P relay contract audit `ok=true`/`fail_count=0`, release verifier `ok=true`/`case_count=66`/`failed_case_count=0`, and `git diff --check`. Qualitative audit found no high/medium issue; low residual risks are increased fallback latency from additional direct candidates and legacy queued payloads staying non-release-grade if metadata is absent. This is runtime evidence carry-path hardening only; release relay tunnel runtime, second-PC proof, support mailbox, and Store evidence remain open. Canonical report: `docs\RELEASE_1_15_0_RC1_RUNTIME_RELAY_CANDIDATE_COVERAGE_CARRY_2026_06_06.md` (wiki/855). |
| 2026-06-06 v681 | Code, docs, specs, wiki, and CoS memory were indexed after runtime relay candidate coverage carry. | MUSU local indexer succeeded via explicit packaged alias `& "$env:LOCALAPPDATA\Microsoft\WindowsApps\musu.exe" indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` with `2675 files`, `2776 symbols`, and `18098 ms` after GOAL v680/wiki/855, runtime relay candidate coverage carry changes, `route_peers_from_target_candidates`, `candidate_route_kind_labels`, `route_kind_labels_to_cloud`, `P2pRelayPayloadRequest`, `P2pRelayPayloadStoredRecord`, canonical report, next-step plan, BETA checklist, P2P control-plane specs, WIKI/WIKI_INDEX, and CoS memory. Search terms should include `GOAL v681`, `wiki/856`, `runtime relay candidate coverage carry index refresh`, `2675 files`, `2776 symbols`, `18098 ms`, `candidate_route_kinds`, `attempted_route_kinds`, `MUSU Desktop local executor`, and `MUSU.PRO remote input control plane`. |
| 2026-06-06 v682 | Current packaged MUSU Desktop was rebuilt/reinstalled and clean-start evidence was refreshed on HUGH_SECOND. | Evidence commits after runtime relay candidate coverage carry record strict MSIX install `docs\evidence\msix-install\1.15.0-rc.1\20260606-171011-HUGH_SECOND.evidence.json`, single-machine smoke `docs\evidence\single-machine\1.15.0-rc.1\20260606-170759-HUGH_SECOND.evidence.json`, desktop-open CPU `docs\evidence\runtime-idle-cpu\1.15.0-rc.1\20260606-171154-HUGH_SECOND.desktop-open.evidence.json`, and full runtime CPU matrix `docs\evidence\runtime-cpu-scenarios\1.15.0-rc.1\20260606-171403-HUGH_SECOND.runtime-cpu-scenario-matrix.json`. Results: strict MSIX install passes with WindowsApps alias first, single-machine `ok=true`/`local-bridge-only`/bridge `http://127.0.0.1:4751`, desktop-open CPU `ok=true`/`git_dirty=false`/`60.043s`/hot `0`/WebView2 max `0.23`/working set `363.69MB`, and runtime matrix verifier `ok=true`/`fail_count=0` with route token `MUSU_CPU_SCENARIO_ROUTE_OK_20260606_171403` and route task `08b81687-bacf-40eb-a677-e92fca76149b`. Clean go/no-go recognizes one-machine evidence with `single_machine_verified=true`, `msix_install_verified=true`, runtime idle CPU valid machines `1`, runtime matrix valid machines `1`, and `manifest_git.dirty=false`; public release remains No-Go on second-PC multi-device/CPU/matrix, targeted second-PC route attempt, hosted P2P/relay proof, support mailbox, and Store evidence. Qualitative/code audit found no high/medium issue in the evidence refresh. `localhost:3001` refusal is not the packaged desktop contract; MUSU Desktop is local executor and MUSU.PRO is remote input/project room/rendezvous/relay-fallback/evidence control plane. Canonical report: `docs\RELEASE_1_15_0_RC1_CURRENT_DESKTOP_CLEAN_START_EVIDENCE_AUDIT_NEXT_STEPS_2026_06_06.md` (wiki/857). |
| 2026-06-06 v683 | Code, evidence, docs, specs, wiki, and CoS memory were indexed after the current desktop clean-start evidence refresh. | MUSU local indexer succeeded via explicit packaged alias `& "$env:LOCALAPPDATA\Microsoft\WindowsApps\musu.exe" indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` with `2686 files`, `2776 symbols`, and `67224 ms` after GOAL v682/wiki/857, current desktop clean-start evidence `20260606-171011-HUGH_SECOND`, `20260606-170759-HUGH_SECOND`, `20260606-171154-HUGH_SECOND.desktop-open`, `20260606-171403-HUGH_SECOND.runtime-cpu-scenario-matrix`, canonical report, next-step plan, BETA checklist, P2P control-plane spec, WIKI/WIKI_INDEX, and CoS memory. Search terms should include `GOAL v683`, `wiki/858`, `current desktop clean-start evidence index refresh`, `2686 files`, `2776 symbols`, `67224 ms`, `MUSU_CPU_SCENARIO_ROUTE_OK_20260606_171403`, `localhost:3001 is not the packaged desktop contract`, `MUSU Desktop local executor`, and `MUSU.PRO remote input control plane`. |
| 2026-06-06 v684 | Current HEAD targeted second-PC route-attempt CPU evidence was restored for HUGH-MAIN. | Targeted matrix `docs\evidence\runtime-cpu-scenarios\1.15.0-rc.1\20260606-173706-HUGH_SECOND.runtime-cpu-scenario-matrix.json` and verification `docs\evidence\runtime-cpu-scenarios\1.15.0-rc.1\20260606-173706-HUGH_SECOND.target-route.verification.json` record a current-HEAD `musu route --target HUGH-MAIN --wait-timeout-sec 30` attempt with `AllowFailedRouteProbe`. Verifier reports `ok=true`/`fail_count=0`; route result is `ok=false`, `failure_allowed=true`, timeout to `http://192.168.1.192:8949/api/tasks/delegate`, token `MUSU_CPU_SCENARIO_ROUTE_OK_20260606_173706`. Post-route CPU stayed under budget for `60.036s`: MUSU `0.03`, Node `0`, WebView2 `0.03`, working set `356.6MB`, hot process count `0`. This is not successful two-machine route proof; it only proves current HUGH_SECOND stays resource-budget-safe after a targeted failed second-PC route attempt. Canonical report: `docs\RELEASE_1_15_0_RC1_CURRENT_TARGETED_SECOND_PC_ROUTE_ATTEMPT_CPU_2026_06_06.md` (wiki/859). |
| 2026-06-06 v685 | Code, evidence, docs, wiki, and CoS memory were indexed after current targeted HUGH-MAIN route-attempt CPU evidence. | MUSU local indexer succeeded via explicit packaged alias `& "$env:LOCALAPPDATA\Microsoft\WindowsApps\musu.exe" indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` with `2691 files`, `2776 symbols`, and `56299 ms` after GOAL v684/wiki/859, targeted `HUGH-MAIN` route-attempt CPU evidence `20260606-173706-HUGH_SECOND`, route token `MUSU_CPU_SCENARIO_ROUTE_OK_20260606_173706`, canonical report, BETA checklist, WIKI/WIKI_INDEX, and CoS memory. Search terms should include `GOAL v685`, `wiki/860`, `targeted second-PC route-attempt CPU index refresh`, `2691 files`, `2776 symbols`, `56299 ms`, `HUGH-MAIN`, `failure_allowed=true`, `WebView2 0.03`, `hot process count 0`, `MUSU Desktop local executor`, and `MUSU.PRO remote input control plane`. |
| 2026-06-06 v686 | Current code audit, product spec, and next-step handoff were refreshed after targeted HUGH-MAIN evidence. | Clean HEAD `c879a849f403aadefdd071a012aaa4cd304cbf24` passed `npm run test:p2p` `111/111`, `npm run typecheck`, P2P relay contract audit `ok=true`/`fail_count=0`, Rust background-loop audit `ok=true`/`fail_count=0`, and release evidence verifier regression `ok=true`/`case_count=66`/`failed_case_count=0`. Clean go/no-go at `2026-06-06T17:48:59+09:00` confirms public metadata, local artifacts, MSIX install, and single-machine evidence pass with `manifest_git.dirty=false`, but `ready_for_public_desktop_release=false`. Qualitative audit found no high/medium code issue. Product spec remains local executor plus web control-plane: MUSU Desktop executes locally, MUSU.PRO handles remote input, rooms, presence, rendezvous, path selection, relay fallback, and evidence. Remaining blockers are real second-PC route/CPU/matrix evidence, live MUSU.PRO P2P proof, production runtime login, KV/Upstash storage, release `quic_relay_tunnel` runtime, release payload endpoint, relay route transport proof, relay payload delivery proof, support mailbox, and Store proof. Canonical report: `docs\RELEASE_1_15_0_RC1_CURRENT_P2P_CONTROL_PLANE_CODE_AUDIT_NEXT_STEPS_2026_06_06.md` (wiki/861). |
| 2026-06-06 v687 | Code, docs, specs, wiki, and CoS memory were indexed after the current code audit and product spec refresh. | MUSU local indexer succeeded via explicit packaged alias `& "$env:LOCALAPPDATA\Microsoft\WindowsApps\musu.exe" indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` with `2694 files`, `2776 symbols`, and `12906 ms` after GOAL v686/wiki/861, current code audit report, next-step plan, BETA checklist, MUSU.PRO P2P control-plane spec update, network boundary spec update, WIKI/WIKI_INDEX, and CoS memory updates. Search terms should include `GOAL v687`, `wiki/862`, `current code audit product spec index refresh`, `2694 files`, `2776 symbols`, `12906 ms`, `P2P tests 111/111`, `release verifier 66/66`, `MUSU Desktop local executor`, `MUSU.PRO remote input control plane`, and `localhost:3001 is not the packaged desktop runtime contract`. |
| 2026-06-06 v688 | Current HEAD external release gates were rechecked after the code audit/product boundary update. | External gate evidence `docs\evidence\external-gates\1.15.0-rc.1\20260606-180122-HUGH_SECOND.external-gates.evidence.json` and hosted P2P evidence `docs\evidence\p2p-control-plane\1.15.0-rc.1\20260606-180311-musu.pro.evidence.json` were recorded for HEAD `c0886f197e3298d896d606b664da0de20b9b0e3a`. Result remains `release_ready=false`; public metadata, local artifacts, and single-machine pass; runtime idle CPU and runtime CPU matrix remain `1/2`; second PC `192.168.1.192:8949` is unreachable with `tcp_connect_timeout`; P2P env/evidence are not ready, runtime logged-in fields are false, owner scope is false, relay transport/payload endpoint wired are false, relay route evidence count is `0`, relay route transport proof valid count is `0`, and relay payload delivery proof valid count is `0`. This confirms the blocker split: local packaged desktop evidence is good on HUGH_SECOND, but real second-PC proof, production MUSU.PRO login/storage, release relay tunnel proof, support mailbox proof, and Store proof remain open. Canonical report updated: `docs\RELEASE_1_15_0_RC1_EXTERNAL_GATE_RECHECK_2026_06_06.md` (wiki/863). |
| 2026-06-06 v689 | Code, evidence, docs, wiki, and CoS memory were indexed after the current HEAD external gate recheck. | MUSU local indexer succeeded via explicit packaged alias `& "$env:LOCALAPPDATA\Microsoft\WindowsApps\musu.exe" indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` with `2699 files`, `2776 symbols`, and `21591 ms` after GOAL v688/wiki/863, external gate evidence `20260606-180122-HUGH_SECOND.external-gates`, hosted P2P evidence `20260606-180311-musu.pro`, updated external gate recheck report, BETA checklist, WIKI/WIKI_INDEX, and CoS memory updates. Search terms should include `GOAL v689`, `wiki/864`, `current HEAD external gate recheck index refresh`, `2699 files`, `2776 symbols`, `21591 ms`, `tcp_connect_timeout`, `p2p_runtime_not_logged_in`, `runtime idle CPU 1/2`, `runtime CPU matrix 1/2`, `MUSU Desktop local executor`, and `MUSU.PRO remote input control plane`. |
| 2026-06-06 v690 | Current HEAD qualitative code audit and next-step handoff were refreshed after the external gate recheck. | Current HEAD `52d325d43b691c6e1b56404e34cfd2ba85257311` passed `npm run typecheck`, `npm run test:p2p` `111/111`, local API auth, operator API security, secret storage, frontend polling, Rust background-loop, P2P store-forward relay, and process ownership audits with `ok=true`/`fail_count=0`, and release evidence verifier regression `ok=true`/`case_count=66`/`failed_case_count=0`. Process ownership proves packaged runtime 1, desktop shell 1, owned Node helpers 0, owned WebView2 helpers 6, and bridge HTTP 200 at `127.0.0.1:4751`. Go/no-go at `2026-06-06T18:13:07+09:00` remains `ready_for_public_desktop_release=false` with local artifacts, single-machine, public metadata, and MSIX install verified, while multi-device remains false. Qualitative audit found no high/medium issue; public release remains blocked by second-PC route/CPU/matrix proof, live MUSU.PRO login/storage and relay proof, support mailbox proof, and Store evidence. Product boundary remains MUSU Desktop local executor plus MUSU.PRO remote input/control plane; `localhost:3001` is not the packaged desktop runtime contract. Canonical report: `docs\RELEASE_1_15_0_RC1_CURRENT_HEAD_QUAL_CODE_AUDIT_NEXT_STEPS_2026_06_06.md` (wiki/865). |
| 2026-06-06 v691 | Code, docs, specs, wiki, and CoS memory were indexed after the current HEAD qualitative code audit. | MUSU local indexer succeeded via explicit packaged alias `& "$env:LOCALAPPDATA\Microsoft\WindowsApps\musu.exe" indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` with `2702 files`, `2776 symbols`, and `11885 ms` after GOAL v690/wiki/865, current HEAD `52d325d43b691c6e1b56404e34cfd2ba85257311`, current qualitative code audit report, next-step plan, BETA checklist, MUSU.PRO P2P control-plane spec, network boundary spec, WIKI/WIKI_INDEX, and CoS memory. Search terms should include `GOAL v691`, `wiki/866`, `current HEAD qualitative code audit index refresh`, `2702 files`, `2776 symbols`, `11885 ms`, `P2P tests 111/111`, `release verifier 66/66`, `127.0.0.1:4751`, `source_release_relay_tunnel_runtime_not_implemented`, `live_evidence_p2p_runtime_not_logged_in`, `MUSU Desktop local executor`, and `MUSU.PRO remote input control plane`. |
| 2026-06-06 v692 | Crash-recovery contract became a release gate for local stale bridge registry cleanup. | `musu up` now removes a dead bridge service registry record before probing health and reports `stale_bridge_registry_removed` plus `stale_bridge_registry_pid`; existing `musu down` stale registry removal remains part of the contract. Added `scripts\windows\audit-musu-crash-recovery-contract.ps1` with schema `musu.crash_recovery_contract.v1`, wired `crash_recovery_contract_verified` and `crash_recovery_contract_audit` into go/no-go, final handoff status, final operator packet generation/verification, freshness classifiers, release verifier regressions, and desktop readiness inventory. Validation passed parser checks, `cargo fmt --check`, `cargo check --bin musu`, `cargo test --lib cleanup_stale_removes_dead_pids` `1/1`, crash-recovery audit `ok=true`/`fail_count=0`, release verifier `ok=true`/`case_count=69`/`failed_case_count=0`, desktop readiness local gates with only multi-device still failing, and `git diff --check`. Qualitative audit found no high/medium issue. This is local packaged runtime hardening only; public release remains blocked by second-PC route/CPU/matrix proof, live MUSU.PRO P2P/relay proof, support mailbox proof, and Store proof. Canonical report: `docs\RELEASE_1_15_0_RC1_CRASH_RECOVERY_CONTRACT_GATE_2026_06_06.md` (wiki/867). |
| 2026-06-06 v693 | Code, docs, specs, wiki, and CoS memory were indexed after the crash-recovery contract gate. | MUSU local indexer succeeded via explicit packaged alias `& "$env:LOCALAPPDATA\Microsoft\WindowsApps\musu.exe" indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` with `2707 files`, `2776 symbols`, and `14571 ms` after GOAL v692/wiki/867, the Rust `musu up` stale bridge registry cleanup, `audit-musu-crash-recovery-contract.ps1`, go/no-go/handoff/operator-packet wiring, release verifier coverage, canonical report, next-step plan, BETA checklist, MUSU.PRO P2P control-plane spec, network boundary spec, WIKI/WIKI_INDEX, and CoS memory. Search terms should include `GOAL v693`, `wiki/868`, `crash-recovery contract index refresh`, `2707 files`, `2776 symbols`, `14571 ms`, `stale_bridge_registry_removed`, `stale_bridge_registry_pid`, `musu.crash_recovery_contract.v1`, `crash_recovery_contract_verified`, `release verifier 69/69`, `MUSU Desktop local executor`, and `MUSU.PRO remote input control plane`. |
| 2026-06-06 v694 | Current installed local runtime was rechecked for four idle/runtime CPU states. | On `HUGH_SECOND`, packaged WindowsApps MUSU was sampled for four 60s scenarios at HEAD `e0474473e366f847f24bd77ec862c5a72f02e59b`: `startup-open`, `runtime-started`, `dashboard-open`, and `desktop-open`. Matrix `.local-build\runtime-cpu-scenarios\20260606-190449-HUGH_SECOND\20260606-190449-HUGH_SECOND.runtime-cpu-scenario-matrix.json` reports `ok=true`, `git_dirty=false`, `fail_count=0`, hot process count `0` in all four scenarios, MUSU `0%`, Node `0%`, WebView2 max `0.16%`, owned Node `0`, owned WebView2 `6`, and working set about `123 MB`. Process ownership passed with packaged runtime `1`, desktop shell `1`, bridge PID `37928`, and bridge HTTP `200` at `127.0.0.1:4751`. Frontend polling audit passed `ok=true`/`fail_count=0` with 29 low-duty call-site files, direct intervals `0`, direct visibility listeners `0`; Rust background-loop audit passed `ok=true`/`fail_count=0` with unaudited loops/spawns `0`. This does not close full release CPU gates because default matrix verification still requires `post-route` and a second machine. The installed package reports only `musu 1.15.0-rc.1` and does not expose the new `stale_bridge_registry_*` JSON fields, so this is current installed runtime CPU evidence only, not proof that crash-recovery source has been rebuilt into the package. Canonical report: `docs\RELEASE_1_15_0_RC1_IDLE_CPU_4_STATE_RECHECK_2026_06_06.md` (wiki/869). |
| 2026-06-06 v695 | Code, docs, wiki, and CoS memory were indexed after the idle CPU 4-state recheck. | MUSU local indexer succeeded via explicit packaged alias `& "$env:LOCALAPPDATA\Microsoft\WindowsApps\musu.exe" indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` with `2709 files`, `2776 symbols`, and `11901 ms` after GOAL v694/wiki/869, the idle CPU 4-state recheck report, BETA checklist update, WIKI/WIKI_INDEX, and CoS memory. Search terms should include `GOAL v695`, `wiki/870`, `idle CPU 4-state index refresh`, `2709 files`, `2776 symbols`, `11901 ms`, `20260606-190449-HUGH_SECOND`, `WebView2 0.16`, `hot process count 0`, `post-route missing`, `MUSU Desktop local executor`, and `MUSU.PRO remote input control plane`. |
| 2026-06-06 v696 | Current HEAD was rebuilt/reinstalled as MSIX and crash-recovery package evidence was restored. | Current HEAD `29dc84db1d8018fd8f8f7bf98588cb6bca0700a2` was rebuilt with `run-msix-workflow.ps1 -Configuration release -StartupContract local-sideload-manual -AttemptInstall -VerifyInstalled -ReplaceExisting` and reinstalled as `Yellowhama.MUSU_1.15.0.0_x64__ygcjq669as2b6` on `HUGH_SECOND`. Packaged `musu up --json` now exposes `stale_bridge_registry_removed` and `stale_bridge_registry_pid`; a dynamic stale registry simulation removed dead PID `999999` and started a healthy bridge at `127.0.0.1:3678`. Crash-recovery audit passed `ok=true`/`fail_count=0`; process ownership, Rust background-loop, and frontend polling audits passed; scoped Rust library test `cargo test cleanup_stale --package musu-rs --lib` passed `2/2`. Canonical single-machine smoke `docs\evidence\single-machine\1.15.0-rc.1\20260606-195631-HUGH_SECOND.evidence.json` passed as `local-bridge-only`; canonical desktop-open idle CPU `docs\evidence\runtime-idle-cpu\1.15.0-rc.1\20260606-200405-HUGH_SECOND.desktop-open.evidence.json` reports `ok=true`, `git_dirty=false`, `60.049s`, hot process `0`, MUSU `0`, Node `0`, WebView2 `0.08`, and working set `178MB`. Qualitative audit found no high/medium issue; residual risks are stale current-process PATH in this Codex shell, one-machine-only proof, missing `post-route` in the current matrix, live MUSU.PRO relay proof, support mailbox, and Store evidence. Product boundary remains MUSU Desktop local executor plus MUSU.PRO remote input/control plane; `localhost:3001` is not the packaged runtime contract. Canonical report: `docs\RELEASE_1_15_0_RC1_CURRENT_HEAD_MSIX_REINSTALL_CRASH_RECOVERY_EVIDENCE_2026_06_06.md` (wiki/871). |
| 2026-06-06 v697 | Code, evidence, specs, wiki, and CoS memory were indexed after current HEAD MSIX reinstall crash-recovery evidence. | MUSU local indexer succeeded via explicit packaged alias `& "$env:LOCALAPPDATA\Microsoft\WindowsApps\musu.exe" indexer sync --work-dir F:\workspace\musu-bee --name musu-bee` with `2716 files`, `2776 symbols`, and `42871 ms` after GOAL v696/wiki/871, current HEAD MSIX reinstall/crash-recovery report, next-step plan, canonical single-machine evidence `20260606-195631-HUGH_SECOND`, canonical desktop-open idle CPU evidence `20260606-200405-HUGH_SECOND.desktop-open`, BETA checklist, P2P control-plane specs, network boundary spec, WIKI/WIKI_INDEX, and CoS memory. Search terms should include `GOAL v697`, `wiki/872`, `current HEAD MSIX reinstall crash-recovery index refresh`, `2716 files`, `2776 symbols`, `42871 ms`, `stale_bridge_registry_removed`, `stale_bridge_registry_pid`, `127.0.0.1:3678`, `MUSU Desktop local executor`, and `MUSU.PRO remote input control plane`. |
