# V23.3 B7 closure — openrc conf convergence (wiki/394)

**Date**: 2026-05-17
**Branch**: `v22/gap-analysis`
**Predecessors**: wiki/379 (V23.3 master plan §2 B7 + §7.6 backward-compat), wiki/372 (V23.2 B4b — original b4b drop-in pattern), wiki/370 (V23.2 B4a — original canonical conf)

## §1 Scope

V23.2 ended with a split: `openrc-musu-gateway.conf` (baked into `musu-backend.tar` by `build-musu-backend.sh:201`, pointed at obsolete `dist/gateway/client.js`) and `openrc-musu-gateway-b4b.conf` (atomic-installed at runtime by `install-wsl2.ps1` step 7 to overwrite the baked file with one pointing at `dist/gateway/main.js`, the B4b telemetry-emitting entry-point). B7 converges these into a single canonical file pointing at `main.js`, deleting the b4b drop-in.

## §2 What changed

- `musu-relay/installer/openrc-musu-gateway.conf` — `command_args` line 16 changed from `dist/gateway/client.js` to `dist/gateway/main.js`. Header comment updated to record B7 convergence + reference wiki/379 §2 B7.
- `musu-relay/installer/openrc-musu-gateway-b4b.conf` — **deleted** (file no longer needed; canonical conf now ships with correct entry-point).
- `musu-relay/installer/install-wsl2.ps1` step 7 (lines 502-506) — `$gwSrc` now points at canonical `openrc-musu-gateway.conf`; throw message updated; atomic-replace pattern unchanged (kept as defensive re-write — no-op for tars built post-B7 but harmless).
- `musu-relay/installer/build-musu-backend.sh:201` — **no change required**; this line already sourced canonical `openrc-musu-gateway.conf`. Post-B7, the bake site automatically picks up the new `main.js` target without modification.

## §3 Backward-compat (wiki/379 §7.6)

Per master plan §7.6 resolution path (c): B4a first-tar build is still operator-gated (V23.2 closure §"operator-side" item 1). No real-world host has imported a V23.2 tar yet. B7 ships before the first real tar reaches an operator. **The drop-in pattern in `install-wsl2.ps1` step 7 atomic-replaces whichever conf shipped in the tar with the canonical conf shipped alongside the installer; after B7, that re-write is a no-op for tars built post-B7.** No re-import requirement; no Const VII coordination cost.

If a tar built **before** B7 ever reaches an operator host (which V23.2 closure says has not happened), the step 7 atomic-replace from the post-B7 installer corrects the on-disk service file at install time. No regression path.

## §4 Tests

- `npx tsc --noEmit` — clean (exit 0)
- `npx jest` — 191/191 passing (V23.2 baseline 189 + A2's 2; B7 changes are content-only, no test impact)
- PowerShell parse-check on `install-wsl2.ps1` — clean (AST parse, no errors)

## §5 Audit

Per wiki/379 §5.1 B7 row: "(skip — mechanical 3-file change)" with `quality-engineer` review folded into closure. The mechanical scope (rename `client.js`→`main.js` + delete redundant duplicate + repoint caller) is reviewed inline here:

- Canonical conf now matches what install-wsl2.ps1 step 7 used to write at runtime → byte-identity verified visually (header comments differ; functional lines now identical to former b4b drop-in's functional lines).
- All git-internal references to `openrc-musu-gateway-b4b.conf` are in `.claude/` session history (not in source/docs/tests).
- No grep hits for `openrc-musu-gateway-b4b` in `musu-relay/` after deletion.
- `build-musu-backend.sh:201` continues to bake `openrc-musu-gateway.conf` into the tar at `/etc/init.d/musu-gateway` — confirmed unchanged.

No HIGH/MEDIUM findings. LOW/INFO: install-wsl2.ps1 step 7 remains an atomic-replace even though it's now usually a no-op; preserved as defense-in-depth for any future-divergence scenario.

## §6 Const gates

- Const III: NO (no schema change)
- Const VI: NO (no experiment required)
- Const VII: feature-branch push only (no main-merge yet)

## §7 References

- wiki/379 §2 B7 (master plan B7 row), §5.1 B7 (audit assignment), §7.6 (backward-compat path)
- wiki/372 (V23.2 B4b — original b4b drop-in motivation)
- wiki/370 (V23.2 B4a — original canonical conf)
