# V23.4 F-B2-2 Closure — PowerShell state file enrichment (wiki/427)

**Date**: 2026-05-17
**Wiki ID**: `wiki/427`
**Sub-WS**: V23.4 Tier-1 F-B2-2
**Branch**: `v22/gap-analysis`
**Detail plan**: wiki/407 (`docs/V23_4_F_B2_2_PLAN_2026_05_17.md`)
**Audit type**: no formal Auditor — one-page closure per V23.3 B7/B8 precedent (single-file PowerShell change, AST + dry-run = acceptance bar)

---

## §1 Summary

Persisted `os_version` + `bios_vt` (probed by `check-prereqs.ps1` at install-wsl2.ps1 step 2) into the fresh-install `Save-MusuState` hashtable so the resume-path can recover them after the WSL-feature-enable reboot. Added `$script:OsVersionResumed` / `$script:BiosVtResumed` script-scoped variables populated in the resume block; relaxed `_Invoke-MusuInstallAttemptTelemetry` helper with `elseif` fallback to those vars when `$script:PrereqResult` is null (resume path).

Closes V23.3 Auditor B wiki/391 §4 NEW-MED-1: post-resume telemetry now emits with actual probed values instead of empty strings.

---

## §2 Changes landed

Single file modified: `musu-relay/installer/install-wsl2.ps1` (+31 LOC, 3 sites).

| Site | Lines (post-edit) | Change |
|---|---|---|
| A — Fresh-install hashtable | ~310-323 (inside `wsl2-off-feature-off` branch, `$state = @{ ... }`) | Added `os_version` + `bios_vt` keys with two-level `PSObject.Properties` guard mirroring `_Invoke-MusuInstallAttemptTelemetry:109-117` |
| B — Resume block restore | ~432-441 (immediately after `$state.musu_pro_base` restore) | Added `$script:OsVersionResumed` + `$script:BiosVtResumed` restores with `$state.PSObject.Properties["..."]` guard for V23.3 state-file back-compat |
| C — Helper elseif fallback | ~118-124 (within `_Invoke-MusuInstallAttemptTelemetry`) | Added `elseif ($script:OsVersionResumed -or $script:BiosVtResumed)` branch to use resumed values when PrereqResult is null |

LOC delta: master-plan estimate 4; actual 31 (two-level guard explosion + comments). Still well within "minimal edit" bar.

---

## §3 Acceptance verification

### §3.1 AST parse clean (per wiki/407 §3.1)

```
pwsh -NoProfile -File ast-check.ps1
```
where `ast-check.ps1` invokes `[System.Management.Automation.PSParser]::Tokenize` against the full file under `Set-StrictMode -Version 3`.

**Output**: `AST_OK` — zero parser errors.

### §3.2 Dry-run verification (per wiki/407 §3.2 / Critic C5)

Ran 6-step synthetic state-file dry-run; all assertions green:

| Step | Scenario | Expected | Observed |
|---|---|---|---|
| 1 | Write V23.3-shape state JSON (no `os_version`/`bios_vt` keys) | file written | `[Step 1] Wrote V23.3 state to: C:\Users\empty\AppData\Local\Temp\musu-state-v233-test.json` |
| 2 | V23.3 state through V23.4 resume restore logic | empty strings, no exception under StrictMode 3 | `[Step 2 V23.3-shape] OsResumed=[] BiosResumed=[]` ✓ |
| 3 | Write V23.4-shape state JSON (with keys) | file written | `[Step 3] Wrote V23.4 state to: ...musu-state-v234-test.json` |
| 4 | V23.4 state through restore logic | restored values | `[Step 4 V23.4-shape] OsResumed=[10.0.22631] BiosResumed=[yes]` ✓ |
| 5 | Site C elseif: PrereqResult=null, Resumed populated | elseif branch fires, tm vars populated | `[Step 5 elseif] branch=Resumed tmOsVer=[10.0.22631] tmBiosVt=[yes]` ✓ |
| 5b | Site C precedence: PrereqResult-present + stale Resumed | PrereqResult branch wins (Resumed ignored) | `[Step 5b precedence] branch=PrereqResult tmOsVer=[FRESH_OS_10.0.99999] tmBiosVt=[FRESH_yes]` ✓ |
| 6 | Cleanup temp files | done | `[Step 6] Cleanup complete` ✓ |

Final marker: `DRYRUN_OK`. No `PropertyNotFoundException` raised at any step.

**Critical back-compat result (Step 2)**: V23.3 state files that pre-date F-B2-2 resume cleanly under StrictMode 3 with empty-string resumed values. The empty strings are helper-falsy (`-or` short-circuit), so the `_Invoke-MusuInstallAttemptTelemetry` elseif drops to the default path and emits with `""` — matching pre-V23.4 behavior. Zero regression risk for V23.3 in-flight installs caught mid-resume.

### §3.3 Repo-wide sanity

| Check | Result |
|---|---|
| `npx tsc --noEmit` (musu-relay) | clean (no TypeScript surface touched, but baseline confirmed) |
| `npx jest` (musu-relay) | 228/228 green in 21.2s — same as F-B2-3 post-state |

---

## §4 Critic resolution (from wiki/425 §11)

| Critic | Status | Evidence |
|---|---|---|
| **C2 — two-level PSObject.Properties guard** | ADDRESSED | All 3 sites use the two-level chain (`-and $.PSObject.Properties["probes"] -and $.probes.PSObject.Properties["os_version"]`). No single-level collapse anywhere. AST parses clean under StrictMode 3. |
| **C5 — dry-run on synthetic state files** | ADDRESSED | §3.2 above — 6-step dry-run covering V23.3 shape, V23.4 shape, elseif fallback, and PrereqResult-precedence ordering. All steps green; no exceptions. |
| **C2 — back-compat with V23.3 state files** | ADDRESSED | Step 2 of §3.2 verifies V23.3-shape state file resumes with empty strings (no `PropertyNotFoundException`). |

No NEW forward-pointers from F-B2-2.

---

## §5 Const gates

- **Const III**: NOT triggered (state-file JSON shape change is not a SQLite schema migration).
- **Const VI**: NOT triggered (no performance characteristic; field read at telemetry-emit time only, no hot-path).
- **Const VII (per-push)**: satisfied at commit of `installer/install-wsl2.ps1` + this closure + wiki/407 detail plan.
- **Const VII (main-merge)**: rolls into V23.4 Tier-1 close bundle (operator-deferred).

---

## §6 Test count baseline

Unchanged: 228/228 (F-B2-2 is PowerShell-only; no jest tests added).

---

## §7 References

- `F:\workspace\musu-bee\docs\V23_4_MASTER_PLAN_2026_05_17.md` (wiki/425 §5.2)
- `F:\workspace\musu-bee\docs\V23_4_F_B2_2_PLAN_2026_05_17.md` (wiki/407 detail plan)
- `F:\workspace\musu-bee\docs\V23_3_B2_CLOSURE_2026_05_17.md` (wiki/391 §4 NEW-MED-1 source)
- `F:\workspace\musu-bee\musu-relay\installer\install-wsl2.ps1` (single file modified at 3 sites)
- V23.3 B7/B8 closure precedent (one-page format for PowerShell-only changes)
