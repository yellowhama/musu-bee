# V23.3 B8 closure — .gitattributes LF enforcement (wiki/395)

**Date**: 2026-05-17
**Branch**: `v22/gap-analysis`
**Commit**: (one-paragraph closure per wiki/379 §2 B8 + §5.1)
**Predecessor**: wiki/379 (V23.3 master plan)

## Scope

Added `.gitattributes` at repo root enforcing `* text=auto eol=lf` plus explicit per-extension declarations for source files (`.ts/.js/.json/.md/.yaml/.yml/.sh/.conf/.ps1/.psm1/.psd1/.sql`) and binary artifacts (`.png/.jpg/.tar/.gz/.zip` etc.). Eliminates the cosmetic "LF will be replaced by CRLF" warnings that fired on every musu-bee commit from Windows hosts (V23.2 qual eval §4.4 flagged this), and also serves as a prerequisite for B6 SOURCE_DATE_EPOCH reproducibility (`musu-backend.tar` built inside Linux/WSL needs source-tree byte-identity).

## What changed

- `F:\workspace\musu-bee\.gitattributes` — new file, 38 lines.

## Tests

No code change → no test impact. `npx tsc --noEmit` and `npx jest` baseline (191/191) unchanged from A2 closure (wiki/387). Future commits should land without CRLF/LF warnings.

## Audit

Per wiki/379 §5.1 B8 row: orchestrator review only, no `quality-engineer` audit needed. Single-file `.gitattributes` is reviewed in this closure.

## Const VII

Feature-branch push only. No main-merge gate.

## References

- wiki/379 §2 B8 (this sub-WS row), §5.1 B8 (no Builder/Auditor subagents)
- V23.2 qual eval (wiki/377) §4.4 (CRLF/LF warning lesson that motivated B8)
- wiki/379 §6 (B8 closure ID = wiki/395)
