# V23.3 A1.c closure — Track A host-port surface + Const VI bench harness

**Wiki ID**: 385
**Status**: SHIP
**Date**: 2026-05-17
**Branch**: `v22/gap-analysis`
**Commits**: `fc61524` (Builder) + `0b873c9` (Auditor A audit-fix: Secret key) + `d460208` (Auditor B audit-fix: success_rate gate + RUNS=3 guard)
**Sub-WS**: V23.3 Tier-A1.c (master plan wiki/379 §2)
**Detail plan**: wiki/384 (`docs/V23_3_A1C_PLAN_2026_05_17.md`)

## §1 Summary

A1.c closes the V23.3 Tier-A1 trio (image → manifest → cutover-bench) by shipping
**Track A only** (WSL2-import host-port surface) plus the developer-side Const VI
bench harness. Two artifacts land:

1. **Host-port surface**: `musu-bridge.yaml` gets `hostPort: 8070` added to the
   container ports block, so the musu-bee frontend's hard-contracted
   `http://localhost:8070` URL (used by ~22 musu-bee files; 15 of which are
   `src/app/api/*/route.ts`) resolves to the K3s Pod via WSL2's automatic
   distro-port localhost-forward. Zero collision risk on Track A because Windows
   operator hosts have no host-side bridge listener.
2. **Const VI bench harness**: `musu-relay/installer/bridge-bench.sh` (115 LOC
   orchestrator with RUNS=3 guard + `success_rate ≥ 99.5%` gate-eligibility
   filter) and `musu-relay/installer/bench-pod.yaml` (176 LOC ephemeral Pod with
   `musu-bridge-token` Secret mount + `Authorization: Bearer $TOKEN` injection on
   every bench request). These are **developer-side** artifacts; not baked into
   the outer tar (per C-A1c-L2 / F-A1c-8: V23.4 may ship a separate operator
   `musu-health` CLI).

Three acceptance bars MET:

1. **Const VI byte-identity** (post-build pinning): Build A == Build B == sha256
   `93b58e274d6038b0cfca0a007410380215d4564affc1c564c0cee008b6461851` intra-hour,
   with Pod-spec image digest aligned to the actual baked bridge image
   (`sha256:e740ddd8…`) and manifest re-pinned to the corresponding inner bridge
   OCI tar sha256 (`f2d05ad9…`).
2. **Dual-audit clearance**: 2× `quality-engineer` (Auditor A bench-protocol-validity
   seed + Auditor B Const-VI-verdict-math seed) both returned SHIP-OK after
   audit-fix commits `0b873c9` + `d460208`. All 4 prior Critic HIGHs verified
   intact; all NEW Auditor HIGHs RESOLVED in audit-fix commits or accepted via
   explicit V23.4 forward-pointer (F-A1c-9, F-A1c-10).
3. **Const VII feature-branch push gate**: 3 commits pushed on `v22/gap-analysis`;
   main-merge is operator-gated (V23.3 closure-time, not A1.c-time).

**Bench execution itself**: the harness is READY to run but NOT yet executed. The
Const VI gate verdict (PASS/FAIL/AMBIGUOUS per §4.1 of the plan) is RESERVED for
the first operator bench run. See §7 below for the absolute-fallback table,
AMBIGUOUS re-run protocol, and rollback measurement step, all embedded verbatim
for the future operator.

## §2 Phase 0-7 trace

| Phase | Agent | Output | Verdict |
|---|---|---|---|
| 0. Researcher | `deep-research-agent` + `Explore` parallel | findings integrated into wiki/384 §3-§4 (Track A vs Track B; route.ts contract; hostPort/NodePort tradeoffs; worker:false /health-vs-/health/ready) | INFO/MED only; no HIGHs from research phase |
| 1. Planner | `Plan` | wiki/384 detail plan (12 sections, 825 LOC) | — |
| 1.5. Critic (round 1) | `system-architect` | 4 HIGH + 8 MED + 7 LOW + 2 INFO → wiki/384 §11.1 table | ALL 4 HIGH resolved in plan via §11.3 edit pass |
| 1.5. Critic (round 2, delta-review) | `system-architect` | wiki/384 §11.4: PARTIAL on H3 (4 stale `/api/route` refs in §1.3/§2.2/§5.2/§5.6 contradict the drop) + 2 NEW MED (trap-rollout-status + Step-7 shell-context) | After §11.5 H3-propagation scrub → CLEARED. NEW MEDs flagged in Builder spawn HANDOFF NOTES; not plan-doc blockers |
| 3. Builder | initially `devops-architect` subagent → spawn hang → orchestrator-direct fallback | commit `fc61524` (3 file changes: musu-bridge.yaml +6 LOC hostPort/digest re-pin; bridge-bench.sh NEW; bench-pod.yaml NEW) | All Critic HIGH resolutions verified in commit + plan §11.3 H3 propagation verified clean |
| 5. Auditor A | `quality-engineer` (bench protocol validity seed) | SHIP-OK with NEW HIGH: bench-pod Secret key reference `key: token` does not match A1.b Secret schema (musu-init writes Secret with key `MUSU_BRIDGE_TOKEN`, not `token`) | NEW HIGH → audit-fix `0b873c9` |
| 5. Auditor B | `quality-engineer` (Const VI verdict math seed) | HOLD-with-followup + 2 NEW HIGH: (B-H1) §5.6 schema under-implemented in bridge-bench.sh; (B-H2) `success_rate ≥ 99.5%` gate-eligibility filter missing | NEW HIGHs → B-H1 ACCEPTED as Option (b) + F-A1c-9 V23.4 forward-pointer; B-H2 RESOLVED via audit-fix `d460208` (+ RUNS=3 guard) → Auditor B CLEARED |
| 6. audit-fix (post-Auditor A) | (orchestrator-direct, devops domain) | commit `0b873c9` — bench-pod.yaml Secret keyRef corrected: `name: musu-bridge-token, key: MUSU_BRIDGE_TOKEN` (matches musu-init's `kubectl create secret generic ... --from-literal=MUSU_BRIDGE_TOKEN=...`) | Auditor A SHIP-OK re-verified |
| 6. audit-fix (post-Auditor B) | (orchestrator-direct, devops domain) | commit `d460208` — bridge-bench.sh adds (a) RUNS=3 hard-coded guard (was RUNS env-tunable but defaulted to 1; protocol requires 3); (b) `success_rate = sum(success_i)/sum(n_i)` computation across all runs with `≥ 0.995` gate-eligibility check; (c) emits `verdict: AMBIGUOUS_UNDER_99.5_SUCCESS` when threshold missed. Plus V23.4 forward-pointers F-A1c-9 (full schema emit) + F-A1c-10 (bench-windows.ps1 peer) | Auditor B CLEARED |
| 7. Scribe | `technical-writer` | wiki/385 (this doc) | — |

**Builder-spawn anomaly (Phase 3)**: The first `devops-architect` subagent invocation hung silently (returned no envelope after the token-budget interval per MODE_Agent_Team.md token-budget guardrails). Per the escalation rule ("Subagent returns silent/off-topic → do NOT re-spawn the same subagent type. Escalate to user with the empty result attached"), the orchestrator escalated and the user authorized orchestrator-direct fallback (small enough commit surface: 3 files, ~290 LOC net). Commit `fc61524` was authored by the orchestrator following the wiki/384 §3 implementation steps verbatim.

## §3 Builder commit chain — file changes

### §3.1 Builder commit `fc61524` (host-port + bench harness creation)

| File | Change | LOC | Notes |
|---|---|---|---|
| `musu-relay/installer/k3s/musu-bridge.yaml` | MODIFY | +5 / -0 | Line 169: re-pin Pod-spec image digest to `sha256:e740ddd8f5e50bf0932282f78f9f69c96632231e4d84eb63342add180fd992d6` (the post-build canonical bridge OCI manifest digest; see §6.3 hash drift). Lines 175 + 177: add `hostPort: 8070` to container ports block + inline comment citing wiki/384 + Track-A-only safety rationale. |
| `musu-relay/installer/bridge-bench.sh` | NEW | 115 LOC | Bench orchestrator with: precheck (`crictl images | grep python:3.11-slim`), rate-limit disable bracket (`kubectl set env deploy/musu-bridge MUSU_DISABLE_RATE_LIMIT=1` + EXIT-trap revert), kubectl apply bench-pod → wait Ready → tail logs → kubectl delete pod → JSON aggregation per V23_1_SPIKE_RESULT_TEMPLATE §2 shape with p99 extension. Initial RUNS=1 (changed to 3 by `d460208`); initial no success_rate gate (added by `d460208`). |
| `musu-relay/installer/bench-pod.yaml` | NEW | 176 LOC | Ephemeral Pod in namespace `musu`, `restartPolicy: Never`, `automountServiceAccountToken: false`, `image: docker.io/library/python:3.11-slim` (`imagePullPolicy: IfNotPresent`), env block injecting `MUSU_BRIDGE_TOKEN` from `secretKeyRef: name=musu-bridge-token, key=token` (key name corrected by `0b873c9`), `WARMUP_REQUESTS=100`, `MEASURE_REQUESTS=3000`, `TARGET_RPS=10`, inline Python aiohttp/urllib fallback with `pct()` helper using `statistics.quantiles(latencies, n=100, method='inclusive')` per C-A1c-L7 fix. |

### §3.2 Audit-fix commit `0b873c9` (Auditor A: Secret key name)

| File | Change | LOC | Notes |
|---|---|---|---|
| `musu-relay/installer/bench-pod.yaml` | MODIFY | +1 / -1 | `secretKeyRef.key`: `token` → `MUSU_BRIDGE_TOKEN`. Matches A1.b musu-init step 3.5 which creates the Secret via `--from-literal=MUSU_BRIDGE_TOKEN=...` (`musu-init` line range covered by wiki/383 §3 row 3). Auditor A flagged that the original `key: token` would surface as `MountVolume.SetUp failed for volume "bench" : couldn't find key token in Secret musu/musu-bridge-token` at bench-pod start, immediately failing the bench with a substrate-irrelevant cause. |

### §3.3 Audit-fix commit `d460208` (Auditor B: success_rate gate + RUNS=3 guard + V23.4 carries)

| File | Change | LOC | Notes |
|---|---|---|---|
| `musu-relay/installer/bridge-bench.sh` | MODIFY | +22 / -3 | (a) Hard-code `RUNS=3` (was `RUNS=${RUNS:-1}` — per Auditor B, env-tunable default of 1 violates §5.5 protocol which requires 3 runs as the unit of statistical reporting). (b) Add post-run aggregation: `success_total=$(jq -s 'map(.health_ready.success) | add' /tmp/musu-bench-runs/*.json)`, `n_total=$(jq -s 'map(.health_ready.n) | add' ...)`, `success_rate=$(awk "BEGIN{print $success_total/$n_total}")`, compare against `0.995`. (c) When `success_rate < 0.995`, the emitted aggregate verdict is `AMBIGUOUS_UNDER_99.5_SUCCESS` and the orchestrator's "VERDICT" exit message instructs operator to NOT count this run for Const VI gate (re-run with H1-class rate-limit / H2-class auth verification before reading latency numbers). (d) Inline comments naming F-A1c-9 (full §5.6 schema emit deferred to V23.4) and F-A1c-10 (WSL2 vEthernet + portmap overhead bench-windows.ps1 peer deferred to V23.4). |

**Net Builder + audit-fix surface**: 3 files touched; ~318 net LOC added across the chain.

## §4 Critic findings + resolutions (Phase 1.5)

All Critic findings from `system-architect` (round 1) are tabled in wiki/384 §11.1 with full claim + evidence + resolution language. The §11.3 resolution log records the post-Critic edit pass. The §11.4 delta-review (round 2) found the H3 propagation gap (4 stale `/api/route` references in plan body that contradicted the H3 drop); the §11.5 follow-up resolved those 4 references; final delta verdict CLEARED.

### §4.1 HIGH findings (4) — all RESOLVED before Builder spawn

| ID | Issue | Resolution |
|---|---|---|
| C-A1c-H1 | Default IP-keyed rate limit (60/60s) throttles bench pod at request #61 → false-negative Const VI verdict | bridge-bench.sh wraps bench with `kubectl set env deploy/musu-bridge MUSU_DISABLE_RATE_LIMIT=1` + EXIT-trap revert. Plan §3 Step 3 + Step 2 preamble cite resolution. |
| C-A1c-H2 | `/health/ready` NOT in auth-bypass whitelist; bench pod's calls would return 401, measuring auth-rejection latency | bench-pod.yaml mounts `musu-bridge-token` Secret; inline script injects `Authorization: Bearer $TOKEN` header on every request. Option (b) per Critic recommendation (lowest-risk; production auth surface unchanged). Builder commit `fc61524` used wrong key name `token` → audit-fix `0b873c9` corrected to `MUSU_BRIDGE_TOKEN`. |
| C-A1c-H3 | `/api/route` POST bench broken under worker:false; was in plan §3/§4/§5 originally | `/api/route` DROPPED entirely from A1.c bench per Option (i). All references scrubbed across §1.3, §2.2, §3, §4, §5.2, §5.6 in §11.3 + §11.5 edit passes. Const VI verdict asserts on `/health/ready` + cold-start + RSS + rollback only. V23.4 forward-pointer when worker sidecar lands. |
| C-A1c-H4 | Build host ambiguity (Windows vs WSL2) risks B6 byte-identity break via Git-Bash-for-Windows divergence | §3 Step 7 preamble pins build host to `alpine-musu-build` WSL2 distro (matches A1.a/A1.b canonical per wiki/381 + wiki/383 §6.2) + absolute working dir `/mnt/f/workspace/musu-bee/musu-relay/installer`. Pre-build verification command added. |

### §4.2 MEDIUM findings (8) and LOW findings (7) — all addressed

- **8 MED**: M1 (hostPort vs ClusterIP path distinction in §4 row 7 wording), M2 (5ms → 10ms WSL2 port-forward p99 OVERRIDE), M3 (§1.1 V21-host-inheritance prose), M4 (absolute-fallback column + Builder HANDOFF NOTE for missing V21 ref), M5 (B6 inner-file diff verification), M6 (cold-start split into Pod-restart vs First-boot), M7 (AMBIGUOUS re-run protocol pinned: `wsl --unregister` + fresh 3-run), M8 (R2 QoS request-bump concrete numbers). All resolved in plan body via §11.3 edit pass.
- **7 LOW**: L1 (keep dual aiohttp/urllib — ACCEPTED), L2 (V23.4 operator `musu-health` CLI forward-pointer F-A1c-8 added), L3 (route.ts count "~22 files; 15 are route.ts" — DEFERRED, Builder may polish inline), L4 (WSL2 forward Microsoft-doc citation in §7 R1), L5 (3000×3=9000 sample size adequate — ACCEPTED), L6 (`crictl images | grep python:3.11-slim` precheck + abort-on-missing in R5), L7 (`statistics.quantiles(..., method='inclusive')` percentile fix). All addressed.

### §4.3 Round-2 delta-review (§11.4) PARTIAL → CLEARED

Critic delta-review found that H3's primary edit (§4 thresholds row drop) was correct, but the H3 drop did not propagate to 4 secondary plan-body locations:
- §1.3 #4 acceptance criterion still mentioned `/api/route`
- §2.2 rationale paragraph still mentioned `/api/route`
- §5.2 measurement load section had full `/api/route` protocol + 6000-per-run / 18000-per-session math
- §5.6 bench output schema had `api_route` keys in `runs[]`, `aggregate`, `systemd_reference`

§11.5 (final orchestrator edit pass before Builder spawn) scrubbed all 4 references and re-emitted §5.2 with `3000/run + 9000/session` math + §5.6 schema without `api_route` keys. Re-Critic delta confirmed CLEARED. The 2 NEW round-2 MEDs (trap-rollout-status missing; Step-7 shell-context ambiguity) were carried in Builder spawn HANDOFF NOTES per the round-2 verdict; Builder handled both inline at commit time without re-blocking.

## §5 Dual-audit findings (Phase 5)

Per C-A1c-INFO1: Orchestrator escalated Phase 5 from single-audit (master plan §5.1 default) to dual-audit per MODE_Agent_Team.md "high-stakes" criterion (wrong Const VI verdict ships A1.c with bench validity unverified = one-way blast). Two `quality-engineer` Auditors with split seed prompts.

### §5.1 Auditor A (quality-engineer, bench protocol validity seed) — SHIP-OK with NEW HIGH

- All 4 prior Critic HIGHs verified RESOLVED in commit `fc61524` with file:line evidence.
- Bench-pod isolation profile (`restartPolicy: Never`, `automountServiceAccountToken: false`, no privileged mode, only `MUSU_BRIDGE_TOKEN` Secret mounted) verified.
- Rate-limit bracket logic (`kubectl set env ... MUSU_DISABLE_RATE_LIMIT=1` + EXIT-trap revert) verified syntactically correct + functionally re-converges deploy on EXIT.
- Percentile math verified (`statistics.quantiles(n=100, method='inclusive')` returns 99 cut-points; reading `q[49]/q[94]/q[98]` for p50/p95/p99 is correct).
- **NEW HIGH (A-H1)**: bench-pod.yaml `secretKeyRef.key: token` does not match the actual Secret schema. A1.b musu-init step 3.5 (per wiki/383 §3 row 3) creates the Secret via `kubectl create secret generic musu-bridge-token --from-literal=MUSU_BRIDGE_TOKEN=...`, so the key inside `data.` is `MUSU_BRIDGE_TOKEN`, NOT `token`. Bench Pod would fail to start with `MountVolume.SetUp failed: couldn't find key token`.

**Resolution**: audit-fix `0b873c9` (1-LOC change in bench-pod.yaml). Auditor A re-verified post-fix → SHIP-OK.

### §5.2 Auditor B (quality-engineer, Const VI verdict math seed) — HOLD-with-followup → CLEARED

- All 4 prior Critic HIGHs verified RESOLVED.
- §4 absolute-fallback column verified populated for all 5 ratio rows.
- §4.1 AMBIGUOUS re-run protocol verified specified (`wsl --unregister` + fresh 3-run).
- Cold-start split into Pod-restart (60s p95) vs First-boot wall-clock (90s p95) verified in §4 + §5.4.
- **NEW HIGH (B-H1)**: §5.6 schema under-implemented in `bridge-bench.sh`. The plan §5.6 specifies a 9-top-level-field + 10-aggregate-field JSON schema (schema name, wiki_id, sub_ws, substrate, bridge_image_digest, outer_tar_sha256, k3s_version, node_kernel, runs[], aggregate{}, systemd_reference{}, gate_thresholds, verdict, verdict_reasoning + per-run rss_samples_mb + cold_starts_ms + aggregate health_ready_p99_canonical / rss_max_mb / cold_start_p95_ms / first_boot_wall_clock_p95_ms). The committed `bridge-bench.sh` emits 2 of 9 top-level fields (`schema` + `runs[]`) and 6 of 10 aggregate fields (the latency percentiles only; missing RSS sampling loop, cold-start trials loop, k3s_version, node_kernel, outer_tar_sha256 stitching). A literal §5.6-compliant emit requires +80-120 LOC.
- **NEW HIGH (B-H2)**: `success_rate ≥ 99.5%` gate-eligibility filter per §5.5 is missing from `bridge-bench.sh`. Without it, a bench run where 50% of requests return 5xx still gets a verdict computed on the surviving 50% — Const VI gate eligibility violated silently.

**Resolution**:
- **B-H1**: ACCEPTED as **Option (b)** under MODE_Agent_Team.md conflict resolution ("Auditor wins over Critic on real-code disagreements, EXCEPT Constitution/policy gates"). Option (b) here = ship partial-schema emit for V23.3 + V23.4 forward-pointer F-A1c-9 to widen to full §5.6 schema. Rationale: the 6 aggregate fields emitted ARE the Const VI-load-bearing fields (latency percentiles + success_rate after B-H2 fix). RSS sampling, cold-start trials, metadata stitching are §5.3/§5.4/§5.6 protocol artifacts that the operator can run as separate kubectl invocations and stitch into wiki/385 manually (instructions in §7.3 below). Auditor B accepted Option (b) on re-verification with the caveat that the operator-side stitching steps MUST be embedded verbatim in wiki/385 §7 → satisfied below.
- **B-H2**: RESOLVED via audit-fix `d460208` (success_rate aggregation + `≥ 0.995` filter + `AMBIGUOUS_UNDER_99.5_SUCCESS` verdict variant + RUNS=3 hard-coded guard).

Auditor B post-fix verification: SHIP-OK with explicit handoff requirement that wiki/385 §7 embed the §4 absolute-fallback table verbatim, the AMBIGUOUS re-run protocol verbatim, and the rollback time measurement step as separate operator bash. All three are embedded below.

### §5.3 Conflict resolution per MODE_Agent_Team.md

- Both Auditors are `quality-engineer` type but split by seed prompt (protocol-validity vs verdict-math). They surfaced disjoint NEW HIGH findings (A-H1 about Secret schema; B-H1+B-H2 about schema completeness + gate filter) — no contradiction; per dual-audit contract, the union of NEW HIGHs is the mandatory fix set.
- Both Auditors confirmed all 4 prior Critic HIGHs intact in committed code. No Critic-vs-Auditor conflict.
- Per the MODE_Agent_Team.md exception clause: B-H1 is Option (b) (defer-with-forward-pointer) and is NOT a Constitution/policy gate (Const VI is the gate; B-H1's "schema emit completeness" is a documentation-of-evidence concern, not a Const VI verdict concern). Schema completeness deferral is acceptable.

## §6 Const VI byte-identity verification

### §6.1 Final canonical Build A==B

- **Outer tar**: sha256 `93b58e274d6038b0cfca0a007410380215d4564affc1c564c0cee008b6461851` (Build A and Build B, intra-hour, byte-identical)
- **Outer tar size**: 397,793,280 bytes (379 MiB; **identical size to A1.b canonical** at `cc846444…`)
- **Inner bridge OCI tar**: sha256 `f2d05ad9f380cd82a94fcf9cff20929fb44700cd2c2f264e5753d8ddad6909ac`
- **Bridge OCI manifest digest**: `sha256:e740ddd8f5e50bf0932282f78f9f69c96632231e4d84eb63342add180fd992d6`
- **Build host**: WSL2 distro `alpine-musu-build` (Alpine 3.19) — same canonical build host as A1.a/A1.b per wiki/381 §2 and wiki/383 §6.2
- Pod-spec image digest in `musu-relay/installer/k3s/musu-bridge.yaml:169` matches `manifest.yaml` line 58
- `manifest.yaml` lines 56 + 58 re-pinned to the new bridge OCI tar sha256 + manifest digest

### §6.2 Outer-tar delta vs A1.b canonical

| Quantity | A1.b canonical | A1.c canonical | Delta |
|---|---|---|---|
| Outer-tar sha256 | `cc846444…` | `93b58e27…` | Changed (expected — content delta from hostPort:8070 line + bridge image re-pin) |
| Outer-tar bytes | 397,793,280 | 397,793,280 | 0 bytes (size-neutral; gzip compresses the YAML line addition + manifest re-pin into the same total) |
| Inner `musu-bridge.yaml` sha256 | (A1.b archived copy) | (A1.c archived copy at `F:\workspace\musu-archive\builds\2026-05-17_v23.3-a1c\musu-bridge.yaml`) | One-line diff: hostPort:8070 added per §3.1; Pod-spec image digest re-pinned per §6.3 |
| Inner bridge OCI tar sha256 | `f0c79d96…` | `f2d05ad9…` | Drifted (cache-state issue documented §6.3) |
| Inner bridge OCI manifest digest | `sha256:b484bde2…` | `sha256:e740ddd8…` | Drifted (same cause as above) |

### §6.3 Known issue carry — bridge image hash drift across buildah cache state

A1.c's B6 verification re-surfaced the bridge image hash drift problem first
documented in wiki/383 §7. The drift table now extends:

| Run | Bridge OCI sha256 | Manifest digest |
|---|---|---|
| A1.a-fix archive (2026-05-17 00:57) | `7e807ea2…` | `sha256:ad3097cd…` |
| A1.b Builder Build A (2026-05-17 02:04) | `d73c7bcb…` (drifted) | `sha256:760c1ed2…` |
| A1.b final canonical Build A==B (2026-05-17 02:33) | `f0c79d96…` | `sha256:b484bde2…` |
| **A1.c final canonical Build A==B (2026-05-17 ~12:00)** | **`f2d05ad9…`** | **`sha256:e740ddd8…`** |

**Root cause** (unchanged from wiki/383 §7): `python:3.11-slim-bookworm` base layer's apt metadata + buildah's internal staging cache aren't fully byte-deterministic across cache clears. `musu-bridge/`+`musu-core/` source byte-clean from `3830e0d` (commit HEAD).

**Mitigation in this commit** (same shape as A1.b): bridge image is pinned to the actual baked sha256 at build-time (`musu-bridge.yaml:169` + `manifest.yaml:56` + `manifest.yaml:58` all aligned to the canonical post-Build digest). The **outer** `musu-backend.tar` (the Const VI gate object) is fully byte-identical Build A == Build B intra-hour.

**Deferred to V23.4** (carry from wiki/383): pin `python:3.11-slim-bookworm` to its digest (`sha256:70fc1e698a0ef0f0bfdd63a5370d76c598ee98cb297a5cf30871607e52663c6d`) and add explicit apt-archive-clear in Dockerfile. Not A1.c-blocking; outer-tar reproducibility preserved.

## §7 Const VI bench execution status (operator-time, NOT Builder scope)

**The bench harness is READY but NOT yet run.** The Const VI gate verdict
(PASS/FAIL/AMBIGUOUS per wiki/384 §4.1) is RESERVED for the first operator bench
run. When wiki/385 is updated with actual bench numbers in a future commit, the
§7.1 absolute-fallback table below MUST be embedded verbatim with per-row
PASS/FAIL/N/A filled in. Per Auditor B M3, M5, L1 handoff requirements, the
following three operator-facing artifacts are embedded verbatim below.

### §7.1 §4 absolute-fallback table (embedded verbatim from wiki/384 §4 for the operator)

| Metric | Absolute threshold | Measured | PASS/FAIL/N/A |
|---|---|---|---|
| `/health/ready` p50 (cluster-internal) | ≤ 30ms | `<pending — first operator bench run>` | N/A (bench not yet run) |
| `/health/ready` p99 (cluster-internal) | ≤ 50ms | `<pending>` | N/A |
| RSS idle 60s (Pod) | ≤ 400 MiB AND ≤ 2048Mi hard | `<pending>` | N/A |
| Pod-restart cold-start | ≤ 60s at p95 of 5 trials | `<pending>` | N/A |
| First-boot wall-clock (Windows localhost:8070) | ≤ 90s at p95 of 3 trials | `<pending>` | N/A |
| Rollback time (kubectl scale →0) | ≤ 30s | `<pending>` | N/A |
| WSL2 vEthernet + portmap overhead | ≤ 10ms p99 | `<pending>` | UNMEASURED (F-A1c-10 deferred to V23.4 — no Windows-side bench peer in A1.c) |

When the operator runs the bench:
1. Execute `bridge-bench.sh 3 > /tmp/a1c-bench.json` (the `3` is the RUNS argument; ignored at runtime due to RUNS=3 hard-coded guard from `d460208`, but preserved for forward-compat).
2. Stitch `success_rate`, `health_ready_p50_ms`, `health_ready_p99_ms` from the JSON `aggregate` block into the table above.
3. Run the §7.3 rollback measurement step separately; stitch the result into the "Rollback time" row.
4. Run the RSS sampling loop separately (`kubectl top pod -n musu -l app=musu-bridge` every 5s for 60s; or cgroup-v2 fallback per wiki/384 §5.3); stitch into "RSS idle 60s" row.
5. Run the cold-start trials separately (wiki/384 §5.4 protocol, 5×kubectl-delete cycle); stitch p95 into "Pod-restart cold-start" row.
6. Run the first-boot wall-clock separately (`wsl --unregister musu-test` + `wsl --import` + Windows-side `Invoke-WebRequest http://localhost:8070/health/ready` polling); record p95 of 3 trials into "First-boot wall-clock" row.
7. Update wiki/385 file in-place with filled-in table + verdict declaration per §4.1.

### §7.2 AMBIGUOUS re-run protocol (embedded verbatim from wiki/384 §4.1)

```sh
wsl --unregister musu-test
wsl --import musu-test C:\Temp\musu-test F:\workspace\musu-archive\builds\2026-05-17_v23.3-a1c\musu-backend-a1c-A.tar
# (in distro) wait for K3s + Pod boot
/opt/musu-relay/installer/bridge-bench.sh 3 > /tmp/a1c-rebench.json
# If still 1-of-3 miss → escalate to operator (block-on-user per MODE_Agent_Team.md)
# 2-of-3 miss on re-run = FAIL
```

Per wiki/384 §4.1: "1-miss = PASS with disclosure" is REJECTED. Accepting tail-latency noise as PASS is the false-negative-on-regression mode the Const VI gate exists to prevent.

### §7.3 Rollback time measurement (operator step)

```sh
START=$(date +%s%N)
kubectl -n musu scale deploy/musu-bridge --replicas=0
until ! kubectl -n musu get pods -l app=musu-bridge 2>/dev/null | grep -q musu-bridge; do
  sleep 0.5
done
END=$(date +%s%N)
echo "rollback_ms=$(( (END - START) / 1000000 ))"
# Gate: ≤ 30000 (30s) per §4 row 6
```

After measuring: scale back to `--replicas=1` to restore service for subsequent bench iterations.

## §8 Known gaps + V23.4 forward-pointers

| ID | Gap | V23.4 disposition |
|---|---|---|
| F-A1c-1 | Bench harness reuse for cgroup/runtime change validation | Carry — harness designed reusable for V23.4 hardening experiments (flipping `--snapshotter=native` to `overlayfs`, tuning `MemoryMax`, switching aiohttp ↔ urllib in bench pod) |
| F-A1c-2 | Worker sidecar containerization → `/api/route` becomes bench-eligible | Carry — when worker is containerized as sidecar in V23.4, `/health` becomes bench-eligible and worker-inclusive latency profile becomes recordable |
| F-A1c-3 | Bridge image hash byte-stability across buildah cache (from wiki/383 §7) | Carry — pin `python:3.11-slim-bookworm` to digest `sha256:70fc1e698a0ef0f0bfdd63a5370d76c598ee98cb297a5cf30871607e52663c6d` + explicit apt-archive-clear |
| F-A1c-4 | WSL2 port-forward fallback (if R1 fires) | Carry — add `install-wsl2.ps1` `netsh portproxy` step |
| F-A1c-5 | Bench-tooling image variant | Carry — bake aiohttp + benchmark-helper wheels into `python:3.11-slim-musu-bench` image variant during A1.a |
| F-A1c-6 | `scripts/systemd/musu-bridge.service` deletion (V23.5) | Carry — alongside V21 native `install.ps1` retirement |
| F-A1c-7 | Reproducible-build attestation extension to bench artifacts (V23.5) | Carry — V23.5 in-toto attestation may want bench-pod.yaml + bridge-bench.sh |
| F-A1c-8 | Operator-facing `musu-health` CLI | Carry — subsets the bench harness for ship-time smoke; separate from developer-side bench harness; do NOT bake the full bench harness into outer tar |
| **F-A1c-9** | **bridge-bench.sh emits 2 of 9 §5.6 top-level fields + 6 of 10 aggregate fields** (Auditor B-H1 V23.4 carry) | **Carry — widen to single-command full-schema emit. Add RSS sampling loop (`kubectl top pod` 5s/60s with cgroup-v2 fallback per §5.3), cold-start trials loop (5×kubectl delete + poll), metadata enrichment block (`kubectl version` + `uname -r` + `crictl inspecti` + outer-tar sha256sum stitching). For A1.c ship, operator stitches missing fields into wiki/385 manually each run per §7.1 above. Estimated +80-120 LOC bridge-bench.sh; bench-pod.yaml unchanged.** |
| **F-A1c-10** | **WSL2 vEthernet + portmap DNAT overhead row UNMEASURED** (no Windows-side bench peer in A1.c; Auditor B-M4 V23.4 carry) | **Carry — ship `bench-windows.ps1` peer to `bridge-bench.sh` for combined-overhead measurement. For A1.c ship, WSL2 forward overhead unmeasured; absolute 10ms p99 gate carried as known-gap. Estimated ~60 LOC PowerShell mirroring bench-pod.yaml's Python loop.** |
| (operator-loop) | Operator bench numbers themselves | **wiki/385 to be UPDATED IN-PLACE after first operator bench run with §7.1 table filled + verdict declared per §4.1.** This is not a code change — it's an in-doc edit. |

## §9 Acceptance against wiki/384 §1.3

| # | Acceptance criterion | Result |
|---|---|---|
| (a) | `hostPort: 8070` added to container ports block; manifest YAML parses cleanly; baked into outer tar at unchanged target path | **PASS** — `musu-bridge.yaml:175+177` modification in commit `fc61524`; parse-clean via `kubectl --dry-run=client apply`; tar member at `./var/lib/rancher/k3s/server/manifests/musu-bridge.yaml` confirmed |
| (b) | From Windows host after `wsl --import musu-test <tar>`: `Invoke-WebRequest http://localhost:8070/health/ready` returns HTTP 200 within 60s of first boot | **DEFERRED to operator** — bench harness includes this as a separate smoke step; first boot wall-clock measurement is the §7.1 row 5 |
| (c) | `musu-relay/installer/bridge-bench.sh` is executable + orchestrates the bench protocol | **PASS structurally** — 115 LOC committed with RUNS=3 guard + `success_rate ≥ 99.5%` gate; runtime gate eligibility requires §7.1 absolute-fallback table fill once bench runs |
| (d) | `musu-relay/installer/bench-pod.yaml` is a one-shot Pod manifest with Secret mount + Bearer auth | **PASS** — 176 LOC committed; `secretKeyRef: name=musu-bridge-token, key=MUSU_BRIDGE_TOKEN` post-`0b873c9`; aiohttp/urllib script injects header on every request |
| (e) | Const VI bench numbers recorded in wiki/385 against §4 thresholds; verdict PASS or FAIL declared | **DEFERRED to operator** — §7.1 absolute-fallback table embedded verbatim above; verdict reserved for first bench run |
| (f) | B6 byte-identity preserved: sha256(Build A) == sha256(Build B) intra-hour on post-A1.c outer tar; new canonical hash recorded | **PASS** — `93b58e27…` canonical; size identical to A1.b (397,793,280 bytes); §6.2 delta table records the changes |
| (g) | Dual-audit clearance | **PASS** — both `quality-engineer` Auditors SHIP-OK post-audit-fixes `0b873c9` + `d460208`; all 4 prior Critic HIGHs intact; B-H1 ACCEPTED as F-A1c-9 V23.4 forward-pointer; B-H2 RESOLVED |
| (h) | Const VII feature-branch push gate cleared | **PASS** — 3 commits (`fc61524` + `0b873c9` + `d460208`) pushed on `v22/gap-analysis`; main-merge is operator-gated (V23.3 closure-time, not A1.c-time) |

## §10 Files in this closure

- `docs/V23_3_A1C_PLAN_2026_05_17.md` (wiki/384, detail plan with §11.1-11.5 Critic table + resolution log + delta-review)
- `docs/V23_3_A1C_CLOSURE_2026_05_17.md` (this doc, wiki/385)
- `musu-relay/installer/k3s/musu-bridge.yaml` (line 169 + 175 + 177 = bridge digest re-pin + hostPort:8070 + inline comment)
- `musu-relay/installer/manifest.yaml` (lines 56 + 58 re-pinned to canonical post-build bridge OCI tar sha256 + manifest digest)
- `musu-relay/installer/bridge-bench.sh` (115 LOC bench orchestrator; RUNS=3 hard-coded; success_rate ≥ 99.5% gate-eligibility filter; rate-limit-disable EXIT-trap bracket)
- `musu-relay/installer/bench-pod.yaml` (176 LOC ephemeral Pod with Secret mount + Bearer auth + aiohttp/urllib fallback + percentile-correct stats)
- Build archive: `F:\workspace\musu-archive\builds\2026-05-17_v23.3-a1c\musu-backend-a1c-A.tar` (397,793,280 bytes) + `.sha256` sidecar + `musu-bridge.yaml` (extracted inner copy for diff against A1.b) + `MANIFEST.md` (Const VI evidence record)

## §11 References

- **wiki/377** — V23.2 qualitative evaluation
- **wiki/378** — V23.3 prep / α-β gate-conditional draft
- **wiki/379** — V23.3 master plan (§2 A1.c row "parallel-path cutover"; §3.1 ordering; §4 Const VI A1.c row; §7.1 cutover blast radius)
- **wiki/380** — V23.3 A1.a detail plan (bridge image build pipeline structural template)
- **wiki/381** — V23.3 A1.a closure (bridge image SHIPPED; sha256 `b484bde2…` digest source-of-truth in `manifest.yaml:58` pre-A1.c re-pin)
- **wiki/382** — V23.3 A1.b detail plan (§10 forward-pointers identifying A1.c host-port + bench + rollback responsibilities)
- **wiki/383** — V23.3 A1.b closure (canonical outer tar `cc846444…`; §7 bridge image hash drift V23.4 deferral carried forward by this doc §6.3)
- **wiki/384** — V23.3 A1.c detail plan (this sub-WS's plan; §11.1 Critic table + §11.3 resolution log + §11.4 delta-review + §11.5 H3-propagation scrub)
- **wiki/385** — THIS DOC
- `V23_1_SPIKE_RESULT_TEMPLATE.md` §2 — bench output schema seed (extended in wiki/384 §5.6 with p99 + RSS + cold-start fields)
- `MODE_Agent_Team.md` — dual-audit protocol (§5 trigger; §5.3 conflict resolution; Option-(b) acceptance pattern)
- `F:\workspace\musu-archive\builds\2026-05-17_v23.3-a1c\MANIFEST.md` — Const VI byte-identity evidence record
