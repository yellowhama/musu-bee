{
  "task": "fix: bridge 재시작 후 18회 연속 실패 — semaphore 불안정, 빈 instruction 미검증, orchestrator backoff 없음",
  "issue": "#1d383ff3",
  "scope": [
    "_get_channel_semaphore: asyncio.Semaphore._value 내부 속성 접근 제거 → acquire() try/except 방식으로 교체",
    "DelegateRequest.text: min_length=1 추가로 빈 문자열 400 반환",
    "DelegateRequest.text: strip 후 빈 문자열도 거부 (validator 추가)",
    "musu-control delegate_task: 429/503 응답 시 지수 backoff(1s→2s→4s, 최대 3회) 추가",
    "musu-control route_task: 동일 backoff 적용"
  ],
  "out_of_scope": [
    "DB 스키마 변경",
    "전역 세마포어 재설계 (장기 이슈로 별도 분리)",
    "orchestrator 재시도 횟수 설정 UI",
    "musu-relay 변경",
    "circuit breaker 로직 변경"
  ],
  "acceptance_criteria": [
    "text='' 로 POST /api/tasks/delegate 시 422 반환 (Pydantic validation error)",
    "text='   ' (공백만) 로 POST /api/tasks/delegate 시 422 반환",
    "text='hello' 로 POST /api/tasks/delegate 시 200/202 반환 (정상 경로 유지)",
    "_get_channel_semaphore 가 asyncio.Semaphore._value 를 직접 접근하지 않음 (grep 확인)",
    "channel semaphore capacity 체크가 acquire()/release() 패턴으로 구현됨",
    "musu-control delegate_task 429 응답 시 최소 1회 재시도 후 오류 반환",
    "musu-control delegate_task backoff 간격이 1s → 2s → 4s (지수) 로 증가함",
    "모든 기존 tests/ 테스트가 통과함",
    "새 단위 테스트: test_empty_instruction_rejected, test_whitespace_instruction_rejected, test_delegate_backoff_on_429 통과"
  ],
  "done_definition": "빈 instruction이 422로 거부되고, semaphore 체크가 내부 속성 없이 동작하며, orchestrator가 429 시 backoff 재시도하는 코드가 커밋됨"
}
