[pytest]
addopts = --import-mode=importlib
asyncio_mode = auto
timeout_method = signal
testpaths = musu-bridge/tests musu-core/tests

# Coverage: run with `pytest --cov` to enable
# Default off to avoid slowing regular test runs
